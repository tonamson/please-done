> Status: Unresolved | Feature: Snapshot Tests | Target: test/smoke-snapshot.test.js
> Patch version: 7.0.0 | Fix attempts: 0

# Snapshot tests stale after Phase 71-72 workflow changes

## Description

13 snapshot tests fail after workflows `test.md`, `what-next.md`, and `complete-milestone.md`
were modified in Phases 71-72 to add standalone mode support. The stored snapshots no longer
match the updated workflow content.

## Affected tests

`test/smoke-snapshot.test.js` — Converter snapshot tests:
- `codex snapshots` → `complete-milestone matches snapshot` ✖
- `codex snapshots` → `test matches snapshot` ✖
- `codex snapshots` → `what-next matches snapshot` ✖
- `copilot snapshots` → `complete-milestone matches snapshot` ✖
- `copilot snapshots` → `test matches snapshot` ✖
- `copilot snapshots` → `what-next matches snapshot` ✖
- `gemini snapshots` → `complete-milestone matches snapshot` ✖
- `gemini snapshots` → `test matches snapshot` ✖
- `gemini snapshots` → `what-next matches snapshot` ✖
- `opencode snapshots` → `complete-milestone matches snapshot` ✖
- `opencode snapshots` → `test matches snapshot` ✖
- `opencode snapshots` → `what-next matches snapshot` ✖

`test/smoke-security-rules.test.js` — 1 additional pre-existing failure (unrelated to Phase 71-72)

## Root cause

Snapshot files in `test/` were not regenerated after workflows changed in:
- Phase 71: `workflows/test.md` (added Steps S0-S8 for standalone flow)
- Phase 72: `workflows/what-next.md` (added Priority 5.7) and `workflows/complete-milestone.md` (added standalone bug skip)

## Fix (deferred to next phase)

Run `node test/generate-snapshots.js` to regenerate snapshot files, then commit updated snapshots.

Command: `node test/generate-snapshots.js 2>&1`

## Notes

- Discovered during Phase 73 full suite verification (Task 2)
- Documented per D-06: no fix in Phase 73
- My new `test/smoke-standalone.test.js` (31 tests) passes 100%
- Failures are pre-existing — identical failure count (13) with and without smoke-standalone.test.js
