---
phase: 63-format-standardization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/pd/agents/pd-sec-fixer.md
  - commands/pd/agents/pd-sec-reporter.md
autonomous: true
requirements: [AGNT-03]

must_haves:
  truths:
    - "pd-sec-fixer.md có frontmatter fields: tools, model, maxTurns, effort"
    - "pd-sec-reporter.md có frontmatter fields: tools, model, maxTurns, effort"
    - "Cả 2 file parsable bởi parseAgentFrontmatter() trong smoke test"
  artifacts:
    - path: "commands/pd/agents/pd-sec-fixer.md"
      provides: "Security fixer agent với format mới"
      contains: "model: opus"
    - path: "commands/pd/agents/pd-sec-reporter.md"
      provides: "Security reporter agent với format mới"
      contains: "model: sonnet"
  key_links:
    - from: "commands/pd/agents/pd-sec-fixer.md"
      to: "bin/lib/resource-config.js"
      via: "AGENT_REGISTRY tier: architect → TIER_MAP model: opus"
      pattern: "model: opus"
    - from: "commands/pd/agents/pd-sec-reporter.md"
      to: "bin/lib/resource-config.js"
      via: "AGENT_REGISTRY tier: builder → TIER_MAP model: sonnet"
      pattern: "model: sonnet"
---

<objective>
Convert pd-sec-fixer.md and pd-sec-reporter.md từ legacy frontmatter (tier + allowed-tools array) sang format mới (tools comma-separated, model, maxTurns, effort).

Purpose: Đảm bảo tất cả 16 agents dùng cùng format YAML frontmatter, parsable bởi smoke test.
Output: 2 agent files cập nhật, smoke test pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-symlink-architecture/61-01-SUMMARY.md

@commands/pd/agents/pd-sec-fixer.md
@commands/pd/agents/pd-sec-reporter.md
@commands/pd/agents/pd-bug-janitor.md
@bin/lib/resource-config.js
</context>

<interfaces>
<!-- TIER_MAP mapping từ resource-config.js -->
From bin/lib/resource-config.js:
```javascript
const TIER_MAP = {
  scout: { model: "haiku", effort: "low", maxTurns: 15, tokenBudget: 4000 },
  builder: { model: "sonnet", effort: "medium", maxTurns: 25, tokenBudget: 8000 },
  architect: { model: "opus", effort: "high", maxTurns: 30, tokenBudget: 12000 },
};
```

New frontmatter format example (pd-bug-janitor.md):
```yaml
---
name: pd-bug-janitor
description: ...
tools: Read, Glob, Grep, AskUserQuestion, Bash
model: haiku
maxTurns: 15
effort: low
---
```

AGENT_REGISTRY entries:
```javascript
"pd-sec-fixer": { tier: "architect", tools: ["Read", "Write", "Glob", "Grep"] },
"pd-sec-reporter": { tier: "builder", tools: ["Read", "Write", "Glob"] },
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Convert frontmatter của 2 security agents</name>
  <files>commands/pd/agents/pd-sec-fixer.md, commands/pd/agents/pd-sec-reporter.md</files>
  <action>
    **File 1: `commands/pd/agents/pd-sec-fixer.md`** (tier: architect)
    Thay thế frontmatter hiện tại:
    ```yaml
    ---
    name: pd-sec-fixer
    description: Phan tich findings bao mat va tao de xuat fix phases decimal theo thu tu uu tien.
    tier: architect
    allowed-tools:
      - Read
      - Write
      - Glob
      - Grep
    ---
    ```
    Bằng format mới:
    ```yaml
    ---
    name: pd-sec-fixer
    description: Phan tich findings bao mat va tao de xuat fix phases decimal theo thu tu uu tien.
    tools: Read, Write, Glob, Grep
    model: opus
    maxTurns: 30
    effort: high
    ---
    ```
    - Xóa `tier` và `allowed-tools` (YAML array)
    - Thêm `tools` (comma-separated), `model: opus`, `maxTurns: 30`, `effort: high` (từ TIER_MAP.architect)
    - GIỮ NGUYÊN toàn bộ body content (objective, process, rules) — KHÔNG thay đổi

    **File 2: `commands/pd/agents/pd-sec-reporter.md`** (tier: builder)
    Thay thế frontmatter hiện tại:
    ```yaml
    ---
    name: pd-sec-reporter
    description: Tong hop vien bao mat — Gop ket qua tu N scanner agent thanh bao cao bao mat tong the, phu du OWASP Top 10.
    tier: builder
    allowed-tools:
      - Read
      - Write
      - Glob
    ---
    ```
    Bằng format mới:
    ```yaml
    ---
    name: pd-sec-reporter
    description: Tong hop vien bao mat — Gop ket qua tu N scanner agent thanh bao cao bao mat tong the, phu du OWASP Top 10.
    tools: Read, Write, Glob
    model: sonnet
    maxTurns: 25
    effort: medium
    ---
    ```
    - Xóa `tier` và `allowed-tools` (YAML array)
    - Thêm `tools` (comma-separated), `model: sonnet`, `maxTurns: 25`, `effort: medium` (từ TIER_MAP.builder)
    - GIỮ NGUYÊN toàn bộ body content — KHÔNG thay đổi
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && grep -c 'model: opus' commands/pd/agents/pd-sec-fixer.md && grep -c 'model: sonnet' commands/pd/agents/pd-sec-reporter.md && ! grep 'allowed-tools' commands/pd/agents/pd-sec-fixer.md && ! grep 'allowed-tools' commands/pd/agents/pd-sec-reporter.md && ! grep '^tier:' commands/pd/agents/pd-sec-fixer.md && ! grep '^tier:' commands/pd/agents/pd-sec-reporter.md && echo "PASS"</automated>
  </verify>
  <done>pd-sec-fixer.md có model/maxTurns/effort/tools, pd-sec-reporter.md có model/maxTurns/effort/tools, không còn tier hoặc allowed-tools</done>
</task>

<task type="auto">
  <name>Task 2: Validate parsability + smoke test</name>
  <files>test/smoke-agent-files.test.js</files>
  <action>
    Chạy smoke test để xác nhận cả 2 security agents parsable bởi parseAgentFrontmatter():
    - `node --test test/smoke-agent-files.test.js` — tất cả 26 tests phải pass
    - Kiểm tra cụ thể: parse frontmatter của pd-sec-fixer và pd-sec-reporter qua node -e script:
      ```bash
      node -e "
        const fs = require('fs');
        const path = require('path');
        for (const name of ['pd-sec-fixer', 'pd-sec-reporter']) {
          const f = fs.readFileSync(path.join('commands/pd/agents', name + '.md'), 'utf8');
          const m = f.match(/tools: (.+)/); const mo = f.match(/model: (.+)/);
          const mt = f.match(/maxTurns: (.+)/); const e = f.match(/effort: (.+)/);
          console.log(name + ': tools=' + (m?.[1]||'MISSING') + ' model=' + (mo?.[1]||'MISSING') + ' maxTurns=' + (mt?.[1]||'MISSING') + ' effort=' + (e?.[1]||'MISSING'));
        }
      "
      ```
    KHÔNG sửa file test — chỉ chạy validation.
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && node --test test/smoke-agent-files.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>26/26 tests pass, cả 2 security agents parsable với đủ 4 fields mới</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-agent-files.test.js` — 26 pass, 0 fail
- `grep -c 'model:' commands/pd/agents/pd-sec-{fixer,reporter}.md` — mỗi file 1 hit
- `grep -c 'allowed-tools' commands/pd/agents/pd-sec-{fixer,reporter}.md` — 0 hits
</verification>

<success_criteria>
- pd-sec-fixer.md: tools, model=opus, maxTurns=30, effort=high
- pd-sec-reporter.md: tools, model=sonnet, maxTurns=25, effort=medium
- Smoke test 26/26 pass
- Không còn legacy format (tier, allowed-tools) trong 2 files
</success_criteria>

<output>
After completion, create `.planning/phases/63-format-standardization/63-01-SUMMARY.md`
</output>
