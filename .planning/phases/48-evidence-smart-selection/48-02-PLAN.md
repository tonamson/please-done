---
phase: 48-evidence-smart-selection
plan: 02
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - commands/pd/agents/pd-sec-scanner.md
  - commands/pd/agents/pd-sec-reporter.md
  - workflows/audit.md
autonomous: true
requirements:
  - EVID-01
  - EVID-02
  - AGENT-03

must_haves:
  truths:
    - "Scanner template co section Function Checklist voi 4 verdicts PASS/FLAG/FAIL/SKIP"
    - "Reporter tao master table sap theo severity roi OWASP"
    - "Reporter tao 2 bang hot spots: top 5 files + top 5 functions"
    - "Reporter merge cung 1 function tu nhieu scanner thanh 1 dong"
    - "Reporter doc evidence files bang Glob (khong hardcode 13 files)"
    - "Workflow B4 goi selectScanners() thay vi stub, --full va --only bypass B4"
  artifacts:
    - path: "commands/pd/agents/pd-sec-scanner.md"
      provides: "Scanner template voi Function Checklist output"
      contains: "## Function Checklist"
    - path: "commands/pd/agents/pd-sec-reporter.md"
      provides: "Reporter template voi master table + hot spots + function merge"
      contains: "## Master Table"
    - path: "workflows/audit.md"
      provides: "Workflow B4 thay stub bang selectScanners() logic"
      contains: "selectScanners"
  key_links:
    - from: "workflows/audit.md"
      to: "bin/lib/smart-selection.js"
      via: "B4 require va goi selectScanners()"
      pattern: "selectScanners"
    - from: "commands/pd/agents/pd-sec-reporter.md"
      to: "evidence files"
      via: "Glob evidence_sec_*.md"
      pattern: "evidence_sec_"
---

<objective>
Cap nhat scanner template (function checklist), reporter template (master table + hot spots + function merge), va thay stub B4 trong workflow bang selectScanners() logic.

Purpose: Hoan thanh 3 integration points con lai: scanner xuat evidence chi tiet tung ham, reporter tong hop thong minh, va workflow chon scanner tu dong.
Output: 3 files sua: pd-sec-scanner.md, pd-sec-reporter.md, workflows/audit.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-evidence-smart-selection/48-CONTEXT.md
@.planning/phases/48-evidence-smart-selection/48-RESEARCH.md
@.planning/phases/48-evidence-smart-selection/48-01-SUMMARY.md

<interfaces>
<!-- Tu Plan 01 -->

From bin/lib/smart-selection.js (created in Plan 01):
```javascript
const BASE_SCANNERS = ['secrets', 'misconfig', 'logging'];
const ALL_CATEGORIES = [ /* 13 slugs */ ];
const SIGNAL_MAP = [ /* 12 signals */ ];

/**
 * @param {{ deps: string[], fileExtensions: string[], codePatterns: string[], hasLockfile: boolean }} projectContext
 * @returns {{ selected: string[], skipped: string[], signals: Signal[], lowConfidence: boolean }}
 */
function selectScanners(projectContext) { ... }

module.exports = { selectScanners, BASE_SCANNERS, ALL_CATEGORIES, SIGNAL_MAP };
```

From bin/lib/parallel-dispatch.js:
```javascript
function buildScannerPlan(categories, batchSize = 2, scanPath = '.') { ... }
// Returns: { waves, totalWaves, totalScanners, warnings }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bo sung Function Checklist vao pd-sec-scanner.md</name>
  <files>commands/pd/agents/pd-sec-scanner.md</files>
  <read_first>
    - commands/pd/agents/pd-sec-scanner.md (noi dung hien tai — PHAI doc truoc khi sua)
    - .planning/phases/48-evidence-smart-selection/48-RESEARCH.md (Pattern 3: Function Checklist Format)
    - references/security-rules.yaml (xem fastcode_queries[] format)
  </read_first>
  <action>
    Sua commands/pd/agents/pd-sec-scanner.md — THEM section Function Checklist vao process, GIU NGUYEN tat ca noi dung cu (backward compatible, per D-07).

    Thay doi cu the:

    1. Trong section `<process>`, SAU buoc 7 (Phan tich ket qua) va TRUOC buoc 8 (Ghi evidence file), THEM buoc moi:

    ```
    8. **Tao Function Checklist.** Sau khi phan tich tat ca ket qua, tao bang kiem tra tung ham:
       - Voi moi ham da phat hien tu FastCode/Grep, gan verdict:
         * **PASS** — Ham an toan voi category dang quet
         * **FLAG** — Nghi ngo, can review them
         * **FAIL** — Lo hong xac nhan
         * **SKIP** — Ham khong lien quan den category dang quet, ghi kem ly do ngan
       - Bao gom CA cac ham da quet, ke ca ham an toan (PASS) va khong lien quan (SKIP)
       - Format bang:
         | # | File | Ham | Dong | Verdict | Chi tiet |
         |---|------|-----|------|---------|----------|
         | 1 | src/api/users.js | getUserById | 42 | FLAG | IDOR — params.id khong kiem tra ownership |
    ```

    2. Trong buoc 8 (doi thanh buoc 9 — Ghi evidence file), THEM section `## Function Checklist` vao Markdown body:
       Sua phan mo ta body tu:
       ```
       - `## Tom tat` — ...
       - `## Phat hien` — ...
       - `## Chi tiet` — ...
       ```
       Thanh:
       ```
       - `## Tom tat` — ...
       - `## Phat hien` — ...
       - `## Chi tiet` — ...
       - `## Function Checklist` — Bang kiem tra tung ham voi verdict PASS/FLAG/FAIL/SKIP (per D-07, D-08). SKIP phai ghi kem ly do ngan.
       ```

    3. Trong section `<rules>`, THEM rule moi:
       ```
       - Moi ham da quet PHAI xuat hien trong Function Checklist — ke ca PASS va SKIP.
       - SKIP phai ghi ly do ngan (vi du: "Khong lien quan den auth category").
       ```

    KHONG thay doi: frontmatter, objective, buoc 1-7 hien tai, cac rules hien tai.
  </action>
  <verify>
    <automated>grep -c "Function Checklist" commands/pd/agents/pd-sec-scanner.md</automated>
  </verify>
  <acceptance_criteria>
    - pd-sec-scanner.md chua string "## Function Checklist"
    - pd-sec-scanner.md chua ca 4 verdicts: "PASS", "FLAG", "FAIL", "SKIP"
    - pd-sec-scanner.md van chua cac section cu: "## Tom tat", "## Phat hien", "## Chi tiet" (backward compatible)
    - pd-sec-scanner.md van co frontmatter voi `name: pd-sec-scanner` va `tier: scout`
    - pd-sec-scanner.md chua "ly do ngan" hoac "lý do ngắn" (SKIP phai ghi ly do)
  </acceptance_criteria>
  <done>
    pd-sec-scanner.md co buoc tao Function Checklist va output section `## Function Checklist` trong evidence format. Backward compatible — khong mat sections cu.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mo rong pd-sec-reporter.md voi master table + hot spots + function merge</name>
  <files>commands/pd/agents/pd-sec-reporter.md</files>
  <read_first>
    - commands/pd/agents/pd-sec-reporter.md (noi dung hien tai — PHAI doc truoc khi sua)
    - .planning/phases/48-evidence-smart-selection/48-RESEARCH.md (Pattern 4: Master Table, Pattern 5: Function Merge)
    - .planning/phases/48-evidence-smart-selection/48-CONTEXT.md (D-10, D-11, D-12)
  </read_first>
  <action>
    Sua commands/pd/agents/pd-sec-reporter.md — THAY THE process va report format hien tai voi logic moi.

    Thay doi cu the:

    1. Trong `<objective>`, sua dong "Tong hop bao cao tu 13 agent scanner" thanh:
       "Tong hop bao cao tu N scanner (co the < 13 neu smart selection hoat dong). Doc evidence files bang Glob, khong hardcode danh sach."

    2. Bo danh sach "13 scanner" hardcode trong objective (thay bang note chung).

    3. Trong `<process>`, THAY buoc 1 tu hardcode 13 files thanh:
       ```
       1. Doc tat ca evidence files tu session dir bang Glob:
          - Glob pattern: `{session_dir}/03-dispatch/evidence_sec_*.md`
          - KHONG hardcode 13 file names — so luong evidence tuy thuoc smart selection
          - Neu khong tim thay evidence file nao: bao loi va dung lai
       ```

    4. THEM buoc moi SAU buoc 3 (Map OWASP), TRUOC buoc 4 (Tong hop):

       ```
       3.5. **Parse Function Checklist tu moi evidence file.**
          - Tim section `## Function Checklist` trong moi evidence file
          - Neu evidence file khong co section nay (scanner cu): bo qua, chi xu ly sections cu
          - Parse bang thanh array: [{file, function, line, verdict, detail, category}]
          - Merge key = "file_path::function_name" (string concat, khong dung hash — per D-12)
          - Merge verdict rule: FAIL + bat ky = FAIL, FLAG + PASS = FLAG, SKIP + bat ky khac = giu verdict khac
          - Ket qua: functionMap voi moi entry co mergedVerdict va findings[] tu nhieu categories
       ```

    5. THAY report format trong buoc 7. Report SECURITY_REPORT.md gom:
       - Frontmatter: agent, timestamp, session (giu nguyen)
       - `# Bao cao bao mat` (giu nguyen)
       - `## Tong quan` — Thay "Scanner hoan tat: .../13" thanh "Scanner hoan tat: {completed}/{total evidence files found}"
       - `## OWASP Top 10 Coverage` (giu nguyen)
       - `## Master Table` — THAY "Bang phat hien tong hop":
         ```
         | # | Severity | OWASP | Category | File | Ham | Dong | Verdict | Mo ta |
         ```
         Sort: Severity (CRITICAL > HIGH > MEDIUM > LOW), cung severity sort OWASP (A01 > A10) — per D-10
       - `## Hot Spots` — THEM 2 bang rieng (per D-11):
         ```
         ### Top 5 files co nhieu finding nhat
         | # | File | FAIL | FLAG | Total | Categories |

         ### Top 5 functions nguy hiem nhat
         | # | File | Ham | FAIL | FLAG | Categories |
         ```
         Chi hien thi top 5 (hoac it hon neu < 5 findings)
       - `## Attack Chains` (giu nguyen)
       - `## Ke hoach remediation` (giu nguyen P0/P1/P2 structure)
       - `## Chi tiet theo loai` — THAY hardcode 13 sections thanh dynamic:
         ```
         Voi moi evidence file tim duoc (tu Glob), tao 1 section:
         ### {N}. {category_name} ({owasp_code})
         (trich tu evidence file tuong ung)
         ```

    6. Trong `<rules>`, THEM rules:
       ```
       - Doc evidence files bang Glob pattern evidence_sec_*.md — KHONG hardcode 13 ten file.
       - Master table sap theo severity TRUOC (CRITICAL > HIGH > MEDIUM > LOW), cung severity sort OWASP (A01 > A10).
       - Hot spots chi hien top 5. Neu < 5 findings, hien tat ca.
       - Function merge key = "file_path::function_name". FAIL > FLAG > PASS khi merge.
       ```
  </action>
  <verify>
    <automated>grep -c "Master Table\|Hot Spots\|evidence_sec_\*\|Glob" commands/pd/agents/pd-sec-reporter.md</automated>
  </verify>
  <acceptance_criteria>
    - pd-sec-reporter.md chua "## Master Table" (thay cho "Bang phat hien tong hop")
    - pd-sec-reporter.md chua "## Hot Spots"
    - pd-sec-reporter.md chua "Top 5 files" va "Top 5 functions"
    - pd-sec-reporter.md chua "evidence_sec_*.md" hoac "evidence_sec_*" (Glob pattern, khong hardcode)
    - pd-sec-reporter.md KHONG con hardcode "evidence_sec_sqli.md", "evidence_sec_xss.md" (13 file rieng le)
    - pd-sec-reporter.md chua "file_path::function_name" hoac "merge key" (function merge logic)
    - pd-sec-reporter.md chua "Severity" va "OWASP" trong context sort (D-10)
    - pd-sec-reporter.md van co frontmatter `name: pd-sec-reporter` va `tier: builder`
  </acceptance_criteria>
  <done>
    pd-sec-reporter.md doc evidence bang Glob, tao master table sap theo severity+OWASP, 2 bang hot spots top 5, merge function outcomes tu nhieu scanner. Dynamic — khong hardcode 13 files.
  </done>
</task>

<task type="auto">
  <name>Task 3: Thay stub B4 trong workflows/audit.md bang selectScanners() logic</name>
  <files>workflows/audit.md</files>
  <read_first>
    - workflows/audit.md (noi dung hien tai — PHAI doc truoc khi sua)
    - .planning/phases/48-evidence-smart-selection/48-RESEARCH.md (Pattern 2: selectScanners Interface, Workflow B4 Replacement, Pattern 6: User Confirm Prompt)
    - .planning/phases/48-evidence-smart-selection/48-CONTEXT.md (D-05, D-06, D-13, D-14, D-15)
    - bin/lib/smart-selection.js (vua tao tu Plan 01 — xem exports)
  </read_first>
  <action>
    Sua workflows/audit.md — THAY Buoc 4 stub bang logic selectScanners() thuc.
    Dong thoi cap nhat Buoc 3 de xu ly --only them 3 base scanners.

    Thay doi cu the:

    1. **Buoc 3: Scope / parse args** — Sua phan "Xac dinh categories_to_scan":
       Tu:
       ```
       - Khong co flag → mac dinh --full (13 categories)
       - --full → 13 categories
       - --only cat1,cat2 → validate moi slug...
       ```
       Thanh:
       ```
       - --full → 13 categories, SKIP Buoc 4
       - --only cat1,cat2 → validate slugs + THEM 3 base (secrets, misconfig, logging) + de-dup, SKIP Buoc 4 (per D-06, D-15)
       - Khong co flag → chuyen qua Buoc 4 Smart Selection
       ```

    2. **Buoc 4: Smart selection** — THAY TOAN BO noi dung stub bang:
       ```
       ## Buoc 4: Smart selection

       Neu --full hoac --only: SKIP buoc nay (da co categories tu B3).

       Khong co flag → chay smart selection:

       1. Thu thap project context (toan bo buoc nay KHONG spawn AI — chi Bash/Glob/Grep):
          a. Doc package.json (neu ton tai) → lay deps:
             ```bash
             node -e "try{const p=JSON.parse(require('fs').readFileSync('package.json','utf8'));console.log(JSON.stringify([...Object.keys(p.dependencies||{}),...Object.keys(p.devDependencies||{})]))}catch(e){console.log('[]')}"
             ```
          b. Doc requirements.txt (neu ton tai) → lay Python deps:
             ```bash
             grep -v '^#' requirements.txt 2>/dev/null | sed 's/[>=<].*//' | tr -d ' ' || echo ""
             ```
          c. Glob file extensions: kiem tra ton tai cua *.jsx, *.tsx, *.vue, *.svelte, *.php, *.ejs, *.pug, *.hbs
          d. Grep code patterns (3-4 lenh gom nhom):
             - `grep -rl "req\.\(body\|params\|query\)" --include="*.js" --include="*.ts" . 2>/dev/null | head -1`
             - `grep -rl "child_process\|exec(\|spawn(" --include="*.js" --include="*.ts" . 2>/dev/null | head -1`
             - `grep -rl "createHash\|createCipher\|jwt\.sign" --include="*.js" --include="*.ts" . 2>/dev/null | head -1`
             - `grep -rl "app\.\(get\|post\|put\|delete\)(\|router\.\(get\|post\)" --include="*.js" --include="*.ts" . 2>/dev/null | head -1`
          e. Kiem tra lockfile: test -f package-lock.json || test -f yarn.lock || test -f pnpm-lock.yaml || test -f requirements.txt

       2. Goi selectScanners():
          ```bash
          node -e "
            const {selectScanners}=require('./bin/lib/smart-selection');
            const ctx={
              deps: $DEPS_JSON,
              fileExtensions: $EXTENSIONS_JSON,
              codePatterns: $CODE_PATTERNS_JSON,
              hasLockfile: $HAS_LOCKFILE
            };
            console.log(JSON.stringify(selectScanners(ctx)));
          "
          ```

       3. Xu ly ket qua:
          a. Neu lowConfidence=false:
             - Log: "Smart Selection: {selected.length}/{13} scanners, {skipped.length} bo qua"
             - Log tung signal: "  - {signal.id}: {signal.description}"
             - Dung selected lam categories_to_scan

          b. Neu lowConfidence=true (< 2 signals — per D-05):
             - Hien thi prompt:
               ```
               Smart Selection ket qua:
                 Tin hieu tim duoc: {signals.length}/12
                 {Liet ke tung signal}

                 Scanner se chay ({selected.length}):
                 {Liet ke selected, danh dau base}

                 Scanner bo qua ({skipped.length}): {liet ke skipped}

                 [1] Chay {selected.length} scanner da chon
                 [2] Chay --full (13 scanner)

               Chon (1/2):
               ```
             - Neu user chon 1: dung selected
             - Neu user chon 2: dung ALL_CATEGORIES (13)
             - Neu khong co interactive (khong the hoi user): default chay selected + log warning "Khong the hoi user — chay {selected.length} scanner da chon"

       4. Ghi {session_dir}/03-selection.md voi:
          - status: completed (thay vi stub)
          - signals: [{id, description, categories}]
          - selected_categories: [...]
          - skipped_categories: [...]
          - lowConfidence: true/false
          - user_choice: (neu lowConfidence=true)
       ```

    3. **Buoc 5: Dispatch** — Sua dong "Lay categories tu B3/B4" thanh:
       "Lay categories_to_scan tu B3 (--full/--only) hoac B4 (smart selection)"

    4. KHONG thay doi: Buoc 1, Buoc 2, Buoc 6-9, rules section.

    5. Trong `<purpose>`, THEM tham chieu: `bin/lib/smart-selection.js (selectScanners)` ben canh parallel-dispatch.js
  </action>
  <verify>
    <automated>grep -c "selectScanners\|smart-selection\|lowConfidence\|03-selection.md" workflows/audit.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/audit.md chua "selectScanners" (it nhat 2 lan — import va goi)
    - workflows/audit.md chua "smart-selection" (reference module)
    - workflows/audit.md chua "lowConfidence" (xu ly < 2 signals)
    - workflows/audit.md chua "03-selection.md" voi "status: completed" (thay vi stub)
    - workflows/audit.md KHONG con "STUB" trong Buoc 4 (da thay bang logic thuc)
    - workflows/audit.md Buoc 3 chua "--only" voi "3 base" hoac "secrets, misconfig, logging" (per D-15)
    - workflows/audit.md Buoc 4 chua "Khong spawn AI" hoac tuong duong (per D-04, chi Bash/Glob/Grep)
    - workflows/audit.md van co 9 buoc (B1-B9)
  </acceptance_criteria>
  <done>
    Workflow B4 thay stub bang selectScanners() logic thuc. --full va --only bypass B4. < 2 signals hoi user confirm. B3 xu ly --only them 3 base.
  </done>
</task>

</tasks>

<verification>
- `grep "STUB" workflows/audit.md` — chi con B2 (delta) va B8 (fix routing) la STUB, B4 KHONG con STUB
- `grep "Function Checklist" commands/pd/agents/pd-sec-scanner.md` — co it nhat 2 ket qua
- `grep "Master Table" commands/pd/agents/pd-sec-reporter.md` — co it nhat 1 ket qua
- `grep "Hot Spots" commands/pd/agents/pd-sec-reporter.md` — co it nhat 1 ket qua
- `grep "selectScanners" workflows/audit.md` — co it nhat 2 ket qua
- `grep "evidence_sec_\*" commands/pd/agents/pd-sec-reporter.md` — co (Glob, khong hardcode)
</verification>

<success_criteria>
- Scanner template xuat Function Checklist voi 4 verdicts (PASS/FLAG/FAIL/SKIP) cho tung ham
- Reporter doc evidence bang Glob, tao master table sort severity+OWASP, 2 hot spots tables, merge function outcomes
- Workflow B4 goi selectScanners(), --full/--only bypass, < 2 signals prompt user
- Backward compatible — khong mat chuc nang cu
</success_criteria>

<output>
After completion, create `.planning/phases/48-evidence-smart-selection/48-02-SUMMARY.md`
</output>
