---
phase: 48-evidence-smart-selection
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/smart-selection.js
  - test/smoke-smart-selection.test.js
autonomous: true
requirements:
  - SMART-01
  - SMART-02
  - SMART-03

must_haves:
  truths:
    - "selectScanners() tra ve selected[] bao gom 3 base (secrets, misconfig, logging) luon co mat"
    - "12 signals mapping dung 10 scanner co dieu kien tu projectContext"
    - "lowConfidence=true khi < 2 signal match"
    - "De-dup: 1 scanner chi xuat hien 1 lan trong selected du nhieu signal kich hoat"
  artifacts:
    - path: "bin/lib/smart-selection.js"
      provides: "selectScanners() pure function"
      exports: ["selectScanners", "BASE_SCANNERS", "ALL_CATEGORIES", "SIGNAL_MAP"]
    - path: "test/smoke-smart-selection.test.js"
      provides: "Unit tests cho selectScanners()"
      min_lines: 80
  key_links:
    - from: "bin/lib/smart-selection.js"
      to: "workflows/audit.md"
      via: "B4 goi selectScanners(projectContext)"
      pattern: "selectScanners"
---

<objective>
Tao smart-selection.js voi pure function selectScanners() va test suite day du.

Purpose: Cung cap engine chon scanner lien quan dua tren project context (deps, file extensions, code patterns, lockfile). Giam 40-60% scanners khi project chi dung 1 stack.
Output: bin/lib/smart-selection.js (production) + test/smoke-smart-selection.test.js (tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-evidence-smart-selection/48-CONTEXT.md
@.planning/phases/48-evidence-smart-selection/48-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From bin/lib/parallel-dispatch.js:
```javascript
// buildScannerPlan nhan categories[] truc tiep — selectScanners().selected se truyen vao day
function buildScannerPlan(categories, batchSize = 2, scanPath = '.') { ... }
// Returns: { waves, totalWaves, totalScanners, warnings }
```

From bin/lib/resource-config.js:
```javascript
// getAgentConfig('pd-sec-scanner').categories tra ve 13 slugs
function getAgentConfig(agentName) { ... }
```

Test pattern tu test/smoke-parallel-dispatch.test.js:
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
```
</interfaces>
</context>

<feature>
  <name>selectScanners() pure function</name>
  <files>bin/lib/smart-selection.js, test/smoke-smart-selection.test.js</files>
  <behavior>
    - selectScanners({}) tra ve { selected: ['secrets','misconfig','logging'], skipped: [...10 con lai], signals: [], lowConfidence: true }
    - selectScanners({ deps: ['express'] }) kich hoat sig-web-framework → selected chua xss, auth, prototype-pollution + 3 base
    - selectScanners({ deps: ['sequelize'], codePatterns: ['req.body'] }) → lowConfidence=false (2+ signals)
    - selectScanners({ deps: ['express', 'sequelize', 'bcrypt'], hasLockfile: true }) → nhieu signal, selected chua nhieu categories, de-dup
    - selectScanners({ fileExtensions: ['.tsx'] }) kich hoat sig-frontend-code → selected chua xss + 3 base
    - selectScanners({ hasLockfile: true }) kich hoat sig-deps-lockfile → selected chua vuln-deps + 3 base
    - selected + skipped = ALL_CATEGORIES (13 phan tu, khong mat khong thua)
    - signals[] moi item co id, description (evidence), categories[]
  </behavior>
  <implementation>
    Tao bin/lib/smart-selection.js voi:
    - BASE_SCANNERS = ['secrets', 'misconfig', 'logging'] (per D-01)
    - ALL_CATEGORIES = 13 slugs (per AGENT-04)
    - SIGNAL_MAP = 12 signals voi deps, pyDeps, codePatterns, filePatterns, lockfiles, categories (per D-04, bang tu RESEARCH.md)
    - selectScanners(projectContext) pure function (per D-03):
      * Input: { deps[], fileExtensions[], codePatterns[], hasLockfile }
      * Duyet 12 signals, check tung loai match
      * De-dup selected = [...new Set([...BASE_SCANNERS, ...categoriesFromSignals])]
      * lowConfidence = signals.length < 2
      * Return: { selected, skipped, signals, lowConfidence }
    - module.exports = { selectScanners, BASE_SCANNERS, ALL_CATEGORIES, SIGNAL_MAP }
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — Test + Implementation selectScanners()</name>
  <files>bin/lib/smart-selection.js, test/smoke-smart-selection.test.js</files>
  <read_first>
    - bin/lib/parallel-dispatch.js (xem pattern pure function, exports)
    - test/smoke-parallel-dispatch.test.js (xem pattern test voi node:test)
    - .planning/phases/48-evidence-smart-selection/48-RESEARCH.md (xem SIGNAL_MAP chi tiet, code example)
    - references/security-rules.yaml (xem 13 category slugs chinh xac)
  </read_first>
  <behavior>
    - Test 1: Empty context → selected = 3 base only, lowConfidence = true, signals = []
    - Test 2: deps=['express'] → sig-web-framework match, selected chua xss + auth + prototype-pollution + 3 base = 6 scanners
    - Test 3: deps=['sequelize'], codePatterns=['req.body'] → 2 signals, lowConfidence = false
    - Test 4: deps=['express','sequelize','bcrypt'], hasLockfile=true → nhieu signal, de-dup chinh xac
    - Test 5: fileExtensions=['.tsx'] → sig-frontend-code match → xss + 3 base = 4 scanners
    - Test 6: hasLockfile=true → sig-deps-lockfile → vuln-deps + 3 base = 4 scanners
    - Test 7: selected.length + skipped.length === 13 (ALL_CATEGORIES invariant)
    - Test 8: Moi signal co id (string), description (string), categories (string[])
    - Test 9: codePatterns=['child_process'] → sig-cmd-exec → cmd-injection + 3 base
    - Test 10: deps=['node-serialize'] → sig-deserialize → deserialization + 3 base
    - Test 11: BASE_SCANNERS export la ['secrets','misconfig','logging']
    - Test 12: ALL_CATEGORIES export la 13 slugs dung
  </behavior>
  <action>
    RED: Tao test/smoke-smart-selection.test.js voi node:test framework (nhu smoke-parallel-dispatch.test.js).
    12 test cases nhu behavior tren. Import tu ../bin/lib/smart-selection.
    Chay: `node --test test/smoke-smart-selection.test.js` — PHAI FAIL (module chua ton tai).

    GREEN: Tao bin/lib/smart-selection.js voi:
    1. `BASE_SCANNERS = ['secrets', 'misconfig', 'logging']` (per D-01)
    2. `ALL_CATEGORIES` = 13 slugs chinh xac tu security-rules.yaml:
       `['sql-injection', 'xss', 'cmd-injection', 'path-traversal', 'secrets', 'auth', 'deserialization', 'misconfig', 'prototype-pollution', 'crypto', 'insecure-design', 'vuln-deps', 'logging']`
    3. `SIGNAL_MAP` = 12 signals tu RESEARCH.md Pattern 1 (copy nguyen van cau truc):
       - sig-sql-deps: deps=['sequelize','knex','prisma','@prisma/client','typeorm','pg','mysql2','mongodb','mongoose','better-sqlite3'], pyDeps=['sqlalchemy','django','psycopg2','pymongo'], categories=['sql-injection']
       - sig-web-framework: deps=['express','koa','fastify','@hapi/hapi','@nestjs/core','next','nuxt'], pyDeps=['flask','django','fastapi','tornado'], filePatterns=['.php'], categories=['xss','auth','prototype-pollution']
       - sig-cmd-exec: codePatterns=['child_process','exec(','spawn(','subprocess'], categories=['cmd-injection']
       - sig-file-io: codePatterns=['fs.readFile','fs.createReadStream','path.join(.*req','sendFile('], categories=['path-traversal']
       - sig-deserialize: deps=['node-serialize','js-yaml','serialize-javascript'], pyDeps=['pyyaml'], codePatterns=['pickle.load','unserialize(','yaml.load('], categories=['deserialization']
       - sig-crypto-usage: deps=['bcrypt','argon2','jsonwebtoken','jose','crypto-js'], pyDeps=['cryptography','pyjwt','passlib'], codePatterns=['createHash(','createCipher','jwt.sign'], categories=['crypto']
       - sig-template-engine: deps=['ejs','pug','handlebars','nunjucks','mustache','eta'], pyDeps=['jinja2','mako'], codePatterns=['render_template_string'], filePatterns=['.ejs','.pug','.hbs'], categories=['deserialization']
       - sig-deps-lockfile: lockfiles=['package-lock.json','yarn.lock','pnpm-lock.yaml','requirements.txt','Pipfile.lock','Gemfile.lock'], categories=['vuln-deps']
       - sig-api-endpoints: codePatterns=['app.(get|post|put|delete)(','router.(get|post|put|delete)(','@(Get|Post|Put|Delete)('], categories=['auth','insecure-design']
       - sig-financial: deps=['stripe','paypal','braintree'], pyDeps=['stripe','paypalrestsdk'], codePatterns=['payment','charge','transfer','balance','withdraw'], categories=['insecure-design']
       - sig-user-input: codePatterns=['req.body','req.params','req.query','request.form','$_GET','$_POST'], categories=['xss','sql-injection','cmd-injection']
       - sig-frontend-code: deps=['react','vue','svelte','@angular/core'], filePatterns=['.jsx','.tsx','.vue','.svelte'], categories=['xss']
    4. `selectScanners(projectContext)`:
       - Destructure: `{ deps = [], fileExtensions = [], codePatterns = [], hasLockfile = false } = projectContext`
       - Loop SIGNAL_MAP, check deps match, pyDeps match, filePatterns match, lockfiles match, codePatterns match
       - De-dup: `[...new Set([...BASE_SCANNERS, ...categoriesFromSignals])]`
       - skipped = ALL_CATEGORIES.filter khong co trong selected
       - lowConfidence = signals.length < 2
       - Return { selected, skipped, signals, lowConfidence }
    5. `module.exports = { selectScanners, BASE_SCANNERS, ALL_CATEGORIES, SIGNAL_MAP }`

    Chay test lai — PHAI PASS.

    REFACTOR (neu can): Clean up, dam bao code doc de hieu.
  </action>
  <verify>
    <automated>node --test test/smoke-smart-selection.test.js</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/smart-selection.js ton tai va co `module.exports = { selectScanners, BASE_SCANNERS, ALL_CATEGORIES, SIGNAL_MAP }`
    - test/smoke-smart-selection.test.js ton tai va co >= 10 test cases
    - `node --test test/smoke-smart-selection.test.js` exit code 0
    - smart-selection.js KHONG co `require('fs')` (pure function, khong doc file)
    - BASE_SCANNERS = ['secrets', 'misconfig', 'logging'] (grep xac nhan)
    - ALL_CATEGORIES co 13 phan tu (grep `ALL_CATEGORIES` va dem)
    - SIGNAL_MAP co 12 entries (grep `id:` trong SIGNAL_MAP)
  </acceptance_criteria>
  <done>
    selectScanners() pass 12+ test cases. 3 base luon co mat, 12 signals mapping dung, lowConfidence logic dung, de-dup hoat dong, selected + skipped = 13.
  </done>
</task>

</tasks>

<verification>
- `node --test test/smoke-smart-selection.test.js` — tat ca tests pass
- `node -e "const {selectScanners}=require('./bin/lib/smart-selection');console.log(JSON.stringify(selectScanners({})))"` — tra ve 3 base, lowConfidence=true
- `node -e "const {selectScanners}=require('./bin/lib/smart-selection');const r=selectScanners({deps:['express','sequelize'],hasLockfile:true});console.log(r.selected.length, r.lowConfidence)"` — nhieu hon 3, lowConfidence=false
</verification>

<success_criteria>
- selectScanners() la pure function (khong doc file, khong side effects)
- 12 signals mapping chinh xac theo RESEARCH.md
- 3 base scanners luon co mat trong selected
- lowConfidence=true khi < 2 signal match
- De-dup dam bao moi scanner chi xuat hien 1 lan
- Test suite >= 10 cases, tat ca PASS
</success_criteria>

<output>
After completion, create `.planning/phases/48-evidence-smart-selection/48-01-SUMMARY.md`
</output>
