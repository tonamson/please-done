---
phase: 76-lint-recovery-status-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/progress.md
  - workflows/write-code.md
autonomous: true
requirements: [LINT-01]

must_haves:
  truths:
    - "After 3 consecutive lint failures in Step 5, lint_fail_count: 3 and last_lint_error are saved to PROGRESS.md"
    - "When lint_fail_count >= 3, resuming write-code.md offers a lint-only mode (skip Steps 2–4, jump to Step 5) or a fresh start (wipe PROGRESS.md, go to Step 2)"
    - "After the 3rd failure the workflow surfaces a pd:fix-bug suggestion"
  artifacts:
    - path: "templates/progress.md"
      provides: "PROGRESS.md schema with lint tracking fields"
      contains: "lint_fail_count: 0"
    - path: "templates/progress.md"
      provides: "PROGRESS.md schema with last lint error field"
      contains: "last_lint_error:"
    - path: "workflows/write-code.md"
      provides: "Step 5 lint failure counter + PROGRESS.md persistence"
      contains: "lint_fail_count"
    - path: "workflows/write-code.md"
      provides: "Step 1.1 lint-fail recovery row"
      contains: "Lint-only"
  key_links:
    - from: "workflows/write-code.md (Step 5)"
      to: "templates/progress.md"
      via: "saves lint_fail_count and last_lint_error to PROGRESS.md on each failure"
      pattern: "lint_fail_count.*3.*STOP"
    - from: "workflows/write-code.md (Step 1.1)"
      to: "PROGRESS.md lint_fail_count field"
      via: "checks lint_fail_count >= 3 before stage-based routing"
      pattern: "lint_fail_count >= 3"
---

<objective>
Add 3-strike lint failure tracking to the write-code workflow and PROGRESS.md template.

Purpose: When lint/build fails 3 consecutive times, the workflow saves failure state to PROGRESS.md and stops with actionable recovery options (pd:fix-bug or lint-only retry). On resume, users choose between retrying lint only or starting fresh.

Output: Modified `templates/progress.md` (2 new fields) and `workflows/write-code.md` (Step 5 counter logic + Step 1.1 recovery row).
</objective>

<context>
@.planning/phases/76-lint-recovery-status-dashboard/76-CONTEXT.md
@.planning/phases/76-lint-recovery-status-dashboard/76-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<read_first>
Read these files completely before making any changes:
1. `workflows/write-code.md` — full file, focus on Step 5 (line 254) and Step 1.1 Case 1 (lines 78-95)
2. `templates/progress.md` — full file (short, ~45 lines)
3. `.planning/phases/76-lint-recovery-status-dashboard/76-CONTEXT.md` — locked decisions D-01 through D-04
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Add lint tracking fields to PROGRESS.md template + modify write-code.md</name>
  <files>templates/progress.md, workflows/write-code.md</files>
  <action>

**Part A — templates/progress.md** (per D-01)

In the Template section, find this exact block:

```markdown
> Stage: [Started | Reading context | Writing code | Lint/Build | Creating report | Commit]
```

Insert two new lines IMMEDIATELY AFTER that line:

```markdown
> lint_fail_count: 0
> last_lint_error: ""
```

The resulting header block should look like:
```markdown
# Execution Progress
> Updated: [DD_MM_YYYY HH:MM]
> Task: [N] — [Task name]
> Stage: [Started | Reading context | Writing code | Lint/Build | Creating report | Commit]
> lint_fail_count: 0
> last_lint_error: ""
```

Also in the `## Rules` section at the bottom, add one new bullet after the existing bullets:

```markdown
- `lint_fail_count` tracks consecutive lint/build failures (0–3); `last_lint_error` stores the last error output. Both reset naturally when PROGRESS.md is deleted after successful commit
```

**Part B — workflows/write-code.md Step 5** (per D-03)

Find this exact line (line 254):
```
- Fail → fix + rerun. Max 3 times → **STOP**, notify user + error message
```

Replace it with these lines:
```
- Fail → fix + rerun. Track consecutive failures:
  1. Increment `lint_fail_count` in PROGRESS.md (update `> lint_fail_count:` line)
  2. Save error output to `> last_lint_error:` in PROGRESS.md (first 500 chars, single line)
  3. If `lint_fail_count < 3` → retry fix + rerun
  4. If `lint_fail_count` reaches 3 → save PROGRESS.md → **STOP** with message:
     "❌ Lint/Build failed 3 times. Last error: [last_lint_error]
      → Run `/pd:fix-bug` to investigate root cause
      → Run `/pd:write-code` to resume (will offer lint-only retry)"
```

**Part C — workflows/write-code.md Step 1.1 Case 1** (per D-04)

Find the Case 1 section starting at line 78:
```
**Case 1: Task 🔄 + PROGRESS.md exists** (resume after network loss/session close):
1. Read PROGRESS.md → last stage + files already written
```

After this first instruction line ("1. Read PROGRESS.md → last stage + files already written"), insert a new sub-step 1.5:

```
1.5. **Lint-fail check (before stage-based routing):**
   - Read `lint_fail_count` from PROGRESS.md header
   - If `lint_fail_count >= 3` → offer user a choice:
     - **(A) Lint-only resume:** Skip Steps 2–4 (research, logic-validate, code-write), jump directly to Step 5 with previously written files
     - **(B) Fresh start:** Delete PROGRESS.md, start from Step 2
   - User picks A → jump to Step 5. User picks B → delete PROGRESS.md → Step 2
   - If `lint_fail_count < 3` or field not present → continue to step 2 below (standard stage-based routing)
```

This must appear BEFORE the "2. Verify actual state on disk:" sub-step so the lint-fail override is checked first (per research recommendation — prevents misrouting to Step 5 without the recovery prompt).

  </action>
  <verify>
    <automated>
grep -c "lint_fail_count: 0" templates/progress.md | grep -q "1" && \
grep -c "last_lint_error" templates/progress.md | grep -q "1" && \
grep -c "lint_fail_count" workflows/write-code.md | xargs test 3 -le && \
grep -c "last_lint_error" workflows/write-code.md | xargs test 1 -le && \
grep -q "Lint-only" workflows/write-code.md && \
grep -q "lint_fail_count >= 3" workflows/write-code.md && \
grep -q "pd:fix-bug" workflows/write-code.md && \
echo "ALL LINT-01 CHECKS PASS"
    </automated>
  </verify>
  <done>
    - templates/progress.md contains `lint_fail_count: 0` and `last_lint_error: ""` in the header block after `> Stage:`
    - workflows/write-code.md Step 5 has expanded lint failure tracking with counter, PROGRESS.md persistence, and STOP message mentioning pd:fix-bug
    - workflows/write-code.md Step 1.1 Case 1 has a lint-fail check (lint_fail_count >= 3) BEFORE the standard stage-based routing table, offering lint-only resume (A) or fresh start (B)
    - No `resume_mode:` field added anywhere (per D-01 — only lint_fail_count and last_lint_error)
    - `npm test` passes (no new test failures introduced)
  </done>
</task>

</tasks>

<verification>
```bash
# 1. Verify PROGRESS.md template has new fields
grep -n "lint_fail_count: 0" templates/progress.md
grep -n "last_lint_error" templates/progress.md

# 2. Verify write-code.md Step 5 has counter logic
grep -n "lint_fail_count" workflows/write-code.md

# 3. Verify write-code.md Step 1.1 has lint-fail recovery
grep -n "Lint-only\|lint_fail_count >= 3\|Fresh start" workflows/write-code.md

# 4. Verify pd:fix-bug suggestion in Step 5
grep -n "pd:fix-bug" workflows/write-code.md

# 5. Full test suite passes
npm test
```
</verification>

<success_criteria>
1. `grep -c "lint_fail_count: 0" templates/progress.md` returns 1
2. `grep -c "last_lint_error" templates/progress.md` returns ≥ 1
3. `grep -c "lint_fail_count" workflows/write-code.md` returns ≥ 3 (Step 5 counter + Step 1.1 check + STOP message)
4. `grep -q "Lint-only" workflows/write-code.md` succeeds (recovery option A)
5. `grep -q "lint_fail_count >= 3" workflows/write-code.md` succeeds (threshold check)
6. `npm test` passes with zero new failures
</success_criteria>

<output>
After completion, create `.planning/phases/76-lint-recovery-status-dashboard/76-01-SUMMARY.md`
</output>
