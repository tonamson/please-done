---
phase: 76-lint-recovery-status-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/pd/status.md
  - workflows/status.md
autonomous: true
requirements: [STATUS-01]

must_haves:
  truths:
    - "pd:status prints a read-only 8–12 line dashboard with 8 fields: milestone, phase, plan, tasks, bugs, lint, blockers, last commit"
    - "pd:status uses the Haiku model and makes zero writes to any planning file"
    - "If no PROGRESS.md exists, Lint field shows '✓ no active task'"
    - "pd:status does NOT suggest next steps"
  artifacts:
    - path: "commands/pd/status.md"
      provides: "pd:status skill definition"
      contains: "model: haiku"
    - path: "commands/pd/status.md"
      provides: "Read-only guard"
      contains: "READ ONLY"
    - path: "workflows/status.md"
      provides: "Status dashboard workflow steps"
      contains: "<process>"
  key_links:
    - from: "commands/pd/status.md"
      to: "workflows/status.md"
      via: "@workflows/status.md reference in execution_context"
      pattern: "@workflows/status.md"
    - from: "workflows/status.md"
      to: "STATE.md, CURRENT_MILESTONE.md, TASKS.md, PROGRESS.md, git log"
      via: "Read operations in process steps"
      pattern: "STATE.md.*CURRENT_MILESTONE.*TASKS.md"
---

<objective>
Create the pd:status skill — a read-only Haiku-powered dashboard that displays 8 fields of project status.

Purpose: Give developers an instant, cheap (Haiku model) status snapshot without the advice/suggestions that pd:what-next provides. Zero file writes.

Output: New `commands/pd/status.md` (skill file) and `workflows/status.md` (workflow file). Both are required — smoke-integrity.test.js enforces that every command has a matching workflow.
</objective>

<context>
@.planning/phases/76-lint-recovery-status-dashboard/76-CONTEXT.md
@.planning/phases/76-lint-recovery-status-dashboard/76-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<interfaces>
<!-- Pattern to follow — extracted from commands/pd/what-next.md -->
Required frontmatter fields for all skills (from smoke-integrity.test.js):
- name, description, model, argument-hint, allowed-tools

Required XML sections in order (from smoke-integrity.test.js):
- objective, guards, context, execution_context, process, output, rules

Workflow structure (from workflows/what-next.md):
- <purpose>, <required_reading>, <process> with numbered Steps

ALLOWED_NO_WORKFLOW whitelist (from smoke-integrity.test.js line 29):
- Only "fetch-doc" and "update" — pd:status is NOT whitelisted, so workflows/status.md MUST exist
</interfaces>

<read_first>
Read these files completely before creating the new files:
1. `commands/pd/what-next.md` — template for skill file structure (frontmatter + all XML sections)
2. `workflows/what-next.md` — template for workflow file structure (purpose + required_reading + process)
3. `test/smoke-integrity.test.js` — understand what tests validate (frontmatter fields, XML section order, workflow existence, ref existence)
4. `.planning/phases/76-lint-recovery-status-dashboard/76-CONTEXT.md` — locked decisions D-05 through D-09
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/pd/status.md skill file</name>
  <files>commands/pd/status.md</files>
  <action>

Create `commands/pd/status.md` with the following exact content. This follows the what-next.md pattern precisely — same frontmatter structure, same XML sections in the same order, same guard/output patterns.

**Frontmatter** (per D-05):
```yaml
---
name: pd:status
description: Display a read-only project status dashboard showing milestone, phase, tasks, bugs, lint state, and blockers
model: haiku
argument-hint: "(no arguments needed)"
allowed-tools:
  - Read
  - Glob
  - Bash
---
```

**Body** — all 7 XML sections in required order:

```xml
<objective>
Display an 8-field read-only project status dashboard.
READ ONLY. DO NOT edit files. DO NOT call FastCode MCP.
</objective>

<guards>
Stop and instruct the user if any of the following conditions fail:

- [ ] `.planning/` directory exists -> "The project has not been initialized yet. Run `/pd:init` first."
</guards>

<context>
User input: $ARGUMENTS (no arguments)
No rules or FastCode MCP needed - only read planning files.
</context>

<execution_context>
@workflows/status.md (required)
@references/conventions.md (required)
</execution_context>

<process>
Execute @workflows/status.md from start to finish.
</process>

<output>
**Create/Update:**
- No files are created or modified, read-only only

**Next step:** None — pd:status does not suggest next steps. Use `/pd:what-next` for suggestions.

**Success when:**

- All 8 dashboard fields are displayed
- Zero files were written or modified
- Output is 8–12 lines of formatted status

**Common errors:**

- `.planning/` does not exist -> run `/pd:init`
- `STATE.md` is missing or broken -> run `/pd:new-milestone` to recreate it
</output>

<rules>
- All output MUST be in English
- READ ONLY. DO NOT edit any files
- DO NOT call FastCode MCP or Context7 MCP
- DO NOT suggest next steps — that is pd:what-next's job (per D-09)
- Display exactly 8 fields in order: Milestone, Phase, Plan, Tasks, Bugs, Lint, Blockers, Last commit
- If PROGRESS.md does not exist → Lint field shows "✓ no active task" (per D-08)
</rules>
```

  </action>
  <verify>
    <automated>
test -f commands/pd/status.md && \
grep -q "name: pd:status" commands/pd/status.md && \
grep -q "model: haiku" commands/pd/status.md && \
grep -q "<objective>" commands/pd/status.md && \
grep -q "<guards>" commands/pd/status.md && \
grep -q "<context>" commands/pd/status.md && \
grep -q "<execution_context>" commands/pd/status.md && \
grep -q "<process>" commands/pd/status.md && \
grep -q "<output>" commands/pd/status.md && \
grep -q "<rules>" commands/pd/status.md && \
grep -q "@workflows/status.md" commands/pd/status.md && \
grep -qi "READ ONLY" commands/pd/status.md && \
echo "SKILL FILE OK"
    </automated>
  </verify>
  <done>
    - `commands/pd/status.md` exists with valid frontmatter (name, description, model: haiku, argument-hint, allowed-tools)
    - All 7 XML sections present in order: objective, guards, context, execution_context, process, output, rules
    - References `@workflows/status.md (required)` and `@references/conventions.md (required)`
    - Contains READ ONLY directive, no next-step suggestions
  </done>
</task>

<task type="auto">
  <name>Task 2: Create workflows/status.md workflow file</name>
  <files>workflows/status.md</files>
  <action>

Create `workflows/status.md` following the `workflows/what-next.md` structure pattern. This file contains the actual step-by-step instructions for the pd:status dashboard.

**Content to create** (per D-06, D-07, D-08, D-09):

```markdown
<purpose>
Read planning files and git log to display an 8-field project status dashboard. READ ONLY, DO NOT modify files.
</purpose>

<required_reading>

- @references/conventions.md → status icons, version format
</required_reading>

<process>

## Step 1: Gather data sources

Read each source in order. Missing files → use fallback values (do NOT stop):

1. `.planning/STATE.md` → extract `milestone:`, `milestone_name:`, blockers section
   - Missing → milestone = "Unknown", blockers = "Unknown (STATE.md missing)"
2. `.planning/CURRENT_MILESTONE.md` → extract `version:`, `phase:`, `status:`
   - Missing → fall back to STATE.md `stopped_at:` field for phase info; plan = "Unknown"
3. Derive phase directory: `.planning/milestones/[version]/phase-[phase]/`
4. `TASKS.md` in phase directory → count tasks by status icon (✅ 🔄 ⬜ 🐛 ❌)
   - Missing → tasks = "No tasks (run /pd:plan)"
5. Glob `.planning/bugs/BUG_*.md` → count files where `> Status:` is `Unresolved` or `In progress`
   - No bugs directory or no files → bugs = 0
6. `PROGRESS.md` in phase directory → read `> lint_fail_count:` and `> last_lint_error:`
   - PROGRESS.md exists + `lint_fail_count >= 1` → lint = "✗ [N] failure(s) — last error: [msg]"
   - PROGRESS.md exists + `lint_fail_count: 0` or field missing → lint = "✓ clean"
   - PROGRESS.md does NOT exist → lint = "✓ no active task"
7. Run `git log -1 --format="%h %s"` → last commit hash + message
   - Not a git repo or no commits → last_commit = "No git history"

## Step 2: Display dashboard

Print exactly 8 fields in this format (align values with spaces for readability):

```
Milestone:   [milestone_name] ([version])
Phase:       [phase_number] — [phase_name or description]
Plan:        [plan_number] — [status] (or "Not started")
Tasks:       [done]/[total] done (✅ [n]  🔄 [n]  ⬜ [n])
Bugs:        [count] open
Lint:        [lint status from Step 1.6]
Blockers:    [blockers or "None"]
Last commit: [hash] [message]
```

- Total lines: 8–12 (the 8 fields, plus optional blank lines for readability)
- DO NOT add any suggestions, recommendations, or "next step" advice
- DO NOT add decorative boxes or borders — plain text with aligned colons

</process>

<rules>
- DO NOT call FastCode MCP — use only Read/Glob/Bash
- DO NOT modify files — read and display only
- DO NOT suggest next steps — pd:status is display-only (pd:what-next handles suggestions)
- Missing data → use fallback values, never stop with an error
- Output MUST be in English
- Lint field MUST reflect actual PROGRESS.md state (or "no active task" if missing)
</rules>
```

After creating this file, run `npm test` to verify that all smoke-integrity tests pass, including:
- "only whitelisted commands have no dedicated workflow" (pd:status now has workflows/status.md)
- "each command has minimum frontmatter and process section" (pd:status has name, description, process)
- "all @workflows/@templates/@references referenced from commands exist" (workflows/status.md exists)

  </action>
  <verify>
    <automated>
test -f workflows/status.md && \
grep -q "<purpose>" workflows/status.md && \
grep -q "<process>" workflows/status.md && \
grep -q "Step 1" workflows/status.md && \
grep -q "Step 2" workflows/status.md && \
grep -q "lint_fail_count" workflows/status.md && \
grep -q "no active task" workflows/status.md && \
grep -qi "READ ONLY\|read and display only" workflows/status.md && \
grep -q "git log" workflows/status.md && \
grep -q "STATE.md" workflows/status.md && \
grep -q "TASKS.md" workflows/status.md && \
grep -q "PROGRESS.md" workflows/status.md && \
npm test 2>&1 | tail -5 && \
echo "WORKFLOW FILE + TESTS OK"
    </automated>
  </verify>
  <done>
    - `workflows/status.md` exists with purpose, required_reading, and process sections
    - Process has 2 steps: gather data (7 sources) and display dashboard (8 fields)
    - Lint field logic: "✗ N failure(s)" when lint_fail_count >= 1, "✓ clean" when 0, "✓ no active task" when no PROGRESS.md
    - References STATE.md, CURRENT_MILESTONE.md, TASKS.md, bugs/, PROGRESS.md, git log as data sources
    - Contains NO next-step suggestions (per D-09)
    - `npm test` passes — all smoke-integrity tests green
  </done>
</task>

</tasks>

<verification>
```bash
# 1. Both files exist
test -f commands/pd/status.md && echo "skill OK" || echo "skill MISSING"
test -f workflows/status.md && echo "workflow OK" || echo "workflow MISSING"

# 2. Skill frontmatter is valid
grep "name: pd:status" commands/pd/status.md
grep "model: haiku" commands/pd/status.md
grep "description:" commands/pd/status.md

# 3. Skill references workflow
grep "@workflows/status.md" commands/pd/status.md

# 4. Workflow has process with steps
grep -c "## Step" workflows/status.md

# 5. All 8 data sources referenced in workflow
grep -c "STATE.md\|CURRENT_MILESTONE\|TASKS.md\|bugs/\|PROGRESS.md\|git log\|Blockers\|Milestone" workflows/status.md

# 6. READ ONLY enforced
grep -ci "READ ONLY\|DO NOT modify\|DO NOT edit" commands/pd/status.md workflows/status.md

# 7. Full test suite passes (critical — validates all smoke-integrity constraints)
npm test
```
</verification>

<success_criteria>
1. `commands/pd/status.md` exists with frontmatter containing `name: pd:status`, `model: haiku`, `description:`, `argument-hint:`, `allowed-tools:`
2. `workflows/status.md` exists with `<process>` section containing Step 1 and Step 2
3. `npm test` passes with zero failures (smoke-integrity tests auto-validate: frontmatter, XML sections, workflow existence, ref existence)
4. No file writes or next-step suggestions in either file
5. Lint field handles 3 states: failures (✗), clean (✓), no active task (✓)
</success_criteria>

<output>
After completion, create `.planning/phases/76-lint-recovery-status-dashboard/76-02-SUMMARY.md`
</output>
