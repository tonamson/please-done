---
phase: 33-resilience-backward-compatibility
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - workflows/fix-bug.md
  - test/smoke-integrity.test.js
  - test/snapshots/
autonomous: true
requirements:
  - FLOW-06
  - FLOW-07

must_haves:
  truths:
    - "Khi INCONCLUSIVE o Buoc 4, orchestrator quay lai Buoc 2 voi Elimination Log va thong tin moi tu user (toi da 3 vong)"
    - "User co the chay single-agent mode khi thieu agent configs hoac truyen --single flag"
    - "Toan bo test suite pass (754 tests, 0 failures)"
  artifacts:
    - path: "workflows/fix-bug.md"
      provides: "INCONCLUSIVE loop-back flow va single-agent fallback detection"
      contains: "buildInconclusiveContext"
    - path: "workflows/fix-bug.md"
      provides: "Buoc 0 agent detection"
      contains: "pd-bug-janitor"
    - path: "test/smoke-integrity.test.js"
      provides: "Updated assertions cho v2.1 fix-bug.md format"
  key_links:
    - from: "workflows/fix-bug.md"
      to: "bin/lib/outcome-router.js"
      via: "buildInconclusiveContext call"
      pattern: "buildInconclusiveContext"
    - from: "workflows/fix-bug.md"
      to: "workflows/fix-bug-v1.5.md"
      via: "single-agent fallback redirect"
      pattern: "fix-bug-v1.5"
---

<objective>
Sua fix-bug.md workflow: (1) INCONCLUSIVE loop-back quay lai Buoc 2 toi da 3 vong, (2) Buoc 0 agent detection + --single fallback, (3) Fix 3 test failures hien co va regenerate snapshots.

Purpose: Hoan thanh FLOW-06 (loop-back) va FLOW-07 (single-agent fallback), dam bao toan bo test suite pass.
Output: fix-bug.md workflow day du, 754+ tests pass, 0 failures.
</objective>

<execution_context>
@workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-resilience-backward-compatibility/33-CONTEXT.md
@.planning/phases/33-resilience-backward-compatibility/33-RESEARCH.md
@.planning/phases/33-resilience-backward-compatibility/33-01-SUMMARY.md

<interfaces>
<!-- Tu Plan 01 -->
From bin/lib/outcome-router.js (sau Plan 01):
```javascript
module.exports = {
  buildRootCauseMenu,
  prepareFixNow,
  prepareFixPlan,
  prepareSelfFix,
  ROOT_CAUSE_CHOICES,
  buildInconclusiveContext,    // MỚI — trich xuat Elimination Log, tao prompt
  MAX_INCONCLUSIVE_ROUNDS,    // MỚI — = 3
};
```

```javascript
// buildInconclusiveContext({ evidenceContent, userInputPath, sessionDir, currentRound })
// -> { prompt, eliminationLog, round, canContinue, warnings }
```

From bin/lib/session-manager.js:
```javascript
// updateSession(currentMd, options) — options gom: status, appendToBody
// -> tra ve updated session markdown string
```

From test/smoke-integrity.test.js (3 tests dang fail):
1. `inlineWorkflow xử lý được mọi command có workflow` — regex `Bước [0-9]+` nhung fix-bug.md dung `Buoc` (khong dau)
2. `fix-bug workflow co effort routing tu bug classification` — fix-bug.md v2.1 khong con effort routing section
3. `executor workflows co backward compat default sonnet` — fix-bug.md v2.1 khong con tu `sonnet` (dung agent tiers thay vi model truc tiep)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Them Buoc 0 agent detection + --single fallback va INCONCLUSIVE loop-back vao fix-bug.md</name>
  <files>workflows/fix-bug.md</files>
  <read_first>
    - workflows/fix-bug.md
    - workflows/fix-bug-v1.5.md
    - bin/lib/outcome-router.js
    - .planning/phases/33-resilience-backward-compatibility/33-CONTEXT.md
  </read_first>
  <action>
    **Phan A: Them Buoc 0 agent detection (per D-06, D-07, D-08, D-09)**

    1. Trong `workflows/fix-bug.md`, tim section `## Buoc 0: Resume UI` (dong 19)
    2. Chen TRUOC Buoc 0 (ngay sau `<process>` tag, truoc `## Buoc 0`) mot block moi:

    ```markdown
    ## Buoc 0: Kiem tra che do hoat dong

    1. Parse $ARGUMENTS -> kiem tra co `--single` flag khong
    2. Neu KHONG co --single:
       - Kiem tra 5 files ton tai:
         `.claude/agents/pd-bug-janitor.md`
         `.claude/agents/pd-code-detective.md`
         `.claude/agents/pd-doc-specialist.md`
         `.claude/agents/pd-repro-engineer.md`
         `.claude/agents/pd-fix-architect.md`
       - Thieu bat ky file nao -> auto-fallback = true
    3. Neu --single HOAC auto-fallback:
       Hien: "--- Che do don agent: Thieu agent configs, dung che do don agent ---"
       Doc va thuc hien theo noi dung file `workflows/fix-bug-v1.5.md`. DUNG workflow v2.1 tai day.
    4. Neu du 5 files -> tiep tuc Buoc 0.5 (Resume UI ben duoi)
    ```

    3. Doi ten section hien tai `## Buoc 0: Resume UI` thanh `## Buoc 0.5: Resume UI`
    4. Doi ten section `## Buoc 0.5: Phan tich bug` thanh `## Buoc 0.6: Phan tich bug`
    5. Cap nhat moi reference `Buoc 0.5` trong file thanh `Buoc 0.6`

    **Phan B: Thay INCONCLUSIVE stub bang loop-back (per D-01 toi D-05, D-10)**

    6. Tim block `**NEU outcome = 'inconclusive':**` (khoang dong 214-220)
    7. Thay TOAN BO block do bang:

    ```markdown
    **NEU outcome = 'inconclusive':**
      1. Goi `buildInconclusiveContext({ evidenceContent: content, userInputPath: null, sessionDir: session_dir, currentRound })` tu `bin/lib/outcome-router.js`
         - currentRound: doc tu SESSION.md (grep `inconclusive_rounds:` -> parse so, mac dinh 1 neu chua co)
         -> { eliminationLog, canContinue, prompt, warnings }
      2. Hien Elimination Log cho user
      3. NEU canContinue = false (da dat 3 vong):
         Thong bao: "Da dieu tra 3 vong. Day la Elimination Log day du:\n{eliminationLog}"
         Read `{session_dir}/SESSION.md` -> currentMd
         Goi `updateSession(currentMd, { status: 'paused' })` tu `bin/lib/session-manager.js`
         Ghi ket qua vao `{session_dir}/SESSION.md`. DUNG workflow.
      4. NEU canContinue = true:
         Hoi user bo sung thong tin qua cau hoi truc tiep (free-text)
         Ghi response vao `{session_dir}/user_input_round_{currentRound}.md`
         Read `{session_dir}/SESSION.md` -> currentMd
         Goi `updateSession(currentMd, { appendToBody: '- inconclusive_rounds: ' + currentRound })` tu `bin/lib/session-manager.js`
         Ghi ket qua vao `{session_dir}/SESSION.md`
         Hien banner: "--- Vong {currentRound}/3: Dang dieu tra them voi thong tin moi ---"
         Quay lai Buoc 2 (spawn Detective + DocSpec voi context moi tu prompt)
    ```

    8. Trong phan Buoc 2, them note SAU dong `1. Goi buildParallelPlan(...)`:
    ```
    Neu day la INCONCLUSIVE loop-back (co user_input_round_N.md trong session_dir):
      Goi `buildInconclusiveContext({ evidenceContent: noi_dung_evidence_architect_cu, userInputPath: '{session_dir}/user_input_round_{N}.md', sessionDir: session_dir, currentRound: N })` tu `bin/lib/outcome-router.js`
      Them prompt vao prompt cua Detective va DocSpec de agents biet Elimination Log va thong tin moi tu user.
    ```

    **QUAN TRONG:**
    - KHONG tao module moi — detection logic nam trong workflow (per D-09)
    - KHONG import fs trong outcome-router.js (pure function pattern)
    - KHONG spawn process moi cho v1.5 fallback — doc file v1.5 content truc tiep (per D-09)
  </action>
  <verify>
    <automated>grep -c "buildInconclusiveContext" workflows/fix-bug.md</automated>
  </verify>
  <acceptance_criteria>
    - fix-bug.md chua `## Buoc 0: Kiem tra che do hoat dong`
    - fix-bug.md chua `--single`
    - fix-bug.md chua `pd-bug-janitor.md` trong Buoc 0 detection check
    - fix-bug.md chua `fix-bug-v1.5.md` trong fallback redirect
    - fix-bug.md chua `auto-fallback`
    - fix-bug.md chua `Thieu agent configs, dung che do don agent`
    - fix-bug.md chua `buildInconclusiveContext`
    - fix-bug.md chua `user_input_round_` (file pattern cho user input)
    - fix-bug.md chua `inconclusive_rounds`
    - fix-bug.md chua `Vong {currentRound}/3` hoac `Vong.*3.*Dang dieu tra`
    - fix-bug.md chua `canContinue = false` va `canContinue = true`
    - fix-bug.md KHONG con dong `(Quay lai Buoc 2 la FLOW-06 — Phase 33, NGOAI scope Phase 32)`
  </acceptance_criteria>
  <done>fix-bug.md co day du INCONCLUSIVE loop-back flow (max 3 vong) va Buoc 0 single-agent detection + fallback</done>
</task>

<task type="auto">
  <name>Task 2: Fix 3 test failures va regenerate snapshots</name>
  <files>test/smoke-integrity.test.js, test/snapshots/</files>
  <read_first>
    - test/smoke-integrity.test.js
    - test/generate-snapshots.js
    - workflows/fix-bug.md
  </read_first>
  <action>
    **Phan A: Fix 3 test failures trong smoke-integrity.test.js**

    fix-bug.md v2.1 la orchestrator workflow — KHONG con effort routing section va KHONG con tu "sonnet" truc tiep (dung agent tiers qua resource-config). Can cap nhat 3 tests:

    1. Tim test `'fix-bug workflow co effort routing tu bug classification'` (khoang dong 456-460)
       Thay TOAN BO test body bang:
       ```javascript
       it('fix-bug workflow co agent-based orchestration', () => {
         const content = fs.readFileSync(path.join(ROOT, 'workflows', 'fix-bug.md'), 'utf8');
         assert.match(content, /pd-bug-janitor/, 'fix-bug.md: thieu agent janitor reference');
         assert.match(content, /pd-fix-architect/, 'fix-bug.md: thieu agent architect reference');
       });
       ```

    2. Tim test `'executor workflows co backward compat default sonnet'` (khoang dong 468-474)
       Cap nhat de KHONG kiem tra fix-bug.md (vi no dung agent tiers, khong hardcode model name):
       ```javascript
       it('executor workflows co backward compat default sonnet', () => {
         const executorWorkflows = ['write-code.md', 'test.md'];
         for (const wf of executorWorkflows) {
           const content = fs.readFileSync(path.join(ROOT, 'workflows', wf), 'utf8');
           assert.match(content, /sonnet/, `${wf}: thieu backward compat default sonnet`);
         }
         // fix-bug.md v2.1 dung agent tiers (haiku/sonnet/opus qua resource-config), khong hardcode sonnet
         const fixBugContent = fs.readFileSync(path.join(ROOT, 'workflows', 'fix-bug.md'), 'utf8');
         assert.match(fixBugContent, /pd-code-detective/, 'fix-bug.md: thieu agent reference (thay the cho sonnet check)');
       });
       ```

    3. Tim test `'inlineWorkflow xử lý được mọi command có workflow'` (khoang dong 77-95)
       Sua regex `Bước [0-9]+` thanh `(Bước|Buoc) [0-9]+` de ho tro ca tieng Viet co dau va khong dau:
       ```javascript
       assert.match(inlined, /<process>[\s\S]*(Bước|Buoc) [0-9]+[\s\S]*<\/process>/, `${skill.name}: process chưa được inline đầy đủ`);
       ```

    **Phan B: Regenerate snapshots**

    4. Chay: `node test/generate-snapshots.js` de regenerate snapshots cho fix-bug.md da sua
    5. Chay full test suite: `node --test 'test/*.test.js'` — PHAI 0 failures

    **Phan C: Xac nhan FLOW-07 integrity**

    6. Them test moi trong section phu hop cua smoke-integrity.test.js (VD sau test agent-based orchestration):
       ```javascript
       it('fix-bug workflow co single-agent fallback', () => {
         const content = fs.readFileSync(path.join(ROOT, 'workflows', 'fix-bug.md'), 'utf8');
         assert.match(content, /--single/, 'fix-bug.md: thieu --single flag handling');
         assert.match(content, /fix-bug-v1\.5/, 'fix-bug.md: thieu v1.5 fallback reference');
         assert.match(content, /Kiem tra che do hoat dong/, 'fix-bug.md: thieu Buoc 0 detection');
       });
       ```

    7. Them test moi cho FLOW-06:
       ```javascript
       it('fix-bug workflow co inconclusive loop-back', () => {
         const content = fs.readFileSync(path.join(ROOT, 'workflows', 'fix-bug.md'), 'utf8');
         assert.match(content, /buildInconclusiveContext/, 'fix-bug.md: thieu buildInconclusiveContext call');
         assert.match(content, /inconclusive_rounds/, 'fix-bug.md: thieu inconclusive round tracking');
         assert.match(content, /user_input_round_/, 'fix-bug.md: thieu user input file pattern');
       });
       ```
  </action>
  <verify>
    <automated>node --test 'test/*.test.js' 2>&1 | grep -E "^ℹ (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - smoke-integrity.test.js chua `fix-bug workflow co agent-based orchestration` (thay the effort routing test)
    - smoke-integrity.test.js chua `(Bước|Buoc)` regex thay vi chi `Bước`
    - smoke-integrity.test.js chua `fix-bug workflow co single-agent fallback`
    - smoke-integrity.test.js chua `fix-bug workflow co inconclusive loop-back`
    - smoke-integrity.test.js test `executor workflows` chi kiem tra write-code.md va test.md cho sonnet (KHONG co fix-bug.md)
    - `node --test 'test/*.test.js'` hien `fail 0`
    - `node --test test/smoke-snapshot.test.js` exit 0
  </acceptance_criteria>
  <done>0 test failures, snapshots regenerated, 754+ tests pass, ca 3 failures cu da fix, 2 tests moi cho FLOW-06 va FLOW-07</done>
</task>

</tasks>

<verification>
node --test 'test/*.test.js' 2>&1 | tail -10
# Expected: fail 0, pass 754+
</verification>

<success_criteria>
- fix-bug.md co Buoc 0 detection check (5 agent files + --single flag)
- fix-bug.md co INCONCLUSIVE loop-back (max 3 vong, dung buildInconclusiveContext)
- fix-bug.md co single-agent fallback redirect sang v1.5
- 0 test failures trong toan bo test suite
- Snapshots da regenerate
- 2 tests moi cho FLOW-06 va FLOW-07
</success_criteria>

<output>
After completion, create `.planning/phases/33-resilience-backward-compatibility/33-02-SUMMARY.md`
</output>
