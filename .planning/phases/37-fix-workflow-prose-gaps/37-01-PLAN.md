---
phase: 37-fix-workflow-prose-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/fix-bug.md
  - test/snapshots/codex/fix-bug.md
  - test/snapshots/copilot/fix-bug.md
  - test/snapshots/gemini/fix-bug.md
  - test/snapshots/opencode/fix-bug.md
autonomous: true
requirements:
  - MEM-04
  - FLOW-06
gap_closure: true

must_haves:
  truths:
    - "Buoc 5e co instruction cu the goi parseFrontmatter() de construct {id, frontmatter} objects truoc khi goi buildIndex()"
    - "INCONCLUSIVE round counter doc bang dem '## Round' headings trong SESSION.md, khong grep body"
    - "INCONCLUSIVE round write ghi '## Round N: INCONCLUSIVE' heading thay vi append '- inconclusive_rounds: N'"
    - "Toan bo tests pass sau khi fix va regenerate snapshots"
  artifacts:
    - path: "workflows/fix-bug.md"
      provides: "Workflow prose voi INT-09 va INT-10 fixes"
      contains: "parseFrontmatter"
    - path: "test/snapshots/codex/fix-bug.md"
      provides: "Codex platform snapshot"
    - path: "test/snapshots/copilot/fix-bug.md"
      provides: "Copilot platform snapshot"
    - path: "test/snapshots/gemini/fix-bug.md"
      provides: "Gemini platform snapshot"
    - path: "test/snapshots/opencode/fix-bug.md"
      provides: "Opencode platform snapshot"
  key_links:
    - from: "workflows/fix-bug.md Buoc 5e"
      to: "bin/lib/bug-memory.js buildIndex()"
      via: "parseFrontmatter -> {id, frontmatter} -> buildIndex(bugRecords)"
      pattern: "parseFrontmatter.*construct.*id.*frontmatter"
    - from: "workflows/fix-bug.md Buoc 4 INCONCLUSIVE"
      to: "bin/lib/session-manager.js updateSession()"
      via: "dem ## Round headings + 1 -> currentRound"
      pattern: "## Round.*INCONCLUSIVE"
---

<objective>
Fix 2 workflow prose gaps cuoi cung cua v2.1 milestone trong fix-bug.md: INT-09 (buildIndex param shape underspecified) va INT-10 (INCONCLUSIVE round counter fragile). Regenerate snapshots va verify tests pass.

Purpose: Dong 2 integration gaps cuoi cung de v2.1 milestone hoan tat voi 0 gaps.
Output: fix-bug.md voi prose chinh xac, 4 platform snapshots cap nhat, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-fix-workflow-prose-gaps/37-CONTEXT.md
@.planning/phases/37-fix-workflow-prose-gaps/37-RESEARCH.md

@workflows/fix-bug.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix INT-09 va INT-10 prose trong fix-bug.md</name>
  <files>workflows/fix-bug.md</files>
  <read_first>
    - workflows/fix-bug.md
    - .planning/phases/37-fix-workflow-prose-gaps/37-RESEARCH.md
  </read_first>
  <action>
Sua 3 doan text trong workflows/fix-bug.md. KHONG sua bat ky JS module nao.

**Fix INT-09 (Buoc 5e, dong ~335):** Thay the doan:
```
  3. Glob `.planning/bugs/BUG-*.md` -> Read tat ca -> parse thanh records
     Goi `buildIndex(bugRecords)` tu `bin/lib/bug-memory.js` -> indexMd
     Ghi indexMd vao `.planning/bugs/INDEX.md`
```
Bang (per D-02, D-03):
```
  3. Glob `.planning/bugs/BUG-*.md` -> Read tat ca
     Voi moi file BUG-*.md:
       - Goi `parseFrontmatter(content)` tu `bin/lib/utils.js` -> { frontmatter }
       - Parse so tu filename (VD: BUG-003.md -> 'BUG-003')
       - Construct object: { id: 'BUG-003', frontmatter: parsed.frontmatter }
     Tao mang bugRecords tu tat ca objects tren
     Goi `buildIndex(bugRecords)` tu `bin/lib/bug-memory.js` -> indexMd
     Ghi indexMd vao `.planning/bugs/INDEX.md`
```

**Fix INT-10 read (dong ~252):** Thay the doan:
```
     - currentRound: doc tu SESSION.md (grep `inconclusive_rounds:` -> parse so, mac dinh 1 neu chua co)
```
Bang (per D-05, D-06):
```
     - currentRound: doc tu SESSION.md body — dem so "## Round" headings + 1 (mac dinh 1 neu chua co heading nao)
```

**Fix INT-10 write (dong ~264):** Thay the doan:
```
     Goi `updateSession(currentMd, { appendToBody: '- inconclusive_rounds: ' + currentRound })` tu `bin/lib/session-manager.js`
```
Bang (per D-05):
```
     Goi `updateSession(currentMd, { appendToBody: '\n## Round ' + currentRound + ': INCONCLUSIVE\n' })` tu `bin/lib/session-manager.js`
```
  </action>
  <verify>
    <automated>grep -n "parseFrontmatter" workflows/fix-bug.md | head -5 && grep -n "## Round" workflows/fix-bug.md | head -5 && grep -c "inconclusive_rounds:" workflows/fix-bug.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "parseFrontmatter(content)" workflows/fix-bug.md tra ve it nhat 1 ket qua (INT-09 fixed)
    - grep "Construct object.*id.*frontmatter" workflows/fix-bug.md tra ve 1 ket qua (shape specified)
    - grep "dem so.*## Round.*headings" workflows/fix-bug.md tra ve 1 ket qua (INT-10 read fixed)
    - grep "## Round.*currentRound.*INCONCLUSIVE" workflows/fix-bug.md tra ve 1 ket qua (INT-10 write fixed)
    - grep -c "inconclusive_rounds:" workflows/fix-bug.md tra ve 0 (old pattern removed completely)
  </acceptance_criteria>
  <done>3 doan prose trong fix-bug.md da duoc thay the: Buoc 5e co parseFrontmatter instruction cu the (INT-09), INCONCLUSIVE read dung heading count (INT-10), INCONCLUSIVE write ghi Round heading (INT-10). Khong con "inconclusive_rounds:" pattern cu.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate snapshots va verify tests</name>
  <files>
    test/snapshots/codex/fix-bug.md
    test/snapshots/copilot/fix-bug.md
    test/snapshots/gemini/fix-bug.md
    test/snapshots/opencode/fix-bug.md
  </files>
  <read_first>
    - workflows/fix-bug.md
    - test/generate-snapshots.js
  </read_first>
  <action>
Chay `node test/generate-snapshots.js` de regenerate 4 platform snapshots tu fix-bug.md da sua (per D-08).
Sau do chay `npm test` de verify toan bo tests pass (per D-07 — KHONG them tests moi).
  </action>
  <verify>
    <automated>node test/generate-snapshots.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - node test/generate-snapshots.js exit code 0
    - npm test exit code 0 — toan bo tests pass, khong regression
    - 4 snapshot files duoc cap nhat (codex, copilot, gemini, opencode)
  </acceptance_criteria>
  <done>4 platform snapshots regenerated thanh cong, toan bo tests pass, khong regression.</done>
</task>

</tasks>

<verification>
1. grep "parseFrontmatter(content)" workflows/fix-bug.md — phai co ket qua (INT-09)
2. grep "dem so.*## Round" workflows/fix-bug.md — phai co ket qua (INT-10 read)
3. grep "## Round.*currentRound.*INCONCLUSIVE" workflows/fix-bug.md — phai co ket qua (INT-10 write)
4. grep -c "inconclusive_rounds:" workflows/fix-bug.md — phai tra ve 0 (old pattern removed)
5. npm test — all tests pass
</verification>

<success_criteria>
- INT-09 dong: Buoc 5e chi ro parseFrontmatter() call va {id, frontmatter} object construction
- INT-10 dong: Round counter doc bang dem "## Round" headings, ghi bang "## Round N: INCONCLUSIVE" heading
- Toan bo tests pass, snapshots cap nhat
- v2.1 milestone co 0 integration gaps con lai
</success_criteria>

<output>
After completion, create `.planning/phases/37-fix-workflow-prose-gaps/37-01-SUMMARY.md`
</output>
