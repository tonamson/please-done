---
phase: 137-workflow-command-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/pd/what-next.md
  - workflows/what-next.md
  - docs/skills/what-next.md
  - docs/skills/what-next.vi.md
  - test/snapshots/opencode/what-next.md
  - test/snapshots/gemini/what-next.md
  - test/snapshots/copilot/what-next.md
  - test/snapshots/codex/what-next.md
  - AGENTS.md
autonomous: true
requirements:
  - L-02

must_haves:
  truths:
    - "Running pd:what-next (no flags) shows current project state and suggested next action without executing"
    - "Running pd:what-next --execute auto-detects state and runs the recommended command"
    - "Advisory mode (default) preserves all existing behavior — shows suggestion, waits for user"
    - "All pd:next behavior is fully subsumed — no functionality lost"
  artifacts:
    - path: "commands/pd/what-next.md"
      provides: "Command definition with --execute flag"
      contains: "--execute"
    - path: "workflows/what-next.md"
      provides: "Workflow with state detection + auto-execute branch"
      contains: "execute"
    - path: "docs/skills/what-next.md"
      provides: "Updated skill documentation"
      contains: "--execute"
  key_links:
    - from: "commands/pd/what-next.md"
      to: "workflows/what-next.md"
      via: "execution_context reference"
      pattern: "workflows/what-next.md"
    - from: "workflows/what-next.md"
      to: "pd commands"
      via: "SlashCommand invocation when --execute"
      pattern: "SlashCommand|/pd:"
---

<objective>
Merge auto-execute behavior from pd:next into pd:what-next with --execute flag.

Purpose: Users currently need two separate commands — pd:what-next for advisory mode and pd:next for auto-execute. This unifies them into one command with a flag, reducing cognitive overhead and maintaining backward compatibility.

Output: Updated command, workflow, docs, test snapshots, and AGENTS.md reflecting the merged command.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-workflow-command-merge/137-RESEARCH.md

<interfaces>
<!-- Key interfaces the executor needs -->

From commands/pd/what-next.md (command definition):
```yaml
name: pd:what-next
description: Check project progress and suggest the next command
model: haiku
argument-hint: "(no arguments needed)"
allowed-tools: [Read, Glob, Grep, Bash]
```

From workflows/what-next.md (workflow process):
- Step 1: Check foundation (planning dir, files)
- Step 2: Check open bugs
- Step 3: Check phase progress
- Step 4: Analyze + suggest (priority table 1-9)
- Step 5: Display report
- Step 6: Check Skills version

The priority suggestion table maps conditions to commands:
| Priority | Condition | Suggestion |
| 1 | Open bugs | /pd:fix-bug |
| 2 | Task in progress | /pd:write-code [N] |
| 3 | Bug task | /pd:fix-bug |
| 4 | Remaining tasks | /pd:write-code |
| 5 | All failed/bug | /pd:fix-bug |
| 5.5 | Verification gaps | /pd:fix-bug or /pd:write-code |
| 5.6 | Untested old phase | /pd:test |
| 5.7 | Standalone failures | /pd:fix-bug |
| 6 | All done, not tested | /pd:test |
| 7 | Phase complete, more ahead | /pd:plan [y.y] |
| 7.5 | All phases done, no audit | /pd:audit |
| 8 | All phases completed | /pd:complete-milestone |
| 9 | Idle >10 min | /pd:status |

From AGENTS.md (current pd entries):
```
| pd:next | Advance to next step | `/pd:next` |
| pd:what-next | Show next task | `/pd:what-next` |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update command definition and workflow with --execute flag</name>
  <files>commands/pd/what-next.md, workflows/what-next.md</files>
  <action>
**Part A: Update commands/pd/what-next.md**

Read the current file first. Then make these specific changes:

1. Update `argument-hint` from `"(no arguments needed)"` to `"[--execute]"`

2. Add `SlashCommand` to the `allowed-tools` list (needed for auto-execution):
   ```yaml
   allowed-tools:
     - Read
     - Glob
     - Grep
     - Bash
     - SlashCommand
   ```

3. Update `<objective>` to include the --execute behavior:
   ```
   Scan `.planning/` to determine unfinished work and the next logical step, then display progress with a suggested command.
   With `--execute` flag, automatically invoke the suggested command instead of just displaying it.
   READ ONLY (advisory mode). EXECUTES COMMANDS (with --execute).
   ```

4. Update `<context>` section to document the flag:
   ```
   User input: $ARGUMENTS
   - No arguments: Display progress and suggest next command (advisory mode, READ ONLY)
   - `--execute`: Auto-detect state and invoke the suggested command immediately
   ```

5. Update `<process>` section to reference the workflow's new execute branch:
   ```
   Execute @workflows/what-next.md from start to finish.

   **If `--execute` flag is set:**
   After determining the suggested command in Step 4, immediately invoke it via SlashCommand.
   The advisory display is still shown (for transparency) before execution.
   ```

6. Update `<output>` section:
   ```
   **Create/Update:**
   - No files are created or modified (advisory mode)
   - Commands may be invoked (with --execute flag)

   **Next step:** Suggested command based on the actual state (advisory mode) or auto-executed (--execute)
   ```

7. Update `<rules>` section — remove "READ ONLY" rule when --execute is active:
   ```
   - All output MUST be in English
   - Without --execute: READ ONLY. DO NOT edit any files
   - With --execute: Invokes the suggested command via SlashCommand after displaying the suggestion
   - DO NOT call FastCode MCP or Context7 MCP
   - The suggested command MUST be based on the actual current state, never guessed
   ```

**Part B: Update workflows/what-next.md**

Read the current file first. Then add an execution branch AFTER Step 4 (Analyze + suggest):

Insert a new "Step 4.5: Execute (if --execute flag)" between Step 4 and Step 5:

```
## Step 4.5: Execute (if --execute flag)

If `$ARGUMENTS` contains `--execute`:

1. Take the `SUGGESTION` determined in Step 4
2. Display the progress report (Step 5) FIRST — user needs to see state before execution
3. Immediately invoke the suggested command via SlashCommand:
   - Extract command name and arguments from the suggestion
   - Execute: `SlashCommand(command)`
   - Do NOT ask for confirmation — the `--execute` flag IS the confirmation
4. STOP — the invoked command takes over from here

If `$ARGUMENTS` does NOT contain `--execute`:
- Skip this step entirely
- Continue to Step 5 (display report) and Step 6 (version check) as normal
```

Also update the rules section at the bottom to add:
```
- With --execute flag: Invokes SlashCommand after displaying suggestion
- Without --execute flag: READ ONLY — no file modifications, no command execution
```
  </action>
  <verify>
    <automated>grep -c "\\-\\-execute" commands/pd/what-next.md workflows/what-next.md</automated>
  </verify>
  <done>
    - commands/pd/what-next.md has argument-hint containing --execute
    - commands/pd/what-next.md has SlashCommand in allowed-tools
    - workflows/what-next.md has Step 4.5 execute branch
    - Advisory mode (no flag) behavior is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Update documentation, test snapshots, and AGENTS.md</name>
  <files>docs/skills/what-next.md, docs/skills/what-next.vi.md, test/snapshots/opencode/what-next.md, test/snapshots/gemini/what-next.md, test/snapshots/copilot/what-next.md, test/snapshots/codex/what-next.md, AGENTS.md</files>
  <action>
**Part A: Update docs/skills/what-next.md**

Read the current file first. Make these changes:

1. Update "Purpose" section to mention --execute:
   ```
   Suggest the next logical action based on current project state. With `--execute`, automatically invoke the suggested command.
   ```

2. Update "When to Use" to include auto-execute scenario:
   Add after "Return after break":
   ```
   - **Auto-advance:** Want to immediately execute the next step without manual confirmation
   ```

3. Add --execute examples section after "Basic Command":
   ```
   ## Auto-Execute Mode

   ```
   /pd:what-next --execute
   ```

   **What it does:**
   1. Scans project state (same as advisory mode)
   2. Displays progress report for transparency
   3. Immediately invokes the suggested command

   **Use when:** You want zero-friction workflow advancement — the command decides and acts.
   ```

4. Update "Common Flags" table to include --execute:
   ```
   | Flag | Description | Example |
   |------|-------------|---------|
   | `--execute` | Auto-execute the suggested command | `/pd:what-next --execute` |
   | `--text` | Plain text output without formatting | `/pd:what-next --text` |
   ```

**Part B: Update docs/skills/what-next.vi.md**

Read the current file. Mirror the English changes in Vietnamese:

1. Update purpose to mention --execute
2. Add auto-execute section (translate to Vietnamese)
3. Add `--execute` to the flags table

**Part C: Update all 4 test snapshots**

For each file in test/snapshots/{opencode,gemini,copilot,codex}/what-next.md:

1. Update `argument-hint` to include `--execute`:
   - opencode: tools list should include `SlashCommand`
   - All: process section should reference `--execute` flag handling

Read each snapshot file first, then update:
- The `<objective>` to include --execute description
- The `<context>` to document the --execute flag
- The process rules to handle --execute
- Add `SlashCommand` to allowed tools

**Part D: Update AGENTS.md**

Read AGENTS.md. Make these changes:

1. Find the row `| pd:next | Advance to next step | `/pd:next` |` — REMOVE this entire row
2. Update the `pd:what-next` row description from "Show next task" to "Check progress and auto-execute next step":
   ```
   | pd:what-next | Check progress + auto-execute next step | `/pd:what-next [--execute]` |
   ```
  </action>
  <verify>
    <automated>grep -c "\\-\\-execute" docs/skills/what-next.md docs/skills/what-next.vi.md test/snapshots/opencode/what-next.md test/snapshots/gemini/what-next.md test/snapshots/copilot/what-next.md test/snapshots/codex/what-next.md AGENTS.md</automated>
  </verify>
  <done>
    - docs/skills/what-next.md documents --execute flag with examples
    - docs/skills/what-next.vi.md has Vietnamese translation of --execute docs
    - All 4 test snapshots include --execute in argument/context
    - AGENTS.md no longer has pd:next row
    - AGENTS.md pd:what-next description mentions --execute
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Skill Execution | User-provided --execute flag triggers command invocation |
| Skill → SlashCommand | Command execution via SlashCommand API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01 | E (Elevation) | --execute flag | accept | Flag only triggers project-scoped commands (/pd:*), no arbitrary command injection possible |
| T-137-02 | S (Spoofing) | State detection | accept | State read from local .planning/ files only — no external input influences routing |
</threat_model>

<verification>
1. `grep --execute commands/pd/what-next.md` — flag documented in command
2. `grep SlashCommand commands/pd/what-next.md` — execution tool available
3. `grep "Step 4.5" workflows/what-next.md` — execute branch exists
4. `grep --execute docs/skills/what-next.md` — documentation updated
5. `grep --execute test/snapshots/opencode/what-next.md` — snapshot updated
6. `grep -v "pd:next" AGENTS.md | grep -c "pd:what-next.*execute"` — pd:next removed, what-next has execute
</verification>

<success_criteria>
- pd:what-next command accepts --execute flag
- Default (no flag) behavior is unchanged — purely advisory
- --execute triggers SlashCommand invocation after displaying suggestion
- All documentation and snapshots reflect the new flag
- pd:next removed from AGENTS.md (merged into what-next)
</success_criteria>

<output>
After completion, create `.planning/phases/137-workflow-command-merge/137-01-SUMMARY.md`
</output>
