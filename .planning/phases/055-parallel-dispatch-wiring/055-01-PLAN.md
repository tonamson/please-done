---
phase: 055-parallel-dispatch-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/resource-config.js
  - test/smoke-resource-config.test.js
autonomous: true
requirements:
  - PARA-05

must_haves:
  truths:
    - "getAdaptiveParallelLimit() tra ve loadAvg field trong return object"
    - "loadAvg > cpuCount va workers > PARALLEL_MIN thi workers giam 1"
    - "loadAvg = 0 (Windows) thi skip loadAvg check"
    - "reason string co append loadAvg info khi degradation xay ra"
  artifacts:
    - path: "bin/lib/resource-config.js"
      provides: "getAdaptiveParallelLimit() voi loadAvg extension"
      contains: "os.loadavg"
    - path: "test/smoke-resource-config.test.js"
      provides: "Unit tests cho loadAvg extension"
      contains: "loadAvg"
  key_links:
    - from: "bin/lib/resource-config.js"
      to: "node:os"
      via: "os.loadavg()[0]"
      pattern: "os\\.loadavg\\(\\)\\[0\\]"
---

<objective>
Mo rong `getAdaptiveParallelLimit()` trong `resource-config.js` them `os.loadavg()[0]` check va `loadAvg` field trong return object.

Purpose: PARA-05 — graceful degradation khi system load cao (loadAvg > cpuCount).
Output: `getAdaptiveParallelLimit()` tra ve `{ workers, reason, cpu, freeMemGB, loadAvg }` voi auto-reduction khi overloaded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/055-parallel-dispatch-wiring/055-CONTEXT.md

<interfaces>
From bin/lib/resource-config.js:
```javascript
// Constants (lines 176-178)
const PARALLEL_MIN = 2;
const PARALLEL_MAX = 4;
const PARALLEL_DEFAULT = 3;

// Current getAdaptiveParallelLimit (lines 307-330)
function getAdaptiveParallelLimit() {
  // Returns: { workers: number, reason: string, cpu: number, freeMemGB: string }
  // Logic: CPU <= 4 || RAM < 2GB -> MIN, CPU >= 8 && RAM > 4GB -> MAX, else DEFAULT
}

// Exports (line 383-399)
module.exports = {
  getModelForTier, getAgentConfig, getParallelLimit,
  getAdaptiveParallelLimit, isHeavyAgent, shouldDegrade,
  TIER_MAP, AGENT_REGISTRY, PARALLEL_LIMIT,
  PARALLEL_MIN, PARALLEL_MAX, PARALLEL_DEFAULT,
  HEAVY_TOOL_PATTERNS, PLATFORM_MODEL_MAP, FALLBACK_CHAIN,
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Them loadAvg tests vao smoke-resource-config.test.js</name>
  <files>test/smoke-resource-config.test.js</files>
  <read_first>
    - test/smoke-resource-config.test.js
    - bin/lib/resource-config.js
  </read_first>
  <behavior>
    - Test: getAdaptiveParallelLimit() return object co field `loadAvg` la number
    - Test: loadAvg field >= 0
    - Test: return object van co workers, reason, cpu, freeMemGB (backward compat)
  </behavior>
  <action>
Them 1 describe block `getAdaptiveParallelLimit — loadAvg extension` vao cuoi file `test/smoke-resource-config.test.js`.

Import them `getAdaptiveParallelLimit` vao destructuring o dong 13 (da co san trong file).

Them tests:

```javascript
describe('getAdaptiveParallelLimit — loadAvg extension', () => {
  it('return object co field loadAvg la number', () => {
    const result = getAdaptiveParallelLimit();
    assert.equal(typeof result.loadAvg, 'number');
  });

  it('loadAvg >= 0', () => {
    const result = getAdaptiveParallelLimit();
    assert.ok(result.loadAvg >= 0, `loadAvg = ${result.loadAvg} phai >= 0`);
  });

  it('van co workers, reason, cpu, freeMemGB (backward compat)', () => {
    const result = getAdaptiveParallelLimit();
    assert.equal(typeof result.workers, 'number');
    assert.equal(typeof result.reason, 'string');
    assert.equal(typeof result.cpu, 'number');
    assert.equal(typeof result.freeMemGB, 'string');
  });
});
```

Luu y: `getAdaptiveParallelLimit` da duoc import o dong 13 nhung chua co trong destructuring — kiem tra va them neu thieu.
  </action>
  <verify>
    <automated>node --test test/smoke-resource-config.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test/smoke-resource-config.test.js chua `describe('getAdaptiveParallelLimit`
    - test/smoke-resource-config.test.js chua `result.loadAvg`
    - test/smoke-resource-config.test.js chua `typeof result.loadAvg, 'number'`
    - Tests FAIL (RED) vi loadAvg chua co trong return object
  </acceptance_criteria>
  <done>3 tests moi cho loadAvg extension da duoc viet, FAIL RED vi chua implement</done>
</task>

<task type="auto">
  <name>Task 2: Mo rong getAdaptiveParallelLimit() them loadAvg check va field</name>
  <files>bin/lib/resource-config.js</files>
  <read_first>
    - bin/lib/resource-config.js
    - test/smoke-resource-config.test.js
  </read_first>
  <action>
Sua function `getAdaptiveParallelLimit()` (dong 307-330) trong `bin/lib/resource-config.js`:

1. Them dong `const loadAvg = os.loadavg()[0];` sau dong `const freeMemGB = ...` (dong 311).

2. Them loadAvg check SAU block if/else hien tai (sau dong 327), TRUOC return:
```javascript
// D-10/D-11: loadAvg degradation — giam 1 worker neu he thong qua tai
if (loadAvg > 0 && loadAvg > cpuCount && workers > PARALLEL_MIN) {
  workers -= 1;
  reason += ` + loadAvg ${loadAvg.toFixed(1)} vuot ${cpuCount} CPU`;
}
```

3. Sua return statement (dong 329) them field `loadAvg`:
```javascript
return { workers, reason, cpu: cpuCount, freeMemGB, loadAvg };
```

4. Update JSDoc @returns (dong 305):
```javascript
 * @returns {{ workers: number, reason: string, cpu: number, freeMemGB: string, loadAvg: number }}
```

KHONG thay doi bat ky logic nao khac trong function. Chi THEM loadAvg check va field.
  </action>
  <verify>
    <automated>node --test test/smoke-resource-config.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/resource-config.js chua `os.loadavg()[0]`
    - bin/lib/resource-config.js chua `loadAvg > 0 && loadAvg > cpuCount`
    - bin/lib/resource-config.js chua `return { workers, reason, cpu: cpuCount, freeMemGB, loadAvg }`
    - `node --test test/smoke-resource-config.test.js` exit code 0 (GREEN)
    - `node --run test` van pass toan bo suite (backward compat — JS destructuring ignore extra fields)
  </acceptance_criteria>
  <done>getAdaptiveParallelLimit() tra ve loadAvg field, giam workers khi loadAvg > cpuCount, tat ca tests GREEN</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-resource-config.test.js` — tat ca tests pass
- `node --run test` — full suite pass (backward compat)
- `node -e "const r = require('./bin/lib/resource-config'); console.log(r.getAdaptiveParallelLimit())"` — output co field loadAvg
</verification>

<success_criteria>
- getAdaptiveParallelLimit() return object co field `loadAvg` (number)
- loadAvg > cpuCount va workers > PARALLEL_MIN thi workers giam 1
- loadAvg = 0 thi skip check (Windows compat)
- Existing tests khong bi break
</success_criteria>

<output>
After completion, create `.planning/phases/055-parallel-dispatch-wiring/055-01-SUMMARY.md`
</output>
