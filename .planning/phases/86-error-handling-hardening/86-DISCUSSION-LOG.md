# Phase 86: Error Handling Hardening - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-03

---

## Area 1: Debug Env Var Name

**Question:** REQUIREMENTS.md specifies `PD_DEBUG=1`, but `install.js:396` already uses `process.env.DEBUG`. Which env var should new debug logging use?

**Options presented:**
- PD_DEBUG — matches REQUIREMENTS.md spec, namespaced to this tool ✅ selected
- DEBUG — matches existing install.js pattern
- Support both: `PD_DEBUG || DEBUG`

**Decision:** `PD_DEBUG` — also update install.js:396 to PD_DEBUG for full consistency.

---

## Area 2: Silent Catches in utils.js

**Question:** utils.js has 3 bare catches — fileHash (returns null), commandExists (returns false), isWSL (returns false). Should they all get debug logging?

**Options presented:**
- Log only fileHash (file read failure has debug value); keep commandExists and isWSL silent ✅ selected
- Log all catches
- Keep all silent

**Decision:** Only fileHash gets `if (PD_DEBUG) console.error(...)`. commandExists and isWSL stay intentionally silent — failure is expected behavior.

---

## Area 3: claude.js Throw Refactor

**Question:** Currently `claude.js` does `log.error(msg)` then `process.exit(1)`. Replacing with `throw new Error(msg)` causes install.js to log the message again → duplicate display.

**Options presented:**
- Throw only: remove `log.error()`, let install.js handle display ✅ selected
- Keep log.error + throw: install.js skips re-logging if already displayed
- Create custom InstallError class

**Decision:** Remove `log.error()` in claude.js, replace with `throw new Error(message)`. install.js `main().catch` already handles display correctly.

---

*Generated by gsd-discuss-phase on 2026-04-03*
