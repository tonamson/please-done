---
phase: 126
wave: 1
depends_on: []
requirements_addressed: [C-02]
files_modified:
  - package.json
autonomous: true
---

# Phase 126: Test Infrastructure (C-02)

## Plan: Fix Test Script for Complete Coverage

<objective>
Update npm test patterns to catch all nested test files, add smoke/integration test scripts, and add c8 coverage tool.
</objective>

<tasks>

<task id="1">
<read_first>
- package.json
</read_first>
<acceptance_criteria>
- [ ] `package.json` contains `"test": "node --test 'test/**/*.test.js'"`
- [ ] `package.json` contains `"test:smoke": "node --test 'test/smoke/**/*.test.js'"`
- [ ] `package.json` contains `"test:integration": "node --test 'test/integration/**/*.test.js'"`
- [ ] `package.json` contains `"test:coverage": "c8 --reporter=text node --test 'test/**/*.test.js'"`
</acceptance_criteria>
<action>
Update package.json scripts section:

1. Change `"test": "node --test 'test/*.test.js'"` to `"test": "node --test 'test/**/*.test.js'"`

2. Add after `"test:platforms"`:
   `"test:smoke": "node --test 'test/smoke/**/*.test.js'",`
   `"test:integration": "node --test 'test/integration/**/*.test.js'",`

3. Add after `"test:coverage": "c8 --reporter=text node --test 'test/**/*.test.js'"` in the scripts section
</action>
</task>

<task id="2">
<read_first>
- package.json
</read_first>
<acceptance_criteria>
- [ ] `package.json` devDependencies contains `"c8": "^11.0.0"` (or latest major version)
- [ ] Running `npm install` adds c8 to node_modules
</acceptance_criteria>
<action>
Add c8 to package.json devDependencies:

Add `"c8": "^11.0.0"` to the devDependencies section (use latest stable version, ^11.0.0 is current as of 2026)

After adding, run `npm install` to install the new dependency.
</action>
</task>

<task id="3">
<read_first>
- package.json
</read_first>
<acceptance_criteria>
- [ ] `npm run test` runs all tests including nested directories
- [ ] `npm run test:smoke` runs only smoke tests
- [ ] `npm run test:integration` runs only integration tests
- [ ] `npm run test:coverage` generates coverage report
</acceptance_criteria>
<action>
Verify all test scripts work correctly by running each one:
1. `npm run test` - should find all .test.js files in test/ and subdirectories
2. `npm run test:smoke` - should find only files in test/smoke/
3. `npm run test:integration` - should find only files in test/integration/
4. `npm run test:coverage` - should generate c8 coverage output
</action>
</task>

</tasks>

<verification>
## Verification

1. Read package.json and verify all script changes are present
2. Run `npm run test` - should pass with all tests found
3. Run `npm run test:coverage` - should generate coverage output
4. Verify c8 is in devDependencies
</verification>

<must_haves>
- [ ] package.json test script catches nested test files (`'test/**/*.test.js'`)
- [ ] package.json has test:smoke script
- [ ] package.json has test:integration script
- [ ] package.json has test:coverage script using c8
- [ ] c8 is installed as devDependency
</must_haves>
