---
phase: 01-skill-structure-normalization
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - commands/pd/init.md
  - commands/pd/scan.md
  - commands/pd/write-code.md
  - commands/pd/test.md
  - commands/pd/fix-bug.md
  - commands/pd/conventions.md
autonomous: true
requirements:
  - READ-01
  - READ-02

must_haves:
  truths:
    - "All 6 skills follow identical section order: objective, guards, context, execution_context, process, output, rules"
    - "Each skill has guards extracted from context into a dedicated guards section"
    - "Each skill has a new output section with files created, next step, success criteria, error cases"
    - "Each skill has standardized frontmatter with all 5 required fields in consistent order"
    - "Reference tags in execution_context are marked required or optional"
  artifacts:
    - path: "commands/pd/init.md"
      provides: "Canonical example -- first skill normalized"
      contains: "<guards>"
    - path: "commands/pd/scan.md"
      provides: "Normalized scan skill"
      contains: "<output>"
    - path: "commands/pd/write-code.md"
      provides: "Normalized write-code skill"
      contains: "<rules>"
    - path: "commands/pd/test.md"
      provides: "Normalized test skill"
      contains: "<guards>"
    - path: "commands/pd/fix-bug.md"
      provides: "Normalized fix-bug skill"
      contains: "<output>"
    - path: "commands/pd/conventions.md"
      provides: "Normalized conventions skill"
      contains: "<guards>"
  key_links:
    - from: "commands/pd/*.md"
      to: "bin/lib/utils.js"
      via: "extractXmlSection reads section tags"
      pattern: "<objective>|<guards>|<context>|<execution_context>|<process>|<output>|<rules>"
    - from: "commands/pd/*.md"
      to: "bin/lib/converters/*.js"
      via: "Converters parse skill files"
      pattern: "convertSkill"
---

<objective>
Normalize the first batch of 6 simpler skill files (init, scan, write-code, test, fix-bug, conventions) to the canonical structure defined in Research. These skills already follow standard patterns (objective first, have workflows, 30-66 lines) so normalization is straightforward.

Purpose: Make 6 of 12 skills structurally identical. After this plan, half the skills pass the canonical structure smoke tests.
Output: 6 normalized skill files in commands/pd/.
</objective>

<execution_context>
@/Users/tonamson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skill-structure-normalization/01-RESEARCH.md
@.planning/phases/01-skill-structure-normalization/01-01-SUMMARY.md

<interfaces>
<!-- Canonical section order from Research -->
The canonical section order for ALL skills is:
1. Frontmatter (name, description, model, argument-hint, allowed-tools)
2. <objective>
3. <guards>
4. <context>
5. <execution_context>
6. <process>
7. <output>
8. <rules>

<!-- Frontmatter field order (consistent across all skills) -->
```yaml
name: pd:skill-name
description: Vietnamese description
model: haiku|sonnet|opus
argument-hint: "[hint text]"
allowed-tools:
  - Tool1
```

<!-- Guard format (checklist) -->
```markdown
<guards>
DUNG va huong dan user neu bat ky dieu kien nao that bai:

- [ ] Condition 1 -> "Error message"
- [ ] Condition 2 -> "Error message"
</guards>
```

<!-- Output section format -->
```markdown
<output>
**Tao/Cap nhat:**
- File list

**Buoc tiep theo:** `/pd:next-command`

**Thanh cong khi:**
- Criteria

**Loi thuong gap:**
- Error -> fix
</output>
```

<!-- Reference tagging format -->
```markdown
<execution_context>
@workflows/skill-name.md (required)
@references/conventions.md (optional)
</execution_context>
```

<!-- Skills with no argument-hint currently: conventions needs argument-hint: "(khong can tham so)" -->

<!-- Guard inventory per skill (from Research) -->
init: No file guards (first skill), path arg validation, FastCode MCP connectivity
scan: CONTEXT.md, path arg, FastCode MCP
write-code: CONTEXT.md, task number + --auto/--parallel, FastCode + Context7, PLAN.md + TASKS.md
test: CONTEXT.md, task number + --all, FastCode + Context7, at least 1 done task
fix-bug: CONTEXT.md, bug description, FastCode + Context7
conventions: No prerequisites, no MCP needed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize init, scan, and write-code skills</name>
  <files>commands/pd/init.md, commands/pd/scan.md, commands/pd/write-code.md</files>
  <action>
For each of the 3 skills (init.md, scan.md, write-code.md), read the current file and restructure to the canonical format. The transformation is:

**General rules for ALL skills:**
- MOVE content between sections -- do NOT rewrite behavioral content
- Frontmatter field order: name, description, model, argument-hint, allowed-tools (exactly this order)
- Section order: objective, guards, context, execution_context, process, output, rules
- Tag every reference in execution_context as (required) or (optional): workflow files are (required), templates used in output are (required), reference docs are (optional)
- For skills without existing rules section, add `<rules>` with minimal constraints extracted from the skill's process/context (e.g., "- Moi output PHAI bang tieng Viet co dau" or skill-specific constraints)

**init.md specifics:**
- Guards: No file existence (first skill). Add: "Tham so path hop le (neu co)" and "FastCode MCP ket noi thanh cong"
- Output: Creates .planning/CONTEXT.md, .planning/rules/*.md. Next step: `/pd:scan`. Success: CONTEXT.md co day du tech stack, FastCode confirmed. Errors: FastCode MCP khong ket noi, khong phat hien tech stack.
- Rules: Add minimal rules from existing process context (e.g., Vietnamese output, must verify FastCode connection)
- execution_context: Tag `@workflows/init.md (required)`

**scan.md specifics:**
- Read current file first to understand its structure
- Guards: Extract from context -- `.planning/CONTEXT.md` must exist, path arg valid, FastCode MCP connectivity
- Output: Derive from objective/process -- what scan creates/updates, next step, success criteria
- Rules: Add minimal rules (e.g., Vietnamese output, read-only analysis)
- execution_context: Tag each reference as (required) or (optional)

**write-code.md specifics:**
- Read current file first to understand its structure
- Guards: Extract from context -- `.planning/CONTEXT.md`, task number valid, PLAN.md + TASKS.md exist, FastCode + Context7 MCP
- Output: Derive from objective/process -- code files created, tests if any, next step, success criteria
- Rules: Add minimal rules from existing content
- execution_context: Tag each reference as (required) or (optional)

After each file, verify it hasn't broken the converter pipeline by running: `node --test test/smoke-integrity.test.js` (the existing 7 tests should still pass).
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && node --test test/smoke-integrity.test.js 2>&1 | grep -E "(pass|fail|#)" | head -20</automated>
  </verify>
  <done>init.md, scan.md, write-code.md follow canonical structure. Existing smoke tests still pass. Each file has all 7 sections in correct order, all 5 frontmatter fields, guards separated from context, output section with subsections, references tagged.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize test, fix-bug, and conventions skills</name>
  <files>commands/pd/test.md, commands/pd/fix-bug.md, commands/pd/conventions.md</files>
  <action>
For each of the 3 skills (test.md, fix-bug.md, conventions.md), read the current file and restructure to the canonical format using the same rules as Task 1.

**test.md specifics:**
- Read current file first
- Guards: `.planning/CONTEXT.md` exists, task number or --all arg valid, FastCode + Context7 MCP, at least 1 task with status done
- Output: Derive from objective/process -- test files created, coverage info, next step
- Rules: Add minimal rules from existing content
- execution_context: Tag each reference as (required) or (optional)

**fix-bug.md specifics:**
- Read current file first
- Guards: `.planning/CONTEXT.md` exists, bug description provided, FastCode + Context7 MCP
- Output: Derive from objective/process -- files fixed, next step
- Rules: Add minimal rules from existing content
- execution_context: Tag each reference as (required) or (optional)

**conventions.md specifics:**
- Has no argument-hint currently -- add `argument-hint: "(khong can tham so)"`
- Guards: No file prerequisites needed. Use "Khong co dieu kien tien quyet. Skill nay co the chay bat ky luc nao." or a minimal guard like "Thu muc du an co source code"
- Output: Creates/updates CLAUDE.md. Next step: continue development. Success: CLAUDE.md covers naming, style, patterns.
- Rules: Add minimal rules
- execution_context: Tag references as (required) or (optional)

After all 3 files are normalized, run the full test suite to verify nothing is broken: `npm test`.

Then run the new canonical structure tests to check how many pass: `node --test test/smoke-integrity.test.js`. All 6 skills from this plan should now pass the canonical structure tests.
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>test.md, fix-bug.md, conventions.md follow canonical structure. Full test suite (npm test) passes -- converters and inline workflows still work. All 6 skills (init, scan, write-code, test, fix-bug, conventions) pass the canonical structure smoke tests from Plan 01.</done>
</task>

</tasks>

<verification>
1. All 6 skill files have sections in order: objective, guards, context, execution_context, process, output, rules
2. All 6 skill files have frontmatter fields: name, description, model, argument-hint, allowed-tools
3. Guards are checklist format with "DUNG va huong dan user" preamble (or equivalent)
4. Output sections contain: Tao/Cap nhat, Buoc tiep theo, Thanh cong khi, Loi thuong gap
5. `npm test` passes (all 184+ tests including converter tests)
6. `git diff` shows only structural changes -- no behavioral content rewritten
</verification>

<success_criteria>
- 6 skill files normalized to identical structure
- Full test suite green
- Canonical structure smoke tests pass for these 6 skills
- Converter pipeline unbroken (Codex, Gemini, Copilot, OpenCode converters still produce correct output)
</success_criteria>

<output>
After completion, create `.planning/phases/01-skill-structure-normalization/01-02-SUMMARY.md`
</output>
