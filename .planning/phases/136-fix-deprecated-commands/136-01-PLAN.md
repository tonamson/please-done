---
phase: 136-fix-deprecated-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.vi.md
  - docs/workflows/getting-started.md
  - docs/workflows/getting-started.vi.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "User can read Vietnamese documentation without deprecated command references"
    - "User can read English workflow guides without deprecated command references"
    - "All pd:map-codebase references replaced with pd:scan"
  artifacts:
    - path: "CLAUDE.vi.md"
      provides: "Vietnamese main documentation"
      contains: "pd:scan"
      excludes: "pd:map-codebase"
    - path: "docs/workflows/getting-started.md"
      provides: "English workflow guide"
      contains: "pd:scan"
      excludes: "pd:map-codebase"
    - path: "docs/workflows/getting-started.vi.md"
      provides: "Vietnamese workflow guide"
      contains: "pd:scan"
      excludes: "pd:map-codebase"
  key_links:
    - from: "CLAUDE.vi.md"
      to: "pd:scan command"
      via: "text replacement"
      pattern: "/pd:scan"
    - from: "docs/workflows/getting-started.md"
      to: "pd:scan command"
      via: "text replacement"
      pattern: "/pd:scan"
    - from: "docs/workflows/getting-started.vi.md"
      to: "pd:scan command"
      via: "text replacement"
      pattern: "/pd:scan"
---

<objective>
Replace all deprecated `/pd:map-codebase` references with `/pd:scan` in user-facing documentation files that were missed in Phase 125.

**Purpose:** Ensure consistency across all documentation (English and Vietnamese) so users don't encounter broken command references.

**Output:**
- CLAUDE.vi.md updated (Vietnamese main doc)
- docs/workflows/getting-started.md updated (English workflow guide)
- docs/workflows/getting-started.vi.md updated (Vietnamese workflow guide)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-fix-deprecated-commands/136-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Vietnamese Documentation (CLAUDE.vi.md)</name>
  <files>CLAUDE.vi.md</files>
  <read_first>
    - CLAUDE.vi.md (current Vietnamese documentation)
    - .planning/phases/136-fix-deprecated-commands/136-RESEARCH.md (affected files list)
  </read_first>
  <action>
Replace deprecated command references in CLAUDE.vi.md:

1. Line 113: Replace "Chạy `/pd:map-codebase` để làm mới" with "Chạy `/pd:scan` để làm mới"

2. Lines 253-258: Replace the entire "Command Reference: pd:map-codebase" section with "Command Reference: pd:scan":
   - Line 253: "### Tài Liệu Tham Khảo Lệnh: pd:map-codebase" → "### Tài Liệu Tham Khảo Lệnh: pd:scan"
   - Line 255: "Skill `pd:map-codebase` tạo bản đồ cấu trúc codebase để các agent khác tham khảo." → "Skill `pd:scan` tạo bản đồ cấu trúc codebase để các agent khác tham khảo."
   - Line 258: "- `/pd:map-codebase` — Map codebase hiện tại" → "- `/pd:scan` — Map codebase hiện tại"

This aligns CLAUDE.vi.md with the English version (CLAUDE.md) which was fixed in Phase 125.
  </action>
  <verify>
    <automated>grep -c "pd:map-codebase" CLAUDE.vi.md | xargs -I {} sh -c 'if [ "{}" -eq 0 ]; then echo "PASS: No pd:map-codebase references"; else echo "FAIL: {} pd:map-codebase references found"; exit 1; fi' && grep -c "pd:scan" CLAUDE.vi.md | xargs -I {} sh -c 'if [ "{}" -ge 4 ]; then echo "PASS: {} pd:scan references found"; else echo "FAIL: Only {} pd:scan references found"; exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pd:map-codebase" CLAUDE.vi.md` returns 0 (no deprecated references)
    - `grep -c "pd:scan" CLAUDE.vi.md` returns greater than or equal to 4 (new references exist)
    - Vietnamese command reference section title updated
    - All `/pd:map-codebase` replaced with `/pd:scan`
  </acceptance_criteria>
  <done>CLAUDE.vi.md contains zero pd:map-codebase references and at least 4 pd:scan references</done>
</task>

<task type="auto">
  <name>Task 2: Fix English Workflow Guide (docs/workflows/getting-started.md)</name>
  <files>docs/workflows/getting-started.md</files>
  <read_first>
    - docs/workflows/getting-started.md (current English workflow guide)
    - CLAUDE.md (reference for correct pd:scan usage)
  </read_first>
  <action>
Replace deprecated command reference in docs/workflows/getting-started.md:

Line 220: Replace "If Map shows "stale", run `/pd:map-codebase` to refresh" with "If Map shows "stale", run `/pd:scan` to refresh"

This is a user-facing workflow guide that instructs users on command usage. Update to match current command name.
  </action>
  <verify>
    <automated>grep -c "pd:map-codebase" docs/workflows/getting-started.md | xargs -I {} sh -c 'if [ "{}" -eq 0 ]; then echo "PASS: No pd:map-codebase references"; else echo "FAIL: {} pd:map-codebase references found"; exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pd:map-codebase" docs/workflows/getting-started.md` returns 0
    - Line 220 now references `/pd:scan`
  </acceptance_criteria>
  <done>getting-started.md contains zero pd:map-codebase references</done>
</task>

<task type="auto">
  <name>Task 3: Fix Vietnamese Workflow Guide (docs/workflows/getting-started.vi.md)</name>
  <files>docs/workflows/getting-started.vi.md</files>
  <read_first>
    - docs/workflows/getting-started.vi.md (current Vietnamese workflow guide)
    - docs/workflows/getting-started.md (English reference after Task 2 fix)
  </read_first>
  <action>
Replace deprecated command reference in docs/workflows/getting-started.vi.md:

Line 224: Replace "Nếu Map hiển thị "stale", chạy `/pd:map-codebase` để refresh" with "Nếu Map hiển thị "stale", chạy `/pd:scan` để refresh"

This is the Vietnamese translation of the workflow guide. Maintain consistency with Task 2.
  </action>
  <verify>
    <automated>grep -c "pd:map-codebase" docs/workflows/getting-started.vi.md | xargs -I {} sh -c 'if [ "{}" -eq 0 ]; then echo "PASS: No pd:map-codebase references"; else echo "FAIL: {} pd:map-codebase references found"; exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pd:map-codebase" docs/workflows/getting-started.vi.md` returns 0
    - Line 224 now references `/pd:scan`
  </acceptance_criteria>
  <done>getting-started.vi.md contains zero pd:map-codebase references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → User | User-facing docs must be accurate and reference correct commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-01 | Tampering | docs/workflows/*.md | accept | Text replacement only, no executable content |
| T-136-02 | Information Disclosure | docs/workflows/*.md | accept | Public documentation, no secrets |
| T-136-03 | Spoofing | CLAUDE.vi.md | accept | Language translation, authenticity verified by commit |

**Disposition rationale:** Documentation-only changes pose minimal security risk. No code execution, no data processing, no authentication/authorization changes. The mitigation is ensuring the text replacement is accurate before commit.
</threat_model>

<verification>
After all tasks complete:

```bash
# Verify no deprecated references remain in user-facing docs
grep -r "pd:map-codebase" CLAUDE.vi.md docs/workflows/getting-started.md docs/workflows/getting-started.vi.md && echo "FAIL: Deprecated references found" || echo "PASS: No deprecated references"

# Verify new command references exist
grep -c "pd:scan" CLAUDE.vi.md docs/workflows/getting-started.md docs/workflows/getting-started.vi.md | awk -F: '{sum+=$2} END {if(sum>=5) print "PASS: " sum " pd:scan references"; else print "FAIL: Only " sum " references"}'
```
</verification>

<success_criteria>
- [ ] CLAUDE.vi.md contains zero references to `pd:map-codebase`
- [ ] docs/workflows/getting-started.md contains zero references to `pd:map-codebase`
- [ ] docs/workflows/getting-started.vi.md contains zero references to `pd:map-codebase`
- [ ] All three files contain references to `pd:scan`
- [ ] Verification commands pass (no deprecated references found)
</success_criteria>

<output>
After completion, create `.planning/phases/136-fix-deprecated-commands/136-01-SUMMARY.md`
</output>