---
phase: 124-testing-documentation
plan: '02'
type: execute
wave: '1'
depends_on: []
files_modified:
  - docs/skills/audit.md
  - docs/skills/audit.vi.md
  - test/smoke-snapshot.test.js
autonomous: true
requirements:
  - INT-06
must_haves:
  truths:
    - Documentation reflects all PTES flags
    - Smoke snapshot test passes
  artifacts:
    - path: docs/skills/audit.md
      provides: Updated audit skill documentation
    - path: docs/skills/audit.vi.md
      provides: Updated Vietnamese audit documentation
    - path: test/smoke-snapshot.test.js
      provides: Updated snapshot for smoke tests
  key_links:
    - from: docs/skills/audit.md
      to: bin/lib/flag-parser.js
      via: Documents parsePtesFlags behavior
    - from: test/smoke-snapshot.test.js
      to: docs/skills/audit.md
      via: Snapshot comparison
---

<objective>
Update documentation with new PTES/OSINT flags and fix smoke-snapshot test.
</objective>

<context>
@docs/skills/audit.md
@docs/skills/audit.vi.md
@test/smoke-snapshot.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify smoke-snapshot test failure cause</name>
  <files>test/smoke-snapshot.test.js</files>
  <action>
Read test/smoke-snapshot.test.js to understand what it's comparing. The test is comparing audit.md content against a snapshot.

Run the failing test to see exact diff:
```bash
cd /Volumes/Code/Nodejs/please-done && node --test test/smoke-snapshot.test.js 2>&1 | head -100
```

The failure shows:
- Actual audit.md has OSINT Modes section in objective
- Snapshot doesn't have this section

Options:
1. Update snapshot to match current audit.md (if audit.md is correct)
2. Update audit.md to match snapshot (if changes were unintended)

Based on context, Phase 124 should have updated audit.md with new flags, so the snapshot needs updating.
  </action>
  <verify>
  <automated>node --test test/smoke-snapshot.test.js 2>&1 | grep -E "(PASS|FAIL|actual|expected)" | head -10</automated>
  <done>Test failure clearly shows snapshot mismatch - needs update</done>
</task>

<task type="auto">
  <name>Task 2: Update audit.md with complete PTES/OSINT documentation</name>
  <files>docs/skills/audit.md</files>
  <action>
Review current docs/skills/audit.md and ensure it documents all PTES flags from the flag-parser and resource-config:

1. Ensure table includes all tiers:
   - Quick SAST (no recon)
   - `--recon-light` (FREE tier, 0 tokens)
   - `--recon` (STANDARD tier, ~2000 tokens)
   - `--recon-full` (DEEP tier, ~6000 tokens)
   - `--redteam` (RED TEAM tier, ~8000 tokens)
   - `--poc` (DAST verification, combinable)

2. Ensure OSINT modes are documented:
   - `--osint`: Quick OSINT scan
   - `--osint-full`: Comprehensive OSINT
   - `--osint-output`: Output format (json/table/markdown)
   - `--osint-timeout`: Timeout per source

3. Add examples for each flag combination

4. Ensure PTES phases explanation is clear

5. Document token optimization and cache behavior
</action>
  <verify>
  <automated>grep -E "(--recon-light|--recon|--recon-full|--redteam|--poc|--osint)" docs/skills/audit.md | wc -l</automated>
  <done>audit.md documents all PTES and OSINT flags with examples</done>
</task>

<task type="auto">
  <name>Task 3: Update audit.vi.md Vietnamese translation</name>
  <files>docs/skills/audit.vi.md</files>
  <action>
Update docs/skills/audit.vi.md to match the English version with all PTES/OSINT flags documented.

1. Translate any missing flag documentation
2. Ensure examples are provided in both languages
3. Keep technical terms (flags, tier names) in English but explain in Vietnamese
4. Verify structure matches audit.md
</action>
  <verify>
  <automated>diff docs/skills/audit.md docs/skills/audit.vi.md | grep -E "(--recon|--recon-light|--recon-full|--redteam|--poc|--osint)" | wc -l</automated>
  <done>audit.vi.md has same flag coverage as audit.md</done>
</task>

<task type="auto">
  <name>Task 4: Update smoke-snapshot test snapshot</name>
  <files>test/smoke-snapshot.test.js</files>
  <action>
The smoke-snapshot.test.js compares audit.md against a stored snapshot. Since the audit.md has been updated with new content (OSINT modes), update the snapshot to match.

Read test/smoke-snapshot.test.js to find where the snapshot is stored and how to update it. Typically this involves:
1. Finding the snapshot data structure
2. Updating it with current audit.md content

Or if there's a way to regenerate snapshots (common in Jest), run:
```bash
node --test --update-snapshots test/smoke-snapshot.test.js 2>&1
```

Note: Node.js test runner doesn't have built-in snapshot update like Jest. You may need to manually update the expected string in the test file.
</action>
  <verify>
  <automated>node --test test/smoke-snapshot.test.js 2>&1 | tail -5</automated>
  <done>smoke-snapshot test passes after snapshot update</done>
</task>

</tasks>

<verification>
All 3 documentation files are consistent and comprehensive.
smoke-snapshot test passes.
</verification>

<success_criteria>
- audit.md documents all PTES flags: --recon-light, --recon, --recon-full, --redteam, --poc
- audit.md documents all OSINT flags: --osint, --osint-full, --osint-output, --osint-timeout
- audit.vi.md has same flag documentation as audit.md
- test/smoke-snapshot.test.js passes
</success_criteria>

<output>
After completion, create `.planning/phases/124-testing-documentation/124-02-SUMMARY.md`
</output>
