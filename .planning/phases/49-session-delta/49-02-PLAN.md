---
phase: 49-session-delta
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - workflows/audit.md
autonomous: true
requirements:
  - DELTA-01
  - DELTA-02
  - DELTA-03

must_haves:
  truths:
    - "Workflow B2 doc evidence cu, chay git diff, goi classifyDelta() va truyen ket qua cho B5"
    - "Sau dispatch B5, evidence moi co commit_sha trong frontmatter"
    - "Sau dispatch B5, evidence moi co Audit History table append"
    - "Khong co evidence cu thi B2 treat nhu full scan (D-09)"
  artifacts:
    - path: "workflows/audit.md"
      provides: "B2 delta-aware logic thay stub"
      contains: "classifyDelta"
  key_links:
    - from: "workflows/audit.md B2"
      to: "bin/lib/session-delta.js"
      via: "require + classifyDelta(evidenceContent, changedFiles)"
      pattern: "classifyDelta"
    - from: "workflows/audit.md B5 post-dispatch"
      to: "bin/lib/session-delta.js"
      via: "appendAuditHistory(content, entry)"
      pattern: "appendAuditHistory"
    - from: "workflows/audit.md B2"
      to: "git CLI"
      via: "git diff --name-only {sha}..HEAD"
      pattern: "git diff --name-only"
---

<objective>
Thay stub B2 "Delta-aware (STUB)" trong workflows/audit.md bang logic delta thuc su: doc evidence cu, chay git diff, goi classifyDelta(), truyen classification cho B5 dispatch, va sau dispatch ghi commit_sha + append audit history vao evidence moi.

Purpose: Ket noi module session-delta.js (Plan 01) vao workflow audit, hoan thanh vong lap delta-aware end-to-end.
Output: workflows/audit.md cap nhat
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/49-session-delta/49-CONTEXT.md
@.planning/phases/49-session-delta/49-RESEARCH.md
@.planning/phases/49-session-delta/49-01-SUMMARY.md

@workflows/audit.md
@bin/lib/session-delta.js

<interfaces>
From bin/lib/session-delta.js (created in Plan 01):
```javascript
const DELTA_STATUS = { SKIP: 'SKIP', RESCAN: 'RE-SCAN', NEW: 'NEW', KNOWN_UNFIXED: 'KNOWN-UNFIXED' };

/**
 * @param {string} oldEvidence - Noi dung evidence file cu (full string)
 * @param {string[]} changedFiles - Danh sach file da doi tu git diff
 * @returns {{ functions: Map<string, {status: string, reason: string}>, summary: {skip: number, rescan: number, new: number, knownUnfixed: number}, isFullScan: boolean }}
 */
function classifyDelta(oldEvidence, changedFiles) { ... }

/**
 * @param {string} evidenceContent - Noi dung evidence file
 * @param {{ date: string, commit: string, verdictSummary: string, deltaSummary: string }} auditEntry
 * @returns {string} - Evidence content da cap nhat voi dong history moi
 */
function appendAuditHistory(evidenceContent, auditEntry) { ... }

function parseAuditHistory(evidenceContent) { ... }

module.exports = { classifyDelta, appendAuditHistory, parseAuditHistory, DELTA_STATUS };
```

From bin/lib/utils.js:
```javascript
function parseFrontmatter(content) { /* tra ve { frontmatter, body, raw } */ }
```

Workflow audit.md hien tai:
- B2 la stub: "Phien ban hien tai chua ho tro delta-aware scanning"
- B5 dispatch scanners voi categories_to_scan
- B9 save report
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thay stub B2 bang delta logic + them post-dispatch audit history</name>
  <files>workflows/audit.md</files>
  <read_first>
    workflows/audit.md
    bin/lib/session-delta.js
    .planning/phases/49-session-delta/49-CONTEXT.md
  </read_first>
  <action>
    Cap nhat workflows/audit.md — thay the section `## Buoc 2: Delta-aware (STUB)` bang logic thuc:

    **B2 moi — "Buoc 2: Delta-aware":**

    1. Xoa toan bo noi dung cu cua B2 (dong "> Phien ban hien tai..." va dong "Ghi {session_dir}/02-delta.md...")
    2. Thay bang logic sau (markdown instructions cho workflow executor):

    ```markdown
    ## Buoc 2: Delta-aware

    Phan loai ham tu evidence phien cu de chi quet lai nhung gi thay doi.

    1. Tim evidence cu trong output_dir:
       ```bash
       ls ${OUTPUT_DIR}/evidence_sec_*.md 2>/dev/null
       ```
    2. Neu KHONG co evidence cu nao:
       - delta_mode = "full-scan"
       - Log: "Khong tim thay evidence phien cu — chay full scan"
       - Ghi `{session_dir}/02-delta.md` voi: status=full-scan, reason="no prior evidence"
       - Chuyen qua B3

    3. Neu CO evidence cu:
       a. Voi MOI evidence file `evidence_sec_{cat}.md`:
          - Doc noi dung file bang Read
          - Parse frontmatter lay commit_sha:
            ```bash
            node -e "const {parseFrontmatter}=require('./bin/lib/utils');const fm=parseFrontmatter(require('fs').readFileSync('${OUTPUT_DIR}/evidence_sec_${cat}.md','utf8'));console.log(fm.frontmatter.commit_sha||'')"
            ```
          - Neu co commit_sha:
            ```bash
            git diff --name-only ${COMMIT_SHA}..HEAD
            ```
            Truyen ket qua (danh sach files) vao changedFiles
          - Neu KHONG co commit_sha (evidence cu tu truoc Phase 49):
            Treat nhu full scan cho category nay — classifyDelta('', [])
       b. Goi classifyDelta:
          ```bash
          node -e "
            const {classifyDelta}=require('./bin/lib/session-delta');
            const evidence=require('fs').readFileSync('${EVIDENCE_PATH}','utf8');
            const changed=${CHANGED_FILES_JSON};
            const result=classifyDelta(evidence, changed);
            // Convert Map to object for JSON output
            const fns=Object.fromEntries(result.functions);
            console.log(JSON.stringify({...result, functions: fns}));
          "
          ```
       c. Luu ket qua classification vao `{session_dir}/02-delta.md`:
          - Per category: so ham SKIP, RE-SCAN, KNOWN-UNFIXED
          - Tong: delta_mode="delta", summary counts

    4. Truyen classification results cho B5:
       - B5 dispatch scanner voi bo sung context: danh sach ham can scan lai (RE-SCAN) va ham moi (NEW)
       - Ham SKIP va KNOWN-UNFIXED khong can quet lai — scanner nhan danh sach nay de bo qua
    ```

    **Them doan post-dispatch vao SAU B5 (truoc B6):**

    Them section `## Buoc 5b: Cap nhat evidence metadata` ngay sau B5:

    ```markdown
    ## Buoc 5b: Cap nhat evidence metadata

    Sau khi TATTt CA waves dispatch hoan tat (B5 xong):

    1. Lay commit SHA hien tai:
       ```bash
       CURRENT_SHA=$(git rev-parse --short HEAD)
       ```

    2. Voi MOI evidence file moi tu B5 (trong session_dir/03-dispatch/):
       a. Doc noi dung evidence file
       b. Them/cap nhat `commit_sha: ${CURRENT_SHA}` vao frontmatter YAML:
          - Neu evidence co frontmatter (---...---): them truong commit_sha
          - Neu khong co: tao frontmatter moi voi commit_sha
       c. Goi appendAuditHistory de them dong history:
          ```bash
          node -e "
            const {appendAuditHistory}=require('./bin/lib/session-delta');
            const content=require('fs').readFileSync('${EVIDENCE_PATH}','utf8');
            const entry={
              date: new Date().toISOString().split('T')[0],
              commit: '${CURRENT_SHA}',
              verdictSummary: '${PASS_COUNT} PASS, ${FLAG_COUNT} FLAG, ${FAIL_COUNT} FAIL',
              deltaSummary: '${NEW_COUNT} new, ${RESCAN_COUNT} re-scan, ${SKIP_COUNT} skip'
            };
            const updated=appendAuditHistory(content, entry);
            require('fs').writeFileSync('${EVIDENCE_PATH}', updated);
          "
          ```
       d. Copy evidence file ra output_dir (ghi de file cu)

    3. Log: "Evidence metadata cap nhat: commit_sha=${CURRENT_SHA}, {N} files co audit history"
    ```

    **Cap nhat <purpose> tag** o dau file: them session-delta.js vao danh sach tham chieu:
    Them vao dong Tham chieu: `bin/lib/session-delta.js (classifyDelta, appendAuditHistory)`

    **KHONG thay doi** cac buoc khac (B1, B3, B4, B6, B7, B8, B9) — chi thay B2 stub va them B5b.
  </action>
  <verify>
    <automated>grep -c "classifyDelta" workflows/audit.md | grep -q '[1-9]' && grep -c "appendAuditHistory" workflows/audit.md | grep -q '[1-9]' && grep -c "STUB" workflows/audit.md | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/audit.md KHONG con chuoi "STUB" (stub B2 da bi thay the)
    - workflows/audit.md co chuoi "classifyDelta" it nhat 1 lan
    - workflows/audit.md co chuoi "appendAuditHistory" it nhat 1 lan
    - workflows/audit.md co chuoi "git diff --name-only" it nhat 1 lan
    - workflows/audit.md co chuoi "commit_sha" it nhat 2 lan (parse cu + ghi moi)
    - workflows/audit.md co chuoi "session-delta" it nhat 1 lan (trong require hoac tham chieu)
    - workflows/audit.md co chuoi "Buoc 5b" hoac "B5b" (section post-dispatch moi)
    - workflows/audit.md van co day du 9 buoc chinh (B1 den B9) — B5b la buoc phu
    - Dong purpose/tham chieu co "session-delta.js"
  </acceptance_criteria>
  <done>
    Stub B2 da thay bang delta logic thuc: doc evidence cu, parse commit_sha, chay git diff, goi classifyDelta(). B5b moi ghi commit_sha vao frontmatter va append audit history sau dispatch. Workflow audit.md tham chieu session-delta.js.
  </done>
</task>

</tasks>

<verification>
grep "classifyDelta" workflows/audit.md
grep "appendAuditHistory" workflows/audit.md
grep -c "STUB" workflows/audit.md  # phai la 0 (con stub B8 nhung do la Phase 50)
node --test test/smoke-session-delta.test.js
</verification>

<success_criteria>
- B2 stub thay bang delta-aware logic goi classifyDelta + git diff
- B5b moi ghi commit_sha + audit history vao evidence files
- Khong lam hu cac buoc khac (B1, B3-B9)
- Module session-delta.js duoc tham chieu dung trong workflow
</success_criteria>

<output>
After completion, create `.planning/phases/49-session-delta/49-02-SUMMARY.md`
</output>
