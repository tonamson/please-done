---
phase: 112-ptes-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/recon-cache.js
  - bin/lib/flag-parser.js
  - bin/lib/recon-cache.test.js
  - bin/lib/flag-parser.test.js
  - .planning/recon-cache/.gitkeep
autonomous: true
requirements:
  - PTES-02
  - PTES-03
must_haves:
  truths:
    - User can parse --recon, --poc, --redteam flags from command arguments
    - Reconnaissance cache stores data keyed by git commit + file list hash
    - Cache automatically invalidates when code changes
    - Cache implements LRU eviction with 50 entry limit
    - Unit tests verify cache hit/miss and flag parsing behavior
  artifacts:
    - path: bin/lib/recon-cache.js
      provides: ReconCache class with get/set/getKey methods
      exports: [ReconCache]
    - path: bin/lib/flag-parser.js
      provides: parsePtesFlags function
      exports: [parsePtesFlags]
    - path: .planning/recon-cache/
      provides: Cache storage directory
  key_links:
    - from: bin/lib/recon-cache.js
      to: .planning/recon-cache/
      via: getCachePath method
      pattern: path.join.*recon-cache
    - from: flag-parser.js
      to: commands/pd/audit.md
      via: import and call
      pattern: parsePtesFlags\(
---

<objective>
Create foundation libraries for PTES workflow: reconnaissance cache system and flag parser.

Purpose: Enable token optimization through caching and support new PTES command flags (--recon, --poc, --redteam).

Output: bin/lib/recon-cache.js, bin/lib/flag-parser.js with unit tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/112-ptes-foundation/112-CONTEXT.md
@.planning/phases/112-ptes-foundation/112-RESEARCH.md
@commands/pd/audit.md
@workflows/audit.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create recon-cache.js library</name>
  <files>bin/lib/recon-cache.js, bin/lib/recon-cache.test.js</files>
  <read_first>
    - .planning/phases/112-ptes-foundation/112-RESEARCH.md (Cache System Implementation section)
    - .planning/phases/112-ptes-foundation/112-CONTEXT.md (D-08, D-09, D-10, D-11 decisions)
  </read_first>
  <behavior>
    - Test 1: getKey() returns consistent hash for same git state
    - Test 2: get() returns null when cache miss
    - Test 3: set() stores data and get() retrieves it on cache hit
    - Test 4: Cache entry includes version "1.0", createdAt timestamp, gitCommit
    - Test 5: Corrupted cache files are handled gracefully (returns null, logs warning)
    - Test 6: LRU eviction removes oldest entries when >50 files
  </behavior>
  <action>
    Create bin/lib/recon-cache.js implementing the ReconCache class per D-08, D-09, D-10:

    1. Class ReconCache with constructor accepting optional cacheDir parameter (default: .planning/recon-cache)
    2. getKey() method: Generate MD5 hash of "git rev-parse HEAD" + "git ls-files" output (per D-08)
    3. get() method: Check cache, log "[Token Save] Reusing cached recon (0 AI tokens)" on hit (per D-11), return null on miss or corruption
    4. set() method: Store data with version "1.0", createdAt ISO timestamp, gitCommit SHA, and data payload; use atomic write (temp file + rename)
    5. _evictIfNeeded() private method: Implement LRU eviction when files > 50, sort by atime, delete oldest
    6. _ensureDir() private method: Create cache directory recursively if not exists

    Cache entry format:
    ```json
    {
      "version": "1.0",
      "createdAt": "2026-04-05T12:00:00.000Z",
      "gitCommit": "abc123...",
      "data": { ... recon data ... }
    }
    ```

    Per RESEARCH.md Common Pitfalls section:
    - Use atomic writes (write to .tmp file, then rename)
    - Handle corrupted JSON gracefully with try/catch
    - Include file list in hash key (not just commit SHA)
    - Implement LRU eviction to prevent unbounded growth

    Create bin/lib/recon-cache.test.js with tests covering all behavior cases above.
  </action>
  <verify>
    <automated>node bin/lib/recon-cache.test.js</automated>
  </verify>
  <done>
    - All 6 tests pass
    - recon-cache.js exports { ReconCache }
    - Cache directory .planning/recon-cache/ created with .gitkeep
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create flag-parser.js library</name>
  <files>bin/lib/flag-parser.js, bin/lib/flag-parser.test.js</files>
  <read_first>
    - .planning/phases/112-ptes-foundation/112-RESEARCH.md (Flag Parser Extension section)
    - .planning/phases/112-ptes-foundation/112-CONTEXT.md (D-04, D-05, D-06, D-12, D-13, D-14, D-15 decisions)
    - workflows/audit.md (existing --poc flag parsing pattern)
  </read_first>
  <behavior>
    - Test 1: No flags → returns { tier: "none", recon: false, poc: false, redteam: false, tokenBudget: 0 }
    - Test 2: --recon → returns { tier: "standard", recon: true, tokenBudget: 2000 }
    - Test 3: --recon-light → returns { tier: "free", recon: true, tokenBudget: 0 }
    - Test 4: --recon-full → returns { tier: "deep", recon: true, tokenBudget: 6000 }
    - Test 5: --redteam → returns { tier: "redteam", recon: true, poc: true, redteam: true, tokenBudget: 8000 }
    - Test 6: --poc → returns { poc: true } with other defaults
    - Test 7: Flag priority: --redteam > --recon-full > --recon > --recon-light
    - Test 8: Combined flags: --recon --poc → recon: true, poc: true, tier: "standard"
  </behavior>
  <action>
    Create bin/lib/flag-parser.js implementing parsePtesFlags per D-04, D-05, D-06, D-12-D-15:

    1. Function parsePtesFlags(args) accepting string argument
    2. Return object with: tier (string), recon (boolean), poc (boolean), redteam (boolean), tokenBudget (number)
    3. Flag priority (highest to lowest): --redteam > --recon-full > --recon > --recon-light
    4. Tier mapping per decisions:
       - --recon-light → tier: "free", recon: true, tokenBudget: 0 (D-12)
       - --recon → tier: "standard", recon: true, tokenBudget: 2000 (D-13)
       - --recon-full → tier: "deep", recon: true, tokenBudget: 6000 (D-14)
       - --redteam → tier: "redteam", recon: true, poc: true, redteam: true, tokenBudget: 8000 (D-15)
    5. --poc flag sets poc: true independently
    6. Use word boundary regex (\b) to avoid partial matches
    7. Maintain backward compatibility: default /pd:audit returns tier: "none", all flags false

    Per RESEARCH.md Common Pitfalls:
    - Define explicit flag priority to avoid conflicts
    - Keep existing --poc behavior (DAST verification) unchanged
    - Don't break default /pd:audit behavior

    Create bin/lib/flag-parser.test.js with tests covering all 8 behavior cases above.
  </action>
  <verify>
    <automated>node bin/lib/flag-parser.test.js</automated>
  </verify>
  <done>
    - All 8 tests pass
    - flag-parser.js exports { parsePtesFlags }
    - Tier mapping matches D-12 through D-15 exactly
  </done>
</task>

<task type="auto">
  <name>Task 3: Create cache directory structure</name>
  <files>.planning/recon-cache/.gitkeep</files>
  <read_first>
    - .planning/phases/112-ptes-foundation/112-CONTEXT.md (D-09: cache location)
  </read_first>
  <action>
    Create .planning/recon-cache/ directory with .gitkeep file:

    1. Ensure .planning/ directory exists (it should from prior phases)
    2. Create subdirectory recon-cache/
    3. Create .gitkeep file to ensure directory is tracked in git
    4. Directory will be used by ReconCache class for storing cache files

    Per D-09: Cache location is .planning/recon-cache/{key}.json
  </action>
  <verify>
    <automated>test -d .planning/recon-cache && test -f .planning/recon-cache/.gitkeep && echo "Cache directory created"</automated>
  </verify>
  <done>
    - Directory .planning/recon-cache/ exists
    - File .planning/recon-cache/.gitkeep exists
    - Directory is git-tracked
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → flag-parser | Untrusted command arguments parsed for flags |
| File system → recon-cache | Cache files read from .planning/recon-cache/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | recon-cache.js | mitigate | Atomic writes (temp + rename), version validation in cache entries |
| T-112-02 | Tampering | recon-cache.js | mitigate | Path normalization via path.join(), whitelist .json extension only |
| T-112-03 | Denial of Service | recon-cache.js | mitigate | LRU eviction with 50 entry max (per Common Pitfall 3) |
| T-112-04 | Information Disclosure | recon-cache.js | accept | Cache contains recon data in project directory only, same trust boundary as source code |
| T-112-05 | Elevation of Privilege | flag-parser.js | accept | Flag parsing has no privilege effects, just configuration |
</threat_model>

<verification>
## Overall Phase Verification

1. Library files exist and export correct interfaces
2. Unit tests pass for both libraries
3. Cache directory created and tracked
4. Tier mapping matches requirements PTES-02, PTES-03

## Automated Verification

```bash
node bin/lib/recon-cache.test.js && node bin/lib/flag-parser.test.js && echo "All tests pass"
```
</verification>

<success_criteria>
- bin/lib/recon-cache.js exists with ReconCache class exporting get(), set(), getKey() methods
- bin/lib/flag-parser.js exists with parsePtesFlags function
- Both libraries have passing unit tests
- Cache directory .planning/recon-cache/ exists with .gitkeep
- Token optimization: 0 tokens for cache operations (code-only)
</success_criteria>

<output>
After completion, create `.planning/phases/112-ptes-foundation/112-01-SUMMARY.md`
</output>
