---
phase: 44-wire-route-query-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/route-query.js
  - workflows/research.md
autonomous: true
requirements:
  - STORE-04
must_haves:
  truths:
    - "pd research su dung routeQuery() de phan loai internal/external thay vi inline heuristic"
    - "CLI script bin/route-query.js in dung ket qua 'internal' hoac 'external' ra stdout"
    - "Workflow khong con inline routing logic — chi goi CLI script"
    - "Snapshot tests pass sau khi workflow thay doi"
  artifacts:
    - path: "bin/route-query.js"
      provides: "CLI wrapper cho routeQuery()"
      contains: "routeQuery"
    - path: "workflows/research.md"
      provides: "Workflow da cap nhat voi CLI call"
      contains: "node bin/route-query.js"
  key_links:
    - from: "bin/route-query.js"
      to: "bin/lib/research-store.js"
      via: "require('./lib/research-store')"
      pattern: "require.*research-store"
    - from: "workflows/research.md"
      to: "bin/route-query.js"
      via: "node bin/route-query.js"
      pattern: "node bin/route-query\\.js"
---

<objective>
Thay the inline routing heuristic trong workflows/research.md Buoc 1 bang CLI script goi routeQuery() tu research-store.js. Dam bao 10+ regex patterns duoc su dung thay vi ~5 luat inline.

Purpose: Dong gap giua routeQuery() (da implement Phase 42 voi 20 tests) va workflow thuc te — hien workflow van dung inline heuristic cu.
Output: bin/route-query.js (CLI wrapper) + workflows/research.md cap nhat + snapshots regenerated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-wire-route-query-workflow/44-CONTEXT.md

<interfaces>
<!-- routeQuery export tu research-store.js -->
From bin/lib/research-store.js (line 386-398):
```javascript
module.exports = {
  CONFIDENCE_LEVELS,
  CONFIDENCE_CRITERIA,
  REQUIRED_FIELDS,
  // ... other exports ...
  routeQuery,  // line 398
};
```

From bin/lib/research-store.js (line 353-358):
```javascript
function routeQuery(query) {
  if (typeof query !== 'string' || !query.trim()) {
    return 'external'; // D-03: fallback external
  }
  const q = query.trim();
  // ... 10+ regex patterns ...
}
```

<!-- CLI pattern tu Phase 43 -->
From bin/update-research-index.js (line 1-17):
```javascript
#!/usr/bin/env node
'use strict';
const fs = require('fs');
const path = require('path');
const { parseResearchFiles, generateIndex } = require('./lib/index-generator');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tao CLI script bin/route-query.js va cap nhat workflow</name>
  <files>bin/route-query.js, workflows/research.md</files>
  <read_first>
    - bin/update-research-index.js (CLI pattern template — follow shebang, 'use strict', require pattern)
    - bin/lib/research-store.js (line 353-398 — routeQuery function va exports, xac nhan export name)
    - workflows/research.md (full file — hieu Buoc 1 hien tai dong 12-21 de thay the chinh xac)
  </read_first>
  <action>
**Phan A — Tao bin/route-query.js (per D-01, D-02):**

Tao file moi `bin/route-query.js` voi noi dung chinh xac:

```javascript
#!/usr/bin/env node
'use strict';

/**
 * CLI script phan loai research query thanh internal hoac external.
 *
 * Nhan 1 argument (topic string), goi routeQuery() tu research-store.js,
 * in ket qua ra stdout ('internal' hoac 'external').
 *
 * Usage: node bin/route-query.js "ten file hoac thu vien"
 */

const { routeQuery } = require('./lib/research-store');

const topic = process.argv[2] || '';
const result = routeQuery(topic);
console.log(result);
```

- Shebang line `#!/usr/bin/env node` PHAI la dong dau tien
- `'use strict'` dong thu 2
- require dung relative path `'./lib/research-store'` (khong phai absolute)
- Khong them validation phuc tap — routeQuery da handle empty string -> 'external'
- Chmod 755 cho file: `chmod +x bin/route-query.js`

**Phan B — Cap nhat workflows/research.md Buoc 1 (per D-05, D-06):**

Thay the NOI DUNG Buoc 1 (dong 12-21 hien tai) thanh:

```markdown
## Buoc 1: Phan loai query

1. Lay topic tu $ARGUMENTS (full text user nhap).
2. Xac dinh thu muc research tuyet doi: `path.resolve('.planning/research')`.
3. Phan loai topic:
   ```
   ROUTE=$(node bin/route-query.js "$TOPIC")
   ```
4. Hien thi: `"Da phan loai: [internal|external] research"`
5. Xac dinh thu muc con: `{research_dir}/internal/` hoac `{research_dir}/external/`
```

XOA HOAN TOAN inline heuristic (dong 16-18 cu co "Neu topic chua ten file..." va "Con lai -> external"). Khong giu comment lich su (per D-05).

GIU NGUYEN tat ca Buoc 2, 3, 4 va phan `<rules>` — chi sua Buoc 1.
  </action>
  <verify>
    <automated>node bin/route-query.js "ham createUser" && node bin/route-query.js "React hooks" && node --test test/smoke-research-store.test.js</automated>
  </verify>
  <acceptance_criteria>
    - bin/route-query.js ton tai va co shebang `#!/usr/bin/env node` dong dau tien
    - bin/route-query.js chua `require('./lib/research-store')` — khong phai path khac
    - bin/route-query.js chua `routeQuery` — goi function tu library
    - `node bin/route-query.js "ham createUser"` in ra `internal`
    - `node bin/route-query.js "React hooks"` in ra `external`
    - `node bin/route-query.js` (khong argument) in ra `external`
    - workflows/research.md chua `node bin/route-query.js "$TOPIC"` trong Buoc 1
    - workflows/research.md KHONG chua `Neu topic chua ten file` (inline heuristic da xoa)
    - workflows/research.md KHONG chua `camelCase, PascalCase` trong Buoc 1 (inline heuristic da xoa)
    - workflows/research.md van chua `Buoc 2: Thu thap bang chung` (Buoc 2 khong bi sua)
    - workflows/research.md van chua `node bin/update-research-index.js` (Buoc 4 khong bi sua)
    - `node --test test/smoke-research-store.test.js` exit code 0
  </acceptance_criteria>
  <done>CLI script bin/route-query.js hoat dong dung (internal cho codebase queries, external cho library queries), workflow Buoc 1 chi goi CLI thay vi inline heuristic, routeQuery tests van pass.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate converter snapshots</name>
  <files>test/snapshots/</files>
  <read_first>
    - test/generate-snapshots.js (hieu cach script hoat dong — no doc workflows/ va tao snapshots/)
    - test/smoke-snapshot.test.js (hieu test so sanh snapshot nhu the nao — de biet loi neu co)
  </read_first>
  <action>
Workflow `research.md` da thay doi o Task 1 — converter snapshots cho 4 platforms (codex, copilot, gemini, opencode) se outdated.

1. Chay regenerate snapshots:
   ```
   node test/generate-snapshots.js
   ```

2. Chay snapshot tests de xac nhan:
   ```
   node --test test/smoke-snapshot.test.js
   ```

3. Neu co loi, doc output de hieu file nao fail va fix.

Luu y: Script `generate-snapshots.js` se tu dong overwrite cac file trong `test/snapshots/`. Chi can chay va verify tests pass.
  </action>
  <verify>
    <automated>node --test test/smoke-snapshot.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/smoke-snapshot.test.js` exit code 0 — tat ca 52 snapshot tests pass
    - Thu muc test/snapshots/ co cac file snapshot cho research workflow duoc cap nhat (modified time sau Task 1)
    - Snapshot files chua `node bin/route-query.js` (phan anh workflow moi) — kiem tra bang grep trong bat ky snapshot file nao chua "research"
  </acceptance_criteria>
  <done>Tat ca 52 snapshot tests pass voi noi dung workflow moi. Pipeline converter nhan dung thay doi tu Buoc 1 cua research workflow.</done>
</task>

</tasks>

<verification>
Chay full test suite xac nhan khong co regression:

```bash
node --test test/smoke-research-store.test.js test/smoke-snapshot.test.js
```

Tat ca tests phai pass (20+ routeQuery tests + 52 snapshot tests).

Kiem tra end-to-end:
```bash
# CLI script hoat dong
node bin/route-query.js "file utils.js" # -> internal
node bin/route-query.js "GraphQL API"   # -> external

# Workflow khong con inline heuristic
grep -c "camelCase" workflows/research.md # -> 0 (da xoa)
grep -c "route-query.js" workflows/research.md # -> 1+ (da them)
```
</verification>

<success_criteria>
1. bin/route-query.js ton tai, chay dung, tra ve 'internal' hoac 'external' theo routeQuery() logic
2. workflows/research.md Buoc 1 goi CLI script thay vi inline heuristic — khong con dong nao chua routing logic cu
3. Tat ca tests pass: routeQuery unit tests (20+) va snapshot tests (52)
4. STORE-04 duoc dam bao: pd research su dung routeQuery() (10+ patterns) thay vi inline (~5 rules)
</success_criteria>

<output>
Sau khi hoan thanh, tao `.planning/phases/44-wire-route-query-workflow/44-01-SUMMARY.md`
</output>
