---
phase: 50-poc-fix-phases
plan: 02
type: execute
wave: 2
depends_on:
  - "50-01"
files_modified:
  - commands/pd/agents/pd-sec-fixer.md
  - commands/pd/agents/pd-sec-scanner.md
  - commands/pd/agents/pd-sec-reporter.md
  - templates/security-fix-phase.md
  - workflows/audit.md
  - bin/lib/resource-config.js
  - test/smoke-agent-files.test.js
  - test/smoke-resource-config.test.js
autonomous: true
requirements:
  - POC-01
  - POC-02
  - FIX-01
  - FIX-02
  - FIX-03

must_haves:
  truths:
    - "Scanner tao section ## POC khi --poc active va finding la FAIL/FLAG (per D-01, D-02, D-03)"
    - "POC co 4 truong bat buoc: input vector, payload mau, buoc tai hien, ket qua du kien (per D-04)"
    - "Reporter B6 goi detectChains() tu gadget-chain.js va ghi ket qua vao report (per D-05)"
    - "pd-sec-fixer agent tao fix phases proposal tu SECURITY_REPORT + gadget chains (per D-14, D-15)"
    - "Fix phases decimal N.1, N.2... voi SEC-VERIFY la phase cuoi (per D-10, D-13)"
    - "Template security-fix-phase.md co evidence trich dan, huong sua, tieu chi hoan thanh (per D-11)"
    - "B8 trong audit.md dispatch pd-sec-fixer thay vi stub (per D-14)"
    - "B3 trong audit.md truyen --poc cho scanners thay vi bao chua ho tro (per D-03)"
  artifacts:
    - path: "commands/pd/agents/pd-sec-fixer.md"
      provides: "Agent template phan tich findings va tao fix phases proposal"
      contains: "pd-sec-fixer"
    - path: "templates/security-fix-phase.md"
      provides: "Template cho moi fix phase decimal"
      contains: "Evidence goc"
    - path: "commands/pd/agents/pd-sec-scanner.md"
      provides: "Scanner template mo rong voi POC logic"
      contains: "## POC"
    - path: "commands/pd/agents/pd-sec-reporter.md"
      provides: "Reporter template mo rong voi gadget chain detection"
      contains: "detectChains"
    - path: "workflows/audit.md"
      provides: "B3 truyen --poc, B8 dispatch pd-sec-fixer"
    - path: "bin/lib/resource-config.js"
      provides: "pd-sec-fixer dang ky trong AGENT_REGISTRY"
      contains: "pd-sec-fixer"
  key_links:
    - from: "commands/pd/agents/pd-sec-reporter.md"
      to: "bin/lib/gadget-chain.js"
      via: "B6 goi detectChains() bang Bash node -e"
      pattern: "detectChains"
    - from: "workflows/audit.md"
      to: "commands/pd/agents/pd-sec-fixer.md"
      via: "B8 dispatch pd-sec-fixer agent"
      pattern: "pd-sec-fixer"
    - from: "commands/pd/agents/pd-sec-fixer.md"
      to: "templates/security-fix-phase.md"
      via: "Fixer dung template de tao fix phases"
      pattern: "security-fix-phase"
    - from: "workflows/audit.md"
      to: "commands/pd/agents/pd-sec-scanner.md"
      via: "B5 dispatch voi --poc flag"
      pattern: "poc"
---

<objective>
Tao pd-sec-fixer agent, security-fix-phase template, wire POC vao scanner, wire gadget chain vao reporter B6, wire fixer dispatch vao audit B8.

Purpose: Hoan thien 3 component con thieu cua pipeline audit: POC generation (--poc), gadget chain cross-category analysis, va tu dong tao fix phases. Thay the stub B8 trong audit.md.
Output: 4 files moi (agent, template) + 4 files sua (scanner, reporter, audit, resource-config) + 2 test extensions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-poc-fix-phases/50-CONTEXT.md
@.planning/phases/50-poc-fix-phases/50-RESEARCH.md
@.planning/phases/50-poc-fix-phases/50-01-SUMMARY.md

@commands/pd/agents/pd-sec-scanner.md (sua: them POC logic)
@commands/pd/agents/pd-sec-reporter.md (sua: them gadget chain B6)
@workflows/audit.md (sua: B3 --poc, B8 fixer dispatch)
@bin/lib/resource-config.js (sua: them pd-sec-fixer)
@bin/lib/gadget-chain.js (tu Plan 01 — detectChains, escalateSeverity, orderFixPriority)
@references/gadget-chain-templates.yaml (tu Plan 01 — 7 chain patterns)
@references/security-rules.yaml (fixes[] cho template)
@bin/lib/session-delta.js (classifyDelta cho SEC-VERIFY)
@templates/ (pattern cho security-fix-phase.md)

<interfaces>
<!-- Tu Plan 01: gadget-chain.js exports -->
Tu bin/lib/gadget-chain.js (Plan 01 output):
```javascript
const SEVERITY_ORDER = ['LOW', 'MEDIUM', 'HIGH', 'CRITICAL'];
function detectChains(findings, templates) { ... }
// findings = [{category, file, name, verdict, severity}, ...]
// templates = [{id, name, links: [{from_cat, to_cat, condition}], root, escalation}, ...]
// return { chains: [{id, name, root, findings, originalSeverities, escalatedSeverity}], linkedFindingKeys: string[] }

function escalateSeverity(severities) { ... }
// return: severity string (cap tai CRITICAL)

function orderFixPriority(chains) { ... }
// return: chains sorted by root-first priority, voi priority field

module.exports = { detectChains, escalateSeverity, orderFixPriority, SEVERITY_ORDER };
```

Tu bin/lib/session-delta.js:
```javascript
module.exports = { classifyDelta, appendAuditHistory, parseAuditHistory, DELTA_STATUS };
// classifyDelta(oldEvidence, changedFiles) → Map<string, {status, verdict, severity}>
```

Tu bin/lib/resource-config.js (AGENT_REGISTRY — them pd-sec-fixer):
```javascript
const AGENT_REGISTRY = {
  // ... existing agents ...
  "pd-sec-scanner": { tier: "scout", tools: [...], categories: [...] },
  "pd-sec-reporter": { tier: "builder", tools: [...] },
  // THEM:
  "pd-sec-fixer": { tier: "architect", tools: ["Read", "Write", "Glob", "Grep"] },
};
```

Tu test/smoke-agent-files.test.js (pattern extend):
```javascript
const AGENT_NAMES = [
  'pd-bug-janitor', 'pd-code-detective', 'pd-doc-specialist',
  'pd-repro-engineer', 'pd-fix-architect', 'pd-evidence-collector', 'pd-fact-checker',
  // THEM: 'pd-sec-fixer'
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tao pd-sec-fixer agent + security-fix-phase template + dang ky AGENT_REGISTRY</name>
  <files>commands/pd/agents/pd-sec-fixer.md, templates/security-fix-phase.md, bin/lib/resource-config.js</files>
  <action>
    **1a. Tao commands/pd/agents/pd-sec-fixer.md (per D-14, D-15)**

    Agent template theo pattern pd-sec-scanner.md. Frontmatter YAML flat:
    ```yaml
    ---
    name: pd-sec-fixer
    description: Phan tich findings bao mat va tao de xuat fix phases decimal theo thu tu uu tien.
    tier: architect
    allowed-tools:
      - Read
      - Write
      - Glob
      - Grep
    ---
    ```

    Noi dung agent (prose thuc thi boi AI agent):
    - **Input**: Nhan duong dan SECURITY_REPORT.md + evidence files + session_dir. Nhan mode: "tich-hop" (co .planning/) hoac "doc-lap" (khong co .planning/).
    - **Buoc 1**: Doc SECURITY_REPORT.md, extract findings co verdict FAIL/FLAG.
    - **Buoc 2**: Doc gadget-chain-templates.yaml. Chay detectChains() bang Bash:
      ```
      node -e "const {detectChains}=require('./bin/lib/gadget-chain'); const fs=require('fs'); ..."
      ```
    - **Buoc 3**: Chay orderFixPriority() de sap thu tu fix.
    - **Buoc 4**: Tao fix phases proposal (output format tu RESEARCH):
      - Bang gadget chains phat hien
      - Bang fix phases de xuat voi numbering decimal N.1, N.2... (per D-10)
      - SEC-VERIFY la phase cuoi cung (per D-13)
    - **Buoc 5 (mode tich-hop)**: Hien thi proposal, hoi user approve (per D-08: KHONG tu dong sua ROADMAP). Khi user approve → dung Write tool de append fix phases vao ROADMAP.md.
    - **Buoc 5 (mode doc-lap)**: Ghi proposal vao session_dir/08-fix-proposal.md. In tom tat ra output.
    - **SEC-VERIFY**: Phase cuoi dung classifyDelta() voi danh sach files da sua tu cac fix phases truoc (per D-12). Chi scan lai files da fix.

    Toan bo prose bang tieng Viet co dau.

    **1b. Tao templates/security-fix-phase.md (per D-11, FIX-02)**

    Template theo RESEARCH Pattern 5. Cac section bat buoc:
    1. **Evidence goc** — bang findings voi file, dong, severity, category. Trich dan tu evidence file.
    2. **Gadget Chain (neu co)** — chain name, vi tri trong chain, tac dong khi fix.
    3. **Huong sua** — huong sua cu the tu security-rules.yaml fixes[]. Code pattern truoc (nguy hiem) va sau (an toan).
    4. **Files can sua** — bang files voi ham, dong, hanh dong cu the.
    5. **Tieu chi hoan thanh** — checklist cu the, ket thuc bang "chay scanner category {cat} tren files da sua → verdict PASS".

    Dung placeholder {N.X}, {ten_lo_hong}, {category}, {severity} cho fixer dien vao.

    **1c. Them pd-sec-fixer vao AGENT_REGISTRY trong bin/lib/resource-config.js**

    Them entry moi vao AGENT_REGISTRY (per D-14: tier architect vi phan tich complex):
    ```javascript
    "pd-sec-fixer": {
      tier: "architect",
      tools: ["Read", "Write", "Glob", "Grep"],
    },
    ```

    Dat SAU entry pd-sec-reporter (giu thu tu logic: scanner → reporter → fixer).
  </action>
  <verify>
    <automated>node -e "const {AGENT_REGISTRY}=require('./bin/lib/resource-config'); if(!AGENT_REGISTRY['pd-sec-fixer']) throw new Error('missing'); console.log('OK:', AGENT_REGISTRY['pd-sec-fixer'].tier)" && test -f commands/pd/agents/pd-sec-fixer.md && test -f templates/security-fix-phase.md && echo "ALL OK"</automated>
  </verify>
  <done>
    - pd-sec-fixer.md co frontmatter dung (name, tier, allowed-tools) va process 5 buoc
    - security-fix-phase.md co 5 sections bat buoc (Evidence, Gadget Chain, Huong sua, Files, Tieu chi)
    - AGENT_REGISTRY co pd-sec-fixer entry voi tier architect
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire POC vao scanner + gadget chain vao reporter B6 + fixer dispatch vao audit B8</name>
  <files>commands/pd/agents/pd-sec-scanner.md, commands/pd/agents/pd-sec-reporter.md, workflows/audit.md</files>
  <action>
    **2a. Sua commands/pd/agents/pd-sec-scanner.md — them POC logic (per D-01, D-02, D-03, D-04)**

    Them section moi vao scanner template, SAU phan ghi evidence findings:

    ```markdown
    ## POC (chi khi --poc active)

    Neu nhan duoc flag --poc trong prompt:
    - Voi MOI finding co verdict FAIL hoac FLAG:
      - Them section ## POC vao cuoi evidence file (per D-02: moi thu 1 file)
      - Moi POC co 4 truong bat buoc (per D-04):
        1. **Input vector**: mo ta diem vao — endpoint, parameter, header
        2. **Payload mau**: payload cu the (TEXT ONLY — KHONG tao script chay that per D-04)
        3. **Buoc tai hien**: 3-5 buoc mo ta
        4. **Ket qua du kien**: mo ta hanh vi nguy hiem xay ra
      - Severity cua finding
    - Neu KHONG nhan --poc: KHONG tao POC section (per D-03)
    ```

    Format POC theo RESEARCH Pattern 4.

    **2b. Sua commands/pd/agents/pd-sec-reporter.md — them gadget chain B6 (per D-05, D-06)**

    Trong buoc tong hop (khi reporter da doc tat ca evidence files), THEM logic:

    ```markdown
    ## Buoc 6: Gadget Chain Detection

    Gadget chain detection LUON chay (khong phu thuoc --poc flag — per D-05).

    1. Doc `references/gadget-chain-templates.yaml` bang Read tool
    2. Thu thap tat ca findings FAIL/FLAG tu cac evidence files da doc
    3. Chay detectChains() bang Bash:
       ```bash
       node -e "
         const {detectChains,orderFixPriority}=require('./bin/lib/gadget-chain');
         const findings = $FINDINGS_JSON;
         const templates = $TEMPLATES_PARSED;
         const result = detectChains(findings, templates);
         const ordered = orderFixPriority(result.chains);
         console.log(JSON.stringify({chains: ordered, linkedKeys: result.linkedFindingKeys}));
       "
       ```
    4. Neu co chains: ghi section "## Gadget Chains" vao SECURITY_REPORT.md voi:
       - Bang chains (id, name, severity tong hop, root, so findings)
       - Chi tiet moi chain: findings lien quan, severity truoc/sau escalation
    5. Neu khong co chains: ghi "Khong phat hien gadget chain cross-category"
    ```

    **2c. Sua workflows/audit.md — B3 truyen --poc + B8 dispatch fixer (per D-03, D-14)**

    **B3**: Thay dong "--poc — parse nhung bao 'Chua ho tro'" bang:
    ```
    4. **--poc** — luu poc_enabled=true vao session context. Truyen --poc cho scanners trong B5.
    ```

    Bo dong trong <rules> "Khi --poc ... thong bao 'Chua ho tro'" — thay bang:
    ```
    - Khi --poc duoc truyen: luu poc_enabled va truyen cho moi scanner trong B5 dispatch prompt
    - Khi --auto-fix duoc truyen: thong bao "Chua ho tro trong phien ban nay" va tiep tuc
    ```

    **B5**: Them vao phan dispatch prompt cho scanner:
    ```
    Neu poc_enabled=true: them "--poc" vao prompt cua scanner de kich hoat POC generation.
    ```

    **B8**: Thay toan bo "## Buoc 8: Fix routing (STUB)" bang:
    ```markdown
    ## Buoc 8: Fix routing

    Dispatch pd-sec-fixer de phan tich findings va tao fix phases proposal.

    1. Lay agent config:
       ```bash
       node -e "const {getAgentConfig}=require('./bin/lib/resource-config');console.log(JSON.stringify(getAgentConfig('pd-sec-fixer')))"
       ```

    2. Xac dinh mode:
       - Neu co .planning/ → mode = "tich-hop"
       - Neu khong → mode = "doc-lap"

    3. Spawn pd-sec-fixer agent (SubAgent) voi:
       - Input: duong dan SECURITY_REPORT.md, session_dir, mode
       - Model/effort tu getAgentConfig
       - Truyen gadget chain results tu B6 (neu co)

    4. Doi ket qua:
       - Mode tich-hop: fixer hien thi proposal, hoi user approve (per D-08), insert vao ROADMAP neu approved
       - Mode doc-lap: fixer ghi proposal vao session_dir/08-fix-proposal.md
    ```
  </action>
  <verify>
    <automated>grep -q "poc_enabled" workflows/audit.md && grep -q "pd-sec-fixer" workflows/audit.md && grep -q "detectChains" commands/pd/agents/pd-sec-reporter.md && grep -q "POC" commands/pd/agents/pd-sec-scanner.md && echo "ALL WIRING OK"</automated>
  </verify>
  <done>
    - Scanner co logic tao ## POC section khi --poc active (per D-01, D-02, D-03, D-04)
    - Reporter B6 goi detectChains() tu gadget-chain.js (per D-05, D-06)
    - audit.md B3 truyen --poc (khong con bao "chua ho tro")
    - audit.md B8 dispatch pd-sec-fixer thay vi stub (per D-14)
    - --auto-fix VAN giu stub "Chua ho tro" (per deferred AUTOFIX-01)
  </done>
</task>

<task type="auto">
  <name>Task 3: Mo rong tests — smoke-agent-files + smoke-resource-config</name>
  <files>test/smoke-agent-files.test.js, test/smoke-resource-config.test.js</files>
  <action>
    **3a. Mo rong test/smoke-agent-files.test.js**

    Them 'pd-sec-fixer' vao AGENT_NAMES array (hien co 7 agents, thanh 8).

    Them test case rieng cho pd-sec-fixer.md:
    ```javascript
    it('pd-sec-fixer.md co frontmatter dung', () => {
      // Verify: name = 'pd-sec-fixer', tier = 'architect'
    });
    ```

    Them test case cho security-fix-phase.md ton tai:
    ```javascript
    describe('security templates', () => {
      it('security-fix-phase.md ton tai trong templates/', () => {
        // readFileSync templates/security-fix-phase.md
        // assert co 'Evidence goc' va 'Huong sua' va 'Tieu chi hoan thanh'
      });
    });
    ```

    **3b. Mo rong test/smoke-resource-config.test.js**

    Them test case:
    ```javascript
    it('AGENT_REGISTRY co pd-sec-fixer', () => {
      assert.ok(AGENT_REGISTRY['pd-sec-fixer']);
      assert.equal(AGENT_REGISTRY['pd-sec-fixer'].tier, 'architect');
    });

    it('getAgentConfig tra ve dung cho pd-sec-fixer', () => {
      const config = getAgentConfig('pd-sec-fixer');
      assert.equal(config.model, 'opus');
      assert.ok(config.tools.includes('Write'));
    });
    ```
  </action>
  <verify>
    <automated>node --test test/smoke-agent-files.test.js && node --test test/smoke-resource-config.test.js && echo "TESTS OK"</automated>
  </verify>
  <done>
    - smoke-agent-files.test.js bao gom pd-sec-fixer va security-fix-phase.md
    - smoke-resource-config.test.js co 2 test cases moi cho pd-sec-fixer
    - Toan bo test suite PASS: node --test 'test/*.test.js'
  </done>
</task>

</tasks>

<verification>
```bash
# Task 1: Agent + template + registry
node -e "const {AGENT_REGISTRY}=require('./bin/lib/resource-config'); console.log('pd-sec-fixer:', AGENT_REGISTRY['pd-sec-fixer']?.tier)"
test -f commands/pd/agents/pd-sec-fixer.md && echo "fixer OK"
test -f templates/security-fix-phase.md && echo "template OK"

# Task 2: Wiring
grep -c "poc_enabled" workflows/audit.md
grep -c "pd-sec-fixer" workflows/audit.md
grep -c "detectChains" commands/pd/agents/pd-sec-reporter.md

# Task 3: Tests
node --test test/smoke-agent-files.test.js
node --test test/smoke-resource-config.test.js

# Full regression
node --test 'test/*.test.js'
```
</verification>

<success_criteria>
- pd-sec-fixer.md agent template hoan chinh voi 5 buoc process (per D-14, D-15)
- security-fix-phase.md template co 5 sections bat buoc (per D-11, FIX-02)
- Scanner tao POC khi --poc, KHONG tao khi khong co flag (per D-01, D-02, D-03, D-04)
- Reporter B6 goi detectChains() LUON (khong phu thuoc --poc) (per D-05)
- audit.md B3 truyen --poc cho scanners (per D-03)
- audit.md B8 dispatch pd-sec-fixer thay vi stub (per D-14)
- AGENT_REGISTRY co pd-sec-fixer tier architect
- Toan bo test suite PASS: node --test 'test/*.test.js'
- --auto-fix VAN la stub (per deferred AUTOFIX-01)
</success_criteria>

<output>
After completion, create `.planning/phases/50-poc-fix-phases/50-02-SUMMARY.md`
</output>
