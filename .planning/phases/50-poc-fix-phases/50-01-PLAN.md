---
phase: 50-poc-fix-phases
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/gadget-chain.js
  - references/gadget-chain-templates.yaml
  - test/smoke-gadget-chain.test.js
autonomous: true
requirements:
  - POC-02
  - FIX-01

must_haves:
  truths:
    - "detectChains() phat hien gadget chain khi findings cross-category khop template"
    - "detectChains() tra ve mang rong khi khong co chain nao khop"
    - "escalateSeverity() tinh max+1 va cap tai CRITICAL"
    - "orderFixPriority() sap theo reverse chain — root truoc"
    - "gadget-chain-templates.yaml co it nhat 7 chain patterns"
  artifacts:
    - path: "bin/lib/gadget-chain.js"
      provides: "Pure function module: detectChains, escalateSeverity, orderFixPriority, SEVERITY_ORDER"
      exports: ["detectChains", "escalateSeverity", "orderFixPriority", "SEVERITY_ORDER"]
    - path: "references/gadget-chain-templates.yaml"
      provides: "7 gadget chain patterns voi id, name, links, root, escalation"
      contains: "chains:"
    - path: "test/smoke-gadget-chain.test.js"
      provides: "Unit tests cho detectChains, escalateSeverity, orderFixPriority"
      min_lines: 80
  key_links:
    - from: "bin/lib/gadget-chain.js"
      to: "references/gadget-chain-templates.yaml"
      via: "Caller doc YAML roi truyen parsed templates vao detectChains()"
      pattern: "detectChains.*templates"
    - from: "test/smoke-gadget-chain.test.js"
      to: "bin/lib/gadget-chain.js"
      via: "require('../bin/lib/gadget-chain')"
      pattern: "require.*gadget-chain"
---

<objective>
Tao gadget-chain.js pure function module (TDD) va gadget-chain-templates.yaml.

Purpose: Module nay la noi logic phat hien gadget chain cross-category va tinh severity escalation. Reporter B6 va pd-sec-fixer se goi cac ham nay. Plan 02 wire vao workflow.
Output: bin/lib/gadget-chain.js (tested), references/gadget-chain-templates.yaml, test/smoke-gadget-chain.test.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-poc-fix-phases/50-CONTEXT.md
@.planning/phases/50-poc-fix-phases/50-RESEARCH.md

@bin/lib/session-delta.js (pattern: pure function module, khong I/O)
@references/security-rules.yaml (pattern: YAML config, 13 categories)

<interfaces>
<!-- Pattern tu session-delta.js: pure function, khong I/O, module.exports cuoi file -->

Tu bin/lib/session-delta.js:
```javascript
module.exports = { classifyDelta, appendAuditHistory, parseAuditHistory, DELTA_STATUS };
```

Tu bin/lib/resource-config.js (AGENT_REGISTRY format):
```javascript
const AGENT_REGISTRY = {
  "pd-sec-scanner": {
    tier: "scout",
    tools: ["Read", "Glob", "Grep", "mcp__fastcode__code_qa"],
    categories: [
      "sql-injection", "xss", "cmd-injection", "path-traversal",
      "secrets", "auth", "deserialization", "misconfig",
      "prototype-pollution", "crypto", "insecure-design",
      "vuln-deps", "logging",
    ],
  },
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD gadget-chain.js — RED tests + GREEN implementation</name>
  <files>bin/lib/gadget-chain.js, references/gadget-chain-templates.yaml, test/smoke-gadget-chain.test.js</files>
  <behavior>
    - escalateSeverity(['HIGH', 'HIGH']) → 'CRITICAL' (max+1, per D-07)
    - escalateSeverity(['CRITICAL', 'HIGH']) → 'CRITICAL' (cap tai CRITICAL, per D-07)
    - escalateSeverity(['LOW', 'MEDIUM']) → 'HIGH' (max MEDIUM idx=1, +1 = HIGH)
    - escalateSeverity([]) → 'LOW' (default)
    - detectChains(findings_2_cats_match, templates) → 1 chain voi escalated severity
    - detectChains(findings_1_cat_only, templates) → 0 chains (khong du link)
    - detectChains(findings_only_PASS, templates) → 0 chains (chi FAIL/FLAG moi tinh)
    - orderFixPriority(chains) → sap theo root truoc (per D-09: fix goc chain truoc)
  </behavior>
  <action>
    **Buoc 1 — Tao gadget-chain-templates.yaml (per D-06: rule-based patterns trong YAML)**

    Tao `references/gadget-chain-templates.yaml` voi 7 chain patterns tu RESEARCH Code Examples section:
    1. sqli-data-leak: sql-injection → secrets
    2. idor-privesc: auth → auth (IDOR + admin endpoint)
    3. xss-session-hijack: xss → auth
    4. cmd-injection-rce: cmd-injection → misconfig
    5. deserialization-rce: deserialization → cmd-injection
    6. path-traversal-data-leak: path-traversal → secrets
    7. prototype-pollution-rce: prototype-pollution → cmd-injection

    Moi chain co: id, name, description, links[{from_cat, to_cat, condition}], root, escalation: "+1"
    Toan bo comments/descriptions bang tieng Viet co dau.

    **Buoc 2 — RED: Tao test/smoke-gadget-chain.test.js**

    Dung `node:test` + `node:assert/strict` (KHONG jest). Cau truc:

    ```javascript
    const { describe, it } = require('node:test');
    const assert = require('node:assert/strict');
    ```

    Test cases:
    - describe('escalateSeverity'): 4 cases tu <behavior>
    - describe('detectChains'): 3 cases tu <behavior>
    - describe('orderFixPriority'): 1 case — 2 chains, chain co root 'sql-injection' xep truoc chain co root 'xss' (vi P0 < P1 theo gadget chain order)

    Mock templates: inline array voi 2 templates (sqli-data-leak va xss-session-hijack) — KHONG doc YAML file (pure function test).

    Mock findings: inline array voi du fields {category, file, name, verdict, severity}.

    Chay test — PHAI FAIL (RED).

    **Buoc 3 — GREEN: Tao bin/lib/gadget-chain.js**

    Theo RESEARCH Code Examples section chinh xac. Pure function module (khong I/O, khong require('fs')):

    - `SEVERITY_ORDER = ['LOW', 'MEDIUM', 'HIGH', 'CRITICAL']`
    - `escalateSeverity(severities)` — max index + 1, cap tai SEVERITY_ORDER.length - 1 (per D-07). Return 'LOW' khi mang rong.
    - `detectChains(findings, templates)` — group findings by category (chi FAIL/FLAG), match templates, de-dup by file::name key, tinh escalated severity. Return { chains, linkedFindingKeys }.
    - `orderFixPriority(chains)` — sap chains: root category cua chain dau tien la P0, cac link la P1, P2... (per D-09: fix goc truoc). Return sorted array voi `priority` field.

    Module.exports: `{ detectChains, escalateSeverity, orderFixPriority, SEVERITY_ORDER }`

    Toan bo JSDoc comments bang tieng Viet co dau.

    Chay test — PHAI PASS (GREEN).

    **Buoc 4 — REFACTOR (neu can)**

    Doc lai code, clean up. Chay test lan cuoi — PHAI PASS.
  </action>
  <verify>
    <automated>node --test test/smoke-gadget-chain.test.js</automated>
  </verify>
  <done>
    - gadget-chain.js exports detectChains, escalateSeverity, orderFixPriority, SEVERITY_ORDER
    - 7 chain templates trong YAML
    - 8+ test cases PASS (4 escalateSeverity + 3 detectChains + 1 orderFixPriority)
    - Module la pure function: khong require('fs'), khong I/O, khong side effects
  </done>
</task>

</tasks>

<verification>
```bash
# Unit tests
node --test test/smoke-gadget-chain.test.js

# Module exports check
node -e "const m = require('./bin/lib/gadget-chain'); console.log(Object.keys(m))"
# Expected: [ 'detectChains', 'escalateSeverity', 'orderFixPriority', 'SEVERITY_ORDER' ]

# YAML file exists
test -f references/gadget-chain-templates.yaml && echo "OK"

# Full suite regression
node --test 'test/*.test.js'
```
</verification>

<success_criteria>
- gadget-chain.js la pure function module khong I/O (tuong tu session-delta.js)
- 8+ test cases PASS trong smoke-gadget-chain.test.js
- gadget-chain-templates.yaml co 7 chain patterns
- escalateSeverity cap tai CRITICAL (per D-07)
- detectChains chi tinh FAIL/FLAG findings (per D-05)
- orderFixPriority sap root truoc (per D-09)
- Full test suite `node --test 'test/*.test.js'` PASS
</success_criteria>

<output>
After completion, create `.planning/phases/50-poc-fix-phases/50-01-SUMMARY.md`
</output>
