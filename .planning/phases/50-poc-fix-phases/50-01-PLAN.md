---
phase: 50-poc-fix-phases
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - references/gadget-chain-templates.yaml
  - bin/lib/gadget-chain.js
  - test/smoke-gadget-chain.test.js
  - bin/lib/session-delta.js
autonomous: true
requirements: [POC-02, FIX-01]

must_haves:
  truths:
    - "detectChains() phat hien gadget chain tu findings cross-category dua tren templates YAML"
    - "escalateSeverity() tinh severity max+1, cap tai CRITICAL"
    - "orderFixPriority() sap fix phases theo reverse chain order (root truoc)"
    - "parseFunctionChecklist() duoc export tu session-delta.js de DRY"
  artifacts:
    - path: "references/gadget-chain-templates.yaml"
      provides: "7 gadget chain templates pho bien"
      contains: "sqli-data-leak"
    - path: "bin/lib/gadget-chain.js"
      provides: "Pure function module: detectChains, escalateSeverity, orderFixPriority, SEVERITY_ORDER"
      exports: ["detectChains", "escalateSeverity", "orderFixPriority", "SEVERITY_ORDER"]
    - path: "test/smoke-gadget-chain.test.js"
      provides: "TDD tests cho gadget-chain.js"
      min_lines: 80
  key_links:
    - from: "bin/lib/gadget-chain.js"
      to: "references/gadget-chain-templates.yaml"
      via: "caller truyen parsed templates vao detectChains()"
      pattern: "detectChains\\(findings, templates\\)"
    - from: "bin/lib/gadget-chain.js"
      to: "bin/lib/session-delta.js"
      via: "parseFunctionChecklist export"
      pattern: "parseFunctionChecklist"
---

<objective>
Tao gadget-chain.js pure function module (TDD) va gadget-chain-templates.yaml. Module cung cap 3 ham chinh: detectChains() phat hien chuoi tan cong cross-category, escalateSeverity() tinh severity tong hop, orderFixPriority() sap thu tu fix theo reverse chain. Dong thoi export parseFunctionChecklist() tu session-delta.js de cac module khac co the dung (DRY).

Purpose: POC-02 can gadget chain detection, FIX-01 can fix priority ordering. Day la logic core cho Reporter B6 va pd-sec-fixer B8.
Output: gadget-chain.js (tested), gadget-chain-templates.yaml (7 templates), parseFunctionChecklist exported
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-poc-fix-phases/50-CONTEXT.md
@.planning/phases/50-poc-fix-phases/50-RESEARCH.md

<interfaces>
<!-- Key types va contracts executor can. -->

From bin/lib/session-delta.js:
```javascript
// parseFunctionChecklist HIEN TAI la internal (khong export)
// Task 1 se export them ham nay
function parseFunctionChecklist(content) { /* ... */ }
// Returns: Array<{file: string, name: string, line: string, verdict: string, detail: string}>

// Existing exports:
module.exports = { classifyDelta, appendAuditHistory, parseAuditHistory, DELTA_STATUS };
```

From bin/lib/resource-config.js:
```javascript
const AGENT_REGISTRY = {
  "pd-sec-scanner": {
    tier: "scout",
    tools: ["Read", "Glob", "Grep", "mcp__fastcode__code_qa"],
    categories: ["sql-injection", "xss", "cmd-injection", "path-traversal", "secrets", "auth", "deserialization", "misconfig", "prototype-pollution", "crypto", "insecure-design", "vuln-deps", "logging"],
  },
  "pd-sec-reporter": { tier: "builder", tools: ["Read", "Write", "Glob"] },
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export parseFunctionChecklist tu session-delta.js + tao gadget-chain-templates.yaml</name>
  <files>bin/lib/session-delta.js, references/gadget-chain-templates.yaml</files>
  <read_first>bin/lib/session-delta.js, test/smoke-session-delta.test.js, references/security-rules.yaml</read_first>
  <action>
**1a. Export parseFunctionChecklist tu session-delta.js:**

Mo file `bin/lib/session-delta.js`, tim dong `module.exports = { classifyDelta, appendAuditHistory, parseAuditHistory, DELTA_STATUS };` va them `parseFunctionChecklist`:

```javascript
module.exports = { classifyDelta, appendAuditHistory, parseAuditHistory, parseFunctionChecklist, DELTA_STATUS };
```

Khong sua logic cua parseFunctionChecklist — chi export them.

**1b. Tao gadget-chain-templates.yaml:**

Tao file `references/gadget-chain-templates.yaml` voi 7 chain templates. Moi template co fields: id, name, description, links (array cua {from_cat, to_cat, condition}), root, escalation.

Noi dung cu the:

```yaml
# gadget-chain-templates.yaml — Chain patterns pho bien cho cross-category analysis
# Dung boi detectChains() trong bin/lib/gadget-chain.js
# Moi chain lien ket findings tu 2+ categories thanh chuoi tan cong

chains:
  - id: sqli-data-leak
    name: "SQL Injection -> Data Leak"
    description: "SQLi cho phep trich xuat du lieu nhay cam tu DB"
    links:
      - from_cat: sql-injection
        to_cat: secrets
        condition: "sql-injection FAIL/FLAG + secrets FAIL/FLAG"
    root: sql-injection
    escalation: "+1"

  - id: idor-privesc
    name: "IDOR -> Privilege Escalation"
    description: "Thieu kiem tra ownership cho phep truy cap tai nguyen nguoi khac"
    links:
      - from_cat: auth
        to_cat: auth
        condition: "auth FAIL/FLAG voi ca IDOR va role check"
    root: auth
    escalation: "+1"

  - id: xss-session-hijack
    name: "XSS -> Session Hijacking"
    description: "XSS cho phep danh cap cookie/token phien"
    links:
      - from_cat: xss
        to_cat: auth
        condition: "xss FAIL/FLAG + auth FAIL/FLAG"
    root: xss
    escalation: "+1"

  - id: cmd-injection-rce
    name: "Command Injection -> RCE"
    description: "Injection command OS dan den thuc thi ma tu xa"
    links:
      - from_cat: cmd-injection
        to_cat: misconfig
        condition: "cmd-injection FAIL/FLAG + misconfig FAIL/FLAG"
    root: cmd-injection
    escalation: "+1"

  - id: deserialization-rce
    name: "Deserialization -> RCE"
    description: "Deserialization khong an toan dan den thuc thi ma tuy y"
    links:
      - from_cat: deserialization
        to_cat: cmd-injection
        condition: "deserialization FAIL/FLAG + cmd-injection FAIL/FLAG"
    root: deserialization
    escalation: "+1"

  - id: path-traversal-data-leak
    name: "Path Traversal -> Sensitive Data Exposure"
    description: "Doc file tuy y dan den lo secrets/config"
    links:
      - from_cat: path-traversal
        to_cat: secrets
        condition: "path-traversal FAIL/FLAG + secrets FAIL/FLAG"
    root: path-traversal
    escalation: "+1"

  - id: prototype-pollution-rce
    name: "Prototype Pollution -> RCE"
    description: "Pollution prototype dan den code execution qua gadget"
    links:
      - from_cat: prototype-pollution
        to_cat: cmd-injection
        condition: "prototype-pollution FAIL/FLAG + cmd-injection FAIL/FLAG"
    root: prototype-pollution
    escalation: "+1"
```

  </action>
  <verify>
    <automated>node -e "const s=require('./bin/lib/session-delta');if(typeof s.parseFunctionChecklist!=='function')throw new Error('parseFunctionChecklist not exported');console.log('OK')" && test -f references/gadget-chain-templates.yaml && node -e "const y=require('fs').readFileSync('references/gadget-chain-templates.yaml','utf8');if(!y.includes('sqli-data-leak'))throw new Error('missing template');const count=(y.match(/- id:/g)||[]).length;if(count!==7)throw new Error('expected 7 templates, got '+count);console.log('OK: 7 templates')"</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/session-delta.js exports dong chua `parseFunctionChecklist`
    - references/gadget-chain-templates.yaml ton tai voi 7 entries co `- id:` prefix
    - Moi template co fields: id, name, description, links, root, escalation
    - node -e require session-delta va goi parseFunctionChecklist khong loi
    - Test suite cu van pass: node --test test/smoke-session-delta.test.js
  </acceptance_criteria>
  <done>parseFunctionChecklist exported, 7 gadget chain templates trong YAML, existing tests van green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD gadget-chain.js — detectChains + escalateSeverity + orderFixPriority</name>
  <files>bin/lib/gadget-chain.js, test/smoke-gadget-chain.test.js</files>
  <read_first>bin/lib/session-delta.js, references/gadget-chain-templates.yaml, bin/lib/gadget-chain.js</read_first>
  <behavior>
    - escalateSeverity([]) -> 'LOW'
    - escalateSeverity(['LOW']) -> 'MEDIUM' (max LOW=0, +1=MEDIUM)
    - escalateSeverity(['HIGH', 'MEDIUM']) -> 'CRITICAL' (max HIGH=2, +1=CRITICAL)
    - escalateSeverity(['CRITICAL', 'CRITICAL']) -> 'CRITICAL' (cap, khong vuot)
    - escalateSeverity(['LOW', 'LOW', 'LOW']) -> 'MEDIUM'
    - detectChains([], templates) -> { chains: [], linkedFindingKeys: [] }
    - detectChains(findings_with_sqli_and_secrets, templates) -> chains chua sqli-data-leak voi escalatedSeverity
    - detectChains(findings_only_xss, templates) -> chains rong (chi 1 category, khong du link)
    - detectChains(findings_multi_chain, templates) -> nhieu chains, linkedFindingKeys de-dup
    - orderFixPriority(chains) -> array sap theo: root truoc, severity giam dan. Chain co CRITICAL root truoc chain co HIGH root
    - orderFixPriority([]) -> []
  </behavior>
  <action>
**RED — Tao test truoc:**

Tao `test/smoke-gadget-chain.test.js` voi node:test + node:assert/strict. Import tu `../bin/lib/gadget-chain`. Tests:

```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const { detectChains, escalateSeverity, orderFixPriority, SEVERITY_ORDER } = require('../bin/lib/gadget-chain');

describe('SEVERITY_ORDER', () => {
  it('co 4 cap dung thu tu', () => {
    assert.deepStrictEqual(SEVERITY_ORDER, ['LOW', 'MEDIUM', 'HIGH', 'CRITICAL']);
  });
});

describe('escalateSeverity', () => {
  it('tra ve LOW khi mang rong', () => {
    assert.equal(escalateSeverity([]), 'LOW');
  });
  it('tang 1 bac tu LOW -> MEDIUM', () => {
    assert.equal(escalateSeverity(['LOW']), 'MEDIUM');
  });
  it('tang 1 bac tu max(HIGH,MEDIUM) -> CRITICAL', () => {
    assert.equal(escalateSeverity(['HIGH', 'MEDIUM']), 'CRITICAL');
  });
  it('cap tai CRITICAL khi da la CRITICAL', () => {
    assert.equal(escalateSeverity(['CRITICAL', 'CRITICAL']), 'CRITICAL');
  });
  it('case-insensitive', () => {
    assert.equal(escalateSeverity(['high']), 'CRITICAL');
  });
});

describe('detectChains', () => {
  // Template fixtures
  const templates = [
    { id: 'sqli-data-leak', name: 'SQLi->DataLeak', links: [{ from_cat: 'sql-injection', to_cat: 'secrets' }], root: 'sql-injection', escalation: '+1' },
    { id: 'xss-session', name: 'XSS->Session', links: [{ from_cat: 'xss', to_cat: 'auth' }], root: 'xss', escalation: '+1' },
  ];

  it('tra ve rong khi khong co findings', () => {
    const r = detectChains([], templates);
    assert.equal(r.chains.length, 0);
    assert.equal(r.linkedFindingKeys.length, 0);
  });

  it('phat hien chain khi ca 2 categories co FAIL/FLAG', () => {
    const findings = [
      { category: 'sql-injection', file: 'a.js', name: 'query', verdict: 'FAIL', severity: 'HIGH' },
      { category: 'secrets', file: 'b.js', name: 'getKey', verdict: 'FLAG', severity: 'MEDIUM' },
    ];
    const r = detectChains(findings, templates);
    assert.equal(r.chains.length, 1);
    assert.equal(r.chains[0].id, 'sqli-data-leak');
    assert.equal(r.chains[0].escalatedSeverity, 'CRITICAL'); // max(HIGH)=2, +1=3=CRITICAL
  });

  it('khong phat hien chain khi chi co 1 category', () => {
    const findings = [
      { category: 'xss', file: 'a.js', name: 'render', verdict: 'FAIL', severity: 'HIGH' },
    ];
    const r = detectChains(findings, templates);
    assert.equal(r.chains.length, 0);
  });

  it('bo qua findings voi verdict PASS', () => {
    const findings = [
      { category: 'sql-injection', file: 'a.js', name: 'query', verdict: 'PASS', severity: 'HIGH' },
      { category: 'secrets', file: 'b.js', name: 'getKey', verdict: 'FAIL', severity: 'MEDIUM' },
    ];
    const r = detectChains(findings, templates);
    assert.equal(r.chains.length, 0);
  });

  it('de-dup findings theo file::name key', () => {
    const findings = [
      { category: 'sql-injection', file: 'a.js', name: 'query', verdict: 'FAIL', severity: 'HIGH' },
      { category: 'secrets', file: 'a.js', name: 'query', verdict: 'FLAG', severity: 'HIGH' },
    ];
    const r = detectChains(findings, templates);
    // from_cat=sql-injection co, to_cat=secrets co -> chain match
    assert.equal(r.chains.length, 1);
    // De-dup: chi 1 unique finding (same file::name)
    assert.equal(r.chains[0].findings.length, 1);
  });
});

describe('orderFixPriority', () => {
  it('tra ve rong khi khong co chains', () => {
    assert.deepStrictEqual(orderFixPriority([]), []);
  });

  it('root truoc, severity giam dan', () => {
    const chains = [
      { id: 'a', root: 'xss', escalatedSeverity: 'HIGH', findings: [{ category: 'xss' }, { category: 'auth' }] },
      { id: 'b', root: 'sql-injection', escalatedSeverity: 'CRITICAL', findings: [{ category: 'sql-injection' }, { category: 'secrets' }] },
    ];
    const ordered = orderFixPriority(chains);
    assert.equal(ordered[0].id, 'b'); // CRITICAL truoc HIGH
    assert.equal(ordered[1].id, 'a');
  });

  it('moi entry co fixPhases voi root truoc', () => {
    const chains = [
      { id: 'a', root: 'sql-injection', escalatedSeverity: 'CRITICAL', findings: [
        { category: 'sql-injection', severity: 'HIGH' },
        { category: 'secrets', severity: 'MEDIUM' },
      ]},
    ];
    const ordered = orderFixPriority(chains);
    assert.equal(ordered[0].fixPhases[0].category, 'sql-injection'); // root truoc
  });
});
```

**GREEN — Implement gadget-chain.js:**

Tao `bin/lib/gadget-chain.js` theo pattern pure function module (giong session-delta.js):

```javascript
'use strict';

const SEVERITY_ORDER = ['LOW', 'MEDIUM', 'HIGH', 'CRITICAL'];

function escalateSeverity(severities) {
  if (!severities || severities.length === 0) return 'LOW';
  const maxIdx = Math.max(...severities.map(s => SEVERITY_ORDER.indexOf(s.toUpperCase())));
  const escalated = Math.min(maxIdx + 1, SEVERITY_ORDER.length - 1);
  return SEVERITY_ORDER[escalated];
}

function detectChains(findings, templates) {
  const chains = [];
  const linkedFindings = new Set();

  // Group findings by category, chi lay FAIL/FLAG
  const byCat = new Map();
  for (const f of findings) {
    if (f.verdict !== 'FAIL' && f.verdict !== 'FLAG') continue;
    if (!byCat.has(f.category)) byCat.set(f.category, []);
    byCat.get(f.category).push(f);
  }

  for (const tmpl of templates) {
    const allPresent = tmpl.links.every(link =>
      byCat.has(link.from_cat) && byCat.has(link.to_cat)
    );
    if (!allPresent) continue;

    const involved = [];
    for (const link of tmpl.links) {
      involved.push(...(byCat.get(link.from_cat) || []));
      involved.push(...(byCat.get(link.to_cat) || []));
    }
    const unique = [...new Map(involved.map(f => [`${f.file}::${f.name}`, f])).values()];
    const severities = unique.map(f => f.severity);
    const escalated = escalateSeverity(severities);

    chains.push({
      id: tmpl.id,
      name: tmpl.name,
      root: tmpl.root,
      findings: unique,
      originalSeverities: severities,
      escalatedSeverity: escalated,
    });

    unique.forEach(f => linkedFindings.add(`${f.file}::${f.name}`));
  }

  return { chains, linkedFindingKeys: [...linkedFindings] };
}

function orderFixPriority(chains) {
  if (!chains || chains.length === 0) return [];

  // Sap theo severity giam dan (per D-09: gadget chain nguoc, root truoc)
  const sorted = [...chains].sort((a, b) => {
    const aIdx = SEVERITY_ORDER.indexOf(a.escalatedSeverity);
    const bIdx = SEVERITY_ORDER.indexOf(b.escalatedSeverity);
    return bIdx - aIdx; // giam dan
  });

  return sorted.map(chain => {
    // Tao fixPhases: root category truoc, cac category khac sau
    const categories = [...new Set(chain.findings.map(f => f.category))];
    const rootFirst = [
      chain.root,
      ...categories.filter(c => c !== chain.root),
    ].filter(c => categories.includes(c));

    return {
      ...chain,
      fixPhases: rootFirst.map((cat, idx) => ({
        priority: `P${idx}`,
        category: cat,
        findings: chain.findings.filter(f => f.category === cat),
      })),
    };
  });
}

module.exports = { detectChains, escalateSeverity, orderFixPriority, SEVERITY_ORDER };
```

Chay test: `node --test test/smoke-gadget-chain.test.js` — tat ca PHAI pass.

**REFACTOR** (neu can): Kiem tra code clean, khong co duplication.
  </action>
  <verify>
    <automated>node --test test/smoke-gadget-chain.test.js</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/gadget-chain.js ton tai va export 4 symbols: detectChains, escalateSeverity, orderFixPriority, SEVERITY_ORDER
    - test/smoke-gadget-chain.test.js co it nhat 10 test cases
    - `node --test test/smoke-gadget-chain.test.js` exit code 0
    - escalateSeverity(['CRITICAL','CRITICAL']) tra ve 'CRITICAL' (khong vuot)
    - detectChains voi findings chi PASS khong tao chain
    - orderFixPriority sap root truoc trong fixPhases
    - Full suite: `node --test 'test/*.test.js'` van green
  </acceptance_criteria>
  <done>gadget-chain.js pass tat ca tests, 3 ham pure function hoat dong dung, TDD RED-GREEN-REFACTOR hoan tat</done>
</task>

</tasks>

<verification>
1. `node --test test/smoke-gadget-chain.test.js` — tat ca tests green
2. `node --test test/smoke-session-delta.test.js` — existing tests van green sau khi export them parseFunctionChecklist
3. `node --test 'test/*.test.js'` — full suite green
4. `node -e "const g=require('./bin/lib/gadget-chain');console.log(Object.keys(g))"` — in ra ['detectChains','escalateSeverity','orderFixPriority','SEVERITY_ORDER']
</verification>

<success_criteria>
- gadget-chain.js la pure function module (khong I/O, khong side effects)
- 7 gadget chain templates trong YAML (sqli-data-leak, idor-privesc, xss-session-hijack, cmd-injection-rce, deserialization-rce, path-traversal-data-leak, prototype-pollution-rce)
- escalateSeverity cap tai CRITICAL (per D-07)
- orderFixPriority sap root truoc (per D-09)
- parseFunctionChecklist exported tu session-delta.js (DRY)
- TDD: test viet truoc, implementation sau, tat ca pass
</success_criteria>

<output>
After completion, create `.planning/phases/50-poc-fix-phases/50-01-SUMMARY.md`
</output>
