---
phase: 30-detective-interactions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/parallel-dispatch.js
  - test/smoke-parallel-dispatch.test.js
autonomous: true
requirements:
  - PROT-08
must_haves:
  truths:
    - "buildParallelPlan() tra ve 2 agents: pd-code-detective (critical=true) va pd-doc-specialist (critical=false)"
    - "mergeParallelResults() khi ca 2 thanh cong tra ve allSucceeded=true va 2 results"
    - "mergeParallelResults() khi DocSpec fail tra ve warning nhung khong block — allSucceeded=false nhung van co Detective result"
    - "mergeParallelResults() giu tach rieng evidence — khong merge thanh 1 file"
  artifacts:
    - path: "bin/lib/parallel-dispatch.js"
      provides: "Parallel dispatch logic — 2 functions"
      exports: ["buildParallelPlan", "mergeParallelResults"]
    - path: "test/smoke-parallel-dispatch.test.js"
      provides: "Unit tests cho parallel-dispatch"
      min_lines: 70
  key_links:
    - from: "bin/lib/parallel-dispatch.js"
      to: "bin/lib/resource-config.js"
      via: "require('./resource-config').getAgentConfig"
      pattern: "getAgentConfig\\("
    - from: "bin/lib/parallel-dispatch.js"
      to: "bin/lib/evidence-protocol.js"
      via: "require('./evidence-protocol').validateEvidence"
      pattern: "validateEvidence\\("
---

<objective>
Tao module parallel-dispatch.js xu ly spawn va hop nhat ket qua tu Detective + DocSpec song song (PROT-08).

Purpose: Detective va DocSpec chay song song, ca 2 doc evidence_janitor.md (chi doc). Module nay tao ke hoach spawn va hop nhat ket qua, xu ly partial failure (DocSpec optional).
Output: bin/lib/parallel-dispatch.js (2 functions) va test/smoke-parallel-dispatch.test.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-detective-interactions/30-CONTEXT.md
@.planning/phases/30-detective-interactions/30-RESEARCH.md

<interfaces>
<!-- Key types va contracts tu codebase hien co. Executor dung truc tiep. -->

From bin/lib/resource-config.js:
```javascript
const AGENT_REGISTRY = {
  'pd-code-detective': { tier: 'builder',   tools: ['Read', 'Glob', 'Grep', 'mcp__fastcode__code_qa'] },
  'pd-doc-specialist': { tier: 'scout',     tools: ['Read', 'mcp__context7__resolve-library-id', 'mcp__context7__query-docs'] },
};

function getAgentConfig(agentName) → { name, tier, model, effort, maxTurns, tools: string[] }
```

From bin/lib/evidence-protocol.js:
```javascript
function validateEvidence(content) → { valid, outcome, agent, warnings }
function parseEvidence(content) → { agent, outcome, timestamp, session, body, sections }
```

From test/smoke-evidence-protocol.test.js (test pattern):
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
function makeEvidence({ agent, outcome, session, body }) → string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tao test file smoke-parallel-dispatch.test.js</name>
  <files>test/smoke-parallel-dispatch.test.js</files>
  <read_first>
    - bin/lib/resource-config.js (getAgentConfig API, AGENT_REGISTRY keys)
    - bin/lib/evidence-protocol.js (validateEvidence API)
    - test/smoke-evidence-protocol.test.js (test pattern: makeEvidence helper)
  </read_first>
  <behavior>
    - buildParallelPlan() tra ve 2 agents voi config dung tu registry
    - buildParallelPlan() agent[0] la pd-code-detective voi critical=true
    - buildParallelPlan() agent[1] la pd-doc-specialist voi critical=false
    - mergeParallelResults() khi ca 2 thanh cong: allSucceeded=true, results.length=2
    - mergeParallelResults() khi DocSpec fail: allSucceeded=false, co warning, van co Detective result
    - mergeParallelResults() khi Detective fail: allSucceeded=false, co warning cho Detective
  </behavior>
  <action>
    Tao file test/smoke-parallel-dispatch.test.js voi cau truc:

    1. Require `node:test` (describe, it) va `node:assert/strict`
    2. Require `{ buildParallelPlan, mergeParallelResults }` tu `../bin/lib/parallel-dispatch`

    3. Tao helper makeEvidence:
    ```javascript
    function makeEvidence({ agent = 'pd-code-detective', outcome = 'root_cause', session = 'S001', body = '' } = {}) {
      return `---\nagent: ${agent}\noutcome: ${outcome}\ntimestamp: 2026-03-24T10:00:00+07:00\nsession: ${session}\n---\n${body}`;
    }
    ```

    4. Tao VALID_DETECTIVE_EVIDENCE:
    ```javascript
    const VALID_DETECTIVE_EVIDENCE = makeEvidence({
      agent: 'pd-code-detective',
      outcome: 'root_cause',
      body: '## ROOT CAUSE FOUND\n\n## Nguyen nhan\nLoi o dong 42.\n\n## Bang chung\nsrc/api.js:42\n\n## De xuat\nSua dong 42.',
    });
    ```

    5. Tao VALID_DOCSPEC_EVIDENCE:
    ```javascript
    const VALID_DOCSPEC_EVIDENCE = makeEvidence({
      agent: 'pd-doc-specialist',
      outcome: 'root_cause',
      body: '## ROOT CAUSE FOUND\n\n## Nguyen nhan\nDocs sai version.\n\n## Bang chung\nREADME.md:10\n\n## De xuat\nCap nhat docs.',
    });
    ```

    6. Test describe('buildParallelPlan'):
      - it('tra ve 2 agents'): goi buildParallelPlan('/tmp/S001-test', '/tmp/S001-test/evidence_janitor.md'), assert agents.length === 2
      - it('agent dau tien la Detective voi critical=true'): assert agents[0].name === 'pd-code-detective', assert agents[0].critical === true, assert agents[0].outputFile === 'evidence_code.md'
      - it('agent thu hai la DocSpec voi critical=false'): assert agents[1].name === 'pd-doc-specialist', assert agents[1].critical === false, assert agents[1].outputFile === 'evidence_docs.md'
      - it('ca 2 agents co inputPath la evidence_janitor.md'): assert agents[0].inputPath === '/tmp/S001-test/evidence_janitor.md', assert agents[1].inputPath === agents[0].inputPath
      - it('agents co config tu registry'): assert agents[0].config.model === 'sonnet', assert agents[1].config.model === 'haiku'

    7. Test describe('mergeParallelResults'):
      - it('hop nhat thanh cong khi ca 2 tra ket qua'): goi mergeParallelResults({ detectiveResult: { evidenceContent: VALID_DETECTIVE_EVIDENCE }, docSpecResult: { evidenceContent: VALID_DOCSPEC_EVIDENCE } }), assert allSucceeded === true, assert results.length === 2
      - it('chi warning khi DocSpec fail — khong block per D-12'): goi mergeParallelResults({ detectiveResult: { evidenceContent: VALID_DETECTIVE_EVIDENCE }, docSpecResult: { error: { message: 'timeout' } } }), assert allSucceeded === false, assert warnings.some(w => w.includes('Doc Specialist')), assert results.filter(r => r.valid).length >= 1
      - it('warning khi Detective fail'): goi mergeParallelResults({ detectiveResult: { error: { message: 'crash' } }, docSpecResult: { evidenceContent: VALID_DOCSPEC_EVIDENCE } }), assert warnings.some(w => w.includes('Code Detective'))
      - it('ca 2 fail — warnings cho ca 2'): goi mergeParallelResults({ detectiveResult: null, docSpecResult: null }), assert warnings.length >= 2
  </action>
  <verify>
    <automated>node --test test/smoke-parallel-dispatch.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test/smoke-parallel-dispatch.test.js ton tai va co it nhat 9 it() blocks
    - File require('../bin/lib/parallel-dispatch')
    - File chua assert cho: 'pd-code-detective', 'pd-doc-specialist', 'evidence_code.md', 'evidence_docs.md', 'evidence_janitor.md', 'critical', 'sonnet', 'haiku', 'Doc Specialist', 'Code Detective'
  </acceptance_criteria>
  <done>Test file chay duoc (co the FAIL vi module chua ton tai — TDD RED phase)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tao module parallel-dispatch.js</name>
  <files>bin/lib/parallel-dispatch.js</files>
  <read_first>
    - test/smoke-parallel-dispatch.test.js (test expectations — day la spec)
    - bin/lib/resource-config.js (getAgentConfig API, AGENT_REGISTRY)
    - bin/lib/evidence-protocol.js (validateEvidence API)
    - bin/lib/session-manager.js (pattern non-blocking warnings)
  </read_first>
  <behavior>
    - buildParallelPlan(sessionDir, janitarEvidencePath) tra ve { agents: Array, warnings }
    - mergeParallelResults({ detectiveResult, docSpecResult }) tra ve { results, allSucceeded, warnings }
    - Detective la critical path (critical=true), DocSpec la optional (critical=false per D-12)
    - Evidence giu tach rieng — evidence_code.md va evidence_docs.md (per D-13)
  </behavior>
  <action>
    Tao file bin/lib/parallel-dispatch.js theo pattern pure function:

    1. Header comment: "Parallel Dispatch Module — Spawn va hop nhat ket qua Detective + DocSpec song song (PROT-08)."
    2. 'use strict';
    3. Require: `const { getAgentConfig } = require('./resource-config');` va `const { validateEvidence } = require('./evidence-protocol');`

    4. Function buildParallelPlan(sessionDir, janitarEvidencePath) (per D-11):
      - const warnings = [];
      - const detectiveConfig = getAgentConfig('pd-code-detective')
      - const docSpecConfig = getAgentConfig('pd-doc-specialist')
      - return { agents: [
          { name: 'pd-code-detective', config: detectiveConfig, inputPath: janitarEvidencePath, outputFile: 'evidence_code.md', critical: true },
          { name: 'pd-doc-specialist', config: docSpecConfig, inputPath: janitarEvidencePath, outputFile: 'evidence_docs.md', critical: false },
        ], warnings }

    5. Function mergeParallelResults({ detectiveResult, docSpecResult }) (per D-12, D-13):
      - const warnings = [];
      - const results = [];

      - XU LY DETECTIVE (critical):
        - Neu detectiveResult?.evidenceContent ton tai:
          - const validation = validateEvidence(detectiveResult.evidenceContent)
          - results.push({ agent: 'pd-code-detective', outcome: validation.outcome, valid: validation.valid })
          - Neu !validation.valid → warnings.push(...validation.warnings.map(w => `Detective: ${w}`))
        - Nguoc lai:
          - const errMsg = detectiveResult?.error?.message || 'Khong co ket qua'
          - warnings.push(`Code Detective that bai: ${errMsg}`)
          - results.push({ agent: 'pd-code-detective', outcome: null, valid: false })

      - XU LY DOCSPEC (optional — per D-12):
        - Neu docSpecResult?.evidenceContent ton tai:
          - const validation = validateEvidence(docSpecResult.evidenceContent)
          - results.push({ agent: 'pd-doc-specialist', outcome: validation.outcome, valid: validation.valid })
          - Neu !validation.valid → warnings.push(...validation.warnings.map(w => `DocSpec: ${w}`))
        - Nguoc lai:
          - const errMsg = docSpecResult?.error?.message || 'Khong co ket qua'
          - warnings.push(`Doc Specialist khong tra ket qua: ${errMsg} — tiep tuc voi evidence co san`)

      - const allSucceeded = results.every(r => r.valid)
      - return { results, allSucceeded, warnings }

    6. module.exports = { buildParallelPlan, mergeParallelResults };

    KHONG merge evidence thanh 1 file — per D-13, giu tach rieng evidence_code.md va evidence_docs.md.
    DocSpec fail KHONG block workflow — per D-12, chi warning.
  </action>
  <verify>
    <automated>node --test test/smoke-parallel-dispatch.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/parallel-dispatch.js ton tai
    - File chua 'use strict'
    - File require('./resource-config') va require('./evidence-protocol')
    - File export 2 symbols: buildParallelPlan, mergeParallelResults
    - buildParallelPlan tra ve 2 agents voi name 'pd-code-detective' va 'pd-doc-specialist'
    - Detective co critical: true, DocSpec co critical: false
    - Output files la 'evidence_code.md' va 'evidence_docs.md' — tach rieng per D-13
    - mergeParallelResults xu ly null detectiveResult va null docSpecResult khong throw
    - Tat ca tests trong test/smoke-parallel-dispatch.test.js PASS
  </acceptance_criteria>
  <done>Module parallel-dispatch.js hoan chinh, tat ca tests PASS. buildParallelPlan tao ke hoach 2 agents, mergeParallelResults xu ly partial failure.</done>
</task>

</tasks>

<verification>
node --test test/smoke-parallel-dispatch.test.js — tat ca tests pass
grep -c 'module.exports' bin/lib/parallel-dispatch.js — tra ve 1
grep 'critical' bin/lib/parallel-dispatch.js — xuat hien
</verification>

<success_criteria>
- parallel-dispatch.js export 2 symbols: buildParallelPlan, mergeParallelResults
- Tat ca 9+ tests pass
- Moi function la pure — khong require('fs')
- buildParallelPlan tra 2 agents voi config tu getAgentConfig() per D-11
- Detective critical=true, DocSpec critical=false per D-12
- Output files tach rieng: evidence_code.md va evidence_docs.md per D-13
- mergeParallelResults chi warning khi DocSpec fail, khong block per D-12
- Khong co merge logic — evidence giu nguyen 2 files doc lap per D-13
</success_criteria>

<output>
Sau khi hoan thanh, tao `.planning/phases/30-detective-interactions/30-03-SUMMARY.md`
</output>
