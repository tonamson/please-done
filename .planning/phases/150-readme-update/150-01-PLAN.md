---
phase: 150-readme-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - VERSION
  - package.json
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "README version badge shows 12.3.0"
    - "README Skills Reference lists 20 commands in 5 categories"
    - "pd:stats, pd:health, pd:discover, pd:sync-version all appear in Skills Reference"
    - "VERSION file contains 12.3.0"
    - "package.json version field is 12.3.0"
  artifacts:
    - path: "README.md"
      provides: "Updated version badge, command counts, Skills Reference table"
      contains: "version-12.3.0"
    - path: "VERSION"
      provides: "Version number"
      contains: "12.3.0"
    - path: "package.json"
      provides: "Package version"
      contains: "\"version\": \"12.3.0\""
  key_links: []
---

<objective>
Update README.md, VERSION, and package.json to reflect v12.3.0 with accurate command counts and complete Skills Reference.

Purpose: Documentation accuracy — version badges, command counts, and Skills Reference table must match actual v12.3 release state.
Output: Three files surgically updated with no other changes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
Target files (read before editing):
@README.md — large file, make ONLY the specified edits
@VERSION — single-line file
@package.json — edit only version field

Command descriptions for new Skills Reference entries:
- sync-version: "Sync version from package.json across README badges, version text, and doc file headers"
- stats: "Display comprehensive project statistics including phases, plans, requirements, milestones, timeline, and file counts"
- health: "Diagnose planning directory issues — missing files, STATE.md validation, orphaned directories"
- discover: "Discover MCP tools and built-in tools across all configured platforms"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update VERSION and package.json</name>
  <files>VERSION, package.json</files>
  <action>
1. Edit VERSION file: change `4.0.0` → `12.3.0`
2. Edit package.json: change `"version": "4.0.0"` → `"version": "12.3.0"` (line 3)

These are single-field surgical edits. Do not modify any other content.
  </action>
  <verify>
    <automated>grep -q "12.3.0" VERSION && grep -q '"version": "12.3.0"' package.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>VERSION contains 12.3.0, package.json version field is 12.3.0</done>
</task>

<task type="auto">
  <name>Task 2: Update README version references</name>
  <files>README.md</files>
  <action>
Make these SURGICAL edits only — do not rewrite sections:

1. Line 3: Change badge from `version-4.0.0` → `version-12.3.0`
2. Line 16: Change `Current version: v4.0.0` → `Current version: v12.3.0`
3. Line 30: Change `all 16 commands` → `all 20 commands`
4. Line 218: Change `16 skills organized into 4 categories` → `20 skills organized into 5 categories`

Use the edit tool with exact old_str/new_str pairs. Do NOT read and rewrite the entire file.
  </action>
  <verify>
    <automated>grep -q "version-12.3.0" README.md && grep -q "v12.3.0" README.md && grep -q "all 20 commands" README.md && grep -q "20 skills organized into 5 categories" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All version badges show 12.3.0, all command counts show 20</done>
</task>

<task type="auto">
  <name>Task 3: Add missing commands to Skills Reference</name>
  <files>README.md</files>
  <action>
Add 4 missing commands to the Skills Reference tables:

1. In "### Project — Development Workflow" section (after line 237, after complete-milestone row):
   Add row: `| sync-version | \`/pd:sync-version\` | Sync version from package.json across README badges, version text, and doc file headers |`

2. In "### Utility — Helper Commands" section (after line 254, after what-next row):
   Add 3 rows:
   - `| stats | \`/pd:stats\` | Display comprehensive project statistics including phases, plans, requirements, milestones |`
   - `| health | \`/pd:health\` | Diagnose planning directory issues — missing files, STATE.md validation |`
   - `| discover | \`/pd:discover\` | Discover MCP tools and built-in tools across all configured platforms |`

Use surgical edits — find the table row to append after and add the new row.
  </action>
  <verify>
    <automated>grep -q "pd:sync-version" README.md && grep -q "pd:stats" README.md && grep -q "pd:health" README.md && grep -q "pd:discover" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 4 missing commands appear in Skills Reference tables</done>
</task>

</tasks>

<verification>
Run all checks:
```bash
# Version checks
grep "12.3.0" VERSION
grep '"version": "12.3.0"' package.json
grep "version-12.3.0" README.md

# Count checks  
grep "all 20 commands" README.md
grep "20 skills organized into 5 categories" README.md

# New commands present
grep "pd:sync-version" README.md
grep "pd:stats" README.md
grep "pd:health" README.md
grep "pd:discover" README.md

# Verify no broken markdown (quick sanity)
head -35 README.md
```
</verification>

<success_criteria>
- VERSION file contains exactly `12.3.0`
- package.json version field is `"version": "12.3.0"`
- README badge shows `version-12.3.0`
- README text shows `Current version: v12.3.0`
- README shows `all 20 commands` and `20 skills organized into 5 categories`
- Skills Reference includes pd:sync-version, pd:stats, pd:health, pd:discover
- No other content in README.md is modified
</success_criteria>

<output>
After completion, create `.planning/phases/150-readme-update/150-01-SUMMARY.md`
</output>
