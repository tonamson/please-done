---
phase: 84-documentation-version-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - INTEGRATION_GUIDE.md
  - docs/commands/audit.md
  - docs/commands/conventions.md
  - docs/commands/onboard.md
  - docs/commands/status.md
autonomous: true
requirements:
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "INTEGRATION_GUIDE.md exists at repo root"
    - "INTEGRATION_GUIDE.md covers all 5 required topics"
    - "All 4 missing command docs exist in docs/commands/"
    - "Command docs use extended format with Arguments and Examples sections"
  artifacts:
    - path: "INTEGRATION_GUIDE.md"
      provides: "Fork workflow, stack customization, skill architecture"
      contains: "Fork Workflow"
    - path: "docs/commands/audit.md"
      provides: "pd:audit command documentation"
      contains: "pd audit"
    - path: "docs/commands/conventions.md"
      provides: "pd:conventions command documentation"
      contains: "pd conventions"
    - path: "docs/commands/onboard.md"
      provides: "pd:onboard command documentation"
      contains: "pd onboard"
    - path: "docs/commands/status.md"
      provides: "pd:status command documentation"
      contains: "pd status"
  key_links:
    - from: "README.md"
      to: "INTEGRATION_GUIDE.md"
      via: "documentation link at lines 238 and 524"
      pattern: "INTEGRATION_GUIDE.md"
    - from: "docs/commands/audit.md"
      to: "commands/pd/audit.md"
      via: "source skill definition"
      pattern: "pd audit"
---

<objective>
Create INTEGRATION_GUIDE.md and 4 missing command docs (audit.md, conventions.md, onboard.md, status.md).

Purpose: Close DOC-02 (dead INTEGRATION_GUIDE.md link) and DOC-03 (missing command docs) requirements.
Output: 5 new documentation files — 1 integration guide at repo root, 4 command docs in docs/commands/.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-documentation-version-consistency/84-CONTEXT.md
@.planning/phases/84-documentation-version-consistency/84-RESEARCH.md

<interfaces>
<!-- Format reference for command docs from docs/commands/scan.md -->
Standard format:
- # Command `pd [name]`
- ## Purpose
- ## How It Works
- ## When to run this command?
- ## Output
- ---
- **Next step:** [pd next](next.md)

Extended format (for DOC-03) adds:
- ## Arguments (after Purpose)
- ## Examples (before ---)

<!-- Source command definitions (read for content) -->
- commands/pd/audit.md — model: opus, args: [path] [--full|--only|--poc|--auto-fix]
- commands/pd/conventions.md — model: sonnet, no args
- commands/pd/onboard.md — model: sonnet, args: [project path]
- commands/pd/status.md — model: haiku, no args, read-only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create INTEGRATION_GUIDE.md (DOC-02)</name>
  <files>INTEGRATION_GUIDE.md</files>
  <read_first>
    - README.md (lines 238 and 524 reference INTEGRATION_GUIDE.md)
    - commands/pd/rules/ (directory listing for stack examples)
    - workflows/init.md (for Step 4 stack detection patterns)
  </read_first>
  <action>
Create `INTEGRATION_GUIDE.md` at repo root. This file is referenced by README.md at lines 238 and 524.

**File content (INTEGRATION_GUIDE.md):**
```markdown
# Integration Guide

This guide explains how to customize Please Done for your workflow — forking, adding stacks, editing rules, and understanding skill architecture.

## Fork Workflow

To customize Please Done for your own conventions:

1. **Fork the repository**
   ```bash
   gh repo fork tonamson/please-done --clone
   cd please-done
   ```

2. **Edit files** in `commands/pd/rules/` to match your coding conventions

3. **Install from your fork**
   ```bash
   npm install
   node bin/install.js --target claude  # or codex, gemini, opencode, copilot
   ```

4. **Update anytime** — re-run install after editing rules

Every time `/pd:init` runs, your customized rules are automatically copied to `.planning/rules/`.

## Adding a New Stack Rule

To add support for a new framework:

1. **Create a rule file** in `commands/pd/rules/[stack].md`:
   ```markdown
   # [Stack Name] Conventions
   
   ## Code Style
   - [Specific conventions for this stack]
   
   ## Architecture
   - [Project structure patterns]
   
   ## Do / Don't
   - [Stack-specific recommendations]
   ```

2. **Add detection pattern** in `workflows/init.md` Step 4:
   - Find the stack detection section
   - Add a new condition checking for stack-specific files (e.g., `pubspec.yaml` for Flutter, `next.config.*` for NextJS)
   - Set the appropriate rule file to copy

3. **Test detection** — run `/pd:init` in a project using your new stack

## Editing Existing Rules

Rules live in `commands/pd/rules/`:

| Rule File | Detected By | Conventions |
|-----------|-------------|-------------|
| `flutter.md` | `pubspec.yaml` + flutter | Dart, GetX, Dio |
| `nestjs.md` | NestJS modules | TypeScript decorators, DI |
| `nextjs.md` | `next.config.*` | App Router, Server Components |
| `solidity.md` | `.sol` files | OpenZeppelin v5, gas optimization |
| `wordpress.md` | `wp-config.php` | WP standards, sanitization |
| `general.md` | Fallback | Generic conventions |

To customize:
1. Edit the rule file directly
2. Reinstall skills: `node bin/install.js --target [platform]`
3. Rules auto-copy to `.planning/rules/` on next `/pd:init`

## Anchor Patterns

Skills reference rules using anchor patterns:

```markdown
@commands/pd/rules/nextjs.md
```

This syntax tells the AI to load the specified file as context. Anchors work for:
- `@commands/pd/rules/` — Framework-specific conventions
- `@workflows/` — Workflow definitions
- `@references/` — Reference documents (guards, templates)
- `@templates/` — File templates

Resolution: Anchors resolve relative to the Please Done installation directory.

## Cross-References Between Skills

Skills (`commands/pd/*.md`) call workflows (`workflows/*.md`) via the `<execution_context>` block:

```markdown
<execution_context>
@workflows/plan.md (required)
@references/conventions.md (required)
</execution_context>
```

**Architecture:**
- **Skills** = Entry points invoked by users (`/pd:init`, `/pd:plan`)
- **Workflows** = Step-by-step execution logic
- **References** = Guards, checklists, templates
- **Rules** = Framework-specific conventions

**Example flow:**
```
/pd:plan (skill)
  → @workflows/plan.md (workflow)
    → @references/guard-context.md (guard)
    → @commands/pd/rules/nextjs.md (rule)
```

Skills define WHAT to do. Workflows define HOW to do it. Rules define WHAT CONVENTIONS to follow.

***

See [README.md](README.md) for installation and usage. See [docs/commands/](docs/commands/) for individual command documentation.
```

Per D-02 from CONTEXT.md. Covers all 5 required topics: fork workflow, add stacks, edit rules, anchor patterns, cross-references.
  </action>
  <verify>
    <automated>test -f INTEGRATION_GUIDE.md && grep -q "Fork Workflow" INTEGRATION_GUIDE.md && grep -q "Adding a New Stack" INTEGRATION_GUIDE.md && grep -q "Editing Existing Rules" INTEGRATION_GUIDE.md && grep -q "Anchor Patterns" INTEGRATION_GUIDE.md && grep -q "Cross-References" INTEGRATION_GUIDE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - INTEGRATION_GUIDE.md exists at repo root (not in docs/ or .planning/)
    - File contains `## Fork Workflow` section
    - File contains `## Adding a New Stack Rule` section
    - File contains `## Editing Existing Rules` section
    - File contains `## Anchor Patterns` section
    - File contains `## Cross-References Between Skills` section
    - Total file length is 80+ lines (comprehensive guide)
  </acceptance_criteria>
  <done>INTEGRATION_GUIDE.md exists at repo root with all 5 required topic sections</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/commands/audit.md (DOC-03)</name>
  <files>docs/commands/audit.md</files>
  <read_first>
    - commands/pd/audit.md (source skill definition — model: opus, args, objective, output)
    - docs/commands/scan.md (format reference)
  </read_first>
  <action>
Create `docs/commands/audit.md` using extended format.

**File content (docs/commands/audit.md):**
```markdown
# Command `pd audit`

## Purpose

Run a comprehensive OWASP Top 10 security audit, dispatching 13 parallel scanners to analyze your codebase for vulnerabilities.

## Arguments

| Argument | Required | Description |
|----------|----------|-------------|
| `path` | No | Path to audit (default: current directory) |
| `--full` | No | Run all 13 scanners (default behavior) |
| `--only cat1,cat2` | No | Run only specified scanner categories |
| `--poc` | No | Generate proof-of-concept exploits for findings |
| `--auto-fix` | No | Not yet supported in this version |

## How It Works

1. **Detect mode:** Check for `.planning/PROJECT.md` — if present, run in "integrated" mode; otherwise "standalone" mode
2. **Dispatch scanners:** Launch 13 OWASP scanners in parallel (2 per wave)
3. **Consolidate results:** Collect findings from all scanners
4. **Cross-analysis:** Identify patterns across multiple vulnerability categories
5. **Generate report:** Create SECURITY_REPORT.md with all findings and evidence

## When to run this command?

- Before deploying to production
- After adding authentication or payment features
- During security review sprints
- When onboarding a new codebase with unknown security posture

## Output

- `SECURITY_REPORT.md` — Consolidated security findings
  - Standalone mode: Created at `./SECURITY_REPORT.md`
  - Integrated mode: Created at `.planning/audit/SECURITY_REPORT.md`
- Evidence files — Supporting data in a temp directory

## Examples

```bash
pd audit                     # Full audit of current directory
pd audit src/                # Audit specific path
pd audit --only injection,auth  # Run only injection and auth scanners
pd audit --poc               # Generate proof-of-concept exploits
```

***

**Next step:** Review `SECURITY_REPORT.md` and fix high-severity findings, or [pd fix-bug](fix-bug.md) to address issues.
```

Per D-03 from CONTEXT.md. Uses extended format with Arguments and Examples sections.
  </action>
  <verify>
    <automated>test -f docs/commands/audit.md && grep -q "# Command \`pd audit\`" docs/commands/audit.md && grep -q "## Arguments" docs/commands/audit.md && grep -q "## Examples" docs/commands/audit.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - docs/commands/audit.md exists
    - Contains heading `# Command \`pd audit\``
    - Contains `## Purpose` section
    - Contains `## Arguments` table with path, --full, --only, --poc, --auto-fix
    - Contains `## How It Works` with numbered steps
    - Contains `## When to run this command?` with bullet points
    - Contains `## Output` describing SECURITY_REPORT.md
    - Contains `## Examples` with bash code block
    - Contains `**Next step:**` footer
  </acceptance_criteria>
  <done>docs/commands/audit.md exists with extended format including Arguments and Examples</done>
</task>

<task type="auto">
  <name>Task 3: Create docs/commands/conventions.md (DOC-03)</name>
  <files>docs/commands/conventions.md</files>
  <read_first>
    - commands/pd/conventions.md (source skill definition — model: sonnet, no args, creates CLAUDE.md)
    - docs/commands/scan.md (format reference)
  </read_first>
  <action>
Create `docs/commands/conventions.md` using extended format.

**File content (docs/commands/conventions.md):**
```markdown
# Command `pd conventions`

## Purpose

Analyze the project codebase to detect coding conventions, ask about user preferences, and create or update `CLAUDE.md` with project-specific style guidelines.

## Arguments

| Argument | Required | Description |
|----------|----------|-------------|
| (none) | — | No arguments needed |

## How It Works

1. **Scan codebase:** Analyze source files to detect existing patterns (naming, formatting, imports)
2. **Identify conventions:** Extract coding style, architecture patterns, and common practices
3. **Ask user preferences:** Prompt for preferences not detectable from code (e.g., commit message style)
4. **Generate CLAUDE.md:** Create or update the conventions file at repo root

## When to run this command?

- When starting work on an existing codebase
- After onboarding to capture project conventions
- When team conventions have changed and need documentation
- Before `/pd:plan` to ensure code follows project style

## Output

- `CLAUDE.md` — Project coding conventions file at repo root
  - Code Style section (formatting, quotes, indentation)
  - Architecture section (state management, CSS approach, API patterns)
  - Do / Don't section (project-specific recommendations)

## Examples

```bash
pd conventions              # Analyze project and create CLAUDE.md
```

***

**Next step:** [pd plan](plan.md) or [pd write-code](write-code.md) to start coding with detected conventions.
```

Per D-03 from CONTEXT.md. Uses extended format with Arguments and Examples sections.
  </action>
  <verify>
    <automated>test -f docs/commands/conventions.md && grep -q "# Command \`pd conventions\`" docs/commands/conventions.md && grep -q "## Arguments" docs/commands/conventions.md && grep -q "## Examples" docs/commands/conventions.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - docs/commands/conventions.md exists
    - Contains heading `# Command \`pd conventions\``
    - Contains `## Purpose` describing CLAUDE.md creation
    - Contains `## Arguments` table (showing no arguments needed)
    - Contains `## How It Works` with numbered steps
    - Contains `## When to run this command?` with bullet points
    - Contains `## Output` describing CLAUDE.md
    - Contains `## Examples` with bash code block
    - Contains `**Next step:**` footer
  </acceptance_criteria>
  <done>docs/commands/conventions.md exists with extended format including Arguments and Examples</done>
</task>

<task type="auto">
  <name>Task 4: Create docs/commands/onboard.md (DOC-03)</name>
  <files>docs/commands/onboard.md</files>
  <read_first>
    - commands/pd/onboard.md (source skill definition — model: sonnet, project path arg, calls init+scan)
    - docs/commands/scan.md (format reference)
  </read_first>
  <action>
Create `docs/commands/onboard.md` using extended format.

**File content (docs/commands/onboard.md):**
```markdown
# Command `pd onboard`

## Purpose

Orient AI to an unfamiliar codebase with a single command — initialize planning structure, scan code, analyze git history, and create a ready-to-use `.planning/` directory. Fully automated with no user prompts.

## Arguments

| Argument | Required | Description |
|----------|----------|-------------|
| `path` | No | Project path to onboard (default: current directory) |

## How It Works

1. **Run init:** Call `/pd:init` internally to create `.planning/` structure and detect tech stack
2. **Run scan:** Call `/pd:scan` internally to analyze codebase and create scan report
3. **Analyze git history:** Extract project vision and patterns from commit history
4. **Create PROJECT.md:** Generate project baseline with vision, tech stack, language policy
5. **Create planning files:** Set up ROADMAP.md, STATE.md, REQUIREMENTS.md with initial templates

## When to run this command?

- First time working with a new codebase
- After cloning a repo that doesn't have `.planning/`
- To quickly orient AI before starting development
- When you want init + scan + project setup in one command

## Output

- `.planning/CONTEXT.md` — Project context (via init)
- `.planning/rules/*.md` — Framework-specific rules (via init)
- `.planning/scan/SCAN_REPORT.md` — Code analysis report (via scan)
- `.planning/PROJECT.md` — Vision, tech stack, language policy
- `.planning/ROADMAP.md` — Initial roadmap with v1.0 placeholder
- `.planning/CURRENT_MILESTONE.md` — Pointer to v1.0
- `.planning/STATE.md` — Initial working state
- `.planning/REQUIREMENTS.md` — Requirements placeholder

## Examples

```bash
pd onboard                  # Onboard current directory
pd onboard ~/projects/myapp # Onboard specific project
```

***

**Next step:** [pd new-milestone](new-milestone.md) to define v1.0 requirements, or [pd plan](plan.md) if requirements are already known.
```

Per D-03 from CONTEXT.md. Uses extended format with Arguments and Examples sections.
  </action>
  <verify>
    <automated>test -f docs/commands/onboard.md && grep -q "# Command \`pd onboard\`" docs/commands/onboard.md && grep -q "## Arguments" docs/commands/onboard.md && grep -q "## Examples" docs/commands/onboard.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - docs/commands/onboard.md exists
    - Contains heading `# Command \`pd onboard\``
    - Contains `## Purpose` describing single-command orientation
    - Contains `## Arguments` table with path argument
    - Contains `## How It Works` with numbered steps mentioning init and scan
    - Contains `## When to run this command?` with bullet points
    - Contains `## Output` listing all created files (.planning/*)
    - Contains `## Examples` with bash code block
    - Contains `**Next step:**` footer
  </acceptance_criteria>
  <done>docs/commands/onboard.md exists with extended format including Arguments and Examples</done>
</task>

<task type="auto">
  <name>Task 5: Create docs/commands/status.md (DOC-03)</name>
  <files>docs/commands/status.md</files>
  <read_first>
    - commands/pd/status.md (source skill definition — model: haiku, no args, read-only, 8 fields)
    - docs/commands/scan.md (format reference)
  </read_first>
  <action>
Create `docs/commands/status.md` using extended format.

**File content (docs/commands/status.md):**
```markdown
# Command `pd status`

## Purpose

Display a read-only 8-field project status dashboard. Does not modify any files or suggest next steps — purely informational.

## Arguments

| Argument | Required | Description |
|----------|----------|-------------|
| (none) | — | No arguments needed |

## How It Works

1. **Read planning files:** Load STATE.md, CURRENT_MILESTONE.md, TASKS.md, PROGRESS.md
2. **Check git log:** Get last commit hash and message
3. **Count bugs:** Glob `.planning/bugs/BUG_*.md` for unresolved issues
4. **Display dashboard:** Print 8 fields in formatted output

## When to run this command?

- Quick check of project state without suggestions
- Before starting work to see current milestone/phase
- After completing tasks to verify progress recorded
- When you need status without the overhead of `what-next`

## Output

- No files created or modified (read-only command)
- 8-line formatted dashboard displayed to console:
  1. **Milestone** — Current milestone name
  2. **Phase** — Current phase number and name
  3. **Plan** — Current plan being executed
  4. **Tasks** — Task completion status (e.g., 2/5)
  5. **Bugs** — Count of unresolved bugs
  6. **Lint** — Lint status or "✓ no active task"
  7. **Blockers** — Any blocking issues
  8. **Last commit** — Most recent commit hash and message

## Examples

```bash
pd status                   # Display 8-field dashboard
```

***

**Next step:** [pd what-next](what-next.md) for actionable suggestions based on current status.
```

Per D-03 from CONTEXT.md. Uses extended format with Arguments and Examples sections. Notes read-only nature and Haiku model usage.
  </action>
  <verify>
    <automated>test -f docs/commands/status.md && grep -q "# Command \`pd status\`" docs/commands/status.md && grep -q "## Arguments" docs/commands/status.md && grep -q "## Examples" docs/commands/status.md && grep -q "read-only" docs/commands/status.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - docs/commands/status.md exists
    - Contains heading `# Command \`pd status\``
    - Contains `## Purpose` mentioning "read-only" and "8-field"
    - Contains `## Arguments` table (showing no arguments needed)
    - Contains `## How It Works` with numbered steps
    - Contains `## When to run this command?` with bullet points
    - Contains `## Output` listing all 8 dashboard fields
    - Contains `## Examples` with bash code block
    - Contains `**Next step:**` footer pointing to what-next
  </acceptance_criteria>
  <done>docs/commands/status.md exists with extended format, notes read-only nature</done>
</task>

</tasks>

<verification>
After all tasks:
```bash
# DOC-02: INTEGRATION_GUIDE.md exists with all 5 topics
test -f INTEGRATION_GUIDE.md && echo "DOC-02 exists: PASS"
grep -q "Fork Workflow" INTEGRATION_GUIDE.md && \
grep -q "Adding a New Stack" INTEGRATION_GUIDE.md && \
grep -q "Anchor Patterns" INTEGRATION_GUIDE.md && \
echo "DOC-02 topics: PASS"

# DOC-03: All 4 command docs exist with extended format
test -f docs/commands/audit.md && \
test -f docs/commands/conventions.md && \
test -f docs/commands/onboard.md && \
test -f docs/commands/status.md && \
echo "DOC-03 files: PASS"

# Extended format check (Arguments + Examples sections)
grep -q "## Arguments" docs/commands/audit.md && \
grep -q "## Examples" docs/commands/audit.md && \
echo "DOC-03 format: PASS"
```
</verification>

<success_criteria>
- INTEGRATION_GUIDE.md exists at repo root (not docs/, not .planning/)
- INTEGRATION_GUIDE.md contains sections for: Fork Workflow, Adding a New Stack Rule, Editing Existing Rules, Anchor Patterns, Cross-References Between Skills
- docs/commands/audit.md exists with extended format
- docs/commands/conventions.md exists with extended format
- docs/commands/onboard.md exists with extended format
- docs/commands/status.md exists with extended format
- All 4 command docs have: Purpose, Arguments, How It Works, When to run, Output, Examples, Next step
- README.md links at lines 238 and 524 now resolve to existing file
</success_criteria>

<output>
After completion, create `.planning/phases/84-documentation-version-consistency/84-02-SUMMARY.md`
</output>
