---
phase: 84-documentation-version-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true
requirements:
  - DOC-01
  - DOC-04

must_haves:
  truths:
    - "README.md version badge displays 4.0.0"
    - "CHANGELOG.md has deprecation notice pointing to MILESTONES.md"
    - "README.md lists all 6 supported tech stacks"
  artifacts:
    - path: "README.md"
      provides: "Version badge fix and Supported Stacks section"
      contains: "version-4.0.0"
    - path: "CHANGELOG.md"
      provides: "Deprecation notice"
      contains: "frozen at v2.8.0"
  key_links:
    - from: "CHANGELOG.md"
      to: ".planning/MILESTONES.md"
      via: "deprecation notice link"
      pattern: "MILESTONES\\.md"
---

<objective>
Fix README.md version badge and add Supported Stacks section; add deprecation notice to CHANGELOG.md.

Purpose: Close DOC-01 (stale badge) and DOC-04 (outdated changelog) requirements. Also implements D-04 (Supported Stacks section).
Output: Updated README.md with correct version and stacks section; CHANGELOG.md with deprecation notice.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-documentation-version-consistency/84-CONTEXT.md
@.planning/phases/84-documentation-version-consistency/84-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix README.md version badge (DOC-01)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current badge on line 3)
    - VERSION (source of truth: 4.0.0)
  </read_first>
  <action>
Edit README.md line 3 to change the version badge from 2.8.0 to 4.0.0.

**Current (line 3):**
```markdown
[![Version](https://img.shields.io/badge/version-2.8.0-blue.svg)](https://github.com/tonamson/please-done/releases)
```

**Target (line 3):**
```markdown
[![Version](https://img.shields.io/badge/version-4.0.0-blue.svg)](https://github.com/tonamson/please-done/releases)
```

Also update line 14 "Current version" text from `v2.8.0` to `v4.0.0`:
- Current: `**Current version: v2.8.0**`
- Target: `**Current version: v4.0.0**`

Per D-01 from CONTEXT.md.
  </action>
  <verify>
    <automated>grep -q "version-4.0.0" README.md && grep -q "Current version: v4.0.0" README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README.md line 3 contains `version-4.0.0-blue.svg`
    - README.md contains `**Current version: v4.0.0**`
    - No occurrence of `2.8.0` remains in README.md badge or version text
  </acceptance_criteria>
  <done>README.md badge shows 4.0.0, current version text shows v4.0.0</done>
</task>

<task type="auto">
  <name>Task 2: Add Supported Stacks section to README.md (D-04)</name>
  <files>README.md</files>
  <read_first>
    - README.md (find insertion point near line 490)
    - commands/pd/rules/ (6 stack files: flutter.md, general.md, nestjs.md, nextjs.md, solidity.md, wordpress.md)
  </read_first>
  <action>
Insert a "Supported Tech Stacks" section in README.md. The Table of Contents already has an entry for "Supported Tech Stacks" (line 31), so this section should be placed to match that ToC entry.

Find the section heading around line 486-490 that discusses stacks. The existing text at line 488 says:
```
NestJS, NextJS, WordPress, Solidity, and Flutter have rules + detailed analysis. Other stacks are detected but only list files, applying `general.md`.
```

Replace lines 486-490 (the paragraph starting "NestJS, NextJS..." and "Extending with a new stack") with a proper section:

**Insert (replacing lines 486-490):**
```markdown
### Supported Tech Stacks

Rules in `commands/pd/rules/` provide framework-specific conventions:

| Stack | Rule File | Description |
|-------|-----------|-------------|
| Flutter | `flutter.md` | Dart conventions, GetX state management, Dio HTTP |
| NestJS | `nestjs.md` | TypeScript decorators, dependency injection, guards |
| NextJS | `nextjs.md` | App Router patterns, server components, data fetching |
| Solidity | `solidity.md` | OpenZeppelin v5, SafeERC20, gas optimization |
| WordPress | `wordpress.md` | WP coding standards, sanitize/escape, REST API |
| General | `general.md` | Fallback conventions for unlisted frameworks |

> Stacks are auto-detected by `/pd:init` and rules are copied to `.planning/rules/`.

**Extending with a new stack**: Add a `commands/pd/rules/[stack].md` file + detection patterns in `workflows/init.md` Step 4.
```

Per D-04 from CONTEXT.md. Lists all 6 stacks from `commands/pd/rules/` directory.
  </action>
  <verify>
    <automated>grep -q "### Supported Tech Stacks" README.md && grep -A 20 "### Supported Tech Stacks" README.md | grep -q "flutter.md" && grep -A 20 "### Supported Tech Stacks" README.md | grep -q "general.md" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `### Supported Tech Stacks` heading
    - README.md contains all 6 stacks in a table: Flutter, NestJS, NextJS, Solidity, WordPress, General
    - Each stack row has rule file name and description
    - Section includes note about auto-detection by `/pd:init`
  </acceptance_criteria>
  <done>README.md has Supported Tech Stacks section listing all 6 stacks with descriptions</done>
</task>

<task type="auto">
  <name>Task 3: Add deprecation notice to CHANGELOG.md (DOC-04)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (current first line is "# Skills Changelog")
    - .planning/MILESTONES.md (verify path exists for link target)
  </read_first>
  <action>
Prepend a deprecation notice at the very top of CHANGELOG.md (before the existing "# Skills Changelog" heading).

**Insert at line 1 (before existing content):**
```markdown
> **Note:** This changelog is frozen at v2.8.0. For history from v3.0 onward, see [MILESTONES.md](.planning/MILESTONES.md).

```

The existing content starting with `# Skills Changelog` follows after a blank line.

**Result structure:**
```markdown
> **Note:** This changelog is frozen at v2.8.0. For history from v3.0 onward, see [MILESTONES.md](.planning/MILESTONES.md).

# Skills Changelog
... existing content ...
```

Per D-05 from CONTEXT.md. Keep all existing entries intact — no removal.
  </action>
  <verify>
    <automated>head -1 CHANGELOG.md | grep -q "frozen at v2.8.0" && grep -q "MILESTONES.md" CHANGELOG.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md line 1 starts with `> **Note:**`
    - CHANGELOG.md contains text `frozen at v2.8.0`
    - CHANGELOG.md contains link `[MILESTONES.md](.planning/MILESTONES.md)`
    - All original changelog entries remain intact (file has more than 30 lines)
  </acceptance_criteria>
  <done>CHANGELOG.md has deprecation notice at top, all existing entries preserved</done>
</task>

</tasks>

<verification>
After all tasks:
```bash
# DOC-01: Badge shows 4.0.0
grep -q "version-4.0.0" README.md && echo "DOC-01: PASS"

# D-04: Supported Stacks section exists with all 6 stacks
grep -q "### Supported Tech Stacks" README.md && echo "D-04 heading: PASS"
grep -q "flutter.md" README.md && grep -q "general.md" README.md && echo "D-04 stacks: PASS"

# DOC-04: CHANGELOG deprecation notice
head -1 CHANGELOG.md | grep -q "frozen at v2.8.0" && echo "DOC-04: PASS"
```
</verification>

<success_criteria>
- README.md version badge displays `4.0.0` (not `2.8.0`)
- README.md version text displays `v4.0.0`
- README.md has "Supported Tech Stacks" section listing all 6 stacks
- CHANGELOG.md has deprecation notice as first line
- CHANGELOG.md links to `.planning/MILESTONES.md`
- All existing CHANGELOG.md entries preserved
</success_criteria>

<output>
After completion, create `.planning/phases/84-documentation-version-consistency/84-01-SUMMARY.md`
</output>
