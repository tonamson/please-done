---
phase: 46-nen-tang-scanner
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - bin/lib/resource-config.js
  - test/smoke-resource-config.test.js
  - test/smoke-agent-files.test.js
autonomous: true
requirements: [AGENT-04, WIRE-04]

must_haves:
  truths:
    - "AGENT_REGISTRY co 9 entries (7 cu + pd-sec-scanner + pd-sec-reporter)"
    - "pd-sec-scanner entry co categories array voi 13 slugs"
    - "getAgentConfig('pd-sec-scanner') tra ve config day du KE CA categories field"
    - "13 scanner files cu da bi xoa, chi con pd-sec-scanner.md va pd-sec-reporter.md"
    - "Tat ca smoke tests PASS voi 9 agents"
  artifacts:
    - path: "bin/lib/resource-config.js"
      provides: "AGENT_REGISTRY voi 9 entries"
      contains: "pd-sec-scanner"
    - path: "test/smoke-resource-config.test.js"
      provides: "Test cho 9 agent entries"
      contains: "9"
  key_links:
    - from: "bin/lib/resource-config.js"
      to: "commands/pd/agents/pd-sec-scanner.md"
      via: "AGENT_REGISTRY entry pd-sec-scanner"
      pattern: "pd-sec-scanner"
    - from: "bin/lib/resource-config.js"
      to: "references/security-rules.yaml"
      via: "categories array slugs khop voi YAML top-level keys"
      pattern: "categories"
---

<objective>
Dang ky pd-sec-scanner va pd-sec-reporter vao AGENT_REGISTRY, sua getAgentConfig() de spread extra fields (categories), cap nhat tests, va xoa 13 scanner files cu.

Purpose: Hoan tat wiring de Phase 47 co the dispatch scanner tu AGENT_REGISTRY va truy cap categories list.
Output: resource-config.js voi 9 entries, tests updated, 13 files cu bi xoa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-nen-tang-scanner/46-CONTEXT.md
@.planning/phases/46-nen-tang-scanner/46-RESEARCH.md
@.planning/phases/46-nen-tang-scanner/46-01-SUMMARY.md

<interfaces>
From bin/lib/resource-config.js (hien tai):
```javascript
const AGENT_REGISTRY = {
  "pd-bug-janitor": { tier: "scout", tools: ["Read", "Glob", "Grep", "AskUserQuestion", "Bash"] },
  "pd-code-detective": { tier: "builder", tools: ["Read", "Glob", "Grep", "mcp__fastcode__code_qa"] },
  "pd-doc-specialist": { tier: "scout", tools: ["Read", "mcp__context7__resolve-library-id", "mcp__context7__query-docs"] },
  "pd-repro-engineer": { tier: "builder", tools: ["Read", "Write", "Edit", "Bash"] },
  "pd-fix-architect": { tier: "architect", tools: ["Read", "Write", "Edit", "Bash", "Glob", "Grep"] },
  "pd-evidence-collector": { tier: "builder", tools: ["Read", "Glob", "Grep", "Write", "Bash", "mcp__context7__resolve-library-id", "mcp__context7__query-docs"] },
  "pd-fact-checker": { tier: "architect", tools: ["Read", "Glob", "Grep", "Bash"] },
};

// getAgentConfig hien tai tra ve 6 fields co dinh:
// { name, tier, model, effort, maxTurns, tools }
// KHONG spread extra fields tu agent entry
```

From test/smoke-resource-config.test.js (dong 217-218):
```javascript
it('AGENT_REGISTRY co 7 agents', () => {
  assert.equal(Object.keys(AGENT_REGISTRY).length, 7);
});
```

From test/smoke-agent-files.test.js (dong 18-26):
```javascript
const AGENT_NAMES = [
  'pd-bug-janitor', 'pd-code-detective', 'pd-doc-specialist',
  'pd-repro-engineer', 'pd-fix-architect',
  'pd-evidence-collector', 'pd-fact-checker',
];
// Test chi check .claude/agents/ directory — pd-sec-scanner nam o commands/pd/agents/ nen KHONG them vao AGENT_NAMES
```

13 files can xoa:
- commands/pd/agents/pd-sec-sql-injection.md
- commands/pd/agents/pd-sec-xss.md
- commands/pd/agents/pd-sec-cmd-injection.md
- commands/pd/agents/pd-sec-path-traversal.md
- commands/pd/agents/pd-sec-secrets.md
- commands/pd/agents/pd-sec-auth.md
- commands/pd/agents/pd-sec-deserialization.md
- commands/pd/agents/pd-sec-misconfig.md
- commands/pd/agents/pd-sec-prototype-pollution.md
- commands/pd/agents/pd-sec-crypto.md
- commands/pd/agents/pd-sec-insecure-design.md
- commands/pd/agents/pd-sec-vuln-deps.md
- commands/pd/agents/pd-sec-logging.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Them 2 entries vao AGENT_REGISTRY va sua getAgentConfig()</name>
  <files>bin/lib/resource-config.js, test/smoke-resource-config.test.js</files>
  <read_first>
    - bin/lib/resource-config.js
    - test/smoke-resource-config.test.js
    - references/security-rules.yaml
  </read_first>
  <action>
    **Buoc 1 — Sua AGENT_REGISTRY trong `bin/lib/resource-config.js`:**

    Them 2 entries moi vao cuoi AGENT_REGISTRY object (per D-06, D-07):

    ```javascript
    "pd-sec-scanner": {
      tier: "scout",
      tools: ["Read", "Glob", "Grep", "mcp__fastcode__code_qa"],
      categories: [
        "sql-injection", "xss", "cmd-injection", "path-traversal",
        "secrets", "auth", "deserialization", "misconfig",
        "prototype-pollution", "crypto", "insecure-design",
        "vuln-deps", "logging",
      ],
    },
    "pd-sec-reporter": {
      tier: "builder",
      tools: ["Read", "Write", "Glob"],
    },
    ```

    **Buoc 2 — Sua getAgentConfig() de spread extra fields:**

    Thay doi return statement trong getAgentConfig() tu:
    ```javascript
    return {
      name: agentName,
      tier: agent.tier,
      model: tierConfig.model,
      effort: tierConfig.effort,
      maxTurns: tierConfig.maxTurns,
      tools: [...agent.tools],
    };
    ```
    Thanh:
    ```javascript
    const { tier, tools, ...extra } = agent;
    return {
      name: agentName,
      tier,
      model: tierConfig.model,
      effort: tierConfig.effort,
      maxTurns: tierConfig.maxTurns,
      tools: [...tools],
      ...extra,
    };
    ```
    Nhu vay `categories` (va bat ky field moi nao trong tuong lai) se tu dong duoc tra ve.

    Cap nhat JSDoc cua getAgentConfig() de ghi nhan them `...extra` fields.

    **Buoc 3 — Cap nhat test/smoke-resource-config.test.js:**

    a) Sua assertion `AGENT_REGISTRY co 7 agents` thanh `AGENT_REGISTRY co 9 agents`:
    ```javascript
    it('AGENT_REGISTRY co 9 agents', () => {
      assert.equal(Object.keys(AGENT_REGISTRY).length, 9);
    });
    ```

    b) Them test cho pd-sec-scanner config:
    ```javascript
    it('tra ve full config cho pd-sec-scanner', () => {
      const cfg = getAgentConfig('pd-sec-scanner');
      assert.equal(cfg.name, 'pd-sec-scanner');
      assert.equal(cfg.tier, 'scout');
      assert.equal(cfg.model, 'haiku');
      assert.equal(cfg.effort, 'low');
      assert.equal(cfg.maxTurns, 15);
      assert.ok(cfg.tools.includes('Read'));
      assert.ok(cfg.tools.includes('Glob'));
      assert.ok(cfg.tools.includes('Grep'));
      assert.ok(cfg.tools.includes('mcp__fastcode__code_qa'));
      // Extra field: categories
      assert.ok(Array.isArray(cfg.categories), 'categories phai la array');
      assert.equal(cfg.categories.length, 13);
      assert.ok(cfg.categories.includes('sql-injection'));
      assert.ok(cfg.categories.includes('logging'));
    });
    ```

    c) Them test cho pd-sec-reporter config:
    ```javascript
    it('tra ve full config cho pd-sec-reporter', () => {
      const cfg = getAgentConfig('pd-sec-reporter');
      assert.equal(cfg.name, 'pd-sec-reporter');
      assert.equal(cfg.tier, 'builder');
      assert.equal(cfg.model, 'sonnet');
      assert.equal(cfg.effort, 'medium');
      assert.equal(cfg.maxTurns, 25);
      assert.ok(cfg.tools.includes('Read'));
      assert.ok(cfg.tools.includes('Write'));
      assert.ok(cfg.tools.includes('Glob'));
    });
    ```

    d) Them test cho isHeavyAgent voi pd-sec-scanner:
    ```javascript
    it('true cho pd-sec-scanner (co fastcode)', () => {
      assert.equal(isHeavyAgent('pd-sec-scanner'), true);
    });
    ```

    e) Them test xac nhan extra fields duoc spread:
    ```javascript
    it('getAgentConfig spread extra fields tu AGENT_REGISTRY', () => {
      const cfg = getAgentConfig('pd-sec-scanner');
      assert.ok('categories' in cfg, 'categories phai co trong config');
    });
    ```

    **Buoc 4 — Chay tests:**
    `node --test test/smoke-resource-config.test.js`
  </action>
  <verify>
    <automated>node --test test/smoke-resource-config.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "pd-sec-scanner" bin/lib/resource-config.js` tra ve it nhat 1 dong
    - `grep "pd-sec-reporter" bin/lib/resource-config.js` tra ve it nhat 1 dong
    - `grep "categories" bin/lib/resource-config.js` tra ve it nhat 1 dong (trong AGENT_REGISTRY entry)
    - `grep "\.\.\.extra" bin/lib/resource-config.js` tra ve it nhat 1 dong (spread trong getAgentConfig)
    - `grep "co 9 agents" test/smoke-resource-config.test.js` tra ve 1 dong
    - `grep "pd-sec-scanner" test/smoke-resource-config.test.js` tra ve it nhat 1 dong
    - `grep "pd-sec-reporter" test/smoke-resource-config.test.js` tra ve it nhat 1 dong
    - `node --test test/smoke-resource-config.test.js` exit code 0
    - getAgentConfig('pd-sec-scanner') tra ve object co categories array length 13
    - isHeavyAgent('pd-sec-scanner') tra ve true
  </acceptance_criteria>
  <done>AGENT_REGISTRY co 9 entries, getAgentConfig() spread extra fields, tat ca tests PASS</done>
</task>

<task type="auto">
  <name>Task 2: Xoa 13 scanner files cu va cap nhat smoke-agent-files test</name>
  <files>test/smoke-agent-files.test.js</files>
  <read_first>
    - test/smoke-agent-files.test.js
    - commands/pd/agents/pd-sec-scanner.md
    - references/security-rules.yaml
  </read_first>
  <action>
    **Buoc 1 — Xac nhan template va YAML san sang:**
    Doc `commands/pd/agents/pd-sec-scanner.md` va `references/security-rules.yaml` de dam bao chung ton tai va day du truoc khi xoa files cu.

    **Buoc 2 — Xoa 13 scanner files cu (per D-04):**
    ```bash
    rm commands/pd/agents/pd-sec-sql-injection.md
    rm commands/pd/agents/pd-sec-xss.md
    rm commands/pd/agents/pd-sec-cmd-injection.md
    rm commands/pd/agents/pd-sec-path-traversal.md
    rm commands/pd/agents/pd-sec-secrets.md
    rm commands/pd/agents/pd-sec-auth.md
    rm commands/pd/agents/pd-sec-deserialization.md
    rm commands/pd/agents/pd-sec-misconfig.md
    rm commands/pd/agents/pd-sec-prototype-pollution.md
    rm commands/pd/agents/pd-sec-crypto.md
    rm commands/pd/agents/pd-sec-insecure-design.md
    rm commands/pd/agents/pd-sec-vuln-deps.md
    rm commands/pd/agents/pd-sec-logging.md
    ```

    **Buoc 3 — Cap nhat test/smoke-agent-files.test.js:**

    Test hien tai chi check `.claude/agents/` directory (7 agents). Template `pd-sec-scanner.md` nam o `commands/pd/agents/` nen KHONG them vao AGENT_NAMES (khong thay doi danh sach 7 agent).

    Tuy nhien, test `Agent files consistency voi resource-config` loop qua `Object.entries(AGENT_REGISTRY)` — sau khi them pd-sec-scanner va pd-sec-reporter vao registry, test se FAIL vi khong tim thay file `.claude/agents/pd-sec-scanner.md`.

    Sua test `Agent files consistency voi resource-config` de chi loop qua agents co trong AGENT_NAMES (agents nam o .claude/agents/), KHONG loop qua toan bo AGENT_REGISTRY:

    ```javascript
    describe('Agent files consistency voi resource-config', () => {
      it('model trong agent files khop voi TIER_MAP', () => {
        for (const agentName of AGENT_NAMES) {
          const agentInfo = AGENT_REGISTRY[agentName];
          const fm = parseAgentFrontmatter(join(AGENTS_DIR, `${agentName}.md`));
          const expectedModel = TIER_MAP[agentInfo.tier].model;
          assert.equal(fm.model, expectedModel,
            `${agentName}: model trong file (${fm.model}) khac voi TIER_MAP[${agentInfo.tier}].model (${expectedModel})`);
        }
      });

      it('tools trong agent files khop voi AGENT_REGISTRY', () => {
        for (const agentName of AGENT_NAMES) {
          const agentInfo = AGENT_REGISTRY[agentName];
          const fm = parseAgentFrontmatter(join(AGENTS_DIR, `${agentName}.md`));
          for (const tool of agentInfo.tools) {
            assert.ok(fm.tools.includes(tool),
              `${agentName}: thieu tool '${tool}' trong agent file`);
          }
          for (const tool of fm.tools) {
            assert.ok(agentInfo.tools.includes(tool),
              `${agentName}: thua tool '${tool}' trong agent file`);
          }
        }
      });
    });
    ```

    Thay doi chinh: doi `Object.entries(AGENT_REGISTRY)` thanh `AGENT_NAMES` de chi kiem tra 7 agents co file tai `.claude/agents/`.

    **Buoc 4 — Chay tat ca tests:**
    ```bash
    node --test test/smoke-agent-files.test.js
    node --test test/smoke-resource-config.test.js
    node --test test/smoke-security-rules.test.js
    ```
  </action>
  <verify>
    <automated>node --test test/smoke-agent-files.test.js && node --test test/smoke-resource-config.test.js && node --test test/smoke-security-rules.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `ls commands/pd/agents/pd-sec-sql-injection.md 2>/dev/null` khong tra ve gi (file da bi xoa)
    - `ls commands/pd/agents/pd-sec-xss.md 2>/dev/null` khong tra ve gi
    - `ls commands/pd/agents/pd-sec-*.md` chi tra ve 2 files: pd-sec-scanner.md va pd-sec-reporter.md
    - `grep "AGENT_NAMES" test/smoke-agent-files.test.js` van co 7 agents (khong them pd-sec-scanner)
    - `grep "Object.entries(AGENT_REGISTRY)" test/smoke-agent-files.test.js` KHONG tra ve ket qua (da doi thanh AGENT_NAMES loop)
    - `node --test test/smoke-agent-files.test.js` exit code 0
    - `node --test test/smoke-resource-config.test.js` exit code 0
    - `node --test test/smoke-security-rules.test.js` exit code 0
  </acceptance_criteria>
  <done>13 scanner files cu da xoa, chi con pd-sec-scanner.md + pd-sec-reporter.md, tat ca smoke tests PASS</done>
</task>

</tasks>

<verification>
1. `node --test test/smoke-security-rules.test.js` — YAML 13 categories dung schema
2. `node --test test/smoke-resource-config.test.js` — 9 agents, extra fields spread, heavy agent detection
3. `node --test test/smoke-agent-files.test.js` — 7 .claude/agents/ files nhat quan voi registry
4. `ls commands/pd/agents/pd-sec-*.md | wc -l` — tra ve 2 (scanner + reporter)
5. `node -e "const r=require('./bin/lib/resource-config');console.log(r.getAgentConfig('pd-sec-scanner').categories.length)"` — tra ve 13
</verification>

<success_criteria>
- AGENT_REGISTRY co 9 entries voi pd-sec-scanner (categories[13]) va pd-sec-reporter
- getAgentConfig() spread extra fields — categories accessible qua API
- 13 scanner files cu da xoa
- Tat ca 3 smoke test files PASS
- Phase 47 co the dispatch scanner bang cach doc categories tu getAgentConfig('pd-sec-scanner')
</success_criteria>

<output>
After completion, create `.planning/phases/46-nen-tang-scanner/46-02-SUMMARY.md`
</output>
