---
phase: 39-tieu-chuan-kiem-chung-chi-muc
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/confidence-scorer.js
  - test/smoke-confidence-scorer.test.js
autonomous: true
requirements:
  - AUDIT-02
must_haves:
  truths:
    - "scoreConfidence tra ve HIGH khi co >= 2 official sources"
    - "scoreConfidence tra ve MEDIUM khi co >= 2 sources bat ky (khong du official)"
    - "scoreConfidence tra ve LOW khi chi co 1 source hoac khong co source"
    - "scoreConfidence tra ve reason giai thich ket qua"
  artifacts:
    - path: "bin/lib/confidence-scorer.js"
      provides: "scoreConfidence pure function rule-based"
      exports: ["scoreConfidence"]
    - path: "test/smoke-confidence-scorer.test.js"
      provides: "Unit tests cho scoreConfidence"
      min_lines: 40
  key_links:
    - from: "bin/lib/confidence-scorer.js"
      to: "research workflow"
      via: "export scoreConfidence"
      pattern: "module\\.exports.*scoreConfidence"
---

<objective>
Tao module confidence-scorer.js voi TDD — pure function tinh confidence rule-based (KHONG LLM self-assessment).

Purpose: AUDIT-02 can confidence scoring cho tung claim. Module nay cung cap logic chung de cac agent va workflow su dung.
Output: `bin/lib/confidence-scorer.js` voi tests xanh toan bo.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-tieu-chuan-kiem-chung-chi-muc/39-CONTEXT.md
@.planning/phases/39-tieu-chuan-kiem-chung-chi-muc/39-RESEARCH.md

<interfaces>
<!-- Pattern tham khao tu research-store.js -->
From bin/lib/research-store.js:
```javascript
const CONFIDENCE_LEVELS = { HIGH: 'HIGH', MEDIUM: 'MEDIUM', LOW: 'LOW' };
function validateConfidence(level) { /* return boolean */ }
module.exports = { CONFIDENCE_LEVELS, CONFIDENCE_CRITERIA, REQUIRED_FIELDS, SOURCE_TYPES, createEntry, parseEntry, validateConfidence, generateFilename };
```

From test/smoke-research-store.test.js:
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD confidence-scorer.js — RED + GREEN + REFACTOR</name>
  <files>bin/lib/confidence-scorer.js, test/smoke-confidence-scorer.test.js</files>
  <read_first>
    - bin/lib/research-store.js (CONFIDENCE_LEVELS constant, validateConfidence pattern)
    - bin/lib/evidence-protocol.js (non-blocking return pattern)
    - test/smoke-research-store.test.js (test structure pattern)
    - .planning/phases/39-tieu-chuan-kiem-chung-chi-muc/39-CONTEXT.md (D-12 den D-15)
  </read_first>
  <behavior>
    - scoreConfidence({ sources: [{ type: 'official' }, { type: 'official' }] }) => { level: 'HIGH', reason: '2 official sources' }
    - scoreConfidence({ sources: [{ type: 'official' }, { type: 'web' }, { type: 'web' }] }) => { level: 'HIGH', reason: ... } (van HIGH vi co >= 2 official? KHONG — chi 1 official => MEDIUM vi co 3 sources tong)
    - scoreConfidence({ sources: [{ type: 'web' }, { type: 'web' }] }) => { level: 'MEDIUM', reason: '2 sources dong y' }
    - scoreConfidence({ sources: [{ type: 'web' }] }) => { level: 'LOW', reason: '1 source duy nhat' }
    - scoreConfidence({ sources: [] }) => { level: 'LOW', reason: 'khong co source nao' }
    - scoreConfidence({}) => { level: 'LOW', reason: 'khong co source nao' }
    - scoreConfidence() => throw Error('thieu tham so options')
    - scoreConfidence(null) => throw Error('thieu tham so options')
    - Source types: 'official' (Context7, codebase, official docs), 'web' (forums, articles, blogs), 'codebase' (project files — counts as official)
    - scoreConfidence({ sources: [{ type: 'codebase' }, { type: 'official' }] }) => { level: 'HIGH', reason: ... }
  </behavior>
  <action>
    TDD RED-GREEN-REFACTOR cycle. Per D-12 den D-15:

    **RED:** Tao test/smoke-confidence-scorer.test.js truoc:
    - describe('scoreConfidence') voi cac test cases tu behavior block
    - Test HIGH: >= 2 official sources (type 'official' hoac 'codebase')
    - Test MEDIUM: >= 2 sources tong nhung < 2 official
    - Test LOW: 0-1 source
    - Test input validation: null/undefined => throw Error
    - Test reason string co noi dung mo ta
    - Chay test — PHAI fail (module chua ton tai)
    - Commit: test(39-01): them failing tests cho scoreConfidence

    **GREEN:** Tao bin/lib/confidence-scorer.js:
    - Module header comment tieng Viet co dau
    - `'use strict';`
    - Function scoreConfidence({ sources }) per D-13, D-14
    - Official source types: 'official', 'codebase' (D-14: "official docs, codebase")
    - HIGH: officialCount >= 2
    - MEDIUM: totalCount >= 2 (nhung officialCount < 2)
    - LOW: totalCount <= 1
    - Return { level, reason } — reason la string tieng Viet mo ta
    - Input validation: throw Error khi thieu tham so (null, undefined)
    - KHONG require('fs'), KHONG I/O — per anti-pattern
    - module.exports = { scoreConfidence }
    - Chay test — PHAI pass
    - Commit: feat(39-01): implement scoreConfidence rule-based

    **REFACTOR (neu can):** Clean up, dam bao JSDoc day du tieng Viet co dau.
  </action>
  <verify>
    <automated>node --test test/smoke-confidence-scorer.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "scoreConfidence" bin/lib/confidence-scorer.js
    - grep -q "module.exports" bin/lib/confidence-scorer.js
    - grep -q "HIGH" bin/lib/confidence-scorer.js
    - grep -q "MEDIUM" bin/lib/confidence-scorer.js
    - grep -q "LOW" bin/lib/confidence-scorer.js
    - grep -q "official" bin/lib/confidence-scorer.js
    - grep -q "scoreConfidence" test/smoke-confidence-scorer.test.js
    - node --test test/smoke-confidence-scorer.test.js 2>&1 | grep -q "pass"
  </acceptance_criteria>
  <done>
    scoreConfidence tra ve HIGH/MEDIUM/LOW dua tren so luong va loai source — rule-based, khong LLM.
    Tests xanh toan bo. Module export scoreConfidence.
  </done>
</task>

</tasks>

<verification>
node --test test/smoke-confidence-scorer.test.js
</verification>

<success_criteria>
- confidence-scorer.js export scoreConfidence pure function
- Rule-based scoring: HIGH >= 2 official, MEDIUM >= 2 any, LOW <= 1
- Tests pass voi >= 8 test cases covering HIGH/MEDIUM/LOW/edge cases
- KHONG co require('fs') trong module
- KHONG co LLM self-assessment logic
</success_criteria>

<output>
After completion, create `.planning/phases/39-tieu-chuan-kiem-chung-chi-muc/39-01-SUMMARY.md`
</output>
