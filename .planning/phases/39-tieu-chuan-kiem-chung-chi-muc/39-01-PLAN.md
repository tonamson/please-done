---
phase: 39-tieu-chuan-kiem-chung-chi-muc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/confidence-scorer.js
  - bin/lib/audit-logger.js
  - bin/lib/index-generator.js
  - test/smoke-confidence-scorer.test.js
  - test/smoke-audit-logger.test.js
  - test/smoke-index-generator.test.js
autonomous: true
requirements:
  - AUDIT-02
  - AUDIT-04
  - STORE-03
---

<objective>
Tao 3 pure function modules cho Phase 39: confidence-scorer.js (tinh confidence rule-based), audit-logger.js (format audit log append-only), index-generator.js (auto-generate INDEX.md tu frontmatter).

Purpose: Cung cap cong cu tinh toan confidence khong dung LLM, ghi log moi hanh dong research, va tu dong tao INDEX.md tu tat ca research files.
Output: 3 modules trong bin/lib/ voi tests day du, toan bo smoke tests pass.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD confidence-scorer.js — tinh confidence rule-based</name>
  <files>bin/lib/confidence-scorer.js, test/smoke-confidence-scorer.test.js</files>
  <action>
Tao confidence-scorer.js pure function module voi:

1. `scoreConfidence(sources)` — nhan array sources, return confidence level:
   - HIGH: co >= 1 source loai 'official-docs' hoac 'codebase'
   - MEDIUM: co >= 2 sources dong y (bat ky loai nao)
   - LOW: chi co 1 source hoac khong co source nao

2. `classifySource(source)` — phan loai 1 source:
   - Input: { type, url, description }
   - Output: { quality: 'high'|'medium'|'low', category: string }
   - high: official-docs, codebase, verified-api
   - medium: blog, stackoverflow, github-issue
   - low: unknown, unverified

3. `validateEvidence(body)` — kiem tra research body co section "## Bang chung" va co it nhat 1 citation:
   - Return: { valid: boolean, claimCount: number, citedCount: number, uncitedCount: number }

Tao test file voi >= 15 tests bao phu tat ca truong hop.
  </action>
  <verify>
    <automated>node --test test/smoke-confidence-scorer.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>confidence-scorer.js co 3 pure functions, tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: TDD audit-logger.js — format audit log append-only</name>
  <files>bin/lib/audit-logger.js, test/smoke-audit-logger.test.js</files>
  <action>
Tao audit-logger.js pure function module voi:

1. `formatLogEntry(entry)` — format 1 dong audit log:
   - Input: { timestamp, agent, action, topic, sourceCount, confidence }
   - Output: markdown table row string
   - Validate required fields, throw neu thieu

2. `parseAuditLog(content)` — parse AUDIT_LOG.md content:
   - Return: { entries: array, header: string }
   - Moi entry la object voi cac truong da parse

3. `appendLogEntry(existingContent, newEntry)` — append 1 entry vao cuoi log:
   - Return: updated content string (append-only, KHONG sua entries cu)
   - Neu existingContent rong, tao header truoc

4. `createAuditLog()` — tao AUDIT_LOG.md moi voi header:
   - Return: markdown string voi title va table header

Tao test file voi >= 15 tests.
  </action>
  <verify>
    <automated>node --test test/smoke-audit-logger.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>audit-logger.js co 4 pure functions, tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: TDD index-generator.js — auto-generate INDEX.md</name>
  <files>bin/lib/index-generator.js, test/smoke-index-generator.test.js</files>
  <action>
Tao index-generator.js pure function module voi:

1. `generateIndex(entries)` — tao INDEX.md content tu array entries:
   - Input: array of { filename, source, topic, confidence, created }
   - Output: markdown string voi bang [File | Source Type | Topic | Confidence | Created]
   - Sort theo created desc

2. `parseResearchFiles(files)` — parse nhieu research files cung luc:
   - Input: array of { filename, content }
   - Output: array of parsed entries (dung parseEntry tu research-store)
   - Bo qua files khong hop le (log warning)

3. `buildIndexRow(entry)` — format 1 dong trong bang INDEX:
   - Return: markdown table row string

Tao test file voi >= 12 tests.
  </action>
  <verify>
    <automated>node --test test/smoke-index-generator.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>index-generator.js co 3 pure functions, tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Chay toan bo test suite xac nhan khong regression</name>
  <files></files>
  <action>
Chay toan bo smoke tests dam bao khong regression:
node --test test/smoke-*.test.js
  </action>
  <verify>
    <automated>node --test test/smoke-*.test.js 2>&1 | tail -10</automated>
  </verify>
  <done>Toan bo smoke tests pass, khong regression.</done>
</task>

</tasks>

<verification>
1. node --test test/smoke-confidence-scorer.test.js pass
2. node --test test/smoke-audit-logger.test.js pass
3. node --test test/smoke-index-generator.test.js pass
4. node --test test/smoke-*.test.js pass (khong regression)
</verification>

<success_criteria>
- 3 pure function modules moi trong bin/lib/
- >= 42 tests moi (15 + 15 + 12)
- Toan bo smoke tests pass
- Khong regression
</success_criteria>

<output>
Sau khi hoan thanh, tao `.planning/phases/39-tieu-chuan-kiem-chung-chi-muc/39-01-SUMMARY.md`
</output>
