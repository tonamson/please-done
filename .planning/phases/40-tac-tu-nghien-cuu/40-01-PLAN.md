---
phase: 40-tac-tu-nghien-cuu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/pd-evidence-collector.md
  - .claude/agents/pd-fact-checker.md
  - bin/lib/resource-config.js
  - test/smoke-agent-files.test.js
  - test/smoke-resource-config.test.js
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02

must_haves:
  truths:
    - "Evidence Collector agent file ton tai voi dung frontmatter (sonnet, medium, 25 turns)"
    - "Fact Checker agent file ton tai voi dung frontmatter (opus, high, 15 turns)"
    - "AGENT_REGISTRY co 7 agents (5 cu + 2 moi)"
    - "Tat ca tests pass — agent files khop voi AGENT_REGISTRY"
  artifacts:
    - path: ".claude/agents/pd-evidence-collector.md"
      provides: "Evidence Collector agent definition"
      contains: "name: pd-evidence-collector"
    - path: ".claude/agents/pd-fact-checker.md"
      provides: "Fact Checker agent definition"
      contains: "name: pd-fact-checker"
    - path: "bin/lib/resource-config.js"
      provides: "AGENT_REGISTRY voi 7 entries"
      contains: "pd-evidence-collector"
    - path: "test/smoke-agent-files.test.js"
      provides: "Tests cho 7 agent files"
      contains: "pd-fact-checker"
  key_links:
    - from: ".claude/agents/pd-evidence-collector.md"
      to: "bin/lib/resource-config.js"
      via: "AGENT_REGISTRY entry tier + tools phai khop frontmatter"
      pattern: "pd-evidence-collector.*tier.*builder"
    - from: ".claude/agents/pd-fact-checker.md"
      to: "bin/lib/resource-config.js"
      via: "AGENT_REGISTRY entry tier + tools phai khop frontmatter"
      pattern: "pd-fact-checker.*tier.*architect"
    - from: "test/smoke-agent-files.test.js"
      to: ".claude/agents/"
      via: "AGENT_NAMES array + frontmatter tests iterate all agents"
      pattern: "pd-evidence-collector.*pd-fact-checker"
---

<objective>
Tao 2 agent definitions (Evidence Collector + Fact Checker) va dang ky trong resource-config.js. Cap nhat tests de cover 7 agents.

Purpose: He thong can 2 agents chuyen biet — mot thu thap bang chung, mot xac minh — de chong ao giac trong research pipeline.
Output: 2 agent files, AGENT_REGISTRY mo rong, tests updated va green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-tac-tu-nghien-cuu/40-CONTEXT.md
@.planning/phases/40-tac-tu-nghien-cuu/40-RESEARCH.md

<interfaces>
<!-- Pattern chuan tu pd-code-detective.md — follow EXACTLY -->

Agent YAML frontmatter fields (bat buoc):
- name: pd-[agent-name]
- description: [Mo ta tieng Viet co dau]
- tools: [Tool1, Tool2, ...]  (comma-separated string)
- model: [haiku|sonnet|opus]
- maxTurns: [number]
- effort: [low|medium|high]

Agent body blocks (bat buoc): <objective>, <process>, <rules>

From bin/lib/resource-config.js:
```javascript
const AGENT_REGISTRY = {
  'pd-bug-janitor':    { tier: 'scout',     tools: ['Read', 'Glob', 'Grep', 'AskUserQuestion', 'Bash'] },
  'pd-code-detective': { tier: 'builder',   tools: ['Read', 'Glob', 'Grep', 'mcp__fastcode__code_qa'] },
  'pd-doc-specialist': { tier: 'scout',     tools: ['Read', 'mcp__context7__resolve-library-id', 'mcp__context7__query-docs'] },
  'pd-repro-engineer': { tier: 'builder',   tools: ['Read', 'Write', 'Edit', 'Bash'] },
  'pd-fix-architect':  { tier: 'architect',  tools: ['Read', 'Write', 'Edit', 'Bash', 'Glob', 'Grep'] },
};
```

From test/smoke-agent-files.test.js:
```javascript
const AGENT_NAMES = [
  'pd-bug-janitor',
  'pd-code-detective',
  'pd-doc-specialist',
  'pd-repro-engineer',
  'pd-fix-architect',
];
// Line 82: it('5 agent files ton tai tai .claude/agents/')
// Lines 95-138: individual frontmatter tests per agent
```

From test/smoke-resource-config.test.js:
```javascript
// Line 190-192:
it('AGENT_REGISTRY co 5 agents', () => {
  assert.equal(Object.keys(AGENT_REGISTRY).length, 5);
});
```

Research modules agents reference (cho prompt content):
- research-store.js: createEntry({agent, source, topic, confidence, body, id, slug}) -> {filename, content}
- research-store.js: validateEvidence(content) -> {valid, warnings}
- research-store.js: parseEntry(content) -> {frontmatter, body}
- confidence-scorer.js: scoreConfidence(sources[]) -> 'HIGH'|'MEDIUM'|'LOW'
- audit-logger.js: appendLogEntry(existing, {timestamp, agent, action, topic, sourceCount, confidence}) -> string
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tao 2 agent definition files + dang ky AGENT_REGISTRY</name>
  <read_first>
    - .claude/agents/pd-code-detective.md (pattern reference)
    - .claude/agents/pd-bug-janitor.md (simpler pattern)
    - bin/lib/resource-config.js (AGENT_REGISTRY, dong 32-38)
  </read_first>
  <files>
    .claude/agents/pd-evidence-collector.md,
    .claude/agents/pd-fact-checker.md,
    bin/lib/resource-config.js
  </files>
  <behavior>
    - pd-evidence-collector.md ton tai voi frontmatter: name=pd-evidence-collector, model=sonnet, maxTurns=25, effort=medium, tools=Read,Grep,Glob,Bash,mcp__context7__resolve-library-id,mcp__context7__query-docs
    - pd-evidence-collector.md body co <objective>, <process>, <rules>
    - pd-evidence-collector.md <rules> dau tien la source-or-skip: "KHONG DUOC ghi claim khong co source"
    - pd-evidence-collector.md <process> co buoc thu thap 2+ nguon doc lap, buoc ghi theo createEntry format, buoc cuoi appendAuditLog pattern
    - pd-fact-checker.md ton tai voi frontmatter: name=pd-fact-checker, model=opus, maxTurns=15, effort=high, tools=Read,Grep,Glob,WebSearch,mcp__context7__resolve-library-id,mcp__context7__query-docs
    - pd-fact-checker.md body co <objective>, <process>, <rules>
    - pd-fact-checker.md <rules> co "KHONG ghi de noi dung Evidence Collector"
    - pd-fact-checker.md <process> co buoc verify internal (Read/Grep), verify external (WebSearch/Context7 chi check ton tai), buoc them section "## Kiem tra Thuc te"
    - AGENT_REGISTRY co 7 entries (them pd-evidence-collector tier builder, pd-fact-checker tier architect)
  </behavior>
  <action>
    **1. Tao `.claude/agents/pd-evidence-collector.md` (per D-01, D-02, D-03, D-04, D-05, D-13, D-14):**
    - YAML frontmatter:
      - name: pd-evidence-collector
      - description: Thu thap bang chung tu nhieu nguon doc lap — ghi ket qua co source citation theo format chuan
      - tools: Read, Grep, Glob, Bash, mcp__context7__resolve-library-id, mcp__context7__query-docs
      - model: sonnet
      - maxTurns: 25
      - effort: medium
    - Body theo pattern pd-code-detective.md (dung `<objective>` KHONG phai `<purpose>`, KHONG co `<success_criteria>` — per Research Pitfall 1):
      - `<objective>`: Thu thap bang chung tu 2+ nguon doc lap cho topic duoc giao. Moi claim PHAI co source citation.
      - `<process>` 6 buoc: (1) Doc topic + routing tu prompt, (2) Thu thap evidence — internal: Read/Grep codebase, external: Context7/Bash, (3) Ghi source cho moi claim (file:dong cho internal, URL/library cho external), (4) Tinh confidence: HIGH = official docs/codebase, MEDIUM = 2+ sources, LOW = 1 source, (5) Ghi theo research-store.js createEntry format (frontmatter 8 fields + `## Bang chung` + inline `(confidence: LEVEL)`), (6) Ghi log entry theo appendAuditLog format
      - `<rules>`: (1) KHONG DUOC ghi claim khong co source — ghi "KHONG TIM THAY" thay vi fabricate, (2) Moi claim PHAI co source separator va confidence marker, (3) Thu thap tu IT NHAT 2 nguon doc lap, (4) Dung tieng Viet co dau
    - Noi dung tieng Viet co dau chuan (per CLAUDE.md)

    **2. Tao `.claude/agents/pd-fact-checker.md` (per D-06, D-07, D-08, D-09, D-10, D-13, D-14):**
    - YAML frontmatter:
      - name: pd-fact-checker
      - description: Xac minh bang chung tu Evidence Collector — kiem tra source con valid, phat hien claim thieu co so
      - tools: Read, Grep, Glob, WebSearch, mcp__context7__resolve-library-id, mcp__context7__query-docs
      - model: opus
      - maxTurns: 15
      - effort: high
    - Body:
      - `<objective>`: Kiem tra tinh chinh xac cua research file tu Evidence Collector. Verify sources, phat hien claim thieu bang chung.
      - `<process>` 6 buoc: (1) Doc research file duoc chi dinh, (2) Parse frontmatter + body de lay claims, (3) Verify moi claim — internal: Read/Grep kiem tra file:dong khop, external: WebSearch/Context7 kiem tra source TON TAI (MEDIUM confidence toi da per Research Pitfall 3), (4) LOW confidence: tim them source hoac danh dau "KHONG XAC MINH DUOC", (5) THEM section `## Kiem tra Thuc te` (KHONG ghi de per D-10) voi: verified/unverified list, flagged claims, summary, (6) Ghi log entry theo appendAuditLog format
      - `<rules>`: (1) KHONG ghi de bat ky noi dung nao cua Evidence Collector, (2) Internal claims verify bang Read/Grep — co the dat HIGH, (3) External claims chi verify source TON TAI — MEDIUM toi da, (4) Dung tieng Viet co dau

    **3. Mo rong AGENT_REGISTRY trong `bin/lib/resource-config.js` (per D-11):**
    - Them 2 entries SAU `pd-fix-architect` (dong 37):
      ```javascript
      'pd-evidence-collector': { tier: 'builder',   tools: ['Read', 'Grep', 'Glob', 'Bash', 'mcp__context7__resolve-library-id', 'mcp__context7__query-docs'] },
      'pd-fact-checker':       { tier: 'architect',  tools: ['Read', 'Grep', 'Glob', 'WebSearch', 'mcp__context7__resolve-library-id', 'mcp__context7__query-docs'] },
      ```
    - Cap nhat comment JSDoc phia tren AGENT_REGISTRY: "Moi agent co 1 tier va danh sach tools duoc phep dung."
  </action>
  <acceptance_criteria>
    - grep "name: pd-evidence-collector" .claude/agents/pd-evidence-collector.md returns match
    - grep "model: sonnet" .claude/agents/pd-evidence-collector.md returns match
    - grep "maxTurns: 25" .claude/agents/pd-evidence-collector.md returns match
    - grep "KHONG DUOC ghi claim khong co source" .claude/agents/pd-evidence-collector.md returns match
    - grep "name: pd-fact-checker" .claude/agents/pd-fact-checker.md returns match
    - grep "model: opus" .claude/agents/pd-fact-checker.md returns match
    - grep "maxTurns: 15" .claude/agents/pd-fact-checker.md returns match
    - grep "KHONG ghi de" .claude/agents/pd-fact-checker.md returns match
    - grep "Kiem tra Thuc te" .claude/agents/pd-fact-checker.md returns match
    - grep "pd-evidence-collector" bin/lib/resource-config.js returns match
    - grep "pd-fact-checker" bin/lib/resource-config.js returns match
    - grep "<objective>" .claude/agents/pd-evidence-collector.md returns match
    - grep "<process>" .claude/agents/pd-evidence-collector.md returns match
    - grep "<rules>" .claude/agents/pd-evidence-collector.md returns match
    - grep "<objective>" .claude/agents/pd-fact-checker.md returns match
    - grep "<process>" .claude/agents/pd-fact-checker.md returns match
    - grep "<rules>" .claude/agents/pd-fact-checker.md returns match
  </acceptance_criteria>
  <verify>
    <automated>node --test test/smoke-resource-config.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>2 agent files ton tai voi dung frontmatter, AGENT_REGISTRY co 7 entries, resource-config tests CHUA pass (vi count con hardcode 5 — Task 2 se fix)</done>
</task>

<task type="auto">
  <name>Task 2: Cap nhat tests cho 7 agents</name>
  <read_first>
    - test/smoke-agent-files.test.js (full file — AGENT_NAMES, frontmatter tests, body tests)
    - test/smoke-resource-config.test.js (dong 190 — count assertion)
  </read_first>
  <files>
    test/smoke-agent-files.test.js,
    test/smoke-resource-config.test.js
  </files>
  <action>
    **1. Cap nhat `test/smoke-agent-files.test.js` (per D-12):**
    - Dong 2: Doi comment "5 agent files" thanh "7 agent files"
    - Dong 18-24: Them 2 entries vao AGENT_NAMES array:
      ```javascript
      const AGENT_NAMES = [
        'pd-bug-janitor',
        'pd-code-detective',
        'pd-doc-specialist',
        'pd-repro-engineer',
        'pd-fix-architect',
        'pd-evidence-collector',
        'pd-fact-checker',
      ];
      ```
    - Dong 82: Doi `'5 agent files ton tai'` thanh `'7 agent files ton tai'`
    - Them 2 frontmatter test cases SAU pd-fix-architect test (sau dong 138), theo EXACT pattern:
      ```javascript
      it('pd-evidence-collector co dung frontmatter', () => {
        const fm = parseAgentFrontmatter(join(AGENTS_DIR, 'pd-evidence-collector.md'));
        assert.equal(fm.name, 'pd-evidence-collector');
        assert.equal(fm.model, 'sonnet');
        assert.equal(fm.maxTurns, 25);
        assert.equal(fm.effort, 'medium');
        assert.deepEqual(fm.tools, ['Read', 'Grep', 'Glob', 'Bash', 'mcp__context7__resolve-library-id', 'mcp__context7__query-docs']);
      });

      it('pd-fact-checker co dung frontmatter', () => {
        const fm = parseAgentFrontmatter(join(AGENTS_DIR, 'pd-fact-checker.md'));
        assert.equal(fm.name, 'pd-fact-checker');
        assert.equal(fm.model, 'opus');
        assert.equal(fm.maxTurns, 15);
        assert.equal(fm.effort, 'high');
        assert.deepEqual(fm.tools, ['Read', 'Grep', 'Glob', 'WebSearch', 'mcp__context7__resolve-library-id', 'mcp__context7__query-docs']);
      });
      ```

    **2. Cap nhat `test/smoke-resource-config.test.js` (per D-12):**
    - Dong 190: Doi description `'AGENT_REGISTRY co 5 agents'` thanh `'AGENT_REGISTRY co 7 agents'`
    - Dong 191: Doi `assert.equal(Object.keys(AGENT_REGISTRY).length, 5)` thanh `7`

    **Luu y Pitfall 5:** Phai cap nhat CA HAI files dong thoi — quen 1 se lam test fail.
  </action>
  <acceptance_criteria>
    - grep "7 agent files" test/smoke-agent-files.test.js returns match
    - grep "pd-evidence-collector" test/smoke-agent-files.test.js returns match
    - grep "pd-fact-checker" test/smoke-agent-files.test.js returns match
    - grep "AGENT_REGISTRY co 7 agents" test/smoke-resource-config.test.js returns match
    - grep "length, 7" test/smoke-resource-config.test.js returns match
  </acceptance_criteria>
  <verify>
    <automated>node --test test/smoke-agent-files.test.js test/smoke-resource-config.test.js</automated>
  </verify>
  <done>Tat ca tests green: 7 agent files ton tai, frontmatter khop, tools khop, AGENT_REGISTRY co 7 entries, consistency tests pass cho ca 7 agents</done>
</task>

</tasks>

<verification>
```bash
# Kiem tra toan bo test suite
npm test

# Kiem tra agent files ton tai
ls -la .claude/agents/pd-evidence-collector.md .claude/agents/pd-fact-checker.md

# Kiem tra AGENT_REGISTRY count
grep -c "pd-" bin/lib/resource-config.js
# Expected: 7 (moi dong co 1 entry)
```
</verification>

<success_criteria>
- 2 agent files (.claude/agents/pd-evidence-collector.md, pd-fact-checker.md) ton tai voi dung YAML frontmatter
- Evidence Collector: sonnet/medium/25 turns, tools KHONG co WebSearch, source-or-skip rule trong rules
- Fact Checker: opus/high/15 turns, tools CO WebSearch, KHONG ghi de rule trong rules
- AGENT_REGISTRY co 7 entries (5 cu + 2 moi)
- Tat ca tests green: smoke-agent-files.test.js va smoke-resource-config.test.js
</success_criteria>

<output>
After completion, create `.planning/phases/40-tac-tu-nghien-cuu/40-01-SUMMARY.md`
</output>
