---
phase: 29-evidence-protocol-session-management
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/evidence-protocol.js
  - test/smoke-evidence-protocol.test.js
autonomous: true
requirements: [PROT-02, PROT-05]

must_haves:
  truths:
    - "validateEvidence() nhan dien dung 3 outcome types: root_cause, checkpoint, inconclusive"
    - "validateEvidence() tra warning khi thieu required sections cho tung outcome"
    - "validateEvidence() kiem tra Elimination Log co bang Markdown khi outcome la inconclusive"
    - "parseEvidence() parse frontmatter + body thanh structured object"
    - "getRequiredSections() tra ve dung danh sach sections cho moi outcome type"
  artifacts:
    - path: "bin/lib/evidence-protocol.js"
      provides: "Evidence protocol module — validate, parse, constants"
      exports: ["validateEvidence", "parseEvidence", "getRequiredSections", "OUTCOME_TYPES"]
    - path: "test/smoke-evidence-protocol.test.js"
      provides: "Unit tests cho evidence-protocol module"
      min_lines: 80
  key_links:
    - from: "bin/lib/evidence-protocol.js"
      to: "bin/lib/utils.js"
      via: "parseFrontmatter() import"
      pattern: "require.*utils.*parseFrontmatter"
---

<objective>
TDD tao evidence-protocol.js — module pure function chuan hoa 3 outcome types (ROOT CAUSE FOUND, CHECKPOINT REACHED, INVESTIGATION INCONCLUSIVE) voi validation non-blocking.

Purpose: Dam bao moi agent tra ket qua theo format chuan, orchestrator co the parse va route theo outcome (per PROT-02, PROT-05).
Output: evidence-protocol.js + tests green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-evidence-protocol-session-management/29-CONTEXT.md
@.planning/phases/29-evidence-protocol-session-management/29-RESEARCH.md

<interfaces>
<!-- Pattern reference tu resource-config.js — module moi theo cung pattern -->

From bin/lib/utils.js:
```javascript
function parseFrontmatter(content) // -> { frontmatter: object, body: string, raw: string }
function buildFrontmatter(fm) // -> string (YAML lines)
function assembleMd(frontmatter, body) // -> string (---\nYAML\n---\nbody)
```

From bin/lib/resource-config.js (pattern reference):
```javascript
'use strict';
const TIER_MAP = { scout: {...}, builder: {...}, architect: {...} };
function getModelForTier(tier) { /* pure, throws on invalid */ }
module.exports = { getModelForTier, TIER_MAP, ... };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Viet tests cho evidence-protocol module</name>
  <files>test/smoke-evidence-protocol.test.js</files>
  <read_first>
    - test/smoke-resource-config.test.js (pattern reference: describe/it structure, assert style, Vietnamese test names)
    - bin/lib/utils.js (parseFrontmatter signature — line 50-101)
    - .planning/phases/29-evidence-protocol-session-management/29-CONTEXT.md (D-05 den D-10 — evidence format decisions)
  </read_first>
  <behavior>
    OUTCOME_TYPES constant:
    - Test: OUTCOME_TYPES co dung 3 keys: root_cause, checkpoint, inconclusive
    - Test: root_cause.label === 'ROOT CAUSE FOUND'
    - Test: root_cause.requiredSections === ['Nguyen nhan', 'Bang chung', 'De xuat'] (per D-07)
    - Test: checkpoint.label === 'CHECKPOINT REACHED'
    - Test: checkpoint.requiredSections === ['Tien do dieu tra', 'Cau hoi cho User', 'Context cho Agent tiep'] (per D-08)
    - Test: inconclusive.label === 'INVESTIGATION INCONCLUSIVE'
    - Test: inconclusive.requiredSections === ['Elimination Log', 'Huong dieu tra tiep'] (per D-09)

    getRequiredSections:
    - Test: getRequiredSections('root_cause') === ['Nguyen nhan', 'Bang chung', 'De xuat']
    - Test: getRequiredSections('checkpoint') === ['Tien do dieu tra', 'Cau hoi cho User', 'Context cho Agent tiep']
    - Test: getRequiredSections('inconclusive') === ['Elimination Log', 'Huong dieu tra tiep']
    - Test: getRequiredSections('invalid') throw 'outcome khong hop le'
    - Test: getRequiredSections(null) throw 'thieu tham so'

    validateEvidence — happy paths:
    - Test: evidence voi outcome root_cause va day du 3 sections -> { valid: true, outcome: 'root_cause', agent: 'pd-code-detective', warnings: [] }
    - Test: evidence voi outcome checkpoint va day du 3 sections -> { valid: true, outcome: 'checkpoint', ... }
    - Test: evidence voi outcome inconclusive va day du 2 sections + Elimination Log table -> { valid: true, outcome: 'inconclusive', ... }

    validateEvidence — warning paths:
    - Test: thieu outcome trong frontmatter -> { valid: false, warnings bao gom 'outcome khong hop le' }
    - Test: outcome khong hop le (vd: 'unknown') -> { valid: false, warnings bao gom 'outcome khong hop le' }
    - Test: outcome root_cause nhung thieu section '## Bang chung' -> { valid: false, warnings bao gom 'thieu section: ## Bang chung' }
    - Test: outcome inconclusive nhung thieu Elimination Log -> { valid: false, warnings bao gom 'thieu section: ## Elimination Log' }
    - Test: outcome inconclusive co Elimination Log heading nhung khong co bang (thieu | rows) -> { valid: false, warnings bao gom 'Elimination Log thieu bang du lieu' }
    - Test: content null/undefined -> throw 'thieu tham so content'
    - Test: content empty string -> throw 'thieu tham so content'

    parseEvidence:
    - Test: parse evidence hoan chinh -> { agent, outcome, timestamp, session, body, sections }
    - Test: sections chua cac headings tu body (## Nguyen nhan -> sections['Nguyen nhan'] co noi dung)
    - Test: content khong co frontmatter -> agent/outcome/timestamp/session deu null/undefined
    - Test: content null -> throw 'thieu tham so content'
  </behavior>
  <action>
    Tao file test/smoke-evidence-protocol.test.js theo pattern smoke-resource-config.test.js.

    Structure:
    ```javascript
    'use strict';
    const { describe, it } = require('node:test');
    const assert = require('node:assert/strict');
    const { validateEvidence, parseEvidence, getRequiredSections, OUTCOME_TYPES } = require('../bin/lib/evidence-protocol');
    ```

    Dung describe blocks: 'OUTCOME_TYPES', 'getRequiredSections', 'validateEvidence', 'parseEvidence'.

    Tao helper function makeEvidence(overrides) de sinh evidence content cho tests:
    ```javascript
    function makeEvidence({ agent = 'pd-code-detective', outcome = 'root_cause', session = 'S001', body = '' } = {}) {
      return `---\nagent: ${agent}\noutcome: ${outcome}\ntimestamp: 2026-03-24T10:00:00+07:00\nsession: ${session}\n---\n${body}`;
    }
    ```

    Body mau cho tung outcome:
    - root_cause: `## ROOT CAUSE FOUND\n\n## Nguyen nhan\nLoi o dong 42.\n\n## Bang chung\nsrc/api.js:42\n\n## De xuat\nSua dong 42.`
    - checkpoint: `## CHECKPOINT REACHED\n\n## Tien do dieu tra\n50% done.\n\n## Cau hoi cho User\nCo thay doi gi gan day?\n\n## Context cho Agent tiep\nDa kiem tra src/api.js.`
    - inconclusive: `## INVESTIGATION INCONCLUSIVE\n\n## Elimination Log\n\n| File/Logic | Ket qua | Ghi chu |\n|------------|---------|--------|\n| src/api.js:42 | BINH THUONG | Khong co loi |\n\n## Huong dieu tra tiep\nKiem tra middleware.`

    Tong cong: ~25-30 test cases.

    Chay tests — TAT CA PHAI FAIL (RED) vi module chua ton tai.
  </action>
  <verify>
    <automated>node --test test/smoke-evidence-protocol.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>File test/smoke-evidence-protocol.test.js ton tai, co it nhat 25 test cases, tat ca FAIL vi module chua implement</done>
  <acceptance_criteria>
    - test/smoke-evidence-protocol.test.js ton tai va co `require('../bin/lib/evidence-protocol')`
    - File co it nhat 4 describe blocks: 'OUTCOME_TYPES', 'getRequiredSections', 'validateEvidence', 'parseEvidence'
    - File co helper function makeEvidence
    - `node --test test/smoke-evidence-protocol.test.js 2>&1` tra ve exit code khac 0 (RED)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement evidence-protocol.js de pass tat ca tests</name>
  <files>bin/lib/evidence-protocol.js</files>
  <read_first>
    - test/smoke-evidence-protocol.test.js (tests vua viet — day la spec)
    - bin/lib/resource-config.js (pattern reference: 'use strict', constants, pure functions, module.exports)
    - bin/lib/utils.js (parseFrontmatter signature — line 50-101)
    - .planning/phases/29-evidence-protocol-session-management/29-CONTEXT.md (D-05 den D-13)
  </read_first>
  <action>
    Tao bin/lib/evidence-protocol.js theo pattern resource-config.js. Module NAY import parseFrontmatter tu utils.js (khac voi resource-config.js khong import gi).

    ```javascript
    'use strict';
    const { parseFrontmatter } = require('./utils');
    ```

    OUTCOME_TYPES constant (per D-06, D-07, D-08, D-09):
    ```javascript
    const OUTCOME_TYPES = {
      root_cause:   { label: 'ROOT CAUSE FOUND',          requiredSections: ['Nguyen nhan', 'Bang chung', 'De xuat'] },
      checkpoint:   { label: 'CHECKPOINT REACHED',         requiredSections: ['Tien do dieu tra', 'Cau hoi cho User', 'Context cho Agent tiep'] },
      inconclusive: { label: 'INVESTIGATION INCONCLUSIVE', requiredSections: ['Elimination Log', 'Huong dieu tra tiep'] },
    };
    ```

    getRequiredSections(outcomeType):
    - Throw 'thieu tham so outcomeType' khi null/undefined/khong phai string
    - Throw 'outcome khong hop le: {outcomeType}' khi khong co trong OUTCOME_TYPES
    - Return [...OUTCOME_TYPES[outcomeType].requiredSections] (copy array)

    validateEvidence(content) (per D-13 — non-blocking):
    - Throw 'thieu tham so content' khi content null/undefined/empty/khong phai string
    - Parse frontmatter bang parseFrontmatter(content)
    - Kiem tra frontmatter.outcome co trong OUTCOME_TYPES
    - Kiem tra moi required section co trong body (tim `## {section}`)
    - Dac biet voi inconclusive: kiem tra Elimination Log co bang du lieu (per D-10, Pitfall 5)
      - Sau heading `## Elimination Log`, kiem tra body co chua `|` characters va it nhat 1 data row
      - Regex kiem tra: sau `## Elimination Log` co it nhat 3 dong chua `|` (header + separator + 1 data row)
    - Return { valid: boolean, outcome: string|null, agent: string|null, warnings: string[] }

    parseEvidence(content):
    - Throw 'thieu tham so content' khi content null/undefined/khong phai string
    - Parse frontmatter + body
    - Extract sections tu body: moi `## Heading` tao 1 key trong sections object, value = noi dung giua heading nay va heading tiep theo (hoac cuoi file)
    - Return { agent, outcome, timestamp, session, body, sections }

    module.exports = { validateEvidence, parseEvidence, getRequiredSections, OUTCOME_TYPES };

    Chay tests — TAT CA PHAI PASS (GREEN).
  </action>
  <verify>
    <automated>node --test test/smoke-evidence-protocol.test.js</automated>
  </verify>
  <done>Tat ca tests trong smoke-evidence-protocol.test.js pass. Module export dung 4 items: validateEvidence, parseEvidence, getRequiredSections, OUTCOME_TYPES.</done>
  <acceptance_criteria>
    - bin/lib/evidence-protocol.js ton tai
    - File co `'use strict'` o dong dau
    - File co `require('./utils')` de import parseFrontmatter
    - File KHONG co `require('fs')` (pure function — per D-20)
    - File export 4 items: validateEvidence, parseEvidence, getRequiredSections, OUTCOME_TYPES
    - OUTCOME_TYPES co dung 3 keys: root_cause, checkpoint, inconclusive
    - `node --test test/smoke-evidence-protocol.test.js` exit code 0 (GREEN)
    - `node --test 'test/*.test.js'` khong co regression (full suite pass)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `node --test test/smoke-evidence-protocol.test.js` — tat ca tests pass
- `node --test 'test/*.test.js'` — full suite khong regression
- `grep -c "require('fs')" bin/lib/evidence-protocol.js` tra ve 0 (pure function)
- `node -e "const ep = require('./bin/lib/evidence-protocol'); console.log(Object.keys(ep))"` — in ra 4 exports
</verification>

<success_criteria>
- evidence-protocol.js la pure function module (khong doc file, khong require fs)
- 3 outcome types co day du label va requiredSections theo D-07/D-08/D-09
- validateEvidence() non-blocking: tra warnings thay vi throw khi format sai
- Elimination Log validation kiem tra ca heading lan table data (per D-10, Pitfall 5)
- Tat ca tests pass, full suite khong regression
</success_criteria>

<output>
Sau khi hoan thanh, tao `.planning/phases/29-evidence-protocol-session-management/29-01-SUMMARY.md`
</output>
