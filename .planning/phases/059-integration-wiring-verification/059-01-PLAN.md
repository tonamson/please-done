---
phase: 059-integration-wiring-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/resource-config.js
  - bin/lib/parallel-dispatch.js
  - .claude/agents/pd-sec-scanner.md
  - test/smoke-agent-files.test.js
  - test/smoke-resource-config.test.js
autonomous: true
requirements: [AGEN-01, AGEN-09, PLAT-01, PLAT-02, PARA-02]

must_haves:
  truths:
    - "getAgentConfig(name, platform) tra ve platform-specific model khi truyen platform"
    - "getAgentConfig(name) khong truyen platform van tra ve generic model (backward compatible)"
    - "buildParallelPlan(sessionDir, path, platform) truyen platform xuong getAgentConfig"
    - "pd-sec-scanner.md ton tai tai .claude/agents/ voi format moi (tools, model, maxTurns, effort)"
    - "Smoke tests pass voi pd-sec-scanner trong AGENT_NAMES"
  artifacts:
    - path: "bin/lib/resource-config.js"
      provides: "getAgentConfig(agentName, platform?) voi platform wiring"
      contains: "function getAgentConfig(agentName, platform)"
    - path: "bin/lib/parallel-dispatch.js"
      provides: "buildParallelPlan voi platform param"
      contains: "function buildParallelPlan(sessionDir, janitarEvidencePath, platform)"
    - path: ".claude/agents/pd-sec-scanner.md"
      provides: "pd-sec-scanner agent file format moi"
      contains: "model: haiku"
    - path: "test/smoke-agent-files.test.js"
      provides: "pd-sec-scanner trong AGENT_NAMES"
      contains: "pd-sec-scanner"
  key_links:
    - from: "bin/lib/parallel-dispatch.js"
      to: "bin/lib/resource-config.js"
      via: "getAgentConfig(name, platform)"
      pattern: "getAgentConfig\\(.*, platform\\)"
    - from: ".claude/agents/pd-sec-scanner.md"
      to: "bin/lib/resource-config.js"
      via: "AGENT_REGISTRY pd-sec-scanner entry"
      pattern: "pd-sec-scanner"
---

<objective>
Wire `getModelForTier(tier, platform)` vao production dispatch flow thong qua `getAgentConfig()`, copy `pd-sec-scanner.md` sang `.claude/agents/` voi format moi, cap nhat tests.

Purpose: Dong integration gaps — PLAT-01/PLAT-02 can it nhat 1 production caller dung platform mapping, pd-sec-scanner can nhat quan voi 6 agents khac tai `.claude/agents/`.
Output: resource-config.js voi platform-aware getAgentConfig, parallel-dispatch.js voi platform param, pd-sec-scanner.md tai .claude/agents/, tests updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/059-integration-wiring-verification/059-CONTEXT.md
@.planning/phases/059-integration-wiring-verification/059-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From bin/lib/resource-config.js (line 214):
```javascript
function getModelForTier(tier, platform) {
  // ...
  // Khong co platform -> tra ve generic (backward compatible)
  if (!platform) {
    return { ...entry };
  }
  // Co platform -> resolve platform-specific model
  // ...
}
```

From bin/lib/resource-config.js (line 269 — CURRENT):
```javascript
function getAgentConfig(agentName) {
  // ...
  const { tier, tools, ...extra } = agent;
  const tierConfig = TIER_MAP[tier];  // <-- can thay bang getModelForTier(tier, platform)
  return {
    name: agentName, tier,
    model: tierConfig.model, effort: tierConfig.effort, maxTurns: tierConfig.maxTurns,
    tools: [...tools], ...extra,
  };
}
```

From bin/lib/parallel-dispatch.js (line 29 — CURRENT):
```javascript
function buildParallelPlan(sessionDir, janitarEvidencePath) {
  const detectiveConfig = getAgentConfig('pd-code-detective');
  const docSpecConfig = getAgentConfig('pd-doc-specialist');
  // ...
}
```

From bin/lib/resource-config.js (AGENT_REGISTRY, line 133):
```javascript
"pd-sec-scanner": {
  tier: "scout",
  tools: ["Read", "Glob", "Grep", "mcp__fastcode__code_qa"],
  categories: [ "sql-injection", "xss", ... ],
},
```

From test/smoke-agent-files.test.js (line 19):
```javascript
const AGENT_NAMES = [
  'pd-bug-janitor', 'pd-code-detective', 'pd-doc-specialist',
  'pd-repro-engineer', 'pd-fix-architect', 'pd-evidence-collector',
  'pd-fact-checker', 'pd-codebase-mapper', 'pd-security-researcher',
  'pd-feature-analyst', 'pd-research-synthesizer', 'pd-planner',
  'pd-regression-analyzer',
];
// 13 agents — can them 'pd-sec-scanner' thanh 14
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire platform param vao getAgentConfig() + buildParallelPlan()</name>
  <files>bin/lib/resource-config.js, bin/lib/parallel-dispatch.js, test/smoke-resource-config.test.js</files>
  <read_first>
    - bin/lib/resource-config.js (xem getAgentConfig line 269-292, getModelForTier line 214-258)
    - bin/lib/parallel-dispatch.js (xem buildParallelPlan line 29-50, buildResearchSquadPlan line 224-260)
    - test/smoke-resource-config.test.js (xem existing tests cho getAgentConfig)
  </read_first>
  <action>
    **resource-config.js — sua getAgentConfig (line 269-292):**

    1. Thay doi function signature tu `function getAgentConfig(agentName)` thanh `function getAgentConfig(agentName, platform)`.
    2. Cap nhat JSDoc: them `@param {string} [platform] - Ten platform (claude, codex, gemini, ...). Optional.`
    3. Thay dong `const tierConfig = TIER_MAP[tier];` (line 281) bang `const tierConfig = getModelForTier(tier, platform);`
    4. Giu nguyen TOAN BO phan return object — khong thay doi gi khac.

    Per D-01: Wire thong qua getAgentConfig. Per D-02: getModelForTier(tier, undefined) tra ve `{ ...TIER_MAP[tier] }` — backward compatible, khong break bat ky caller nao.

    **parallel-dispatch.js — sua buildParallelPlan (line 29):**

    1. Thay doi function signature tu `function buildParallelPlan(sessionDir, janitarEvidencePath)` thanh `function buildParallelPlan(sessionDir, janitarEvidencePath, platform)`.
    2. Cap nhat JSDoc: them `@param {string} [platform] - Ten platform. Optional.`
    3. Thay 2 dong getAgentConfig calls (line 32-33):
       - `getAgentConfig('pd-code-detective', platform)` (thay vi khong co platform)
       - `getAgentConfig('pd-doc-specialist', platform)` (thay vi khong co platform)
    4. Giu nguyen TOAN BO phan con lai cua function.

    Per D-03: it nhat 1 production caller (buildParallelPlan) truyen platform param.

    **test/smoke-resource-config.test.js — them tests:**

    Them 2 test cases moi trong describe block cho getAgentConfig:

    ```javascript
    it('getAgentConfig(name, platform) tra ve platform-specific model', () => {
      const config = getAgentConfig('pd-code-detective', 'gemini');
      assert.ok(config.model, 'phai co model');
      assert.strictEqual(config.tier, 'builder');
      // gemini builder → model phai la gemini-specific, khong phai generic 'sonnet'
    });

    it('getAgentConfig(name) khong truyen platform van tra ve generic model', () => {
      const config = getAgentConfig('pd-code-detective');
      assert.strictEqual(config.model, TIER_MAP.builder.model);
    });
    ```
  </action>
  <verify>
    <automated>node --test test/smoke-resource-config.test.js test/platform-models.test.js</automated>
  </verify>
  <acceptance_criteria>
    - resource-config.js contains `function getAgentConfig(agentName, platform)`
    - resource-config.js contains `const tierConfig = getModelForTier(tier, platform);`
    - resource-config.js JSDoc contains `@param {string} [platform]`
    - parallel-dispatch.js contains `function buildParallelPlan(sessionDir, janitarEvidencePath, platform)`
    - parallel-dispatch.js contains `getAgentConfig('pd-code-detective', platform)`
    - parallel-dispatch.js contains `getAgentConfig('pd-doc-specialist', platform)`
    - test/smoke-resource-config.test.js contains `getAgentConfig('pd-code-detective', 'gemini')`
    - `node --test test/smoke-resource-config.test.js` exits 0
    - `node --test test/platform-models.test.js` exits 0
  </acceptance_criteria>
  <done>getAgentConfig co platform param, buildParallelPlan truyen platform xuong, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Copy pd-sec-scanner sang .claude/agents/ + cap nhat smoke test</name>
  <files>.claude/agents/pd-sec-scanner.md, test/smoke-agent-files.test.js</files>
  <read_first>
    - commands/pd/agents/pd-sec-scanner.md (file nguon — xem full content de convert)
    - .claude/agents/pd-code-detective.md (mau format moi de nhat quan)
    - test/smoke-agent-files.test.js (xem AGENT_NAMES array line 19-33)
    - bin/lib/resource-config.js (xem AGENT_REGISTRY pd-sec-scanner line 133-142 de lay tier/tools)
  </read_first>
  <action>
    **Tao .claude/agents/pd-sec-scanner.md:**

    1. Copy TOAN BO body (tu `<objective>` den het file) tu `commands/pd/agents/pd-sec-scanner.md`.
    2. Thay the YAML frontmatter tu format cu sang format moi:

    Format cu (commands/pd/agents/):
    ```yaml
    name: pd-sec-scanner
    description: Scanner bao mat tong hop — Quet ma nguon theo OWASP category duoc chi dinh tu security-rules.yaml.
    tier: scout
    allowed-tools:
      - Read
      - Glob
      - Grep
      - mcp__fastcode__code_qa
    ```

    Format moi (.claude/agents/):
    ```yaml
    name: pd-sec-scanner
    description: Scanner bao mat tong hop — Quet ma nguon theo OWASP category duoc chi dinh tu security-rules.yaml.
    tools: Read, Glob, Grep, mcp__fastcode__code_qa
    model: haiku
    maxTurns: 15
    effort: low
    ```

    Giai thich conversion: `tier: scout` → lay tu TIER_MAP.scout → `model: haiku, maxTurns: 15, effort: low`. `allowed-tools` YAML array → `tools: comma-separated string`.

    Per D-04: Copy sang .claude/agents/. Per D-05: Giu nguyen file goc tai commands/pd/agents/ — KHONG xoa.

    **Cap nhat test/smoke-agent-files.test.js:**

    Them `'pd-sec-scanner'` vao AGENT_NAMES array (line 33, truoc `];`):
    ```javascript
    const AGENT_NAMES = [
      'pd-bug-janitor',
      'pd-code-detective',
      // ... existing entries ...
      'pd-regression-analyzer',
      'pd-sec-scanner',    // <-- THEM DONG NAY
    ];
    ```

    Per D-06: AGENT_REGISTRY da co pd-sec-scanner (line 133) — chi can them vao test AGENT_NAMES.
  </action>
  <verify>
    <automated>node --test test/smoke-agent-files.test.js</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/pd-sec-scanner.md exists
    - .claude/agents/pd-sec-scanner.md contains `tools: Read, Glob, Grep, mcp__fastcode__code_qa`
    - .claude/agents/pd-sec-scanner.md contains `model: haiku`
    - .claude/agents/pd-sec-scanner.md contains `maxTurns: 15`
    - .claude/agents/pd-sec-scanner.md contains `effort: low`
    - .claude/agents/pd-sec-scanner.md contains `<objective>`
    - commands/pd/agents/pd-sec-scanner.md still exists (khong bi xoa — per D-05)
    - test/smoke-agent-files.test.js contains `'pd-sec-scanner'`
    - AGENT_NAMES array co 14 entries
    - `node --test test/smoke-agent-files.test.js` exits 0
  </acceptance_criteria>
  <done>pd-sec-scanner.md ton tai tai .claude/agents/ voi format moi, smoke tests pass voi 14 agents</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-resource-config.test.js test/smoke-agent-files.test.js test/platform-models.test.js` — tat ca pass
- `grep -n 'function getAgentConfig' bin/lib/resource-config.js` — co platform param
- `grep -n 'function buildParallelPlan' bin/lib/parallel-dispatch.js` — co platform param
- `ls .claude/agents/pd-sec-scanner.md` — file ton tai
- `ls commands/pd/agents/pd-sec-scanner.md` — file goc van con
</verification>

<success_criteria>
- getAgentConfig(name, platform) wire thanh cong vao production flow
- buildParallelPlan la production caller truyen platform (per D-03)
- pd-sec-scanner.md ton tai tai .claude/agents/ voi format chuyen doi dung
- File goc tai commands/pd/agents/ van con nguyen (per D-05)
- Tat ca smoke tests + platform tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/059-integration-wiring-verification/059-01-SUMMARY.md`
</output>
