---
phase: 058-token-budget-benchmark
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/audit.md
  - workflows/scan.md
  - promptfooconfig.yaml
autonomous: true
requirements:
  - TOKN-03
  - TOKN-04
must_haves:
  truths:
    - "Ít nhất 2 workflows mới có conditional_reading block"
    - "Format conditional_reading nhất quán với 8 workflows hiện có"
    - "promptfooconfig.yaml validate thành công (YAML hợp lệ)"
    - "Eval pipeline verify hoạt động đúng (config parseable, run.js logic OK)"
  artifacts:
    - path: "workflows/audit.md"
      provides: "conditional_reading block cho audit workflow"
      contains: "conditional_reading"
    - path: "workflows/scan.md"
      provides: "conditional_reading block cho scan workflow"
      contains: "conditional_reading"
  key_links:
    - from: "workflows/audit.md"
      to: "references/"
      via: "conditional_reading XML tags"
      pattern: "conditional_reading"
    - from: "workflows/scan.md"
      to: "references/"
      via: "conditional_reading XML tags"
      pattern: "conditional_reading"
    - from: "evals/run.js"
      to: "promptfooconfig.yaml"
      via: "promptfoo eval --config"
      pattern: "promptfoo"
---

<objective>
Mở rộng conditional_reading pattern sang audit.md và scan.md (2 workflows mới), verify eval pipeline hoạt động đúng với promptfooconfig.yaml.

Purpose: Giảm token consumption cho workflows nặng bằng conditional loading, đảm bảo eval pipeline sẵn sàng đo chất lượng.
Output: 2 workflows có conditional_reading mới, eval pipeline verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@workflows/plan.md (reference implementation của conditional_reading pattern)
@workflows/audit.md
@workflows/scan.md
@promptfooconfig.yaml
@evals/run.js
</context>

<interfaces>
<!-- Pattern conditional_reading từ workflows/plan.md (reference implementation) -->

```markdown
<required_reading>
Đọc trước khi bắt đầu:
- @templates/plan.md, @templates/tasks.md
- @references/conventions.md → icons, version, commit
</required_reading>

<conditional_reading>
Đọc KHI cần (phân tích mô tả task trước):
- @references/questioning.md -> cách hỏi người dùng (DISCUSS) — KHI chế độ DISCUSS
- @references/prioritization.md -> thứ tự ưu tiên tasks — KHI cần sắp xếp ưu tiên
- @references/ui-brand.md -> thiết kế giao diện, kế thừa patterns — KHI phase liên quan UI
</conditional_reading>
```

Format: `- @references/file.md -> mô tả ngắn — KHI điều kiện cụ thể`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Thêm conditional_reading vào audit.md và scan.md</name>
  <files>workflows/audit.md, workflows/scan.md</files>
  <read_first>
    - workflows/plan.md (dòng 9-20: xem reference implementation của required_reading + conditional_reading pattern)
    - workflows/audit.md (toàn bộ: xem cấu trúc hiện tại, tìm vị trí phù hợp chèn conditional_reading)
    - workflows/scan.md (toàn bộ: xem cấu trúc hiện tại, tìm vị trí phù hợp chèn conditional_reading)
    - workflows/fix-bug.md (dòng 1-20: xem thêm ví dụ conditional_reading pattern)
  </read_first>
  <action>
    **Per D-08, D-09, D-10, D-11:** Thêm conditional_reading block vào 2 workflows ưu tiên cao nhất chưa có pattern này.

    1. **audit.md** (3,992 tokens — ưu tiên cao, workflow nặng nhất trong nhóm chưa có):
       - Chèn block `<conditional_reading>` ngay SAU thẻ `</purpose>` (trước `<process>`), nhất quán với pattern hiện có ở các workflows khác.
       - Nội dung:
         ```markdown
         <conditional_reading>
         Đọc KHI cần (phân tích mô tả task trước):
         - @references/security-checklist.md -> danh sách kiểm tra bảo mật — KHI audit liên quan kiểm tra bảo mật thủ công
         - @references/conventions.md -> quy ước commit, icons — KHI cần tạo báo cáo hoặc commit kết quả audit
         </conditional_reading>
         ```
       - GIỮA NGUYÊN toàn bộ nội dung hiện tại — chỉ chèn block mới.

    2. **scan.md** (1,938 tokens — ưu tiên trung bình):
       - Chèn block `<conditional_reading>` ngay SAU thẻ `</purpose>` (trước `<process>`).
       - Nội dung:
         ```markdown
         <conditional_reading>
         Đọc KHI cần (phân tích mô tả task trước):
         - @references/security-checklist.md -> danh sách kiểm tra bảo mật — KHI scan phát hiện lỗ hổng bảo mật dependencies
         - @references/conventions.md -> quy ước commit, icons — KHI cần format báo cáo scan
         </conditional_reading>
         ```
       - GIỮA NGUYÊN toàn bộ nội dung hiện tại — chỉ chèn block mới.

    3. **Verify count:** Sau khi thêm, chạy `grep -l 'conditional_reading' workflows/*.md | wc -l` — kết quả phải >= 10 (8 hiện có + 2 mới).
  </action>
  <verify>
    <automated>grep -c 'conditional_reading' workflows/audit.md workflows/scan.md && test $(grep -rl 'conditional_reading' workflows/*.md | wc -l) -ge 10</automated>
  </verify>
  <acceptance_criteria>
    - `workflows/audit.md` chứa `<conditional_reading>`
    - `workflows/audit.md` chứa `</conditional_reading>`
    - `workflows/audit.md` chứa `@references/security-checklist.md`
    - `workflows/scan.md` chứa `<conditional_reading>`
    - `workflows/scan.md` chứa `</conditional_reading>`
    - `workflows/scan.md` chứa `@references/security-checklist.md`
    - `grep -rl 'conditional_reading' workflows/*.md | wc -l` trả về >= 10
    - Cả hai file giữ nguyên nội dung `<process>` hiện tại (không mất nội dung)
  </acceptance_criteria>
  <done>audit.md và scan.md có conditional_reading block, tổng >= 10 workflows dùng pattern này</done>
</task>

<task type="auto">
  <name>Task 2: Verify eval pipeline và cập nhật BENCHMARK_RESULTS.md</name>
  <files>promptfooconfig.yaml</files>
  <read_first>
    - promptfooconfig.yaml (toàn bộ: verify cấu trúc YAML hợp lệ)
    - evals/run.js (dòng 1-60: hiểu logic load .env, check ANTHROPIC_API_KEY, saveBenchmark)
    - evals/prompt-wrapper.js (toàn bộ: hiểu PromptFoo integration wrapper)
  </read_first>
  <action>
    **Per D-12, D-13, D-14:** Verify eval pipeline hoạt động đúng — KHÔNG thêm tests mới, chỉ verify config và logic.

    1. **Validate YAML** (per D-14):
       ```bash
       node -e "const yaml=require('js-yaml');const fs=require('fs');const cfg=yaml.load(fs.readFileSync('promptfooconfig.yaml','utf8'));console.log('providers:',cfg.providers?.length||0,'tests:',cfg.tests?.length||0);console.log('YAML valid')"
       ```
       Nếu parse thành công → YAML hợp lệ. Ghi số providers và tests vào output.

    2. **Verify evals/run.js logic** (per D-12):
       - Đọc `evals/run.js` và confirm:
         a. Có `saveBenchmark()` function (lưu JSON vào `evals/benchmarks/`)
         b. Có `compareBenchmarks()` function (so sánh history)
         c. Có `--full` flag handler (chạy cả 2 eval types + save benchmark)
         d. Có `--compare` flag handler
       - KHÔNG chạy eval thực tế (cần ANTHROPIC_API_KEY)

    3. **Verify promptfoo availability**:
       ```bash
       command -v promptfoo 2>/dev/null && echo "promptfoo installed" || echo "promptfoo not found — npx promptfoo available as fallback"
       ```

    4. **Đảm bảo evals/benchmarks/ directory tồn tại** (per D-13):
       ```bash
       mkdir -p evals/benchmarks
       ```

    5. **Ghi kết quả verify vào BENCHMARK_RESULTS.md** (append section nếu Plan 01 đã tạo file, hoặc tạo section trong file mới):
       Thêm section `## Eval Pipeline Status`:
       ```markdown
       ## Eval Pipeline Status

       | Component | Status | Notes |
       |-----------|--------|-------|
       | promptfooconfig.yaml | Valid | N providers, M tests |
       | evals/run.js | Verified | saveBenchmark(), compareBenchmarks() present |
       | promptfoo CLI | [installed/fallback npx] | [version or npx fallback] |
       | evals/benchmarks/ | Ready | Directory exists |
       | ANTHROPIC_API_KEY | Required for live eval | Set in .env to run `npm run eval:full` |
       ```
  </action>
  <verify>
    <automated>node -e "require('js-yaml').load(require('fs').readFileSync('promptfooconfig.yaml','utf8'));console.log('YAML valid')" && test -d evals/benchmarks</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('js-yaml').load(require('fs').readFileSync('promptfooconfig.yaml','utf8'))"` exit code 0
    - `evals/benchmarks/` directory tồn tại
    - `evals/run.js` chứa `saveBenchmark` (function tồn tại)
    - `evals/run.js` chứa `compareBenchmarks` (function tồn tại)
    - `BENCHMARK_RESULTS.md` chứa `## Eval Pipeline Status`
    - `BENCHMARK_RESULTS.md` chứa `promptfooconfig.yaml` và `Valid`
  </acceptance_criteria>
  <done>promptfooconfig.yaml validated, eval pipeline verified, benchmark directory ready, kết quả ghi vào BENCHMARK_RESULTS.md</done>
</task>

</tasks>

<verification>
- `grep -rl 'conditional_reading' workflows/*.md | wc -l` >= 10 (8 cũ + 2 mới)
- `node -e "require('js-yaml').load(require('fs').readFileSync('promptfooconfig.yaml','utf8'))"` — YAML valid
- `test -d evals/benchmarks` — directory tồn tại
- `npm test` — tất cả tests pass (snapshot tests không bị ảnh hưởng bởi conditional_reading thêm vào)
</verification>

<success_criteria>
- audit.md và scan.md có conditional_reading block nhất quán với pattern hiện có
- Tổng >= 10 workflows dùng conditional_reading pattern
- promptfooconfig.yaml validate YAML thành công
- evals/run.js có saveBenchmark() và compareBenchmarks()
- evals/benchmarks/ directory tồn tại
- BENCHMARK_RESULTS.md có section Eval Pipeline Status
</success_criteria>

<output>
After completion, create `.planning/phases/058-token-budget-benchmark/058-02-SUMMARY.md`
</output>
