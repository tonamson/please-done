---
phase: 058-token-budget-benchmark
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/resource-config.js
  - test/smoke-resource-config.test.js
  - test/baseline-tokens.json
  - BENCHMARK_RESULTS.md
autonomous: true
requirements:
  - TOKN-01
  - TOKN-02
must_haves:
  truths:
    - "TOKEN_BUDGET constant tồn tại trong resource-config.js với đúng giá trị per tier"
    - "TIER_MAP có tokenBudget field cho mỗi tier"
    - "Baseline mới phản ánh trạng thái post-v5.0 (48+ files)"
    - "BENCHMARK_RESULTS.md chứa bảng before/after với delta %"
  artifacts:
    - path: "bin/lib/resource-config.js"
      provides: "TOKEN_BUDGET constant và tokenBudget field trong TIER_MAP"
      contains: "TOKEN_BUDGET"
    - path: "test/smoke-resource-config.test.js"
      provides: "Tests cho TOKEN_BUDGET và tokenBudget fields"
      contains: "tokenBudget"
    - path: "test/baseline-tokens.json"
      provides: "Baseline token counts post-v5.0"
    - path: "BENCHMARK_RESULTS.md"
      provides: "Bảng benchmark before/after, per-tier budget compliance"
      contains: "Per-Tier Budget Compliance"
  key_links:
    - from: "bin/lib/resource-config.js"
      to: "test/smoke-resource-config.test.js"
      via: "require('../bin/lib/resource-config')"
      pattern: "TOKEN_BUDGET"
    - from: "scripts/count-tokens.js"
      to: "test/baseline-tokens.json"
      via: "writeBaseline()"
      pattern: "baseline-tokens.json"
---

<objective>
Thêm TOKEN_BUDGET constant vào resource-config.js theo tier (Scout <=4K, Builder <=8K, Architect <=12K), capture baseline mới post-v5.0, và tạo BENCHMARK_RESULTS.md với kết quả before/after.

Purpose: Thiết lập token budget enforcement và đo lường token consumption hiện tại làm cơ sở so sánh.
Output: resource-config.js có TOKEN_BUDGET, tests pass, BENCHMARK_RESULTS.md tại project root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@bin/lib/resource-config.js
@test/smoke-resource-config.test.js
@scripts/count-tokens.js
@test/baseline-tokens.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thêm tokenBudget vào TIER_MAP và export TOKEN_BUDGET constant</name>
  <files>bin/lib/resource-config.js, test/smoke-resource-config.test.js</files>
  <read_first>
    - bin/lib/resource-config.js (xem TIER_MAP hiện tại dòng 22-26, exports dòng 390-406)
    - test/smoke-resource-config.test.js (xem tests hiện tại, thêm test cases mới)
  </read_first>
  <behavior>
    - Test 1: TIER_MAP.scout.tokenBudget === 4000
    - Test 2: TIER_MAP.builder.tokenBudget === 8000
    - Test 3: TIER_MAP.architect.tokenBudget === 12000
    - Test 4: TOKEN_BUDGET export tồn tại với 3 keys (scout, builder, architect)
    - Test 5: TOKEN_BUDGET.scout === TIER_MAP.scout.tokenBudget (nhất quán)
    - Test 6: Mỗi tier trong TIER_MAP có tokenBudget > 0
  </behavior>
  <action>
    1. **Test trước (RED):** Thêm vào cuối `test/smoke-resource-config.test.js`:
       - Import `TOKEN_BUDGET` từ resource-config (thêm vào destructuring dòng 5)
       - Thêm describe block `TOKEN_BUDGET`:
         ```javascript
         describe('TOKEN_BUDGET', () => {
           it('moi tier co tokenBudget trong TIER_MAP', () => {
             for (const [tier, config] of Object.entries(TIER_MAP)) {
               assert.ok(config.tokenBudget > 0, `${tier} thieu tokenBudget`);
             }
           });
           it('scout = 4000, builder = 8000, architect = 12000', () => {
             assert.equal(TIER_MAP.scout.tokenBudget, 4000);
             assert.equal(TIER_MAP.builder.tokenBudget, 8000);
             assert.equal(TIER_MAP.architect.tokenBudget, 12000);
           });
           it('TOKEN_BUDGET export nhat quan voi TIER_MAP', () => {
             assert.equal(TOKEN_BUDGET.scout, TIER_MAP.scout.tokenBudget);
             assert.equal(TOKEN_BUDGET.builder, TIER_MAP.builder.tokenBudget);
             assert.equal(TOKEN_BUDGET.architect, TIER_MAP.architect.tokenBudget);
           });
           it('TOKEN_BUDGET co dung 3 keys', () => {
             assert.deepEqual(Object.keys(TOKEN_BUDGET).sort(), ['architect', 'builder', 'scout']);
           });
         });
         ```
       - Chạy test — phải FAIL (RED)

    2. **Implementation (GREEN):** Sửa `bin/lib/resource-config.js`:
       - TIER_MAP dòng 22-26: thêm `tokenBudget` field cho mỗi tier (per D-01):
         ```javascript
         const TIER_MAP = {
           scout: { model: "haiku", effort: "low", maxTurns: 15, tokenBudget: 4000 },
           builder: { model: "sonnet", effort: "medium", maxTurns: 25, tokenBudget: 8000 },
           architect: { model: "opus", effort: "high", maxTurns: 30, tokenBudget: 12000 },
         };
         ```
       - Thêm TOKEN_BUDGET constant sau TIER_MAP (per D-02):
         ```javascript
         const TOKEN_BUDGET = {
           scout: TIER_MAP.scout.tokenBudget,
           builder: TIER_MAP.builder.tokenBudget,
           architect: TIER_MAP.architect.tokenBudget,
         };
         ```
       - Thêm `TOKEN_BUDGET` vào module.exports (dòng 390-406)
       - Chạy test — phải PASS (GREEN)

    3. **Cập nhật JSDoc** cho TIER_MAP comment (dòng 18-20): thêm `tokenBudget` vào mô tả fields.
  </action>
  <verify>
    <automated>node --test test/smoke-resource-config.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `bin/lib/resource-config.js` chứa `tokenBudget: 4000` trong scout entry
    - `bin/lib/resource-config.js` chứa `tokenBudget: 8000` trong builder entry
    - `bin/lib/resource-config.js` chứa `tokenBudget: 12000` trong architect entry
    - `bin/lib/resource-config.js` chứa `const TOKEN_BUDGET = {`
    - `bin/lib/resource-config.js` exports chứa `TOKEN_BUDGET`
    - `test/smoke-resource-config.test.js` chứa `describe('TOKEN_BUDGET'`
    - `node --test test/smoke-resource-config.test.js` exit code 0
  </acceptance_criteria>
  <done>TOKEN_BUDGET constant exported, TIER_MAP có tokenBudget field, tất cả tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Capture baseline mới và tạo BENCHMARK_RESULTS.md</name>
  <files>test/baseline-tokens.json, BENCHMARK_RESULTS.md</files>
  <read_first>
    - scripts/count-tokens.js (hiểu --baseline và --compare modes)
    - test/baseline-tokens.json (xem baseline hiện tại — sẽ bị overwrite)
  </read_first>
  <action>
    1. **Capture baseline mới** (per D-04):
       ```bash
       node scripts/count-tokens.js --baseline
       ```
       Lệnh này sẽ overwrite `test/baseline-tokens.json` với trạng thái hiện tại (48+ files, ~86K tokens).

    2. **Chạy compare** để lấy delta data (per D-05, D-07):
       ```bash
       node scripts/count-tokens.js --compare
       ```
       Capture output vào biến để dùng cho BENCHMARK_RESULTS.md.

    3. **Tạo BENCHMARK_RESULTS.md** tại project root (per D-06, D-07) với nội dung:
       - Header: `# Benchmark Results` + ngày capture + tool reference
       - Section `## Before/After Summary`: bảng markdown 5 cột (Directory | Before | After | Delta | Change) cho 4 directories + TOTAL. Giá trị "Before" lấy từ baseline cũ (84,899 tokens, 39 files, captured 2026-03-22). Giá trị "After" từ scan mới.
       - Section `## Per-Tier Budget Compliance` (per D-06): bảng 4 cột (Tier | Budget | Typical Workflows | Status). Map dựa trên AGENT_REGISTRY:
         - Scout (4000): scan.md (~1,938 tokens), init.md (~2,241 tokens) → OK
         - Builder (8000): fix-bug.md, write-code.md, test.md → kiểm tra tổng workflow + required_reading
         - Architect (12000): plan.md, new-milestone.md, write-code.md → kiểm tra tổng
       - Section `## Token Distribution`: bảng per-directory (commands/pd, workflows, references, templates) với file count, tokens, % tổng
       - Section `## Baseline History`: ghi lại baseline cũ (v1.0: 84,899/39 files) và baseline mới (v5.0: actual/48 files)

    4. **Verify** file tạo thành công:
       ```bash
       test -f BENCHMARK_RESULTS.md && head -5 BENCHMARK_RESULTS.md
       ```
  </action>
  <verify>
    <automated>node scripts/count-tokens.js --compare && test -f BENCHMARK_RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `test/baseline-tokens.json` có `capturedAt` chứa ngày hôm nay (2026-03-27)
    - `test/baseline-tokens.json` có tổng files >= 48
    - `BENCHMARK_RESULTS.md` chứa `# Benchmark Results`
    - `BENCHMARK_RESULTS.md` chứa `## Before/After Summary`
    - `BENCHMARK_RESULTS.md` chứa `## Per-Tier Budget Compliance`
    - `BENCHMARK_RESULTS.md` chứa `Scout` và `4,000` (hoặc `4000`)
    - `BENCHMARK_RESULTS.md` chứa `Builder` và `8,000` (hoặc `8000`)
    - `BENCHMARK_RESULTS.md` chứa `Architect` và `12,000` (hoặc `12000`)
    - `BENCHMARK_RESULTS.md` chứa `## Baseline History`
  </acceptance_criteria>
  <done>Baseline mới captured (48+ files), BENCHMARK_RESULTS.md có đầy đủ bảng before/after và per-tier compliance</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-resource-config.test.js` — tất cả tests pass bao gồm TOKEN_BUDGET tests mới
- `node scripts/count-tokens.js --compare` — chạy thành công, baseline mới phản ánh 48+ files
- `test -f BENCHMARK_RESULTS.md` — file tồn tại tại project root
- `grep 'TOKEN_BUDGET' bin/lib/resource-config.js` — constant được export
</verification>

<success_criteria>
- TOKEN_BUDGET constant exported từ resource-config.js với giá trị: scout=4000, builder=8000, architect=12000
- TIER_MAP có tokenBudget field cho cả 3 tiers
- Baseline mới captured tại test/baseline-tokens.json (48+ files, timestamp hôm nay)
- BENCHMARK_RESULTS.md tại project root chứa bảng before/after và per-tier budget compliance
- Tất cả smoke tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/058-token-budget-benchmark/058-01-SUMMARY.md`
</output>
