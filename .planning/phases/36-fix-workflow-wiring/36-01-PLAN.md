---
phase: 36-fix-workflow-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/fix-bug.md
  - test/snapshots/codex/fix-bug.md
  - test/snapshots/copilot/fix-bug.md
  - test/snapshots/gemini/fix-bug.md
  - test/snapshots/opencode/fix-bug.md
autonomous: true
requirements: [PROT-08, FLOW-02, FLOW-05]
gap_closure: true

must_haves:
  truths:
    - "mergeParallelResults nhan { evidenceContent: detectiveContent } thay vi validateEvidence result — Detective khong con FAILED gia"
    - "runLogicSync return duoc destructure dung: logicResult, reportResult, rulesResult, warnings"
    - "Khi logicResult?.hasLogicChange && reportResult, PDF update prompt fires"
    - "Khi rulesResult?.suggestions?.length > 0, CLAUDE.md update prompt fires"
    - "Toan bo tests pass, snapshots nhat quan"
  artifacts:
    - path: "workflows/fix-bug.md"
      provides: "Workflow voi 2 call sites da fix"
      contains: "detectiveValidation"
    - path: "workflows/fix-bug.md"
      provides: "runLogicSync destructuring dung"
      contains: "logicResult, reportResult, rulesResult, warnings"
  key_links:
    - from: "workflows/fix-bug.md dong 126"
      to: "bin/lib/parallel-dispatch.js mergeParallelResults"
      via: "detectiveResult = { evidenceContent: detectiveContent }"
      pattern: "evidenceContent: detectiveContent"
    - from: "workflows/fix-bug.md dong 357"
      to: "bin/lib/logic-sync.js runLogicSync"
      via: "{ logicResult, reportResult, rulesResult, warnings }"
      pattern: "logicResult, reportResult, rulesResult"
---

<objective>
Fix 2 wiring bugs trong fix-bug.md: detectiveResult shape mismatch khi goi mergeParallelResults (INT-07), va runLogicSync return destructuring sai (INT-08). Day la gap closure cuoi cung cua v2.1 milestone.

Purpose: Dam bao E2E flows hoat dong dung — Detective khong con FAILED gia, PDF update va CLAUDE.md rule suggestion fire khi can.
Output: workflows/fix-bug.md da fix + 4 platform snapshots regenerated + toan bo tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-fix-workflow-wiring/36-CONTEXT.md
@.planning/phases/36-fix-workflow-wiring/36-RESEARCH.md

<interfaces>
<!-- Module signatures — KHONG sua modules, chi sua workflow goi chung -->

From bin/lib/parallel-dispatch.js dong 66:
```javascript
function mergeParallelResults({ detectiveResult, docSpecResult })
// detectiveResult: { evidenceContent: string } hoac { error: { message } }
// docSpecResult: { evidenceContent: string } hoac { error: { message } } hoac null
// Check: detectiveResult?.evidenceContent (dong 71) — neu undefined -> FAILED
```

From bin/lib/logic-sync.js dong 220-249:
```javascript
function runLogicSync(params) {
  return { logicResult, reportResult, rulesResult, warnings };
}
// logicResult: { hasLogicChange, signals, summary } hoac null
// reportResult: { updatedContent, diagramResult } hoac null
// rulesResult: { suggestions: string[], reasoning: string }
// warnings: string[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix detectiveResult shape mismatch (INT-07) va runLogicSync destructuring (INT-08)</name>
  <files>workflows/fix-bug.md</files>
  <read_first>
    - workflows/fix-bug.md (dong 120-145 cho INT-07, dong 350-365 cho INT-08)
    - bin/lib/parallel-dispatch.js (dong 60-85 — mergeParallelResults signature)
    - bin/lib/logic-sync.js (dong 215-250 — runLogicSync return shape)
  </read_first>
  <action>
**INT-07 fix (dong 125-138) — per D-01, D-02, D-03:**

Dong 126 hien tai:
```
   - Goi `validateEvidence(detectiveContent)` tu `bin/lib/evidence-protocol.js` -> detectiveResult
```
Doi thanh:
```
   - Goi `validateEvidence(detectiveContent)` tu `bin/lib/evidence-protocol.js` -> detectiveValidation
   - Construct detectiveResult: { evidenceContent: detectiveContent }
```

Dong 133 hien tai:
```
   - Neu file ton tai: validateEvidence(docSpecContent) -> docSpecResult
   - Neu file KHONG ton tai hoac invalid: docSpecResult = null
```
Doi thanh:
```
   - Neu file ton tai: validateEvidence(docSpecContent) -> docSpecValidation
   - Construct docSpecResult: { evidenceContent: docSpecContent }
   - Neu file KHONG ton tai hoac invalid: docSpecResult = null
```

Giu nguyen dong 136-139 (mergeParallelResults call va comments) — chi thay doi variable names o dong 126 va 133.

**INT-08 fix (dong 356-361) — per D-04, D-05, D-06:**

Dong 357 hien tai:
```
   -> { hasLogicChange, signals, diagramUpdated, rulesSuggested }
```
Doi thanh:
```
   -> { logicResult, reportResult, rulesResult, warnings }
```

Dong 359 hien tai:
```
7. hasLogicChange = true va diagramUpdated -> hoi: "Cap nhat lai PDF? (Y/n)"
```
Doi thanh:
```
7. logicResult?.hasLogicChange = true va reportResult !== null -> hoi: "Cap nhat lai PDF? (Y/n)"
```

Dong 361 hien tai:
```
8. rulesSuggested co noi dung -> hien va hoi: "Them vao CLAUDE.md? (Y/n)"
```
Doi thanh:
```
8. rulesResult?.suggestions?.length > 0 -> hien rulesResult.suggestions va hoi: "Them vao CLAUDE.md? (Y/n)"
```
  </action>
  <verify>
    <automated>grep -n "detectiveValidation" workflows/fix-bug.md && grep -n "docSpecValidation" workflows/fix-bug.md && grep -n "evidenceContent: detectiveContent" workflows/fix-bug.md && grep -n "logicResult, reportResult, rulesResult, warnings" workflows/fix-bug.md && grep -n "logicResult?.hasLogicChange" workflows/fix-bug.md && grep -n "rulesResult?.suggestions?.length" workflows/fix-bug.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/fix-bug.md chua "detectiveValidation" (rename tu detectiveResult)
    - workflows/fix-bug.md chua "docSpecValidation" (rename tu docSpecResult)
    - workflows/fix-bug.md chua "evidenceContent: detectiveContent" (construct dung shape)
    - workflows/fix-bug.md chua "evidenceContent: docSpecContent" (construct dung shape)
    - workflows/fix-bug.md chua "logicResult, reportResult, rulesResult, warnings" (destructuring dung)
    - workflows/fix-bug.md chua "logicResult?.hasLogicChange" (optional chaining)
    - workflows/fix-bug.md chua "rulesResult?.suggestions?.length > 0" (optional chaining)
    - workflows/fix-bug.md KHONG chua "-> detectiveResult" (old assignment da xoa)
    - workflows/fix-bug.md KHONG chua "hasLogicChange, signals, diagramUpdated, rulesSuggested" (old destructuring da xoa)
    - validateEvidence call van ton tai (per D-03 — giu validation rieng)
  </acceptance_criteria>
  <done>2 call sites trong fix-bug.md da fix: mergeParallelResults nhan dung { evidenceContent } shape, runLogicSync return destructure dung voi optional chaining.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate snapshots va verify toan bo tests pass</name>
  <files>
    test/snapshots/codex/fix-bug.md
    test/snapshots/copilot/fix-bug.md
    test/snapshots/gemini/fix-bug.md
    test/snapshots/opencode/fix-bug.md
  </files>
  <read_first>
    - workflows/fix-bug.md (xac nhan Task 1 da apply)
    - test/generate-snapshots.js (hieu cach regenerate)
  </read_first>
  <action>
1. Chay `node test/generate-snapshots.js` de regenerate 4 platform snapshots tu workflows/fix-bug.md da fix (per D-08).
2. Chay `npm test` de verify toan bo tests pass (per D-07 — KHONG them tests moi).
3. Xac nhan 763+ tests pass, 0 failures, 0 regressions.
  </action>
  <verify>
    <automated>node test/generate-snapshots.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - test/snapshots/codex/fix-bug.md chua "detectiveValidation" (snapshot da regenerate)
    - test/snapshots/copilot/fix-bug.md chua "detectiveValidation"
    - test/snapshots/gemini/fix-bug.md chua "detectiveValidation"
    - test/snapshots/opencode/fix-bug.md chua "detectiveValidation"
    - npm test exit code 0
    - Khong co test failures hoac regressions
  </acceptance_criteria>
  <done>4 platform snapshots regenerated, toan bo tests pass, khong regression. Phase 36 hoan tat — v2.1 milestone gap closure xong.</done>
</task>

</tasks>

<verification>
1. `grep -c "detectiveValidation" workflows/fix-bug.md` tra ve >= 1
2. `grep -c "evidenceContent: detectiveContent" workflows/fix-bug.md` tra ve >= 1
3. `grep -c "logicResult, reportResult, rulesResult, warnings" workflows/fix-bug.md` tra ve >= 1
4. `grep -c "logicResult?.hasLogicChange" workflows/fix-bug.md` tra ve >= 1
5. `grep -c "rulesResult?.suggestions?.length" workflows/fix-bug.md` tra ve >= 1
6. `npm test` pass toan bo (763+ tests, 0 failures)
7. 4 platform snapshots chua noi dung moi (detectiveValidation, logicResult)
</verification>

<success_criteria>
- INT-07 closed: mergeParallelResults nhan { evidenceContent: detectiveContent } — Detective khong con FAILED gia
- INT-08 closed: runLogicSync return destructure dung voi optional chaining — PDF update va CLAUDE.md rules fire khi can
- Toan bo 763+ tests pass, snapshots nhat quan
- v2.1 milestone gap closure hoan tat
</success_criteria>

<output>
After completion, create `.planning/phases/36-fix-workflow-wiring/36-01-SUMMARY.md`
</output>
