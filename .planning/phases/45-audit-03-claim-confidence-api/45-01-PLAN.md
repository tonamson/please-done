---
phase: 45-audit-03-claim-confidence-api
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/research-store.js
  - test/smoke-research-store.test.js
autonomous: true
requirements:
  - AUDIT-03
must_haves:
  truths:
    - "parseClaims(content) extract structured claims tu section ## Bang chung"
    - "parseClaims tra confidence per claim (HIGH/MEDIUM/LOW hoac null)"
    - "createEntry voi claims[] render inline confidence tags trong ## Bang chung"
    - "createEntry backward-compatible khi khong co claims[]"
    - "validateEvidence goi parseClaims noi bo, API output khong doi"
    - "Round-trip: createEntry(claims) -> parseClaims khop data"
  artifacts:
    - path: "bin/lib/research-store.js"
      provides: "parseClaims function + createEntry claims rendering + validateEvidence refactor"
      exports: ["parseClaims"]
      contains: "function parseClaims"
    - path: "test/smoke-research-store.test.js"
      provides: "Tests cho parseClaims, createEntry claims, validateEvidence refactor, round-trip"
      contains: "parseClaims"
  key_links:
    - from: "bin/lib/research-store.js:validateEvidence"
      to: "bin/lib/research-store.js:parseClaims"
      via: "internal function call"
      pattern: "parseClaims\\(content\\)"
    - from: "bin/lib/research-store.js:createEntry"
      to: "bin/lib/research-store.js:parseClaims"
      via: "render claims -> parseClaims can parse back"
      pattern: "claims\\.map"
---

<objective>
Mo rong research-store.js de ho tro confidence o cap claim — hoan thanh AUDIT-03.

Purpose: Hien tai chi co file-level confidence (frontmatter). Phase nay them claim-level confidence: parseClaims() extract, createEntry() render, validateEvidence() refactor de reuse parseClaims().
Output: parseClaims function moi, createEntry ho tro claims[], validateEvidence refactored, tat ca tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-audit-03-claim-confidence-api/45-CONTEXT.md
@.planning/phases/45-audit-03-claim-confidence-api/45-RESEARCH.md

<interfaces>
<!-- Key types va contracts executor can biet. Extract tu codebase. -->

From bin/lib/research-store.js:
```javascript
// Existing exports (KHONG thay doi API):
module.exports = {
  CONFIDENCE_LEVELS,    // { HIGH: 'HIGH', MEDIUM: 'MEDIUM', LOW: 'LOW' }
  CONFIDENCE_CRITERIA,
  REQUIRED_FIELDS,      // ['agent', 'created', 'source', 'topic', 'confidence']
  SOURCE_TYPES,         // ['internal', 'external']
  createEntry,          // (options) => { content, filename }
  parseEntry,           // (content) => { frontmatter, body, valid, errors }
  validateConfidence,   // (level) => boolean
  generateFilename,     // (options) => string
  validateEvidence,     // (content) => { valid, warnings }
  appendAuditLog,       // (existingContent, entry) => string
  generateIndex,        // (entries) => string
  routeQuery,           // (query) => 'internal' | 'external'
};

// THEM MOI:
// parseClaims(content) => [{ text, source, confidence }]
```

From bin/lib/utils.js (used by research-store):
```javascript
function parseFrontmatter(content) // => { frontmatter: object, body: string }
function buildFrontmatter(obj)     // => string (YAML lines)
```

From bin/lib/confidence-scorer.js (reference):
```javascript
function scoreConfidence(data) // rule-based confidence scoring
function classifySource(source) // classify source quality
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD parseClaims + createEntry claims + validateEvidence refactor</name>
  <files>bin/lib/research-store.js, test/smoke-research-store.test.js</files>
  <read_first>
    - bin/lib/research-store.js (full file — hieu createEntry dong 130, validateEvidence dong 238, section extraction regex dong 253)
    - test/smoke-research-store.test.js (full file — hieu test patterns hien co, import list)
    - bin/lib/utils.js (parseFrontmatter, buildFrontmatter — hieu interface)
  </read_first>
  <behavior>
    parseClaims tests:
    - parseClaims(content_with_claims) tra ve array 3 objects voi text, source, confidence dung
    - parseClaims(content_without_confidence_tags) tra ve confidence: null per D-09
    - parseClaims(content_with_em_dash) va parseClaims(content_with_double_dash) deu parse dung per D-06
    - parseClaims(content_no_bang_chung_section) tra ve []
    - parseClaims(content_empty_bang_chung) tra ve []
    - parseClaims(null) tra ve []
    - parseClaims(content_claim_no_source_separator) tra ve { text, source: null, confidence: null }
    - parseClaims(content_source_with_parentheses) parse dung — khong nham voi confidence tag

    createEntry claims tests:
    - createEntry({ ...required, claims: [{ text, source, confidence }] }) render `- text — source (confidence: LEVEL)` trong ## Bang chung
    - createEntry({ ...required, body: custom_body, claims: [...] }) append claims sau body (D-02)
    - createEntry({ ...required, body: body_with_existing_bang_chung, claims: [...] }) append vao cuoi section, khong duplicate header
    - createEntry({ ...required }) KHONG co claims => backward-compatible, output giong cu
    - createEntry({ ...required, claims: [] }) empty array => backward-compatible

    validateEvidence refactor tests:
    - Tat ca existing validateEvidence tests VAN PASS (D-08 — API output khong doi)
    - validateEvidence noi bo goi parseClaims (kiem tra qua behavior — cung ket qua)

    Round-trip test:
    - createEntry voi claims -> parseClaims tren output content -> ket qua khop claims dau vao
  </behavior>
  <action>
    TDD RED-GREEN-REFACTOR cycle. Lam tuan tu theo thu tu:

    **RED phase — Viet tests truoc:**

    1. Them `parseClaims` vao import list trong test file (dong 14, them vao destructure tu require)

    2. Them describe blocks cho parseClaims:

    Test fixtures dung trong tests:
    ```javascript
    const CLAIMS_CONTENT = `---
    agent: evidence-collector
    created: 2026-03-25T10:00:00.000Z
    source: internal
    topic: Auth Module
    confidence: MEDIUM
    ---
    # Auth Module

    ## Bang chung

    - API dung JWT tokens \u2014 Source code auth.js (confidence: HIGH)
    - Session timeout 30 phut \u2014 Config docs (confidence: MEDIUM)
    - Rate limiting chua implement -- Grep ket qua (confidence: LOW)
    `;

    const CLAIMS_NO_CONF = `---
    agent: test
    created: 2026-03-25T10:00:00.000Z
    source: internal
    topic: Test
    confidence: MEDIUM
    ---
    # Test

    ## Bang chung

    - Claim A \u2014 SourceX
    - Claim B -- SourceY
    `;

    const CLAIMS_NO_SECTION = `---
    agent: test
    created: 2026-03-25T10:00:00.000Z
    source: internal
    topic: Test
    confidence: MEDIUM
    ---
    # Test

    No bang chung section here.
    `;

    const CLAIMS_EMPTY_SECTION = `# Test
    ## Bang chung

    ## Ket luan
    Done.
    `;

    const CLAIMS_NO_SEPARATOR = `# Test
    ## Bang chung

    - Claim khong co source separator chi tiet
    `;

    const CLAIMS_SOURCE_PARENS = `# Test
    ## Bang chung

    - React hooks (v18) giup state management \u2014 React docs (v18.2) (confidence: HIGH)
    `;
    ```

    3. Them describe blocks cho createEntry voi claims[]:
    - Test voi claims array => content chua `- API dung JWT tokens \u2014 Source code auth.js (confidence: HIGH)`
    - Test body + claims song song => body content VAN con, claims duoc append
    - Test body da co ## Bang chung + claims => khong duplicate `## Bang chung` header
    - Test khong co claims => output giong het createEntry hien tai (backward-compatible)
    - Test claims = [] => output giong het createEntry hien tai

    4. Them round-trip test:
    ```javascript
    describe('Round-trip: createEntry claims -> parseClaims', () => {
      it('claims input khop voi parseClaims output', () => {
        const inputClaims = [
          { text: 'JWT auth', source: 'auth.js', confidence: 'HIGH' },
          { text: 'Session 30min', source: 'config', confidence: 'MEDIUM' },
        ];
        const { content } = createEntry({
          agent: 'test', source: 'internal', topic: 'RT',
          confidence: 'HIGH', claims: inputClaims,
          created: '2026-03-25T10:00:00.000Z',
        });
        const parsed = parseClaims(content);
        assert.equal(parsed.length, 2);
        assert.equal(parsed[0].text, 'JWT auth');
        assert.equal(parsed[0].source, 'auth.js');
        assert.equal(parsed[0].confidence, 'HIGH');
        assert.equal(parsed[1].text, 'Session 30min');
        assert.equal(parsed[1].source, 'config');
        assert.equal(parsed[1].confidence, 'MEDIUM');
      });
    });
    ```

    5. Chay tests — PHAI FAIL (parseClaims chua ton tai).
    6. Commit: `test(45-01): add failing tests for parseClaims, createEntry claims, round-trip`

    **GREEN phase — Implement:**

    7. Trong `bin/lib/research-store.js`, them function `parseClaims(content)` SAU validateEvidence (truoc appendAuditLog, khoang dong 271):

    ```javascript
    function parseClaims(content) {
      if (content == null || typeof content !== 'string') return [];

      // Reuse section extraction regex tu validateEvidence dong 253
      const sectionMatch = content.match(/^## Bang chung\s*\n([\s\S]*?)(?=^## |\s*$)/m);
      if (!sectionMatch || !sectionMatch[1].trim()) return [];

      const lines = sectionMatch[1].match(/^- .+/gm) || [];
      return lines.map(line => {
        const text = line.slice(2).trim(); // Bo "- " prefix

        // Tim source separator: em dash hoac double dash (D-06)
        const sepMatch = text.match(/^(.+?)\s*(?:\u2014|--)\s*(.+)$/);
        if (!sepMatch) {
          return { text, source: null, confidence: null };
        }

        const claimText = sepMatch[1].trim();
        let sourceText = sepMatch[2].trim();
        let confidence = null;

        // Extract confidence tag tu cuoi source (D-09: null neu khong co)
        const confMatch = sourceText.match(/^(.*?)\s*\(confidence:\s*(HIGH|MEDIUM|LOW)\)\s*$/i);
        if (confMatch) {
          sourceText = confMatch[1].trim();
          confidence = confMatch[2].toUpperCase();
        }

        return { text: claimText, source: sourceText, confidence };
      });
    }
    ```

    8. Mo rong `createEntry()` — them `claims` vao destructure (dong 135), them rendering logic TRUOC `return { content, filename }` (dong 175):

    ```javascript
    // Sau dong 135, them claims vao destructure:
    const { agent, source, topic, confidence, body, id, slug, created, claims } = options;

    // Sau dong 167 (bodyContent), them logic render claims:
    let finalBody = bodyContent;
    if (Array.isArray(claims) && claims.length > 0) {
      const renderedClaims = claims.map(c => {
        let line = `- ${c.text}`;
        if (c.source) line += ` \u2014 ${c.source}`;
        if (c.confidence && validateConfidence(c.confidence)) {
          line += ` (confidence: ${c.confidence.toUpperCase()})`;
        }
        return line;
      }).join('\n');

      if (finalBody.includes('## Bang chung')) {
        // Append claims vao cuoi section Bang chung hien co
        const sectionEnd = finalBody.match(/^## Bang chung\s*\n([\s\S]*?)(?=^## |\s*$)/m);
        if (sectionEnd) {
          const insertPos = sectionEnd.index + sectionEnd[0].length;
          const before = finalBody.slice(0, insertPos).trimEnd();
          const after = finalBody.slice(insertPos);
          finalBody = before + '\n' + renderedClaims + '\n' + after;
        }
      } else {
        // Them section ## Bang chung moi
        finalBody = finalBody.trimEnd() + '\n\n## Bang chung\n\n' + renderedClaims + '\n';
      }
    }

    // Thay bodyContent bang finalBody trong dong build content:
    const content = `---\n${buildFrontmatter(fm)}\n---\n${finalBody}`;
    ```

    9. Refactor `validateEvidence()` de goi `parseClaims()` (D-07, D-08):
    ```javascript
    function validateEvidence(content) {
      if (content == null || typeof content !== 'string' || content.trim() === '') {
        throw new Error('thieu tham so content');
      }

      const warnings = [];

      // Kiem tra section ## Bang chung ton tai (giu nguyen)
      const hasSection = /^## Bang chung/m.test(content);
      if (!hasSection) {
        warnings.push('thieu section: ## Bang chung');
        return { valid: false, warnings };
      }

      // Dung parseClaims thay vi tu parse (D-07)
      const claims = parseClaims(content);
      if (claims.length === 0) {
        warnings.push('section Bang chung rong');
        return { valid: false, warnings };
      }

      // Kiem tra tung claim co source separator khong (giu nguyen logic)
      for (const claim of claims) {
        if (claim.source === null) {
          const truncated = claim.text.slice(0, 60) + (claim.text.length > 60 ? '...' : '');
          warnings.push(`claim thieu source: - ${truncated}`);
        }
      }

      return { valid: warnings.length === 0, warnings };
    }
    ```

    10. Them `parseClaims` vao module.exports (dong 386).

    11. Chay tests — TAT CA PHAI PASS (ca tests moi va tests cu).
    12. Commit: `feat(45-01): implement parseClaims, createEntry claims, refactor validateEvidence`

    **REFACTOR phase (neu can):**
    13. Review code, clean up neu co duplication. Chay tests lai. Commit neu co thay doi.
  </action>
  <verify>
    <automated>node --test test/smoke-research-store.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/smoke-research-store.test.js chua `parseClaims` trong import destructure
    - test/smoke-research-store.test.js chua `describe('parseClaims`
    - test/smoke-research-store.test.js chua `describe('Round-trip: createEntry claims -> parseClaims`
    - bin/lib/research-store.js chua `function parseClaims(content)`
    - bin/lib/research-store.js chua `parseClaims` trong module.exports
    - bin/lib/research-store.js createEntry destructure chua `claims`
    - bin/lib/research-store.js validateEvidence goi `parseClaims(content)` thay vi tu parse
    - `node --test test/smoke-research-store.test.js` exit code 0
    - Existing validateEvidence tests van pass (backward-compatible D-08)
    - Existing createEntry tests van pass (backward-compatible D-02)
  </acceptance_criteria>
  <done>
    parseClaims() extract claims tu content, createEntry() render claims voi inline confidence tags, validateEvidence() refactored de goi parseClaims(). Tat ca tests pass bao gom round-trip test.
  </done>
</task>

</tasks>

<verification>
1. `node --test test/smoke-research-store.test.js` — tat ca tests pass
2. `grep 'function parseClaims' bin/lib/research-store.js` — ham ton tai
3. `grep 'parseClaims' bin/lib/research-store.js | grep 'module.exports'` — duoc export
4. `grep 'parseClaims(content)' bin/lib/research-store.js` — validateEvidence goi parseClaims
</verification>

<success_criteria>
- parseClaims(content) tra ve [{ text, source, confidence }] tu section ## Bang chung
- createEntry({ claims: [...] }) render inline confidence tags
- createEntry khong co claims => output giong cu (backward-compatible)
- validateEvidence API output { valid, warnings } KHONG thay doi (D-08)
- Round-trip createEntry -> parseClaims khop data
- Tat ca tests (cu + moi) pass
</success_criteria>

<output>
After completion, create `.planning/phases/45-audit-03-claim-confidence-api/45-01-SUMMARY.md`
</output>
