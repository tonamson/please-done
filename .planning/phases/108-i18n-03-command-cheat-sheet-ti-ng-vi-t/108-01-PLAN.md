---
phase: 108-i18n-03-command-cheat-sheet-ti-ng-vi-t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/cheatsheet.vi.md
  - docs/cheatsheet.md
autonomous: true
requirements:
  - I18N-03
must_haves:
  truths:
    - docs/cheatsheet.vi.md exists with complete Vietnamese translation
    - docs/cheatsheet.md has language switcher badges linking to Vietnamese version
    - All 16 commands preserved in English (not translated)
    - All 15 flags preserved in English (not translated)
    - All 5 command category tables have identical structure
    - Popular Flags Reference table present with translated descriptions
    - Legend section preserved with universal notation symbols
    - HTML comment header tracks source version
  artifacts:
    - path: docs/cheatsheet.vi.md
      provides: Vietnamese translation of command cheat sheet
      min_lines: 195
    - path: docs/cheatsheet.md
      provides: English source with language switcher badges
      contains: shield.io badge linking to cheatsheet.vi.md
  key_links:
    - from: docs/cheatsheet.md
      to: docs/cheatsheet.vi.md
      via: language switcher badge
      pattern: "cheatsheet.vi.md"
---

<objective>
Create Vietnamese translation of the Command Cheat Sheet (docs/cheatsheet.vi.md) with full content preservation. Translate natural language content (descriptions, headers, notes) while keeping all technical elements (commands, flags, paths, table structures) identical to the English source.

Purpose: Provide Vietnamese-speaking developers with a translated quick reference guide while maintaining technical accuracy by preserving command syntax and flag names.

Output: Fully translated docs/cheatsheet.vi.md with HTML header, language switchers, and identical table structures to docs/cheatsheet.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-i18n-03-command-cheat-sheet-ti-ng-vi-t/108-CONTEXT.md
@.planning/phases/108-i18n-03-command-cheat-sheet-ti-ng-vi-t/108-RESEARCH.md
@docs/cheatsheet.md
@README.vi.md
@CLAUDE.vi.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/cheatsheet.vi.md with Vietnamese translation</name>
  <files>docs/cheatsheet.vi.md</files>
  <read_first>
    - docs/cheatsheet.md (source content ~202 lines)
    - README.vi.md (translation pattern reference)
    - CLAUDE.vi.md (technical terminology reference)
  </read_first>
  <action>
Create docs/cheatsheet.vi.md with the following structure:

1. HTML Comment Header (lines 1-4):
   ```
   <!-- Translated from cheatsheet.md -->
   <!-- Source version: 4.0.0 -->
   <!-- Translation date: 2026-04-05 -->
   ```

2. Language Switcher Badges (lines 5-6):
   ```markdown
   [![English](https://img.shields.io/badge/lang-English-blue.svg)](cheatsheet.md)
   [![Tiếng Việt](https://img.shields.io/badge/lang-Tiếng%20Việt-red.svg)](cheatsheet.vi.md)
   ```

3. Title: "# Bảng Tham Khảo Lệnh Please Done"

4. Translate all sections following this mapping:
   - "How to Read This Cheat Sheet" → "Cách Đọc Bảng Tham Khảo Này"
   - "Table of Contents" → "Mục Lục"
   - "Project Commands" → "Lệnh Quản Lý Dự Án"
   - "Planning Commands" → "Lệnh Lập Kế Hoạch"
   - "Execution Commands" → "Lệnh Thực Thi"
   - "Debug Commands" → "Lệnh Gỡ Lỗi"
   - "Utility Commands" → "Lệnh Tiện Ích"
   - "Popular Flags Reference" → "Tham Khảo Cờ Thường Dùng"
   - "Legend" → "Chú Giải"
   - "Command Count Summary" → "Tổng Kết Số Lệnh"

5. Preserve ALL commands (/pd:*), flags (--*), file paths exactly as in source

6. Translate descriptions in tables but keep Command/Usage/Example columns with English commands

7. Keep Legend notation symbols ([--flag], |, etc.) universal, translate explanations only

8. Maintain all table structures (5 command category tables + flags reference + legend value types + command count)

Reference terminology from CLAUDE.vi.md for consistency:
- "onboard" → "Định hướng AI"
- "init" → "Khởi tạo"
- "plan" → "Lập kế hoạch"
- "write-code" → "Viết code"
- "status" → "Trạng thái"
- "fix-bug" → "Sửa lỗi"
  </action>
  <verify>
    <automated>test -f docs/cheatsheet.vi.md && wc -l docs/cheatsheet.vi.md</automated>
  </verify>
  <acceptance_criteria>
    - File docs/cheatsheet.vi.md exists
    - File contains HTML comment header with "Translated from cheatsheet.md"
    - File contains language switcher badges with shield.io URLs
    - File has title "Bảng Tham Khảo Lệnh Please Done" or equivalent
    - Line count is within 10% of docs/cheatsheet.md (180-220 lines)
    - All 5 command category sections are present (Project, Planning, Execution, Debug, Utility)
    - grep -c "^|" docs/cheatsheet.vi.md returns approximately the same count as English source
  </acceptance_criteria>
  <done>docs/cheatsheet.vi.md created with complete Vietnamese translation, HTML header, badges, and all table structures preserved</done>
</task>

<task type="auto">
  <name>Task 2: Add language switcher badges to docs/cheatsheet.md</name>
  <files>docs/cheatsheet.md</files>
  <read_first>
    - docs/cheatsheet.md (to understand current structure)
    - README.vi.md (badge format reference)
  </read_first>
  <action>
Add language switcher badges to the top of docs/cheatsheet.md (after the title, before the intro paragraph):

Insert these two lines after line 1 (the title):
```markdown

[![English](https://img.shields.io/badge/lang-English-blue.svg)](cheatsheet.md)
[![Tiếng Việt](https://img.shields.io/badge/lang-Tiếng%20Việt-red.svg)](cheatsheet.vi.md)
```

Ensure proper spacing: blank line after title, then badges on consecutive lines, then blank line before "Quick reference..." paragraph.
  </action>
  <verify>
    <automated>head -10 docs/cheatsheet.md | grep -c "img.shields.io/badge/lang"</automated>
  </verify>
  <acceptance_criteria>
    - docs/cheatsheet.md contains language badge with lang-English-blue.svg
    - docs/cheatsheet.md contains language badge with lang-Tiếng%20Việt-red.svg
    - Badge links point to cheatsheet.md and cheatsheet.vi.md respectively
    - Badges appear in the first 10 lines of the file
    - No duplicate badges added
  </acceptance_criteria>
  <done>Language switcher badges added to docs/cheatsheet.md linking to Vietnamese version</done>
</task>

<task type="auto">
  <name>Task 3: Verify command and flag preservation</name>
  <files>docs/cheatsheet.vi.md</files>
  <read_first>
    - docs/cheatsheet.vi.md (to verify translation)
    - docs/cheatsheet.md (source of truth for commands)
  </read_first>
  <action>
Verify that all technical elements are preserved correctly in the Vietnamese translation:

1. Extract and count commands from both files:
   - Source: grep -oE '/pd:[a-z-]+' docs/cheatsheet.md | sort | uniq
   - Translation: grep -oE '/pd:[a-z-]+' docs/cheatsheet.vi.md | sort | uniq
   - Both should have identical output

2. Extract and count flags from both files:
   - Source: grep -oE '--[a-z-]+' docs/cheatsheet.md | sort | uniq
   - Translation: grep -oE '--[a-z-]+' docs/cheatsheet.vi.md | sort | uniq
   - Both should have identical output

3. Verify 5 command categories exist:
   - grep -c "## Lệnh Quản Lý Dự Án\|## Project Commands" docs/cheatsheet.vi.md
   - grep -c "## Lệnh Lập Kế Hoạch\|## Planning Commands" docs/cheatsheet.vi.md
   - grep -c "## Lệnh Thực Thi\|## Execution Commands" docs/cheatsheet.vi.md
   - grep -c "## Lệnh Gỡ Lỗi\|## Debug Commands" docs/cheatsheet.vi.md
   - grep -c "## Lệnh Tiện Ích\|## Utility Commands" docs/cheatsheet.vi.md

4. Verify table structures match (pipe count per line should be similar)
  </action>
  <verify>
    <automated>
      # Command count check
      CMD_SOURCE=$(grep -oE '/pd:[a-z-]+' docs/cheatsheet.md | sort -u | wc -l)
      CMD_TRANS=$(grep -oE '/pd:[a-z-]+' docs/cheatsheet.vi.md | sort -u | wc -l)
      test "$CMD_SOURCE" -eq "$CMD_TRANS" && echo "Commands: PASS ($CMD_SOURCE)" || echo "Commands: FAIL (source=$CMD_SOURCE, trans=$CMD_TRANS)"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -oE '/pd:[a-z-]+' docs/cheatsheet.vi.md produces 16 unique commands
    - grep -oE '--[a-z-]+' docs/cheatsheet.vi.md produces 15 unique flags
    - File contains "## Lệnh" sections for all 5 categories (translated headers)
    - File contains "## Chú Giải" or "## Legend" section
    - File contains "Tham Khảo Cờ" or "Popular Flags" section
    - No commands were accidentally translated (no /pd:lập-kế-hoạch, etc.)
    - No flags were accidentally translated (no --tự-động, etc.)
  </acceptance_criteria>
  <done>All 16 commands and 15 flags preserved in English; section headers translated to Vietnamese; table structures intact</done>
</task>

<task type="auto">
  <name>Task 4: Verify completeness and structure integrity</name>
  <files>docs/cheatsheet.vi.md, docs/cheatsheet.md</files>
  <read_first>
    - docs/cheatsheet.vi.md (final verification)
    - docs/cheatsheet.md (comparison baseline)
  </read_first>
  <action>
Final verification that the translation is complete and structurally sound:

1. Line count comparison:
   - wc -l docs/cheatsheet.md (should be ~202)
   - wc -l docs/cheatsheet.vi.md (should be within ±10 lines)

2. Table integrity check - count pipe characters:
   - grep -c "^|" docs/cheatsheet.md
   - grep -c "^|" docs/cheatsheet.vi.md
   - Both should be similar (within ±5%)

3. Section presence check - verify all major sections exist:
   - Header comment present: head -5 docs/cheatsheet.vi.md | grep "Translated from"
   - Badges present: grep "img.shields.io" docs/cheatsheet.vi.md | wc -l (should be 2)
   - Table of Contents: grep -c "Mục Lục\|Table of Contents" docs/cheatsheet.vi.md
   - Command Count Summary: grep -c "Tổng Kết\|Command Count" docs/cheatsheet.vi.md
   - Footer: tail -5 docs/cheatsheet.vi.md | grep "Last updated\|Cập nhật"

4. Verify no broken markdown tables (each | should have matching closing | on same line)
  </action>
  <verify>
    <automated>
      # Section completeness check
      echo "Sections found:"
      grep -n "^##" docs/cheatsheet.vi.md | head -15
      echo "Total sections: $(grep -c "^##" docs/cheatsheet.vi.md)"
    </automated>
  </verify>
  <acceptance_criteria>
    - docs/cheatsheet.vi.md has HTML comment header in first 4 lines
    - File has exactly 2 language badge lines with img.shields.io
    - File has 10+ sections (## headers) matching source structure
    - grep -c "^##" docs/cheatsheet.vi.md equals grep -c "^##" docs/cheatsheet.md
    - File ends with footer containing date or "Cập nhật"
    - All markdown tables render correctly (no broken | syntax)
  </acceptance_criteria>
  <done>Translation verified complete: header present, badges correct, all sections translated, structure matches source, tables intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/cheatsheet.vi.md → user | Translated documentation crosses language boundary; technical accuracy must be preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Tampering | docs/cheatsheet.vi.md | accept | Documentation file; no executable content. Risk: low. |
| T-108-02 | Information Disclosure | Translation accuracy | mitigate | Verification task (Task 3) validates all commands/flags preserved in English. Explicit grep checks prevent accidental translation of technical terms. |
| T-108-03 | Denial of Service | Broken table formatting | mitigate | Task 4 verifies table structure integrity. Pipe character count validation ensures markdown tables render correctly. |
</threat_model>

<verification>
## Verification Commands

### Content Completeness
```bash
# Verify file exists with expected line count
wc -l docs/cheatsheet.vi.md docs/cheatsheet.md

# Compare table row counts
echo "English tables: $(grep -c "^|" docs/cheatsheet.md)"
echo "Vietnamese tables: $(grep -c "^|" docs/cheatsheet.vi.md)"
```

### Technical Accuracy
```bash
# Extract and compare commands (should be identical)
echo "=== Commands in English source ==="
grep -oE '/pd:[a-z-]+' docs/cheatsheet.md | sort -u | wc -l
echo "=== Commands in Vietnamese translation ==="
grep -oE '/pd:[a-z-]+' docs/cheatsheet.vi.md | sort -u | wc -l

# Extract and compare flags (should be identical)
echo "=== Flags in English source ==="
grep -oE '--[a-z-]+' docs/cheatsheet.md | sort -u | wc -l
echo "=== Flags in Vietnamese translation ==="
grep -oE '--[a-z-]+' docs/cheatsheet.vi.md | sort -u | wc -l
```

### Structure Integrity
```bash
# Verify section count matches
sections_en=$(grep -c "^##" docs/cheatsheet.md)
sections_vi=$(grep -c "^##" docs/cheatsheet.vi.md)
echo "English sections: $sections_en, Vietnamese sections: $sections_vi"

# Verify badges present
grep -c "img.shields.io/badge/lang" docs/cheatsheet.vi.md
grep -c "img.shields.io/badge/lang" docs/cheatsheet.md
```
</verification>

<success_criteria>
1. docs/cheatsheet.vi.md exists with ~195-210 lines (within 10% of source)
2. HTML comment header present in first 4 lines with source version tracking
3. Language switcher badges on both cheatsheet.md and cheatsheet.vi.md
4. All 16 /pd:* commands appear identically in both files
5. All 15 --* flags appear identically in both files
6. 5 command category sections translated (Lệnh Quản Lý Dự Án, Lệnh Lập Kế Hoạch, Lệnh Thực Thi, Lệnh Gỡ Lỗi, Lệnh Tiện Ích)
7. Popular Flags Reference table present with translated descriptions
8. Legend section present with universal notation symbols (translated explanations)
9. Command Count Summary table present with translated headers
10. No broken table formatting (all | characters balanced)
</success_criteria>

<output>
After completion, create `.planning/phases/108-i18n-03-command-cheat-sheet-ti-ng-vi-t/108-01-SUMMARY.md`

Summary must include:
- Translation completed: docs/cheatsheet.vi.md created
- Pattern consistency: Follows Phase 106-107 patterns (HTML header, badges, terminology)
- Command preservation: 16 commands kept in English
- Flag preservation: 15 flags kept in English
- Structure match: All 5 category tables + flags reference + legend + summary preserved
- Cross-links: Language switchers added to both English and Vietnamese files
</output>
