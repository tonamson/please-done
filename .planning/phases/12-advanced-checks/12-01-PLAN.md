---
phase: 12-advanced-checks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/plan-checker.js
  - references/plan-checker.md
autonomous: true
requirements: [ADV-01, ADV-02, ADV-03]

must_haves:
  truths:
    - "checkKeyLinks returns BLOCK when Key Link path not in any task Files"
    - "checkKeyLinks returns BLOCK when no task touches both ends simultaneously"
    - "checkKeyLinks returns PASS when no Key Links section exists or v1.0 format"
    - "checkScopeThresholds returns WARN when any of 4 dimensions exceed thresholds"
    - "checkScopeThresholds works with both v1.0 and v1.1 formats"
    - "checkEffortClassification detects underestimate and overestimate mismatches"
    - "checkEffortClassification returns PASS for v1.0 format (graceful)"
    - "runAllChecks returns 7 checks (4 existing + 3 new)"
  artifacts:
    - path: "bin/lib/plan-checker.js"
      provides: "3 new check functions + helpers"
      exports: "checkKeyLinks, checkScopeThresholds, checkEffortClassification, parseKeyLinksV11, normalizeKeyLinkPath, countFilesInString, computeActualEffort, detectMultiDomain"
    - path: "references/plan-checker.md"
      provides: "Rules spec updated with ADV-01, ADV-02, ADV-03 sections"
      contains: "ADV-01"
  key_links:
    - from: "bin/lib/plan-checker.js (runAllChecks)"
      to: "bin/lib/plan-checker.js (checkKeyLinks)"
      via: "checks array append"
      pattern: "checkKeyLinks"
    - from: "bin/lib/plan-checker.js (runAllChecks)"
      to: "bin/lib/plan-checker.js (checkScopeThresholds)"
      via: "checks array append"
      pattern: "checkScopeThresholds"
    - from: "bin/lib/plan-checker.js (runAllChecks)"
      to: "bin/lib/plan-checker.js (checkEffortClassification)"
      via: "checks array append"
      pattern: "checkEffortClassification"
---

<objective>
Implement 3 advanced check functions (ADV-01, ADV-02, ADV-03) in plan-checker.js and update the rules spec.

Purpose: Extend plan checker with Key Links verification, scope threshold warnings, and effort classification validation to catch more plan quality issues before execution.

Output: Extended plan-checker.js with 7 total checks (4 existing + 3 new) and updated references/plan-checker.md with 3 new rule sections.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-advanced-checks/12-CONTEXT.md
@.planning/phases/12-advanced-checks/12-RESEARCH.md
@.planning/phases/11-workflow-integration/11-01-SUMMARY.md
@bin/lib/plan-checker.js
@references/plan-checker.md
@references/conventions.md
@templates/plan.md

<interfaces>
<!-- Key types and contracts the executor needs from existing plan-checker.js -->

Check function signature pattern:
```javascript
function checkXxx(planContent, tasksContent) {
  const result = { checkId: 'ADV-XX', status: 'pass', issues: [] };
  // ... logic
  result.status = result.issues.length > 0 ? 'block' : 'pass';  // or 'warn' for WARN severity
  return result;
}
```

Issue object format:
```javascript
{ message: 'string', location: 'string', fixHint: 'string' }
```

Existing helpers available (already in plan-checker.js):
- `detectPlanFormat(planContent)` -> 'v1.0' | 'v1.1' | 'unknown'
- `parseTasksV10(planContent)` -> array of { id, name, hasFiles, hasDescription, hasCriteria }
- `parseTaskDetailBlocksV11(tasksContent)` -> array of { id, effort, files, truths, hasDescription, hasCriteria, ... }
- `parseTruthsV11(planContent)` -> array of { id, description }
- `parseMustHavesTruths(rawFrontmatterString)` -> array of { id, description }
- `parseTruthRefs(truthsStr)` -> array of 'T1', 'T2', etc.
- `parseTaskDepsV11(tasksContent)` -> { nodes, edges }

From utils.js (already imported):
- `parseFrontmatter(content)` -> flattened key-value object (CANNOT parse nested key_links)
- `extractXmlSection(content, tag)` -> string content of XML tag

runAllChecks signature:
```javascript
function runAllChecks({ planContent, tasksContent, requirementIds }) {
  const checks = [
    checkRequirementCoverage(planContent, requirementIds),
    checkTaskCompleteness(planContent, tasksContent),
    checkDependencyCorrectness(planContent, tasksContent),
    checkTruthTaskCoverage(planContent, tasksContent),
    // New checks append here
  ];
  const hasBlock = checks.some(c => c.status === 'block');
  const hasWarn = checks.some(c => c.status === 'warn');
  const overall = hasBlock ? 'block' : hasWarn ? 'warn' : 'pass';
  return { overall, checks };
}
```

Effort classification table from references/conventions.md:
| Signal | simple | standard | complex |
|--------|--------|----------|---------|
| Files | 1-2 | 3-4 | 5+ |
| Truths | 1 | 2-3 | 4+ |
| Dependencies | 0 | 1-2 | 3+ |
| Multi-domain | no | no | yes |

Key Links table format from templates/plan.md (v1.1):
```markdown
### Lien ket then chot (Key Links)
| Tu | Den | Mo ta |
|----|-----|-------|
| auth.controller | auth.service | Controller goi service.login() |
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 3 check functions + helpers to plan-checker.js</name>
  <files>bin/lib/plan-checker.js</files>
  <read_first>
    - bin/lib/plan-checker.js (current full source — 643 lines, 4 checks + 12 helpers + runAllChecks + exports)
    - references/conventions.md (effort classification table at lines 82-88: simple/standard/complex thresholds)
    - templates/plan.md (Key Links table format at lines 153-157: "| Tu | Den | Mo ta |" headers)
    - .planning/phases/12-advanced-checks/12-CONTEXT.md (locked decisions D-01 through D-13)
    - .planning/phases/12-advanced-checks/12-RESEARCH.md (parsing examples, algorithm details)
  </read_first>
  <behavior>
    - parseKeyLinksV11('') returns []
    - parseKeyLinksV11(plan with Key Links table) returns array of { from, to, description }
    - parseKeyLinksV11(plan with diacritics header "Lien ket then chot") returns correct links
    - parseKeyLinksV11(plan without Key Links section) returns []
    - normalizeKeyLinkPath('workflows/plan.md (Step 8.1)') returns 'workflows/plan.md'
    - normalizeKeyLinkPath('bin/lib/plan-checker.js') returns 'bin/lib/plan-checker.js'
    - countFilesInString('src/a.js, src/b.js') returns 2
    - countFilesInString('src/a.js\nsrc/b.js') returns 2
    - countFilesInString(null) returns 0
    - detectMultiDomain('bin/lib/checker.js, references/spec.md') returns true
    - detectMultiDomain('bin/lib/checker.js, bin/lib/utils.js') returns false
    - computeActualEffort with 1 file, 1 truth, 0 deps, single domain returns 'simple'
    - computeActualEffort with 5 files returns 'complex' (highest signal wins per D-08)
    - checkKeyLinks returns PASS for v1.0 format (D-12)
    - checkKeyLinks returns PASS when no Key Links section exists
    - checkKeyLinks returns BLOCK when from path not in any task Files (D-01, D-04)
    - checkKeyLinks returns BLOCK when no task touches both ends (D-02)
    - checkScopeThresholds returns PASS when all dimensions within thresholds
    - checkScopeThresholds returns WARN when >6 tasks (D-05)
    - checkScopeThresholds returns WARN when >7 files per task (D-05)
    - checkScopeThresholds returns WARN when >25 total files (D-05)
    - checkScopeThresholds returns WARN when >6 truths (D-05)
    - checkScopeThresholds works with v1.0 format (D-13)
    - checkEffortClassification returns PASS for v1.0 format (D-12)
    - checkEffortClassification detects underestimate: labeled simple, actual complex (D-07)
    - checkEffortClassification detects overestimate: labeled complex, actual simple (D-07)
    - checkEffortClassification returns WARN severity, not BLOCK (D-11)
    - runAllChecks returns 7 checks total
  </behavior>
  <action>
Add the following new functions to bin/lib/plan-checker.js, inserting them AFTER the existing helper functions (after `parseTaskDepsV11` at line ~295) and BEFORE `detectPlanFormat` (line ~305):

**New helper functions:**

1. `normalizeKeyLinkPath(rawPath)` — Strip parenthetical suffixes from Key Link paths:
```javascript
function normalizeKeyLinkPath(rawPath) {
  return rawPath.replace(/\s*\(.*?\)\s*$/, '').trim();
}
```

2. `parseKeyLinksV11(planContent)` — Parse Key Links table from v1.1 PLAN.md body:
   - Find section heading matching `/###\s*Li[eê]n\s*k[eế]t\s*then\s*ch[oố]t/i` (handle diacritics)
   - Extract markdown table rows after the header row and separator row
   - For each data row `| from | to | description |`, return `{ from, to, description }`
   - Skip separator rows (starting with `-`)
   - If no section found, return `[]`

3. `countFilesInString(filesStr)` — Count files from a comma/newline-separated string:
   - If null/undefined/empty, return 0
   - Split by `/[,\n]/`, trim each item, filter empty strings
   - Return count of non-empty entries

4. `detectMultiDomain(filesStr)` — Per D-09, check if files span multiple top-level directories:
   - Split files by comma, trim
   - Extract top-level directory from each path (substring before first `/`)
   - Return `true` if 2+ different top-level directories found

5. `computeActualEffort(task, tasksContent)` — Per D-08, compute effort from 4 signals:
   - `fileCount` = `countFilesInString(task.files)` — simple: 1-2, standard: 3-4, complex: 5+
   - `truthCount` = `task.truths.length` — simple: <=1, standard: 2-3, complex: 4+
   - `depCount` = count `Task N` references in the task's dependency metadata from tasksContent (match the task section, find `Phu thuoc:` line, count `Task \d+` refs; "Khong" or missing = 0)
   - `isMultiDomain` = `detectMultiDomain(task.files)` — true = complex
   - Map each signal to effort level, take highest (conservative per D-08):
     ```javascript
     const levels = { simple: 0, standard: 1, complex: 2 };
     const max = Math.max(levels[fileEffort], levels[truthEffort], levels[depEffort], levels[domainEffort]);
     return max === 2 ? 'complex' : max === 1 ? 'standard' : 'simple';
     ```

**New check functions (add AFTER `checkTruthTaskCoverage`, BEFORE `runAllChecks`):**

6. `checkKeyLinks(planContent, tasksContent)` — ADV-01 per D-01, D-02, D-03, D-04, D-12:
   - `checkId: 'ADV-01'`
   - v1.0/unknown format: graceful PASS (D-12)
   - Parse Key Links via `parseKeyLinksV11(planContent)`
   - If no links found: PASS (plan may not have Key Links)
   - For v1.1 with tasksContent: parse tasks via `parseTaskDetailBlocksV11(tasksContent)`
   - For v1.1 without tasksContent: parse tasks via `parseTasksV10(planContent)` for file extraction from `<files>` tags
   - For each link:
     - Normalize both `from` and `to` paths via `normalizeKeyLinkPath()`
     - Check `from` path appears in at least 1 task's `files` field (substring containment)
     - Check `to` path appears in at least 1 task's `files` field (substring containment)
     - Check at least 1 task touches BOTH `from` AND `to` simultaneously (D-02)
     - Each failure produces an issue with `fixHint` suggesting what to add
   - Status: `result.issues.length > 0 ? 'block' : 'pass'` (D-04: BLOCK severity)

7. `checkScopeThresholds(planContent, tasksContent)` — ADV-02 per D-05, D-06, D-13:
   - `checkId: 'ADV-02'`
   - Works with BOTH v1.0 and v1.1 (D-13 — unlike other checks, no graceful PASS for v1.0)
   - Only unknown format gets graceful PASS
   - For v1.0: use `parseTasksV10(planContent)` for task count, parse `<files>` tag content for file counts, use `parseMustHavesTruths()` for truth count
   - For v1.1: use `parseTaskDetailBlocksV11(tasksContent)` for task count + file counts, use `parseTruthsV11(planContent)` for truth count
   - 4 threshold checks (D-05):
     - Tasks per plan > 6: WARN with message `"Plan co ${count} tasks (threshold: 6)"` and fixHint `"Chia plan thanh nhieu plans nho hon"`
     - Files per task > 7 (for each task): WARN with message `"Task ${id} co ${count} files (threshold: 7)"` and fixHint `"Chia task thanh nhieu tasks nho hon"`
     - Total unique files per plan > 25: WARN with message `"Plan co ${count} unique files (threshold: 25)"` and fixHint `"Chia plan thanh nhieu plans nho hon"`
     - Truths per plan > 6: WARN with message `"Plan co ${count} Truths (threshold: 6)"` and fixHint `"Giam so Truths hoac chia plan"`
   - Status: `result.issues.length > 0 ? 'warn' : 'pass'` (D-06: WARN severity)

8. `checkEffortClassification(planContent, tasksContent)` — ADV-03 per D-07, D-08, D-09, D-10, D-11, D-12:
   - `checkId: 'ADV-03'`
   - v1.0/unknown format: graceful PASS (D-12 — no Effort field in v1.0)
   - If no tasksContent: PASS
   - Parse tasks via `parseTaskDetailBlocksV11(tasksContent)`
   - For each task with an effort field:
     - Compute actual effort via `computeActualEffort(task, tasksContent)`
     - Compare labeled effort (lowercase) with computed actual
     - If mismatch: determine direction — `levels[labeled] < levels[actual]` means underestimate, otherwise overestimate (D-07)
     - Issue message: `"Task ${task.id} effort \"${labeled}\" co the la ${direction} (signals cho thay \"${actualEffort}\")"`
     - Issue fixHint: `"Xem xet doi Effort thanh \"${actualEffort}\" hoac giu nguyen neu co ly do"`
   - Status: `result.issues.length > 0 ? 'warn' : 'pass'` (D-11: WARN severity)

**Update `runAllChecks()`:**

Append 3 new check calls to the `checks` array:
```javascript
checkKeyLinks(planContent, tasksContent),
checkScopeThresholds(planContent, tasksContent),
checkEffortClassification(planContent, tasksContent),
```

**Update `module.exports`:**

Add to exports object:
```javascript
// New ADV-* check functions
checkKeyLinks,
checkScopeThresholds,
checkEffortClassification,
// New helpers (exported for unit testing)
parseKeyLinksV11,
normalizeKeyLinkPath,
countFilesInString,
detectMultiDomain,
computeActualEffort,
```

IMPORTANT: Keep all functions pure — no `fs.readFileSync()` or any file I/O. Content strings passed as arguments only.
  </action>
  <verify>
    <automated>node -e "const pc = require('./bin/lib/plan-checker'); const fns = ['checkKeyLinks','checkScopeThresholds','checkEffortClassification','parseKeyLinksV11','normalizeKeyLinkPath','countFilesInString','detectMultiDomain','computeActualEffort']; fns.forEach(f => { if (typeof pc[f] !== 'function') throw new Error(f + ' not exported'); }); const r = pc.runAllChecks({ planContent: 'test', tasksContent: null, requirementIds: [] }); if (r.checks.length !== 7) throw new Error('Expected 7 checks, got ' + r.checks.length); console.log('PASS: all 8 functions exported, runAllChecks returns 7 checks');"</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/plan-checker.js exports `checkKeyLinks` as a function
    - bin/lib/plan-checker.js exports `checkScopeThresholds` as a function
    - bin/lib/plan-checker.js exports `checkEffortClassification` as a function
    - bin/lib/plan-checker.js exports `parseKeyLinksV11` as a function
    - bin/lib/plan-checker.js exports `normalizeKeyLinkPath` as a function
    - bin/lib/plan-checker.js exports `countFilesInString` as a function
    - bin/lib/plan-checker.js exports `detectMultiDomain` as a function
    - bin/lib/plan-checker.js exports `computeActualEffort` as a function
    - `runAllChecks({ planContent: 'test', tasksContent: null, requirementIds: [] })` returns object with `checks.length === 7`
    - `checkKeyLinks('test', null)` returns `{ checkId: 'ADV-01', status: 'pass', issues: [] }`
    - `checkScopeThresholds('test', null)` returns `{ checkId: 'ADV-02', status: 'pass', issues: [] }`
    - `checkEffortClassification('test', null)` returns `{ checkId: 'ADV-03', status: 'pass', issues: [] }`
    - No `require('fs')` or `require('path')` added to plan-checker.js (pure functions only)
  </acceptance_criteria>
  <done>All 3 check functions + 5 helpers implemented, exported, and runAllChecks returns 7 checks. Functions follow existing check pattern (pure, no I/O).</done>
</task>

<task type="auto">
  <name>Task 2: Update references/plan-checker.md with 3 new rule sections</name>
  <files>references/plan-checker.md</files>
  <read_first>
    - references/plan-checker.md (current rules spec — 200 lines, sections 1-7, covers CHECK-01 through CHECK-04)
    - .planning/phases/12-advanced-checks/12-CONTEXT.md (locked decisions D-01 through D-13 for exact rules)
    - .planning/phases/12-advanced-checks/12-RESEARCH.md (parsing details, threshold values)
  </read_first>
  <action>
Add 3 new sections to references/plan-checker.md AFTER section "5. CHECK-04" and BEFORE section "6. Result Format". Renumber existing "6. Result Format" to "9. Result Format" and "7. Severity Summary Table" to "10. Severity Summary Table". Add the following content:

**Section 6. ADV-01: Key Links Verification**

```markdown
## 6. ADV-01: Key Links Verification

> Per D-01, D-02, D-03, D-04, D-12 tu Phase 12

**Input:** PLAN.md content + TASKS.md content

**Rule:** Key Links trong PLAN.md phai duoc phan anh trong task Files. Ca 2 dau (from + to) phai co task touch, va it nhat 1 task phai touch ca 2 dau cung luc.

**Logic:**
1. Detect format — v1.0/unknown: graceful PASS (D-12)
2. Parse Key Links tu v1.1 PLAN.md body table (section "Lien ket then chot")
3. Neu khong co Key Links section: PASS (plan co the khong co Key Links)
4. Normalize paths: strip parenthetical suffixes VD "plan.md (Step 8.1)" -> "plan.md"
5. Voi moi Key Link:
   a. Check `from` path xuat hien trong Files cua it nhat 1 task (substring containment)
   b. Check `to` path xuat hien trong Files cua it nhat 1 task (substring containment)
   c. Check it nhat 1 task co CA HAI `from` va `to` trong Files (D-02 — dam bao integration thuc su)
6. Bat ky violation nao -> BLOCK (D-04)

**Severity:**
| Dieu kien | Severity |
|-----------|----------|
| Key Link path khong co trong bat ky task Files nao | BLOCK |
| Khong co task nao touch ca 2 dau cung luc | BLOCK |
| Khong co Key Links section | PASS (skip) |
| v1.0/unknown format | PASS (graceful) |
```

**Section 7. ADV-02: Scope Threshold Warnings**

```markdown
## 7. ADV-02: Scope Threshold Warnings

> Per D-05, D-06, D-13 tu Phase 12

**Input:** PLAN.md content + TASKS.md content

**Rule:** Canh bao khi plan vuot scope thresholds hop ly tren 4 dimensions.

**Logic:**
1. Parse tasks tu v1.0 (parseTasksV10) hoac v1.1 (parseTaskDetailBlocksV11)
2. Unknown format: graceful PASS
3. v1.0 va v1.1 deu duoc check (D-13)
4. Check 4 dimensions:
   a. Tasks per plan > 6: WARN
   b. Files per task > 7 (cho tung task): WARN
   c. Total unique files per plan > 25: WARN
   d. Truths per plan > 6: WARN (v1.0 dung parseMustHavesTruths, v1.1 dung parseTruthsV11)

**Severity:**
| Dieu kien | Severity |
|-----------|----------|
| Bat ky dimension nao vuot threshold | WARN |
| Tat ca dimensions trong threshold | PASS |
| Unknown format | PASS (graceful) |
```

**Section 8. ADV-03: Effort Classification Validation**

```markdown
## 8. ADV-03: Effort Classification Validation

> Per D-07, D-08, D-09, D-10, D-11, D-12 tu Phase 12

**Input:** PLAN.md content + TASKS.md content

**Rule:** Effort classification (simple/standard/complex) phai khop voi scope thuc te cua task. Canh bao mismatch ca 2 chieu (underestimate va overestimate).

**Logic:**
1. Detect format — v1.0/unknown: graceful PASS (D-12 — v1.0 khong co Effort field)
2. Parse tasks tu TASKS.md (parseTaskDetailBlocksV11)
3. Voi moi task co Effort field:
   a. Tinh actual effort tu 4 signals (D-08):
      - Files: 1-2 = simple, 3-4 = standard, 5+ = complex
      - Truths: 1 = simple, 2-3 = standard, 4+ = complex
      - Dependencies: 0 = simple, 1-2 = standard, 3+ = complex
      - Multi-domain: files span 2+ top-level directories = complex (D-09)
   b. Lay signal cao nhat (conservative — D-08)
   c. So sanh labeled effort voi actual effort
   d. Mismatch -> WARN voi direction (underestimate/overestimate)
4. fixHint: de nghi doi Effort hoac giu nguyen neu planner co ly do (D-10)

**Multi-domain detection (D-09):**
- Tach files theo dau phay, lay top-level directory cua moi file (phan truoc dau `/` dau tien)
- 2+ top-level directories khac nhau = multi-domain

**Severity:**
| Dieu kien | Severity |
|-----------|----------|
| Effort mismatch (ca 2 chieu) | WARN |
| v1.0/unknown format | PASS (graceful) |
| Task khong co Effort field | skip (da xu ly boi CHECK-02) |
```

**Update Severity Summary Table (section 10):**

Add these rows to the existing severity table:
```markdown
| ADV-01 | Key Link path khong co trong bat ky task Files nao | BLOCK |
| ADV-01 | Khong co task nao touch ca 2 dau cung luc | BLOCK |
| ADV-01 | Khong co Key Links section | PASS (skip) |
| ADV-01 | v1.0/unknown format | PASS (graceful) |
| ADV-02 | Bat ky dimension nao vuot threshold | WARN |
| ADV-02 | Tat ca dimensions trong threshold | PASS |
| ADV-02 | Unknown format | PASS (graceful) |
| ADV-03 | Effort mismatch (ca 2 chieu) | WARN |
| ADV-03 | v1.0/unknown format | PASS (graceful) |
```

**Update Result Format section:**

Update the combined result example to show 7 checks instead of 4:
```json
{
  "overall": "pass|block|warn",
  "checks": [
    { "checkId": "CHECK-01", "status": "pass", "issues": [] },
    { "checkId": "CHECK-02", "status": "pass", "issues": [] },
    { "checkId": "CHECK-03", "status": "pass", "issues": [] },
    { "checkId": "CHECK-04", "status": "pass", "issues": [] },
    { "checkId": "ADV-01", "status": "pass", "issues": [] },
    { "checkId": "ADV-02", "status": "pass", "issues": [] },
    { "checkId": "ADV-03", "status": "pass", "issues": [] }
  ]
}
```

Update the version footer to: `*Plan Checker Rules v1.1*` and `*Updated: 23_03_2026*`
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('references/plan-checker.md', 'utf8'); const checks = ['ADV-01', 'ADV-02', 'ADV-03']; checks.forEach(id => { if (!c.includes(id)) throw new Error(id + ' not found'); }); if (!c.includes('Key Links Verification')) throw new Error('Missing ADV-01 section'); if (!c.includes('Scope Threshold')) throw new Error('Missing ADV-02 section'); if (!c.includes('Effort Classification')) throw new Error('Missing ADV-03 section'); if (!c.includes('v1.1')) throw new Error('Missing version update'); console.log('PASS: all 3 ADV sections present in rules spec');"</automated>
  </verify>
  <acceptance_criteria>
    - references/plan-checker.md contains "## 6. ADV-01: Key Links Verification"
    - references/plan-checker.md contains "## 7. ADV-02: Scope Threshold Warnings"
    - references/plan-checker.md contains "## 8. ADV-03: Effort Classification Validation"
    - references/plan-checker.md contains "ADV-01" in severity summary table
    - references/plan-checker.md contains "ADV-02" in severity summary table
    - references/plan-checker.md contains "ADV-03" in severity summary table
    - references/plan-checker.md result format example shows 7 checks
    - references/plan-checker.md version footer says "v1.1"
  </acceptance_criteria>
  <done>Rules spec updated with 3 new check rule sections, severity table extended, result format showing 7 checks, version bumped to v1.1.</done>
</task>

</tasks>

<verification>
1. `node -e "const pc = require('./bin/lib/plan-checker'); console.log(Object.keys(pc).length + ' exports');"` — should show increased export count (was 18, now 26)
2. `node -e "const pc = require('./bin/lib/plan-checker'); const r = pc.runAllChecks({ planContent: 'test', tasksContent: null, requirementIds: [] }); console.log(r.checks.map(c => c.checkId));"` — should show ['CHECK-01', 'CHECK-02', 'CHECK-03', 'CHECK-04', 'ADV-01', 'ADV-02', 'ADV-03']
3. `grep -c 'ADV-0' references/plan-checker.md` — should return count > 0
</verification>

<success_criteria>
- plan-checker.js has 7 check functions (4 existing + 3 new)
- All 3 new checks follow pure function pattern (no I/O)
- All 3 new checks return correct checkId and proper severity (ADV-01: block, ADV-02: warn, ADV-03: warn)
- v1.0 format gets graceful PASS for ADV-01 and ADV-03 (D-12)
- ADV-02 works with v1.0 format (D-13)
- Rules spec documents all 3 new checks with severity tables
- runAllChecks returns 7 checks in the array
</success_criteria>

<output>
After completion, create `.planning/phases/12-advanced-checks/12-01-SUMMARY.md`
</output>
