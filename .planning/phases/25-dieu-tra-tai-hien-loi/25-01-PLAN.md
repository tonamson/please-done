---
phase: 25-dieu-tra-tai-hien-loi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/truths-parser.js
  - bin/lib/generate-diagrams.js
  - test/smoke-truths-parser.test.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "parseTruthsFromContent() la shared helper export tu bin/lib/truths-parser.js"
    - "generate-diagrams.js import parseTruthsFromContent tu truths-parser.js thay vi dinh nghia inline"
    - "Tat ca tests hien tai (bao gom smoke-generate-diagrams.test.js) van pass sau refactor"
  artifacts:
    - path: "bin/lib/truths-parser.js"
      provides: "Shared helper parse Truths table"
      exports: ["parseTruthsFromContent"]
    - path: "test/smoke-truths-parser.test.js"
      provides: "Unit tests cho truths-parser"
      min_lines: 30
  key_links:
    - from: "bin/lib/generate-diagrams.js"
      to: "bin/lib/truths-parser.js"
      via: "require('./truths-parser')"
      pattern: "require\\('./truths-parser'\\)"
---

<objective>
Tao shared helper truths-parser.js (D-07) va refactor generate-diagrams.js de import tu helper moi (D-08).

Purpose: DRY — hien tai parseTruthsFromContent() inline trong generate-diagrams.js (dong 34-45). Tach ra shared helper de repro-test-generator va cac module khac co the import.
Output: bin/lib/truths-parser.js + test file + generate-diagrams.js refactored
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@bin/lib/generate-diagrams.js
@bin/lib/mermaid-validator.js
@test/smoke-generate-diagrams.test.js
@test/smoke-mermaid-validator.test.js

<interfaces>
<!-- Key types va contracts executor can. Extract tu codebase. -->

Tu bin/lib/generate-diagrams.js dong 34-45 (SE BI XOA va move sang truths-parser.js):
```javascript
function parseTruthsFromContent(content) {
  const truths = [];
  const tableRegex = /\|\s*(T\d+)\s*\|\s*([^|\n]+)\s*\|(?:\s*[^|\n]+\s*\|)+/g;
  let match;
  while ((match = tableRegex.exec(content)) !== null) {
    truths.push({
      id: match[1].trim(),
      description: match[2].trim(),
    });
  }
  return truths;
}
```

Tu bin/lib/generate-diagrams.js dong 11 (import pattern):
```javascript
const { mermaidValidator } = require('./mermaid-validator');
const { parseFrontmatter } = require('./utils');
```

Tu test/smoke-mermaid-validator.test.js (test pattern):
```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const { mermaidValidator } = require('../bin/lib/mermaid-validator');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tao bin/lib/truths-parser.js va test/smoke-truths-parser.test.js</name>
  <files>bin/lib/truths-parser.js, test/smoke-truths-parser.test.js</files>
  <read_first>
    - bin/lib/generate-diagrams.js (dong 34-45: parseTruthsFromContent hien tai)
    - bin/lib/mermaid-validator.js (pattern module: JSDoc header, 'use strict', module.exports)
    - test/smoke-mermaid-validator.test.js (pattern test: node:test, node:assert/strict, makeHelper)
  </read_first>
  <behavior>
    - parseTruthsFromContent('') tra ve mang rong []
    - parseTruthsFromContent voi 3-col table (id, desc, col3) tra ve [{id: 'T1', description: 'Mo ta'}]
    - parseTruthsFromContent voi 5-col table (v1.3 format) tra ve [{id: 'T1', description: 'Mo ta'}]
    - parseTruthsFromContent voi content KHONG co Truths table tra ve []
    - parseTruthsFromContent voi nhieu Truths (T1-T5) tra ve 5 items dung thu tu
    - parseTruthsFromContent voi whitespace trong cell van trim dung
  </behavior>
  <action>
    **Buoc 1 — Tao truths-parser.js:**

    Tao `bin/lib/truths-parser.js` voi noi dung:
    ```javascript
    /**
     * Truths Parser — Parse Truths table tu plan body content.
     *
     * Shared helper — duoc import boi generate-diagrams.js va cac module khac.
     * KHONG doc file — tat ca content truyen qua tham so.
     */

    'use strict';

    /**
     * Parse Truths table tu plan body content.
     * Hoat dong voi ca 3-col (v1.1) va 5-col (v1.3) Truths tables.
     * @param {string} content - Plan body content (sau frontmatter)
     * @returns {Array<{id: string, description: string}>}
     */
    function parseTruthsFromContent(content) {
      const truths = [];
      const tableRegex = /\|\s*(T\d+)\s*\|\s*([^|\n]+)\s*\|(?:\s*[^|\n]+\s*\|)+/g;
      let match;
      while ((match = tableRegex.exec(content)) !== null) {
        truths.push({
          id: match[1].trim(),
          description: match[2].trim(),
        });
      }
      return truths;
    }

    module.exports = { parseTruthsFromContent };
    ```

    Regex giong het dong 36 trong generate-diagrams.js — KHONG thay doi logic.

    **Buoc 2 — Tao test file:**

    Tao `test/smoke-truths-parser.test.js` theo pattern smoke-mermaid-validator.test.js:
    - `'use strict'`
    - `require('node:test')`, `require('node:assert/strict')`
    - `require('../bin/lib/truths-parser')`
    - Helper `makeTable()` tao markdown table content in-memory
    - Tests cho 6 behaviors trong <behavior> block
    - Moi test dung `assert.equal` hoac `assert.deepStrictEqual`

    **Buoc 3 — RED:** Chay test TRUOC khi module ton tai (verify test fail).
    **Buoc 4 — GREEN:** Tao module → chay test → phai pass.
  </action>
  <verify>
    <automated>node --test test/smoke-truths-parser.test.js</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/truths-parser.js ton tai va co `module.exports = { parseTruthsFromContent }`
    - bin/lib/truths-parser.js co `// KHONG doc file` trong JSDoc header
    - bin/lib/truths-parser.js co `'use strict'` o dong 8 (sau JSDoc)
    - bin/lib/truths-parser.js KHONG co `require('fs')`
    - test/smoke-truths-parser.test.js ton tai va co it nhat 6 it() blocks
    - `node --test test/smoke-truths-parser.test.js` exit code 0
  </acceptance_criteria>
  <done>parseTruthsFromContent() exported tu truths-parser.js, 6+ tests pass, module tuan thu pure function pattern</done>
</task>

<task type="auto">
  <name>Task 2: Refactor generate-diagrams.js import tu truths-parser.js</name>
  <files>bin/lib/generate-diagrams.js</files>
  <read_first>
    - bin/lib/generate-diagrams.js (toan bo file — can thay parseTruthsFromContent inline bang import)
    - bin/lib/truths-parser.js (module moi tu Task 1 — verify export name)
    - test/smoke-generate-diagrams.test.js (tests hien tai — phai van pass)
  </read_first>
  <action>
    **3 buoc atomic (theo Pitfall 4 trong RESEARCH.md):**

    **Buoc 1:** Trong `bin/lib/generate-diagrams.js`, them import SAU dong 11 (require utils):
    ```javascript
    const { parseTruthsFromContent } = require('./truths-parser');
    ```

    **Buoc 2:** Xoa function `parseTruthsFromContent` inline (dong 28-45 — tu JSDoc comment `Parse Truths from plan body content` den closing brace).

    Cu the xoa tu:
    ```javascript
    /**
     * Parse Truths from plan body content.
     * Inline regex from plan-checker.js parseTruthsV11 — do NOT require plan-checker to avoid circular deps.
     * Works for both 3-col (v1.1) and 5-col (v1.3) Truths tables.
     * @param {string} content - Plan body content (after frontmatter)
     * @returns {Array<{id: string, description: string}>}
     */
    function parseTruthsFromContent(content) {
      const truths = [];
      const tableRegex = /\|\s*(T\d+)\s*\|\s*([^|\n]+)\s*\|(?:\s*[^|\n]+\s*\|)+/g;
      let match;
      while ((match = tableRegex.exec(content)) !== null) {
        truths.push({
          id: match[1].trim(),
          description: match[2].trim(),
        });
      }
      return truths;
    }
    ```

    **Buoc 3:** Chay `node --test test/smoke-generate-diagrams.test.js` → PHAI pass.

    Sau do chay `npm test` de verify toan bo 526 tests van pass.

    LUU Y: D-09 quy dinh plan-checker.js GIU internal parser rieng — KHONG sua plan-checker.js.
  </action>
  <verify>
    <automated>node --test test/smoke-generate-diagrams.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/generate-diagrams.js co `require('./truths-parser')` trong 15 dong dau
    - bin/lib/generate-diagrams.js KHONG co `function parseTruthsFromContent` (inline da bi xoa)
    - bin/lib/generate-diagrams.js KHONG co `const tableRegex` (regex da move sang truths-parser)
    - `node --test test/smoke-generate-diagrams.test.js` exit code 0
    - `npm test` exit code 0 (toan bo 526+ tests pass)
  </acceptance_criteria>
  <done>generate-diagrams.js import parseTruthsFromContent tu truths-parser.js, inline function da xoa, tat ca tests pass</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-truths-parser.test.js` — truths-parser unit tests pass
- `node --test test/smoke-generate-diagrams.test.js` — generate-diagrams regression tests pass
- `npm test` — full 526+ tests pass
- `grep -c "require.*truths-parser" bin/lib/generate-diagrams.js` tra ve 1
- `grep -c "function parseTruthsFromContent" bin/lib/generate-diagrams.js` tra ve 0
</verification>

<success_criteria>
- truths-parser.js la pure function module theo dung pattern (JSDoc, 'use strict', KHONG doc file)
- generate-diagrams.js import tu truths-parser.js thay vi inline
- Tat ca tests pass (526+ tests bao gom test moi)
</success_criteria>

<output>
After completion, create `.planning/phases/25-dieu-tra-tai-hien-loi/25-01-SUMMARY.md`
</output>
