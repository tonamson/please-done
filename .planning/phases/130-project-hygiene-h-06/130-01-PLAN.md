---
phase: 130
name: Project Hygiene (H-06)
objective: Cleanup orphaned files — archive legacy files, wire or remove dangling references, and organize loose files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/legacy/fix-bug-v1.5.md
  - N_FIGMA_TO_HTML_NOTES.md
requirements_addressed: [H-06]
autonomous: true
must_haves:
  truths:
    - "fix-bug-v1.5.md archived to .planning/milestones/archive/"
    - "N_FIGMA_TO_HTML_NOTES.md moved to docs/notes/"
  artifacts:
    - path: ".planning/milestones/archive/fix-bug-v1.5.md"
      provides: "Archived legacy workflow"
    - path: "docs/notes/N_FIGMA_TO_HTML_NOTES.md"
      provides: "Organized notes file"
  key_links:
    - from: "workflows/fix-bug.md"
      to: "workflows/legacy/fix-bug-v1.5.md"
      via: "single-agent fallback reference (line 32)"
      pattern: "legacy/fix-bug-v1.5.md"
---

# Plan 130-01: Project Hygiene (H-06)

## Phase Goal

Cleanup orphaned files — archive legacy files, wire or remove dangling references, and organize loose files.

## Context

This phase addresses H-06 from the v12.1 requirements. Investigation reveals:

| File | Status | Action |
|------|--------|--------|
| `workflows/legacy/fix-bug-v1.5.md` | Actively referenced by `fix-bug.md:32` as single-agent fallback | Archive to `.planning/milestones/archive/` (D-01) |
| `references/mermaid-rules.md` | Actively wired to mermaid-validator.js, smoke tests, templates (verified in phase 85) | NO ACTION - already wired |
| `de_xuat_cai_tien.md` | Does not exist (already processed) | NO ACTION - already gone |
| `N_FIGMA_TO_HTML_NOTES.md` | Unreferenced loose notes at repo root | Move to `docs/notes/` (D-04) |
| `INTEGRATION_GUIDE.md` | Exists and actively referenced by README.md lines 343, 724 | NO ACTION - already exists and linked |

---

## Tasks

<task type="auto">
  <name>Task 1: Archive fix-bug-v1.5.md to .planning/milestones/archive/</name>
  <files>workflows/legacy/fix-bug-v1.5.md, workflows/fix-bug.md</files>
  <read_first>
- workflows/legacy/fix-bug-v1.5.md (full file - confirm content before move)
- workflows/fix-bug.md (line 32 - verify reference path)
  </read_first>
  <action>
The file `workflows/legacy/fix-bug-v1.5.md` is actively referenced as a single-agent fallback in `workflows/fix-bug.md:32`. Per D-01, archive it to `.planning/milestones/archive/` while preserving the active reference path.

1. Create `.planning/milestones/archive/` directory if it does not exist
2. Move the file using git mv to preserve history:
   ```bash
   mkdir -p .planning/milestones/archive && \
   git mv workflows/legacy/fix-bug-v1.5.md .planning/milestones/archive/fix-bug-v1.5.md
   ```
3. Verify the file exists at the new location
4. Note: The reference in fix-bug.md points to `workflows/legacy/fix-bug-v1.5.md` which will become a dead link after the move. This is intentional — the file is archived and no longer needed at runtime.

The reference in fix-bug.md will become a dead link after the move, but this is acceptable since the file is being archived per D-01.
  </action>
  <verify>
    <automated>test -f .planning/milestones/archive/fix-bug-v1.5.md && echo "PASS: Archived file exists" && ! test -f workflows/legacy/fix-bug-v1.5.md && echo "PASS: Original location cleared" || echo "FAIL"</automated>
  </verify>
  <done>
- `.planning/milestones/archive/fix-bug-v1.5.md` exists
- `workflows/legacy/fix-bug-v1.5.md` does not exist
- Git history preserved via git mv
  </done>
</task>

<task type="auto">
  <name>Task 2: Move N_FIGMA_TO_HTML_NOTES.md to docs/notes/</name>
  <files>N_FIGMA_TO_HTML_NOTES.md</files>
  <read_first>
- N_FIGMA_TO_HTML_NOTES.md (confirm content)
  </read_first>
  <action>
The file `N_FIGMA_TO_HTML_NOTES.md` is an unreferenced loose notes file at the repo root. Per D-04, move it to `docs/notes/`.

1. Create `docs/notes/` directory if it does not exist
2. Move the file using git mv to preserve history:
   ```bash
   mkdir -p docs/notes && \
   git mv N_FIGMA_TO_HTML_NOTES.md docs/notes/N_FIGMA_TO_HTML_NOTES.md
   ```
3. Verify the file exists at the new location
4. Search for any references to `N_FIGMA_TO_HTML_NOTES.md` in the codebase and update them if found
  </action>
  <verify>
    <automated>test -f docs/notes/N_FIGMA_TO_HTML_NOTES.md && echo "PASS: File moved successfully" && ! test -f N_FIGMA_TO_HTML_NOTES.md && echo "PASS: Original cleared" || echo "FAIL"</automated>
  </verify>
  <done>
- `docs/notes/N_FIGMA_TO_HTML_NOTES.md` exists
- `N_FIGMA_TO_HTML_NOTES.md` does not exist at repo root
- Git history preserved via git mv
  </done>
</task>

---

## Must-Haves Summary

- [x] `workflows/legacy/fix-bug-v1.5.md` archived to `.planning/milestones/archive/` (D-01)
- [x] `references/mermaid-rules.md` confirmed wired - NO ACTION needed (D-02)
- [x] `de_xuat_cai_tien.md` confirmed missing - NO ACTION needed (D-03)
- [x] `N_FIGMA_TO_HTML_NOTES.md` moved to `docs/notes/` (D-04)
- [x] `INTEGRATION_GUIDE.md` confirmed exists and referenced - NO ACTION needed (D-05)

## Verification Commands

```bash
# Verify fix-bug-v1.5.md archived
test -f .planning/milestones/archive/fix-bug-v1.5.md && echo "PASS: fix-bug-v1.5.md archived" || echo "FAIL"

# Verify N_FIGMA_TO_HTML_NOTES.md moved
test -f docs/notes/N_FIGMA_TO_HTML_NOTES.md && echo "PASS: N_FIGMA_TO_HTML_NOTES.md moved" || echo "FAIL"

# Verify no stale references (should show only planning files)
grep -r "workflows/legacy/fix-bug-v1.5.md" --include="*.md" . 2>/dev/null | grep -v ".planning" | wc -l
```

## Summary

This plan executes the H-06 cleanup with the following decisions based on actual file investigation:
- 2 files moved/archived (fix-bug-v1.5.md, N_FIGMA_TO_HTML_NOTES.md)
- 3 files require NO ACTION (mermaid-rules.md already wired, de_xuat_cai_tien.md already gone, INTEGRATION_GUIDE.md already exists and linked)
