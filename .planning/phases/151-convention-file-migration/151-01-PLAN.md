---
phase: 151-convention-file-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/pd/conventions.md
  - workflows/conventions.md
  - CLAUDE.md
  - test/snapshots/codex/conventions.md
  - test/snapshots/copilot/conventions.md
  - test/snapshots/gemini/conventions.md
  - test/snapshots/opencode/conventions.md
autonomous: true
requirements:
  - CONV-01
  - CONV-02

must_haves:
  truths:
    - "pd:conventions skill references CONVENTIONS.md in description"
    - "pd:conventions workflow creates CONVENTIONS.md instead of CLAUDE.md"
    - "Repo root has no CLAUDE.md file"
    - "Snapshot tests pass after regeneration"
  artifacts:
    - path: "commands/pd/conventions.md"
      provides: "Skill definition for conventions"
      contains: "CONVENTIONS.md"
    - path: "workflows/conventions.md"
      provides: "Workflow logic for conventions"
      contains: "CONVENTIONS.md"
  key_links:
    - from: "commands/pd/conventions.md"
      to: "workflows/conventions.md"
      via: "execution_context reference"
      pattern: "@workflows/conventions.md"
---

<objective>
Migrate the `pd:conventions` skill and workflow from Claude-specific `CLAUDE.md` output to universal `CONVENTIONS.md` output.

Purpose: Enable any AI model (not just Claude) to use project conventions by removing Claude-specific file naming.

Output: Updated skill/workflow files, regenerated snapshots, deleted `CLAUDE.md`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-convention-file-migration/151-CONTEXT.md
@.planning/phases/151-convention-file-migration/151-RESEARCH.md

<interfaces>
<!-- Key files to modify — executor should reference these directly -->

From commands/pd/conventions.md (6 CLAUDE.md occurrences):
- Line 3: description field
- Line 17: objective section
- Line 41: output section (file reference)
- Line 46: success when section
- Line 56: rules section (ask user)
- Line 57: rules section (MUST reflect)

From workflows/conventions.md (10 CLAUDE.md occurrences + 1 deletion):
- Line 2: header comment
- Line 10: Step 1 header
- Line 11: check existence
- Line 12: exists message
- Line 48: Step 5 header
- Line 59: merge description
- Lines 63-69: notification box (multiple lines)
- Line 75: under 50 lines rule
- Line 81: DELETE THIS LINE (Claude Code auto-load reference)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update skill and workflow files</name>
  <files>commands/pd/conventions.md, workflows/conventions.md</files>
  <read_first>
    - commands/pd/conventions.md (skill definition — 6 occurrences to change)
    - workflows/conventions.md (workflow logic — 10 occurrences to change, 1 line to delete)
    - .planning/phases/151-convention-file-migration/151-RESEARCH.md (exact line map)
  </read_first>
  <action>
Replace all occurrences of `CLAUDE.md` with `CONVENTIONS.md` in both files.

**In commands/pd/conventions.md (6 replacements):**
1. Line 3: `"...create CLAUDE.md..."` → `"...create CONVENTIONS.md..."`
2. Line 17: `create or update \`CLAUDE.md\`` → `create or update \`CONVENTIONS.md\``
3. Line 41: `\`CLAUDE.md\` -- project coding conventions` → `\`CONVENTIONS.md\` -- project coding conventions`
4. Line 46: `\`CLAUDE.md\` includes naming conventions...` → `\`CONVENTIONS.md\` includes naming conventions...`
5. Line 56: `before creating \`CLAUDE.md\`` → `before creating \`CONVENTIONS.md\``
6. Line 57: `\`CLAUDE.md\` MUST reflect...` → `\`CONVENTIONS.md\` MUST reflect...`

**In workflows/conventions.md (10 replacements + 1 deletion):**
1. Line 2: `create/update CLAUDE.md` → `create/update CONVENTIONS.md`
2. Line 10: `## Step 1: Check for existing CLAUDE.md` → `## Step 1: Check for existing CONVENTIONS.md`
3. Line 11: `\`CLAUDE.md\` at root:` → `\`CONVENTIONS.md\` at root:`
4. Line 12: `"CLAUDE.md already exists..."` → `"CONVENTIONS.md already exists..."`
5. Line 48: `## Step 5: Create CLAUDE.md` → `## Step 5: Create CONVENTIONS.md`
6. Line 59: `Existing CLAUDE.md + additions` → `Existing CONVENTIONS.md + additions`
7. Line 64: `CLAUDE.md created!` → `CONVENTIONS.md created!`
8. Line 66: `File: CLAUDE.md ([N] lines)` → `File: CONVENTIONS.md ([N] lines)`
9. Line 67: `Claude Code auto-reads each session` → `Run /pd:write-code to use conventions`
10. Line 75: `CLAUDE.md UNDER 50 lines` → `CONVENTIONS.md UNDER 50 lines`
11. **DELETE Line 81** entirely: `- File compatible with Claude Code auto-load (CLAUDE.md at root)`

Also adjust notification box width (lines 63, 65, 69) from 38 chars to 39 chars to fit "CONVENTIONS.md" (1 char longer).
  </action>
  <verify>
    <automated>grep -c "CLAUDE\.md" commands/pd/conventions.md workflows/conventions.md | grep -v ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep "CLAUDE\.md" commands/pd/conventions.md returns 0 matches
    - grep "CLAUDE\.md" workflows/conventions.md returns 0 matches
    - grep "CONVENTIONS\.md" commands/pd/conventions.md returns 6 matches
    - grep "CONVENTIONS\.md" workflows/conventions.md returns 9 matches (10 minus deleted line)
    - grep "Claude Code auto-load" workflows/conventions.md returns 0 matches (line 81 deleted)
  </acceptance_criteria>
  <done>Both files contain only CONVENTIONS.md references, no CLAUDE.md references remain, Claude-specific line 81 deleted</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate snapshots and verify tests</name>
  <files>test/snapshots/codex/conventions.md, test/snapshots/copilot/conventions.md, test/snapshots/gemini/conventions.md, test/snapshots/opencode/conventions.md</files>
  <read_first>
    - test/generate-snapshots.js (snapshot generator script)
    - test/smoke-snapshot.test.js (snapshot test to verify)
  </read_first>
  <action>
Run the snapshot generator to update all 4 platform snapshots for the conventions skill:

```bash
node test/generate-snapshots.js
```

Then verify snapshots pass:

```bash
node --test test/smoke-snapshot.test.js
```

The generator will automatically create updated snapshots reflecting the CONVENTIONS.md changes in `commands/pd/conventions.md`.
  </action>
  <verify>
    <automated>node --test test/smoke-snapshot.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node test/generate-snapshots.js exits with code 0
    - node --test test/smoke-snapshot.test.js passes (all assertions pass)
    - grep "CONVENTIONS\.md" test/snapshots/codex/conventions.md returns at least 1 match
    - grep "CONVENTIONS\.md" test/snapshots/copilot/conventions.md returns at least 1 match
    - grep "CONVENTIONS\.md" test/snapshots/gemini/conventions.md returns at least 1 match
    - grep "CONVENTIONS\.md" test/snapshots/opencode/conventions.md returns at least 1 match
  </acceptance_criteria>
  <done>All 4 platform snapshots regenerated with CONVENTIONS.md references, snapshot tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Delete CLAUDE.md from repo root</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (confirm it exists before deletion)
    - .planning/phases/151-convention-file-migration/151-CONTEXT.md (confirms deletion decision)
  </read_first>
  <action>
Delete the Claude-specific `CLAUDE.md` file from the repository root:

```bash
rm CLAUDE.md
git add -A CLAUDE.md
```

This file contains Claude Code custom instructions which are model-specific. The project aims to be model-agnostic, so this file is removed entirely per CONTEXT.md decision #3.

Note: `CLAUDE.vi.md` (Vietnamese version) is OUT OF SCOPE per CONTEXT.md — do NOT delete it.
  </action>
  <verify>
    <automated>test ! -f CLAUDE.md && echo "CLAUDE.md deleted" || echo "CLAUDE.md still exists"</automated>
  </verify>
  <acceptance_criteria>
    - ls CLAUDE.md returns "No such file or directory"
    - git status shows CLAUDE.md as deleted
    - CLAUDE.vi.md still exists (not deleted — out of scope)
  </acceptance_criteria>
  <done>CLAUDE.md removed from repo root, only CLAUDE.vi.md remains (out of scope)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase is pure file renaming with no external input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-01 | Information Disclosure | CLAUDE.md deletion | accept | File contains project conventions (non-sensitive); deletion is intentional |

No additional threats — this is a documentation/config change with no logic modifications.
</threat_model>

<verification>
After all tasks complete:

```bash
# 1. Verify no CLAUDE.md references in changed files
grep -c "CLAUDE\.md" commands/pd/conventions.md workflows/conventions.md

# 2. Verify CONVENTIONS.md references exist
grep -c "CONVENTIONS\.md" commands/pd/conventions.md workflows/conventions.md

# 3. Verify CLAUDE.md file deleted
ls CLAUDE.md 2>&1

# 4. Verify all tests pass
node --test test/

# 5. Verify Claude-specific line removed
grep "Claude Code auto-load" workflows/conventions.md
```

Expected results:
- Step 1: Both files show 0
- Step 2: commands/pd/conventions.md shows 6, workflows/conventions.md shows 9
- Step 3: "No such file or directory"
- Step 4: All tests pass
- Step 5: No output (line deleted)
</verification>

<success_criteria>
- [ ] `pd:conventions` skill description references `CONVENTIONS.md`
- [ ] `pd:conventions` workflow creates `CONVENTIONS.md` (all 9 references updated)
- [ ] Claude-specific "auto-load" line deleted from workflow
- [ ] Snapshot tests pass for all 4 platforms
- [ ] `CLAUDE.md` file deleted from repo root
- [ ] `node --test test/` passes
- [ ] CONV-01 satisfied: skill creates CONVENTIONS.md
- [ ] CONV-02 satisfied: CLAUDE.md removed from project
</success_criteria>

<output>
After completion, create `.planning/phases/151-convention-file-migration/151-01-SUMMARY.md`
</output>
