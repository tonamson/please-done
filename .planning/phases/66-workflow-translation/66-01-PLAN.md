---
phase: 66-workflow-translation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/init.md
  - workflows/scan.md
  - workflows/conventions.md
  - workflows/what-next.md
  - workflows/research.md
  - workflows/complete-milestone.md
  - workflows/test.md
  - test/smoke-integrity.test.js
autonomous: true
user_setup: []
requirements:
  - TRANS-03
must_haves:
  truths:
    - "7 smaller workflow files contain zero Vietnamese diacritics"
    - "Step numbering uses 'Step X' instead of 'Bước X' in all 7 files"
    - "XML tags, cross-references, and placeholders are preserved verbatim"
    - "smoke-integrity.test.js passes after workflow + assertion updates"
  artifacts:
    - path: "workflows/init.md"
      provides: "English translation of init workflow"
    - path: "workflows/scan.md"
      provides: "English translation of scan workflow"
    - path: "workflows/conventions.md"
      provides: "English translation of conventions workflow"
    - path: "workflows/what-next.md"
      provides: "English translation of what-next workflow"
    - path: "workflows/research.md"
      provides: "English translation of research workflow"
    - path: "workflows/complete-milestone.md"
      provides: "English translation of complete-milestone workflow"
    - path: "workflows/test.md"
      provides: "English translation of test workflow"
  key_links:
    - from: "workflows/*.md"
      to: "test/smoke-integrity.test.js"
      via: "Test assertions match workflow content"
      pattern: "workflows"
    - from: "commands/pd/*.md"
      to: "workflows/*.md"
      via: "@workflows/name.md inline references"
      pattern: "@workflows/"
---

<objective>
Translate 7 smaller workflow files to English and update test assertions that would break.

Purpose: Deliver first batch of TRANS-03. These 7 files (1,058 lines total) are the smaller half of workflow content.
Output: 7 English workflow files + updated test assertions, smoke-integrity tests passing.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-workflow-translation/66-CONTEXT.md
@.planning/phases/66-workflow-translation/66-RESEARCH.md
@.planning/phases/65-skills-config-foundation/65-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate 7 smaller workflow files to English (per D-01, D-02, D-04, D-05)</name>
  <files>workflows/init.md, workflows/scan.md, workflows/conventions.md, workflows/what-next.md, workflows/research.md, workflows/complete-milestone.md, workflows/test.md</files>
  <read_first>
    - workflows/init.md
    - workflows/scan.md
    - workflows/conventions.md
    - workflows/what-next.md
    - workflows/research.md
    - workflows/complete-milestone.md
    - workflows/test.md
    - .planning/phases/66-workflow-translation/66-CONTEXT.md
  </read_first>
  <action>
    For each of the 7 workflow files, translate ALL Vietnamese prose content to English:

    1. **Step numbering (per D-04):** Replace every "Bước X" with "Step X". Preserve sub-step patterns: "Bước 1.7" → "Step 1.7", "Bước 5b.1" → "Step 5b.1". Only the label word changes; numbering scheme stays identical.

    2. **Prose translation:** Translate all Vietnamese descriptive text, instructions, comments, and section headings to English. Use standard terminology (per D-05): `phase`, `milestone`, `verification`, `requirements`, `success criteria`.

    3. **Behavioral keywords:** Translate with matching force level:
       - "DỪNG" → "STOP"
       - "BẮT BUỘC" → "REQUIRED"
       - "KHÔNG" → "DO NOT"
       - "CHỉ" → "ONLY"

    4. **Preserve verbatim (per D-02, D-03, D-10):**
       - All XML tags: `<process>`, `<purpose>`, `<rules>`, `<required_reading>`, `<conditional_reading>`, `<research_injection>`, `<verification>`, `<success_criteria>`, etc.
       - All frontmatter keys and values
       - All `@workflows/name.md`, `@references/name.md`, `@templates/name.md` path references
       - All placeholders: `$ARGUMENTS`, `$PHASE`, `{resolved_model}`, etc.
       - All command names: `/pd:*`, `pd:*`
       - All code blocks, inline code, regex patterns
       - All file paths and directory references
       - All variable names and function names

    5. **Non-diacritical Vietnamese:** Some files may use Vietnamese without diacritics (e.g., "Buoc", "Kiem tra"). Translate these to English too — diacritic grep won't catch them.

    6. **File-specific notes:**
       - `test.md` (~247 lines): Contains "Effort routing cho test" → translate to "Effort routing for test"
       - `complete-milestone.md` (~272 lines): Largest in this batch, has tables — preserve pipe alignment
       - `conventions.md` (~81 lines): Smallest file, straightforward translation

    7. Do NOT rename files, change paths, or expand scope beyond these 7 files.
  </action>
  <verify>
    <automated>rg -c '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/init.md workflows/scan.md workflows/conventions.md workflows/what-next.md workflows/research.md workflows/complete-milestone.md workflows/test.md && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - `rg '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/init.md` returns empty
    - `rg '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/scan.md` returns empty
    - `rg '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/conventions.md` returns empty
    - `rg '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/what-next.md` returns empty
    - `rg '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/research.md` returns empty
    - `rg '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/complete-milestone.md` returns empty
    - `rg '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/test.md` returns empty
    - `grep -c 'Step [0-9]' workflows/test.md` returns a non-zero count
    - `grep -c 'Bước' workflows/test.md` returns 0
    - All XML tags (`<process>`, `<purpose>`, `<rules>`, etc.) still present in each file
    - All `@workflows/`, `@references/`, `@templates/` path refs unchanged
  </acceptance_criteria>
  <done>7 smaller workflow files fully translated to English, structure and behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update smoke-integrity.test.js assertions that break from Plan 01 translations (per D-08, D-09)</name>
  <files>test/smoke-integrity.test.js</files>
  <read_first>
    - test/smoke-integrity.test.js
    - workflows/test.md
    - workflows/init.md
  </read_first>
  <action>
    Update ONLY the test assertions that will break because they regex-match Vietnamese strings in the 7 translated workflow files. Per D-09, defer broader test string migration to Phase 69.

    **Line 115 — inline workflow step regex:**
    Current: `/<process>[\s\S]*(Bước|Buoc) [0-9]+[\s\S]*<\/process>/`
    Change to: `/<process>[\s\S]*(Step|Bước|Buoc) [0-9]+[\s\S]*<\/process>/`
    Reason: The 7 translated workflows now use "Step X". The remaining 6 (Plan 02) still use Vietnamese. Adding "Step" to alternation keeps both passing until Plan 02 finalizes.

    **Line 727 — test.md effort routing assertion:**
    Current: `/Effort routing cho test/`
    Change to: `/Effort routing for test/`
    Reason: test.md is translated in Task 1, changing "cho" to "for".

    **DO NOT change:**
    - Test names (it/describe strings in Vietnamese) — these are labels, not functional assertions
    - Assertion failure messages (e.g., "thieu", "còn sót") — these don't affect pass/fail
    - Assertions matching write-code.md, fix-bug.md, plan.md content — those files are not yet translated (Plan 02)

    After updating, run `node --test test/smoke-integrity.test.js` to confirm all tests pass.
  </action>
  <verify>
    <automated>node --test test/smoke-integrity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'Step|Bước|Buoc' test/smoke-integrity.test.js` shows the updated inline regex on line ~115
    - `grep 'Effort routing for test' test/smoke-integrity.test.js` returns a match
    - `grep 'Effort routing cho test' test/smoke-integrity.test.js` returns empty
    - `node --test test/smoke-integrity.test.js` exits with code 0
    - Assertions for write-code.md (lines ~1144, ~1156, ~1168) are NOT modified (still Vietnamese — Plan 02 scope)
    - Assertions for fix-bug.md (line ~693) are NOT modified (still Vietnamese — Plan 02 scope)
  </acceptance_criteria>
  <done>Test assertions updated to match translated workflow content, smoke-integrity suite passes.</done>
</task>

</tasks>

<verification>
- Vietnamese diacritic grep across 7 workflow files returns zero matches
- `grep -c 'Bước' workflows/{init,scan,conventions,what-next,research,complete-milestone,test}.md` returns 0 for each
- `node --test test/smoke-integrity.test.js` passes
</verification>

<success_criteria>
- 7 workflow files translated to English (first batch of TRANS-03)
- Step numbering follows "Step X" convention in all 7 files
- XML tags, cross-references, placeholders preserved unchanged
- smoke-integrity tests pass with updated assertions
</success_criteria>

<output>
After completion, create `.planning/phases/66-workflow-translation/66-01-SUMMARY.md`
</output>
