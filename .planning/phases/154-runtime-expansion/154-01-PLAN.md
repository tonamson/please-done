---
phase: 154-runtime-expansion
plan: 01
type: execute
wave: 1
status: done
depends_on: []
files_modified:
  - bin/lib/platforms.js
  - bin/install.js
autonomous: true
requirements: [RT-01, RT-02, RT-03, RT-04, RT-05, RT-06, CFG-01, CFG-02]

must_haves:
  truths:
    - "User can install to kilo via --kilo flag"
    - "User can install to antigravity via --antigravity flag"
    - "User can install to augment via --augment flag"
    - "User can install to trae via --trae flag"
    - "User can install to cursor via --cursor flag"
    - "User can install to windsurf via --windsurf flag with correct path ~/.codeium/windsurf/"
    - "User sees --force-statusline parsed and warning logged"
    - "User running --all gets all 11 runtimes"
    - "User running --help sees all 11 runtimes listed"
  artifacts:
    - path: "bin/lib/platforms.js"
      provides: "PLATFORMS + TOOL_MAP with 4 new runtimes, fixed windsurf path"
      exports: ["PLATFORMS", "TOOL_MAP", "getGlobalDir", "getAllRuntimes"]
    - path: "bin/install.js"
      provides: "CLI flags for new runtimes + --force-statusline"
      contains: "--kilo, --antigravity, --augment, --trae, --cursor, --windsurf, --force-statusline"
  key_links:
    - from: "bin/install.js"
      to: "bin/lib/platforms.js"
      via: "getAllRuntimes(), getGlobalDir()"
      pattern: "getAllRuntimes\\(\\)|getGlobalDir\\("
---

<objective>
Expand runtime support from 7 to 11 platforms by adding kilo, antigravity, augment, and trae. Fix windsurf's global config path to ~/.codeium/windsurf/. Add --force-statusline flag (parse-only with warning).

Purpose: Enable users to install please-done skills to 4 additional AI coding runtimes while fixing the incorrect windsurf path and preparing for future statusline configuration support.

Output: Updated platforms.js with 4 new runtime definitions and corrected windsurf path; updated install.js with CLI flags for all 11 runtimes plus --force-statusline.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-runtime-expansion/154-CONTEXT.md

## Interface Context

From bin/lib/platforms.js (current exports):
```javascript
const TOOL_MAP = {
  claude: {},
  codex: {},
  gemini: { Read: 'read_file', ... },
  opencode: {},
  copilot: { Read: 'read', ... },
  cursor: {},
  windsurf: {},
};

const PLATFORMS = {
  claude: { name: 'Claude Code', dirName: '.claude', envVar: 'CLAUDE_CONFIG_DIR', ... },
  codex: { name: 'Codex CLI', dirName: '.codex', envVar: 'CODEX_HOME', ... },
  gemini: { name: 'Gemini CLI', dirName: '.gemini', envVar: 'GEMINI_CONFIG_DIR', ... },
  opencode: { name: 'OpenCode', dirName: '.opencode', envVar: 'OPENCODE_CONFIG_DIR', ... },
  copilot: { name: 'GitHub Copilot', dirName: '.github', envVar: 'COPILOT_CONFIG_DIR', ... },
  cursor: { name: 'Cursor', dirName: '.cursor', envVar: 'CURSOR_CONFIG_DIR', ... },
  windsurf: { name: 'Windsurf', dirName: '.windsurf', envVar: 'WINDSURF_CONFIG_DIR', ... },
};

function getGlobalDir(runtime, explicitDir) { ... }
function getAllRuntimes() { return Object.keys(PLATFORMS); }
```

From bin/install.js (parseArgs structure):
```javascript
function parseArgs(argv) {
  const flags = { runtimes: [], isGlobal: true, uninstall: false, configDir: null, help: false };
  // switch cases: --claude, --codex, --gemini, --opencode, --copilot, --all, --global, --local, --uninstall, --config-dir, --help
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 new runtimes to platforms.js + fix windsurf path</name>
  <files>bin/lib/platforms.js</files>
  <action>
**A: Add to TOOL_MAP** (after windsurf entry, line 41):

Find:
```javascript
  cursor: {},    // Cursor uses Claude native tool names
  windsurf: {},  // Windsurf uses Claude native tool names
};
```

Replace with:
```javascript
  cursor: {},       // Cursor uses Claude native tool names
  windsurf: {},     // Windsurf uses Claude native tool names
  kilo: {},         // Kilo uses Claude native tool names
  antigravity: {},  // Antigravity uses Claude native tool names
  augment: {},      // Augment uses Claude native tool names
  trae: {},         // Trae uses Claude native tool names
};
```

**B: Add to PLATFORMS** (after windsurf entry, before closing brace):

Find:
```javascript
  windsurf: {
    name: 'Windsurf',
    description: 'Agentic IDE by Codeium',
    dirName: '.windsurf',
    commandPrefix: '/pd:',
    commandSeparator: ':',
    envVar: 'WINDSURF_CONFIG_DIR',
    skillFormat: 'nested',
    frontmatterFormat: 'yaml',
    toolMap: TOOL_MAP.windsurf,
  },
};
```

Replace with:
```javascript
  windsurf: {
    name: 'Windsurf',
    description: 'Agentic IDE by Codeium',
    dirName: '.codeium/windsurf',
    commandPrefix: '/pd:',
    commandSeparator: ':',
    envVar: 'WINDSURF_CONFIG_DIR',
    skillFormat: 'nested',
    frontmatterFormat: 'yaml',
    toolMap: TOOL_MAP.windsurf,
  },
  kilo: {
    name: 'Kilo',
    description: 'AI coding agent by Kilo',
    dirName: '.config/kilo',
    commandPrefix: '/pd:',
    commandSeparator: ':',
    envVar: 'KILO_CONFIG_DIR',
    skillFormat: 'nested',
    frontmatterFormat: 'yaml',
    toolMap: TOOL_MAP.kilo,
  },
  antigravity: {
    name: 'Antigravity',
    description: 'AI coding agent by Antigravity',
    dirName: '.gemini/antigravity',
    commandPrefix: '/pd:',
    commandSeparator: ':',
    envVar: 'ANTIGRAVITY_CONFIG_DIR',
    skillFormat: 'nested',
    frontmatterFormat: 'yaml',
    toolMap: TOOL_MAP.antigravity,
  },
  augment: {
    name: 'Augment',
    description: 'AI coding agent by Augment',
    dirName: '.augment',
    commandPrefix: '/pd:',
    commandSeparator: ':',
    envVar: 'AUGMENT_CONFIG_DIR',
    skillFormat: 'nested',
    frontmatterFormat: 'yaml',
    toolMap: TOOL_MAP.augment,
  },
  trae: {
    name: 'Trae',
    description: 'AI IDE by ByteDance',
    dirName: '.trae',
    commandPrefix: '/pd:',
    commandSeparator: ':',
    envVar: 'TRAE_CONFIG_DIR',
    skillFormat: 'nested',
    frontmatterFormat: 'yaml',
    toolMap: TOOL_MAP.trae,
  },
};
```

**C: Fix windsurf in getGlobalDir() and add new runtime cases**:

Find:
```javascript
    case 'cursor':
      return path.join(home, '.cursor');
    case 'windsurf':
      return path.join(home, '.windsurf');
    default:
      return path.join(home, platform.dirName);
```

Replace with:
```javascript
    case 'cursor':
      return path.join(home, '.cursor');
    case 'windsurf':
      return path.join(home, '.codeium', 'windsurf');
    case 'kilo': {
      const xdg = process.env.XDG_CONFIG_HOME || path.join(home, '.config');
      return path.join(xdg, 'kilo');
    }
    case 'antigravity':
      return path.join(home, '.gemini', 'antigravity');
    case 'augment':
      return path.join(home, '.augment');
    case 'trae':
      return path.join(home, '.trae');
    default:
      return path.join(home, platform.dirName);
```
  </action>
  <verify>
    <automated>NO_COLOR=1 node -e "const {getGlobalDir,getAllRuntimes,PLATFORMS} = require('./bin/lib/platforms'); const r=getAllRuntimes(); console.log('Runtimes:',r.length); console.log('windsurf:',getGlobalDir('windsurf')); console.log('kilo:',getGlobalDir('kilo')); console.log('antigravity:',getGlobalDir('antigravity')); console.log('augment:',getGlobalDir('augment')); console.log('trae:',getGlobalDir('trae'));" | grep -E "Runtimes: 11|windsurf:.*codeium|kilo:.*config|antigravity:.*gemini|augment:.*augment|trae:.*trae"</automated>
  </verify>
  <done>
    - getAllRuntimes() returns 11 runtimes
    - getGlobalDir('windsurf') returns ~/.codeium/windsurf/
    - getGlobalDir('kilo') returns ~/.config/kilo/ (or XDG_CONFIG_HOME/kilo)
    - getGlobalDir('antigravity') returns ~/.gemini/antigravity/
    - getGlobalDir('augment') returns ~/.augment/
    - getGlobalDir('trae') returns ~/.trae/
    - All 4 new runtimes have PLATFORMS entries with correct config
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CLI flags + getInstalledDirs + showHelp update in install.js</name>
  <files>bin/install.js</files>
  <action>
**A: Update parseArgs() — add forceStatusline to initial flags object**:

Find:
```javascript
  const flags = {
    runtimes: [],
    isGlobal: true,
    uninstall: false,
    configDir: null,
    help: false,
  };
```

Replace with:
```javascript
  const flags = {
    runtimes: [],
    isGlobal: true,
    uninstall: false,
    configDir: null,
    help: false,
    forceStatusline: false,
  };
```

**B: Add new runtime flags and --force-statusline in parseArgs() switch**:

Find:
```javascript
      case "--copilot":
        flags.runtimes.push("copilot");
        break;
      case "--all":
        flags.runtimes = getAllRuntimes();
        break;
```

Replace with:
```javascript
      case "--copilot":
        flags.runtimes.push("copilot");
        break;
      case "--cursor":
        flags.runtimes.push("cursor");
        break;
      case "--windsurf":
        flags.runtimes.push("windsurf");
        break;
      case "--kilo":
        flags.runtimes.push("kilo");
        break;
      case "--antigravity":
        flags.runtimes.push("antigravity");
        break;
      case "--augment":
        flags.runtimes.push("augment");
        break;
      case "--trae":
        flags.runtimes.push("trae");
        break;
      case "--force-statusline":
        flags.forceStatusline = true;
        break;
      case "--all":
        flags.runtimes = getAllRuntimes();
        break;
```

**C: Add new runtimes to getInstalledDirs()**:

Find:
```javascript
function getInstalledDirs(runtime) {
  switch (runtime) {
    case "claude":
      return ["commands/pd"];
    case "codex":
      return ["skills"];
    case "gemini":
      return ["commands/pd"];
    case "opencode":
      return ["command"];
    case "copilot":
      return ["skills"];
    default:
      return [];
  }
}
```

Replace with:
```javascript
function getInstalledDirs(runtime) {
  switch (runtime) {
    case "claude":
      return ["commands/pd"];
    case "codex":
      return ["skills"];
    case "gemini":
      return ["commands/pd"];
    case "opencode":
      return ["command"];
    case "copilot":
      return ["skills"];
    case "cursor":
      return ["commands/pd"];
    case "windsurf":
      return ["commands/pd"];
    case "kilo":
      return ["commands/pd"];
    case "antigravity":
      return ["commands/pd"];
    case "augment":
      return ["commands/pd"];
    case "trae":
      return ["commands/pd"];
    default:
      return [];
  }
}
```

**D: Add forceStatusline warning in main() after WSL check**:

Find:
```javascript
  // WSL check
  if (isWSL()) {
    log.warn(
      "WSL detected. Consider running the installer from native Linux, not through Windows.",
    );
  }

  // Resolve runtimes
```

Replace with:
```javascript
  // WSL check
  if (isWSL()) {
    log.warn(
      "WSL detected. Consider running the installer from native Linux, not through Windows.",
    );
  }

  // Force statusline warning (D-03: parse-only, no implementation)
  if (flags.forceStatusline) {
    log.warn("--force-statusline: statusline config not yet supported in this version");
  }

  // Resolve runtimes
```

**E: Update showHelp() to include all 11 runtimes and --force-statusline**:

Find:
```javascript
${bold("Platforms:")}
  ${yellow("--claude")}          Install for Claude Code
  ${yellow("--codex")}           Install for Codex CLI
  ${yellow("--gemini")}          Install for Gemini CLI
  ${yellow("--opencode")}        Install for OpenCode
  ${yellow("--copilot")}         Install for GitHub Copilot
  ${yellow("--all")}             Install all platforms
```

Replace with:
```javascript
${bold("Platforms:")}
  ${yellow("--claude")}          Install for Claude Code
  ${yellow("--codex")}           Install for Codex CLI
  ${yellow("--gemini")}          Install for Gemini CLI
  ${yellow("--opencode")}        Install for OpenCode
  ${yellow("--copilot")}         Install for GitHub Copilot
  ${yellow("--cursor")}          Install for Cursor
  ${yellow("--windsurf")}        Install for Windsurf
  ${yellow("--kilo")}            Install for Kilo
  ${yellow("--antigravity")}     Install for Antigravity
  ${yellow("--augment")}         Install for Augment
  ${yellow("--trae")}            Install for Trae
  ${yellow("--all")}             Install all platforms
```

Find:
```javascript
${bold("Options:")}
  ${dim("-g")}, ${yellow("--global")}      Global install ${dim("(default)")}
  ${dim("-l")}, ${yellow("--local")}       Local install (project-level)
  ${dim("-u")}, ${yellow("--uninstall")}   Uninstall
  ${dim("-c")}, ${yellow("--config-dir")}  Custom config directory
  ${dim("-h")}, ${yellow("--help")}        Show help
```

Replace with:
```javascript
${bold("Options:")}
  ${dim("-g")}, ${yellow("--global")}         Global install ${dim("(default)")}
  ${dim("-l")}, ${yellow("--local")}          Local install (project-level)
  ${dim("-u")}, ${yellow("--uninstall")}      Uninstall
  ${dim("-c")}, ${yellow("--config-dir")}     Custom config directory
  ${yellow("--force-statusline")}  Replace existing statusline config
  ${dim("-h")}, ${yellow("--help")}           Show help
```
  </action>
  <verify>
    <automated>node bin/install.js --help 2>&1 | grep -E "kilo|antigravity|augment|trae|windsurf|cursor|force-statusline" | wc -l | grep -q "7" && echo "PASS: All 7 new flags in help" || echo "FAIL"</automated>
  </verify>
  <done>
    - parseArgs() recognizes --kilo, --antigravity, --augment, --trae, --cursor, --windsurf, --force-statusline
    - flags.forceStatusline defaults to false and is set to true when --force-statusline passed
    - getInstalledDirs() returns ["commands/pd"] for all 6 new runtimes
    - main() logs warning when --force-statusline is used
    - showHelp() lists all 11 runtimes and --force-statusline option
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify full integration and commit</name>
  <files>bin/lib/platforms.js, bin/install.js</files>
  <action>
Run comprehensive verification:

1. **Verify runtime count**:
```bash
NO_COLOR=1 node -e "console.log(require('./bin/lib/platforms').getAllRuntimes())"
# Expected: 11 runtimes
```

2. **Verify windsurf path fix (D-02)**:
```bash
NO_COLOR=1 node -e "console.log(require('./bin/lib/platforms').getGlobalDir('windsurf'))"
# Expected: /home/{user}/.codeium/windsurf
```

3. **Verify new runtime paths (D-01)**:
```bash
NO_COLOR=1 node -e "const p = require('./bin/lib/platforms'); ['kilo','antigravity','augment','trae'].forEach(r => console.log(r + ': ' + p.getGlobalDir(r)))"
```

4. **Verify --help output**:
```bash
node bin/install.js --help | grep -E "(--kilo|--antigravity|--augment|--trae|--cursor|--windsurf|--force-statusline)"
```

5. **Verify --force-statusline warning (D-03)**:
```bash
node bin/install.js --force-statusline --claude 2>&1 | grep -q "statusline config not yet supported" && echo "PASS" || echo "FAIL"
```

6. **Verify --all includes all runtimes**:
```bash
PD_TEST_MODE=1 node -e "const {parseArgs} = require('./bin/install'); console.log(parseArgs(['node','install.js','--all']).runtimes.length)"
# Expected: 11
```

7. **Run existing tests to ensure no regression**:
```bash
node --test test/smoke-install.test.js test/smoke-all-platforms.test.js
```

Commit changes:
```bash
git add bin/lib/platforms.js bin/install.js
git commit -m "feat(platforms): add kilo, antigravity, augment, trae runtimes; fix windsurf path

- Add 4 new runtime definitions to PLATFORMS and TOOL_MAP
- Fix windsurf global path: ~/.windsurf → ~/.codeium/windsurf (D-02)
- Add CLI flags: --kilo, --antigravity, --augment, --trae, --cursor, --windsurf
- Add --force-statusline flag with parse-only warning (D-03)
- Update getInstalledDirs() for all new runtimes
- Update showHelp() to list all 11 runtimes

Runtime count: 7 → 11

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"
```
  </action>
  <verify>
    <automated>node --test test/smoke-install.test.js 2>&1 | grep -E "pass|fail" | tail -5</automated>
  </verify>
  <done>
    - getAllRuntimes() returns exactly 11 items
    - All new runtime paths resolve correctly
    - --force-statusline logs warning but doesn't crash
    - --all flag includes all 11 runtimes
    - --help shows all 11 platforms and --force-statusline option
    - Existing tests pass
    - Changes committed with proper message
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → parseArgs | User-provided flags parsed into flags object |
| env vars → getGlobalDir | XDG_CONFIG_HOME, *_CONFIG_DIR environment variables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-01 | S (Spoofing) | env vars | accept | Environment variables are user-controlled by design; no elevation |
| T-154-02 | T (Tampering) | CLI flags | accept | Unknown flags already logged as warnings; no code execution |
| T-154-03 | I (Information Disclosure) | getGlobalDir | accept | Paths are non-sensitive; intentionally shown in output |
</threat_model>

<verification>
After all tasks complete:

```bash
# 1. Runtime count
NO_COLOR=1 node -e "console.log('Runtimes:', require('./bin/lib/platforms').getAllRuntimes().length)"
# Expected: Runtimes: 11

# 2. Windsurf path (D-02)
NO_COLOR=1 node -e "console.log(require('./bin/lib/platforms').getGlobalDir('windsurf'))"
# Expected: .../.codeium/windsurf

# 3. New runtime paths (D-01)
NO_COLOR=1 node -e "const p=require('./bin/lib/platforms'); ['kilo','antigravity','augment','trae'].forEach(r=>console.log(r+':',p.getGlobalDir(r)))"

# 4. Help output has all new flags
node bin/install.js --help | grep -c -E "(kilo|antigravity|augment|trae|cursor|windsurf|force-statusline)"
# Expected: 7

# 5. Force statusline warning (D-03)
node bin/install.js --force-statusline --help 2>&1 | grep "statusline"
# Expected: warning message

# 6. Tests pass
node --test test/smoke-install.test.js
```
</verification>

<success_criteria>
- [ ] getAllRuntimes() returns 11 runtimes (was 7)
- [ ] getGlobalDir('windsurf') returns ~/.codeium/windsurf/ (per D-02)
- [ ] getGlobalDir('kilo') returns ~/.config/kilo/ (per D-01)
- [ ] getGlobalDir('antigravity') returns ~/.gemini/antigravity/ (per D-01)
- [ ] getGlobalDir('augment') returns ~/.augment/ (per D-01)
- [ ] getGlobalDir('trae') returns ~/.trae/ (per D-01)
- [ ] --force-statusline parses and logs warning (per D-03)
- [ ] --help lists all 11 runtimes
- [ ] --all flag uses getAllRuntimes() and includes all 11
- [ ] Existing tests pass without regression
- [ ] Changes committed with proper message
</success_criteria>

<output>
After completion, create `.planning/phases/154-runtime-expansion/154-01-SUMMARY.md`
</output>
