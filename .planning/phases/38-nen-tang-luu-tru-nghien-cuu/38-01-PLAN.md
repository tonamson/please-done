---
phase: 38-nen-tang-luu-tru-nghien-cuu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/research-store.js
  - test/smoke-research-store.test.js
  - .planning/research/internal/.gitkeep
  - .planning/research/external/.gitkeep
autonomous: true
requirements: [STORE-01, STORE-02, AUDIT-01, AUDIT-03]
---

<objective>
Tao nen tang luu tru nghien cuu: thu muc phan tach internal/external, frontmatter chuan YAML, confidence 3 bac, va research-store.js pure function module (createEntry, parseEntry, listEntries, nextId).

Output: 1 JS module, 1 test file, 2 thu muc voi .gitkeep.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tao cau truc thu muc research va .gitkeep</name>
  <files>.planning/research/internal/.gitkeep, .planning/research/external/.gitkeep</files>
  <action>
  Tao 2 thu muc:
  - `.planning/research/internal/` — luu ket qua phan tich codebase
  - `.planning/research/external/` — luu ket qua tra cuu web/docs

  Moi thu muc co 1 file `.gitkeep` de git track thu muc rong.
  </action>
  <verify>
    <automated>[ -d ".planning/research/internal" ] && [ -d ".planning/research/external" ] && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>2 thu muc research ton tai voi .gitkeep</done>
</task>

<task type="auto">
  <name>Task 2: Tao research-store.js pure function module</name>
  <files>bin/lib/research-store.js</files>
  <action>
  Tao module bin/lib/research-store.js voi cac pure functions:

  1. `createEntry({ type, topic, agent, confidence, claims })` — tao noi dung markdown voi YAML frontmatter chuan
     - type: 'internal' | 'external'
     - frontmatter: agent, created (ISO-8601), source (internal/external), topic, confidence (HIGH/MEDIUM/LOW)
     - Body: ## Tong ket, ## Bang chung (voi tung claim co inline confidence)
     - Tra ve { filename, content }

  2. `parseEntry(content)` — parse markdown research file thanh object
     - Tra ve { frontmatter: {...}, claims: [{text, confidence, source}], sections: {...} }

  3. `nextId(existingFiles)` — tinh ID tiep theo cho external files
     - Parse RES-[ID]-*.md pattern, tra ve ID tiep theo (zero-padded 3 digits)

  4. `formatFilename({ type, id, slug })` — tao ten file chuan
     - internal: `INT-[slug].md`
     - external: `RES-[ID]-[slug].md`

  5. `CONFIDENCE_LEVELS` — constant { HIGH, MEDIUM, LOW } voi mo ta

  KHONG doc file — tat ca content truyen qua tham so (pure function pattern).
  </action>
  <verify>
    <automated>node -e "const rs = require('./bin/lib/research-store'); console.log(Object.keys(rs).sort().join(','))" | grep -q "CONFIDENCE_LEVELS,createEntry,formatFilename,nextId,parseEntry" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>research-store.js export 5 functions/constants, khong doc file</done>
</task>

<task type="auto">
  <name>Task 3: Tao smoke tests cho research-store.js</name>
  <files>test/smoke-research-store.test.js</files>
  <action>
  Tao test file test/smoke-research-store.test.js voi cac test cases:

  1. createEntry — internal type co frontmatter day du
  2. createEntry — external type co frontmatter day du
  3. createEntry — confidence levels hop le (HIGH/MEDIUM/LOW)
  4. createEntry — claims duoc render voi inline confidence
  5. parseEntry — parse frontmatter chinh xac
  6. parseEntry — parse claims voi confidence
  7. parseEntry — empty content tra ve defaults
  8. nextId — tra ve '001' khi khong co files
  9. nextId — tra ve ID tiep theo
  10. formatFilename — internal pattern
  11. formatFilename — external pattern
  12. CONFIDENCE_LEVELS — co 3 bac

  Dung node:test + node:assert/strict pattern nhu cac test khac.
  </action>
  <verify>
    <automated>node --test test/smoke-research-store.test.js</automated>
  </verify>
  <done>Tat ca tests PASS, coverage day du cho 5 exports</done>
</task>

</tasks>

<verification>
1. `[ -d ".planning/research/internal" ] && [ -d ".planning/research/external" ]` — thu muc ton tai
2. `node -e "const rs = require('./bin/lib/research-store'); console.log(Object.keys(rs))"` — 5 exports
3. `node --test test/smoke-research-store.test.js` — tat ca PASS
</verification>

<success_criteria>
- Thu muc .planning/research/internal/ va external/ ton tai
- research-store.js co 5 exports: createEntry, parseEntry, nextId, formatFilename, CONFIDENCE_LEVELS
- Tat ca pure functions — khong doc file, khong side effects
- Tat ca tests PASS
- Frontmatter co day du: agent, created, source, topic, confidence
- Confidence 3 bac hoat dong o ca cap file va cap claim
</success_criteria>

<output>
After completion, create `.planning/phases/38-nen-tang-luu-tru-nghien-cuu/38-01-SUMMARY.md`
</output>
