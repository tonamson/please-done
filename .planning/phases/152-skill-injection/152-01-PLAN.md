---
phase: 152-skill-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/pd/write-code.md
  - commands/pd/fix-bug.md
  - commands/pd/plan.md
autonomous: true
requirements: [CONV-03, CONV-04, CONV-05]
must_haves:
  truths:
    - "pd:write-code prompts any AI model to read CONVENTIONS.md before writing code"
    - "pd:fix-bug prompts any AI model to read CONVENTIONS.md before fixing code"
    - "pd:plan prompts any AI model to read CONVENTIONS.md before planning tasks"
  artifacts:
    - path: "commands/pd/write-code.md"
      provides: "CONVENTIONS.md injection in execution_context"
      contains: "@CONVENTIONS.md (optional)"
    - path: "commands/pd/fix-bug.md"
      provides: "CONVENTIONS.md injection in execution_context"
      contains: "@CONVENTIONS.md (optional)"
    - path: "commands/pd/plan.md"
      provides: "CONVENTIONS.md injection in execution_context"
      contains: "@CONVENTIONS.md (optional)"
  key_links:
    - from: "commands/pd/*.md"
      to: "CONVENTIONS.md"
      via: "@ reference + instruction comment"
      pattern: "@CONVENTIONS\\.md \\(optional\\)"
---

<objective>
Inject CONVENTIONS.md reading instructions into three code-writing skill command files.

Purpose: Enable any AI model (not just Claude) to read and follow project-specific conventions before writing, fixing, or planning code.

Output: Modified command files with injection, passing snapshot tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-skill-injection/152-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject CONVENTIONS.md reading into write-code, fix-bug, plan commands</name>
  <files>commands/pd/write-code.md, commands/pd/fix-bug.md, commands/pd/plan.md</files>
  <read_first>
    - commands/pd/write-code.md (lines 55-65 — execution_context block)
    - commands/pd/fix-bug.md (lines 44-49 — execution_context block)
    - commands/pd/plan.md (lines 45-56 — execution_context block)
    - .planning/phases/152-skill-injection/152-CONTEXT.md (decisions D1, D2, D3)
  </read_first>
  <action>
For each of the three files, inject TWO lines into the `<execution_context>` block, immediately AFTER the `@references/conventions.md (required)` line:

**EXACT TEXT TO INSERT (same for all three files):**
```
@CONVENTIONS.md (optional)
<!-- If CONVENTIONS.md exists at project root, read it before writing code to follow project-specific conventions -->
```

**File-specific insertion points:**

1. **commands/pd/write-code.md** — Insert after line 58 (`@references/conventions.md (required)`):
   - Line 58: `@references/conventions.md (required)`
   - INSERT: `@CONVENTIONS.md (optional)`
   - INSERT: `<!-- If CONVENTIONS.md exists at project root, read it before writing code to follow project-specific conventions -->`
   - Line 59: `@references/prioritization.md (optional)` (continues)

2. **commands/pd/fix-bug.md** — Insert after line 46 (`@references/conventions.md (required)`):
   - Line 46: `@references/conventions.md (required)`
   - INSERT: `@CONVENTIONS.md (optional)`
   - INSERT: `<!-- If CONVENTIONS.md exists at project root, read it before writing code to follow project-specific conventions -->`
   - Line 47: `@references/prioritization.md (optional)` (continues)

3. **commands/pd/plan.md** — Insert after line 51 (`@references/conventions.md (required)`):
   - Line 51: `@references/conventions.md (required)`
   - INSERT: `@CONVENTIONS.md (optional)`
   - INSERT: `<!-- If CONVENTIONS.md exists at project root, read it before writing code to follow project-specific conventions -->`
   - Line 52: `@references/prioritization.md (optional)` (continues)

**IMPORTANT:**
- Keep `@references/conventions.md (required)` — this is the GSD framework conventions (task icons, version rules)
- The new `@CONVENTIONS.md (optional)` is the PROJECT-specific conventions file (different purpose)
- Both references must coexist
  </action>
  <acceptance_criteria>
    - grep -c "@CONVENTIONS.md (optional)" commands/pd/write-code.md returns 1
    - grep -c "@CONVENTIONS.md (optional)" commands/pd/fix-bug.md returns 1
    - grep -c "@CONVENTIONS.md (optional)" commands/pd/plan.md returns 1
    - grep -c "If CONVENTIONS.md exists at project root" commands/pd/write-code.md returns 1
    - grep -c "If CONVENTIONS.md exists at project root" commands/pd/fix-bug.md returns 1
    - grep -c "If CONVENTIONS.md exists at project root" commands/pd/plan.md returns 1
    - grep -c "@references/conventions.md (required)" commands/pd/write-code.md returns 1 (still present)
    - grep -c "@references/conventions.md (required)" commands/pd/fix-bug.md returns 1 (still present)
    - grep -c "@references/conventions.md (required)" commands/pd/plan.md returns 1 (still present)
  </acceptance_criteria>
  <verify>
    <automated>grep -l "@CONVENTIONS.md (optional)" commands/pd/write-code.md commands/pd/fix-bug.md commands/pd/plan.md | wc -l | grep -q "3" && echo "PASS: All 3 files have injection" || echo "FAIL"</automated>
  </verify>
  <done>All three command files have @CONVENTIONS.md (optional) and instruction comment in execution_context block, and original @references/conventions.md (required) is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate snapshots and verify smoke tests pass</name>
  <files>test/snapshots/*.txt</files>
  <read_first>
    - test/generate-snapshots.js (understand snapshot generation)
    - test/smoke-snapshot.test.js (understand test expectations)
    - .planning/phases/151-convention-file-migration/151-01-SUMMARY.md (precedent for snapshot regen)
  </read_first>
  <action>
1. Regenerate snapshots to capture the modified command files:
   ```bash
   node test/generate-snapshots.js
   ```

2. Run smoke snapshot tests to verify consistency:
   ```bash
   node --test test/smoke-snapshot.test.js
   ```

3. If tests fail, inspect diff and ensure the injection lines appear correctly in the snapshots.
  </action>
  <acceptance_criteria>
    - node test/generate-snapshots.js exits with code 0
    - node --test test/smoke-snapshot.test.js exits with code 0
    - git diff --name-only shows changes to test/snapshots/ files
  </acceptance_criteria>
  <verify>
    <automated>node test/generate-snapshots.js && node --test test/smoke-snapshot.test.js</automated>
  </verify>
  <done>Snapshots regenerated and smoke tests pass with the new injection text visible in snapshot output.</done>
</task>

<task type="auto">
  <name>Task 3: Commit changes and update STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state)
    - .planning/REQUIREMENTS.md (CONV-03, CONV-04, CONV-05 to mark complete)
  </read_first>
  <action>
1. Stage and commit all changes:
   ```bash
   git add commands/pd/write-code.md commands/pd/fix-bug.md commands/pd/plan.md test/snapshots/
   git commit -m "feat(152): inject CONVENTIONS.md reading into code-writing skills

   - Add @CONVENTIONS.md (optional) to execution_context in:
     - commands/pd/write-code.md
     - commands/pd/fix-bug.md
     - commands/pd/plan.md
   - Include instruction comment for all AI models
   - Regenerate snapshots
   
   Closes: CONV-03, CONV-04, CONV-05"
   ```

2. Update STATE.md:
   - Change `completed_phases: 1` to `completed_phases: 2`
   - Change `percent: 100` to reflect v12.4 milestone progress
   - Update `Current Position` section to reflect Phase 152 complete
   - Update `Last activity` to current date with Phase 152 completion note
  </action>
  <acceptance_criteria>
    - git log --oneline -1 contains "feat(152)"
    - grep "completed_phases: 2" .planning/STATE.md returns match
    - grep "Phase 152" .planning/STATE.md returns match in Current Position or Last activity
  </acceptance_criteria>
  <verify>
    <automated>git log --oneline -1 | grep -q "feat(152)" && grep -q "completed_phases: 2" .planning/STATE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Changes committed with descriptive message, STATE.md updated to reflect Phase 152 completion and CONV-03/04/05 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This phase modifies internal skill files only; no external input crosses trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-01 | Tampering | CONVENTIONS.md content | accept | File is user-controlled project config; AI reads but doesn't execute |
| T-152-02 | Information Disclosure | conventions content | accept | Conventions are meant to be read by AI; no sensitive data expected |

No security-sensitive code paths are modified. Changes are documentation/prompt injection for AI models.
</threat_model>

<verification>
After all tasks complete:
1. `grep "@CONVENTIONS.md (optional)" commands/pd/*.md` returns 3 matches
2. `grep "If CONVENTIONS.md exists" commands/pd/*.md` returns 3 matches  
3. `node --test test/smoke-snapshot.test.js` passes
4. All three requirement IDs (CONV-03, CONV-04, CONV-05) are satisfied
</verification>

<success_criteria>
- [ ] write-code.md has CONVENTIONS.md injection in execution_context
- [ ] fix-bug.md has CONVENTIONS.md injection in execution_context
- [ ] plan.md has CONVENTIONS.md injection in execution_context
- [ ] Original @references/conventions.md (required) preserved in all files
- [ ] Snapshots regenerated
- [ ] Smoke tests pass
- [ ] Changes committed
- [ ] STATE.md updated
</success_criteria>

<output>
After completion, create `.planning/phases/152-skill-injection/152-01-SUMMARY.md`
</output>
