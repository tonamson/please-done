---
phase: 16-bug-fixes
task: gap-closure
total_tasks: 3
status: gaps_found
last_updated: "2026-03-23T10:37:39.336Z"
---

<current_state>
Phase 16 đã execute xong 4 plans (16-01 → 16-04). Verification phát hiện 3 gaps (score 6/10 truths). User đã chạy `/gsd:plan-phase 16 --gaps` nhưng interrupt trước khi workflow bắt đầu.
</current_state>

<completed_work>

- 16-01: JS module bug fixes — dead exports removed, stale comment fixed, Unicode escapes replaced, plan-check CLI created (commits: a510b62, 5741799, 7409d70)
- 16-02: Workflow text fixes — 8 issues resolved across 6 files (commits: 707d7b3, 6331b56)
- 16-03: Wire missing references + audit comments into 5 skill files (commits: de5beaf, 70893ca)
- 16-04: Snapshot regeneration — 28/48 changed (commit: 7409d70, shared with 16-01)
- Verification completed — 16-VERIFICATION.md created with detailed gap analysis
</completed_work>

<remaining_work>

Gap closure cần 3 fixes (tất cả bắt nguồn từ 1 lỗi gốc):

1. **BLOCKER — write-code.md YAML breakage**: HTML comment `<!-- Audit 2026-03-23 -->` đặt TRONG YAML frontmatter tại line 14 (giữa `- Agent` và `- AskUserQuestion`). Cần di chuyển comment ra SAU closing `---` delimiter.

2. **smoke-integrity.test.js — 2 false-failing tests**:
   - Xóa `test` khỏi `noOptionalSkills` list (test.md giờ có `@context7-pipeline.md (optional)` từ 16-03)
   - Update fix-bug effort routing assertion (16-02 đã xóa haiku/sonnet/opus table, thay bằng sonnet-only note)

3. **Regenerate 48 snapshots** sau khi fix write-code.md — hiện 28/48 fail do YAML breakage
</remaining_work>

<decisions_made>

- Effort routing table removed from fix-bug.md — fix-bug always runs with sonnet
- AskUserQuestion conflict resolved: text first, auto-backup on no response, log action
- FastCode hard STOP replaced with bypass option (Grep/Read fallback)
- All new execution_context references added as (optional) to avoid breaking existing flows
- Audit comments use HTML comment format for markdown files
- Replace Unicode escapes only in generateSkillAdapter scope, not box-drawing chars elsewhere
</decisions_made>

<blockers>
- 1 BLOCKER: I4 HTML comment inside YAML frontmatter of write-code.md → cascades to 31 test failures (28 snapshot + 3 integrity)
</blockers>

<context>
Root cause rất rõ ràng: 1 HTML comment đặt sai vị trí gây cascade failure. Fix đơn giản nhưng cần plan chính thức vì liên quan đến 3 files và snapshot regeneration. BFIX-03 (workflow text fixes) đã VERIFIED hoàn toàn. Chỉ BFIX-01 (partial) và BFIX-02 (failed) cần gap closure.
</context>

<next_action>
Chạy `/gsd:plan-phase 16 --gaps` để tạo gap closure plan từ 16-VERIFICATION.md. Plan sẽ đọc gaps trong VERIFICATION.md và tạo 1 plan nhỏ gọn cho 3 fixes trên.
</next_action>
