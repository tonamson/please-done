---
phase: 16-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/fix-bug.md
  - workflows/new-milestone.md
  - workflows/write-code.md
  - workflows/complete-milestone.md
  - workflows/scan.md
  - workflows/init.md
autonomous: true
requirements:
  - BFIX-03

must_haves:
  truths:
    - "fix-bug.md co row Generic/Khac trong bang stack trace va fallback text truoc bang"
    - "fix-bug.md khong con effort routing table (da xoa), thay bang note ve sonnet"
    - "new-milestone.md line 105 resolve conflict giua Step 3 va rules section"
    - "write-code.md co canh bao RO RANG khi tasks thieu > Files: va post-wave review sub-step"
    - "complete-milestone.md co fallback instruction khi verification tools fail"
    - "scan.md co comment giai thich tai sao .json bi loai tru"
    - "init.md co option continue without FastCode"
  artifacts:
    - path: "workflows/fix-bug.md"
      provides: "Generic stack fallback + cleaned effort routing"
      contains: "Generic/Khac"
    - path: "workflows/new-milestone.md"
      provides: "Resolved AskUserQuestion fallback conflict"
      contains: "hoi van ban thuong"
    - path: "workflows/write-code.md"
      provides: "Explicit parallel degradation warnings"
      contains: "CANH BAO RO RANG"
    - path: "workflows/complete-milestone.md"
      provides: "Verification tool failure fallback"
      contains: "xac minh thu cong"
    - path: "workflows/scan.md"
      provides: "JSON exclusion rationale"
      contains: "config files"
    - path: "workflows/init.md"
      provides: "FastCode bypass option"
      contains: "continue without FastCode"
  key_links:
    - from: "workflows/fix-bug.md"
      to: "commands/pd/fix-bug.md"
      via: "model: sonnet note replacing aspirational table"
      pattern: "fix-bug luon chay voi sonnet"
    - from: "workflows/new-milestone.md"
      to: "rules section (same file)"
      via: "Consistent fallback behavior"
      pattern: "hoi van ban thuong.*theo rules"
---

<objective>
Fix all workflow text issues found in Phase 14 audit and Phase 15 verification: C2/V1 (stack fallback), V2 (effort routing), V3/W12 (AskUserQuestion conflict), V5/W9 (parallel degradation), W11, W14, W15.

Purpose: Resolve all logic gaps in the 6 workflow files so agents receive correct, unambiguous instructions.
Output: 6 corrected workflow markdown files with no remaining logic conflicts or missing fallbacks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-bug-fixes/16-CONTEXT.md
@.planning/phases/16-bug-fixes/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Critical + Warning-High workflow issues (C2/V1, V2, V3/W12, W3)</name>
  <files>workflows/fix-bug.md, workflows/new-milestone.md</files>
  <read_first>
    - workflows/fix-bug.md (lines 130-175 -- stack trace table and effort routing table)
    - workflows/new-milestone.md (lines 100-110 -- AskUserQuestion fallback area, and lines 395-405 -- rules section)
    - commands/pd/new-milestone.md (line 30 -- guard condition for W3)
  </read_first>
  <action>
**fix-bug.md -- C2/V1 (per D-11, D-12):**

Find the stack trace table section (around lines 133-141). The current content starts with:
```
CONTEXT.md -> stack -> doc .planning/rules/[stack].md -> truy vet luong:
```
followed by a 5-row table (NestJS, NextJS, WordPress, Solidity, Flutter).

Replace the line BEFORE the table with:
```
CONTEXT.md -> stack -> doc .planning/rules/[stack].md -> truy vet luong:
Khong xac dinh duoc stack tu CONTEXT.md -> dung luong truy vet generic: entry point -> handler -> business logic -> data layer -> response. Ghi note: "Stack khong xac dinh, dung luong generic."
```

Add a new row at the END of the stack table:
```
| Generic/Khac | entry point -> handler -> business logic -> data layer -> response |
```

**fix-bug.md -- V2 (per D-14):**

Find the effort routing table section (around lines 162-174). This is the table that maps risk classification to effort/model (green->haiku, blue->haiku, yellow->sonnet, orange->sonnet, red->opus).

Delete the ENTIRE effort routing table (the table header, separator, and all rows), plus the line announcing the spawn format like "Spawning {model} agent cho fix-bug ({effort}...)".

Replace with this single note:
```
fix-bug luon chay voi sonnet (theo skill file `commands/pd/fix-bug.md` line 4: `model: sonnet`). Effort routing khong ap dung cho fix-bug -- agent da duoc spawn voi model co dinh truoc khi workflow chay.
```

**new-milestone.md -- V3/W12 (per D-13):**

Find line 105 area. The current content is:
```
- Khong hoi duoc -> tu dong sao luu: .planning/milestones/ -> .planning/milestones_backup_[DD_MM_YYYY]/
```

Replace with:
```
- AskUserQuestion khong kha dung nhu tool -> hoi van ban thuong (theo rules). Nguoi dung khong phan hoi HOAC tool loi ky thuat -> tu dong sao luu: .planning/milestones/ -> .planning/milestones_backup_[DD_MM_YYYY]/. Ghi chu: "Da tu dong sao luu do khong nhan duoc phan hoi."
```

This resolves the conflict between Step 3 (auto backup) and rules line 403 (ask text) by: (1) try text first, (2) auto backup only when no response, (3) log the action.

**new-milestone.md -- W3 (per D-23):**

Find the guard condition around line 30 that says:
```
- [ ] WebSearch kha dung khi can nghien cuu
```
(Note: the actual file may use Vietnamese with diacritics)

Add the error message pattern. Change to:
```
- [ ] WebSearch kha dung khi can nghien cuu -> "WebSearch khong kha dung. Kiem tra ket noi mang."
```
(Preserve any existing diacritics from the original text. The pattern is: add ` -> "error message"` at the end.)
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && grep -c "Generic/Khac" workflows/fix-bug.md && grep -c "luong truy vet generic" workflows/fix-bug.md && grep -c "fix-bug luon chay voi sonnet" workflows/fix-bug.md && grep -c "hoi van ban thuong" workflows/new-milestone.md && grep "WebSearch" commands/pd/new-milestone.md | grep -c '-> "'</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Generic/Khac" workflows/fix-bug.md` returns exactly 1 match (the new table row)
    - `grep "luong truy vet generic" workflows/fix-bug.md` returns at least 1 match (the fallback text)
    - `grep "fix-bug luon chay voi sonnet" workflows/fix-bug.md` returns exactly 1 match
    - `grep -c "green.*haiku\|blue.*haiku\|yellow.*sonnet\|orange.*sonnet\|red.*opus" workflows/fix-bug.md` returns 0 (effort routing table deleted)
    - `grep "hoi van ban thuong" workflows/new-milestone.md` returns at least 1 match
    - `grep "Da tu dong sao luu do khong nhan duoc phan hoi" workflows/new-milestone.md` returns at least 1 match
    - Guard condition in `commands/pd/new-milestone.md` matching `WebSearch` contains `-> "` (error message pattern)
  </acceptance_criteria>
  <done>C2/V1 resolved (generic stack fallback added), V2 resolved (aspirational effort table removed, sonnet note added), V3/W12 resolved (AskUserQuestion fallback conflict fixed), W3 resolved (guard error message added).</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining Warning workflow issues (V5/W9, W11, W14, W15)</name>
  <files>workflows/write-code.md, workflows/complete-milestone.md, workflows/scan.md, workflows/init.md</files>
  <read_first>
    - workflows/write-code.md (lines 114-120 -- parallel mode degradation warning, and lines 362-370 -- post-wave safety net)
    - workflows/complete-milestone.md (lines 60-90 -- Step 3.5 verification logic)
    - workflows/scan.md (lines 12-16 -- glob exclusion area)
    - workflows/init.md (lines 12-20 -- FastCode check area)
  </read_first>
  <action>
**write-code.md -- V5/W9 (per D-15, D-16):**

*Part 1 (D-15) -- around line 118:*
Find the line that says something like:
```
- Task thieu > Files: -> canh bao nhung van cho chay song song (degraded detection)
```

Replace with (preserve Vietnamese diacritics if present in file):
```
- Task thieu `> Files:` -> **CANH BAO RO RANG** truoc khi spawn agents:
  "Task [N] thieu `> Files:` metadata. Conflict detection bi giam cap -- chi phat hien hotspot patterns.
  Khuyen nghi: them `> Files:` vao TASKS.md truoc khi chay --parallel, hoac chap nhan rui ro conflict."
- Hien thi CA danh sach tasks thieu `> Files:` trong wave plan (Buoc 1.5 sub-step 6)
```

*Part 2 (D-16) -- around line 366 (post-wave safety net area):*
Find the post-wave safety net section. After the existing sub-steps (a: git diff, b: conflict check, c: build check, d: OK), add a new sub-step:
```
  b2. Tasks thieu `> Files:` trong wave vua chay -> hien thi:
    "⚠ [N] tasks thieu `> Files:` metadata. Review ky cac files sau day (co the bi conflict khong phat hien): [list files from git diff --name-only]"
```

**complete-milestone.md -- W11 (per D-28):**

Find Step 3.5 verification logic (around lines 62-90). After the verification steps/levels section, add a fallback instruction:
```
Cong cu xac minh that bai (tool error, timeout) -> ghi nhan that bai vao bao cao, tiep tuc voi co xac minh thu cong: "[Level N] -- xac minh thu cong do cong cu that bai."
```

**scan.md -- W14 (per D-30):**

Find line 14-15 area where the glob pattern excludes .json and .css. The line should say something like:
```
Glob **/*.{ts,tsx,js,jsx,py,php,sol,dart,html} (tru ... KHONG gom .json/.css):
```

Add rationale comment right after or inline:
```
(.json loai tru khoi phat hien code vi config files khong chi thi ma nguon dang hoat dong. .css loai tru vi styling khong chi thi logic code.)
```

**init.md -- W15 (per D-31):**

Find the FastCode check area (around line 16) that says something like:
```
- **THAT BAI** -> **DUNG**: "FastCode MCP khong hoat dong..."
```

Change to provide a bypass option:
```
- **THAT BAI** -> Canh bao: "FastCode MCP khong hoat dong. Code search se dung Grep/Read (cham hon)."
  Tiep tuc khong co FastCode -- chuc nang co ban van hoat dong (Grep/Read fallback).
  Hien thi: "Tiep tuc khong co FastCode? (Khuyen nghi: Co)"
```
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && grep -c "CANH BAO RO RANG" workflows/write-code.md && grep -c "b2\." workflows/write-code.md && grep -c "xac minh thu cong" workflows/complete-milestone.md && grep -c "config files" workflows/scan.md && grep -c "Tiep tuc khong co FastCode" workflows/init.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CANH BAO RO RANG" workflows/write-code.md` returns at least 1 match (explicit warning)
    - `grep "b2\." workflows/write-code.md` returns at least 1 match (post-wave sub-step)
    - `grep "xac minh thu cong" workflows/complete-milestone.md` returns at least 1 match (verification fallback)
    - `grep "config files" workflows/scan.md` returns at least 1 match (JSON exclusion rationale)
    - `grep "Tiep tuc khong co FastCode" workflows/init.md` returns at least 1 match (bypass option)
    - All 4 files are valid markdown (no broken formatting)
  </acceptance_criteria>
  <done>V5/W9 resolved (explicit parallel degradation warnings + post-wave review), W11 resolved (verification tool failure fallback), W14 resolved (JSON exclusion rationale), W15 resolved (FastCode bypass option).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 6 workflow files have the expected fixes (grep verification above)
2. No broken markdown formatting in any workflow
3. `node --test test/smoke-*.test.js` still passes (workflow text changes do NOT affect snapshots per D-20)
</verification>

<success_criteria>
- C2/V1 resolved: fix-bug.md has Generic/Khac stack + fallback text
- V2 resolved: fix-bug.md effort routing table removed, sonnet note added
- V3/W12 resolved: new-milestone.md AskUserQuestion fallback conflict fixed
- W3 resolved: new-milestone.md guard has error message
- V5/W9 resolved: write-code.md has explicit warnings + post-wave review
- W11 resolved: complete-milestone.md has verification fallback
- W14 resolved: scan.md has JSON exclusion rationale
- W15 resolved: init.md has FastCode bypass option
</success_criteria>

<output>
After completion, create `.planning/phases/16-bug-fixes/16-02-SUMMARY.md`
</output>
