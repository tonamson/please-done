---
phase: 115
plan_number: 00
name: Test Infrastructure and Fixtures
wave: 0
autonomous: true
depends_on: []
requirements_addressed: [RECON-06, RECON-07]
gap_closure: false
estimated_files: 11
created: 2026-04-05
---

# Plan 115-00: Test Infrastructure and Fixtures

## Objective
Create test infrastructure and fixture files for Phase 115 (Wave 0). Establishes test files and sample fixtures before implementation to satisfy Nyquist validation requirements.

## must_haves
- [ ] Test directory structure exists with test files for all modules
- [ ] Sample fixtures for state machine detection (useState, Redux, XState patterns)
- [ ] Sample fixtures for taint analysis (source-to-sink flows)
- [ ] All test files import the modules they test (placeholder tests)
- [ ] Test files can be run with `node --test`

---

## Task 1: Create Test Directory and Business Logic Test Stub

<files>
- test/lib/business-logic-mapper.test.js
- test/fixtures/sample-state-machines/react-usestate.jsx
- test/fixtures/sample-state-machines/redux-reducer.js
- test/fixtures/sample-state-machines/xstate-machine.js
</files>

<read_first>
- bin/lib/source-mapper.js (pattern for AST analysis tests)
- bin/lib/recon-cache.js (caching patterns)
</read_first>

<action>
Create test/lib/business-logic-mapper.test.js with:
1. Import statement for business-logic-mapper.js (will be created in Plan 115-01)
2. Placeholder test structure using Node.js built-in test runner
3. Test cases covering: useState detection, Redux reducer detection, XState machine detection
4. Each test should import from ../../bin/lib/business-logic-mapper.js
5. Use assert module for assertions
6. Include before/after hooks for test isolation

Create test/fixtures/sample-state-machines/ directory with three fixture files:

**react-usestate.jsx:**
- React component with useState for form status ('idle' | 'submitting' | 'success' | 'error')
- Pattern: const [status, setStatus] = useState('idle')
- State transitions based on form submission
- Export the component for test consumption

**redux-reducer.js:**
- Cart reducer with switch on action.type
- States: 'empty', 'has-items', 'checkout', 'confirmed'
- Actions: ADD_ITEM, REMOVE_ITEM, CHECKOUT, CONFIRM
- Export reducer and action creators

**xstate-machine.js:**
- XState createMachine() for order workflow
- States: pending, processing, shipped, delivered, cancelled
- Transitions with event triggers
- Export the machine definition
</action>

<verify>
<automated>
- test/lib/business-logic-mapper.test.js exists and contains:
  - `import { BusinessLogicMapper } from '../../bin/lib/business-logic-mapper.js'` OR `const { BusinessLogicMapper } = require('../../bin/lib/business-logic-mapper.js')`
  - `import { test } from 'node:test'` OR `const test = require('node:test').test`
  - At least 3 test() calls with descriptive names
- test/fixtures/sample-state-machines/ directory exists with:
  - react-usestate.jsx with useState pattern
  - redux-reducer.js with switch statement for state transitions
  - xstate-machine.js with createMachine
- File count: 4 new files created (1 test + 3 fixtures)
</automated>
</verify>

<done>
**Definition of Done:**
- test/lib/business-logic-mapper.test.js created with proper imports and test structure
- test/fixtures/sample-state-machines/ directory created with 3 state machine fixtures
- Files can be read without errors
</done>

---

## Task 2: Create Taint Engine Test Stubs and Fixture Files

<files>
- test/lib/taint-engine.test.js
- test/lib/sanitization-detector.test.js
- test/lib/data-flow-graph.test.js
- test/fixtures/taint-flows/multi-hop.js
- test/fixtures/taint-flows/sanitized-flow.js
- test/fixtures/taint-flows/object-property.js
</files>

<read_first>
- test/lib/business-logic-mapper.test.js (follow same pattern)
- bin/lib/source-mapper.js (source/sink patterns)
</read_first>

<action>
Create test/lib/taint-engine.test.js with:
1. Import for taint-engine.js
2. Tests for: source detection, sink detection, multi-hop tracking, sanitization detection
3. Test fixtures reference from ../fixtures/taint-flows/
4. Use describe/it or test/subtest pattern
5. Include edge cases: property access, array elements, function returns

Create test/lib/sanitization-detector.test.js with:
1. Import for sanitization-detector.js
2. Tests for: Joi validation, Yup schemas, Zod parsing, manual sanitization
3. Pattern matching tests for common sanitization function names

Create test/lib/data-flow-graph.test.js with:
1. Import for data-flow-graph.js
2. Tests for: DOT format generation, graph structure validation
3. Tests for: node creation, edge creation, path traversal
4. Output validation tests for graphviz compatibility

Create test/fixtures/taint-flows/ directory with three fixture files:

**multi-hop.js:**
- Multi-hop taint: req.body.user.name assigned to variable, passed to function, then to SQL query
- Shows 3+ hop propagation through function calls
- Export vulnerable pattern

**sanitized-flow.js:**
- req.body.email → validator.sanitize() → SQL query (safe)
- Shows sanitization stopping taint propagation
- Export both sanitized and unsanitized variants

**object-property.js:**
- Nested object taint: req.body.data.nested.value → eval
- Array element taint: req.query.items[0] → shell.exec
- Export patterns with property access chains
</action>

<verify>
<automated>
- test/lib/taint-engine.test.js exists with:
  - Import from '../../bin/lib/taint-engine.js'
  - At least 4 test() calls
  - Tests for source detection, sink detection, multi-hop tracking, sanitization
- test/lib/sanitization-detector.test.js exists with import from sanitization-detector.js
- test/lib/data-flow-graph.test.js exists with import from data-flow-graph.js
- test/fixtures/taint-flows/ directory exists with:
  - multi-hop.js with multi-hop propagation
  - sanitized-flow.js with sanitization patterns
  - object-property.js with nested property access
- File count: 6 new files created (3 tests + 3 fixtures)
</automated>
</verify>

<done>
**Definition of Done:**
- All test stubs created with proper structure
- test/fixtures/taint-flows/ directory created with 3 taint flow fixtures
- Test files reference modules that will be created in Plan 115-02
- Fixture files contain representative security patterns for testing
</done>

---

## Task 3: Create Workflow Analyzer Test Stub

<files>
- test/lib/workflow-analyzer.test.js
</files>

<read_first>
- test/lib/business-logic-mapper.test.js (same test structure)
- bin/lib/target-enumerator.js (route patterns for workflow entry points)
- test/fixtures/sample-state-machines/ (fixture patterns from Task 1)
</read_first>

<action>
Create test/lib/workflow-analyzer.test.js with:
1. Import for workflow-analyzer.js
2. Tests for: API chain detection, async flow tracking, transaction boundaries
3. Tests for: Workflow entry point identification, sequence validation
4. Tests for: Business logic flaw detection (missing auth, bypass patterns)
5. Reference fixtures from ../fixtures/sample-state-machines/ (created in Task 1)

Test cases should cover:
- Express route handler with multiple API calls (checkout flow with validation → payment → notification)
- Workflow step sequencing: ensure steps execute in correct order
- Missing auth detection: identify routes accessing sensitive data without preceding auth check
- State machine workflows: use fixtures from Task 1 (redux-reducer.js patterns)
- Async/await chain detection: track promises through multiple async operations
</action>

<verify>
<automated>
- test/lib/workflow-analyzer.test.js exists with:
  - Import from '../../bin/lib/workflow-analyzer.js'
  - At least 3 test() calls for workflow detection
  - Tests for API chain detection and auth gap identification
  - References to ../fixtures/sample-state-machines/ fixtures
- File count: 1 new file created
</automated>
</verify>

<done>
**Definition of Done:**
- test/lib/workflow-analyzer.test.js created with proper structure
- Test references fixtures from Task 1 (sample-state-machines/)
- Test structure follows project conventions
</done>
