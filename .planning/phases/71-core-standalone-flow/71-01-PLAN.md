---
phase: 71-core-standalone-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/pd/test.md
autonomous: true
requirements:
  - TEST-01
  - GUARD-01
  - GUARD-02
  - GUARD-03

must_haves:
  truths:
    - "Standard mode guards are identical to current behavior — CONTEXT.md required, FastCode hard-fail, Context7 hard-fail, task status check"
    - "Standalone mode skips CONTEXT.md guard, skips task status check"
    - "Standalone mode treats FastCode and Context7 as soft warnings with fallback"
    - "argument-hint shows --standalone option to users"
  artifacts:
    - path: "commands/pd/test.md"
      provides: "Conditional guard logic per mode + standalone argument-hint"
      contains: "--standalone"
  key_links:
    - from: "commands/pd/test.md argument-hint"
      to: "workflows/test.md Step 0"
      via: "$ARGUMENTS parsing"
      pattern: "--standalone"
    - from: "commands/pd/test.md guards"
      to: "workflows/test.md routing"
      via: "mode-conditional guard sections"
      pattern: "Standalone mode|Standard mode"
---

<objective>
Update the `pd:test` skill file to support `--standalone` mode via conditional guards and updated frontmatter.

Purpose: Enable `pd:test --standalone [path]` to be recognized as a valid invocation with relaxed guards (no CONTEXT.md, no task status, soft FastCode/Context7 warnings), while keeping standard flow guards byte-for-byte identical.

Output: Modified `commands/pd/test.md` with conditional guard sections and updated argument-hint.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-core-standalone-flow/71-CONTEXT.md
@.planning/phases/71-core-standalone-flow/71-RESEARCH.md

<interfaces>
<!-- Current guard file contents (read-only — these files are NOT modified) -->

From references/guard-context.md:

```markdown
- [ ] `.planning/CONTEXT.md` exists -> "Run `/pd:init` first."
```

From references/guard-fastcode.md:

```markdown
- [ ] FastCode MCP connected successfully -> "Check that Docker is running and FastCode MCP is configured."
```

From references/guard-context7.md:

```markdown
- [ ] Context7 MCP connected successfully -> "Check that Context7 MCP is configured."
- [ ] Context7 MCP working (try resolve-library-id "react") -> "Context7 not responding. Check MCP connection."
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test.md skill file with conditional guards and standalone argument-hint</name>
  <files>commands/pd/test.md</files>

<read_first> - commands/pd/test.md (current state — full file) - references/guard-context.md (guard content to preserve verbatim in standard mode) - references/guard-fastcode.md (guard content to preserve verbatim in standard mode) - references/guard-context7.md (guard content to preserve verbatim in standard mode)
</read_first>

  <action>
Modify `commands/pd/test.md` with these exact changes. All other sections NOT listed below remain unchanged.

**1. Frontmatter — change `argument-hint` only:**

From:

```yaml
argument-hint: "[task number | --all]"
```

To:

```yaml
argument-hint: "[task number | --all | --standalone [path] [--all]]"
```

All other frontmatter fields (`name`, `description`, `model`, `allowed-tools`) remain identical.

**2. `<objective>` section — append standalone description:**

Keep the existing 3 lines of objective text. Append after the existing objective content (before `</objective>`):

```markdown
**Standalone mode** (`--standalone`): Test any module or file without requiring `/pd:init`, milestone, plan, or `/pd:write-code`. Auto-detects tech stack. Creates standalone report in `.planning/reports/`.
```

**3. `<guards>` section — replace content with conditional blocks:**

Replace the ENTIRE content between `<guards>` and `</guards>` with:

```markdown
Stop and instruct the user if any of the following conditions fail:

**If `--standalone` flag is present (standalone mode):**

- [ ] Valid path provided OR `--all` flag used OR prompt user → "Provide a target path or use `--standalone --all`."
- FastCode MCP: check connection → if unavailable, warn "⚠️ FastCode unavailable — using Grep/Read fallback" and CONTINUE (do not stop).
- Context7 MCP: check connection → if unavailable, warn "⚠️ Context7 unavailable — skipping library docs lookup" and CONTINUE (do not stop).

**Otherwise (standard mode — default):**
@references/guard-context.md

- [ ] Valid task number or `--all` flag provided -> "Provide a task number or use `--all`."
      @references/guard-fastcode.md
      @references/guard-context7.md
- [ ] At least one task is in `done` state -> "No completed tasks yet. Run `/pd:write-code` first."
```

CRITICAL: The "Otherwise (standard mode)" block MUST contain the EXACT same lines as the current `<guards>` section — same `@references/guard-*.md` includes, same checklist items, same error messages. This is per D-08: standard flow guards 100% unchanged.

**4. `<context>` section — add standalone context:**

Keep ALL existing content. Append after the existing content (before `</context>`):

```markdown
Standalone mode additional context:

- `--standalone [path]` → test only the specified file or directory (recursive scan)
- `--standalone --all` → test all project source files
- `--standalone` alone (no path, no --all) → prompt user for target
- No `.planning/CONTEXT.md` required — auto-detect tech stack from file markers
```

**5. `<output>` section — add standalone output info:**

Keep ALL existing content. Add before the closing `</output>` tag:

```markdown
**Standalone mode output:**

- `STANDALONE_TEST_REPORT_[YYYYMMDD_HHMMSS].md` in `.planning/reports/`
- Bug reports with `Patch version: standalone` in `.planning/bugs/`
- **Next step:** Fix bugs manually or re-test with `/pd:test --standalone`
```

All other sections (`<execution_context>`, `<process>`, `<rules>`) remain completely unchanged.
</action>

  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && grep -c "\-\-standalone" commands/pd/test.md && grep "Standalone mode" commands/pd/test.md | head -3 && grep "@references/guard-context.md" commands/pd/test.md && grep "@references/guard-fastcode.md" commands/pd/test.md && grep "@references/guard-context7.md" commands/pd/test.md && grep "At least one task" commands/pd/test.md && grep "STANDALONE_TEST_REPORT" commands/pd/test.md</automated>
  </verify>

<acceptance_criteria> - `commands/pd/test.md` line 4 contains `argument-hint: "[task number | --all | --standalone [path] [--all]]"` - `commands/pd/test.md` contains `**If \`--standalone\` flag is present (standalone mode):**`    -`commands/pd/test.md`contains`**Otherwise (standard mode — default):\*\*`    -`commands/pd/test.md`contains`@references/guard-context.md`(standard mode guard preserved)
    -`commands/pd/test.md`contains`@references/guard-fastcode.md`(standard mode guard preserved)
    -`commands/pd/test.md`contains`@references/guard-context7.md`(standard mode guard preserved)
    -`commands/pd/test.md`contains`At least one task is in \`done\` state`(standard mode guard preserved)
    -`commands/pd/test.md`contains`FastCode unavailable — using Grep/Read fallback`(standalone soft warning)
    -`commands/pd/test.md`contains`Context7 unavailable — skipping library docs lookup`(standalone soft warning)
    -`commands/pd/test.md`contains`STANDALONE_TEST_REPORT`in output section
    -`commands/pd/test.md`contains`Patch version: standalone`in output section
    -`<execution_context>`section is unchanged (still references`@workflows/test.md`)
    - `<process>`section is unchanged (still says`Execute @workflows/test.md`)
    - `<rules>` section is unchanged (4 rules, all in English)
</acceptance_criteria>

  <done>
    Skill file recognizes `--standalone` as a valid argument. Standard mode guards are identical to current behavior. Standalone mode has conditional soft-warning guards for FastCode/Context7 and skips CONTEXT.md + task status checks.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "guard-context.md" commands/pd/test.md` returns `1` (still referenced in standard mode)
2. `grep -c "guard-fastcode.md" commands/pd/test.md` returns `1` (still referenced in standard mode)
3. `grep -c "guard-context7.md" commands/pd/test.md` returns `1` (still referenced in standard mode)
4. `grep "argument-hint" commands/pd/test.md` shows `--standalone`
5. `grep -c "Standalone mode" commands/pd/test.md` returns at least `2` (objective + guards)
6. `grep "Patch version: standalone" commands/pd/test.md` matches (output section)
</verification>

<success_criteria>

- `commands/pd/test.md` has dual-mode guard structure (standard + standalone)
- Standard mode guard lines are byte-for-byte identical to current file
- Standalone mode has soft warnings for FastCode/Context7
- argument-hint includes `--standalone [path] [--all]`
  </success_criteria>

<output>
After completion, create `.planning/phases/71-core-standalone-flow/71-01-SUMMARY.md`
</output>
