---
phase: 155-gsd-independence-audit-cleanup
plan: 01
type: execute
wave: 1
status: done
depends_on: []
files_modified:
  - test/smoke-integrity.test.js
  - test/smoke-plan-checker.test.js
  - test/smoke-prompt.test.js
  - test/smoke-utils.test.js
  - test/platform-models.test.js
  - test/snapshots/codex/discover.md
  - test/snapshots/codex/health.md
  - test/snapshots/codex/stats.md
  - test/snapshots/codex/sync-version.md
  - test/snapshots/copilot/discover.md
  - test/snapshots/copilot/health.md
  - test/snapshots/copilot/stats.md
  - test/snapshots/copilot/sync-version.md
  - test/snapshots/gemini/discover.md
  - test/snapshots/gemini/health.md
  - test/snapshots/gemini/stats.md
  - test/snapshots/gemini/sync-version.md
  - test/snapshots/opencode/discover.md
  - test/snapshots/opencode/health.md
  - test/snapshots/opencode/stats.md
  - test/snapshots/opencode/sync-version.md
  - commands/pd/discover.md
  - commands/pd/health.md
  - commands/pd/stats.md
  - commands/pd/sync-version.md
  - workflows/audit.md
autonomous: true
requirements: [GSDC-01, GSDC-02, GSDC-03]

must_haves:
  truths:
    - "Zero GSD/get-shit-done references exist in non-.planning source files"
    - "All 56 smoke-integrity tests pass"
    - "Platform count tests pass with 11 platforms"
    - "Plan checker historical path tests pass"
    - "Snapshot tests pass for all 4 platforms × 4 skills"
    - "Test suite failure count does not regress from baseline"
  artifacts:
    - path: "commands/pd/discover.md"
      provides: "Added <execution_context> section (required canonical structure)"
    - path: "commands/pd/health.md"
      provides: "Added <execution_context> section (required canonical structure)"
    - path: "commands/pd/stats.md"
      provides: "Added <execution_context> section (required canonical structure)"
    - path: "commands/pd/sync-version.md"
      provides: "Added <execution_context> section (required canonical structure)"
    - path: "test/smoke-integrity.test.js"
      provides: "ALLOWED_NO_WORKFLOW expanded: +audit, +discover, +health, +stats, +sync-version"
  key_links:
    - from: "test/smoke-integrity.test.js"
      to: "commands/pd/*.md"
      via: "listSkillFiles()"
      pattern: "ALLOWED_NO_WORKFLOW|REQUIRED_SECTIONS"
---

<objective>
Audit all non-.planning source files for GSD references, confirm zero exist, fix pre-existing test failures surfaced during audit, and ensure test suite has zero regressions from the GSD independence state.
</objective>

<action>
## Step 1: GSD Reference Scan

Scan all non-.planning files for GSD references:

```bash
grep -rn "GSD\|get-shit-done\|gsd-" . \
  --include="*.md" --include="*.js" --include="*.json" --include="*.sh" \
  --exclude-dir=".planning" --exclude-dir="node_modules" \
  --exclude-dir=".git"
```

Expected: zero results (already cleaned in prior commit e29895f).

## Step 2: Fix Stale Platform Count Tests

Tests expect 7 platforms but `bin/lib/platforms.js` now has 11:
- `test/platform-models.test.js` line ~168: `7 → 11`
- `test/smoke-utils.test.js` line ~363: `"has all 7 platforms" → "has all 11 platforms"`, count `7 → 11`
- `test/smoke-prompt.test.js` line ~40-52: `"all 7 platforms" → "all 11 platforms"`, count `7 → 11`
- `test/platform-models.test.js` windsurf dirName: `.windsurf → .codeium/windsurf`

## Step 3: Fix Plan Checker Historical Path

`test/smoke-plan-checker.test.js`: HISTORICAL_PLANS path prefix:
`.planning/phases/ → .planning/milestones/v11.2-phases/`

## Step 4: Fix Skill Structure (execution_context)

Add `<execution_context>` section between `</context>` and `<process>` in:
- `commands/pd/discover.md`
- `commands/pd/health.md`
- `commands/pd/stats.md`
- `commands/pd/sync-version.md`

Content pattern: `No external workflow needed — pd:<name> executes inline using bin/lib/<lib>.js.`

## Step 5: Update Integrity Test Whitelist

In `test/smoke-integrity.test.js` line 29:
```js
// Before:
const ALLOWED_NO_WORKFLOW = new Set(["fetch-doc", "update"]);
// After:
const ALLOWED_NO_WORKFLOW = new Set(["audit", "discover", "fetch-doc", "health", "stats", "sync-version", "update"]);
```

## Step 6: Remove Orphaned Workflow

`workflows/audit.md` is an old security-scan workflow. `commands/pd/audit.md` was repurposed to audit-trail viewer and does NOT reference this file. Remove it:
```bash
git rm workflows/audit.md
```

## Step 7: Regenerate Snapshots

After adding `<execution_context>` to 4 skills:
```bash
node test/generate-snapshots.js
```

## Step 8: Verify Test Suite

```bash
npm test 2>&1 | grep "ℹ \(pass\|fail\)"
```

Expected: ~1825 pass, ≤21 fail (pre-existing integration failures only).
</action>

<verification>
Run after execution:
```bash
npm test 2>&1 | grep "ℹ \(pass\|fail\)"
node --test test/smoke-integrity.test.js 2>&1 | grep "ℹ \(pass\|fail\)"
grep -rn "GSD\|get-shit-done" . --include="*.md" --include="*.js" --exclude-dir=".planning" --exclude-dir="node_modules" --exclude-dir=".git"
```
</verification>
