---
phase: 057-reference-dedup-runtime-dry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/verification.md
  - references/verification-patterns.md
  - references/plan-checker.md
  - bin/lib/utils.js
  - bin/lib/plan-checker.js
  - test/baseline-tokens.json
  - workflows/plan.md
  - workflows/write-code.md
  - workflows/complete-milestone.md
  - templates/plan.md
  - templates/verification-report.md
autonomous: true
requirements: [DEDU-01, DEDU-02]

must_haves:
  truths:
    - "references/verification.md tồn tại với nội dung từ cả 2 file gốc"
    - "references/verification-patterns.md và references/plan-checker.md đã bị xóa"
    - "Không còn reference nào trỏ tới verification-patterns.md hoặc plan-checker.md trong source files"
    - "Snapshot tests pass sau khi regenerate"
  artifacts:
    - path: "references/verification.md"
      provides: "Merged verification reference (patterns + checker rules)"
      contains: "Phan 1: Mau xac minh"
    - path: "references/verification.md"
      provides: "Plan checker rules section"
      contains: "Phan 2: Plan Checker Rules"
  key_links:
    - from: "bin/lib/utils.js"
      to: "references/verification.md"
      via: "CONDITIONAL_LOADING_MAP key"
      pattern: "references/verification\\.md"
    - from: "bin/lib/plan-checker.js"
      to: "references/verification.md"
      via: "JSDoc reference"
      pattern: "references/verification\\.md"
    - from: "workflows/plan.md"
      to: "references/verification.md"
      via: "@reference"
      pattern: "@references/verification\\.md"
---

<objective>
Gộp 2 reference files (verification-patterns.md + plan-checker.md) thành 1 file verification.md, cập nhật toàn bộ references trong source files, regenerate snapshots.

Purpose: Giảm reference fragmentation — 2 file liên quan verification nằm tách rời gây confuse và tăng context load không cần thiết.
Output: 1 file verification.md thay thế 2 file cũ, zero broken references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@references/verification-patterns.md
@references/plan-checker.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gộp verification-patterns.md + plan-checker.md thành verification.md</name>
  <files>references/verification.md, references/verification-patterns.md, references/plan-checker.md</files>
  <read_first>
    - references/verification-patterns.md (file gốc 1 — 147 dòng, mẫu phát hiện stub/placeholder)
    - references/plan-checker.md (file gốc 2 — 320 dòng, plan checker rules spec)
  </read_first>
  <action>
Tạo file `references/verification.md` với cấu trúc sau (per D-01, D-02):

```
# Verification Reference

> Gộp từ: verification-patterns.md + plan-checker.md
> Đọc bởi: write-code (Bước 9.5), complete-milestone (Bước 3.5), plan-checker.js

## Phần 1: Mẫu xác minh — Phát hiện stub và placeholder

[Toàn bộ nội dung từ verification-patterns.md, BỎ dòng tiêu đề gốc (dòng 1: "# Mẫu xác minh...") và dòng mô tả ("> Dùng bởi...")]

---

## Phần 2: Plan Checker Rules

[Toàn bộ nội dung từ plan-checker.md, BỎ dòng tiêu đề gốc (dòng 1: "# Plan Checker Rules") và dòng mô tả ("> Nguon su that...")]
```

Sau khi tạo xong `verification.md`, XÓA 2 file gốc:
- `references/verification-patterns.md`
- `references/plan-checker.md`

Kiểm tra file mới có khoảng 460-480 dòng (147 + 320 - headers + section separators).
  </action>
  <verify>
    <automated>test -f references/verification.md && ! test -f references/verification-patterns.md && ! test -f references/plan-checker.md && grep -q "Phan 1" references/verification.md && grep -q "Phan 2" references/verification.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - references/verification.md tồn tại và chứa chuỗi "Phần 1: Mẫu xác minh" (hoặc "Phan 1: Mau xac minh")
    - references/verification.md chứa chuỗi "Phần 2: Plan Checker Rules" (hoặc "Phan 2: Plan Checker Rules")
    - references/verification-patterns.md KHÔNG còn tồn tại
    - references/plan-checker.md KHÔNG còn tồn tại
    - references/verification.md có >= 400 dòng (merged content)
  </acceptance_criteria>
  <done>File verification.md tồn tại với nội dung đầy đủ từ cả 2 file gốc, 2 file cũ đã xóa</done>
</task>

<task type="auto">
  <name>Task 2: Cập nhật toàn bộ references và regenerate snapshots</name>
  <files>bin/lib/utils.js, bin/lib/plan-checker.js, test/baseline-tokens.json, workflows/plan.md, workflows/write-code.md, workflows/complete-milestone.md, templates/plan.md, templates/verification-report.md</files>
  <read_first>
    - bin/lib/utils.js (xem line ~254 CONDITIONAL_LOADING_MAP key cần đổi)
    - bin/lib/plan-checker.js (xem line ~10 JSDoc reference cần đổi)
    - test/baseline-tokens.json (xem entry verification-patterns.md cần đổi key + values)
    - workflows/plan.md (xem line ~215 @reference cần đổi)
    - workflows/write-code.md (xem lines 16, 162, 344, 356 references cần đổi)
    - workflows/complete-milestone.md (xem lines 13, 31, 76 references cần đổi)
    - templates/plan.md (xem line ~147 @reference cần đổi)
    - templates/verification-report.md (xem line ~63 @reference cần đổi)
  </read_first>
  <action>
Cập nhật references trong 8 source files (per D-03, D-06, D-07, D-08):

1. **bin/lib/utils.js** line ~254 — Đổi CONDITIONAL_LOADING_MAP key:
   - TỪ: `'references/verification-patterns.md'`
   - THÀNH: `'references/verification.md'`

2. **bin/lib/plan-checker.js** line ~10 — Đổi JSDoc reference:
   - TỪ: `* Rules spec: references/plan-checker.md`
   - THÀNH: `* Rules spec: references/verification.md`

3. **test/baseline-tokens.json** — Đổi entry key:
   - XÓA key: `"references/verification-patterns.md": { "tokens": 1918, "lines": 151 }`
   - THÊM key: `"references/verification.md": { "tokens": <tính lại>, "lines": <tính lại> }`
   - Chạy `node bin/count-tokens.js references/verification.md` để lấy giá trị chính xác
   - Nếu `plan-checker.md` có entry riêng thì xóa luôn

4. **workflows/plan.md** line ~215:
   - TỪ: `@references/verification-patterns.md`
   - THÀNH: `@references/verification.md`

5. **workflows/write-code.md** lines 16, 162, 344, 356 — Tất cả 4 chỗ:
   - TỪ: `verification-patterns.md`
   - THÀNH: `verification.md`

6. **workflows/complete-milestone.md** lines 13, 31, 76 — Tất cả 3 chỗ:
   - TỪ: `verification-patterns.md`
   - THÀNH: `verification.md`

7. **templates/plan.md** line ~147:
   - TỪ: `@references/verification-patterns.md`
   - THÀNH: `@references/verification.md`

8. **templates/verification-report.md** line ~63:
   - TỪ: `@references/verification-patterns.md`
   - THÀNH: `@references/verification.md`

KHÔNG sửa: `.planning/` files (D-05), `test/snapshots/` (D-04).

Sau khi cập nhật tất cả source files, regenerate snapshots:
```bash
node test/generate-snapshots.js
```

Chạy full smoke tests:
```bash
node --test test/smoke-integrity.test.js test/smoke-snapshot.test.js
```
  </action>
  <verify>
    <automated>grep -rn "verification-patterns\.md\|plan-checker\.md" bin/lib/ workflows/ templates/ test/baseline-tokens.json 2>/dev/null | grep -v node_modules | grep -v ".planning/" | head -5; node --test test/smoke-integrity.test.js test/smoke-snapshot.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "verification-patterns.md" bin/lib/ workflows/ templates/ test/baseline-tokens.json` trả về 0 kết quả
    - `grep -rn "plan-checker.md" bin/lib/ workflows/ templates/` trả về 0 kết quả
    - bin/lib/utils.js chứa chuỗi `'references/verification.md'` trong CONDITIONAL_LOADING_MAP
    - bin/lib/plan-checker.js chứa chuỗi `references/verification.md` trong JSDoc
    - test/baseline-tokens.json chứa key `"references/verification.md"` và KHÔNG chứa key `"references/verification-patterns.md"`
    - `node --test test/smoke-integrity.test.js` exit code 0
    - `node --test test/smoke-snapshot.test.js` exit code 0
  </acceptance_criteria>
  <done>Zero broken references trong source files, snapshots regenerated, smoke tests pass</done>
</task>

</tasks>

<verification>
- `references/verification.md` tồn tại với nội dung merged đầy đủ
- `references/verification-patterns.md` và `references/plan-checker.md` đã xóa
- `grep -rn "verification-patterns.md\|plan-checker.md" bin/lib/ workflows/ templates/ test/baseline-tokens.json` trả về 0 kết quả
- `node --test test/smoke-*.test.js` tất cả pass
</verification>

<success_criteria>
- verification.md tồn tại, 2 file cũ đã xóa (DEDU-01)
- Zero broken references trong toàn bộ source files (DEDU-02)
- Smoke tests + snapshot tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/057-reference-dedup-runtime-dry/057-01-SUMMARY.md`
</output>
