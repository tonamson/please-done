---
plan: 145-02
phase: 145
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/platforms.js
autonomous: true
requirements:
  - INSTALL-04

must_haves:
  truths:
    - "Each platform entry has a description field"
    - "Descriptions are one-line summaries per D-10"
  artifacts:
    - path: "bin/lib/platforms.js"
      provides: "Platform definitions with description field"
      contains: "description:"
  key_links:
    - from: "promptRuntime() in prompt.js"
      to: "PLATFORMS[rt].description"
      via: "property access"
      pattern: "PLATFORMS\\[.*\\]\\.description"
---

<objective>
Add `description` field to all 7 platform entries in `bin/lib/platforms.js`.

Purpose: Enable the numbered platform selector (Wave 2) to display one-line descriptions alongside platform names.
Output: Modified `bin/lib/platforms.js` with `description` field in each PLATFORMS entry.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/145-installer-prompt-ux/145-CONTEXT.md
@.planning/phases/145-installer-prompt-ux/145-RESEARCH.md

<interfaces>
<!-- Current PLATFORMS structure (to be modified) -->

From bin/lib/platforms.js (lines 44-115):
```javascript
const PLATFORMS = {
  claude: {
    name: 'Claude Code',
    // ADD: description: 'AI-powered dev assistant by Anthropic',
    dirName: '.claude',
    commandPrefix: '/pd:',
    // ...other fields
  },
  codex: {
    name: 'Codex CLI',
    // ADD: description: "OpenAI's terminal coding agent",
    // ...
  },
  // ... 5 more platforms
};
```

Each entry currently has: `name`, `dirName`, `commandPrefix`, `commandSeparator`, `envVar`, `skillFormat`, `frontmatterFormat`, `toolMap`.

**D-10 specifies exact descriptions:**
- Claude Code → "AI-powered dev assistant by Anthropic"
- Codex CLI → "OpenAI's terminal coding agent"
- Gemini CLI → "Google's AI coding assistant"
- OpenCode → "Open-source AI coding agent"
- GitHub Copilot → "GitHub's AI pair programmer"
- Cursor → "AI-first code editor"
- Windsurf → "Agentic IDE by Codeium"
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Static data addition, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-03 | (N/A) | platforms.js | accept | Static strings only, no user input, no new attack surface |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add description field to all 7 platform entries</name>
  <files>bin/lib/platforms.js</files>
  <read_first>
    - bin/lib/platforms.js (see PLATFORMS object at lines 44-115)
  </read_first>
  <action>
Add `description` field to each of the 7 platform entries in the PLATFORMS object. Place `description` immediately after `name` in each entry for consistency.

**Exact changes (per D-09, D-10):**

1. **claude** (after line 46 `name: 'Claude Code',`):
```javascript
    description: 'AI-powered dev assistant by Anthropic',
```

2. **codex** (after line 55 `name: 'Codex CLI',`):
```javascript
    description: "OpenAI's terminal coding agent",
```

3. **gemini** (after line 64 `name: 'Gemini CLI',`):
```javascript
    description: "Google's AI coding assistant",
```

4. **opencode** (after line 74 `name: 'OpenCode',`):
```javascript
    description: 'Open-source AI coding agent',
```

5. **copilot** (after line 84 `name: 'GitHub Copilot',`):
```javascript
    description: "GitHub's AI pair programmer",
```

6. **cursor** (after line 94 `name: 'Cursor',`):
```javascript
    description: 'AI-first code editor',
```

7. **windsurf** (after line 104 `name: 'Windsurf',`):
```javascript
    description: 'Agentic IDE by Codeium',
```

All other fields remain unchanged.
  </action>
  <verify>
    <automated>node -e "const p = require('./bin/lib/platforms'); const keys = Object.keys(p.PLATFORMS); const allHaveDesc = keys.every(k => typeof p.PLATFORMS[k].description === 'string' && p.PLATFORMS[k].description.length > 0); console.log(allHaveDesc ? 'PASS' : 'FAIL'); process.exit(allHaveDesc ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/platforms.js contains `description:` for all 7 platforms (claude, codex, gemini, opencode, copilot, cursor, windsurf)
    - `grep -c "description:" bin/lib/platforms.js` returns 7
    - PLATFORMS.claude.description === 'AI-powered dev assistant by Anthropic'
    - PLATFORMS.codex.description === "OpenAI's terminal coding agent"
    - PLATFORMS.gemini.description === "Google's AI coding assistant"
    - PLATFORMS.opencode.description === 'Open-source AI coding agent'
    - PLATFORMS.copilot.description === "GitHub's AI pair programmer"
    - PLATFORMS.cursor.description === 'AI-first code editor'
    - PLATFORMS.windsurf.description === 'Agentic IDE by Codeium'
  </acceptance_criteria>
  <done>All 7 platform entries have description field with exact text from D-10</done>
</task>

</tasks>

<verification>
1. `grep -c "description:" bin/lib/platforms.js` — should return 7
2. `node -e "const p = require('./bin/lib/platforms'); console.log(p.PLATFORMS.claude.description)"` — should print "AI-powered dev assistant by Anthropic"
3. `node -e "const p = require('./bin/lib/platforms'); Object.keys(p.PLATFORMS).forEach(k => console.log(k + ': ' + p.PLATFORMS[k].description))"` — should list all 7 descriptions
</verification>

<success_criteria>
- [ ] All 7 platform entries in PLATFORMS have a `description` field
- [ ] Descriptions match exactly the text specified in D-10
- [ ] `description` field is placed immediately after `name` field in each entry
- [ ] No other changes to the PLATFORMS object structure
</success_criteria>

<output>
After completion, create `.planning/phases/145-installer-prompt-ux/145-02-SUMMARY.md`
</output>
