---
phase: 120-code-libraries
plan: '02'
type: execute
wave: '1'
depends_on: []
files_modified:
  - bin/lib/evasion-engine.js
  - bin/lib/evasion-engine.test.js
autonomous: true
requirements:
  - LIB-03
must_haves:
  truths:
    - "evasion-engine.js provides timing attack evasion techniques"
    - "evasion-engine.js provides sleep/bypass evasion methods"
    - "evasion-engine.js provides rate limiting evasion"
    - "evasion-engine.js returns structured results with MITRE ATT&CK mappings"
  artifacts:
    - path: bin/lib/evasion-engine.js
      provides: Red Team evasion techniques
      exports: EvasionEngine class, timingBypass, sleepJitter, rateLimitEvade, detectEvasion
    - path: bin/lib/evasion-engine.test.js
      provides: Unit tests for EvasionEngine
      min_tests: 8
  key_links:
    - from: bin/lib/evasion-engine.js
      to: bin/lib/recon-cache.js
      via: optional cache injection via constructor
      pattern: options.cache
---

<objective>
Create evasion-engine.js (LIB-03) - Red Team evasion techniques for timing attacks, sleep/bypass evasion, and rate limiting evasion. Follow the established library pattern (constructor with cache, analyze method, structured results).
</objective>

<context>
@bin/lib/recon-cache.js (pattern reference - constructor with cache, analyze method, structured results)
@bin/lib/payloads.js (pattern reference - MITRE ATT&CK T1027.010, evasion techniques)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create evasion-engine.js library</name>
  <files>bin/lib/evasion-engine.js</files>
  <action>
Create bin/lib/evasion-engine.js with the following structure and functions:

**Class: EvasionEngine**
- Constructor accepts `{ cache }` for token optimization (matches existing pattern)
- Main `analyze(target)` method returns structured results
- MITRE ATT&CK mappings included

**Exported Functions:**
1. `timingBypass(options)` - Timing attack evasion. Options: `{ baseDelay, jitter, method }`. Methods: 'random', 'jitter', 'adaptive'. Returns `{ delay, nextDelay, method, formula }`

2. `sleepJitter(delayMs, jitterPercent)` - Add random jitter to sleep time. Returns `{ original, jittered, variance }`

3. `rateLimitEvade(requestCount, timeWindow, strategy)` - Rate limiting evasion. Strategies: 'exponential_backoff', 'jitter', 'spread', 'burst_then_wait'. Returns `{ strategy, effectiveRate, nextRequestIn, recommendations }`

4. `detectEvasionTechnique(payload)` - Detect if a payload uses evasion. Returns `{ evasions: [{type, description, mitre}], score, isEvasion }`

5. `generateEvasionVariants(payload, attackType)` - Generate evasion variants of a payload. Attack types: 'xss', 'sqli', 'command', 'ssrf'. Returns `[{ variant, technique, description }]`

6. `analyzeTarget(target)` - Main analysis of target's evasion countermeasures. Returns `{ evasionScore, techniques: [], recommendations: [], summary }`

**MITRE ATT&CK mappings:**
- T1027: Obfuscated Files or Information
- T1027.010: Obfuscated Payloads - Command obfuscation techniques
- T1499.002: Endpoint Denial of Service - Service exhaustion
- T1565: Manipulate Master Boot Record (timing-related patterns)

**Pattern:** Follow existing library pattern from payloads.js and token-analyzer.js - JSDoc comments, "use strict", structured results, MITRE references in docstring.
  </action>
  <verify>
    <automated>node -e "const ee = require('./bin/lib/evasion-engine'); console.log('EvasionEngine loaded:', typeof ee.EvasionEngine === 'function' ? 'OK' : 'FAIL')"</automated>
  </verify>
  <done>
EvasionEngine class exported, all 6 functions available, MITRE ATT&CK mappings in docstring
</done>
</task>

<task type="auto">
  <name>Task 2: Create evasion-engine.test.js</name>
  <files>bin/lib/evasion-engine.test.js</files>
  <action>
Create bin/lib/evasion-engine.test.js following the test pattern from recon-cache.test.js:

**Test Pattern:** IIFE wrapper, assert() function, ok/FAIL console output

**Tests to implement:**
1. `timingBypass - random method` - Call with { baseDelay: 100, method: 'random' }, verify returns delay > 0
2. `timingBypass - jitter method` - Call with { baseDelay: 100, jitter: 20, method: 'jitter' }, verify delay varies
3. `sleepJitter - basic` - Call with 1000ms and 10% jitter, verify result.jittered within range
4. `rateLimitEvade - exponential backoff` - Call with strategy 'exponential_backoff', verify recommendations array
5. `rateLimitEvade - spread` - Call with strategy 'spread', verify effectiveRate is reduced
6. `detectEvasionTechnique - no evasion` - Call with plain "alert(1)", verify isEvasion false
7. `detectEvasionTechnique - with evasion` - Call with "alert/**/(1)", verify isEvasion true
8. `EvasionEngine class - constructor and analyze` - Create instance, call analyze, verify result structure

Use tmp directory for any file operations. Follow the `assert(name, condition)` pattern from existing tests.
  </action>
  <verify>
    <automated>node bin/lib/evasion-engine.test.js 2>&1 | grep -c "ok:"</automated>
  </verify>
  <done>
All 8 tests pass with "ok:" output
</done>
</task>

</tasks>

<verification>
- [ ] evasion-engine.js loads without errors
- [ ] All exported functions present (EvasionEngine, timingBypass, sleepJitter, rateLimitEvade, detectEvasion, generateEvasionVariants, analyzeTarget)
- [ ] Test file runs and all tests pass
</verification>

<success_criteria>
- LIB-03 (evasion-engine.js) created and tested
- All functions have JSDoc comments
- MITRE ATT&CK mappings in docstring
- Test coverage for all exported functions
</success_criteria>

<output>
After completion, create `.planning/phases/120-code-libraries/120-02-SUMMARY.md`
</output>
