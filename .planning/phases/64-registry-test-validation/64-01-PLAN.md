---
phase: 64-registry-test-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/smoke-agent-files.test.js
autonomous: true
requirements: [VALD-01, VALD-02, VALD-03]

must_haves:
  truths:
    - "AGENT_NAMES chứa 16 agents bao gồm pd-sec-fixer và pd-sec-reporter"
    - "Smoke test pass cho tất cả 16 agents"
    - "AGENT_REGISTRY đã có 16 entries (đã sẵn từ trước)"
  artifacts:
    - path: "test/smoke-agent-files.test.js"
      provides: "AGENT_NAMES với 16 agents, test descriptions cập nhật"
      contains: "pd-sec-fixer"
  key_links:
    - from: "test/smoke-agent-files.test.js"
      to: "bin/lib/resource-config.js"
      via: "AGENT_REGISTRY lookup per agent name"
      pattern: "AGENT_REGISTRY\\[name\\]"
    - from: "test/smoke-agent-files.test.js"
      to: "commands/pd/agents/"
      via: "AGENTS_DIR file reads"
      pattern: "join.*AGENTS_DIR"
---

<objective>
Thêm pd-sec-fixer và pd-sec-reporter vào AGENT_NAMES trong smoke test, cập nhật test counts từ 14→16, đảm bảo tất cả 16 agents pass validation.

Purpose: Hoàn tất kiểm thử toàn bộ 16 agents sau consolidation.
Output: Smoke test đầy đủ cho 16 agents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-format-standardization/63-01-SUMMARY.md

@test/smoke-agent-files.test.js
@bin/lib/resource-config.js
</context>

<interfaces>
<!-- Current AGENT_NAMES (14 entries, missing pd-sec-fixer and pd-sec-reporter) -->
From test/smoke-agent-files.test.js line 19-34:
```javascript
const AGENT_NAMES = [
  'pd-bug-janitor',
  'pd-code-detective',
  'pd-doc-specialist',
  'pd-repro-engineer',
  'pd-fix-architect',
  'pd-evidence-collector',
  'pd-fact-checker',
  'pd-codebase-mapper',
  'pd-security-researcher',
  'pd-feature-analyst',
  'pd-research-synthesizer',
  'pd-planner',
  'pd-regression-analyzer',
  'pd-sec-scanner',
];
```

<!-- AGENT_REGISTRY already has 16 entries including pd-sec-fixer and pd-sec-reporter -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Thêm 2 security agents vào AGENT_NAMES + cập nhật test counts</name>
  <files>test/smoke-agent-files.test.js</files>
  <action>
    1. Thêm `'pd-sec-fixer'` và `'pd-sec-reporter'` vào mảng AGENT_NAMES (sau `'pd-sec-scanner'`).
       AGENT_NAMES phải có 16 entries.

    2. Cập nhật test description ở dòng 92: `'14 agent files'` → `'16 agent files'`

    3. Cập nhật comment ở dòng 3: `'14 agent files'` → `'16 agent files'`

    4. KHÔNG thay đổi bất kỳ logic test nào khác — chỉ thêm names và cập nhật counts.
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && node --test test/smoke-agent-files.test.js 2>&1 | tail -10</automated>
  </verify>
  <done>AGENT_NAMES có 16 entries, tất cả tests pass, counts hiển thị 16</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-agent-files.test.js` — tất cả tests pass (0 fail)
- `grep -c 'pd-sec-fixer' test/smoke-agent-files.test.js` ≥ 1
- `grep -c 'pd-sec-reporter' test/smoke-agent-files.test.js` ≥ 1
</verification>

<success_criteria>
- AGENT_NAMES chứa 16 agents
- Smoke test pass 100% cho tất cả 16 agents
- AGENT_REGISTRY đã có 16 entries (VALD-01 verified)
</success_criteria>

<output>
After completion, create `.planning/phases/64-registry-test-validation/64-01-SUMMARY.md`
</output>
