---
phase: 27-dong-bo-logic-bao-cao
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/report-filler.js
  - bin/lib/logic-sync.js
  - test/smoke-logic-sync.test.js
autonomous: true
requirements: [LOGIC-01, RPT-01, PM-01]

must_haves:
  truths:
    - "detectLogicChanges(diffText) tra hasLogicChange=true khi diff co condition/arithmetic/endpoint/database signals"
    - "detectLogicChanges(diffText) tra hasLogicChange=false khi diff chi co whitespace/comment"
    - "updateReportDiagram(params) cap nhat Mermaid block trong report content co san"
    - "suggestClaudeRules(params) extract bug patterns va kiem tra trung lap voi claudeContent"
    - "runLogicSync(params) non-blocking — throw tu sub-function chi tao warning, KHONG crash"
  artifacts:
    - path: "bin/lib/logic-sync.js"
      provides: "Module pure functions: detectLogicChanges, updateReportDiagram, suggestClaudeRules, runLogicSync"
      exports: ["detectLogicChanges", "updateReportDiagram", "suggestClaudeRules", "runLogicSync"]
    - path: "test/smoke-logic-sync.test.js"
      provides: "Unit tests cho 4 functions trong logic-sync.js"
      min_lines: 80
    - path: "bin/lib/report-filler.js"
      provides: "Export them replaceMermaidBlock"
      exports: ["fillManagementReport", "replaceMermaidBlock"]
  key_links:
    - from: "bin/lib/logic-sync.js"
      to: "bin/lib/generate-diagrams.js"
      via: "require('./generate-diagrams')"
      pattern: "generateBusinessLogicDiagram"
    - from: "bin/lib/logic-sync.js"
      to: "bin/lib/report-filler.js"
      via: "require('./report-filler')"
      pattern: "replaceMermaidBlock"
    - from: "bin/lib/logic-sync.js"
      to: "runLogicSync orchestrator"
      via: "try/catch per sub-function"
      pattern: "warnings\\.push"
---

<objective>
Tao module logic-sync.js voi 4 pure functions (detectLogicChanges, updateReportDiagram, suggestClaudeRules, runLogicSync) va export replaceMermaidBlock tu report-filler.js. TDD: test truoc, code sau.

Purpose: Cung cap tat ca business logic cho Phase 27 — phat hien thay doi logic, cap nhat report diagram, de xuat rules. Module nay se duoc goi boi workflow fix-bug o Plan 02.
Output: bin/lib/logic-sync.js, test/smoke-logic-sync.test.js, report-filler.js cap nhat exports
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-dong-bo-logic-bao-cao/27-CONTEXT.md
@.planning/phases/27-dong-bo-logic-bao-cao/27-RESEARCH.md

<interfaces>
<!-- Key types va contracts executor can. Extracted tu codebase. -->
<!-- Executor dung truc tiep — KHONG can kham pha codebase. -->

Tu bin/lib/report-filler.js (HIEN TAI chi export fillManagementReport):
```javascript
// replaceMermaidBlock la INTERNAL function — CAN THEM VAO module.exports
function replaceMermaidBlock(content, sectionPrefix, newDiagram) { ... }
// content: string markdown, sectionPrefix: '## 3.' hoac '## 4.', newDiagram: Mermaid text (khong bao gom fence)
// Returns: string — content da thay the Mermaid block

module.exports = { fillManagementReport }; // <-- CAN THEM replaceMermaidBlock
```

Tu bin/lib/generate-diagrams.js:
```javascript
function generateBusinessLogicDiagram(planContents, options = {}) { ... }
// planContents: Array<{planNumber: number, content: string, phase: string}>
// Returns: { diagram: string, valid: boolean, errors: Array, warnings: Array, truthCount: number, planCount: number }

module.exports = { generateBusinessLogicDiagram, generateArchitectureDiagram };
```

Tu bin/lib/debug-cleanup.js (PATTERN MAU cho module moi):
```javascript
'use strict';
// JSDoc cho moi function
// Input validation: throw Error khi thieu tham so bat buoc
// Pure function: nhan content, tra ket qua, KHONG doc file
module.exports = { scanDebugMarkers, matchSecurityWarnings };
```

Tu test/smoke-debug-cleanup.test.js (PATTERN MAU cho test):
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
// describe blocks theo function name
// Happy path + edge cases + error handling
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export replaceMermaidBlock + tao test smoke-logic-sync.test.js (RED phase)</name>
  <files>bin/lib/report-filler.js, test/smoke-logic-sync.test.js</files>
  <read_first>
    bin/lib/report-filler.js
    bin/lib/debug-cleanup.js
    test/smoke-debug-cleanup.test.js
    .planning/phases/27-dong-bo-logic-bao-cao/27-RESEARCH.md
  </read_first>
  <behavior>
    - detectLogicChanges(diffText) tra hasLogicChange=true va signals array khi diff co dong `+` chua if/else/switch (condition signal)
    - detectLogicChanges(diffText) tra hasLogicChange=true khi diff co dong `+` chua Math.floor/parseInt/threshold (arithmetic signal)
    - detectLogicChanges(diffText) tra hasLogicChange=true khi diff co dong `+` chua router.get/app.post/endpoint (endpoint signal)
    - detectLogicChanges(diffText) tra hasLogicChange=true khi diff co dong `+` chua SELECT/INSERT/.query()/.findOne (database signal)
    - detectLogicChanges(diffText) tra hasLogicChange=false khi diff chi co whitespace/comment/import changes
    - detectLogicChanges(null) throw Error voi message chua 'diffText'
    - detectLogicChanges('') throw Error voi message chua 'diffText'
    - updateReportDiagram({reportContent, planContents}) tra updatedContent co Mermaid block moi
    - updateReportDiagram({reportContent: null}) throw Error voi message chua 'report'
    - updateReportDiagram skip khi generateBusinessLogicDiagram tra diagram rong
    - suggestClaudeRules({sessionContent, bugReportContent, claudeContent}) tra {suggestions, reasoning}
    - suggestClaudeRules({}) tra suggestions=[] va reasoning chua 'du lieu'
    - suggestClaudeRules kiem tra trung lap: khong de xuat rule da co trong claudeContent
    - runLogicSync({diffText, ...}) goi detectLogicChanges va tra logicResult
    - runLogicSync non-blocking: khi detectLogicChanges throw, warnings chua error message, logicResult=null
    - runLogicSync chi goi updateReportDiagram khi hasLogicChange=true
  </behavior>
  <action>
    1. Sua bin/lib/report-filler.js: them replaceMermaidBlock vao module.exports (per D-11 — Pitfall 1 tu RESEARCH.md):
       ```javascript
       module.exports = { fillManagementReport, replaceMermaidBlock };
       ```
       Chi sua DONG NAY, khong thay doi gi khac.

    2. Tao test/smoke-logic-sync.test.js theo pattern test/smoke-debug-cleanup.test.js:
       - `'use strict'`
       - Import `{ describe, it }` tu `node:test`, `assert` tu `node:assert/strict`
       - Import `{ detectLogicChanges, updateReportDiagram, suggestClaudeRules, runLogicSync }` tu `../bin/lib/logic-sync`

       Cac test groups:

       **describe('detectLogicChanges — happy path'):**
       - Test condition signal: diff co `+  if (x > 0) {` → hasLogicChange=true, signals[0].type='condition'
       - Test arithmetic signal: diff co `+  const total = Math.floor(price * 1.1);` → hasLogicChange=true, signals[0].type='arithmetic'
       - Test endpoint signal: diff co `+  app.post('/api/users', handler);` → hasLogicChange=true, signals[0].type='endpoint'
       - Test database signal: diff co `+  const user = await db.findOne({email});` → hasLogicChange=true, signals[0].type='database'
       - Test summary format: signals.length > 0 → summary chua signal count va types

       **describe('detectLogicChanges — no logic change'):**
       - Test whitespace only: diff co `+  ` (whitespace), `+// comment` → hasLogicChange=false
       - Test import only: diff co `+const fs = require('fs');` → hasLogicChange=false
       - Test dong khong co `+` prefix: `  if (x > 0)` (context line) → khong match

       **describe('detectLogicChanges — file tracking'):**
       - Test currentFile: diff co `+++ b/src/app.js` → signals[0].file='src/app.js'

       **describe('detectLogicChanges — error handling'):**
       - Test null → throw Error chua 'diffText'
       - Test undefined → throw Error chua 'diffText'
       - Test '' → throw Error chua 'diffText'

       **describe('updateReportDiagram — happy path'):**
       - Test cap nhat: reportContent co `## 3. Business Logic` + mermaid block → updatedContent co mermaid block moi
       - Test skip: reportContent KHONG co section `## 3.` → updatedContent giu nguyen

       **describe('updateReportDiagram — error handling'):**
       - Test null reportContent → throw Error chua 'report'

       **describe('suggestClaudeRules — happy path'):**
       - Test co sessionContent + bugReportContent → tra {suggestions: Array, reasoning: string}
       - Test trung lap: claudeContent da co rule → suggestions KHONG chua rule do

       **describe('suggestClaudeRules — no data'):**
       - Test khong co session + bug → suggestions=[], reasoning chua 'du lieu'

       **describe('runLogicSync — orchestrator'):**
       - Test happy path: diffText co logic change → logicResult.hasLogicChange=true
       - Test non-blocking: diffText null → warnings.length > 0, logicResult=null
       - Test conditional RPT-01: hasLogicChange=false → reportResult=null (khong goi updateReportDiagram)

    3. Chay test — PHAI FAIL vi logic-sync.js chua ton tai:
       `node --test test/smoke-logic-sync.test.js`
       Ket qua mong doi: Error "Cannot find module '../bin/lib/logic-sync'"
  </action>
  <verify>
    <automated>node -e "const m = require('./bin/lib/report-filler'); if (!m.replaceMermaidBlock) throw new Error('replaceMermaidBlock not exported')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'replaceMermaidBlock' bin/lib/report-filler.js` >= 2 (function definition + export)
    - `grep 'module.exports' bin/lib/report-filler.js` chua 'replaceMermaidBlock'
    - `wc -l test/smoke-logic-sync.test.js` >= 80
    - `grep -c 'describe(' test/smoke-logic-sync.test.js` >= 8 (8 describe blocks)
    - `grep -c "it('" test/smoke-logic-sync.test.js` >= 15 (15+ test cases)
    - `grep 'detectLogicChanges' test/smoke-logic-sync.test.js` co ket qua
    - `grep 'updateReportDiagram' test/smoke-logic-sync.test.js` co ket qua
    - `grep 'suggestClaudeRules' test/smoke-logic-sync.test.js` co ket qua
    - `grep 'runLogicSync' test/smoke-logic-sync.test.js` co ket qua
  </acceptance_criteria>
  <done>
    report-filler.js export them replaceMermaidBlock. Test file smoke-logic-sync.test.js da tao voi 15+ test cases covering 4 functions. Test FAIL vi module chua ton tai (RED phase hoan tat).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tao module logic-sync.js (GREEN + REFACTOR phase)</name>
  <files>bin/lib/logic-sync.js</files>
  <read_first>
    test/smoke-logic-sync.test.js
    bin/lib/debug-cleanup.js
    bin/lib/report-filler.js
    bin/lib/generate-diagrams.js
    .planning/phases/27-dong-bo-logic-bao-cao/27-RESEARCH.md
  </read_first>
  <action>
    Tao bin/lib/logic-sync.js theo pattern bin/lib/debug-cleanup.js (per D-02, D-07):
    - `'use strict'`
    - JSDoc cho moi function
    - Pure functions: nhan content string, tra ket qua object, KHONG doc file, KHONG chay git commands

    **1. Constants — Heuristic signal patterns (per D-05):**
    ```javascript
    const SIGNALS = {
      condition: {
        pattern: /^\+.*\b(if|else|switch|case|default|\?)\b/,
        label: 'Condition change',
      },
      arithmetic: {
        pattern: /^\+.*(\b\d+\b.*[+\-*/%<>=!]+|\b(Math\.|parseInt|parseFloat|Number|\.toFixed|\.ceil|\.floor)\b)/,
        label: 'Arithmetic/threshold change',
      },
      endpoint: {
        pattern: /^\+.*\b(router\.|app\.(get|post|put|delete|patch)|@(Get|Post|Put|Delete|Patch|Controller)|fetch\(|axios\.|\.route\(|endpoint|api\/)/i,
        label: 'Endpoint/API change',
      },
      database: {
        pattern: /^\+.*\b(SELECT|INSERT|UPDATE|DELETE|CREATE|ALTER|DROP|\.query\(|\.execute\(|\.findOne|\.findMany|\.create\(|\.update\(|\.delete\(|schema\.|migration|\.model\()/i,
        label: 'Database/query change',
      },
    };
    ```

    **2. detectLogicChanges(diffText) (per D-05, D-06, D-07):**
    - Input validation: if (!diffText || typeof diffText !== 'string') throw Error chua 'diffText'
    - Parse tung dong: track currentFile qua `+++ b/(.+)`, chi scan dong bat dau voi `+` (khong phai `+++`)
    - Moi dong match 1 signal (break sau match dau tien)
    - Return: `{ hasLogicChange: boolean, signals: Array<{type, file, detail}>, summary: string }`
    - summary format: `"N logic signal(s): type1, type2"` hoac `"Khong phat hien thay doi logic"`

    **3. updateReportDiagram(params) (per D-09, D-10, D-11, D-12):**
    - Require: `const { generateBusinessLogicDiagram } = require('./generate-diagrams');`
    - Require: `const { replaceMermaidBlock } = require('./report-filler');`
    - Input: `{ reportContent: string, planContents: Array }`
    - Validation: if (!params.reportContent) throw Error chua 'report'
    - Goi generateBusinessLogicDiagram(params.planContents || [])
    - Neu diagramResult.diagram rong hoac !diagramResult.valid → return { updatedContent: params.reportContent, diagramResult }
    - Goi replaceMermaidBlock(params.reportContent, '## 3.', diagramResult.diagram)
    - Return: `{ updatedContent: string, diagramResult: object }`

    **4. suggestClaudeRules(params) (per D-15, D-16, D-17):**
    - Input: `{ sessionContent: string, bugReportContent: string, claudeContent: string }`
    - Neu khong co sessionContent VA khong co bugReportContent → return { suggestions: [], reasoning: 'Khong co du lieu de phan tich' }
    - Extract bug patterns tu content:
      - Tim "Nguyen nhan"/"Root cause"/"Nguyên nhân" section → extract root cause text
      - Tim "Phan loai"/"Phân loại" → extract risk category
    - Generate 1-2 rule suggestions dua tren patterns:
      - Format: bat dau voi "- ", 1-2 dong (per D-17)
    - Kiem tra trung lap: moi suggestion, check claudeContent co chua text tuong tu khong (case-insensitive substring match)
    - Filter ra suggestions da ton tai
    - Return: `{ suggestions: string[], reasoning: string }`

    **5. runLogicSync(params) — orchestrator (per D-14, D-20):**
    - Input: `{ diffText, bugReportContent, sessionContent, planContents, reportContent, claudeContent }`
    - Non-blocking pipeline — moi sub-step trong try/catch rieng:
      ```javascript
      const warnings = [];
      let logicResult = null, reportResult = null, rulesResult = null;

      // 1. LOGIC-01
      try { logicResult = detectLogicChanges(params.diffText); }
      catch (e) { warnings.push('Logic detection: ' + e.message); }

      // 2. RPT-01 (chi khi hasLogicChange)
      if (logicResult?.hasLogicChange) {
        try { reportResult = updateReportDiagram(params); }
        catch (e) { warnings.push('Report update: ' + e.message); }
      }

      // 3. PM-01
      try { rulesResult = suggestClaudeRules(params); }
      catch (e) { warnings.push('Rule suggestion: ' + e.message); }

      return { logicResult, reportResult, rulesResult, warnings };
      ```

    **6. Exports:**
    ```javascript
    module.exports = { detectLogicChanges, updateReportDiagram, suggestClaudeRules, runLogicSync };
    ```

    Sau khi tao xong, chay tests:
    `node --test test/smoke-logic-sync.test.js`
    Tat ca tests PHAI PASS (GREEN phase).

    Refactor neu can: rut gon code, cai thien naming, nhung KHONG thay doi behavior.
  </action>
  <verify>
    <automated>node --test test/smoke-logic-sync.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/smoke-logic-sync.test.js` — 0 failures
    - `grep 'module.exports' bin/lib/logic-sync.js` chua 'detectLogicChanges, updateReportDiagram, suggestClaudeRules, runLogicSync'
    - `grep -c 'function ' bin/lib/logic-sync.js` >= 4 (4 functions chinh)
    - `grep "'use strict'" bin/lib/logic-sync.js` co ket qua
    - `grep 'require.*generate-diagrams' bin/lib/logic-sync.js` co ket qua
    - `grep 'require.*report-filler' bin/lib/logic-sync.js` co ket qua
    - `grep 'replaceMermaidBlock' bin/lib/logic-sync.js` co ket qua
    - `npm test` — toan bo test suite PASS (bao gom tests hien co)
  </acceptance_criteria>
  <done>
    Module logic-sync.js tao thanh cong voi 4 exported functions. Tat ca smoke tests pass. npm test toan bo pass. TDD RED-GREEN-REFACTOR hoan tat.
  </done>
</task>

</tasks>

<verification>
1. `node --test test/smoke-logic-sync.test.js` — tat ca tests pass
2. `npm test` — full suite pass (bao gom 120+ tests hien co)
3. `node -e "const m = require('./bin/lib/report-filler'); console.log(Object.keys(m))"` — output chua replaceMermaidBlock
4. `node -e "const m = require('./bin/lib/logic-sync'); console.log(Object.keys(m))"` — output chua 4 functions
</verification>

<success_criteria>
- Module logic-sync.js ton tai voi 4 exported pure functions
- replaceMermaidBlock duoc export tu report-filler.js
- Tat ca unit tests trong smoke-logic-sync.test.js pass
- Full test suite npm test pass khong break gi
- Code theo pure function pattern (khong doc file, khong chay git)
- Non-blocking orchestrator: throw chi tao warning
</success_criteria>

<output>
After completion, create `.planning/phases/27-dong-bo-logic-bao-cao/27-01-SUMMARY.md`
</output>
