---
phase: 27-dong-bo-logic-bao-cao
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - workflows/fix-bug.md
  - test/snapshots/codex/fix-bug.md
  - test/snapshots/copilot/fix-bug.md
  - test/snapshots/gemini/fix-bug.md
  - test/snapshots/opencode/fix-bug.md
autonomous: true
requirements: [LOGIC-01, RPT-01, PM-01]

must_haves:
  truths:
    - "Workflow fix-bug.md co Buoc 10a goi runLogicSync() tu bin/lib/logic-sync.js"
    - "Buoc 10a nam SAU Buoc 10 (xac nhan da sua) va TRUOC process/rules sections"
    - "Buoc 10a toan bo non-blocking — moi sub-step trong try/catch"
    - "Workflow duoi 450 dong (D-04 limit cho 7 tinh nang moi)"
    - "4 platform snapshots phan anh noi dung Buoc 10a"
    - "Toan bo test suite pass bao gom snapshot tests"
  artifacts:
    - path: "workflows/fix-bug.md"
      provides: "Buoc 10a: Dong bo Logic & Bao cao"
      contains: "logic-sync"
    - path: "test/snapshots/codex/fix-bug.md"
      provides: "Codex platform snapshot cap nhat"
    - path: "test/snapshots/copilot/fix-bug.md"
      provides: "Copilot platform snapshot cap nhat"
    - path: "test/snapshots/gemini/fix-bug.md"
      provides: "Gemini platform snapshot cap nhat"
    - path: "test/snapshots/opencode/fix-bug.md"
      provides: "Opencode platform snapshot cap nhat"
  key_links:
    - from: "workflows/fix-bug.md"
      to: "bin/lib/logic-sync.js"
      via: "Buoc 10a instructions goi runLogicSync()"
      pattern: "logic-sync"
    - from: "workflows/fix-bug.md"
      to: "bin/generate-pdf-report.js"
      via: "Buoc 10a RPT-01 hoi re-render PDF"
      pattern: "generate-pdf-report"
---

<objective>
Chen Buoc 10a vao workflow fix-bug.md — dong bo logic, cap nhat report diagram, de xuat rules. Regenerate 4 platform snapshots.

Purpose: Hoan thanh integration 3 tinh nang cuoi cung vao workflow fix-bug, cho phep AI tu dong phat hien logic changes, cap nhat diagram, va de xuat rules sau khi fix bug.
Output: Workflow fix-bug.md cap nhat, 4 platform snapshots moi
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-dong-bo-logic-bao-cao/27-CONTEXT.md
@.planning/phases/27-dong-bo-logic-bao-cao/27-01-SUMMARY.md

<interfaces>
<!-- Workflow hien tai va module can tham chieu -->

Tu workflows/fix-bug.md (vi tri chen — dong 362-383):
```markdown
## Buoc 10: Yeu cau xac nhan
> "Da sua [mo ta]. Vui long kiem tra va xac nhan."

### User xac nhan DA SUA:
- Bao cao: Trang thai -> Da giai quyet, tick checklist
- Phien dieu tra: `Da giai quyet`
- TASKS.md: dung version GOC ...
- HAS_GIT:
...
git commit -m '[LOI] Xac nhan da khac phuc [tom tat]'

### User bao CHUA SUA:
- Thu thap them (trieu chung moi?)
...
- **TIEP TUC cho den khi user xac nhan**

</process>
```

Vi tri chen Buoc 10a: SAU dong `### User xac nhan DA SUA:` block va git commit (dong ~373),
TRUOC dong `### User bao CHUA SUA:` (dong ~375).

Tu bin/lib/logic-sync.js (Plan 01 output):
```javascript
module.exports = { detectLogicChanges, updateReportDiagram, suggestClaudeRules, runLogicSync };
// runLogicSync({diffText, bugReportContent, sessionContent, planContents, reportContent, claudeContent})
// Returns: { logicResult, reportResult, rulesResult, warnings }
```

Tu bin/generate-pdf-report.js:
```
node bin/generate-pdf-report.js [path-to-report.md]
```

Sub-step wiring pattern (tu Phase 25-04 va Phase 26-02):
- Chen sub-step vao buoc hien co, KHONG tao buoc so moi (per D-21)
- Sub-step numbering: 10a (giong 9a, 5b.1, 8a)
- Blocking = false cho pipeline nay (per D-14, khac voi 5b.1/8a la blocking)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chen Buoc 10a vao workflow fix-bug.md</name>
  <files>workflows/fix-bug.md</files>
  <read_first>
    workflows/fix-bug.md
    .planning/phases/27-dong-bo-logic-bao-cao/27-CONTEXT.md
    .planning/phases/27-dong-bo-logic-bao-cao/27-RESEARCH.md
    .planning/phases/26-don-dep-an-toan/26-02-SUMMARY.md
  </read_first>
  <action>
    Chen Buoc 10a vao workflow fix-bug.md. Vi tri: SAU block "User xac nhan DA SUA" (sau dong git commit xac nhan, TRUOC "User bao CHUA SUA"). Per D-19: chi co y nghia sau khi user confirm bug da fix. Per D-20: tat ca 3 features trong 1 sub-step. Per D-21: chen vao buoc hien co, KHONG tao buoc so moi.

    Noi dung Buoc 10a (toi da 15-20 dong them, per D-03):

    ```markdown
    ### 10a. Dong bo logic va bao cao (non-blocking)

    > Toan bo buoc nay la non-blocking — loi chi tao warning, KHONG chan workflow.

    **1. Logic detection (LOGIC-01):**
    `git diff HEAD~1` → goi `detectLogicChanges(diffText)` tu `bin/lib/logic-sync.js`
    - Ghi BUG report: "Thay doi logic: CO/KHONG" + signals (neu co)

    **2. Report update (RPT-01) — CHI KHI hasLogicChange = CO:**
    Glob `.planning/reports/*.md` + `.planning/milestones/*/` → file moi nhat theo mtime
    - Khong co report → warning: "Khong tim thay report de cap nhat"
    - Co → goi `updateReportDiagram({reportContent, planContents})` → ghi file
    - Hoi: "Cap nhat lai PDF? (Y/n)" → Y: `node bin/generate-pdf-report.js [path]`

    **3. Rule suggestion (PM-01):**
    Doc SESSION + BUG report + CLAUDE.md → goi `suggestClaudeRules({sessionContent, bugReportContent, claudeContent})`
    - Co de xuat → hien: "[De xuat rule] ..." → hoi "Them vao CLAUDE.md? (Y/n)"
    - Y → append rules vao cuoi CLAUDE.md → `git add CLAUDE.md && git commit -m '[LOI] Them rule tu post-mortem'`
    ```

    SAU KHI CHEN: dem dong `wc -l workflows/fix-bug.md`. PHAI duoi 450 dong (per D-04).
    Neu qua 450 dong: rut gon text trong Buoc 10a, giu cac instruction thiet yeu.

    LUU Y:
    - Giu tat ca noi dung hien co NGUYEN VEN — chi THEM Buoc 10a
    - KHONG sua bat ky buoc nao khac
    - Sub-step 10a dat GIUA "User xac nhan DA SUA" block va "User bao CHUA SUA" block
  </action>
  <verify>
    <automated>wc -l workflows/fix-bug.md | awk '{if ($1 > 450) exit 1; else print "OK: " $1 " dong"}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep '10a' workflows/fix-bug.md` co ket qua
    - `grep 'logic-sync' workflows/fix-bug.md` co ket qua
    - `grep 'detectLogicChanges' workflows/fix-bug.md` co ket qua
    - `grep 'updateReportDiagram' workflows/fix-bug.md` co ket qua
    - `grep 'suggestClaudeRules' workflows/fix-bug.md` co ket qua
    - `grep 'non-blocking' workflows/fix-bug.md` co ket qua (it nhat 1 lan trong 10a)
    - `grep 'generate-pdf-report' workflows/fix-bug.md` co ket qua
    - `wc -l workflows/fix-bug.md` <= 450
    - `grep -c 'CHUA SUA' workflows/fix-bug.md` >= 1 (block "User bao CHUA SUA" van con)
    - `grep -c 'DA SUA' workflows/fix-bug.md` >= 1 (block "User xac nhan DA SUA" van con)
  </acceptance_criteria>
  <done>
    Workflow fix-bug.md co Buoc 10a voi 3 sub-features (logic detection, report update, rule suggestion). Tong so dong duoi 450. Tat ca noi dung hien co giu nguyen.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate 4 platform snapshots va verify full test suite</name>
  <files>
    test/snapshots/codex/fix-bug.md
    test/snapshots/copilot/fix-bug.md
    test/snapshots/gemini/fix-bug.md
    test/snapshots/opencode/fix-bug.md
  </files>
  <read_first>
    test/smoke-snapshot.test.js
    test/snapshots/codex/fix-bug.md
    workflows/fix-bug.md
    .planning/phases/26-don-dep-an-toan/26-02-SUMMARY.md
  </read_first>
  <action>
    Regenerate 4 platform snapshots cho fix-bug workflow (giong Phase 25-04 va Phase 26-02):

    1. Doc workflows/fix-bug.md (da cap nhat voi Buoc 10a)
    2. Chay 4 converters de tao snapshot moi:
       ```bash
       node -e "
         const fs = require('fs');
         const path = require('path');
         const ROOT = path.resolve('.');
         const content = fs.readFileSync('commands/pd/fix-bug/SKILL.md', 'utf8');

         const codex = require('./bin/lib/converters/codex');
         const copilot = require('./bin/lib/converters/copilot');
         const gemini = require('./bin/lib/converters/gemini');
         const opencode = require('./bin/lib/converters/opencode');

         fs.writeFileSync('test/snapshots/codex/fix-bug.md', codex.convertSkill(content, 'fix-bug', ROOT));
         fs.writeFileSync('test/snapshots/copilot/fix-bug.md', copilot.convertSkill(content, true, ROOT));
         fs.writeFileSync('test/snapshots/gemini/fix-bug.md', gemini.convertSkill(content, ROOT));
         fs.writeFileSync('test/snapshots/opencode/fix-bug.md', opencode.convertSkill(content, ROOT));
         console.log('4 snapshots regenerated');
       "
       ```

    3. Verify snapshot tests pass:
       `node --test test/smoke-snapshot.test.js`

    4. Verify logic-sync tests van pass:
       `node --test test/smoke-logic-sync.test.js`

    5. Verify full test suite:
       `npm test`
       Tat ca tests PHAI pass — 0 failures.

    6. Dem dong workflow lan cuoi:
       `wc -l workflows/fix-bug.md`
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` — 0 failures
    - `node --test test/smoke-snapshot.test.js` — 0 failures (bao gom 4 fix-bug snapshots)
    - `node --test test/smoke-logic-sync.test.js` — 0 failures
    - `grep '10a' test/snapshots/codex/fix-bug.md` co ket qua (snapshot phan anh Buoc 10a)
    - `grep '10a' test/snapshots/copilot/fix-bug.md` co ket qua
    - `grep '10a' test/snapshots/gemini/fix-bug.md` co ket qua
    - `grep '10a' test/snapshots/opencode/fix-bug.md` co ket qua
    - `wc -l workflows/fix-bug.md` <= 450
  </acceptance_criteria>
  <done>
    4 platform snapshots regenerate thanh cong, phan anh Buoc 10a. Full test suite pass. Workflow duoi 450 dong. Phase 27 hoan tat — v1.5 milestone ship ready.
  </done>
</task>

</tasks>

<verification>
1. `npm test` — full suite pass (bao gom snapshot + logic-sync + tat ca tests hien co)
2. `wc -l workflows/fix-bug.md` <= 450
3. `grep '10a' workflows/fix-bug.md` — Buoc 10a ton tai
4. `grep 'logic-sync' workflows/fix-bug.md` — reference toi module
5. 4 snapshots co noi dung Buoc 10a
</verification>

<success_criteria>
- Workflow fix-bug.md co Buoc 10a day du 3 sub-features (logic detection, report update, rule suggestion)
- Buoc 10a non-blocking — loi chi tao warning
- Workflow duoi 450 dong
- 4 platform snapshots cap nhat phan anh Buoc 10a
- Toan bo test suite pass — 0 failures
- Phase 27 hoan thanh — tat ca 7 tinh nang v1.5 da tich hop vao workflow fix-bug
</success_criteria>

<output>
After completion, create `.planning/phases/27-dong-bo-logic-bao-cao/27-02-SUMMARY.md`
</output>
