---
phase: 06-context7-standardization
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - workflows/write-code.md
  - workflows/plan.md
  - workflows/fix-bug.md
  - workflows/test.md
  - workflows/new-milestone.md
autonomous: true
requirements: [LIBR-01]

must_haves:
  truths:
    - "All 4 workflow files (write-code, plan, fix-bug, test) reference @references/context7-pipeline.md"
    - "No workflow contains stack-specific Context7 rules (antd, Guard/JWT, nestjs)"
    - "No workflow contains silent fallback pattern (knowledge san)"
    - "new-milestone agent contracts retain Context7 tools with pipeline awareness note"
    - "All Plan 01 TDD RED tests turn GREEN"
  artifacts:
    - path: "workflows/write-code.md"
      provides: "Refactored Context7 section referencing pipeline"
      contains: "context7-pipeline"
    - path: "workflows/plan.md"
      provides: "Refactored Context7 section referencing pipeline"
      contains: "context7-pipeline"
    - path: "workflows/fix-bug.md"
      provides: "Refactored Context7 section referencing pipeline"
      contains: "context7-pipeline"
    - path: "workflows/test.md"
      provides: "New Context7 section referencing pipeline"
      contains: "context7-pipeline"
    - path: "workflows/new-milestone.md"
      provides: "Agent contract with Context7 pipeline awareness"
      contains: "context7-pipeline"
  key_links:
    - from: "workflows/write-code.md"
      to: "references/context7-pipeline.md"
      via: "@references/context7-pipeline.md"
      pattern: "context7-pipeline"
    - from: "workflows/plan.md"
      to: "references/context7-pipeline.md"
      via: "@references/context7-pipeline.md"
      pattern: "context7-pipeline"
    - from: "workflows/fix-bug.md"
      to: "references/context7-pipeline.md"
      via: "@references/context7-pipeline.md"
      pattern: "context7-pipeline"
    - from: "workflows/test.md"
      to: "references/context7-pipeline.md"
      via: "@references/context7-pipeline.md"
      pattern: "context7-pipeline"
---

<objective>
Refactor Context7 sections in all 5 workflow files to reference the canonical pipeline, removing ad-hoc patterns, stack-specific rules, and silent fallbacks.

Purpose: Make all 5 skills follow the same Context7 invocation pattern via a single source of truth, turning Plan 01 TDD RED tests GREEN.
Output: 5 updated workflow files, all smoke tests GREEN
</objective>

<execution_context>
@/Users/tonamson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-context7-standardization/06-CONTEXT.md
@.planning/phases/06-context7-standardization/06-RESEARCH.md
@.planning/phases/06-context7-standardization/06-01-SUMMARY.md

<interfaces>
<!-- Pipeline reference file created in Plan 01 -->
From references/context7-pipeline.md:
```markdown
# Context7 Pipeline

> Dung boi: write-code, plan, new-milestone, fix-bug, test
> Nguon su that cho quy trinh tra cuu thu vien ngoai

## Khi nao
BAT KY task nao su dung thu vien ngoai -> TU DONG tra cuu, KHONG can user yeu cau.

## Quy trinh 2 buoc
### Buoc 1: Resolve
`resolve-library-id` cho TUNG thu vien -> lay ID.
Nhieu thu vien -> resolve TAT CA truoc khi query.

### Buoc 2: Query
`query-docs` voi ID da resolve -> docs dung version.

## Xu ly loi
Context7 loi hoac khong co -> **DUNG** task, thong bao user:
1. Tiep tuc khong docs — chap nhan rui ro API sai
2. Dung, sua Context7 roi chay lai
3. Dung /pd:fetch-doc tai docs thu cong

KHONG am tham tiep tuc khi Context7 khong kha dung.
```

<!-- Current Context7 in write-code.md Buoc 3 (lines 131-137) to REPLACE: -->
```markdown
**Context7** (task dung thu vien ngoai):
1. `resolve-library-id` -> lay ID
2. `query-docs` -> docs dung version
- TU DONG tra cuu -- KHONG can user yeu cau
- Admin (antd): BAT BUOC tra Context7 moi component moi
- Guard/JWT/Role: BAT BUOC tra Context7 (nestjs) + FastCode pattern
- Context7 khong co -> `.planning/docs/` hoac knowledge san
```

<!-- Current Context7 in plan.md Buoc 3B (line 103) to REPLACE: -->
```markdown
**Context7** (uu tien -- CAO): `resolve-library-id` -> `query-docs`. TU DONG tra cuu. Khong co -> `.planning/docs/` hoac knowledge.
```

<!-- Current Context7 in fix-bug.md Buoc 4 (line 92) to REPLACE: -->
```markdown
**Loi lien quan thu vien** -> Context7: `resolve-library-id` -> `query-docs` -> xac nhan cu phap. TU DONG tra cuu. Khong co -> `.planning/docs/` hoac knowledge.
```

<!-- Current test.md - NO explicit Context7 step (need to ADD one) -->
<!-- test.md Buoc 3 (line 84) is "Doc code de hieu logic" - add Context7 ref here -->

<!-- new-milestone.md agent contracts (line 389+) - Context7 in tools_allowed, keep but add note -->
```
tools_allowed: WebSearch, mcp__context7__resolve-library-id, mcp__context7__query-docs, Read, Write
```

<!-- Smoke tests from Plan 01 that must turn GREEN: -->
Test 7: workflows co tham chieu context7-pipeline (checks write-code, plan, fix-bug, test)
Test 8: workflows KHONG con stack-specific Context7 rules (checks write-code for antd, Guard/JWT)
Test 9: workflows KHONG con silent fallback (checks write-code, plan, fix-bug for "knowledge san")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Context7 sections in write-code.md, plan.md, and fix-bug.md workflows</name>
  <files>workflows/write-code.md, workflows/plan.md, workflows/fix-bug.md</files>
  <read_first>
    - references/context7-pipeline.md (the canonical pipeline to reference)
    - workflows/write-code.md (full file - find Context7 section in Buoc 3, lines ~131-137)
    - workflows/plan.md (full file - find Context7 in Buoc 3B, line ~103)
    - workflows/fix-bug.md (full file - find Context7 in Buoc 4, line ~92)
  </read_first>
  <action>
**CRITICAL: Do NOT change Buoc numbering. Replace CONTENT within existing steps only.**

**workflows/write-code.md** - Replace the entire Context7 block in Buoc 3 (lines 131-137, the block starting with `**Context7** (task dung thu vien ngoai):` through `- Context7 khong co -> ...`).

Replace with exactly:
```markdown
**Context7** (task dung thu vien ngoai): Thuc hien theo @references/context7-pipeline.md
```

This removes:
- The inline 2-step flow (now in pipeline reference)
- Stack-specific rules: `Admin (antd): BAT BUOC...` and `Guard/JWT/Role: BAT BUOC...` (D-07)
- Silent fallback: `Context7 khong co -> .planning/docs/ hoac knowledge san` (D-10)

Keep everything else in Buoc 3 unchanged (the FastCode section above, the `---` separator below).

**workflows/plan.md** - Replace the Context7 line in Buoc 3B (line 103, the line starting with `**Context7** (uu tien -- CAO):`).

Replace with exactly:
```markdown
**Context7** (uu tien — CAO): Thuc hien theo @references/context7-pipeline.md
```

This removes:
- Inline flow `resolve-library-id -> query-docs`
- Silent fallback `Khong co -> .planning/docs/ hoac knowledge.`

Keep the rest of Buoc 3B unchanged (the "LUON chay neu..." intro above, "Phan tich he sinh thai" section below).

**workflows/fix-bug.md** - Replace the Context7 line in Buoc 4 (line 92, the line starting with `**Loi lien quan thu vien**`).

Replace with exactly:
```markdown
**Loi lien quan thu vien** -> Thuc hien theo @references/context7-pipeline.md
```

This removes:
- Inline flow `Context7: resolve-library-id -> query-docs -> xac nhan cu phap`
- Silent fallback `Khong co -> .planning/docs/ hoac knowledge.`

Keep the rest of Buoc 4 and Buoc 5 unchanged.
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && grep -c "context7-pipeline" workflows/write-code.md workflows/plan.md workflows/fix-bug.md && ! grep -q "Admin (antd)" workflows/write-code.md && ! grep -q "Guard/JWT" workflows/write-code.md && ! grep "knowledge s" workflows/write-code.md workflows/plan.md workflows/fix-bug.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/write-code.md contains "context7-pipeline.md" (exactly 1 occurrence)
    - workflows/write-code.md does NOT contain "Admin (antd): BAT BUOC"
    - workflows/write-code.md does NOT contain "Guard/JWT/Role: BAT BUOC"
    - workflows/write-code.md does NOT contain "knowledge san" or "knowledge s\u1EB5n"
    - workflows/write-code.md Buoc 3 header still exists (`## Buoc 3` or `## B\u01B0\u1EDBc 3`)
    - workflows/write-code.md Buoc 4 header still exists (step numbering unchanged)
    - workflows/plan.md contains "context7-pipeline.md" (exactly 1 occurrence)
    - workflows/plan.md does NOT contain "knowledge" in Context7 context
    - workflows/plan.md Buoc 3B header still exists
    - workflows/fix-bug.md contains "context7-pipeline.md" (exactly 1 occurrence)
    - workflows/fix-bug.md does NOT contain "knowledge" in Context7 context
    - workflows/fix-bug.md Buoc 4 header and Buoc 5 header still exist (numbering unchanged)
  </acceptance_criteria>
  <done>3 workflow files refactored to reference @references/context7-pipeline.md. Stack-specific rules and silent fallbacks removed. Step numbering preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add Context7 reference to test.md and pipeline note to new-milestone.md, verify all tests GREEN</name>
  <files>workflows/test.md, workflows/new-milestone.md</files>
  <read_first>
    - references/context7-pipeline.md (the canonical pipeline to reference)
    - workflows/test.md (full file - need to add Context7 step, currently no explicit Context7 workflow step)
    - workflows/new-milestone.md (full file - Context7 in agent contracts, lines ~389+)
    - test/smoke-integrity.test.js (the TDD RED tests from Plan 01 - verify they will turn GREEN)
  </read_first>
  <action>
**workflows/test.md** - Add a Context7 reference line at the end of Buoc 3 ("Doc code de hieu logic", currently at line 84).

After the existing FastCode content in Buoc 3 (after line 88 `FastCode MCP loi -> Fallback Grep/Read...`), add:

```markdown

**Context7** (test dung thu vien ngoai): Thuc hien theo @references/context7-pipeline.md
```

This adds Context7 awareness for test writers who need to look up testing library APIs. The `---` separator before Buoc 4 should remain after this addition.

Do NOT change Buoc numbering. Do NOT modify any other step.

**workflows/new-milestone.md** - The agent contracts already have `mcp__context7__resolve-library-id` and `mcp__context7__query-docs` in `tools_allowed` (line 389). This is the correct pattern for agent-based Context7 usage.

Add a brief pipeline awareness note in the agent's common input section (before the `### Agent 1` heading, after the `tools_allowed` line). Add this line:

```markdown
context7_pipeline: @references/context7-pipeline.md
```

This ensures agents know about the canonical pipeline pattern. The agent contracts keep their existing `tools_allowed` Context7 tools unchanged.

**After both files are updated**, run the full smoke test suite to verify all Plan 01 TDD RED tests are now GREEN:
- Test 7: "workflows co tham chieu context7-pipeline" - checks write-code, plan, fix-bug, test
- Test 8: "workflows KHONG con stack-specific Context7 rules" - checks write-code
- Test 9: "workflows KHONG con silent fallback" - checks write-code, plan, fix-bug

Run `node --test test/smoke-integrity.test.js` and verify all tests pass (0 failures).
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && node --test test/smoke-integrity.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - workflows/test.md contains "context7-pipeline" (exactly 1 occurrence)
    - workflows/test.md Buoc 3 header still exists
    - workflows/test.md Buoc 4 header still exists (numbering unchanged)
    - workflows/new-milestone.md contains "context7-pipeline" (at least 1 occurrence)
    - workflows/new-milestone.md still contains "mcp__context7__resolve-library-id" in agent tools_allowed
    - workflows/new-milestone.md still contains "mcp__context7__query-docs" in agent tools_allowed
    - `node --test test/smoke-integrity.test.js` exits with 0 failures
    - All 9 Context7 standardization tests pass (including previously RED tests 7-9)
    - `npm test` full suite passes with 0 failures
  </acceptance_criteria>
  <done>test.md has Context7 reference in Buoc 3. new-milestone.md agent contracts have pipeline awareness. All smoke tests GREEN.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 5 workflow files reference `context7-pipeline.md`
2. `node --test test/smoke-integrity.test.js` — all tests pass (0 failures)
3. `npm test` — full test suite passes
4. No stack-specific Context7 rules remain in any workflow
5. No silent fallback patterns remain in any workflow
6. All Buoc numbers preserved (no step renumbering)
7. new-milestone agent contracts retain Context7 tools
</verification>

<success_criteria>
- 5 workflows refactored to use canonical @references/context7-pipeline.md
- Stack-specific rules (antd, Guard/JWT, nestjs) removed per D-07
- Silent fallbacks replaced with hard stop per D-10
- All 9 Context7 smoke tests GREEN
- Full test suite GREEN
- Step numbering unchanged in all workflows
</success_criteria>

<output>
After completion, create `.planning/phases/06-context7-standardization/06-02-SUMMARY.md`
</output>
