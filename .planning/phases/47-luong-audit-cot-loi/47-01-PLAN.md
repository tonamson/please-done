---
phase: 47-luong-audit-cot-loi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/parallel-dispatch.js
  - test/smoke-parallel-dispatch.test.js
autonomous: true
requirements: [BATCH-01, BATCH-02]

must_haves:
  truths:
    - "buildScannerPlan chia N categories thanh waves of K"
    - "mergeScannerResults hop nhat ket qua tu N scanners voi failure isolation"
    - "Scanner fail ghi inconclusive, khong chan cac scanner con lai"
  artifacts:
    - path: "bin/lib/parallel-dispatch.js"
      provides: "buildScannerPlan + mergeScannerResults"
      exports: ["buildScannerPlan", "mergeScannerResults"]
    - path: "test/smoke-parallel-dispatch.test.js"
      provides: "Tests cho buildScannerPlan va mergeScannerResults"
  key_links:
    - from: "bin/lib/parallel-dispatch.js"
      to: "bin/lib/resource-config.js"
      via: "getAgentConfig('pd-sec-scanner')"
      pattern: "getAgentConfig\\('pd-sec-scanner'\\)"
---

<objective>
Mo rong parallel-dispatch.js them 2 ham buildScannerPlan() va mergeScannerResults() de ho tro wave-based scanner dispatch (per D-09).

Purpose: Cung cap nen tang JS cho workflow audit dispatch scanners song song 2/wave voi failure isolation.
Output: 2 ham moi trong parallel-dispatch.js + tests xac nhan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-luong-audit-cot-loi/47-CONTEXT.md
@.planning/phases/47-luong-audit-cot-loi/47-RESEARCH.md

<interfaces>
<!-- Tu bin/lib/resource-config.js -->
```javascript
function getAgentConfig(agentName) → { name, tier, model, effort, maxTurns, tools[], categories[]?, ...extra }
// getAgentConfig('pd-sec-scanner').categories = ['sql-injection', 'xss', 'cmd-injection', 'path-traversal', 'secrets', 'auth', 'deserialization', 'misconfig', 'prototype-pollution', 'crypto', 'insecure-design', 'vuln-deps', 'logging']
```

<!-- Tu bin/lib/evidence-protocol.js -->
```javascript
function validateEvidence(content) → { valid: boolean, outcome: string|null, warnings: string[] }
```

<!-- Tu bin/lib/parallel-dispatch.js (existing exports) -->
```javascript
module.exports = { buildParallelPlan, mergeParallelResults };
// Se them: buildScannerPlan, mergeScannerResults
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Them buildScannerPlan() va mergeScannerResults() vao parallel-dispatch.js (TDD)</name>
  <files>bin/lib/parallel-dispatch.js, test/smoke-parallel-dispatch.test.js</files>
  <read_first>
    - bin/lib/parallel-dispatch.js (trang thai hien tai — 2 ham cu buildParallelPlan, mergeParallelResults)
    - test/smoke-parallel-dispatch.test.js (test hien tai — pattern de mo rong)
    - bin/lib/resource-config.js (getAgentConfig API, AGENT_REGISTRY structure)
    - bin/lib/evidence-protocol.js (validateEvidence API)
    - .planning/phases/47-luong-audit-cot-loi/47-RESEARCH.md (code examples section)
  </read_first>
  <behavior>
    buildScannerPlan tests:
    - Test 1: 13 categories, batchSize=2 → 7 waves (6 waves of 2 + 1 wave of 1)
    - Test 2: 4 categories, batchSize=2 → 2 waves of 2
    - Test 3: categories rong → waves=[], totalWaves=0, warnings co message "Danh sach categories rong"
    - Test 4: batchSize < 1 → clamp to 1
    - Test 5: moi item trong wave co category, agentName='pd-sec-scanner', outputFile string

    mergeScannerResults tests:
    - Test 6: 3 scanners thanh cong → completedCount=3, failedCount=0
    - Test 7: 1 scanner fail (error) → outcome='inconclusive', failedCount=1, warnings co message
    - Test 8: evidenceContent null → treat as fail, ghi inconclusive
    - Test 9: tat ca fail → failedCount = scanResults.length
  </behavior>
  <action>
    **RED phase — Them tests vao test/smoke-parallel-dispatch.test.js:**

    Them 2 describe blocks moi:

    ```javascript
    describe('buildScannerPlan', () => { ... })  // 5 tests
    describe('mergeScannerResults', () => { ... })  // 4 tests
    ```

    Import moi o dau file:
    ```javascript
    const { buildParallelPlan, mergeParallelResults, buildScannerPlan, mergeScannerResults } = require('../bin/lib/parallel-dispatch');
    ```

    Chay `node --test test/smoke-parallel-dispatch.test.js` — PHAI FAIL (chua co ham).

    **GREEN phase — Them 2 ham vao bin/lib/parallel-dispatch.js:**

    1. `buildScannerPlan(categories, batchSize = 2, scanPath = '.')`:
       - Kiem tra `!Array.isArray(categories) || categories.length === 0` → return `{ waves: [], totalWaves: 0, totalScanners: 0, warnings: ['Danh sach categories rong — khong co scanner nao de dispatch'] }`
       - Clamp `batchSize < 1` → `batchSize = 1`
       - Loop `for (let i = 0; i < categories.length; i += batchSize)` tao waves
       - Moi item: `{ category: cat, agentName: 'pd-sec-scanner', outputFile: \`evidence_sec_${cat}.md\` }`
       - Return `{ waves, totalWaves: waves.length, totalScanners: categories.length, warnings: [] }`
       - Per D-09: Mo rong file hien co, KHONG tao module moi
       - Per D-10: batchSize mac dinh 2

    2. `mergeScannerResults(scanResults)` nhan array `[{category, evidenceContent, error}]`:
       - Loop qua scanResults
       - Co evidenceContent → goi validateEvidence(), push `{ category, outcome: validation.outcome, valid: validation.valid }`
       - Khong co evidenceContent (null hoac error) → push `{ category, outcome: 'inconclusive', valid: false }`, them warning
       - Per D-11: Scanner fail ghi inconclusive, khong chan cac scanner con lai
       - Return `{ results, completedCount, failedCount, warnings }`

    3. Cap nhat module.exports:
       ```javascript
       module.exports = { buildParallelPlan, mergeParallelResults, buildScannerPlan, mergeScannerResults };
       ```

    Chay `node --test test/smoke-parallel-dispatch.test.js` — PHAI PASS tat ca.
  </action>
  <verify>
    <automated>node --test test/smoke-parallel-dispatch.test.js</automated>
  </verify>
  <acceptance_criteria>
    - parallel-dispatch.js chua `function buildScannerPlan(categories, batchSize = 2, scanPath = '.')`
    - parallel-dispatch.js chua `function mergeScannerResults(scanResults)`
    - parallel-dispatch.js exports chua `buildScannerPlan` va `mergeScannerResults`
    - test file chua `describe('buildScannerPlan'`
    - test file chua `describe('mergeScannerResults'`
    - `node --test test/smoke-parallel-dispatch.test.js` exit code 0
    - Test kiem tra 13 categories / batchSize=2 → totalWaves=7
    - Test kiem tra scanner fail → outcome='inconclusive'
    - Test kiem tra categories rong → warnings chua 'Danh sach categories rong'
  </acceptance_criteria>
  <done>buildScannerPlan chia categories thanh waves, mergeScannerResults hop nhat voi failure isolation. 9+ tests moi PASS.</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-parallel-dispatch.test.js` — tat ca tests PASS (cu + moi)
- `npm test` — full suite khong bi regression
</verification>

<success_criteria>
- buildScannerPlan(13 categories, 2) tra ve 7 waves
- mergeScannerResults xu ly scanner fail bang inconclusive
- Tat ca tests PASS, khong regression
</success_criteria>

<output>
After completion, create `.planning/phases/47-luong-audit-cot-loi/47-01-SUMMARY.md`
</output>
