---
phase: 62-reference-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/smoke-agent-files.test.js
  - workflows/fix-bug.md
  - test/snapshots/codex/fix-bug.md
  - test/snapshots/copilot/fix-bug.md
  - test/snapshots/gemini/fix-bug.md
  - test/snapshots/opencode/fix-bug.md
autonomous: true
requirements: [REFR-01, REFR-02]

must_haves:
  truths:
    - "smoke-agent-files.test.js đọc agents từ commands/pd/agents/ thay vì .claude/agents/"
    - "fix-bug.md workflow kiểm tra agent files tại commands/pd/agents/"
    - "grep '.claude/agents/' trong code chính (loại trừ .planning/) trả về 0 kết quả"
  artifacts:
    - path: "test/smoke-agent-files.test.js"
      provides: "AGENTS_DIR trỏ đến commands/pd/agents"
      contains: "commands/pd/agents"
    - path: "workflows/fix-bug.md"
      provides: "5 agent path references dùng commands/pd/agents/"
      contains: "commands/pd/agents/pd-bug-janitor.md"
  key_links:
    - from: "test/smoke-agent-files.test.js"
      to: "commands/pd/agents/"
      via: "AGENTS_DIR constant"
      pattern: "join.*commands.*pd.*agents"
    - from: "workflows/fix-bug.md"
      to: "commands/pd/agents/"
      via: "agent file path references"
      pattern: "commands/pd/agents/pd-"
---

<objective>
Cập nhật tất cả references từ `.claude/agents/` sang `commands/pd/agents/` trong test và workflow files.

Purpose: Hoàn tất migration — source of truth là `commands/pd/agents/`, `.claude/agents/` chỉ còn symlinks.
Output: Zero references tới `.claude/agents/` trong core code (ngoài .planning/).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-symlink-architecture/61-01-SUMMARY.md

@test/smoke-agent-files.test.js
@workflows/fix-bug.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cập nhật AGENTS_DIR trong test + agent paths trong workflow</name>
  <files>test/smoke-agent-files.test.js, workflows/fix-bug.md</files>
  <action>
    **File 1: `test/smoke-agent-files.test.js`** (per REFR-01)
    - Dòng 3: Đổi comment từ `.claude/agents/` sang `commands/pd/agents/`
    - Dòng 19: Đổi `AGENTS_DIR` từ `join(__dirname, '..', '.claude', 'agents')` sang `join(__dirname, '..', 'commands', 'pd', 'agents')`
    - Dòng 92: Đổi test description từ `'.claude/agents/'` sang `'commands/pd/agents/'`
    - KHÔNG thay đổi danh sách AGENT_NAMES (14 agents) — việc thêm 2 agents mới thuộc Phase 64.

    **File 2: `workflows/fix-bug.md`** (per REFR-02)
    - Dòng 24-28: Đổi 5 agent path references:
      - `.claude/agents/pd-bug-janitor.md` → `commands/pd/agents/pd-bug-janitor.md`
      - `.claude/agents/pd-code-detective.md` → `commands/pd/agents/pd-code-detective.md`
      - `.claude/agents/pd-doc-specialist.md` → `commands/pd/agents/pd-doc-specialist.md`
      - `.claude/agents/pd-repro-engineer.md` → `commands/pd/agents/pd-repro-engineer.md`
      - `.claude/agents/pd-fix-architect.md` → `commands/pd/agents/pd-fix-architect.md`
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && grep -c 'commands/pd/agents' test/smoke-agent-files.test.js && grep -c 'commands/pd/agents' workflows/fix-bug.md && ! grep '\.claude/agents/' test/smoke-agent-files.test.js && ! grep '\.claude/agents/' workflows/fix-bug.md && echo "PASS"</automated>
  </verify>
  <done>AGENTS_DIR trỏ commands/pd/agents, 5 agent paths trong workflow đã đổi, zero references tới .claude/agents/ trong cả 2 files</done>
</task>

<task type="auto">
  <name>Task 2: Cập nhật 4 snapshot files + xác nhận zero hits</name>
  <files>test/snapshots/codex/fix-bug.md, test/snapshots/copilot/fix-bug.md, test/snapshots/gemini/fix-bug.md, test/snapshots/opencode/fix-bug.md</files>
  <action>
    Cập nhật 4 snapshot files (derived từ workflows/fix-bug.md) — thay tất cả `.claude/agents/pd-` bằng `commands/pd/agents/pd-` trong mỗi file:
    - `test/snapshots/codex/fix-bug.md`
    - `test/snapshots/copilot/fix-bug.md`
    - `test/snapshots/gemini/fix-bug.md`
    - `test/snapshots/opencode/fix-bug.md`

    Sau khi cập nhật, chạy `node --test test/smoke-agent-files.test.js` để xác nhận test vẫn pass.

    Cuối cùng verify: `grep -rn '\.claude/agents/' --include='*.js' --include='*.md' --exclude-dir='.planning' --exclude-dir='node_modules' | grep -v 'fix_agents.md' | grep -v 'FINAL_'` phải trả về 0 kết quả.
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && node --test test/smoke-agent-files.test.js 2>&1 | tail -5 && HITS=$(grep -rn '\.claude/agents/' --include='*.js' --include='*.md' --exclude-dir='.planning' --exclude-dir='node_modules' | grep -v 'fix_agents.md' | grep -v 'FINAL_' | wc -l) && echo "Remaining .claude/agents/ refs: $HITS" && [[ "$HITS" -eq 0 ]] && echo "PASS"</automated>
  </verify>
  <done>4 snapshots đã cập nhật, smoke test pass, zero references tới .claude/agents/ trong toàn bộ core code</done>
</task>

</tasks>

<verification>
- `grep -rn '\.claude/agents/' --include='*.js' --include='*.md' --exclude-dir='.planning' --exclude-dir='node_modules' | grep -v 'fix_agents.md' | grep -v 'FINAL_'` trả về 0 dòng
- `node --test test/smoke-agent-files.test.js` — tất cả test pass
</verification>

<success_criteria>
- AGENTS_DIR trong smoke-agent-files.test.js = `commands/pd/agents`
- 5 agent paths trong fix-bug.md dùng `commands/pd/agents/`
- 4 snapshot files đã đồng bộ
- Zero hits cho `.claude/agents/` trong core code
</success_criteria>

<output>
After completion, create `.planning/phases/62-reference-migration/62-01-SUMMARY.md`
</output>
