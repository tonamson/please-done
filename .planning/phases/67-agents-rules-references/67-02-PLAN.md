---
phase: 67-agents-rules-references
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - references/conventions.md
  - references/prioritization.md
  - references/questioning.md
  - references/security-checklist.md
  - references/state-machine.md
  - references/ui-brand.md
  - references/context7-pipeline.md
  - references/mermaid-rules.md
  - references/verification.md
  - references/security-rules.yaml
  - references/gadget-chain-templates.yaml
  - references/guard-context.md
  - references/guard-fastcode.md
  - references/guard-context7.md
  - references/guard-valid-path.md
  - test/smoke-integrity.test.js
autonomous: true
user_setup: []
requirements:
  - TRANS-06

must_haves:
  truths:
    - "15 reference files contain zero Vietnamese text (diacritical and non-diacritical)"
    - "YAML file keys and structure preserved — only string values and comments translated"
    - "Guard file content regex in tests updated to match English translations"
    - "smoke-integrity tests pass after guard and pipeline translations"
  artifacts:
    - path: "references/security-rules.yaml"
      provides: "OWASP scanner rules with English descriptions"
    - path: "references/security-checklist.md"
      provides: "Security checklist in English"
    - path: "references/guard-context.md"
      provides: "Guard template with English content"
      contains: "exists"
    - path: "references/guard-context7.md"
      provides: "Guard template with English content"
      contains: "connected"
    - path: "references/context7-pipeline.md"
      provides: "Context7 pipeline doc in English"
      contains: "AUTOMATIC"
    - path: "test/smoke-integrity.test.js"
      provides: "Test assertions matching English guard/reference content"
  key_links:
    - from: "references/guard-*.md"
      to: "test/smoke-integrity.test.js"
      via: "Guard content regex assertion at line 414"
      pattern: "exists|connected|valid"
    - from: "references/context7-pipeline.md"
      to: "test/smoke-integrity.test.js"
      via: "Pipeline content checks at lines 792-797"
      pattern: "AUTOMATIC|resolve ALL"
    - from: "references/security-rules.yaml"
      to: "bin/lib/gadget-chain.js"
      via: "OWASP scanner rule loading"
      pattern: "security-rules"
---

<objective>
Translate all 15 reference files (.md + .yaml) and update test assertions that break from guard/pipeline content changes.

Purpose: Complete TRANS-06. Reference files are loaded by skills/workflows at runtime — security rules, coding conventions, guard checklists, and Context7 pipeline.
Output: 15 reference files translated + test assertions updated for content-matching regex.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-agents-rules-references/67-CONTEXT.md
@.planning/phases/65-skills-config-foundation/65-01-SUMMARY.md
@.planning/phases/66-workflow-translation/66-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate 9 reference .md files to English (per D-01, D-04, D-05)</name>
  <files>references/conventions.md, references/prioritization.md, references/questioning.md, references/security-checklist.md, references/state-machine.md, references/ui-brand.md, references/context7-pipeline.md, references/mermaid-rules.md, references/verification.md</files>
  <read_first>
    - references/security-checklist.md (170 diacritical lines — largest reference file)
    - references/ui-brand.md (124 diacritical lines)
    - references/verification.md (125 lines — non-diacritical Vietnamese only)
    - references/state-machine.md (77 diacritical lines)
    - references/conventions.md (58 diacritical lines)
    - references/context7-pipeline.md (17 lines — contains TU DONG and TAT CA checked by tests)
    - references/questioning.md (43 diacritical lines)
    - references/prioritization.md (38 diacritical lines)
    - references/mermaid-rules.md (42 lines — non-diacritical Vietnamese only)
  </read_first>
  <action>
    Translate 9 reference .md files from Vietnamese to English:

    1. **security-checklist.md** (~170 diacritical lines) — Largest file. Translate all section headers, table content, and descriptions:
       - "Bảng kiểm bảo mật khi viết code" → "Security checklist for writing code"
       - "Phần A: Phân tích ngữ cảnh bảo mật" → "Part A: Security context analysis"
       - Table headers: "Loại" → "Type", "Mô tả" → "Description", "Giả định" → "Assumption"
       - "CAO/TRUNG BÌNH/THẤP" → "HIGH/MEDIUM/LOW"
       - "Mật khẩu, token, thông tin thanh toán" → "Passwords, tokens, payment information"
       - Preserve all code examples, vulnerability patterns, and technical identifiers

    2. **ui-brand.md** (~124 diacritical lines) — UI/brand guidelines. Translate descriptions while preserving CSS/design tokens.

    3. **verification.md** (~125 lines, non-diacritical Vietnamese) — Verification reference. Full file review per D-12. Translate all Vietnamese including non-diacritical phrases like "kiem tra", "xac nhan", "thanh cong".

    4. **state-machine.md** (~77 diacritical lines) — State machine docs. Translate state names and transition descriptions. Preserve state identifiers if used in code.

    5. **conventions.md** (~58 diacritical lines) — Coding conventions. Translate section headers and rules. CRITICAL: Section `## Effort level` is already English — verify preserved. Update language policy section to reflect English convention.

    6. **context7-pipeline.md** (~17 lines, non-diacritical Vietnamese) — CRITICAL for tests. Translate:
       - "TU DONG" → "AUTOMATIC" (tested at smoke-integrity.test.js:792)
       - "resolve TAT CA" → "resolve ALL" (tested at smoke-integrity.test.js:797)
       - "Buoc N" → "Step N"
       - "Khi nao" → "When"
       - "Fallback (Context7 loi hoac khong co ket qua)" → "Fallback (Context7 error or no results)"
       - "Dung knowledge san, co the khong chinh xac" → "Using built-in knowledge, may not be accurate for current version"
       - Table headers: "Nguon" → "Source", "Cach thu" → "Method", "Dieu kien thanh cong" → "Success condition"

    7. **questioning.md** (~43 diacritical lines) — Questioning reference. Translate question templates and guidance.

    8. **prioritization.md** (~38 diacritical lines) — Prioritization framework. Translate criteria and examples.

    9. **mermaid-rules.md** (~42 lines, non-diacritical Vietnamese) — Mermaid diagram rules. Translate descriptions. Preserve Mermaid syntax examples.

    **Preservation rules (per D-01, D-02):**
    - Keep markdown formatting, code blocks, table structure
    - Keep file references (@references/, @workflows/)
    - Keep technical identifiers (function names, library names, API endpoints)
    - Keep all heading anchor IDs intact

    **Process:** Read each file fully, translate to English, write via temp file (.tmp then mv).
  </action>
  <verify>
    <automated>rg -n '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' references/*.md && exit 1 || echo "Zero diacritics in reference .md files"</automated>
  </verify>
  <acceptance_criteria>
    - rg for Vietnamese diacritics in references/*.md returns zero matches
    - security-checklist.md does NOT contain "Bảng kiểm" or "Phân tích" or "ngữ cảnh"
    - security-checklist.md contains "HIGH" and "MEDIUM" and "LOW" (translated from CAO/TRUNG BÌNH/THẤP)
    - conventions.md contains "## Effort level" (unchanged — already English)
    - conventions.md does NOT contain "Quy tắc chung" or "Phong cách code"
    - context7-pipeline.md contains "AUTOMATIC" (translated from "TU DONG")
    - context7-pipeline.md contains "resolve ALL" (translated from "resolve TAT CA")
    - context7-pipeline.md does NOT contain "Buoc" or "Khi nao" or "Nguon"
    - verification.md does NOT contain "kiem tra" or "xac nhan" (non-diacritical Vietnamese)
    - mermaid-rules.md does NOT contain non-diacritical Vietnamese phrases
    - All 9 files retain their markdown heading structure
  </acceptance_criteria>
  <done>9 reference .md files translated to English. Zero Vietnamese remaining (diacritical and non-diacritical).</done>
</task>

<task type="auto">
  <name>Task 2: Translate 2 YAML files + 4 guard files to English (per D-08, D-09)</name>
  <files>references/security-rules.yaml, references/gadget-chain-templates.yaml, references/guard-context.md, references/guard-fastcode.md, references/guard-context7.md, references/guard-valid-path.md</files>
  <read_first>
    - references/security-rules.yaml (288 diacritical lines — largest YAML, OWASP rules)
    - references/gadget-chain-templates.yaml (7 non-diacritical Vietnamese lines)
    - references/guard-context.md (1 line: "- [ ] `.planning/CONTEXT.md` ton tai -> \"Chay `/pd:init` truoc.\"")
    - references/guard-fastcode.md (1 line: "- [ ] FastCode MCP ket noi thanh cong -> ...")
    - references/guard-context7.md (2 lines about Context7 MCP connection and operation)
    - references/guard-valid-path.md (1 line: "- [ ] Tham so path hop le ...")
    - test/smoke-integrity.test.js lines 390-420 (guard content assertions — to understand what regex matches against)
  </read_first>
  <action>
    **Part A — YAML files (per D-08):**

    1. **security-rules.yaml** (~288 diacritical lines) — Translate `description:` string values and `#` comments ONLY. Preserve:
       - All YAML keys (sql-injection, owasp, severity, evidence_file, patterns, regex, stack, fixes, fastcode_queries)
       - All regex patterns verbatim (they match code, not Vietnamese)
       - YAML anchors and aliases if present
       - Structure and indentation
       - Example translations:
         - `description: "Raw SQL nối chuỗi bằng toán tử +"` → `description: "Raw SQL concatenation using + operator"`
         - `description: "Template literal trong SQL query"` → `description: "Template literal in SQL query"`
         - `description: "Câu SELECT nối chuỗi với dấu +"` → `description: "SELECT statement concatenated with + operator"`
         - `# Nguồn sự thật duy nhất cho 13 OWASP scanner categories` → `# Single source of truth for 13 OWASP scanner categories`
         - `# Migrate từ 13 file pd-sec-*.md riêng lẻ thành 1 file YAML tập trung` → `# Migrated from 13 separate pd-sec-*.md files into 1 centralized YAML file`

    2. **gadget-chain-templates.yaml** (~7 non-diacritical Vietnamese lines) — Translate `description:` values and `#` comments. Preserve keys, structure, chain IDs. Examples:
       - `description: "SQLi cho phep trich xuat du lieu nhay cam tu DB"` → `description: "SQLi allows extraction of sensitive data from DB"`
       - `description: "Thieu kiem tra ownership cho phep truy cap tai nguyen nguoi khac"` → `description: "Missing ownership check allows access to other users' resources"`
       - `# Chain patterns pho bien cho cross-category analysis` → `# Common chain patterns for cross-category analysis`

    **Part B — Guard files (per D-09):**

    Translate all 4 guard files. Each is 1-2 lines in checklist format `- [ ] condition -> "error message"`.

    3. **guard-context.md** — Current: `- [ ] .planning/CONTEXT.md ton tai -> "Chay /pd:init truoc."`
       Translate to: `- [ ] .planning/CONTEXT.md exists -> "Run /pd:init first."`

    4. **guard-fastcode.md** — Current: `- [ ] FastCode MCP ket noi thanh cong -> "Kiem tra Docker dang chay va FastCode MCP da duoc cau hinh."`
       Translate to: `- [ ] FastCode MCP connected successfully -> "Check that Docker is running and FastCode MCP is configured."`

    5. **guard-context7.md** — Current content has 2 checklist lines about Context7 MCP connection and operation.
       Translate conditions and error messages. Example: `ket noi thanh cong` → `connected successfully`, `hoat dong` → `working`, `Kiem tra` → `Check`.

    6. **guard-valid-path.md** — Current: `- [ ] Tham so path hop le (neu co) -> "Path khong ton tai hoac khong phai thu muc."`
       Translate to: `- [ ] Path parameter is valid (if provided) -> "Path does not exist or is not a directory."`

    **Process:** Read each file fully, translate, write via temp file (.tmp then mv).
  </action>
  <verify>
    <automated>rg -n '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' references/security-rules.yaml references/gadget-chain-templates.yaml references/guard-*.md && exit 1 || echo "Zero diacritics in YAML and guard files"</automated>
  </verify>
  <acceptance_criteria>
    - rg for Vietnamese diacritics in security-rules.yaml returns zero matches
    - security-rules.yaml retains all YAML keys unchanged (sql-injection, owasp, severity, patterns, regex, etc.)
    - security-rules.yaml retains all regex patterns unchanged (e.g., `'query\(.*\+'` still present)
    - gadget-chain-templates.yaml does NOT contain "cho phep" or "trich xuat" or "kiem tra"
    - guard-context.md contains "exists" (translated from "ton tai")
    - guard-fastcode.md contains "connected successfully" (translated from "ket noi thanh cong")
    - guard-context7.md contains "connected" (translated from "ket noi")
    - guard-valid-path.md contains "valid" (translated from "hop le")
    - All guard files retain `- [ ]` checklist format
    - All guard files retain `->` separator between condition and error message
  </acceptance_criteria>
  <done>2 YAML files and 4 guard files translated. YAML keys/regex preserved. Guard checklist format preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Update test assertions for translated guard/reference content (per D-11)</name>
  <files>test/smoke-integrity.test.js</files>
  <read_first>
    - test/smoke-integrity.test.js lines 388-462 (guard deduplication tests — content regex at line 414)
    - test/smoke-integrity.test.js lines 778-830 (context7-pipeline tests — TU DONG at 792, TAT CA at 797)
    - test/smoke-integrity.test.js lines 867-895 (guard-context7 behavior check)
    - references/guard-context.md (to verify final English content)
    - references/guard-context7.md (to verify final English content)
    - references/context7-pipeline.md (to verify final English content)
  </read_first>
  <action>
    Update test assertions in smoke-integrity.test.js that will BREAK from Plan 02 Tasks 1-2 translations.

    **Content-matching regex updates (CRITICAL — tests will fail without these):**

    1. **Line 414** — Guard content regex currently: `/ton tai|ket noi|hop le/`
       Update to: `/exists|connected|valid/`
       (matches English guard file content after translation)

    2. **Line 792** — Pipeline content check currently: `/TU DONG/`
       Update to: `/AUTOMATIC/`
       (matches context7-pipeline.md after "TU DONG" → "AUTOMATIC")

    3. **Line 797** — Pipeline content check currently: `/resolve TAT CA/`
       Update to: `/resolve ALL/`
       (matches context7-pipeline.md after "resolve TAT CA" → "resolve ALL")

    **Vietnamese test description and error message translations (lines 390-459, 779-830, 867-895):**

    4. **Line 390** — `it("guard micro-templates ton tai trong references/")` → `it("guard micro-templates exist in references/")`

    5. **Line 402** — `"thieu file: references/${guardFile}"` → `"missing file: references/${guardFile}"`

    6. **Line 409** — `"references/${guardFile}: khong bat dau bang \"- [ ]\""` → `"references/${guardFile}: does not start with \"- [ ]\""`

    7. **Line 415** — `"references/${guardFile}: khong chua non-diacritical Vietnamese"` → `"references/${guardFile}: does not contain expected English content"`

    8. **Line 420** — `it("guard micro-templates duoc tham chieu dung trong skills")` → `it("guard micro-templates are correctly referenced in skills")`

    9. **Line 436** — `"${guardFile}: chi duoc tham chieu boi ${refCount} skill (can >= 2)"` → `"${guardFile}: only referenced by ${refCount} skill(s) (need >= 2)"`

    10. **Line 441** — `it("khong con guard text trung lap giua cac skills")` → `it("no duplicate guard text across skills")`

    11. **Line 459** — `"${skill.name}: van con guard text trung lap du da dung @references/"` → `"${skill.name}: still has duplicate guard text despite using @references/"`

    12. **Line 779** — `it("context7-pipeline.md ton tai voi noi dung bat buoc")` → `it("context7-pipeline.md exists with required content")`

    13. **Line 783** — `"thieu references/context7-pipeline.md"` → `"missing references/context7-pipeline.md"`

    14. **Line 789** — `"pipeline thieu resolve-library-id"` → `"pipeline missing resolve-library-id"`

    15. **Line 791** — `"pipeline thieu query-docs"` → `"pipeline missing query-docs"`

    16. **Line 792** — `"pipeline thieu TU DONG trigger rule"` → `"pipeline missing AUTOMATIC trigger rule"` (and update regex per item 2)

    17. **Line 793** — `"pipeline thieu fallback section"` → `"pipeline missing fallback section"`

    18. **Line 797** — `"pipeline thieu multi-lib batch pattern"` → `"pipeline missing multi-lib batch pattern"` (and update regex per item 3)

    19. **Line 801** — `it("pipeline KHONG co stack-specific rules (D-07)")` → `it("pipeline does NOT have stack-specific rules (D-07)")`

    20. **Line 817** — `it("pipeline KHONG con hard-stop 3 lua chon (thay the boi fallback)")` → `it("pipeline does NOT have hard-stop 3 options (replaced by fallback)")`

    21. **Line 823** — `"pipeline con sot hard-stop option 1"` → `"pipeline still contains hard-stop option 1"`

    22. **Lines 867-895** — guard-context7 behavior test:
        - `it("guard-context7.md co kiem tra hoat dong (D-09)")` → `it("guard-context7.md has operation check (D-09)")`
        - `"guard thieu kiem tra resolve-library-id"` → `"guard missing resolve-library-id check"`
        - `"guard can it nhat 2 dieu kien kiem tra, co ${checklistLines.length}"` → `"guard needs at least 2 check conditions, has ${checklistLines.length}"`

    **DO NOT modify these lines** (they check content that is ALREADY English or won't break):
    - Lines 593-611: conventions.md effort level checks — content is already English ("## Effort level", "simple|haiku")
    - Lines 807-814: pipeline negative checks for "antd", "Guard/JWT" — English strings, no translation needed
    - Lines 509-543: security-checklist/ui-brand reading list checks — file name references, no content change
  </action>
  <verify>
    <automated>node --test test/smoke-integrity.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - node --test test/smoke-integrity.test.js exits with code 0 (all tests pass)
    - Line 414 contains regex `/exists|connected|valid/` (not `/ton tai|ket noi|hop le/`)
    - Line 792 contains regex `/AUTOMATIC/` (not `/TU DONG/`)
    - Line 797 contains regex `/resolve ALL/` (not `/resolve TAT CA/`)
    - All `it()` descriptions in lines 390-459 are in English (no "ton tai", "tham chieu", "trung lap")
    - All `it()` descriptions in lines 779-830 are in English (no "ton tai", "KHONG co", "KHONG con")
    - All assertion error messages in lines 390-459 are in English (no "thieu", "khong", "chi duoc", "van con")
    - All assertion error messages in lines 779-830 are in English (no "thieu", "con sot")
  </acceptance_criteria>
  <done>Test assertions updated. Content regex matches English guard/pipeline content. smoke-integrity tests all pass.</done>
</task>

</tasks>

<verification>
- rg for Vietnamese diacritics in references/ returns zero matches
- security-rules.yaml YAML structure valid (keys preserved, only values translated)
- Guard files contain English content matching test regex: exists, connected, valid
- context7-pipeline.md contains AUTOMATIC and resolve ALL (matching updated test regex)
- node --test test/smoke-integrity.test.js passes
</verification>

<success_criteria>
- TRANS-06 complete: 15 reference files fully in English
- YAML keys and regex patterns 100% preserved in security-rules.yaml
- Guard checklist format preserved (- [ ] ... -> "...")
- smoke-integrity tests pass with updated content-matching assertions
- No file renames, no structural changes, no behavioral changes
</success_criteria>

<output>
After completion, create .planning/phases/67-agents-rules-references/67-02-SUMMARY.md
</output>
