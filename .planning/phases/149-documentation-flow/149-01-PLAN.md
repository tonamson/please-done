---
phase: 149-documentation-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/WORKFLOW_OVERVIEW.md
autonomous: true
requirements:
  - DOCS-04

must_haves:
  truths:
    - "WORKFLOW_OVERVIEW.md is ≤60 lines total"
    - "File contains a Mermaid flowchart LR diagram showing the lifecycle"
    - "File answers 'when do I use which command?' via a quick-reference section"
    - "File has exactly 2 sections: diagram and quick-ref"
    - "Footer links to COMMAND_REFERENCE.md"
  artifacts:
    - path: "docs/WORKFLOW_OVERVIEW.md"
      provides: "Lifecycle overview with Mermaid diagram"
      max_lines: 60
  key_links:
    - from: "docs/WORKFLOW_OVERVIEW.md"
      to: "docs/COMMAND_REFERENCE.md"
      via: "footer link"
      pattern: "\\[COMMAND_REFERENCE\\.md\\]"
---

<objective>
Rewrite `docs/WORKFLOW_OVERVIEW.md` to be short and flow-focused.

Purpose: Replace the verbose 53-line prose document with a concise ≤60 line file containing a Mermaid lifecycle diagram and a "when to use which command?" quick-reference section.

Output: `docs/WORKFLOW_OVERVIEW.md` — fully rewritten, diagram-first, scannable in under 1 minute.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
## Current Content (to be replaced entirely)

The current `docs/WORKFLOW_OVERVIEW.md` is 53 lines of prose organized as Step 1-5 blocks.
**Do NOT preserve any of it.** This is a full rewrite per D-07.

## Target Structure (per user decisions)

1. **Section 1: Mermaid diagram** (D-01, D-02)
   - Use `flowchart LR` (left-to-right)
   - Show the lifecycle: Init → Plan → Execute → Test → Complete
   - Include the key commands at each step

2. **Section 2: Quick-reference** (D-03)
   - Answer: "When do I use which command?"
   - Format: Table or bullet list (your choice — pick clearest)
   - Cover: onboard, plan, write-code, test, complete-milestone, what-next

3. **Footer** (D-06)
   - Single line linking to COMMAND_REFERENCE.md

## Constraints

- **≤60 lines total** (including blank lines, code blocks, everything) — D-04
- **Audience**: Developer who knows PD exists, wants workflow sequence — not marketing, not beginner tutorial — D-05
- **2 sections only**: diagram + quick-ref — D-03

## Command Mapping (from COMMAND_REFERENCE.md)

Key workflow commands to include:
- `pd:onboard` — Orient AI to codebase, create .planning/
- `pd:new-milestone` — Start new milestone with roadmap
- `pd:plan` — Technical planning and task breakdown
- `pd:write-code` — Execute tasks from TASKS.md
- `pd:test` — Write and run tests
- `pd:complete-milestone` — Finalize milestone
- `pd:what-next` — Suggest next command when lost
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite WORKFLOW_OVERVIEW.md</name>
  <files>docs/WORKFLOW_OVERVIEW.md</files>
  <action>
Delete all current content and create new file with:

1. **Title**: `# PD Workflow Overview`

2. **Section 1: Lifecycle Diagram**
   - Brief intro sentence (1 line)
   - Mermaid `flowchart LR` showing:
     ```
     Onboard --> Milestone --> Plan --> Code --> Test --> Complete
     ```
   - Include command names as node labels or annotations
   - Keep diagram simple — 6-8 nodes max

3. **Section 2: When to Use Which Command?**
   - Table format recommended:
     ```
     | I want to... | Command |
     |--------------|---------|
     | Start a new project | `pd:onboard` |
     | Define next milestone | `pd:new-milestone` |
     | ... | ... |
     ```
   - Cover 6-8 most common use cases
   - Include `pd:what-next` as the "I'm lost" escape hatch

4. **Footer**: Single line: `For command details, see [COMMAND_REFERENCE.md](COMMAND_REFERENCE.md).`

**Line budget:**
- Title: 1 line
- Blank lines: ~5
- Mermaid block: ~15 lines
- Quick-ref section: ~25 lines
- Footer: 2 lines
- **Target: 50-58 lines, hard cap 60**

Verify line count with `wc -l docs/WORKFLOW_OVERVIEW.md` before committing.
  </action>
  <verify>
    <automated>wc -l docs/WORKFLOW_OVERVIEW.md | awk '{print ($1 <= 60) ? "PASS: "$1" lines" : "FAIL: "$1" lines (max 60)"}'</automated>
  </verify>
  <done>
- WORKFLOW_OVERVIEW.md exists with new content
- Line count ≤60
- Contains Mermaid flowchart LR block
- Contains "When to use" section with table
- Footer links to COMMAND_REFERENCE.md
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify diagram renders and content accuracy</name>
  <files>docs/WORKFLOW_OVERVIEW.md</files>
  <action>
Run validation checks:

1. **Mermaid syntax check**: Extract the Mermaid block and verify it's valid flowchart LR syntax
   - Must start with `flowchart LR`
   - Must have at least 5 nodes
   - No syntax errors

2. **Link check**: Verify footer link target exists
   - `docs/COMMAND_REFERENCE.md` must exist

3. **Content check**: Verify key commands are mentioned
   - Must mention: onboard, plan, write-code, test, what-next
   - Must have a "when to use" or "which command" section

4. **Final line count**: Confirm ≤60 lines
  </action>
  <verify>
    <automated>grep -c "flowchart LR" docs/WORKFLOW_OVERVIEW.md && test -f docs/COMMAND_REFERENCE.md && echo "Link target exists" && wc -l docs/WORKFLOW_OVERVIEW.md</automated>
  </verify>
  <done>
- Mermaid block uses flowchart LR
- COMMAND_REFERENCE.md link target exists
- Line count confirmed ≤60
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — documentation-only changes with no code execution.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-01 | N/A | docs/WORKFLOW_OVERVIEW.md | accept | Documentation file — no security impact |
</threat_model>

<verification>
```bash
# Line count check (must be ≤60)
wc -l docs/WORKFLOW_OVERVIEW.md

# Mermaid block exists
grep -q "flowchart LR" docs/WORKFLOW_OVERVIEW.md && echo "✓ Mermaid flowchart LR found"

# Footer link exists
grep -q "COMMAND_REFERENCE.md" docs/WORKFLOW_OVERVIEW.md && echo "✓ Footer link found"

# Key commands mentioned
for cmd in onboard plan write-code test what-next; do
  grep -qi "$cmd" docs/WORKFLOW_OVERVIEW.md && echo "✓ $cmd mentioned"
done
```
</verification>

<success_criteria>
1. `docs/WORKFLOW_OVERVIEW.md` is ≤60 lines (SC-1)
2. Contains a Mermaid flowchart LR diagram showing the lifecycle (SC-2)
3. Answers "when do I use which command?" via quick-ref section (SC-3)
4. Footer links to COMMAND_REFERENCE.md
5. No content from old file preserved (full rewrite)
</success_criteria>

<output>
After completion, create `.planning/phases/149-documentation-flow/149-01-SUMMARY.md`

Commit message: `docs(149): rewrite WORKFLOW_OVERVIEW with Mermaid lifecycle diagram`
</output>
