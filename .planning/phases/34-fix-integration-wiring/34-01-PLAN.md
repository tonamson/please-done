---
phase: 34-fix-integration-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/fix-bug.md
  - test/snapshots/codex/fix-bug.md
  - test/snapshots/copilot/fix-bug.md
  - test/snapshots/gemini/fix-bug.md
  - test/snapshots/opencode/fix-bug.md
autonomous: true
requirements:
  - ORCH-03
  - ORCH-04
  - PROT-03
  - PROT-06
  - PROT-08
  - FLOW-02
  - FLOW-05
gap_closure: true

must_haves:
  truths:
    - "mergeParallelResults nhan dung param names {detectiveResult, docSpecResult}"
    - "buildContinuationContext nhan object {evidencePath, userAnswer, sessionDir, currentRound, agentName}"
    - "prepareSelfFix return fields duoc doc dung: filesForReview, resumeHint"
    - "prepareFixNow return fields duoc doc dung: action, reusableModules, evidence, suggestion"
    - "isHeavyAgent() duoc goi truoc khi spawn Detective tai Buoc 2"
    - "shouldDegrade() duoc goi trong error handler khi agent spawn fail"
  artifacts:
    - path: "workflows/fix-bug.md"
      provides: "Orchestrator workflow v2.1 voi dung call signatures"
      contains: "detectiveResult, docSpecResult"
  key_links:
    - from: "workflows/fix-bug.md dong 130"
      to: "bin/lib/parallel-dispatch.js dong 66"
      via: "mergeParallelResults({ detectiveResult, docSpecResult })"
      pattern: "detectiveResult.*docSpecResult"
    - from: "workflows/fix-bug.md dong 225"
      to: "bin/lib/checkpoint-handler.js dong 65"
      via: "buildContinuationContext({ evidencePath, userAnswer, sessionDir, currentRound, agentName })"
      pattern: "evidencePath.*userAnswer.*sessionDir.*currentRound.*agentName"
    - from: "workflows/fix-bug.md dong 207"
      to: "bin/lib/outcome-router.js dong 85-93"
      via: "prepareFixNow returns { action, reusableModules, evidence, suggestion }"
      pattern: "action.*reusableModules.*evidence.*suggestion"
    - from: "workflows/fix-bug.md dong 215"
      to: "bin/lib/outcome-router.js dong 145"
      via: "prepareSelfFix returns { filesForReview, resumeHint }"
      pattern: "filesForReview.*resumeHint"
---

<objective>
Sua 4 call signature bugs va them 2 enforcement points trong workflows/fix-bug.md de tat ca modules duoc goi dung tu workflow.

Purpose: Audit v2.1 phat hien 4 bugs se crash at runtime va 2 functions duoc build nhung khong bao gio goi. Plan nay fix toan bo wiring trong file workflow chinh.
Output: workflows/fix-bug.md voi dung call signatures, snapshots cap nhat, tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-fix-integration-wiring/34-CONTEXT.md

<interfaces>
<!-- Module signatures — KHONG sua modules, chi sua caller trong fix-bug.md -->

From bin/lib/parallel-dispatch.js dong 66:
```javascript
function mergeParallelResults({ detectiveResult, docSpecResult })
// detectiveResult: { evidenceContent, error } | null
// docSpecResult: { evidenceContent, error } | null
```

From bin/lib/checkpoint-handler.js dong 65:
```javascript
function buildContinuationContext({ evidencePath, userAnswer, sessionDir, currentRound, agentName })
// Returns: { prompt, agentName, round, canContinue, warnings }
```

From bin/lib/outcome-router.js dong 79-93:
```javascript
function prepareFixNow(evidenceContent)
// Returns: { action, reusableModules, evidence, suggestion, commitPrefix, warnings }
```

From bin/lib/outcome-router.js dong 139-152:
```javascript
function prepareSelfFix(evidenceContent)
// Returns: { action, sessionUpdate, summary, filesForReview, resumeHint, warnings }
```

From bin/lib/resource-config.js:
```javascript
function isHeavyAgent(agentName) // Returns boolean
function shouldDegrade(error)    // Returns boolean
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sua 4 call signature bugs trong fix-bug.md (D-01, D-02, D-03, D-04)</name>
  <files>workflows/fix-bug.md</files>
  <read_first>
    - workflows/fix-bug.md (file can sua — doc toan bo de thay current state)
    - bin/lib/parallel-dispatch.js (dong 60-80 — xac nhan mergeParallelResults signature)
    - bin/lib/checkpoint-handler.js (dong 55-78 — xac nhan buildContinuationContext signature)
    - bin/lib/outcome-router.js (dong 78-155 — xac nhan prepareFixNow va prepareSelfFix signatures)
  </read_first>
  <action>
Thuc hien 4 text replacements trong workflows/fix-bug.md:

**BUG-1 (D-01): mergeParallelResults param names — dong 130-132**
Tim:
```
6. Goi `mergeParallelResults({ detective: detectiveResult, docSpec: docSpecResult })` tu `bin/lib/parallel-dispatch.js`
   - detective: { evidenceContent: detectiveContent } hoac { error: { message: '...' } }
   - docSpec: { evidenceContent: docSpecContent } hoac { error: { message: '...' } } hoac null
```
Thay bang:
```
6. Goi `mergeParallelResults({ detectiveResult, docSpecResult })` tu `bin/lib/parallel-dispatch.js`
   - detectiveResult: { evidenceContent: detectiveContent } hoac { error: { message: '...' } }
   - docSpecResult: { evidenceContent: docSpecContent } hoac { error: { message: '...' } } hoac null
```

**BUG-2 (D-02): buildContinuationContext call signature — dong 225-226**
Tim:
```
  3. User tra loi -> Goi `buildContinuationContext(content, userAnswer, roundNumber)` tu `bin/lib/checkpoint-handler.js`
     -> { canContinue, prompt, warnings }
```
Thay bang:
```
  3. User tra loi -> Goi `buildContinuationContext({ evidencePath: '{session_dir}/evidence_architect.md', userAnswer, sessionDir: session_dir, currentRound: roundNumber, agentName: 'pd-fix-architect' })` tu `bin/lib/checkpoint-handler.js`
     -> { canContinue, prompt, agentName, round, warnings }
```

**BUG-3 (D-03): prepareSelfFix return fields — dong 215-216**
Tim:
```
     - self_fix -> Goi `prepareSelfFix(content)` tu `bin/lib/outcome-router.js`
       -> { summary, suggestedSteps }
       Hien summary va suggestedSteps cho user.
```
Thay bang:
```
     - self_fix -> Goi `prepareSelfFix(content)` tu `bin/lib/outcome-router.js`
       -> { action, sessionUpdate, summary, filesForReview, resumeHint, warnings }
       Hien summary, filesForReview va resumeHint cho user.
```

**BUG-4 (D-04): prepareFixNow return fields — dong 207, 266-269, 277**

Dong 207 — Tim:
```
     - fix_now -> Goi `prepareFixNow(content)` tu `bin/lib/outcome-router.js`
       -> { fixInstructions, targetFiles, rootCause } -> Buoc 5
```
Thay bang:
```
     - fix_now -> Goi `prepareFixNow(content)` tu `bin/lib/outcome-router.js`
       -> { action, reusableModules, evidence, suggestion, commitPrefix, warnings } -> Buoc 5
```

Dong 266 — Tim:
```
1. Doc fixInstructions tu prepareFixNow() (Buoc 4) -> lay targetFiles, targetFunction
```
Thay bang:
```
1. Doc evidence va suggestion tu prepareFixNow() (Buoc 4) -> parse targetFiles va targetFunction tu evidence content
```

Dong 277 — Tim:
```
1. Doc fixInstructions va rootCause tu prepareFixNow() output
```
Thay bang:
```
1. Doc evidence va suggestion tu prepareFixNow() output — evidence chua bang chung, suggestion chua de xuat fix
```
  </action>
  <verify>
    <automated>grep -n "detectiveResult, docSpecResult" workflows/fix-bug.md && grep -n "evidencePath.*userAnswer.*sessionDir.*currentRound.*agentName" workflows/fix-bug.md && grep -n "filesForReview.*resumeHint" workflows/fix-bug.md && grep -n "action.*reusableModules.*evidence.*suggestion.*commitPrefix" workflows/fix-bug.md && echo "ALL 4 BUGS FIXED"</automated>
  </verify>
  <acceptance_criteria>
    - grep "detectiveResult, docSpecResult" workflows/fix-bug.md tra ve dong 130 (KHONG con "detective:" hoac "docSpec:")
    - grep "evidencePath.*session_dir.*evidence_architect" workflows/fix-bug.md tra ve dong 225
    - grep "filesForReview.*resumeHint" workflows/fix-bug.md tra ve dong 215-216
    - grep "action.*reusableModules.*evidence.*suggestion.*commitPrefix" workflows/fix-bug.md tra ve dong 207
    - KHONG con string "fixInstructions" trong file (grep -c "fixInstructions" workflows/fix-bug.md = 0)
    - KHONG con string "suggestedSteps" trong file (grep -c "suggestedSteps" workflows/fix-bug.md = 0)
  </acceptance_criteria>
  <done>4 call signatures trong fix-bug.md khop chinh xac voi module definitions. Khong con fixInstructions, suggestedSteps, hoac sai param names.</done>
</task>

<task type="auto">
  <name>Task 2: Them isHeavyAgent() va shouldDegrade() enforcement (D-05, D-06) + regenerate snapshots</name>
  <files>workflows/fix-bug.md, test/snapshots/codex/fix-bug.md, test/snapshots/copilot/fix-bug.md, test/snapshots/gemini/fix-bug.md, test/snapshots/opencode/fix-bug.md</files>
  <read_first>
    - workflows/fix-bug.md (doc lai sau Task 1 de thay state hien tai)
    - bin/lib/resource-config.js (dong 135-170 — xac nhan isHeavyAgent va shouldDegrade signatures)
  </read_first>
  <action>
**D-05: Them isHeavyAgent() check vao Buoc 2**

Tim dong bat dau "## Buoc 2: Phan tich code va tai lieu" (dong 102-104), sau banner "--- Buoc 2/5 ---", TRUOC dong "1. Goi `buildParallelPlan`":

Them block moi giua banner va buildParallelPlan:

```
0. Kiem tra Heavy Lock:
   Goi `isHeavyAgent('pd-code-detective')` tu `bin/lib/resource-config.js`
   - Ket qua true -> WARNING: "Detective dung heavy tool (FastCode). Dam bao khong co tac vu nang khac dang chay."
   - Ghi warning vao SESSION.md neu co
```

Cu the: sau dong 104 (`--- Buoc 2/5: Phan tich code va tai lieu ---`), truoc dong 106 (`1. Goi \`buildParallelPlan`), chen:

```
0. Goi `isHeavyAgent('pd-code-detective')` tu `bin/lib/resource-config.js`
   - true -> WARNING: "Detective dung heavy tool (FastCode). Chi 1 tac vu nang tai 1 thoi diem."
   - Ghi warning vao `{session_dir}/SESSION.md` qua updateSession() neu co

```

**D-06: Them shouldDegrade() call trong error handler cua Buoc 2**

Tim dong "DocSpec fail la NON-BLOCKING" (dong 145). Truoc dong do, sau dong "CA HAI FAIL -> STOP" (dong 143), them:

```
   - Detective FAIL do timeout/spawn error:
     Goi `shouldDegrade(error)` tu `bin/lib/resource-config.js`
     true -> WARNING: "Ha cap sang che do tuan tu." Spawn DocSpec truoc, dung evidence_docs.md de spawn Detective lai.
     false -> xu ly nhu tren (STOP neu ca hai fail)

```

**Regenerate snapshots:**
Chay `node test/generate-snapshots.js` de cap nhat 4 snapshot files (codex, copilot, gemini, opencode) cho fix-bug.md.

**Chay tests:**
Chay `node --test test/smoke-integrity.test.js test/smoke-snapshot.test.js` de xac nhan toan bo pass.
  </action>
  <verify>
    <automated>grep -n "isHeavyAgent" workflows/fix-bug.md && grep -n "shouldDegrade" workflows/fix-bug.md && node test/generate-snapshots.js && node --test test/smoke-integrity.test.js test/smoke-snapshot.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "isHeavyAgent.*pd-code-detective" workflows/fix-bug.md tra ve it nhat 1 dong trong Buoc 2
    - grep "shouldDegrade" workflows/fix-bug.md tra ve it nhat 1 dong trong Buoc 2 error handling
    - node --test test/smoke-integrity.test.js exit code 0
    - node --test test/smoke-snapshot.test.js exit code 0
  </acceptance_criteria>
  <done>isHeavyAgent() va shouldDegrade() duoc goi tu workflow. Snapshots cap nhat. Toan bo tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -c "fixInstructions\|suggestedSteps\|detective:\|docSpec:" workflows/fix-bug.md` = 0 (khong con old param names)
2. `grep "detectiveResult, docSpecResult" workflows/fix-bug.md` match dong 130
3. `grep "evidencePath.*currentRound.*agentName" workflows/fix-bug.md` match dong 225
4. `grep "isHeavyAgent" workflows/fix-bug.md` match trong Buoc 2
5. `grep "shouldDegrade" workflows/fix-bug.md` match trong Buoc 2
6. `node --test test/smoke-integrity.test.js test/smoke-snapshot.test.js` pass
</verification>

<success_criteria>
- 4 call signature bugs da fix (BUG-1 den BUG-4)
- 2 enforcement points da them (ORCH-03, ORCH-04)
- Snapshots cap nhat khop voi fix-bug.md moi
- Toan bo smoke tests pass
</success_criteria>

<output>
Sau khi hoan thanh, tao `.planning/phases/34-fix-integration-wiring/34-01-SUMMARY.md`
</output>
