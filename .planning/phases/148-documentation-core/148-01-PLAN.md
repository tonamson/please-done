---
phase: 148-documentation-core
plan: 01
type: execute
wave: 1
title: "Update cheatsheet.md — add 4 commands, fix count, remove stale footer"
goal: "Surgically update docs/cheatsheet.md so it covers all 20 commands and contains no stale data"
depends_on: []
files_modified:
  - docs/cheatsheet.md
autonomous: true
requirements:
  - DOCS-148-cheatsheet-count
  - DOCS-148-cheatsheet-rows
  - DOCS-148-cheatsheet-footer

must_haves:
  truths:
    - "The intro paragraph says '20 Please Done (PD) commands'"
    - "The TOC Utility line says '9 commands'"
    - "The Utility Commands table has exactly 9 rows (5 existing + 4 new)"
    - "The Command Count Summary shows Utility count 9 and Total 20"
    - "The stale footer referencing commands/pd/ is gone"
  artifacts:
    - path: "docs/cheatsheet.md"
      provides: "Complete cheatsheet for all 20 commands"
      contains: "20 Please Done"
  key_links:
    - from: "docs/cheatsheet.md line 6"
      to: "actual command count"
      via: "literal text '20 Please Done'"
      pattern: "20 Please Done"
---

# Plan 148-01: Update cheatsheet.md

## Goal
Surgically update `docs/cheatsheet.md` to add 4 missing commands (pd:stats, pd:health, pd:discover, pd:sync-version), fix the stale command count from 16 to 20, and remove the stale footer that references the `commands/pd/` directory.

## Context
The cheatsheet currently documents 16 commands but the project has 20. Four commands (stats, health, discover, sync-version) are missing entirely. The intro paragraph, TOC, Command Count Summary table, and a footer all carry the stale count "16". No structural changes to existing sections — only additive edits plus a footer deletion.

Locked decisions in effect:
- **D-01**: 20 commands total
- **D-02**: Surgical update only — do not restructure existing sections or move existing rows
- **D-03**: All 4 new commands go in the Utility Commands section
- **D-09**: Remove the stale footer (last 2 lines)

## Tasks

### Task 1: Fix stale "16" count in intro, TOC, and Command Count Summary

**File(s)**: `docs/cheatsheet.md`

**Action**: Make three targeted text replacements in `docs/cheatsheet.md`:

1. **Line 6 — Intro paragraph**: Replace `"16 Please Done (PD) commands"` with `"20 Please Done (PD) commands"`.

   Old:
   ```
   Quick reference for all 16 Please Done (PD) commands. Each command follows the `/pd:command` syntax with optional flags and arguments.
   ```
   New:
   ```
   Quick reference for all 20 Please Done (PD) commands. Each command follows the `/pd:command` syntax with optional flags and arguments.
   ```

2. **Line 21 — Table of Contents, Utility entry**: Replace `"5 commands for status and helpers"` with `"9 commands for status and helpers"`.

   Old:
   ```
   - [Utility Commands](#utility-commands) — 5 commands for status and helpers
   ```
   New:
   ```
   - [Utility Commands](#utility-commands) — 9 commands for status and helpers
   ```

3. **Lines 197–198 — Command Count Summary table**: Replace the Utility row and Total row.

   Old:
   ```
   | Utility | status, conventions, fetch-doc, update, what-next | 5 |
   | **Total** | | **16** |
   ```
   New:
   ```
   | Utility | status, conventions, fetch-doc, update, what-next, stats, health, discover, sync-version | 9 |
   | **Total** | | **20** |
   ```

**Expected result**: `grep "20 Please Done" docs/cheatsheet.md` returns 1 match. `grep "**16**\|16 Please Done" docs/cheatsheet.md` returns no matches.

---

### Task 2: Add 4 new rows to the Utility Commands table

**File(s)**: `docs/cheatsheet.md`

**Action**: Insert 4 new table rows immediately after line 116 (the existing `/pd:what-next` row). The rows must use the same `| Command | Usage | Example |` format as the existing 5 rows. Add them in this exact order:

```
| `/pd:stats` | `/pd:stats [--json]` | `/pd:stats` |
| `/pd:health` | `/pd:health [--json]` | `/pd:health` |
| `/pd:discover` | `/pd:discover [--verbose] [--json]` | `/pd:discover` |
| `/pd:sync-version` | `/pd:sync-version [--check]` | `/pd:sync-version` |
```

After the insert, the Utility Commands table block should look like:

```markdown
| Command | Usage | Example |
|---------|-------|---------|
| `/pd:status` | `/pd:status [--auto-refresh] [--refresh-threshold=N]` | `/pd:status --auto-refresh` |
| `/pd:conventions` | `/pd:conventions [language]` | `/pd:conventions typescript` |
| `/pd:fetch-doc` | `/pd:fetch-doc [library]` | `/pd:fetch-doc react` |
| `/pd:update` | `/pd:update [--check]` | `/pd:update` |
| `/pd:what-next` | `/pd:what-next` | `/pd:what-next` |
| `/pd:stats` | `/pd:stats [--json]` | `/pd:stats` |
| `/pd:health` | `/pd:health [--json]` | `/pd:health` |
| `/pd:discover` | `/pd:discover [--verbose] [--json]` | `/pd:discover` |
| `/pd:sync-version` | `/pd:sync-version [--check]` | `/pd:sync-version` |
```

Do NOT add Notes bullet points for the 4 new commands — the existing Notes block below the table is left untouched (surgical edit only).

**Expected result**: `grep -c "^| \`/pd:" docs/cheatsheet.md` returns 20.

---

### Task 3: Remove stale footer (last 2 lines)

**File(s)**: `docs/cheatsheet.md`

**Action**: Delete the last 2 lines of the file (currently lines 203–204):

```
*For detailed documentation on each command, see the `commands/pd/` directory or run `/pd:status` to check project state.*
```

And the blank line immediately before it (line 203 blank). The file should end after:

```
*Last updated: 2026-04-04*
```

The final 4 lines of the file after the edit should be:

```
---

*Last updated: 2026-04-04*
```
(trailing newline only, no extra blank line or stale footer)

**Expected result**: `grep "commands/pd/" docs/cheatsheet.md` returns no matches.

## Verification

Run all three checks — every one must pass:

```bash
# 1. Intro says "20 Please Done"
grep "20 Please Done" docs/cheatsheet.md

# 2. Exactly 20 command rows in the cheatsheet tables
grep -c "^| \`/pd:" docs/cheatsheet.md

# 3. TOC Utility line updated
grep "9 commands for status" docs/cheatsheet.md

# 4. Command Count Summary updated
grep "| \*\*Total\*\* |" docs/cheatsheet.md | grep "\*\*20\*\*"

# 5. Stale footer gone
grep "commands/pd/" docs/cheatsheet.md && echo "FAIL: stale footer found" || echo "PASS: footer removed"

# 6. All 4 new commands present
grep "pd:stats\|pd:health\|pd:discover\|pd:sync-version" docs/cheatsheet.md
```

Expected outputs:
- Check 1: one line containing `20 Please Done`
- Check 2: `20`
- Check 3: one line containing `9 commands for status`
- Check 4: one line containing `**20**`
- Check 5: `PASS: footer removed`
- Check 6: 4+ lines (one per new command, possibly more from the Summary table)

## Commit message

```
docs(148): update cheatsheet.md — add 4 commands, fix count 16→20, remove stale footer
```

<output>
After completing all tasks, create `.planning/phases/148-documentation-core/148-01-SUMMARY.md` with:
- What was changed (list of edits made)
- Verification results (paste command outputs)
- Any deviations from plan (none expected)
</output>
