---
phase: 24-workflow-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/report-filler.js
  - test/smoke-report-filler.test.js
  - workflows/complete-milestone.md
autonomous: true
requirements:
  - INTG-01
  - INTG-02

must_haves:
  truths:
    - "fillManagementReport() nhan template content va tra ve filled markdown string voi placeholders da thay the"
    - "Mermaid code block Section 3 duoc thay bang business logic diagram, Section 4 duoc thay bang architecture diagram, KHONG lan nhau"
    - "Khi generateBusinessLogicDiagram hoac generateArchitectureDiagram throw, pipeline tiep tuc va giu placeholder goc"
    - "fillManagementReport() KHONG doc file — chi nhan content strings lam tham so"
    - "workflows/complete-milestone.md co Buoc 3.6 giua Buoc 3.5 va Buoc 4, voi 4 sub-steps non-blocking"
  artifacts:
    - path: "bin/lib/report-filler.js"
      provides: "fillManagementReport pure function"
      exports: ["fillManagementReport"]
    - path: "test/smoke-report-filler.test.js"
      provides: "Unit tests cho report-filler module"
      min_lines: 80
    - path: "workflows/complete-milestone.md"
      provides: "Workflow co Buoc 3.6 tao bao cao quan ly"
      contains: "Buoc 3.6"
  key_links:
    - from: "bin/lib/report-filler.js"
      to: "bin/lib/generate-diagrams.js"
      via: "require('./generate-diagrams')"
      pattern: "require\\('./generate-diagrams'\\)"
    - from: "bin/lib/report-filler.js"
      to: "bin/lib/utils.js"
      via: "require('./utils')"
      pattern: "require\\('./utils'\\)"
    - from: "workflows/complete-milestone.md"
      to: "bin/lib/report-filler.js"
      via: "Huong dan AI goi fillManagementReport()"
      pattern: "fillManagementReport"
    - from: "workflows/complete-milestone.md"
      to: "bin/generate-pdf-report.js"
      via: "Huong dan AI chay CLI"
      pattern: "generate-pdf-report"
---

<objective>
Tich hop pipeline bao cao quan ly vao workflow complete-milestone.

Purpose: Khi milestone hoan thanh, AI tu dong tao so do Mermaid + fill template bao cao + xuat PDF. Toan bo pipeline la non-blocking — loi chi log warning, khong chan milestone completion.

Output: Module report-filler.js (pure function), tests, va workflow update voi Buoc 3.6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-workflow-integration/24-CONTEXT.md
@.planning/phases/24-workflow-integration/24-RESEARCH.md

<interfaces>
<!-- Module hien co ma report-filler.js se goi -->

Tu bin/lib/generate-diagrams.js:
```javascript
// generateBusinessLogicDiagram(planContents, options?)
// planContents: Array<{ planNumber: number, content: string, phase: string }>
// Tra ve: { diagram: string, valid: boolean, errors: string[], warnings: string[], truthCount: number, planCount: number }
module.exports = { generateBusinessLogicDiagram, generateArchitectureDiagram };
```

Tu bin/lib/utils.js:
```javascript
// parseFrontmatter(content) -> { frontmatter: object, body: string, raw: string }
// log.info(), log.warn(), log.error(), log.success()
module.exports = { parseFrontmatter: parseFrontmatter, log, ... };
```

Tu templates/management-report.md:
```markdown
# Bao cao quan ly
> Milestone: {{milestone_name}} ({{version}})
> Ngay: {{date}}
## 1. Tom tat dieu hanh        <!-- AI fill: ... -->
## 2. Tong quan Milestone       <!-- AI fill: ... --> + bang {{phase_name}} {{status}} {{plan_count}} {{duration}}
## 3. Luong nghiep vu           ```mermaid ... ``` (flowchart TD placeholder)
## 4. Kien truc tong quan       ```mermaid ... ``` (flowchart LR placeholder)
## 5. Thanh tuu noi bat         <!-- AI fill: ... -->
## 6. Chi so chat luong         <!-- AI fill: ... --> + bang {{metric_name}} {{metric_value}}
## 7. Buoc tiep theo            <!-- AI fill: ... -->
```

Tu test/smoke-pdf-renderer.test.js (test pattern):
```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
// describe('moduleName', () => { it('Test N: mo ta', () => { ... }); });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD report-filler.js — fillManagementReport() pure function</name>
  <files>bin/lib/report-filler.js, test/smoke-report-filler.test.js</files>
  <read_first>
    - templates/management-report.md (template goc voi placeholders — day la input cua fillManagementReport)
    - bin/lib/generate-diagrams.js (API signatures: generateBusinessLogicDiagram, generateArchitectureDiagram)
    - bin/lib/utils.js (parseFrontmatter signature)
    - test/smoke-pdf-renderer.test.js (test pattern: node:test + node:assert/strict)
    - .planning/STATE.md (data format: YAML frontmatter + Markdown body voi Performance Metrics table)
  </read_first>
  <behavior>
    - Test 1: Header placeholders — fillManagementReport({templateContent: template_goc, version: 'v1.4', milestoneName: 'Mermaid Diagrams', date: '2026-03-24', ...defaults}) => output chua 'v1.4', 'Mermaid Diagrams', '2026-03-24', KHONG chua '{{milestone_name}}' hoac '{{version}}' hoac '{{date}}'
    - Test 2: Mermaid Section 3 — khi planContents co data hop le, Section 3 (giua '## 3.' va '## 4.') chua 'flowchart TD', Section 4 KHONG chua 'flowchart TD'
    - Test 3: Mermaid Section 4 — khi codebaseMaps va planMeta co data, Section 4 (sau '## 4.') chua 'flowchart LR', Section 3 KHONG chua 'flowchart LR'
    - Test 4: AI comments xoa — output KHONG chua '<!-- AI fill'
    - Test 5: Non-blocking business diagram — khi generateBusinessLogicDiagram throw, fillManagementReport KHONG throw, output van la string hop le co Section 3 heading
    - Test 6: Non-blocking architecture diagram — khi generateArchitectureDiagram throw, fillManagementReport KHONG throw, output van la string hop le co Section 4 heading
    - Test 7: Return structure — result luon co { filledMarkdown: string, diagramResults: { business: object, architecture: object } }
    - Test 8: Khong require('fs') — doc file report-filler.js, kiem tra KHONG chua require('fs') hoac require('node:fs')
    - Test 9: Empty inputs khong throw — fillManagementReport({ templateContent: '', stateContent: '', planContents: [], summaryContents: [] }) tra ve { filledMarkdown: string } ma khong throw
  </behavior>
  <action>
    RED: Tao test/smoke-report-filler.test.js truoc voi 9 test cases tren.
    - Dung pattern tu smoke-pdf-renderer.test.js: 'use strict', require('node:test'), require('node:assert/strict')
    - Import { fillManagementReport } tu '../bin/lib/report-filler'
    - Template mau cho tests: doc templates/management-report.md noi dung goc lam templateContent input
    - Test 8 dung require('fs').readFileSync de doc source code report-filler.js va assert khong chua 'require.*fs'
    - Chay tests => TAT CA PHAI FAIL (module chua ton tai)

    GREEN: Tao bin/lib/report-filler.js:
    - 'use strict'
    - const { parseFrontmatter } = require('./utils');
    - const { generateBusinessLogicDiagram, generateArchitectureDiagram } = require('./generate-diagrams');
    - function fillManagementReport(params):
      - Destructure: { templateContent, stateContent, planContents = [], summaryContents = [], codebaseMaps = {}, planMeta = {}, version = '', milestoneName = '', date = new Date().toISOString().split('T')[0] }
      - let filled = templateContent;
      - Buoc 1: Replace header placeholders: filled.replace(/\{\{milestone_name\}\}/g, milestoneName), tuong tu cho version, date
      - Buoc 2: Parse STATE.md body cho velocity data — parseFrontmatter(stateContent), doc body bang regex de lay Performance Metrics table rows
      - Buoc 3: Fill Section 2 — thay dong '| {{phase_name}} | {{status}} | ...' bang data rows thuc te tu STATE.md body (regex match bang `| Phase ... |`). Neu khong co data, thay bang "(Khong co du lieu phases)"
      - Buoc 4: Fill Section 3 — try { businessResult = generateBusinessLogicDiagram(planContents) } catch(e) { businessResult = default }. Goi replaceMermaidBlock(filled, '## 3.', businessResult.diagram)
      - Buoc 5: Fill Section 4 — try { archResult = generateArchitectureDiagram(codebaseMaps, planMeta) } catch(e) { archResult = default }. Goi replaceMermaidBlock(filled, '## 4.', archResult.diagram)
      - Buoc 6: Fill Sections 1, 5, 6, 7 — thay `<!-- AI fill ... -->` comments bang data tu summaryContents va stateContent. Section 1: bullet points tu milestone info. Section 5: features tu summaryContents. Section 6: thay bang {{metric_name}}/{{metric_value}} bang stats tu STATE.md. Section 7: "(Xem ROADMAP.md)" placeholder
      - Buoc 7: Xoa tat ca con lai `<!-- AI fill ... -->` comments: filled.replace(/<!--\s*AI fill[^>]*-->\n?/g, '')
      - Return { filledMarkdown: filled, diagramResults: { business: businessResult, architecture: archResult } }
    - function replaceMermaidBlock(content, sectionPrefix, newDiagram):
      - Neu !newDiagram return content (giu placeholder goc)
      - Tim vi tri sectionPrefix trong content
      - Tim mermaid code block gan nhat SAU section heading bang regex /```mermaid\n[\s\S]*?```/
      - Replace chi block do, giu phan con lai nguyen ven
    - Helper parseStateData(stateContent) — lay milestone info va metrics tu STATE.md body
    - Helper fillPhaseTable(filled, stateData) — thay placeholder table rows
    - Helper fillSummarySections(filled, summaryContents, stateData) — fill sections 1, 5, 6, 7
    - module.exports = { fillManagementReport }
    - Chay tests => TAT CA PHAI PASS

    REFACTOR: Rut gon helpers neu can, dam bao code sach.

    Quy uoc per D-07: Pure function — KHONG doc file, KHONG goi AI API.
    Quy uoc per D-03/D-04: Try/catch rieng tung diagram call, loi giu placeholder goc.
    Quy uoc per CLAUDE.md: Comments tieng Viet co dau.
  </action>
  <verify>
    <automated>node --test test/smoke-report-filler.test.js</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/report-filler.js ton tai va chua 'module.exports' voi 'fillManagementReport'
    - bin/lib/report-filler.js chua "require('./generate-diagrams')" va "require('./utils')"
    - bin/lib/report-filler.js KHONG chua "require('fs')" va KHONG chua "require('node:fs')"
    - bin/lib/report-filler.js chua 'use strict' o dong dau
    - bin/lib/report-filler.js chua function replaceMermaidBlock
    - test/smoke-report-filler.test.js ton tai va co it nhat 9 test cases (it() calls)
    - node --test test/smoke-report-filler.test.js exit 0 voi 0 failures
    - node -e "const m = require('./bin/lib/report-filler'); if (typeof m.fillManagementReport !== 'function') process.exit(1)" exit 0
  </acceptance_criteria>
  <done>
    fillManagementReport() nhan template + data strings, tra ve filled markdown voi header placeholders da thay, Mermaid blocks section-specific, AI comments da xoa. Non-blocking khi diagram generation loi. 9 tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cap nhat workflow complete-milestone.md — them Buoc 3.6 tao bao cao quan ly (per D-01, D-02, D-03)</name>
  <files>workflows/complete-milestone.md</files>
  <read_first>
    - workflows/complete-milestone.md (file hien tai — xac dinh vi tri chen Buoc 3.6 giua dong 92-94 va Buoc 4 dong 96)
    - .planning/phases/24-workflow-integration/24-CONTEXT.md (D-01 vi tri, D-02 thu tu, D-03 error handling, D-05 output path, D-06 MILESTONE_COMPLETE section)
    - .planning/phases/24-workflow-integration/24-RESEARCH.md (Pattern 2: workflow markdown template cho Buoc 3.6)
  </read_first>
  <action>
    Chen Buoc 3.6 vao workflows/complete-milestone.md:

    Vi tri: SAU dong `---` (khoang dong 94) va TRUOC `## Buoc 4: Bao cao tong ket` (dong 96).
    Them mot block `---` separator truoc Buoc 4 neu can.

    Noi dung chen (CHINH XAC nhu sau):

    ```markdown
    ## Buoc 3.6: Tao bao cao quan ly (non-blocking)

    > QUAN TRONG: Toan bo buoc nay la non-blocking.
    > Bat ky loi nao chi log warning va ghi chu — KHONG BAO GIO chan milestone completion.

    Bien luu ket qua:
    - `reportPath` = null
    - `reportWarnings` = []

    ### 3.6a — Thu thap du lieu va tao so do
    Trong try/catch:
    1. Doc tat ca `.planning/phases/*/XX-*-PLAN.md` cua milestone hien tai
    2. Goi `generateBusinessLogicDiagram(planContents)` tu `bin/lib/generate-diagrams.js` -> luu ket qua
    3. Doc `.planning/codebase/ARCHITECTURE.md`
    4. Thu thap `filesModified` tu SUMMARY.md cac plans
    5. Goi `generateArchitectureDiagram(codebaseMaps, planMeta)` tu `bin/lib/generate-diagrams.js` -> luu ket qua
    6. NEU loi -> ghi reportWarnings, diagram se dung placeholder goc tu template

    ### 3.6b — Fill template bao cao
    Trong try/catch:
    1. Doc `templates/management-report.md`
    2. Doc `.planning/STATE.md`
    3. Doc tat ca `*-SUMMARY.md` files cua milestone
    4. Goi `fillManagementReport()` tu `bin/lib/report-filler.js` voi toan bo data
    5. Ghi filled markdown ra `.planning/reports/management-report-v{version}.md`
    6. NEU loi -> ghi reportWarnings, bo qua

    ### 3.6c — Xuat PDF
    Trong try/catch:
    1. Chay: `node bin/generate-pdf-report.js .planning/reports/management-report-v{version}.md`
    2. Kiem tra `.planning/reports/management-report-v{version}.pdf` ton tai
    3. `reportPath` = pdf path (hoac md fallback path)
    4. NEU loi -> `reportPath` = md path (fallback), ghi reportWarnings

    ### 3.6d — Ghi ket qua vao MILESTONE_COMPLETE.md
    Them vao MILESTONE_COMPLETE.md (Buoc 4):
    ```
    ## Bao cao quan ly
    - {reportPath ? "Duong dan: " + reportPath : "Khong tao duoc bao cao (xem canh bao)"}
    - {reportWarnings.length > 0 ? "Canh bao: " + reportWarnings.join(", ") : "Khong co canh bao"}
    ```
    ```

    Cap nhat dong 91-92 cua file hien tai:
    - Dong 91 (`- TAT CA dat -> Buoc 4`): thay "Buoc 4" thanh "Buoc 3.6"
    - Dong 92 (`- Co van de -> ... (2) Bo qua`): thay reference "Buoc 4" thanh "Buoc 3.6" neu co

    KHONG thay doi bat ky noi dung nao khac trong file. Chi chen Buoc 3.6 va cap nhat references.
  </action>
  <verify>
    <automated>grep -c "Buoc 3.6" workflows/complete-milestone.md | grep -qv "^0$" && grep -c "3\.6[abcd]" workflows/complete-milestone.md | grep -q "[4-9]" && grep -ci "non-blocking" workflows/complete-milestone.md | grep -qv "^0$" && grep -c "fillManagementReport" workflows/complete-milestone.md | grep -qv "^0$" && grep -c "generate-pdf-report" workflows/complete-milestone.md | grep -qv "^0$" && grep -c "Bao cao quan ly" workflows/complete-milestone.md | grep -qv "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/complete-milestone.md chua "## Buoc 3.6: Tao bao cao quan ly (non-blocking)"
    - workflows/complete-milestone.md chua "### 3.6a", "### 3.6b", "### 3.6c", "### 3.6d" (4 sub-steps)
    - workflows/complete-milestone.md chua "KHONG BAO GIO chan milestone completion"
    - workflows/complete-milestone.md chua "fillManagementReport()" va "bin/lib/report-filler.js"
    - workflows/complete-milestone.md chua "generate-pdf-report.js"
    - workflows/complete-milestone.md chua "## Bao cao quan ly" (section reference cho MILESTONE_COMPLETE.md)
    - Buoc 3.6 nam GIUA Buoc 3.5 va Buoc 4 (grep line numbers xac nhan thu tu)
    - node --test 'test/*.test.js' exit 0 (khong regression)
  </acceptance_criteria>
  <done>
    Workflow complete-milestone.md co Buoc 3.6 voi 4 sub-steps non-blocking. Moi sub-step co try/catch rieng, loi chi log warning. Pipeline: thu thap data + tao so do -> fill template -> xuat PDF -> ghi ket qua. Khong regression cho tests hien co.
  </done>
</task>

</tasks>

<verification>
1. Module test: `node --test test/smoke-report-filler.test.js` — 0 failures
2. Full regression: `node --test 'test/*.test.js'` — 0 failures
3. Module export: `node -e "const m = require('./bin/lib/report-filler'); if (typeof m.fillManagementReport !== 'function') process.exit(1)"` — exit 0
4. Pure function: `grep -c "require.*fs" bin/lib/report-filler.js` — ket qua "0"
5. Workflow content: `grep "Buoc 3.6" workflows/complete-milestone.md` — co ket qua
6. Sub-steps: `grep -c "3\.6[abcd]" workflows/complete-milestone.md` — ket qua >= 4
7. Non-blocking: `grep -i "non-blocking" workflows/complete-milestone.md` — co ket qua
</verification>

<success_criteria>
- fillManagementReport() fill template dung voi data tu STATE.md, PLAN.md, SUMMARY.md (INTG-01)
- Mermaid diagrams thay the dung section (3 = business TD, 4 = architecture LR) (INTG-01)
- Pipeline non-blocking: diagram/fill/PDF loi chi log warning, khong throw (INTG-02)
- Workflow co Buoc 3.6 voi huong dan chi tiet 4 sub-steps (INTG-01)
- 9+ tests pass, 0 regression tren test suite hien co (INTG-01, INTG-02)
</success_criteria>

<output>
Sau khi hoan thanh, tao `.planning/phases/24-workflow-integration/24-01-SUMMARY.md`
</output>
