---
phase: 21-mermaid-foundation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/mermaid-validator.js
  - test/smoke-mermaid-validator.test.js
autonomous: true
requirements: [MERM-02]

must_haves:
  truths:
    - "Function mermaidValidator() nhan Mermaid text va tra ve { valid, errors, warnings } — pure function, zero dependencies"
    - "Syntax errors (unclosed quotes, reserved keywords, missing declaration) tra ve valid=false voi errors co line number"
    - "Style warnings (wrong palette, unquoted labels, vuot 15 nodes) tra ve valid=true voi warnings co line number"
    - "Unit tests pass cho ca valid syntax, invalid syntax, va edge cases (empty string, Vietnamese characters, special characters)"
  artifacts:
    - path: "bin/lib/mermaid-validator.js"
      provides: "Pure function Mermaid validator"
      exports: ["mermaidValidator"]
    - path: "test/smoke-mermaid-validator.test.js"
      provides: "Unit tests cho mermaid-validator"
      contains: "mermaidValidator"
  key_links:
    - from: "test/smoke-mermaid-validator.test.js"
      to: "bin/lib/mermaid-validator.js"
      via: "require('../bin/lib/mermaid-validator')"
      pattern: "require.*mermaid-validator"
    - from: "bin/lib/mermaid-validator.js"
      to: "references/mermaid-rules.md"
      via: "implements rules defined in spec"
      pattern: "PALETTE|RESERVED_KEYWORDS|MAX_NODES|SHAPE_BY_ROLE"
---

<objective>
Implement mermaid-validator.js theo TDD: viet tests truoc (RED), implement validator (GREEN), refactor neu can. Validator la pure function nhan Mermaid text, tra ve { valid, errors, warnings } voi line numbers (per D-05, D-06).

Purpose: Cung cap validation layer de Phase 22 kiem tra diagrams truoc khi nhung vao report.
Output: bin/lib/mermaid-validator.js, test/smoke-mermaid-validator.test.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mermaid-foundation/21-CONTEXT.md
@.planning/phases/21-mermaid-foundation/21-RESEARCH.md

<interfaces>
<!-- Pattern tu bin/lib/plan-checker.js — mermaid-validator.js PHAI theo pattern nay -->

From bin/lib/plan-checker.js (module structure pattern):
```javascript
'use strict';
const { parseFrontmatter, extractXmlSection } = require('./utils');
// ... pure functions ...
module.exports = { checkRequirementCoverage, checkTaskCompleteness, ... };
```

From bin/lib/utils.js (available utilities — khong bat buoc dung):
```javascript
module.exports = { parseFrontmatter, extractXmlSection, buildFrontmatter, ... };
```

From test/smoke-plan-checker.test.js (test pattern):
```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const pc = require('../bin/lib/plan-checker');
function makePlanV10(overrides = {}) { /* helper tao test fixture */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Viet failing tests cho mermaid-validator (Wave 0)</name>
  <files>test/smoke-mermaid-validator.test.js</files>
  <read_first>
    - test/smoke-plan-checker.test.js (pattern: test structure, helper functions, describe/it blocks)
    - .planning/phases/21-mermaid-foundation/21-RESEARCH.md (Test Case Categories section, Error Object Format D-06)
    - .planning/phases/21-mermaid-foundation/21-CONTEXT.md (D-05: syntax vs style, D-06: error format voi line number)
  </read_first>
  <behavior>
    ## Valid diagrams (valid=true, errors=[], warnings=[])
    - Basic flowchart TD voi quoted labels: `flowchart TD\n  A["Bat dau"] --> B["Ket thuc"]` -> valid=true, errors.length=0
    - Flowchart LR voi tat ca 6 shape types (rectangle, cylinder, rounded, diamond, stadium, subroutine) -> valid=true
    - Diagram voi classDef dung palette (5 mau) -> valid=true, warnings.length=0
    - Diagram voi subgraph -> valid=true
    - Diagram voi edge labels: `A --> |"label"| B` -> valid=true

    ## Syntax errors (valid=false, errors.length > 0)
    - Missing flowchart/graph declaration (dong dau khong co `flowchart` hoac `graph`) -> valid=false, error co message "Missing.*declaration" va line=1
    - Invalid direction keyword (`flowchart XX`) -> valid=false, error co message "Invalid direction"
    - Unclosed double quotes (`A["Bat dau] --> B`) -> valid=false, error co message "Unclosed.*quote" va type='syntax'
    - Empty string input -> valid=false, errors.length >= 1

    ## Style warnings (valid=true, warnings.length > 0)
    - Color khong co trong palette (classDef custom fill:#FF0000) -> valid=true, warning co message match /palette|color/i
    - Vuot 15 nodes -> valid=true, warning co message match /15|node.*limit|max.*node/i
    - Unquoted label (`A[Bat dau]` thay vi `A["Bat dau"]`) -> valid=true, warning co message match /quote/i

    ## Edge cases
    - Empty string -> handled (valid=false)
    - Only whitespace -> handled (valid=false hoac errors)
    - Vietnamese characters trong quoted labels -> valid=true (khong error, khong warning ve chars)
    - Reserved keyword "end" lam unquoted node ID -> valid=false, error co message match /reserved/i
    - Moi error/warning object co 3 fields: line (number), message (string), type (string = 'syntax' hoac 'style')
  </behavior>
  <action>
Tao file `test/smoke-mermaid-validator.test.js` theo DUNG pattern cua `test/smoke-plan-checker.test.js`:

```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const { mermaidValidator } = require('../bin/lib/mermaid-validator');
```

Tao helper function `makeFlowchart(overrides = {})`:
```javascript
function makeFlowchart(overrides = {}) {
  const direction = overrides.direction || 'TD';
  const nodes = overrides.nodes || ['A["Bat dau"] --> B["Ket thuc"]'];
  const styles = overrides.styles || [];
  let content = `flowchart ${direction}\n`;
  for (const node of nodes) {
    content += `  ${node}\n`;
  }
  for (const style of styles) {
    content += `  ${style}\n`;
  }
  return content.trimEnd();
}
```

Tao 4 describe blocks voi tests cu the:

1. `describe('valid diagrams', () => { ... })` — 5 tests cho valid flowcharts
2. `describe('syntax errors', () => { ... })` — 4 tests cho syntax errors (declaration, direction, quotes, empty)
3. `describe('style warnings', () => { ... })` — 3 tests cho style warnings (palette, nodes, unquoted)
4. `describe('edge cases', () => { ... })` — 4 tests cho edge cases (whitespace, Vietnamese, reserved keywords, error object format)

Moi test PHAI assert:
- `result.valid` (true/false)
- `result.errors` va/hoac `result.warnings` length
- Error/warning object structure: `{ line: number, message: string, type: string }`
- Error type la 'syntax', warning type la 'style'

Tong: 16 tests.

Chay tests — PHAI FAIL (RED) vi chua co module `mermaid-validator.js`.
Commit: `test(21-02): add failing tests for mermaid-validator`
  </action>
  <verify>
    <automated>node --test test/smoke-mermaid-validator.test.js 2>&1 | grep -E "fail|error|Cannot find module" && echo "RED phase confirmed — tests fail as expected"</automated>
  </verify>
  <acceptance_criteria>
    - File test/smoke-mermaid-validator.test.js exists
    - File bat dau voi `'use strict';`
    - File co `require('../bin/lib/mermaid-validator')` import
    - File co helper function `makeFlowchart`
    - File co 4 describe blocks: 'valid diagrams', 'syntax errors', 'style warnings', 'edge cases'
    - File co >= 16 test cases (it blocks)
    - Tests FAIL khi chay (RED phase) — expected vi module chua ton tai
    - Moi test assert structure { valid, errors, warnings }
    - Tests cho error objects kiem tra 3 fields: line, message, type
  </acceptance_criteria>
  <done>File test/smoke-mermaid-validator.test.js ton tai voi 16+ tests covering valid diagrams, syntax errors, style warnings, va edge cases. Tests FAIL vi module chua implement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN + REFACTOR — Implement mermaid-validator.js cho tat ca tests pass (MERM-02)</name>
  <files>bin/lib/mermaid-validator.js</files>
  <read_first>
    - test/smoke-mermaid-validator.test.js (source of truth — tests vua tao o Task 1)
    - bin/lib/plan-checker.js (pattern: module structure, 'use strict', pure functions, module.exports)
    - .planning/phases/21-mermaid-foundation/21-RESEARCH.md (Mermaid Validator Core Structure, constants, check functions)
    - .planning/phases/21-mermaid-foundation/21-CONTEXT.md (D-01 through D-06 — all validator-related decisions)
  </read_first>
  <action>
Tao file `bin/lib/mermaid-validator.js` theo DUNG pattern cua `bin/lib/plan-checker.js`: 'use strict', section headers voi box drawing (`// ─── Section ────`), JSDoc comments, module.exports cuoi file.

**KHONG** require bat ky external dependency nao. KHONG require utils.js (validator la self-contained).

**Constants (per D-01, D-02, D-04):**

```javascript
const VALID_DIRECTIONS = ['TD', 'TB', 'BT', 'LR', 'RL'];

const RESERVED_KEYWORDS = [
  'end', 'graph', 'subgraph', 'click', 'call', 'default',
  'style', 'classDef', 'class', 'linkStyle',
];

const PALETTE = {
  primary: '#2563EB',
  secondary: '#64748B',
  accent: '#10B981',
  warning: '#F59E0B',
  error: '#DC2626',
};

const SHAPE_BY_ROLE = {
  service: 'rectangle',
  database: 'cylinder',
  api: 'rounded',
  decision: 'diamond',
  'start-end': 'stadium',
  external: 'subroutine',
};

const MAX_NODES = 15;
```

**Syntax check functions (tra ve errors voi type='syntax'):**

1. `checkDeclaration(lines)` — Dong dau tien (bo qua blank/comment lines) PHAI match `/^(flowchart|graph)\s+(TD|TB|BT|LR|RL)\s*$/`. Neu khong match: tra ve error `{ line: N, message: 'Missing flowchart/graph declaration with valid direction', type: 'syntax' }`. Neu direction khong valid: error `{ line: N, message: 'Invalid direction "XX" — must be one of: TD, TB, BT, LR, RL', type: 'syntax' }`.

2. `checkUnclosedQuotes(lines)` — Dem so luong double quotes tren moi line (chi dem lines co content, bo qua comment lines bat dau %%): neu le -> error `{ line: N, message: 'Unclosed double quote', type: 'syntax' }`.

3. `checkReservedKeywords(lines)` — Scan moi line cho pattern: node ID la reserved keyword KHONG co trong quotes. Regex: kiem tra tu o dau line (sau optional whitespace) hoac sau arrow (`-->`, `-.->`, `==>`) co match reserved keyword VA KHONG nam trong `["..."]` context. Error: `{ line: N, message: 'Reserved keyword "X" used as unquoted node ID', type: 'syntax' }`.

**Style check functions (tra ve warnings voi type='style'):**

4. `checkPalette(lines)` — Tim cac dong co `classDef` va extract fill color. Neu fill color KHONG nam trong Object.values(PALETTE): warning `{ line: N, message: 'Color #XXXXXX not in approved palette', type: 'style' }`.

5. `checkNodeCount(lines)` — Dem unique node IDs (pattern: word characters o dau line hoac sau arrow, theo sau boi shape syntax `[`, `(`, `{`). Neu count > MAX_NODES: warning `{ line: N, message: 'Diagram has N nodes — maximum is 15. Consider splitting into subgraphs', type: 'style' }` voi N la so thuc te, line = 0 (diagram-level warning).

6. `checkLabelQuoting(lines)` — Tim cac dong co node definitions (co shape syntax `[`, `(`, `{`) VA co label text. Neu label KHONG nam trong double quotes: warning `{ line: N, message: 'Label not wrapped in double quotes', type: 'style' }`. Bo qua comment lines (%%), classDef lines, linkStyle lines.

**Main function:**

```javascript
function mermaidValidator(mermaidText, options = {}) {
  if (!mermaidText || !mermaidText.trim()) {
    return {
      valid: false,
      errors: [{ line: 1, message: 'Empty or whitespace-only input', type: 'syntax' }],
      warnings: [],
    };
  }

  const lines = mermaidText.split('\n');
  const errors = [];
  const warnings = [];

  // Syntax checks -> errors
  errors.push(...checkDeclaration(lines));
  errors.push(...checkUnclosedQuotes(lines));
  errors.push(...checkReservedKeywords(lines));

  // Style checks -> warnings
  warnings.push(...checkPalette(lines));
  warnings.push(...checkNodeCount(lines));
  warnings.push(...checkLabelQuoting(lines));

  return {
    valid: errors.length === 0,
    errors,
    warnings,
  };
}

module.exports = { mermaidValidator };
```

Sau khi implement, chay tests — PHAI PASS (GREEN).
Refactor neu can (clean up, optimize regex) — tests van PASS.

Commit GREEN: `feat(21-02): implement mermaid-validator.js — pure function syntax+style checker`
Commit REFACTOR (neu can): `refactor(21-02): clean up mermaid-validator.js`
  </action>
  <verify>
    <automated>node --test test/smoke-mermaid-validator.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File bin/lib/mermaid-validator.js exists
    - File bat dau voi `'use strict';`
    - File KHONG co `require('fs')` hoac `require('path')` hoac bat ky external require nao (zero dependencies)
    - File co `module.exports = { mermaidValidator }`
    - File co constants: VALID_DIRECTIONS, RESERVED_KEYWORDS, PALETTE, MAX_NODES
    - PALETTE object chua 5 mau: '#2563EB', '#64748B', '#10B981', '#F59E0B', '#DC2626'
    - Function mermaidValidator nhan (mermaidText, options) va tra ve { valid, errors, warnings }
    - `node --test test/smoke-mermaid-validator.test.js` exit code 0 — tat ca tests pass
    - `node --test 'test/*.test.js'` — full suite van pass (khong break existing tests)
  </acceptance_criteria>
  <done>File bin/lib/mermaid-validator.js ton tai la pure function zero-dependency. Function mermaidValidator() nhan text, tra ve { valid, errors, warnings } voi error objects co { line, message, type }. Tat ca 16+ tests trong smoke-mermaid-validator.test.js PASS. Full test suite PASS.</done>
</task>

</tasks>

<verification>
1. `node --test test/smoke-mermaid-validator.test.js` — all tests pass, exit code 0
2. `node --test 'test/*.test.js'` — full suite pass, no regressions
3. `grep "module.exports" bin/lib/mermaid-validator.js` — exports mermaidValidator
4. `grep -c "require(" bin/lib/mermaid-validator.js` — 0 (zero dependencies)
5. `node -e "const {mermaidValidator} = require('./bin/lib/mermaid-validator'); const r = mermaidValidator('flowchart TD\n  A[\"X\"] --> B[\"Y\"]'); console.log(r.valid)"` — prints true
</verification>

<success_criteria>
- mermaidValidator() la pure function, zero dependencies (MERM-02)
- Return { valid, errors, warnings } voi error objects { line, message, type } (D-05, D-06)
- Syntax errors -> valid=false (unclosed quotes, reserved keywords, missing declaration)
- Style warnings -> valid=true (palette, max nodes, unquoted labels)
- 16+ unit tests pass covering valid, invalid, va edge cases
- Full test suite (test/*.test.js) pass — no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/21-mermaid-foundation/21-02-SUMMARY.md`
</output>
