# Requirements: v12.5 Installer UX & Runtime Expansion (ARCHIVED)

**Status:** ✅ SHIPPED 2026-04-09
**All requirements satisfied.**

## Requirements

### UX-01: ASCII Art Banner ✅
Installer shows styled ASCII art "PD" banner in cyan at startup (TTY only, no external deps).
**Outcome:** `PD_BANNER` const + `log.showBanner(version)` in `bin/lib/utils.js` — 6-line `██` block art.

### UX-02: Colorized Help Output ✅
`--help` output uses cyan/yellow/dim colors, documents all 11 runtimes and all flags with examples.
**Outcome:** `showHelp()` rewritten with inline ANSI helpers (bold/cyan/yellow/dim).

### UX-03: Progress Step Indicators ✅
Install shows numbered steps `[1/N]` with ✓/✗ outcomes.
**Outcome:** `log.step()` enhanced with `●○○○` filled/empty dot progress pattern.

### RT-01: Kilo Runtime ✅
`~/.config/kilo/` via `--kilo` flag.
**Outcome:** `PLATFORMS['kilo']` + `getGlobalDir('kilo')` using XDG pattern.

### RT-02: Antigravity Runtime ✅
`~/.gemini/antigravity/` via `--antigravity` flag.
**Outcome:** `PLATFORMS['antigravity']` + `getGlobalDir('antigravity')`.

### RT-03: Cursor Runtime ✅
`~/.cursor/` via `--cursor` flag.
**Outcome:** CLI flag added; PLATFORMS entry already existed.

### RT-04: Windsurf Runtime (path fixed) ✅
`~/.codeium/windsurf/` via `--windsurf` flag (corrected from `~/.windsurf/`).
**Outcome:** `dirName` + `getGlobalDir` case updated to `.codeium/windsurf`.

### RT-05: Augment Runtime ✅
`~/.augment/` via `--augment` flag.
**Outcome:** `PLATFORMS['augment']` + `getGlobalDir('augment')`.

### RT-06: Trae Runtime ✅
`~/.trae/` via `--trae` flag.
**Outcome:** `PLATFORMS['trae']` + `getGlobalDir('trae')`.

### CFG-01: --config-dir Flag ✅
`--config-dir <path>` (or `-c`) overrides the default config directory for any runtime.
**Outcome:** Pre-existing; verified working with all 11 runtimes.

### CFG-02: --force-statusline Flag ✅ (parse-only)
Replaces existing statusline config in IDE editors when provided; skips with warning otherwise.
**Outcome:** Flag parsed, `flags.forceStatusline = true` set, `log.warn()` message shown. Actual statusline wiring deferred.

---

## Traceability

| REQ-ID | Phase | Status |
|--------|-------|--------|
| UX-01 | Phase 153 | ✅ Complete |
| UX-02 | Phase 153 | ✅ Complete |
| UX-03 | Phase 153 | ✅ Complete |
| RT-01 | Phase 154 | ✅ Complete |
| RT-02 | Phase 154 | ✅ Complete |
| RT-03 | Phase 154 | ✅ Complete |
| RT-04 | Phase 154 | ✅ Complete |
| RT-05 | Phase 154 | ✅ Complete |
| RT-06 | Phase 154 | ✅ Complete |
| CFG-01 | Phase 154 | ✅ Complete |
| CFG-02 | Phase 154 | ✅ Complete (parse-only) |
