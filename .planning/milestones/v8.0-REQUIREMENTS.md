# Requirements Archive: v8.0 Developer Experience & Quality Hardening

**Archived:** 2026-04-03
**Status:** ✅ All requirements shipped

## v8.0 Requirements

### New Skills

- [x] **ONBOARD-01**: User can run `pd:onboard` to auto-orient the AI to an unfamiliar codebase before any workflow step (calls init+scan internally, creates PROJECT.md baseline)
  - **Outcome:** VALIDATED — `commands/pd/onboard.md` + `workflows/onboard.md` created. Delivers 6-step orchestration (init → git analysis → PROJECT.md → scan → .planning/ prerequisites → summary). Verified 3/3 criteria.

### Developer Experience

- [x] **LINT-01**: When lint fails 3 times, the workflow saves the fail count to PROGRESS.md, suggests `pd:fix-bug`, and supports resume-only-lint mode
  - **Outcome:** VALIDATED — `templates/progress.md` `lint_fail_count`/`last_lint_error` fields added; `write-code.md` Step 5 3-strike counter + STOP; Step 1.1 lint-only/fresh-start recovery. Verified 4/4 criteria.
- [x] **STATUS-01**: User can run `pd:status` to view current phase, plan, pending tasks, and blockers at a glance (read-only, Haiku skill)
  - **Outcome:** VALIDATED — `commands/pd/status.md` (Haiku, Read/Glob/Bash only) + `workflows/status.md` 8-field dashboard. Zero writes confirmed. Verified 4/4 criteria.

### Quality & Observability

- [x] **STALE-01**: Codebase mapper auto-detects staleness via git commit-delta (>20 commits since last map) and prompts refresh; maps store `Mapped at commit: [sha]`
  - **Outcome:** VALIDATED — `pd-codebase-mapper.md` Step 6 writes META.json with git SHA; `scan.md` Step 0 reads META.json + runs `git rev-list <sha>..HEAD --count` + warns at N>20. Non-blocking on missing META.json. Verified 4/4 criteria.
- [x] **INTEG-01**: Integration tests verify format contracts between skill chain artifacts (CONTEXT.md, TASKS.md, PROGRESS.md schemas) — not agent execution tests
  - **Outcome:** VALIDATED — `test/integration-contracts.test.js` with 28 inline-fixture tests across 6 describe blocks. Imports `validateLogEntry` from Phase 79. Zero live fs reads. All 28 pass in CI. Verified 7/7 criteria.
- [x] **LOG-01**: Agent errors are logged as JSONL at `.planning/logs/agent-errors.jsonl` with fields: timestamp, level, phase, step, agent, error, context
  - **Outcome:** VALIDATED (infrastructure scope) — `bin/lib/log-schema.js` pure functions (createLogEntry/validateLogEntry, zero fs); `bin/log-writer.js` validates + appends JSONL. 36 tests GREEN. **Note:** No agent workflow currently calls `appendLogEntry` — wiring deferred to v9.0. Module infrastructure is production-ready.

## Future Requirements (v9.0)

- **REPLAY-01**: `pd:replay [phase]` — re-run a failed phase with full context. *Deferred: requires LOG-01 stable for ≥1 milestone first.*
- **DIFF-01**: `pd:diff-milestone [v1] [v2]` — compare two milestone archives. *Deferred: requires milestone archive format definition first.*
- **HOTREL-01**: Workflow reloads config.json changes without restart. *Deferred: already ~80% free per invocation; zero documented user blockers.*

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| LINT-01     | 76    | ✅ Complete |
| STATUS-01   | 76    | ✅ Complete |
| STALE-01    | 77    | ✅ Complete |
| ONBOARD-01  | 78    | ✅ Complete |
| LOG-01      | 79    | ✅ Complete (infrastructure; call-site wiring deferred) |
| INTEG-01    | 80    | ✅ Complete |
