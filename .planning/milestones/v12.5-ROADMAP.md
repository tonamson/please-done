# Milestone v12.5: Installer UX & Runtime Expansion

**Status:** ✅ SHIPPED 2026-04-09
**Phases:** 153-154
**Total Plans:** 2

## Overview

Upgraded the `please-done` installer experience to match GSD build repo quality: ASCII art "PD" banner, colorized `--help` output, improved progress step formatting. Expanded runtime support from 7 to 11 platforms (added Kilo, Antigravity, Augment, Trae; fixed Windsurf path; added `--force-statusline` flag).

## Phases

### Phase 153: Installer UX Polish

**Goal:** Users see a polished, professional installer experience with visual feedback
**Depends on:** Nothing (first phase of v12.5)
**Requirements:** UX-01, UX-02, UX-03
**Plans:** 1 plan

Plans:
- [x] 153-01: Add PD ASCII art banner, colorize --help, improve progress step format

**Details:**
- Added `PD_BANNER` ASCII art constant (6-line `██` block chars spelling "PD") to `bin/lib/utils.js`
- Added `log.showBanner(version)` — shown at startup BEFORE `--help` check (both modes)
- Enhanced `log.step()` with `[N/M] ●●○○  msg` visual progress dots
- Rewrote `showHelp()` with bold headers, cyan/yellow/dim ANSI colors
- All color paths respect `NO_COLOR` env var and TTY detection
- 54/54 tests pass

### Phase 154: Runtime Expansion

**Goal:** Users can install skills to 6 additional AI coding runtimes with flexible config
**Depends on:** Phase 153
**Requirements:** RT-01, RT-02, RT-03, RT-04, RT-05, RT-06, CFG-01, CFG-02
**Plans:** 1 plan

Plans:
- [x] 154-01: Add kilo, antigravity, augment, trae runtimes; fix windsurf path; add --force-statusline

**Details:**
- Added 4 new runtimes to `PLATFORMS` + `TOOL_MAP` in `bin/lib/platforms.js`
- Fixed windsurf global path: `~/.windsurf` → `~/.codeium/windsurf`
- Added CLI flags: `--cursor`, `--windsurf`, `--kilo`, `--antigravity`, `--augment`, `--trae`
- Added `--force-statusline` flag (parse-only, logs warning — deferred implementation)
- Updated `getInstalledDirs()` and `showHelp()` for all 11 runtimes
- Runtime count: 7 → 11
- 41/41 tests pass

---

## Milestone Summary

**Key Decisions:**
- D1 (Phase 153): Banner spells "PD" in `██` block characters (matching GSD build repo style)
- D2 (Phase 153): Banner shown on both `--help` AND interactive install
- D3 (Phase 153): Keep `INSTALL_STEPS = 4`, improve existing output format only
- D4 (Phase 154): Windsurf path corrected to `~/.codeium/windsurf` (actual Codeium path)
- D5 (Phase 154): `--force-statusline` is parse-only with warning; wiring deferred

**Requirements Satisfied:** 11/11 (UX-01/02/03, RT-01–06, CFG-01/02)

**Technical Debt:**
- `--force-statusline` statusline wiring deferred to future milestone
- No `VERIFICATION.md` files (installer-only changes — SUMMARYs provide equivalent evidence)

**Issues Resolved:**
- Windsurf was using incorrect `~/.windsurf` path; now correctly `~/.codeium/windsurf`
- Installer lacked visual feedback; now has ASCII art, colorized help, progress dots

**Issues Deferred:**
- Actual statusline config writing for IDE editors (`--force-statusline`)

---

_For current project status, see .planning/ROADMAP.md_
