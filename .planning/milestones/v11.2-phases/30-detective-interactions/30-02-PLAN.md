---
phase: 30-detective-interactions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/checkpoint-handler.js
  - test/smoke-checkpoint-handler.test.js
autonomous: true
requirements:
  - PROT-04
  - PROT-06
must_haves:
  truths:
    - "extractCheckpointQuestion() trich xuat dung section 'Cau hoi cho User' tu evidence checkpoint"
    - "buildContinuationContext() tao prompt voi 4 thanh phan: evidence path, cau tra loi user, session dir, vong hien tai"
    - "buildContinuationContext() tra canContinue=false khi round > 2 va warning escalation"
    - "MAX_CONTINUATION_ROUNDS bang 2"
  artifacts:
    - path: "bin/lib/checkpoint-handler.js"
      provides: "CHECKPOINT flow + Continuation Agent — 2 functions + 1 constant"
      exports: ["extractCheckpointQuestion", "buildContinuationContext", "MAX_CONTINUATION_ROUNDS"]
    - path: "test/smoke-checkpoint-handler.test.js"
      provides: "Unit tests cho checkpoint-handler"
      min_lines: 60
  key_links:
    - from: "bin/lib/checkpoint-handler.js"
      to: "bin/lib/evidence-protocol.js"
      via: "require('./evidence-protocol').parseEvidence"
      pattern: "parseEvidence\\(evidenceContent\\)"
---

<objective>
Tao module checkpoint-handler.js xu ly CHECKPOINT flow va Continuation Agent (PROT-04, PROT-06).

Purpose: Khi agent ghi CHECKPOINT REACHED, orchestrator can trich xuat cau hoi cho user va tao context cho agent tiep theo (Continuation Agent). Module nay enforce gioi han 2 vong continuation.
Output: bin/lib/checkpoint-handler.js (2 functions + constant) va test/smoke-checkpoint-handler.test.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-detective-interactions/30-CONTEXT.md
@.planning/phases/30-detective-interactions/30-RESEARCH.md

<interfaces>
<!-- Key types va contracts tu codebase hien co. Executor dung truc tiep. -->

From bin/lib/evidence-protocol.js:
```javascript
const OUTCOME_TYPES = {
  checkpoint: { label: 'CHECKPOINT REACHED', requiredSections: ['Tien do dieu tra', 'Cau hoi cho User', 'Context cho Agent tiep'] },
};

function parseEvidence(content) → { agent, outcome, timestamp, session, body, sections: { [heading]: content } }
```

From test/smoke-evidence-protocol.test.js (test pattern):
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
function makeEvidence({ agent, outcome, session, body }) → string
const BODY_CHECKPOINT = `## CHECKPOINT REACHED\n\n## Tien do dieu tra\n50% done.\n\n## Cau hoi cho User\nCo thay doi gi gan day?\n\n## Context cho Agent tiep\nDa kiem tra src/api.js.`;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tao test file smoke-checkpoint-handler.test.js</name>
  <files>test/smoke-checkpoint-handler.test.js</files>
  <read_first>
    - bin/lib/evidence-protocol.js (parseEvidence API, OUTCOME_TYPES.checkpoint.requiredSections)
    - test/smoke-evidence-protocol.test.js (test pattern: makeEvidence helper, BODY_CHECKPOINT format)
  </read_first>
  <behavior>
    - extractCheckpointQuestion() voi evidence checkpoint tra ve question chua 'Co thay doi gi gan day?'
    - extractCheckpointQuestion() voi evidence khong phai checkpoint tra ve question rong va warning
    - extractCheckpointQuestion() tra ve agentName tu frontmatter
    - buildContinuationContext() voi round=1 tra ve canContinue=true va prompt chua 4 thanh phan
    - buildContinuationContext() voi round=2 tra ve canContinue=true
    - buildContinuationContext() voi round=3 tra ve canContinue=false va warning escalation
    - MAX_CONTINUATION_ROUNDS === 2
  </behavior>
  <action>
    Tao file test/smoke-checkpoint-handler.test.js voi cau truc:

    1. Require `node:test` (describe, it) va `node:assert/strict`
    2. Require `{ extractCheckpointQuestion, buildContinuationContext, MAX_CONTINUATION_ROUNDS }` tu `../bin/lib/checkpoint-handler`

    3. Tao helper makeEvidence (tuong tu pattern da co):
    ```javascript
    function makeEvidence({ agent = 'pd-code-detective', outcome = 'checkpoint', session = 'S001', body = '' } = {}) {
      return `---\nagent: ${agent}\noutcome: ${outcome}\ntimestamp: 2026-03-24T10:00:00+07:00\nsession: ${session}\n---\n${body}`;
    }
    ```

    4. Tao BODY_CHECKPOINT:
    ```javascript
    const BODY_CHECKPOINT = `## CHECKPOINT REACHED\n\n## Tien do dieu tra\nDa kiem tra 3/5 files nghi ngo.\n\n## Cau hoi cho User\nBan co thay doi gi o src/config.js gan day khong?\n\n## Context cho Agent tiep\nDa loai tru src/api.js va src/utils.js. Con lai src/config.js, src/routes.js, src/middleware.js.`;
    ```

    5. Test describe('MAX_CONTINUATION_ROUNDS'):
      - it('bang 2'): assert.equal(MAX_CONTINUATION_ROUNDS, 2)

    6. Test describe('extractCheckpointQuestion'):
      - it('trich xuat cau hoi tu evidence checkpoint'): goi extractCheckpointQuestion(makeEvidence({ body: BODY_CHECKPOINT })), assert question includes 'src/config.js gan day'
      - it('tra ve agentName tu frontmatter'): assert agentName === 'pd-code-detective'
      - it('tra ve context cho agent tiep'): assert context includes 'Da loai tru'
      - it('tra ve question rong khi outcome khong phai checkpoint'): goi voi outcome='root_cause', assert question === '', warnings.length > 0

    7. Test describe('buildContinuationContext'):
      - it('tra ve prompt voi 4 thanh phan khi round=1'): goi buildContinuationContext({ evidencePath: '/path/evidence_code.md', userAnswer: 'Co, toi sua config hom qua', sessionDir: '/tmp/S001-test', currentRound: 1, agentName: 'pd-code-detective' }), assert prompt includes 'evidence_code.md', assert prompt includes 'Co, toi sua config hom qua', assert prompt includes '/tmp/S001-test', assert prompt includes '1/2', assert canContinue === true
      - it('canContinue=true khi round=2'): goi voi currentRound: 2, assert canContinue === true
      - it('canContinue=false khi round=3 va co warning'): goi voi currentRound: 3, assert canContinue === false, assert warnings.length > 0, assert warnings[0] includes 'nguoi xem xet'
      - it('tra ve agentName dung'): assert result.agentName === 'pd-code-detective'
  </action>
  <verify>
    <automated>node --test test/smoke-checkpoint-handler.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test/smoke-checkpoint-handler.test.js ton tai va co it nhat 8 it() blocks
    - File require('../bin/lib/checkpoint-handler')
    - File chua assert cho: MAX_CONTINUATION_ROUNDS, 'src/config.js', 'pd-code-detective', 'canContinue', 'nguoi xem xet'
  </acceptance_criteria>
  <done>Test file chay duoc (co the FAIL vi module chua ton tai — TDD RED phase)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tao module checkpoint-handler.js</name>
  <files>bin/lib/checkpoint-handler.js</files>
  <read_first>
    - test/smoke-checkpoint-handler.test.js (test expectations — day la spec)
    - bin/lib/evidence-protocol.js (parseEvidence API, OUTCOME_TYPES.checkpoint)
    - bin/lib/session-manager.js (pattern pure function, non-blocking warnings)
  </read_first>
  <behavior>
    - extractCheckpointQuestion(evidenceContent) tra ve { question, context, agentName, warnings }
    - buildContinuationContext(params) tra ve { prompt, agentName, round, canContinue, warnings }
    - MAX_CONTINUATION_ROUNDS = 2
    - canContinue = currentRound <= MAX_CONTINUATION_ROUNDS (round 1,2 = true, round 3+ = false)
  </behavior>
  <action>
    Tao file bin/lib/checkpoint-handler.js theo pattern pure function:

    1. Header comment: "Checkpoint Handler Module — CHECKPOINT flow va Continuation Agent (PROT-04, PROT-06)."
    2. 'use strict';
    3. Require: `const { parseEvidence } = require('./evidence-protocol');`

    4. Constant MAX_CONTINUATION_ROUNDS = 2 (per D-09)

    5. Function extractCheckpointQuestion(evidenceContent) (per D-05):
      - const warnings = [];
      - const parsed = parseEvidence(evidenceContent);
      - Neu parsed.outcome !== 'checkpoint' → warnings.push(`outcome khong phai checkpoint: ${parsed.outcome}`), return { question: '', context: '', agentName: null, warnings }
      - const question = parsed.sections['Cau hoi cho User'] || ''
      - const context = parsed.sections['Context cho Agent tiep'] || ''
      - const agentName = parsed.agent
      - Neu !question → warnings.push('Evidence thieu section "Cau hoi cho User"')
      - return { question, context, agentName, warnings }

    6. Function buildContinuationContext(params) (per D-10):
      - Destructure: { evidencePath, userAnswer, sessionDir, currentRound, agentName } = params
      - const warnings = [];
      - const canContinue = currentRound <= MAX_CONTINUATION_ROUNDS
      - Neu !canContinue → warnings.push(`Da vuot qua ${MAX_CONTINUATION_ROUNDS} vong continuation — can nguoi xem xet`)
      - const prompt gom 4 dong (per D-10):
        - `CONTINUATION — Vong ${currentRound}/${MAX_CONTINUATION_ROUNDS}`
        - `Session dir: ${sessionDir}`
        - `Evidence truoc: ${evidencePath}`
        - `Cau tra loi user: ${userAnswer}`
      - return { prompt, agentName, round: currentRound, canContinue, warnings }

    7. module.exports = { extractCheckpointQuestion, buildContinuationContext, MAX_CONTINUATION_ROUNDS };
  </action>
  <verify>
    <automated>node --test test/smoke-checkpoint-handler.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/checkpoint-handler.js ton tai
    - File chua 'use strict'
    - File require('./evidence-protocol')
    - File export 3 symbols: extractCheckpointQuestion, buildContinuationContext, MAX_CONTINUATION_ROUNDS
    - MAX_CONTINUATION_ROUNDS = 2 (hardcode, khong phai config)
    - extractCheckpointQuestion doc sections['Cau hoi cho User']
    - buildContinuationContext tao prompt voi join('\n') gom 4 dong
    - canContinue logic: currentRound <= MAX_CONTINUATION_ROUNDS
    - Tat ca tests trong test/smoke-checkpoint-handler.test.js PASS
  </acceptance_criteria>
  <done>Module checkpoint-handler.js hoan chinh, tat ca tests PASS. extractCheckpointQuestion trich xuat cau hoi, buildContinuationContext tao prompt 4 thanh phan, enforce max 2 vong.</done>
</task>

</tasks>

<verification>
node --test test/smoke-checkpoint-handler.test.js — tat ca tests pass
grep -c 'module.exports' bin/lib/checkpoint-handler.js — tra ve 1
grep 'MAX_CONTINUATION_ROUNDS' bin/lib/checkpoint-handler.js — xuat hien
</verification>

<success_criteria>
- checkpoint-handler.js export 3 symbols: extractCheckpointQuestion, buildContinuationContext, MAX_CONTINUATION_ROUNDS
- Tat ca 8+ tests pass
- Moi function la pure — khong require('fs')
- MAX_CONTINUATION_ROUNDS = 2 per D-09
- extractCheckpointQuestion trich xuat 'Cau hoi cho User' section per D-05
- buildContinuationContext tao prompt voi 4 thanh phan per D-10
- canContinue=false khi round > 2 per D-09
- Agent tiep nhan chi evidence cuoi + cau tra loi per D-07
</success_criteria>

<output>
Sau khi hoan thanh, tao `.planning/phases/30-detective-interactions/30-02-SUMMARY.md`
</output>
