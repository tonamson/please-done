---
phase: 30-detective-interactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/outcome-router.js
  - test/smoke-outcome-router.test.js
autonomous: true
requirements:
  - PROT-03
must_haves:
  truths:
    - "buildRootCauseMenu() tra ve 3 lua chon (Sua ngay, Len ke hoach, Tu sua) tu evidence root_cause"
    - "prepareFixNow() tra ve action descriptor voi reusableModules va commitPrefix [LOI]"
    - "prepareFixPlan() tra ve FIX-PLAN.md content co YAML frontmatter va 5 sections"
    - "prepareSelfFix() tra ve sessionUpdate status=paused va resumeHint"
  artifacts:
    - path: "bin/lib/outcome-router.js"
      provides: "ROOT CAUSE choices routing — 4 pure functions"
      exports: ["buildRootCauseMenu", "prepareFixNow", "prepareFixPlan", "prepareSelfFix", "ROOT_CAUSE_CHOICES"]
    - path: "test/smoke-outcome-router.test.js"
      provides: "Unit tests cho 4 functions cua outcome-router"
      min_lines: 80
  key_links:
    - from: "bin/lib/outcome-router.js"
      to: "bin/lib/evidence-protocol.js"
      via: "require('./evidence-protocol').parseEvidence"
      pattern: "parseEvidence\\(evidenceContent\\)"
    - from: "bin/lib/outcome-router.js"
      to: "bin/lib/utils.js"
      via: "require('./utils').assembleMd"
      pattern: "assembleMd\\("
---

<objective>
Tao module outcome-router.js xu ly 3 lua chon khi ROOT CAUSE duoc tim thay (PROT-03).

Purpose: Khi agent tim duoc nguyen nhan loi, user can duoc hien 3 lua chon: "Sua ngay", "Len ke hoach", "Tu sua". Module nay cung cap pure functions de orchestrator (Phase 32) goi.
Output: bin/lib/outcome-router.js (4 functions + constant) va test/smoke-outcome-router.test.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-detective-interactions/30-CONTEXT.md
@.planning/phases/30-detective-interactions/30-RESEARCH.md

<interfaces>
<!-- Key types va contracts tu codebase hien co. Executor dung truc tiep. -->

From bin/lib/evidence-protocol.js:
```javascript
const OUTCOME_TYPES = {
  root_cause:   { label: 'ROOT CAUSE FOUND',          requiredSections: ['Nguyen nhan', 'Bang chung', 'De xuat'] },
  checkpoint:   { label: 'CHECKPOINT REACHED',         requiredSections: ['Tien do dieu tra', 'Cau hoi cho User', 'Context cho Agent tiep'] },
  inconclusive: { label: 'INVESTIGATION INCONCLUSIVE', requiredSections: ['Elimination Log', 'Huong dieu tra tiep'] },
};

function parseEvidence(content) → { agent, outcome, timestamp, session, body, sections: { [heading]: content } }
function validateEvidence(content) → { valid, outcome, agent, warnings }
```

From bin/lib/utils.js:
```javascript
function assembleMd(frontmatter: object, body: string) → string  // YAML frontmatter + body
function parseFrontmatter(content) → { frontmatter: object, body: string, raw: string }
```

From test/smoke-evidence-protocol.test.js (test pattern):
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
function makeEvidence({ agent, outcome, session, body }) → string  // helper tao evidence content
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tao test file smoke-outcome-router.test.js</name>
  <files>test/smoke-outcome-router.test.js</files>
  <read_first>
    - bin/lib/evidence-protocol.js (parseEvidence API, OUTCOME_TYPES sections)
    - test/smoke-evidence-protocol.test.js (test pattern: makeEvidence helper, describe/it structure)
    - bin/lib/utils.js (assembleMd signature)
  </read_first>
  <behavior>
    - buildRootCauseMenu() voi evidence root_cause tra ve 3 choices: {key:'fix_now',label:'Sua ngay'}, {key:'fix_plan',label:'Len ke hoach'}, {key:'self_fix',label:'Tu sua'}
    - buildRootCauseMenu() voi evidence KHONG phai root_cause tra ve choices rong va warning
    - prepareFixNow() tra ve action='fix_now', reusableModules co 3 items ['debug-cleanup','logic-sync','regression-analyzer'], commitPrefix='[LOI]'
    - prepareFixPlan() tra ve action='fix_plan', planContent chua '## Nguyen nhan' va '## Risk Assessment', planPath='FIX-PLAN.md'
    - prepareSelfFix() tra ve action='self_fix', sessionUpdate.status='paused', resumeHint chua 'pd:fix-bug'
  </behavior>
  <action>
    Tao file test/smoke-outcome-router.test.js voi cau truc:

    1. Require `node:test` (describe, it) va `node:assert/strict`
    2. Require `{ buildRootCauseMenu, prepareFixNow, prepareFixPlan, prepareSelfFix, ROOT_CAUSE_CHOICES }` tu `../bin/lib/outcome-router`

    3. Tao helper `makeEvidence()` tuong tu test/smoke-evidence-protocol.test.js:
    ```javascript
    function makeEvidence({ agent = 'pd-fix-architect', outcome = 'root_cause', session = 'S001', body = '' } = {}) {
      return `---\nagent: ${agent}\noutcome: ${outcome}\ntimestamp: 2026-03-24T10:00:00+07:00\nsession: ${session}\n---\n${body}`;
    }
    ```

    4. Tao BODY_ROOT_CAUSE:
    ```javascript
    const BODY_ROOT_CAUSE = `## ROOT CAUSE FOUND\n\n## Nguyen nhan\nLoi null pointer o dong 42 cua src/api.js.\n\n## Bang chung\nsrc/api.js:42 — variable user chua duoc kiem tra null.\n\n## De xuat\nThem null check truoc khi truy cap user.name.`;
    ```

    5. Test describe('buildRootCauseMenu'):
      - it('tra ve 3 choices tu evidence root_cause'): goi buildRootCauseMenu(makeEvidence({ body: BODY_ROOT_CAUSE })), assert choices.length === 3, assert choices[0].key === 'fix_now', choices[1].key === 'fix_plan', choices[2].key === 'self_fix'
      - it('tra ve question chua nguyen nhan'): assert question includes 'null pointer'
      - it('tra ve choices rong khi outcome khong phai root_cause'): goi voi outcome='checkpoint', assert choices.length === 0, warnings.length > 0

    6. Test describe('prepareFixNow'):
      - it('tra ve action fix_now voi reusable modules'): goi prepareFixNow(makeEvidence({ body: BODY_ROOT_CAUSE })), assert result.action === 'fix_now', assert result.reusableModules deep equal ['debug-cleanup','logic-sync','regression-analyzer'], assert result.commitPrefix === '[LOI]'

    7. Test describe('prepareFixPlan'):
      - it('tra ve FIX-PLAN.md content voi template sections'): goi prepareFixPlan(makeEvidence({ body: BODY_ROOT_CAUSE }), '/tmp/S001-test'), assert result.action === 'fix_plan', assert result.planContent includes '## Nguyen nhan', assert result.planContent includes '## Risk Assessment', assert result.planPath === 'FIX-PLAN.md'

    8. Test describe('prepareSelfFix'):
      - it('tra ve session update status paused'): goi prepareSelfFix(makeEvidence({ body: BODY_ROOT_CAUSE })), assert result.action === 'self_fix', assert result.sessionUpdate.status === 'paused', assert result.resumeHint includes 'pd:fix-bug'
  </action>
  <verify>
    <automated>node --test test/smoke-outcome-router.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test/smoke-outcome-router.test.js ton tai va co it nhat 7 it() blocks
    - File require('../bin/lib/outcome-router')
    - File require('node:test') va require('node:assert/strict')
    - File chua assert string 'fix_now', 'fix_plan', 'self_fix', 'debug-cleanup', '[LOI]', 'FIX-PLAN.md', 'pd:fix-bug', 'paused'
  </acceptance_criteria>
  <done>Test file chay duoc (co the FAIL vi module chua ton tai — dung cho TDD RED phase)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tao module outcome-router.js</name>
  <files>bin/lib/outcome-router.js</files>
  <read_first>
    - test/smoke-outcome-router.test.js (test expectations — day la spec)
    - bin/lib/evidence-protocol.js (parseEvidence API, pattern pure function)
    - bin/lib/utils.js (assembleMd de tao FIX-PLAN.md content)
    - bin/lib/session-manager.js (pattern non-blocking warnings)
  </read_first>
  <behavior>
    - buildRootCauseMenu(evidenceContent) tra ve { question, choices, summary, warnings }
    - prepareFixNow(evidenceContent) tra ve { action, reusableModules, evidence, suggestion, commitPrefix, warnings }
    - prepareFixPlan(evidenceContent, sessionDir) tra ve { action, planContent, planPath, warnings }
    - prepareSelfFix(evidenceContent) tra ve { action, sessionUpdate, summary, filesForReview, resumeHint, warnings }
    - Tat ca functions: pure, khong doc file, nhan content string, tra structured object voi warnings array
  </behavior>
  <action>
    Tao file bin/lib/outcome-router.js theo pattern pure function da thiet lap:

    1. Header comment: "Outcome Router Module — Dinh tuyen hanh dong theo ROOT CAUSE outcome (PROT-03)."
    2. 'use strict';
    3. Require: `const { parseEvidence } = require('./evidence-protocol');` va `const { assembleMd } = require('./utils');`

    4. Constant ROOT_CAUSE_CHOICES (per D-01):
    ```javascript
    const ROOT_CAUSE_CHOICES = [
      { key: 'fix_now',  label: 'Sua ngay' },
      { key: 'fix_plan', label: 'Len ke hoach' },
      { key: 'self_fix', label: 'Tu sua' },
    ];
    ```

    5. Function buildRootCauseMenu(evidenceContent) (per D-01):
      - Goi parseEvidence(evidenceContent)
      - Neu parsed.outcome !== 'root_cause' → tra { question: '', choices: [], summary: '', warnings: ['outcome khong phai root_cause: {actual}'] }
      - Lay rootCause = parsed.sections['Nguyen nhan'] || 'Khong co mo ta'
      - Lay suggestion = parsed.sections['De xuat'] || ''
      - Tao question = `Da tim thay nguyen nhan:\n${rootCause}\n\nBan muon lam gi?`
      - Tao summary = rootCause.split('\n')[0].slice(0, 120)
      - Tra { question, choices: [...ROOT_CAUSE_CHOICES], summary, warnings: [] }

    6. Function prepareFixNow(evidenceContent) (per D-02):
      - Goi parseEvidence(evidenceContent)
      - Lay evidence = parsed.sections['Bang chung'] || ''
      - Lay suggestion = parsed.sections['De xuat'] || ''
      - Tra { action: 'fix_now', reusableModules: ['debug-cleanup', 'logic-sync', 'regression-analyzer'], evidence, suggestion, commitPrefix: '[LOI]', warnings: [] }
      - KHONG tra agentName — orchestrator truc tiep sua code (D-02)

    7. Function prepareFixPlan(evidenceContent, sessionDir) (per D-03):
      - Goi parseEvidence(evidenceContent)
      - Lay rootCause, evidence, suggestion tu parsed.sections
      - Tao planContent bang assembleMd:
        - frontmatter: { type: 'fix-plan', session: parsed.session, created: new Date().toISOString() }
        - body: `\n# FIX-PLAN\n\n## Nguyen nhan\n${rootCause}\n\n## Files can sua\n${evidence}\n\n## Test can viet\n- [ ] Test tai hien loi\n- [ ] Test sau khi sua\n\n## De xuat\n${suggestion}\n\n## Risk Assessment\n- [ ] Anh huong modules khac?\n- [ ] Can cap nhat docs?\n`
      - Tra { action: 'fix_plan', planContent, planPath: 'FIX-PLAN.md', warnings: [] }
      - planPath la relative to session dir (D-03, Pitfall 4)

    8. Function prepareSelfFix(evidenceContent) (per D-04):
      - Goi parseEvidence(evidenceContent)
      - Lay rootCause, evidence tu parsed.sections
      - Tra { action: 'self_fix', sessionUpdate: { status: 'paused' }, summary: rootCause.split('\n')[0].slice(0, 200), filesForReview: evidence, resumeHint: 'Chay lai pd:fix-bug de verify sau khi sua.', warnings: [] }

    9. module.exports = { buildRootCauseMenu, prepareFixNow, prepareFixPlan, prepareSelfFix, ROOT_CAUSE_CHOICES };
  </action>
  <verify>
    <automated>node --test test/smoke-outcome-router.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/outcome-router.js ton tai
    - File chua 'use strict'
    - File require('./evidence-protocol') va require('./utils')
    - File export 5 symbols: buildRootCauseMenu, prepareFixNow, prepareFixPlan, prepareSelfFix, ROOT_CAUSE_CHOICES
    - ROOT_CAUSE_CHOICES chua 3 entries voi key fix_now, fix_plan, self_fix
    - prepareFixNow tra ve commitPrefix '[LOI]'
    - prepareFixPlan tra ve planPath 'FIX-PLAN.md'
    - prepareSelfFix tra ve sessionUpdate.status 'paused'
    - Tat ca tests trong test/smoke-outcome-router.test.js PASS
  </acceptance_criteria>
  <done>Module outcome-router.js hoan chinh, tat ca tests PASS. buildRootCauseMenu tra 3 choices, prepareFixNow/FixPlan/SelfFix tra dung action descriptors.</done>
</task>

</tasks>

<verification>
node --test test/smoke-outcome-router.test.js — tat ca tests pass
grep -c 'module.exports' bin/lib/outcome-router.js — tra ve 1
grep 'ROOT_CAUSE_CHOICES' bin/lib/outcome-router.js — xuat hien
</verification>

<success_criteria>
- outcome-router.js export 5 symbols: buildRootCauseMenu, prepareFixNow, prepareFixPlan, prepareSelfFix, ROOT_CAUSE_CHOICES
- Tat ca 7+ tests pass
- Moi function la pure — khong require('fs'), khong doc file
- ROOT_CAUSE_CHOICES co dung 3 entries per D-01
- prepareFixNow KHONG tra agentName per D-02
- prepareFixPlan tra planPath relative (khong absolute) per D-03, Pitfall 4
- prepareSelfFix tra sessionUpdate.status='paused' per D-04
</success_criteria>

<output>
Sau khi hoan thanh, tao `.planning/phases/30-detective-interactions/30-01-SUMMARY.md`
</output>
