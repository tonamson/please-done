---
phase: 053-new-agent-files
plan: 02
type: execute
wave: 2
depends_on: ["053-01"]
files_modified:
  - test/smoke-agent-files.test.js
autonomous: true
requirements: [AGEN-08]

must_haves:
  truths:
    - "Mỗi agent mới có smoke test kiểm tra file existence, frontmatter, body structure, và registry consistency"
    - "Tests bidirectional validate allowed-tools khớp AGENT_REGISTRY tools"
    - "Existing agent tests vẫn pass (backward compatibility)"
  artifacts:
    - path: "test/smoke-agent-files.test.js"
      provides: "Smoke tests cho 6 agents mới"
      contains: "pd-codebase-mapper"
  key_links:
    - from: "test/smoke-agent-files.test.js"
      to: "commands/pd/agents/*.md"
      via: "readFileSync + parseFrontmatter"
      pattern: "parseFrontmatter"
    - from: "test/smoke-agent-files.test.js"
      to: "bin/lib/resource-config.js"
      via: "AGENT_REGISTRY import"
      pattern: "AGENT_REGISTRY"
---

<objective>
Mở rộng smoke tests để cover 6 agents mới với file existence, frontmatter validation, body structure, và bidirectional registry consistency.

Purpose: Đảm bảo mọi agent mới đều hợp lệ và khớp với AGENT_REGISTRY (per D-10, D-14, AGEN-08).
Output: Test file mở rộng với ~80-120 dòng mới, tất cả tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/053-new-agent-files/053-CONTEXT.md
@.planning/phases/053-new-agent-files/053-RESEARCH.md
@.planning/phases/053-new-agent-files/053-01-SUMMARY.md

<interfaces>
<!-- Existing test patterns and imports -->

From test/smoke-agent-files.test.js (top):
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const { readFileSync } = require('node:fs');
const { join } = require('node:path');
const { AGENT_REGISTRY, TIER_MAP } = require('../bin/lib/resource-config');
```

From test/smoke-agent-files.test.js (PD_AGENTS_DIR, line 208):
```javascript
const PD_AGENTS_DIR = join(__dirname, '..', 'commands', 'pd', 'agents');
```

From bin/lib/utils.js (parseFrontmatter - đúng parser cho YAML arrays):
```javascript
const { parseFrontmatter } = require('../bin/lib/utils');
```

From test/smoke-agent-files.test.js (Security agent test pattern, lines 211-227):
```javascript
describe('Security agent files', () => {
  it('pd-sec-fixer.md ton tai tai commands/pd/agents/', () => {
    const path = join(PD_AGENTS_DIR, 'pd-sec-fixer.md');
    const content = readFileSync(path, 'utf8');
    assert.ok(content.includes('name: pd-sec-fixer'));
    assert.ok(content.includes('tier: architect'));
  });
});
```

From bin/lib/resource-config.js (AGENT_REGISTRY keys for new agents):
```javascript
"pd-codebase-mapper": { tier: "scout", tools: ["Read", "Glob", "Grep", "Bash", "Write"] },
"pd-security-researcher": { tier: "scout", tools: ["Read", "Glob", "Grep", "Bash", "mcp__fastcode__code_qa"] },
"pd-feature-analyst": { tier: "scout", tools: ["Read", "Glob", "Grep", "Bash", "mcp__context7__resolve-library-id", "mcp__context7__query-docs"] },
"pd-research-synthesizer": { tier: "architect", tools: ["Read", "Write", "Glob", "Grep", "Bash"] },
"pd-planner": { tier: "architect", tools: ["Read", "Write", "Glob", "Grep", "Bash"] },
"pd-regression-analyzer": { tier: "builder", tools: ["Read", "Glob", "Grep", "Bash", "mcp__fastcode__code_qa"] },
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mở rộng smoke-agent-files.test.js với tests cho 6 agents mới</name>
  <files>test/smoke-agent-files.test.js</files>
  <read_first>
    test/smoke-agent-files.test.js
    bin/lib/utils.js
    bin/lib/resource-config.js
    .planning/phases/053-new-agent-files/053-CONTEXT.md
  </read_first>
  <action>
Thêm section mới vào `test/smoke-agent-files.test.js` sau section "Security agent files" (line 227). Dùng `parseFrontmatter` từ `bin/lib/utils.js` (KHÔNG dùng `parseAgentFrontmatter` nội bộ vì nó không hiểu YAML arrays — per Research pitfall 5).

**1. Thêm import:**
Ở đầu file (sau các require hiện tại), thêm:
```javascript
const { parseFrontmatter } = require('../bin/lib/utils');
```

**2. Thêm constants:**
```javascript
const NEW_AGENT_NAMES = [
  'pd-codebase-mapper',
  'pd-security-researcher',
  'pd-feature-analyst',
  'pd-research-synthesizer',
  'pd-planner',
  'pd-regression-analyzer',
];
```

**3. Thêm describe block `New agent files (commands/pd/agents/)`** với các tests sau:

**Test 1: File existence** — Loop qua NEW_AGENT_NAMES, readFileSync mỗi file từ PD_AGENTS_DIR, assert content.length > 0.

**Test 2: Frontmatter fields** — Loop qua NEW_AGENT_NAMES, parseFrontmatter(content), assert:
- `frontmatter.name === name`
- `frontmatter.description` tồn tại và là string
- `frontmatter.tier` tồn tại và là 1 trong ['scout', 'builder', 'architect']
- `frontmatter['allowed-tools']` tồn tại và là Array

**Test 3: Body structure** — Loop qua NEW_AGENT_NAMES, assert content chứa `<objective>`, `<process>`, `<rules>`.

**Test 4: Không có model/maxTurns/effort** — Loop qua NEW_AGENT_NAMES, assert content KHÔNG chứa `model:`, `maxTurns:`, `effort:` trong phần frontmatter (giữa 2 dấu `---`).

**Test 5: Bidirectional tools consistency (per D-10)** — Loop qua NEW_AGENT_NAMES:
- Lấy `frontmatter['allowed-tools']` từ file
- Lấy `AGENT_REGISTRY[name].tools` từ registry
- Assert mỗi tool trong registry có trong file tools (registry → file)
- Assert mỗi tool trong file có trong registry tools (file → registry)

**Test 6: Tier consistency** — Loop qua NEW_AGENT_NAMES:
- Assert `frontmatter.tier === AGENT_REGISTRY[name].tier`

**4. Giữ nguyên tất cả tests hiện tại** — KHÔNG sửa đổi bất kỳ test nào đang có (per D-13, D-14).
  </action>
  <verify>
    <automated>node --test test/smoke-agent-files.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/smoke-agent-files.test.js` exit code 0
    - File chứa `const { parseFrontmatter } = require('../bin/lib/utils')`
    - File chứa `const NEW_AGENT_NAMES = [`
    - File chứa `'pd-codebase-mapper'` trong NEW_AGENT_NAMES
    - File chứa `describe('New agent files`
    - File chứa test cho bidirectional tools: `registryTools` và `fileTools`
    - File chứa test cho tier: `frontmatter.tier` và `AGENT_REGISTRY[name].tier`
    - Existing tests section cho `.claude/agents/` và "Security agent files" vẫn nguyên vẹn
    - Tổng ≥ 6 test cases mới (existence, frontmatter, body, no-model, tools, tier)
  </acceptance_criteria>
  <done>smoke-agent-files.test.js có 6 test cases mới cho 6 agents, bao gồm bidirectional tools validation. Tất cả tests (cũ + mới) pass.</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-agent-files.test.js` — tất cả tests pass
- `node --test test/smoke-*.test.js` — full smoke suite pass
- Existing tests không bị sửa đổi
</verification>

<success_criteria>
- 6+ test cases mới cover 6 agents
- Bidirectional tools validation (frontmatter ↔ registry)
- Tier consistency validation
- All tests pass (cũ + mới)
</success_criteria>

<output>
After completion, create `.planning/phases/053-new-agent-files/053-02-SUMMARY.md`
</output>
