---
phase: 053-new-agent-files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/pd/agents/pd-codebase-mapper.md
  - commands/pd/agents/pd-security-researcher.md
  - commands/pd/agents/pd-feature-analyst.md
  - commands/pd/agents/pd-research-synthesizer.md
  - commands/pd/agents/pd-planner.md
  - commands/pd/agents/pd-regression-analyzer.md
  - bin/lib/resource-config.js
autonomous: true
requirements: [AGEN-02, AGEN-03, AGEN-04, AGEN-05, AGEN-06, AGEN-07]

must_haves:
  truths:
    - "6 agent files tồn tại tại commands/pd/agents/ với YAML frontmatter hợp lệ"
    - "Mỗi agent có body gồm 3 XML blocks: objective, process, rules"
    - "5 entries mới trong AGENT_REGISTRY khớp tier và tools với frontmatter"
    - "pd-regression-analyzer agent file wrap 2 functions từ regression-analyzer.js"
  artifacts:
    - path: "commands/pd/agents/pd-codebase-mapper.md"
      provides: "Scout agent quét cấu trúc codebase"
      contains: "tier: scout"
    - path: "commands/pd/agents/pd-security-researcher.md"
      provides: "Scout agent nghiên cứu bảo mật"
      contains: "tier: scout"
    - path: "commands/pd/agents/pd-feature-analyst.md"
      provides: "Scout agent phân tích tính năng"
      contains: "tier: scout"
    - path: "commands/pd/agents/pd-research-synthesizer.md"
      provides: "Architect agent tổng hợp research"
      contains: "tier: architect"
    - path: "commands/pd/agents/pd-planner.md"
      provides: "Architect agent lập kế hoạch"
      contains: "tier: architect"
    - path: "commands/pd/agents/pd-regression-analyzer.md"
      provides: "Builder agent phân tích regression"
      contains: "tier: builder"
    - path: "bin/lib/resource-config.js"
      provides: "Registry entries cho 5 agents mới"
      contains: "pd-codebase-mapper"
  key_links:
    - from: "commands/pd/agents/*.md"
      to: "bin/lib/resource-config.js"
      via: "allowed-tools khớp tools trong AGENT_REGISTRY"
      pattern: "tools:.*Read.*Glob.*Grep"
---

<objective>
Tạo 6 agent files mới và thêm 5 entries vào AGENT_REGISTRY.

Purpose: Cung cấp agents chuyên biệt cho codebase mapping, security research, feature analysis, research synthesis, planning, và regression analysis.
Output: 6 agent .md files tại commands/pd/agents/ + 5 registry entries trong resource-config.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/053-new-agent-files/053-CONTEXT.md
@.planning/phases/053-new-agent-files/053-RESEARCH.md

<interfaces>
<!-- Template patterns cho agent files. Executor MUST read actual files listed in read_first. -->

Scout template (pd-sec-scanner.md): frontmatter gồm name, description, tier: scout, allowed-tools (YAML array). Body gồm objective, process (numbered steps), rules (bullet list).

Architect template (pd-sec-fixer.md): frontmatter gồm name, description, tier: architect, allowed-tools (YAML array). Body gồm objective, process (numbered steps), rules (bullet list).

Builder template (pd-code-detective.md): frontmatter gồm name, description, tier: builder, allowed-tools (YAML array). Body gồm objective, process, rules.

AGENT_REGISTRY entry format (resource-config.js):
  "pd-agent-name": { tier: "scout", tools: ["Read", "Glob", "Grep", "Bash"] }

regression-analyzer.js exports 2 functions:
  analyzeFromCallChain({ callChainText, targetFile, targetFunction? }) -> { affectedFiles, totalFound }
  analyzeFromSourceFiles({ sourceFiles, targetFile, targetFunction? }) -> { affectedFiles, totalFound }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo 6 agent files tại commands/pd/agents/</name>
  <files>
    commands/pd/agents/pd-codebase-mapper.md
    commands/pd/agents/pd-security-researcher.md
    commands/pd/agents/pd-feature-analyst.md
    commands/pd/agents/pd-research-synthesizer.md
    commands/pd/agents/pd-planner.md
    commands/pd/agents/pd-regression-analyzer.md
  </files>
  <read_first>
    commands/pd/agents/pd-sec-scanner.md
    commands/pd/agents/pd-sec-fixer.md
    commands/pd/agents/pd-code-detective.md
    bin/lib/regression-analyzer.js
    .planning/phases/053-new-agent-files/053-CONTEXT.md
  </read_first>
  <action>
Tạo 6 agent files theo format YAML frontmatter mới (per D-04, D-05). Mỗi file có frontmatter (name, description tiếng Việt có dấu, tier, allowed-tools YAML array với dashes) và body gồm 3 XML blocks (objective, process 5-7 steps, rules) per D-06, D-07, D-08. Mỗi agent ~30-50 dòng body.

KHÔNG thêm `model`, `maxTurns`, `effort` vào frontmatter (per D-05).
Body viết tiếng Việt có dấu (per D-07, CLAUDE.md).
allowed-tools dùng YAML array format với dashes (mỗi tool trên 1 dòng).

**1. pd-codebase-mapper.md** (per D-01, AGEN-02):
- Frontmatter: name: pd-codebase-mapper, description: "Quét cấu trúc codebase nhanh và cập nhật bản đồ codebase trong .planning/codebase/.", tier: scout, allowed-tools: Read, Glob, Grep, Bash, Write
- Objective: Quét toàn bộ cấu trúc codebase và tạo/cập nhật bản đồ tổng thể trong `.planning/codebase/`.
- Process (5-6 steps): Quét file extensions bằng Glob → Phân tích cấu trúc thư mục → Trích xuất exports/imports chính → Nhận diện patterns và conventions → Ghi kết quả vào `.planning/codebase/` → Tạo summary.
- Rules: Không sửa code, chỉ đọc và ghi bản đồ. Ưu tiên Glob trước khi Grep. Ghi tiếng Việt có dấu.

**2. pd-security-researcher.md** (per D-01, AGEN-03):
- Frontmatter: name: pd-security-researcher, description: "Nghiên cứu bảo mật chuyên sâu — phân tích threat model và đề xuất biện pháp phòng ngừa.", tier: scout, allowed-tools: Read, Glob, Grep, Bash, mcp__fastcode__code_qa
- Objective: Nghiên cứu bảo mật chuyên sâu cho codebase, phân tích threat model, và đề xuất biện pháp phòng ngừa.
- Process (5-6 steps): Nhận target scope → Dùng FastCode trace call chains liên quan → Grep tìm patterns nguy hiểm → Phân tích threat model → Tổng hợp findings → Ghi báo cáo.
- Rules: FastCode ưu tiên, Grep fallback. Không sửa code. Dẫn chứng file:dòng.

**3. pd-feature-analyst.md** (per D-01, AGEN-04):
- Frontmatter: name: pd-feature-analyst, description: "Phân tích tính năng và thư viện — đánh giá khả năng tích hợp và đề xuất giải pháp.", tier: scout, allowed-tools: Read, Glob, Grep, Bash, mcp__context7__resolve-library-id, mcp__context7__query-docs
- Objective: Phân tích tính năng dự án, tra cứu thư viện qua Context7, và đề xuất giải pháp tích hợp.
- Process (5-6 steps): Nhận yêu cầu phân tích → Quét codebase tìm patterns liên quan → Tra cứu thư viện qua Context7 resolve-library-id → Query docs chi tiết → Đánh giá khả năng tích hợp → Ghi báo cáo.
- Rules: Luôn dùng Context7 tra cứu docs thay vì đoán. Không sửa code. Tiếng Việt có dấu.

**4. pd-research-synthesizer.md** (per D-02, AGEN-05):
- Frontmatter: name: pd-research-synthesizer, description: "Tổng hợp kết quả nghiên cứu từ nhiều agents thành báo cáo thống nhất.", tier: architect, allowed-tools: Read, Write, Glob, Grep, Bash
- Objective: Tổng hợp kết quả nghiên cứu từ nhiều agents (mapper, security, feature) thành báo cáo thống nhất có cấu trúc.
- Process (5-6 steps): Đọc raw research files → Trích xuất findings chính → Phát hiện conflicts/overlaps → Tổng hợp thành báo cáo → Ghi file output → Tạo summary.
- Rules: Giữ nguyên dẫn chứng gốc. Không thêm thông tin không có trong sources. Tiếng Việt có dấu.

**5. pd-planner.md** (per D-02, AGEN-06):
- Frontmatter: name: pd-planner, description: "Lập kế hoạch thực thi cho các phases của dự án Please-Done.", tier: architect, allowed-tools: Read, Write, Glob, Grep, Bash
- Objective: Lập kế hoạch thực thi cho phases, tạo PLAN.md files với tasks cụ thể và dependency graph.
- Process (5-7 steps): Đọc ROADMAP + STATE + CONTEXT → Phân tích requirements → Xác định dependencies → Nhóm tasks theo waves → Áp dụng goal-backward → Ghi PLAN.md files → Cập nhật ROADMAP.
- Rules: Mỗi plan 2-3 tasks. Ưu tiên vertical slices. Tiếng Việt có dấu.

**6. pd-regression-analyzer.md** (per D-03, D-11, D-12, AGEN-07):
- Frontmatter: name: pd-regression-analyzer, description: "Phân tích regression — xác định files bị ảnh hưởng khi thay đổi code qua call chain analysis.", tier: builder, allowed-tools: Read, Glob, Grep, Bash, mcp__fastcode__code_qa
- Objective: Xác định chính xác các files bị ảnh hưởng khi thay đổi code, sử dụng call chain analysis từ FastCode và BFS fallback.
- Process (5-6 steps per D-12): Nhận target file từ prompt → Gọi FastCode trace call chain của target file → Chạy `analyzeFromCallChain()` từ `bin/lib/regression-analyzer.js` bằng Bash node -e → Nếu FastCode fail, fallback BFS với `analyzeFromSourceFiles()` → Lọc affected files (max 5, depth <= 2) → Output danh sách files bị ảnh hưởng kèm lý do.
- Rules: FastCode ưu tiên, BFS fallback. Max 5 affected files (MAX_AFFECTED). Depth <= 2 (MAX_DEPTH). Wrap functions từ regression-analyzer.js, KHÔNG tự implement lại.
  </action>
  <verify>
    <automated>for f in pd-codebase-mapper pd-security-researcher pd-feature-analyst pd-research-synthesizer pd-planner pd-regression-analyzer; do test -f "commands/pd/agents/${f}.md" || echo "MISSING: ${f}"; done && grep -c "tier:" commands/pd/agents/pd-codebase-mapper.md commands/pd/agents/pd-security-researcher.md commands/pd/agents/pd-feature-analyst.md commands/pd/agents/pd-research-synthesizer.md commands/pd/agents/pd-planner.md commands/pd/agents/pd-regression-analyzer.md</automated>
  </verify>
  <acceptance_criteria>
    - commands/pd/agents/pd-codebase-mapper.md chứa `name: pd-codebase-mapper` và `tier: scout` và `- Write`
    - commands/pd/agents/pd-security-researcher.md chứa `name: pd-security-researcher` và `tier: scout` và `- mcp__fastcode__code_qa`
    - commands/pd/agents/pd-feature-analyst.md chứa `name: pd-feature-analyst` và `tier: scout` và `- mcp__context7__resolve-library-id` và `- mcp__context7__query-docs`
    - commands/pd/agents/pd-research-synthesizer.md chứa `name: pd-research-synthesizer` và `tier: architect` và `- Write`
    - commands/pd/agents/pd-planner.md chứa `name: pd-planner` và `tier: architect` và `- Write`
    - commands/pd/agents/pd-regression-analyzer.md chứa `name: pd-regression-analyzer` và `tier: builder` và `- mcp__fastcode__code_qa` và `analyzeFromCallChain` và `analyzeFromSourceFiles`
    - Mỗi file chứa `<objective>`, `<process>`, `<rules>`
    - KHÔNG file nào chứa `model:` hoặc `maxTurns:` hoặc `effort:` trong frontmatter (per D-05)
    - Tất cả description có dấu tiếng Việt (per D-07)
  </acceptance_criteria>
  <done>6 agent files tồn tại với đúng frontmatter (name, description tiếng Việt, tier, allowed-tools) và body (objective, process, rules). Không có model/maxTurns/effort.</done>
</task>

<task type="auto">
  <name>Task 2: Thêm 5 registry entries vào AGENT_REGISTRY trong resource-config.js</name>
  <files>bin/lib/resource-config.js</files>
  <read_first>
    bin/lib/resource-config.js
    .planning/phases/053-new-agent-files/053-CONTEXT.md
  </read_first>
  <action>
Thêm 5 entries mới vào `AGENT_REGISTRY` trong `bin/lib/resource-config.js`, ngay sau entry `pd-regression-analyzer` (line 94, trước dấu `};` đóng object). KHÔNG thêm pd-regression-analyzer vì đã có từ Phase 52. KHÔNG thêm `categories` (per D-09).

Thêm chính xác các entries sau (copy nguyên văn):

```javascript
"pd-codebase-mapper": {
  tier: "scout",
  tools: ["Read", "Glob", "Grep", "Bash", "Write"],
},
"pd-security-researcher": {
  tier: "scout",
  tools: ["Read", "Glob", "Grep", "Bash", "mcp__fastcode__code_qa"],
},
"pd-feature-analyst": {
  tier: "scout",
  tools: ["Read", "Glob", "Grep", "Bash", "mcp__context7__resolve-library-id", "mcp__context7__query-docs"],
},
"pd-research-synthesizer": {
  tier: "architect",
  tools: ["Read", "Write", "Glob", "Grep", "Bash"],
},
"pd-planner": {
  tier: "architect",
  tools: ["Read", "Write", "Glob", "Grep", "Bash"],
},
```

Tools trong registry PHẢI khớp chính xác với `allowed-tools` trong frontmatter (per D-10).
  </action>
  <verify>
    <automated>node -e "const {AGENT_REGISTRY} = require('./bin/lib/resource-config'); const names = ['pd-codebase-mapper','pd-security-researcher','pd-feature-analyst','pd-research-synthesizer','pd-planner']; for (const n of names) { if (!AGENT_REGISTRY[n]) { console.error('MISSING: ' + n); process.exit(1); } console.log(n + ': tier=' + AGENT_REGISTRY[n].tier + ' tools=' + AGENT_REGISTRY[n].tools.join(',')); }"</automated>
  </verify>
  <acceptance_criteria>
    - node -e verify command chạy thành công, in ra 5 agents với tier và tools đúng
    - AGENT_REGISTRY["pd-codebase-mapper"].tier === "scout"
    - AGENT_REGISTRY["pd-codebase-mapper"].tools bao gồm "Write"
    - AGENT_REGISTRY["pd-security-researcher"].tools bao gồm "mcp__fastcode__code_qa"
    - AGENT_REGISTRY["pd-feature-analyst"].tools bao gồm "mcp__context7__resolve-library-id" và "mcp__context7__query-docs"
    - AGENT_REGISTRY["pd-research-synthesizer"].tier === "architect"
    - AGENT_REGISTRY["pd-planner"].tier === "architect"
    - Không có entry trùng lặp pd-regression-analyzer
    - Existing tests vẫn pass: `node --test test/resource-config.test.js`
  </acceptance_criteria>
  <done>5 registry entries mới tồn tại trong AGENT_REGISTRY, tools khớp chính xác với frontmatter. Existing tests pass.</done>
</task>

</tasks>

<verification>
- Tất cả 6 agent files tồn tại tại commands/pd/agents/
- Mỗi file có YAML frontmatter hợp lệ với name, description, tier, allowed-tools
- AGENT_REGISTRY có 5 entries mới (pd-regression-analyzer đã có sẵn)
- `node --test test/resource-config.test.js` pass
</verification>

<success_criteria>
- 6 agent .md files tồn tại với đúng format
- 5 registry entries mới trong resource-config.js
- Existing tests pass (backward compatibility per D-13)
</success_criteria>

<output>
After completion, create `.planning/phases/053-new-agent-files/053-01-SUMMARY.md`
</output>
