---
phase: 71-core-standalone-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/test.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - GUARD-01
  - REPORT-01
  - REPORT-02
  - RECOV-01

must_haves:
  truths:
    - "Step 0 routes --standalone to S1 and non-standalone to Step 1"
    - "User can run pd:test --standalone [path] and get a standalone test flow (S1-S8)"
    - "User can run pd:test --standalone --all and get full-project standalone testing"
    - "Auto-detection identifies NestJS/WordPress/Solidity/Flutter/Frontend from file markers"
    - "Standalone report is created as STANDALONE_TEST_REPORT_[YYYYMMDD_HHMMSS].md in .planning/reports/"
    - "Standalone bugs use Patch version: standalone"
    - "Interrupted standalone sessions are detected and offer resume/rewrite"
    - "Standard flow Steps 1-10 are completely unmodified"
  artifacts:
    - path: "workflows/test.md"
      provides: "Step 0 router + recovery check + Steps S1-S8 standalone flow"
      contains: "Step 0: Route by mode"
  key_links:
    - from: "workflows/test.md Step 0"
      to: "workflows/test.md Step S1"
      via: "--standalone flag check"
      pattern: "go to.*Step S1"
    - from: "workflows/test.md Step S2"
      to: "workflows/test.md Step S3-S6"
      via: "detected stack determines test framework"
      pattern: "nest-cli.json|composer.json|hardhat.config|pubspec.yaml|package.json"
    - from: "workflows/test.md Step S7"
      to: ".planning/reports/"
      via: "report file creation"
      pattern: "STANDALONE_TEST_REPORT"
---

<objective>
Add Step 0 (mode router), standalone recovery check, and Steps S1-S8 (full standalone test flow) to the test workflow file, while keeping standard flow Steps 1-10 completely unmodified.

Purpose: This is the core standalone test flow, the main feature of v7.0. Users can test any module without the milestone pipeline.

Output: Modified `workflows/test.md` with Step 0 prepended before Step 1 and Steps S1-S8 appended after Step 10.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-core-standalone-flow/71-CONTEXT.md
@.planning/phases/71-core-standalone-flow/71-RESEARCH.md

<interfaces>
<!-- Current workflow structure (read-only reference) -->

From workflows/test.md:

- Lines 1-8: purpose + required_reading + conditional_reading
- process block contains Steps 1 through 10
- Step 1: Determine scope + read context (includes effort routing)
- Step 1.5: Check for interrupted session recovery
- Step 2: Check test infrastructure
- Step 3: Read code to understand logic
- Step 4: Write test files (NestJS .spec.ts example)
- Step 5: Run tests
- Step 6: User confirms database + UI
- Step 7: TEST_REPORT.md
- Step 8: Bug Report (if failures)
- Step 9: Update TASKS.md
- Step 10: Git commit
- rules section follows after Step 10

Insertion strategy:

- Step 0 goes INSIDE process, BEFORE `## Step 1:`
- Steps S1-S8 go INSIDE process, AFTER Step 10 content (before closing process tag)
- Steps 1-10 content is NOT touched
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 0 router and standalone recovery check before Step 1</name>
  <files>workflows/test.md</files>

<read_first> - workflows/test.md (full file. Understand current structure and exact position of `## Step 1:`) - .planning/phases/71-core-standalone-flow/71-CONTEXT.md (decisions D-01 through D-04 for argument parsing) - .planning/phases/71-core-standalone-flow/71-RESEARCH.md (Pattern 3: Step 0 router, Pattern 4: recovery)
</read_first>

  <action>
Insert the following content INSIDE the `<process>` block, BEFORE the line `## Step 1: Determine scope + read context`. Do NOT modify any existing content.

Insert this exact text (two sections, each separated by blank lines):

SECTION A - Step 0 Router:

## Step 0: Route by mode

Check `$ARGUMENTS`:

- Contains `--standalone` -> go to **Step S0.5** (standalone recovery check)
- Does NOT contain `--standalone` -> continue to **Step 1** (standard flow)

SECTION B - Step S0.5 Recovery:

## Step S0.5: Standalone recovery check

Check for interrupted standalone sessions:

1. **Standalone report already exists?**
   - Glob `.planning/reports/STANDALONE_TEST_REPORT_*.md`
   - If found, show most recent: "Found existing standalone report: [filename]. 1. KEEP -- view and stop | 2. NEW -- create a fresh test run"
   - KEEP -> display report content and stop | NEW -> continue to Step S1

2. **Test files already exist but uncommitted?** (ONLY check when no report found)
   - Glob by stack patterns: `**/*.spec.ts`, `**/test-*.php`, `**/test/*.ts`, `**/test/*.t.sol`, `**/test/**/*_test.dart`
   - Check `git status` for uncommitted matches
   - Found -> "Found [N] uncommitted test files. 1. KEEP -- run tests only (skip writing) | 2. REWRITE from scratch"
   - KEEP -> jump to Step S5 | REWRITE -> continue to Step S1

3. **No traces** -> continue to Step S1

END OF INSERTION. Standard flow Step 1 follows unchanged below.

CRITICAL: This content must appear AFTER the `<process>` opening tag but BEFORE `## Step 1:`. Do NOT modify any part of Steps 1-10.
</action>

  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && grep -n "Step 0: Route by mode" workflows/test.md && grep -n "Step S0.5" workflows/test.md && grep -n "Step 1: Determine scope" workflows/test.md | head -1</automated>
  </verify>

<acceptance*criteria> - `workflows/test.md` contains `## Step 0: Route by mode` - `workflows/test.md` contains `## Step S0.5: Standalone recovery check` - `## Step 0:` appears BEFORE `## Step 1:` (lower line number) - `## Step S0.5:` appears BEFORE `## Step 1:` (lower line number) - `workflows/test.md` contains `Contains \`--standalone\` -> go to **Step S0.5**`    -`workflows/test.md`contains`Does NOT contain \`--standalone\` -> continue to **Step 1**`    -`workflows/test.md`contains`STANDALONE_TEST_REPORT*\*.md`(recovery glob pattern)
    -`workflows/test.md`contains`KEEP -- run tests only (skip writing)`(recovery option)
    - Standard flow`## Step 1:`through`## Step 10:` content is unchanged (verified in Task 3)
</acceptance_criteria>

  <done>
    Step 0 routes standalone invocations to S0.5 recovery check, then to S1. Non-standalone invocations continue to Step 1 unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add standalone flow Steps S1-S8 after standard flow Step 10</name>
  <files>workflows/test.md</files>

<read_first> - workflows/test.md (current state after Task 1. See Step 10 location for insertion point) - .planning/phases/71-core-standalone-flow/71-CONTEXT.md (all decisions D-01 through D-13) - .planning/phases/71-core-standalone-flow/71-RESEARCH.md (code examples 2-6, auto-detection logic) - references/conventions.md (patch version format, commit prefixes)
</read_first>

  <action>
Insert the following content AFTER the Step 10 section (after the Step 10 git commit code block) but BEFORE the `</process>` closing tag. Do NOT modify any existing content.

Insert these 8 step sections:

STEP S1 - Parse standalone arguments:

## Step S1: Parse standalone arguments

> Only reached via Step 0 routing when `$ARGUMENTS` contains `--standalone`.
> Standard flow Steps 1-10 are NOT used in standalone mode.

Parse `$ARGUMENTS` after removing the `--standalone` flag:

- **Has a path** (e.g., `--standalone src/users` or `--standalone ./lib/auth.ts`):
  - Check path exists: `ls [path]` or `stat [path]`
  - Path does NOT exist -> **STOP**: "Path not found: [path]. Check the path and try again."
  - Path is a file -> `TARGET_TYPE=file`, `TARGET_PATH=[path]`
  - Path is a directory -> `TARGET_TYPE=directory`, `TARGET_PATH=[path]`

- **Has `--all` flag** (e.g., `--standalone --all`):
  - `TARGET_TYPE=all`, `TARGET_PATH=.` (project root)

- **Neither path nor --all**:
  - Ask user: "Provide a target path or use `--standalone --all` to test entire project."
  - Wait for response -> parse as path or --all

- `git rev-parse --git-dir 2>/dev/null` -> save `HAS_GIT`

STEP S2 - Detect tech stack:

## Step S2: Detect tech stack

**If `.planning/CONTEXT.md` exists:** Read it -> extract Tech Stack -> use that (skip auto-detection).

**If `.planning/CONTEXT.md` does NOT exist:** Auto-detect from file markers in this priority order:

| Priority | Check                                                                             | Stack              | Test Framework                                                                                                      |
| -------- | --------------------------------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------- |
| 1        | `nest-cli.json` exists OR `package.json` contains `@nestjs/core`                  | NestJS             | Jest + Supertest                                                                                                    |
| 2        | `composer.json` contains `wordpress` dependency OR `wp-content/` directory exists | WordPress          | PHPUnit + WP_UnitTestCase                                                                                           |
| 3        | `hardhat.config.js` or `hardhat.config.ts` exists                                 | Solidity (Hardhat) | Hardhat test (`npx hardhat test`)                                                                                   |
| 3b       | `foundry.toml` exists                                                             | Solidity (Foundry) | Foundry test (`forge test -vvv`)                                                                                    |
| 4        | `pubspec.yaml` exists AND contains `flutter` in sdk                               | Flutter            | flutter_test + mocktail                                                                                             |
| 5        | `package.json` contains `react` or `vue` or `angular` or `next`                   | Frontend-only      | Manual test checklist                                                                                               |
| 6        | No match                                                                          | N/A                | **STOP**: "Cannot auto-detect tech stack. Create `.planning/CONTEXT.md` with `/pd:init` or specify stack manually." |

Announce: "Detected stack: **[stack]** -> using [test framework]."

STEP S3 - Check test infrastructure:

## Step S3: Check test infrastructure

Use the same infrastructure table as standard flow Step 2:

| Stack            | Check                                                 | Install if missing                                                                               |
| ---------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| NestJS           | Jest config + `@nestjs/testing`, `supertest`, `jest`  | `npm install --save-dev @nestjs/testing supertest @types/supertest`                              |
| WordPress        | PHPUnit + WP test suite                               | `composer require --dev phpunit/phpunit wp-phpunit/wp-phpunit` + create `phpunit.xml` if missing |
| Solidity/Hardhat | `@nomicfoundation/hardhat-toolbox` or `chai`+`ethers` | `npm install --save-dev @nomicfoundation/hardhat-toolbox`                                        |
| Solidity/Foundry | `lib/forge-std/`                                      | `forge install foundry-rs/forge-std`                                                             |
| Flutter          | `flutter_test` + `mocktail` in `dev_dependencies`     | `flutter pub add --dev mocktail` + `mkdir -p test/unit test/widget`                              |

STEP S4 - Read target code:

## Step S4: Read target code

Read the target code to understand what to test:

- **FastCode available** -> `mcp__fastcode__code_qa`: "What does [TARGET_PATH] do? What are the public functions, endpoints, classes, and their expected behavior?"
- **FastCode unavailable** -> Use Grep + Read tools as fallback:
  - `grep -rn "export\|function\|class\|describe\|module" [TARGET_PATH] --include="*.ts" --include="*.js" --include="*.php" --include="*.sol" --include="*.dart" | head -50`
  - Read key files to understand logic

- **Context7 available** -> Look up test patterns for detected libraries:
  - `mcp__context7__resolve-library-id` -> `mcp__context7__query-docs` for test utilities
- **Context7 unavailable** -> Skip library docs lookup. Use framework defaults.

- `TARGET_TYPE=file` -> read that specific file
- `TARGET_TYPE=directory` -> recursively scan for source files (exclude `node_modules`, `dist`, `.git`, `vendor`, `build`, `coverage`, `.next`)
- `TARGET_TYPE=all` -> scan entire project source (same exclusions)

STEP S5 - Write test files:

## Step S5: Write test files

Write test files using the same patterns as standard flow Step 4 (per detected stack):

- NestJS -> `.spec.ts` files alongside source
- WordPress -> `test-*.php` files in `tests/` directory
- Solidity/Hardhat -> `test/*.ts` files
- Solidity/Foundry -> `test/*.t.sol` files
- Flutter -> `test/**/*_test.dart` files
- Frontend-only -> manual test checklist (no test files)

Rules (same as standard flow):

- Each test case has CLEAR input + SPECIFIC expected output
- Test data uses `Date.now()` or unique identifiers for uniqueness
- Group: happy path -> validation -> auth -> edge cases
- Describe/it/comments in English

STEP S6 - Run tests + display results:

## Step S6: Run tests + display results

Run tests per stack:

| Stack            | Command                                                                       |
| ---------------- | ----------------------------------------------------------------------------- |
| NestJS           | `cd [backend-path] && npm test -- --verbose --testPathPattern=[pattern] 2>&1` |
| WordPress        | `cd [project-path] && composer test 2>&1`                                     |
| Solidity/Hardhat | `cd [project-path] && npx hardhat test [test-files] 2>&1`                     |
| Solidity/Foundry | `cd [project-path] && forge test -vvv --match-path [pattern] 2>&1`            |
| Flutter          | `cd [project-path] && flutter test [test-files] 2>&1`                         |
| Frontend-only    | Display manual test checklist -> ask user to confirm each item                |

Display results table:

| #   | Test case | Result    | Input   | Output   |
| --- | --------- | --------- | ------- | -------- |
| 1   | [name]    | pass/fail | [input] | [output] |

Total: X/Y passed

Ask user: "Results above. Confirm or request changes? (y/n)"

STEP S7 - Create standalone test report:

## Step S7: Create standalone test report

Create directory if missing: `mkdir -p .planning/reports`

Generate timestamp: `YYYYMMDD_HHMMSS` format (e.g., `20260329_143022`)

Write `.planning/reports/STANDALONE_TEST_REPORT_[YYYYMMDD_HHMMSS].md`:

```markdown
# Standalone Test Report

> Date: [DD_MM_YYYY HH:MM]
> Mode: Standalone
> Target: [TARGET_PATH or --all]
> Stack: [detected stack name]
> Total: [X] tests | ✅ [Y] passed | ❌ [Z] failed

## Results [Jest|PHPUnit|Hardhat|Foundry|FlutterTest|Manual Testing]

| Test case | Input | Expected | Actual | Result |
| --------- | ----- | -------- | ------ | ------ |
| [name]    | [in]  | [exp]    | [act]  | ✅/❌  |

## Notes

- Tested via: `pd:test --standalone [arguments]`
- Stack detection: [auto-detected / from CONTEXT.md]
```

STEP S8 - Bug report (if failures) + git commit:

## Step S8: Bug report (if failures) + git commit

**If any tests failed:**

Create `.planning/bugs/BUG_[DD_MM_YYYY_HH_MM_SS].md`:

```markdown
# Bug Report (from standalone testing)

> Date: [DD_MM_YYYY HH:MM:SS] | Severity: [Critical/High/Medium/Low]
> Status: Unresolved | Feature: [Name] | Target: [TARGET_PATH]
> Patch version: standalone | Fix attempts: 0

## Bug Description

Test case: [name] | Input: [...] | Expected: [...] | Actual: [...]

## References

> Test report: .planning/reports/STANDALONE*TEST_REPORT*[timestamp].md
> Test framework: [Jest|PHPUnit|Hardhat|Foundry|FlutterTest]
```

Note: `Patch version: standalone` is a literal string, NOT a numbered version. This is per D-13.

**Git commit (ONLY if HAS_GIT = true):**

```bash
git add [test files]
git add .planning/reports/STANDALONE_TEST_REPORT_[timestamp].md
# If bug report exists:
git add .planning/bugs/BUG_[timestamp].md

git commit -m "[TEST] Standalone tests for [TARGET_PATH]

Mode: standalone
Stack: [detected stack]
Tests: [X] passed, [Y] failed
Target: [TARGET_PATH]"
```

**Done.** Suggest next step: "Fix bugs manually, or re-test with `/pd:test --standalone [path]`."

END OF STANDALONE FLOW.

CRITICAL: All S-step content must appear AFTER Step 10 and BEFORE `</process>`. Do NOT modify Steps 1-10 or the `<rules>` section.
</action>

  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && grep -c "Step S[0-9]" workflows/test.md && grep "Step S1: Parse standalone" workflows/test.md && grep "Step S2: Detect tech stack" workflows/test.md && grep "Step S7: Create standalone" workflows/test.md && grep "Step S8: Bug report" workflows/test.md && grep "Patch version: standalone" workflows/test.md && grep "STANDALONE_TEST_REPORT" workflows/test.md</automated>
  </verify>

<acceptance*criteria> - `workflows/test.md` contains `## Step S1: Parse standalone arguments` - `workflows/test.md` contains `## Step S2: Detect tech stack` - `workflows/test.md` contains `## Step S3: Check test infrastructure` - `workflows/test.md` contains `## Step S4: Read target code` - `workflows/test.md` contains `## Step S5: Write test files` - `workflows/test.md` contains `## Step S6: Run tests + display results` - `workflows/test.md` contains `## Step S7: Create standalone test report` - `workflows/test.md` contains `## Step S8: Bug report (if failures) + git commit` - `workflows/test.md` contains `nest-cli.json` in Step S2 auto-detection table - `workflows/test.md` contains `composer.json` with `wordpress` in Step S2 - `workflows/test.md` contains `hardhat.config` in Step S2 - `workflows/test.md` contains `pubspec.yaml` with `flutter` in Step S2 - `workflows/test.md` contains `STANDALONE_TEST_REPORT*[YYYYMMDD_HHMMSS].md`report filename
    -`workflows/test.md`contains`Patch version: standalone`literal string
    -`workflows/test.md`contains`mkdir -p .planning/reports` directory creation - Steps S1-S8 appear AFTER Step 10 content (higher line numbers)
</acceptance_criteria>

  <done>
    Complete standalone test flow (S1-S8) exists in workflow file. Covers argument parsing, tech stack auto-detection, infrastructure check, code reading, test writing, test running, report creation, and bug reporting with git commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify standard flow Steps 1-10 are unmodified</name>
  <files>workflows/test.md</files>

<read_first> - workflows/test.md (full file after Tasks 1 and 2)
</read_first>

  <action>
Verify that standard flow Steps 1 through 10 are completely unmodified by comparing against git baseline.

Run this exact verification sequence:

1. Verify key step headers exist:

```bash
grep "^## Step 1: Determine scope" workflows/test.md
grep "^## Step 1.5: Check for interrupted" workflows/test.md
grep "^## Step 2: Check test infrastructure" workflows/test.md
grep "^## Step 10: Git commit" workflows/test.md
```

2. Compare with git baseline to confirm no changes:

```bash
git diff HEAD -- workflows/test.md | grep "^[-+]" | grep -v "^[-+][-+][-+]" | grep -v "^[-+].*Step 0\|Step S\|Standalone\|standalone" | head -20
```

The diff should show ONLY additions (lines starting with `+`) for Step 0, Step S0.5, and Steps S1-S8. There should be ZERO removed lines (`-`) and ZERO modified lines within Steps 1-10.

3. Verify structural integrity:

```bash
grep -c "^## Step [0-9]" workflows/test.md
grep -c "^## Step S" workflows/test.md
```

Expected: `^## Step [0-9]` count should be 12 (Step 0, Steps 1, 1.5, 2, 3, 4, 5, 6, 7, 8, 9, 10). `^## Step S` count should be 9 (S0.5, S1-S8).

If ANY standard flow content was modified, revert ONLY the changed lines and re-apply Task 1/Task 2 changes correctly.
</action>

  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && git diff HEAD -- workflows/test.md | grep "^-[^-]" | grep -v "Step 0\|Step S\|Standalone\|standalone" | wc -l | tr -d ' '</automated>
  </verify>

<acceptance_criteria> - `git diff HEAD -- workflows/test.md | grep "^-[^-]" | grep -v "Step 0\|Step S\|Standalone\|standalone" | wc -l` returns `0` (no removed/modified standard flow lines) - `grep "^## Step 1: Determine scope" workflows/test.md` matches (Step 1 header intact) - `grep "^## Step 1.5: Check for interrupted" workflows/test.md` matches (Step 1.5 header intact) - `grep "^## Step 10: Git commit" workflows/test.md` matches (Step 10 header intact) - `grep "npm test -- --verbose" workflows/test.md` matches (Step 5 run command intact) - `grep "TEST_REPORT.md" workflows/test.md` matches at least twice (Step 7 + Step 1.5 recovery) - `grep "<rules>" workflows/test.md` matches (rules section intact)
</acceptance_criteria>

  <done>
    Standard flow Steps 1-10 are byte-for-byte identical to the git baseline. Only Step 0, Step S0.5, and Steps S1-S8 were added. No existing content was modified or removed.
  </done>
</task>

</tasks>

<verification>
1. `grep "Step 0: Route by mode" workflows/test.md` found (router exists)
2. `grep "Step S1:" workflows/test.md` found (standalone flow starts)
3. `grep "Step S8:" workflows/test.md` found (standalone flow ends)
4. `grep "STANDALONE_TEST_REPORT" workflows/test.md` found (report format)
5. `grep "Patch version: standalone" workflows/test.md` found (bug format)
6. `git diff HEAD -- workflows/test.md | grep "^-[^-]" | grep -v "Step 0\|Step S\|Standalone" | wc -l` returns `0` (no standard flow changes)
7. `grep -c "Step S" workflows/test.md` returns at least 9 (S0.5 + S1-S8)
</verification>

<success_criteria>

- Step 0 routes `--standalone` to standalone flow, non-standalone to standard flow
- Steps S1-S8 cover: argument parsing, auto-detection, infrastructure, code reading, test writing, test running, report creation, bug reporting
- Recovery check (Step S0.5) detects interrupted sessions
- Standard flow Steps 1-10 are completely unmodified (zero diff lines in standard sections)
- Report uses `STANDALONE_TEST_REPORT_[YYYYMMDD_HHMMSS].md` format in `.planning/reports/`
- Bug reports use `Patch version: standalone` literal
  </success_criteria>

<output>
After completion, create `.planning/phases/71-core-standalone-flow/71-02-SUMMARY.md`
</output>
