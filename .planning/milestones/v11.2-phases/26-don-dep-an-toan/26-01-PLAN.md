---
phase: 26-don-dep-an-toan
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/debug-cleanup.js
  - test/smoke-debug-cleanup.test.js
autonomous: true
requirements:
  - CLEAN-01
  - SEC-01

must_haves:
  truths:
    - "scanDebugMarkers tra ve danh sach dong co marker [PD-DEBUG] voi path, line (1-indexed), text"
    - "scanDebugMarkers tra mang rong khi khong co marker nao"
    - "scanDebugMarkers KHONG match dong tuong tu nhung thieu marker ([DEBUG], PD-DEBUG, [PD-INFO])"
    - "matchSecurityWarnings tra ve toi da 3 canh bao match file path trong report section"
    - "matchSecurityWarnings tra mang rong khi khong co section 'Canh bao bao mat'"
    - "matchSecurityWarnings tra mang rong khi file paths khong match"
  artifacts:
    - path: "bin/lib/debug-cleanup.js"
      provides: "2 pure functions: scanDebugMarkers + matchSecurityWarnings"
      exports: ["scanDebugMarkers", "matchSecurityWarnings"]
    - path: "test/smoke-debug-cleanup.test.js"
      provides: "Smoke tests cho ca 2 functions"
      min_lines: 80
  key_links:
    - from: "test/smoke-debug-cleanup.test.js"
      to: "bin/lib/debug-cleanup.js"
      via: "require('../bin/lib/debug-cleanup')"
      pattern: "require.*debug-cleanup"
---

<objective>
Tao module `bin/lib/debug-cleanup.js` voi 2 pure functions va smoke test tuong ung, su dung TDD (test truoc, code sau).

Purpose: CLEAN-01 can `scanDebugMarkers()` de tim dong co marker `[PD-DEBUG]` trong staged files. SEC-01 can `matchSecurityWarnings()` de lien ket canh bao bao mat tu SCAN_REPORT.md cho file bi loi. Ca 2 la pure functions — nhan content string, tra ket qua, KHONG doc file, KHONG co side effects (per D-11, D-12, D-13, D-14).

Output: `bin/lib/debug-cleanup.js` + `test/smoke-debug-cleanup.test.js` — tat ca tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-don-dep-an-toan/26-CONTEXT.md
@.planning/phases/26-don-dep-an-toan/26-RESEARCH.md

<interfaces>
<!-- Pattern module tu du an — executor dung lam template -->

From bin/lib/repro-test-generator.js:
```javascript
/**
 * Pure function: nhan params, tra ve test code string.
 * KHONG doc file — tat ca content truyen qua tham so.
 * Zero dependencies — self-contained.
 */
'use strict';
function generateReproTest(params) { ... }
module.exports = { generateReproTest };
```

From test/smoke-repro-test-generator.test.js:
```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const { generateReproTest } = require('../bin/lib/repro-test-generator');
function makeParams(overrides = {}) { ... }
describe('generateReproTest — happy path', () => { ... });
describe('generateReproTest — error handling', () => { ... });
```
</interfaces>
</context>

<feature>
  <name>Module debug-cleanup.js — scanDebugMarkers + matchSecurityWarnings</name>
  <files>bin/lib/debug-cleanup.js, test/smoke-debug-cleanup.test.js</files>
  <behavior>
    ## scanDebugMarkers(stagedFiles) — per D-12

    - Input: `[{path: string, content: string}]`
    - Output: `[{path: string, line: number, text: string}]`

    Test cases:
    1. Happy path: staged file co 2 dong chua `[PD-DEBUG]` → tra 2 entries voi line 1-indexed, text trimEnd
    2. Khong co marker: staged file khong co `[PD-DEBUG]` → tra mang rong `[]`
    3. False positive prevention: dong co `[DEBUG]`, `PD-DEBUG` (khong brackets), `[PD-INFO]` → KHONG match, tra mang rong
    4. Nhieu files: 3 files, chi 2 co marker → tra entries tu 2 files do, group dung path
    5. Edge: stagedFiles rong `[]` → tra mang rong
    6. Edge: file.content la empty string → tra mang rong
    7. Error: stagedFiles la null/undefined → throw Error voi message chua 'stagedFiles'
    8. Error: stagedFiles khong phai array → throw Error

    ## matchSecurityWarnings(reportContent, filePaths) — per D-13

    - Input: `string, string[]`
    - Output: `[{file: string, severity: string, desc: string}]`

    Test cases:
    1. Happy path: report co section "Canh bao bao mat" + 1 file match → tra 1 warning voi severity + desc
    2. Match ca dau: report co section "Canh bao bao mat" (co dau tieng Viet) → van match
    3. Max 3: report co 5 canh bao match → chi tra 3 (per D-09)
    4. Khong co section: report khong co heading "Canh bao bao mat" → tra mang rong
    5. File khong match: report co canh bao nhung cho file khac → tra mang rong
    6. Match basename: report chi chua basename (khong full path) → van match
    7. Severity extraction: dong chua "critical", "high", "moderate", "low" → extract dung
    8. Error: reportContent la null → throw Error voi message chua 'reportContent'
    9. Error: filePaths la null → throw Error voi message chua 'filePaths'
  </behavior>
  <implementation>
    Sau khi tat ca tests pass (GREEN), implement:

    ### scanDebugMarkers(stagedFiles)
    ```javascript
    'use strict';
    const MARKER = /\[PD-DEBUG\]/;

    function scanDebugMarkers(stagedFiles) {
      if (!stagedFiles || !Array.isArray(stagedFiles)) {
        throw new Error('scanDebugMarkers: thieu tham so stagedFiles');
      }
      const results = [];
      for (const file of stagedFiles) {
        if (!file.path || typeof file.content !== 'string') continue;
        const lines = file.content.split('\n');
        for (let i = 0; i < lines.length; i++) {
          if (MARKER.test(lines[i])) {
            results.push({ path: file.path, line: i + 1, text: lines[i].trimEnd() });
          }
        }
      }
      return results;
    }
    ```

    ### matchSecurityWarnings(reportContent, filePaths)
    ```javascript
    const MAX_WARNINGS = 3;
    const SECTION_RE = /## C[aả]nh b[aá]o b[aả]o m[aậ]t\s*\n([\s\S]*?)(?=\n## |\n$|$)/i;

    function matchSecurityWarnings(reportContent, filePaths) {
      if (!reportContent || typeof reportContent !== 'string') {
        throw new Error('matchSecurityWarnings: thieu tham so reportContent');
      }
      if (!filePaths || !Array.isArray(filePaths)) {
        throw new Error('matchSecurityWarnings: thieu tham so filePaths');
      }
      const sectionMatch = reportContent.match(SECTION_RE);
      if (!sectionMatch) return [];
      const sectionContent = sectionMatch[1];
      const warnings = [];
      for (const fp of filePaths) {
        if (warnings.length >= MAX_WARNINGS) break;
        const escapedPath = fp.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
        const basename = fp.split('/').pop();
        const escapedBasename = basename.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
        const lineRegex = new RegExp(`^.*(?:${escapedPath}|${escapedBasename}).*$`, 'gm');
        let lineMatch;
        while ((lineMatch = lineRegex.exec(sectionContent)) !== null) {
          if (warnings.length >= MAX_WARNINGS) break;
          const line = lineMatch[0];
          const sevMatch = line.match(/\b(critical|high|moderate|low)\b/i);
          warnings.push({
            file: fp,
            severity: sevMatch ? sevMatch[1].toLowerCase() : 'unknown',
            desc: line.trim(),
          });
        }
      }
      return warnings.slice(0, MAX_WARNINGS);
    }

    module.exports = { scanDebugMarkers, matchSecurityWarnings };
    ```

    ### JSDoc va module header
    - Header: `/** ... Pure function: nhan input, tra output. KHONG doc file ... */`
    - Moi function co JSDoc voi @param va @returns
    - Comment bang tieng Viet
  </implementation>
</feature>

<verification>
```bash
node --test test/smoke-debug-cleanup.test.js
```
Tat ca tests (17 cases) phai pass.
</verification>

<success_criteria>
- `bin/lib/debug-cleanup.js` ton tai, export `scanDebugMarkers` va `matchSecurityWarnings`
- `test/smoke-debug-cleanup.test.js` ton tai voi 17+ test cases
- `node --test test/smoke-debug-cleanup.test.js` exit 0, 0 failures
- Module la pure function: KHONG co `require('fs')`, KHONG co `require('child_process')`, KHONG co side effects
- scanDebugMarkers dung regex `/\[PD-DEBUG\]/` — literal match, escape brackets
- matchSecurityWarnings gioi han MAX_WARNINGS = 3
- matchSecurityWarnings section regex match ca co dau va khong dau tieng Viet
</success_criteria>

<output>
After completion, create `.planning/phases/26-don-dep-an-toan/26-01-SUMMARY.md`
</output>
