---
phase: 25-dieu-tra-tai-hien-loi
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/repro-test-generator.js
  - test/smoke-repro-test-generator.test.js
autonomous: true
requirements: [REPRO-01]

must_haves:
  truths:
    - "generateReproTest() nhan symptoms + bugTitle, tra ve testCode string + testFileName string"
    - "testCode co TODO markers de AI dien logic, co describe/it blocks, co node:test imports"
    - "testFileName follow pattern repro-{bugTitle}.test.js voi ky tu dac biet da sanitize"
    - "Throw error khi thieu required params (symptoms, bugTitle)"
  artifacts:
    - path: "bin/lib/repro-test-generator.js"
      provides: "Pure function tao skeleton repro test"
      exports: ["generateReproTest"]
    - path: "test/smoke-repro-test-generator.test.js"
      provides: "Unit tests cho repro-test-generator"
      min_lines: 50
  key_links:
    - from: "workflows/fix-bug.md"
      to: "bin/lib/repro-test-generator.js"
      via: "Sub-step 5b.1 goi generateReproTest()"
      pattern: "repro-test-generator"
---

<objective>
Tao module repro-test-generator.js — pure function tao skeleton test tai hien loi (REPRO-01).

Purpose: AI co the tu dong tao file test skeleton trong `.planning/debug/repro/` voi TODO markers de AI dien logic tai hien loi.
Output: bin/lib/repro-test-generator.js + test file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@bin/lib/mermaid-validator.js
@test/smoke-mermaid-validator.test.js

<interfaces>
<!-- Pattern pure function module tu mermaid-validator.js -->

Tu bin/lib/mermaid-validator.js (pattern JSDoc + export):
```javascript
/**
 * Mermaid Validator Module — syntax + style compliance checker
 *
 * Pure function: nhan Mermaid text, tra ve { valid, errors, warnings }.
 * Zero dependencies — self-contained.
 */
'use strict';
// ...
module.exports = { mermaidValidator };
```

Tu test/smoke-mermaid-validator.test.js (pattern test):
```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const { mermaidValidator } = require('../bin/lib/mermaid-validator');
function makeFlowchart(overrides = {}) { /* helper */ }
```

API Design tu RESEARCH.md (Claude's Discretion — dung lam reference):
```javascript
/**
 * @param {object} params
 * @param {object} params.symptoms - { expected, actual, errorMessage, timeline, reproduce }
 * @param {string} params.bugTitle - Ten tat cua bug (vi du: 'login-timeout')
 * @param {string} params.filePath - Duong dan file bi loi
 * @param {string} [params.functionName] - Ten function bi loi (optional)
 * @returns {{ testCode: string, testFileName: string }}
 */
function generateReproTest(params) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tao repro-test-generator.js va smoke test</name>
  <files>bin/lib/repro-test-generator.js, test/smoke-repro-test-generator.test.js</files>
  <read_first>
    - bin/lib/mermaid-validator.js (pattern module: JSDoc header, 'use strict', module.exports, zero deps)
    - bin/lib/report-filler.js (pattern pure function nhan content string, tra string)
    - test/smoke-mermaid-validator.test.js (pattern test: helper function, describe/it, assert)
    - .planning/phases/25-dieu-tra-tai-hien-loi/25-RESEARCH.md (API design section: generateReproTest)
  </read_first>
  <behavior>
    - generateReproTest({ symptoms: {expected:'a',actual:'b'}, bugTitle:'login-timeout', filePath:'src/auth.js' }) tra ve { testCode: string, testFileName: 'repro-login-timeout.test.js' }
    - testCode chua `require('node:test')` va `require('node:assert/strict')`
    - testCode chua `describe('Tai hien: login-timeout'`
    - testCode chua `assert.fail('TODO: Dien logic tai hien loi')`
    - testCode chua `// --- Arrange ---` va `// --- Act ---` va `// --- Assert ---`
    - testCode chua symptoms.expected va symptoms.actual trong comments
    - testFileName sanitize ky tu dac biet: generateReproTest({..., bugTitle:'bug with spaces!'}) → testFileName = 'repro-bug-with-spaces-.test.js'
    - Throw Error khi thieu params.symptoms: generateReproTest({bugTitle:'x'})
    - Throw Error khi thieu params.bugTitle: generateReproTest({symptoms:{}})
    - Throw Error khi params = null hoac undefined
    - functionName optional: khi co, xuat hien trong comment header; khi thieu, ghi 'chua xac dinh'
  </behavior>
  <action>
    **Buoc 1 — Tao test file truoc (RED):**

    Tao `test/smoke-repro-test-generator.test.js`:
    - Import `generateReproTest` tu `../bin/lib/repro-test-generator`
    - Helper `makeParams(overrides)` tra ve default params: `{ symptoms: { expected: 'ket qua dung', actual: 'bi loi', errorMessage: 'Error X', timeline: 'Hom nay', reproduce: 'Click button' }, bugTitle: 'test-bug', filePath: 'src/app.js' }`
    - 4 describe blocks:
      1. `happy path` — valid params tra ve testCode + testFileName dung format
      2. `template content` — testCode chua node:test imports, describe/it, TODO markers, AAA comments, symptoms trong comments
      3. `filename sanitization` — ky tu dac biet thay bang '-', lowercase
      4. `error handling` — thieu symptoms throw, thieu bugTitle throw, null params throw
    - Tong cong 10+ it() blocks cho 10 behaviors

    Chay test → PHAI FAIL (module chua ton tai).

    **Buoc 2 — Tao module (GREEN):**

    Tao `bin/lib/repro-test-generator.js`:
    ```javascript
    /**
     * Repro Test Generator — Tao skeleton test tai hien loi.
     *
     * Pure function: nhan params, tra ve test code string.
     * KHONG doc file — tat ca content truyen qua tham so.
     */

    'use strict';
    ```

    Function `generateReproTest(params)`:
    - Validate: throw Error khi `!params`, `!params.symptoms`, `!params.bugTitle`
    - Error message: `'generateReproTest: thieu params.symptoms hoac params.bugTitle'`
    - `safeBugTitle = params.bugTitle.replace(/[^a-z0-9-]/gi, '-').toLowerCase()`
    - `testFileName = 'repro-' + safeBugTitle + '.test.js'`
    - `testCode` dung template literal (D-02: chi 1 Generic template):
      - Header JSDoc comment voi bugTitle, filePath, functionName
      - `'use strict'`
      - `require('node:test')` va `require('node:assert/strict')`
      - `describe('Tai hien: ${bugTitle}', () => {`
      - `it('phai tai hien duoc loi', () => {`
      - Comments voi symptoms (expected, actual, errorMessage)
      - AAA pattern: `// --- Arrange ---`, `// --- Act ---`, `// --- Assert ---`
      - `assert.fail('TODO: Dien logic tai hien loi');`
      - Close brackets
    - Return `{ testCode, testFileName }`
    - `module.exports = { generateReproTest };`

    Per D-01: module chi TRA VE string, KHONG ghi file. Workflow se ghi file vao `.planning/debug/repro/`.
    Per D-03: testCode la skeleton voi TODO markers — KHONG chay tu dong.

    Chay test → PHAI PASS.
  </action>
  <verify>
    <automated>node --test test/smoke-repro-test-generator.test.js</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/repro-test-generator.js ton tai va co `module.exports = { generateReproTest }`
    - bin/lib/repro-test-generator.js co `KHONG doc file` trong JSDoc header
    - bin/lib/repro-test-generator.js co `'use strict'`
    - bin/lib/repro-test-generator.js KHONG co `require('fs')`
    - bin/lib/repro-test-generator.js KHONG co `require('child_process')`
    - test/smoke-repro-test-generator.test.js ton tai va co it nhat 10 it() blocks
    - `node --test test/smoke-repro-test-generator.test.js` exit code 0
    - `npm test` exit code 0
  </acceptance_criteria>
  <done>generateReproTest() pure function hoat dong, 10+ tests pass, output la skeleton test voi TODO markers va Generic template duy nhat</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-repro-test-generator.test.js` — unit tests pass
- `npm test` — full suite pass
- `grep "module.exports" bin/lib/repro-test-generator.js` chua `generateReproTest`
- `grep "require.*fs" bin/lib/repro-test-generator.js` tra ve empty (khong co fs import)
</verification>

<success_criteria>
- repro-test-generator.js la pure function module (nhan params, tra { testCode, testFileName })
- Chi 1 Generic template (per D-02)
- testCode co TODO markers (per D-03)
- Output la string, KHONG ghi file (per D-01)
- Module throw error khi thieu required params
</success_criteria>

<output>
After completion, create `.planning/phases/25-dieu-tra-tai-hien-loi/25-02-SUMMARY.md`
</output>
