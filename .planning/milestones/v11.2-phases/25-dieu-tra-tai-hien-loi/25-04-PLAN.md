---
phase: 25-dieu-tra-tai-hien-loi
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - workflows/fix-bug.md
  - test/snapshots/codex/fix-bug.md
  - test/snapshots/copilot/fix-bug.md
  - test/snapshots/gemini/fix-bug.md
  - test/snapshots/opencode/fix-bug.md
autonomous: true
requirements: [REPRO-01, REGR-01]

must_haves:
  truths:
    - "fix-bug.md co sub-step 5b.1 tao reproduction test goi generateReproTest()"
    - "fix-bug.md co sub-step 8a phan tich regression goi analyzeFromCallChain/analyzeFromSourceFiles()"
    - "Ca 2 sub-steps la blocking — loi thi workflow DUNG"
    - "fix-bug.md duoi 420 dong (D-12)"
    - "4 platform snapshots da duoc regenerate va tests pass"
  artifacts:
    - path: "workflows/fix-bug.md"
      provides: "Workflow fix-bug voi 2 sub-steps moi"
      contains: "5b.1"
    - path: "test/snapshots/codex/fix-bug.md"
      provides: "Codex platform snapshot"
    - path: "test/snapshots/copilot/fix-bug.md"
      provides: "Copilot platform snapshot"
    - path: "test/snapshots/gemini/fix-bug.md"
      provides: "Gemini platform snapshot"
    - path: "test/snapshots/opencode/fix-bug.md"
      provides: "Opencode platform snapshot"
  key_links:
    - from: "workflows/fix-bug.md"
      to: "bin/lib/repro-test-generator.js"
      via: "Sub-step 5b.1 instructions reference generateReproTest"
      pattern: "generateReproTest"
    - from: "workflows/fix-bug.md"
      to: "bin/lib/regression-analyzer.js"
      via: "Sub-step 8a instructions reference analyzeFromCallChain"
      pattern: "analyzeFromCallChain"
---

<objective>
Them 2 sub-steps vao workflow fix-bug.md (D-10) va regenerate snapshots.

Purpose: Tich hop repro-test-generator va regression-analyzer vao workflow fix-bug de AI tu dong su dung khi fix bug.
Output: fix-bug.md updated + 4 snapshots regenerated
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@workflows/fix-bug.md
@bin/lib/repro-test-generator.js
@bin/lib/regression-analyzer.js

<interfaces>
<!-- Exports tu modules moi (tao boi Plan 02 va Plan 03) -->

Tu bin/lib/repro-test-generator.js:
```javascript
/**
 * @param {object} params
 * @param {object} params.symptoms - { expected, actual, errorMessage, timeline, reproduce }
 * @param {string} params.bugTitle - Ten tat cua bug
 * @param {string} params.filePath - Duong dan file bi loi
 * @param {string} [params.functionName] - Ten function bi loi
 * @returns {{ testCode: string, testFileName: string }}
 */
function generateReproTest(params) { ... }
module.exports = { generateReproTest };
```

Tu bin/lib/regression-analyzer.js:
```javascript
/**
 * Mode 1: Parse FastCode call chain response.
 * @param {object} params
 * @param {string} params.callChainText - Text response tu FastCode
 * @param {string} params.targetFile - File bi loi
 * @param {string} [params.targetFunction] - Function bi loi
 * @returns {{ affectedFiles: Array<{path: string, reason: string, depth: number}>, totalFound: number }}
 */
function analyzeFromCallChain(params) { ... }

/**
 * Mode 2: BFS fallback.
 * @param {object} params
 * @param {Array<{path: string, content: string}>} params.sourceFiles
 * @param {string} params.targetFile
 * @returns {{ affectedFiles: Array<{path: string, reason: string, depth: number}>, totalFound: number }}
 */
function analyzeFromSourceFiles(params) { ... }
module.exports = { analyzeFromCallChain, analyzeFromSourceFiles };
```

Tu workflows/fix-bug.md (vi tri chen — hien tai):
- Dong ~126: `### 5b. Tai hien toi gian (cho loi kho)`
- Dong ~131: `### 5c. Hinh thanh + kiem chung gia thuyet`
- Dong ~272: `## Buoc 8: Sua code`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Them sub-step 5b.1 va 8a vao fix-bug.md</name>
  <files>workflows/fix-bug.md</files>
  <read_first>
    - workflows/fix-bug.md (toan bo — can hieu structure de chen dung vi tri)
    - bin/lib/repro-test-generator.js (verify export names va params)
    - bin/lib/regression-analyzer.js (verify export names va params)
    - .planning/phases/25-dieu-tra-tai-hien-loi/25-CONTEXT.md (D-10, D-11, D-12)
  </read_first>
  <action>
    **Chen sub-step 5b.1 SAU section 5b (Tai hien toi gian) va TRUOC section 5c (Hinh thanh gia thuyet):**

    Tim dong `### 5b. Tái hiện tối giản (cho lỗi khó)` va section content cua no (ket thuc truoc `### 5c.`).
    Chen ngay SAU section 5b, TRUOC `### 5c.`:

    ```markdown
    ### 5b.1: Tạo Reproduction Test (SAU giả thuyết, TRƯỚC fix)

    **CHỈ KHI** có đủ triệu chứng (Bước 1b) VÀ file lỗi đã xác định (Bước 4).

    1. `mkdir -p .planning/debug/repro`
    2. Gọi `generateReproTest()` từ `bin/lib/repro-test-generator.js`:
       - Input: `{ symptoms }` (từ Bước 1b), `bugTitle` (từ SESSION tên-tắt), `filePath` (từ Bước 4), `functionName` (nếu có)
       - **Lỗi → DỪNG.** Báo: "Không tạo được reproduction test: [lỗi]"
    3. Ghi file `.planning/debug/repro/[testFileName]`
    4. Ghi SESSION: `Reproduction test: .planning/debug/repro/[testFileName]`
    ```

    **Chen sub-step 8a TRUOC noi dung Buoc 8 hien tai (Sua code):**

    Tim dong `## Bước 8: Sửa code`.
    Chen ngay SAU heading `## Bước 8: Sửa code` va TRUOC dong `- Áp dụng bản sửa`:

    ```markdown
    ### 8a: Phân tích Regression (TRƯỚC khi sửa)

    **CHỈ KHI** đã xác định file lỗi (Bước 4).

    1. Thử FastCode `code_qa` hỏi: "Liệt kê các files import hoặc gọi [function] trong [targetFile]"
       - Thành công → gọi `analyzeFromCallChain()` từ `bin/lib/regression-analyzer.js`:
         Input: `{ callChainText, targetFile, targetFunction }`
       - FastCode lỗi → đọc source files quanh targetFile → gọi `analyzeFromSourceFiles()`:
         Input: `{ sourceFiles: [{path, content}], targetFile, targetFunction }`
       - **Lỗi → DỪNG.** Báo: "Không phân tích được regression: [lỗi]"
    2. Kết quả `affectedFiles` (tối đa 5) → ghi vào BUG report section "Ảnh hưởng"
    3. Ghi SESSION: `Regression: [số] files bị ảnh hưởng`

    ```

    **KIEM TRA SAU KHI SUA:**
    - `wc -l workflows/fix-bug.md` PHAI < 420 (D-12). Hien tai 360 dong, them ~30-40 dong → ~390-400 dong.
    - Neu vuot 420, rut gon text trong sub-steps (viet ngan hon, bo vi du).
    - Ca 2 sub-steps phai co "DUNG" khi loi (D-11: blocking mode).
    - KHONG tao buoc moi voi so moi (D-10: them sub-steps trong buoc hien co).
  </action>
  <verify>
    <automated>grep -c "5b.1" workflows/fix-bug.md && grep -c "8a:" workflows/fix-bug.md && wc -l workflows/fix-bug.md | awk '{if ($1 < 420) print "OK: "$1" dong"; else print "FAIL: "$1" dong > 420"}'</automated>
  </verify>
  <acceptance_criteria>
    - workflows/fix-bug.md co `### 5b.1` (sub-step moi)
    - workflows/fix-bug.md co `### 8a:` (sub-step moi)
    - workflows/fix-bug.md co `generateReproTest()` trong section 5b.1
    - workflows/fix-bug.md co `analyzeFromCallChain()` trong section 8a
    - workflows/fix-bug.md co `analyzeFromSourceFiles()` trong section 8a
    - workflows/fix-bug.md co `DỪNG` trong ca 2 sub-steps (blocking mode D-11)
    - workflows/fix-bug.md co `bin/lib/repro-test-generator.js` (path day du)
    - workflows/fix-bug.md co `bin/lib/regression-analyzer.js` (path day du)
    - `wc -l workflows/fix-bug.md` < 420 (D-12)
    - Section 5b.1 nam GIUA 5b va 5c (KHONG truoc 5b, KHONG sau 5c)
    - Section 8a nam trong Buoc 8 (KHONG phai buoc rieng)
  </acceptance_criteria>
  <done>fix-bug.md co 2 sub-steps moi (5b.1 repro, 8a regression), blocking mode, duoi 420 dong</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate snapshots va verify full test suite</name>
  <files>test/snapshots/codex/fix-bug.md, test/snapshots/copilot/fix-bug.md, test/snapshots/gemini/fix-bug.md, test/snapshots/opencode/fix-bug.md</files>
  <read_first>
    - workflows/fix-bug.md (da sua o Task 1 — verify sub-steps da them)
    - test/generate-snapshots.js (script regenerate snapshots — hieu cach chay)
  </read_first>
  <action>
    **Buoc 1:** Chay regenerate snapshots:
    ```bash
    node test/generate-snapshots.js
    ```

    **Buoc 2:** Verify chi fix-bug snapshots thay doi:
    ```bash
    git diff --name-only test/snapshots/
    ```
    Ket qua mong doi: chi 4 files fix-bug.md thay doi (codex, copilot, gemini, opencode).
    Neu co snapshot KHAC (khong phai fix-bug) cung thay doi → DUNG, dieu tra. Pitfall 5 warning.

    **Buoc 3:** Chay full test suite:
    ```bash
    npm test
    ```
    Ket qua mong doi: 526+ tests pass (bao gom snapshot tests va 3 module tests moi).

    **Buoc 4:** Verify dong count workflow:
    ```bash
    wc -l workflows/fix-bug.md
    ```
    Ket qua mong doi: < 420 dong.
  </action>
  <verify>
    <automated>node test/generate-snapshots.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - test/snapshots/codex/fix-bug.md da duoc regenerate (co `5b.1` trong noi dung)
    - test/snapshots/copilot/fix-bug.md da duoc regenerate (co `5b.1` trong noi dung)
    - test/snapshots/gemini/fix-bug.md da duoc regenerate (co `5b.1` trong noi dung)
    - test/snapshots/opencode/fix-bug.md da duoc regenerate (co `5b.1` trong noi dung)
    - `npm test` exit code 0 (toan bo 526+ tests pass, bao gom snapshot tests)
    - `git diff --name-only test/snapshots/` chi liet ke fix-bug.md files (khong co snapshot khac)
  </acceptance_criteria>
  <done>4 platform snapshots regenerated, full test suite pass, khong co snapshot ngoai y muon thay doi</done>
</task>

</tasks>

<verification>
- `grep "5b.1" workflows/fix-bug.md` — sub-step ton tai
- `grep "8a:" workflows/fix-bug.md` — sub-step ton tai
- `wc -l workflows/fix-bug.md` < 420 — gioi han dong (D-12)
- `grep "5b.1" test/snapshots/codex/fix-bug.md` — snapshot da cap nhat
- `npm test` — full 526+ tests pass bao gom snapshots
</verification>

<success_criteria>
- Workflow fix-bug.md co 2 sub-steps moi (5b.1 va 8a) o dung vi tri
- Blocking mode cho ca 2 sub-steps (D-11)
- Workflow duoi 420 dong (D-12)
- 4 platform snapshots da regenerate
- Full test suite pass
</success_criteria>

<output>
After completion, create `.planning/phases/25-dieu-tra-tai-hien-loi/25-04-SUMMARY.md`
</output>
