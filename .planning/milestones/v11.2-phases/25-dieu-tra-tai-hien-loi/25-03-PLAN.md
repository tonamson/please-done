---
phase: 25-dieu-tra-tai-hien-loi
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/regression-analyzer.js
  - test/smoke-regression-analyzer.test.js
autonomous: true
requirements: [REGR-01]

must_haves:
  truths:
    - "analyzeFromCallChain() parse FastCode text response va tra ve danh sach files bi anh huong (max 5)"
    - "analyzeFromSourceFiles() parse import/require tu source files va tra ve danh sach files bi anh huong (max 5)"
    - "Ca 2 functions gioi han depth 1-2 levels"
    - "Throw error khi thieu required params"
  artifacts:
    - path: "bin/lib/regression-analyzer.js"
      provides: "Pure function phan tich regression qua call chain"
      exports: ["analyzeFromCallChain", "analyzeFromSourceFiles"]
    - path: "test/smoke-regression-analyzer.test.js"
      provides: "Unit tests cho regression-analyzer"
      min_lines: 60
  key_links:
    - from: "workflows/fix-bug.md"
      to: "bin/lib/regression-analyzer.js"
      via: "Sub-step 8a goi analyzeFromCallChain hoac analyzeFromSourceFiles"
      pattern: "regression-analyzer"
---

<objective>
Tao module regression-analyzer.js — pure function phan tich module phu thuoc qua call chain (REGR-01).

Purpose: AI co the phan tich cac module bi anh huong truoc khi sua code, giup giam rui ro regression.
Output: bin/lib/regression-analyzer.js + test file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@bin/lib/mermaid-validator.js
@test/smoke-mermaid-validator.test.js

<interfaces>
<!-- Pattern pure function module tu mermaid-validator.js -->

Tu bin/lib/mermaid-validator.js (pattern):
```javascript
/**
 * Module Name — mo ta.
 * Pure function: nhan X, tra ve Y.
 * Zero dependencies — self-contained.
 */
'use strict';
module.exports = { functionName };
```

API Design tu RESEARCH.md (Claude's Discretion):
```javascript
const MAX_AFFECTED = 5;  // D-05: maximum 5 files
const MAX_DEPTH = 2;     // D-04: 1-2 levels

/**
 * Mode 1: Parse FastCode call chain response.
 * @param {object} params
 * @param {string} params.callChainText - Text response tu FastCode
 * @param {string} params.targetFile - File bi loi
 * @param {string} [params.targetFunction] - Function bi loi
 * @returns {{ affectedFiles: Array<{path: string, reason: string, depth: number}>, totalFound: number }}
 */
function analyzeFromCallChain(params) { ... }

/**
 * Mode 2: BFS fallback — parse import/require tu source files.
 * @param {object} params
 * @param {Array<{path: string, content: string}>} params.sourceFiles - Source files content
 * @param {string} params.targetFile - File bi loi
 * @param {string} [params.targetFunction] - Function bi loi
 * @returns {{ affectedFiles: Array<{path: string, reason: string, depth: number}>, totalFound: number }}
 */
function analyzeFromSourceFiles(params) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tao regression-analyzer.js va smoke test</name>
  <files>bin/lib/regression-analyzer.js, test/smoke-regression-analyzer.test.js</files>
  <read_first>
    - bin/lib/mermaid-validator.js (pattern module: JSDoc, 'use strict', constants, module.exports)
    - bin/lib/report-filler.js (pattern pure function voi content string args)
    - test/smoke-mermaid-validator.test.js (pattern test: helper, describe/it, assert)
    - .planning/phases/25-dieu-tra-tai-hien-loi/25-RESEARCH.md (API design va Pitfall 2: noise)
  </read_first>
  <behavior>
    === analyzeFromCallChain ===
    - Nhan callChainText co 3 callers → tra ve 3 affectedFiles voi path, reason, depth
    - Nhan callChainText co 10 callers → tra ve chi 5 (MAX_AFFECTED per D-05)
    - totalFound = so callers thuc te (truoc khi cap)
    - Filter theo depth: callers depth 3+ bi loai bo (D-04: max 2 levels)
    - affectedFiles sorted by depth (depth 1 truoc depth 2)
    - Throw Error khi thieu callChainText hoac targetFile
    - callChainText rong → tra ve { affectedFiles: [], totalFound: 0 }

    === analyzeFromSourceFiles ===
    - Nhan 3 source files, 1 file import targetFile → tra ve 1 affectedFile depth 1
    - BFS depth 2: file A import targetFile (depth 1), file B import file A (depth 2)
    - Max 5 files trong output (D-05)
    - Filter test files: files co `.test.` hoac `.spec.` KHONG xuat hien trong affectedFiles
    - Throw Error khi thieu sourceFiles hoac targetFile
    - sourceFiles rong → tra ve { affectedFiles: [], totalFound: 0 }
  </behavior>
  <action>
    **Buoc 1 — Tao test file truoc (RED):**

    Tao `test/smoke-regression-analyzer.test.js`:
    - Import `analyzeFromCallChain`, `analyzeFromSourceFiles` tu `../bin/lib/regression-analyzer`
    - Helper `makeCallChainText(callerCount)` tao mock FastCode output:
      ```
      Call chain for targetFunction in target.js:
      - src/service.js:10 → targetFunction() [depth 1]
      - src/controller.js:25 → serviceCall() → targetFunction() [depth 2]
      ```
      Format: moi dong co `- {path}:{line} → {function}() [depth {n}]`
    - Helper `makeSourceFiles(files)` tao array `[{path, content}]` voi require/import statements
    - 4 describe blocks:
      1. `analyzeFromCallChain — happy path` (3 callers, cap max 5, sort by depth)
      2. `analyzeFromCallChain — edge cases` (empty text, depth filter, error handling)
      3. `analyzeFromSourceFiles — happy path` (depth 1 import, BFS depth 2)
      4. `analyzeFromSourceFiles — edge cases` (empty array, test file filter, error handling)
    - Tong cong 12+ it() blocks cho behaviors tren

    Chay test → PHAI FAIL.

    **Buoc 2 — Tao module (GREEN):**

    Tao `bin/lib/regression-analyzer.js`:
    ```javascript
    /**
     * Regression Analyzer — Phan tich module phu thuoc qua call chain.
     *
     * Pure function: nhan dependency data, tra ve danh sach files bi anh huong.
     * KHONG doc file, KHONG goi MCP.
     *
     * Co 2 mode:
     * 1. FastCode mode: nhan callChainText tu MCP ket qua, parse va filter
     * 2. BFS fallback: nhan sourceFiles array, parse import/require, tim phu thuoc
     */

    'use strict';

    const MAX_AFFECTED = 5;  // D-05: maximum 5 files
    const MAX_DEPTH = 2;     // D-04: 1-2 levels
    ```

    **analyzeFromCallChain(params):**
    - Validate: throw khi `!params.callChainText` hoac `!params.targetFile`
    - Parse moi dong trong callChainText theo regex: `/^\s*-\s*(.+?):(\d+)\s*.*\[depth\s*(\d+)\]/gm`
    - Filter: chi giu depth <= MAX_DEPTH
    - Sort: by depth ascending
    - Cap: lay MAX_AFFECTED items dau tien
    - Return: `{ affectedFiles: [{path, reason, depth}], totalFound }`
    - `reason` = `'goi {function} tu dong {line}'` (extract tu parsed text)
    - callChainText rong (khong match regex) → return `{ affectedFiles: [], totalFound: 0 }`

    **analyzeFromSourceFiles(params):**
    - Validate: throw khi `!params.sourceFiles` hoac `!params.targetFile`
    - BFS implementation:
      - Level 1: Scan moi file content cho `require('...targetFile...')` hoac `import ... from '...targetFile...'`
        Regex: `/(?:require\s*\(\s*['"]([^'"]+)['"]\s*\)|from\s+['"]([^'"]+)['"])/g`
        Match khi path chua targetFile basename (khong co extension)
      - Level 2: Files tim duoc o level 1 → scan cac file khac import nhung files do
    - Filter test files: skip files co `.test.` hoac `.spec.` trong path
    - Cap: MAX_AFFECTED
    - Return: `{ affectedFiles: [{path, reason, depth}], totalFound }`
    - `reason` = `'import {targetFile} truc tiep'` (depth 1) hoac `'import {level1File} — gian tiep phu thuoc'` (depth 2)

    `module.exports = { analyzeFromCallChain, analyzeFromSourceFiles };`

    Chay test → PHAI PASS.
  </action>
  <verify>
    <automated>node --test test/smoke-regression-analyzer.test.js</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/regression-analyzer.js ton tai va co `module.exports = { analyzeFromCallChain, analyzeFromSourceFiles }`
    - bin/lib/regression-analyzer.js co `KHONG doc file` trong JSDoc header
    - bin/lib/regression-analyzer.js co `KHONG goi MCP` trong JSDoc header
    - bin/lib/regression-analyzer.js co `'use strict'`
    - bin/lib/regression-analyzer.js co `const MAX_AFFECTED = 5`
    - bin/lib/regression-analyzer.js co `const MAX_DEPTH = 2`
    - bin/lib/regression-analyzer.js KHONG co `require('fs')`
    - bin/lib/regression-analyzer.js KHONG co `require('child_process')`
    - test/smoke-regression-analyzer.test.js ton tai va co it nhat 12 it() blocks
    - `node --test test/smoke-regression-analyzer.test.js` exit code 0
    - `npm test` exit code 0
  </acceptance_criteria>
  <done>regression-analyzer.js co 2 exported functions (FastCode mode + BFS fallback), max 5 files output, depth 1-2, 12+ tests pass</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-regression-analyzer.test.js` — unit tests pass
- `npm test` — full suite pass
- `grep "MAX_AFFECTED" bin/lib/regression-analyzer.js` tra ve `const MAX_AFFECTED = 5`
- `grep "MAX_DEPTH" bin/lib/regression-analyzer.js` tra ve `const MAX_DEPTH = 2`
- `grep "require.*fs" bin/lib/regression-analyzer.js` tra ve empty
</verification>

<success_criteria>
- regression-analyzer.js la pure function module (KHONG doc file, KHONG goi MCP)
- 2 modes: FastCode (analyzeFromCallChain) va BFS fallback (analyzeFromSourceFiles) per D-06
- Max depth 2 levels per D-04
- Max 5 files per D-05
- Test files (.test., .spec.) bi filter khoi output
</success_criteria>

<output>
After completion, create `.planning/phases/25-dieu-tra-tai-hien-loi/25-03-SUMMARY.md`
</output>
