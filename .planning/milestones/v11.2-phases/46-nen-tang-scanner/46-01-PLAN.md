---
phase: 46-nen-tang-scanner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/security-rules.yaml
  - commands/pd/agents/pd-sec-scanner.md
  - test/smoke-security-rules.test.js
autonomous: true
requirements: [AGENT-01, AGENT-02]

must_haves:
  truths:
    - "security-rules.yaml chua day du 13 OWASP categories voi patterns, fixes, fastcode_queries"
    - "pd-sec-scanner.md nhan --category slug va huong dan doc YAML, chay FastCode tool-first, xuat evidence"
    - "Test smoke-security-rules.test.js xac nhan YAML co 13 categories va 5 truong bat buoc moi category"
  artifacts:
    - path: "references/security-rules.yaml"
      provides: "Nguon su that duy nhat cho 13 OWASP scanner categories"
      contains: "sql-injection"
    - path: "commands/pd/agents/pd-sec-scanner.md"
      provides: "Template agent nhan --category"
      contains: "pd-sec-scanner"
    - path: "test/smoke-security-rules.test.js"
      provides: "Validation test cho YAML schema"
      contains: "smoke-security-rules"
  key_links:
    - from: "commands/pd/agents/pd-sec-scanner.md"
      to: "references/security-rules.yaml"
      via: "Doc YAML theo --category slug"
      pattern: "security-rules\\.yaml"
---

<objective>
Tao file cau hinh `references/security-rules.yaml` chua day du rules cho 13 OWASP categories (migrate tu 13 scanner files cu) va template agent `pd-sec-scanner.md` doc YAML de quet bao mat.

Purpose: Gop 13 file scanner rieng le thanh 1 template + 1 YAML — DRY, de bao tri, de mo rong.
Output: 2 files moi (YAML + template) + 1 test file moi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-nen-tang-scanner/46-CONTEXT.md
@.planning/phases/46-nen-tang-scanner/46-RESEARCH.md

@references/security-checklist.md
@commands/pd/agents/pd-sec-sql-injection.md
@commands/pd/agents/pd-code-detective.md

<interfaces>
<!-- 13 scanner files can migrate — doc tat ca de extract patterns/fixes/fastcode_queries -->
<!-- Moi file co frontmatter (name, description, tier, allowed-tools) + body (objective, process, rules) -->
<!-- Evidence file names lay tu reporter: -->
From commands/pd/agents/pd-sec-reporter.md:
1. evidence_sec_sqli.md (sql-injection)
2. evidence_sec_xss.md (xss)
3. evidence_sec_cmdi.md (cmd-injection)
4. evidence_sec_pathtr.md (path-traversal)
5. evidence_sec_secrets.md (secrets)
6. evidence_sec_auth.md (auth)
7. evidence_sec_deser.md (deserialization)
8. evidence_sec_misconfig.md (misconfig)
9. evidence_sec_pollution.md (prototype-pollution)
10. evidence_sec_crypto.md (crypto)
11. evidence_sec_design.md (insecure-design)
12. evidence_sec_deps.md (vuln-deps)
13. evidence_sec_logging.md (logging)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tao security-rules.yaml va test kiem chung</name>
  <files>references/security-rules.yaml, test/smoke-security-rules.test.js</files>
  <read_first>
    - commands/pd/agents/pd-sec-sql-injection.md
    - commands/pd/agents/pd-sec-xss.md
    - commands/pd/agents/pd-sec-cmd-injection.md
    - commands/pd/agents/pd-sec-path-traversal.md
    - commands/pd/agents/pd-sec-secrets.md
    - commands/pd/agents/pd-sec-auth.md
    - commands/pd/agents/pd-sec-deserialization.md
    - commands/pd/agents/pd-sec-misconfig.md
    - commands/pd/agents/pd-sec-prototype-pollution.md
    - commands/pd/agents/pd-sec-crypto.md
    - commands/pd/agents/pd-sec-insecure-design.md
    - commands/pd/agents/pd-sec-vuln-deps.md
    - commands/pd/agents/pd-sec-logging.md
    - commands/pd/agents/pd-sec-reporter.md
    - references/security-checklist.md
    - test/smoke-resource-config.test.js
  </read_first>
  <behavior>
    - Test 1: YAML co dung 13 top-level keys (sql-injection, xss, cmd-injection, path-traversal, secrets, auth, deserialization, misconfig, prototype-pollution, crypto, insecure-design, vuln-deps, logging)
    - Test 2: Moi category co 5 truong bat buoc: owasp (string), severity (string), patterns (array), fixes (array), fastcode_queries (array)
    - Test 3: Moi category co truong evidence_file (string)
    - Test 4: patterns[].regex va patterns[].description ton tai cho moi pattern entry
    - Test 5: severity chi nhan 1 trong 4 gia tri: critical, high, medium, low
    - Test 6: fastcode_queries co it nhat 1 entry moi category
  </behavior>
  <action>
    **Buoc 1 — Tao test truoc (RED):**
    Tao `test/smoke-security-rules.test.js` dung node:test + js-yaml:
    ```javascript
    const yaml = require('js-yaml');
    const { readFileSync } = require('node:fs');
    const { join } = require('node:path');
    const rulesPath = join(__dirname, '..', 'references', 'security-rules.yaml');
    const rules = yaml.load(readFileSync(rulesPath, 'utf8'));
    ```
    Viet 6 test cases nhu behavior o tren. Dung `assert.equal(Object.keys(rules).length, 13)` cho test 1. Dung loop qua 13 keys cho test 2-6.

    QUAN TRONG: Kiem tra js-yaml da co trong devDependencies chua. Neu chua, chay `npm install --save-dev js-yaml` truoc.

    **Buoc 2 — Tao YAML (GREEN):**
    Tao `references/security-rules.yaml` voi 13 categories. Moi category co cau truc (per D-01):
    ```yaml
    {category-slug}:
      owasp: "{OWASP ID}"
      severity: {critical|high|medium|low}
      evidence_file: {evidence_sec_xxx.md}
      patterns:
        - regex: '{regex pattern}'
          description: "{Mo ta bang tieng Viet co dau}"
          stack: [{file extensions}]
      fixes:
        - pattern: "{ten pattern}"
          fix: "{Huong sua bang tieng Viet co dau}"
      fastcode_queries:
        - "{Cau hoi bang tieng Viet co dau}"
    ```

    Migrate TON BO patterns, fixes, fastcode_queries tu 13 scanner files cu. KHONG bo sot bat ky pattern nao.

    13 categories va thong tin chinh:
    1. sql-injection: owasp "A03:2021 Injection", severity critical, evidence_file evidence_sec_sqli.md
    2. xss: owasp "A03:2021 Injection", severity high, evidence_file evidence_sec_xss.md
    3. cmd-injection: owasp "A03:2021 Injection", severity critical, evidence_file evidence_sec_cmdi.md
    4. path-traversal: owasp "A10:2021 SSRF", severity high, evidence_file evidence_sec_pathtr.md
    5. secrets: owasp "A02:2021 Cryptographic Failures", severity critical, evidence_file evidence_sec_secrets.md
    6. auth: owasp "A01:2021 Broken Access Control", severity high, evidence_file evidence_sec_auth.md
    7. deserialization: owasp "A08:2021 Software and Data Integrity", severity critical, evidence_file evidence_sec_deser.md
    8. misconfig: owasp "A05:2021 Security Misconfiguration", severity medium, evidence_file evidence_sec_misconfig.md
    9. prototype-pollution: owasp "A03:2021 Injection", severity high, evidence_file evidence_sec_pollution.md
    10. crypto: owasp "A02:2021 Cryptographic Failures", severity high, evidence_file evidence_sec_crypto.md
    11. insecure-design: owasp "A04:2021 Insecure Design", severity medium, evidence_file evidence_sec_design.md
    12. vuln-deps: owasp "A06:2021 Vulnerable Components", severity high, evidence_file evidence_sec_deps.md
    13. logging: owasp "A09:2021 Security Logging Failures", severity medium, evidence_file evidence_sec_logging.md

    Doc tung file scanner cu de extract CHINH XAC cac regex patterns, fixes text, va fastcode queries. Moi scanner file co section `<process>` chua cac Grep patterns va section mo ta cach xu ly ket qua.

    **Buoc 3 — Validate:**
    Chay `node -e "require('js-yaml').load(require('fs').readFileSync('references/security-rules.yaml'))"` de dam bao YAML hop le.
    Chay `node --test test/smoke-security-rules.test.js` de dam bao tat ca tests PASS.
  </action>
  <verify>
    <automated>node --test test/smoke-security-rules.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `references/security-rules.yaml` ton tai va co 13 top-level keys
    - `node -e "const y=require('js-yaml').load(require('fs').readFileSync('references/security-rules.yaml'));console.log(Object.keys(y).length)"` in ra 13
    - `test/smoke-security-rules.test.js` ton tai va co `describe('security-rules.yaml'`
    - `node --test test/smoke-security-rules.test.js` exit code 0
    - YAML moi category co 6 truong: owasp, severity, evidence_file, patterns, fixes, fastcode_queries
    - patterns[].regex trong YAML khop voi regex tu cac scanner files cu (khong bo sot)
    - Toan bo noi dung YAML viet bang tieng Viet co dau (descriptions, fixes)
  </acceptance_criteria>
  <done>security-rules.yaml co 13 categories day du patterns/fixes/fastcode_queries migrate tu 13 scanner cu, test xac nhan schema dung</done>
</task>

<task type="auto">
  <name>Task 2: Tao template pd-sec-scanner.md</name>
  <files>commands/pd/agents/pd-sec-scanner.md</files>
  <read_first>
    - commands/pd/agents/pd-sec-sql-injection.md
    - commands/pd/agents/pd-code-detective.md
    - commands/pd/agents/pd-sec-reporter.md
    - references/security-rules.yaml
    - references/guard-fastcode.md
  </read_first>
  <action>
    Tao `commands/pd/agents/pd-sec-scanner.md` — template agent tong hop thay the 13 scanner rieng le (per D-03).

    **Frontmatter** (per D-07 — tier scout):
    ```yaml
    ---
    name: pd-sec-scanner
    description: Scanner bao mat tong hop — Quet ma nguon theo OWASP category duoc chi dinh tu security-rules.yaml.
    tier: scout
    allowed-tools:
      - Read
      - Glob
      - Grep
      - mcp__fastcode__code_qa
    ---
    ```

    **Body** gom 3 blocks: `<objective>`, `<process>`, `<rules>` (nhat quan voi moi agent file khac).

    **`<objective>`:** Mo ta ngan gon: "Quet ma nguon du an theo 1 OWASP category cu the, su dung rules tu security-rules.yaml. Nhan --category slug tu prompt."

    **`<process>`** — 7 buoc (per D-08 tool-first flow):
    1. Nhan `--category {slug}` tu prompt (vd: `--category sql-injection`). Neu khong co --category, bao loi va dung lai.
    2. Doc `references/security-rules.yaml` bang Read tool.
    3. Extract rules cua category tuong ung: owasp, severity, patterns[], fixes[], fastcode_queries[], evidence_file.
    4. Xac dinh stack du an tu prompt context hoac tu file extensions co trong codebase.
    5. **FastCode tool-first** (per D-08): Voi moi query trong `fastcode_queries[]`, goi `mcp__fastcode__code_qa` de discovery code lien quan. AI KHONG tu tim code — chi danh gia ket qua FastCode tra ve.
    6. **Fallback khi FastCode unavailable** (per D-09): Neu `mcp__fastcode__code_qa` khong kha dung (Docker chua chay, tool error), su dung Grep voi `patterns[].regex` de tim code thay the. Ghi note "FastCode khong kha dung — su dung Grep fallback" trong evidence.
    7. Voi moi ket qua tim duoc, doc context +-15 dong bang Read, phan loai:
       - FAIL: User input di thang vao sink nguy hiem khong qua sanitize
       - FLAG: Co pattern nguy hiem nhung dung bien noi bo (can review)
       - PASS: Da su dung bien phap bao ve (parameterized, sanitized, escaped)
    8. Ghi evidence file vao session dir voi ten tu `evidence_file` trong YAML (per D-05):
       - YAML frontmatter: `agent: pd-sec-scanner`, `category: {slug}`, `outcome: (vulnerabilities_found | clean | inconclusive)`, `timestamp: ISO 8601`, `session: {session_id}`
       - Markdown body: `## Tom tat` (tong file quet, so phat hien), `## Phat hien` (bang: File | Dong | Muc do | Mo ta | De xuat sua), `## Chi tiet` (code snippet + giai thich cho moi FAIL/FLAG)

    **`<rules>`:**
    - Luon su dung tieng Viet co dau
    - Khong duoc sua code, chi quet va bao cao
    - Phai dan chung file:dong cu the cho moi phat hien
    - Doc/ghi evidence tu session dir duoc truyen qua prompt. KHONG hardcode paths
    - FastCode la uu tien — chi dung Grep khi FastCode khong kha dung
    - Khong bao false positive cho ORM query builder (TypeORM `.where()`, Prisma `.findMany()`, Sequelize `.findAll()`)
    - Dung `fixes[]` tu YAML lam de xuat sua — khong tu nghi ra fix

    Toan bo noi dung template PHAI viet bang tieng Viet co dau chuan.
  </action>
  <verify>
    <automated>grep -c "pd-sec-scanner" commands/pd/agents/pd-sec-scanner.md && grep -c "security-rules.yaml" commands/pd/agents/pd-sec-scanner.md && grep -c "<objective>" commands/pd/agents/pd-sec-scanner.md && grep -c "<process>" commands/pd/agents/pd-sec-scanner.md && grep -c "<rules>" commands/pd/agents/pd-sec-scanner.md</automated>
  </verify>
  <acceptance_criteria>
    - `commands/pd/agents/pd-sec-scanner.md` ton tai
    - Frontmatter co `name: pd-sec-scanner`, `tier: scout`, `allowed-tools` gom Read, Glob, Grep, mcp__fastcode__code_qa
    - Body co `<objective>`, `<process>`, `<rules>` blocks
    - Process co buoc doc `references/security-rules.yaml`
    - Process co buoc goi `mcp__fastcode__code_qa` (tool-first)
    - Process co buoc fallback Grep khi FastCode unavailable
    - Process co buoc ghi evidence file voi YAML frontmatter (agent, category, outcome, timestamp, session)
    - Toan bo noi dung viet bang tieng Viet co dau
    - `grep "security-rules.yaml" commands/pd/agents/pd-sec-scanner.md` tra ve it nhat 1 dong
    - `grep "mcp__fastcode__code_qa" commands/pd/agents/pd-sec-scanner.md` tra ve it nhat 1 dong
    - `grep "Grep" commands/pd/agents/pd-sec-scanner.md` tra ve it nhat 1 dong (fallback)
  </acceptance_criteria>
  <done>Template pd-sec-scanner.md hoan chinh voi frontmatter scout, process 7 buoc tool-first, fallback Grep, evidence output format chuan</done>
</task>

</tasks>

<verification>
1. `node --test test/smoke-security-rules.test.js` — YAML schema dung, 13 categories, 5+ truong moi category
2. `node -e "require('js-yaml').load(require('fs').readFileSync('references/security-rules.yaml'))"` — YAML parse thanh cong
3. `grep -c "pd-sec-scanner" commands/pd/agents/pd-sec-scanner.md` — template ton tai voi dung name
4. `grep "security-rules.yaml" commands/pd/agents/pd-sec-scanner.md` — template tham chieu YAML
</verification>

<success_criteria>
- security-rules.yaml co 13 categories voi day du patterns migrate tu 13 files cu
- pd-sec-scanner.md la template hoan chinh nhan --category, doc YAML, chay FastCode/Grep, xuat evidence
- Test smoke-security-rules.test.js PASS xac nhan YAML schema
</success_criteria>

<output>
After completion, create `.planning/phases/46-nen-tang-scanner/46-01-SUMMARY.md`
</output>
