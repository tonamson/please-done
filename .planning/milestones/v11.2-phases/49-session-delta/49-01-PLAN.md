---
phase: 49-session-delta
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/session-delta.js
  - test/smoke-session-delta.test.js
autonomous: true
requirements:
  - DELTA-01
  - DELTA-02
  - DELTA-03
must_haves:
  truths:
    - "classifyDelta phan loai dung 7 truong hop: PASS+unchanged=SKIP, PASS+changed=RE-SCAN, FLAG+unchanged=KNOWN-UNFIXED, FLAG+changed=RE-SCAN, FAIL+unchanged=KNOWN-UNFIXED, FAIL+changed=RE-SCAN, SKIP=SKIP"
    - "Khong co evidence cu (empty/null) tra ve isFullScan=true"
    - "Path normalization strip ./ va normalize separators truoc khi so sanh"
    - "appendAuditHistory tao section moi khi chua co, append dong khi da co"
    - "parseAuditHistory doc table cu tra ve array entries"
    - "Map key dung compound format file::functionName tranh collision"
  artifacts:
    - path: "bin/lib/session-delta.js"
      provides: "classifyDelta, appendAuditHistory, parseAuditHistory, DELTA_STATUS pure functions"
      exports: ["classifyDelta", "appendAuditHistory", "parseAuditHistory", "DELTA_STATUS"]
      min_lines: 80
    - path: "test/smoke-session-delta.test.js"
      provides: "Unit tests bao phu 3 requirements DELTA-01, DELTA-02, DELTA-03"
      min_lines: 60
  key_links:
    - from: "bin/lib/session-delta.js"
      to: "evidence_sec_*.md format"
      via: "parseFunctionChecklist regex"
      pattern: "## Function Checklist"
    - from: "bin/lib/session-delta.js"
      to: "## Audit History table"
      via: "appendAuditHistory markdown table append"
      pattern: "## Audit History"
---

<objective>
Tao module session-delta.js voi 3 pure functions: classifyDelta (phan loai ham SKIP/RE-SCAN/NEW/KNOWN-UNFIXED), appendAuditHistory (tao/append dong audit history), parseAuditHistory (doc history table). TDD: viet tests truoc, implement sau.

Purpose: Day la core logic cua Phase 49 — cho phep audit lan sau chi quet lai nhung gi thay doi, tiet kiem token va thoi gian.
Output: bin/lib/session-delta.js (module) + test/smoke-session-delta.test.js (tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-session-delta/49-CONTEXT.md
@.planning/phases/49-session-delta/49-RESEARCH.md

@bin/lib/smart-selection.js
@bin/lib/evidence-protocol.js
@test/smoke-smart-selection.test.js

<interfaces>
<!-- Pattern tham khao tu smart-selection.js -->
From bin/lib/smart-selection.js:
```javascript
'use strict';
const BASE_SCANNERS = ['secrets', 'misconfig', 'logging'];
function selectScanners(projectContext) { ... }
module.exports = { selectScanners, BASE_SCANNERS, ALL_CATEGORIES, SIGNAL_MAP };
```

From bin/lib/evidence-protocol.js:
```javascript
const { parseFrontmatter } = require('./utils');
function validateEvidence(content) { ... }
function parseEvidence(content) { ... }
module.exports = { validateEvidence, parseEvidence, getRequiredSections, OUTCOME_TYPES };
```

From bin/lib/utils.js:
```javascript
function parseFrontmatter(content) { /* Parse YAML frontmatter tu markdown, tra ve { frontmatter, body, raw } */ }
```

Evidence Function Checklist format (tu pd-sec-scanner.md):
```markdown
## Function Checklist

| # | File | Ham | Dong | Verdict | Chi tiet |
|---|------|-----|------|---------|----------|
| 1 | src/api/users.js | getUserById | 42 | FLAG | IDOR |
| 2 | src/api/auth.js | login | 15 | PASS | OK |
```

Evidence YAML frontmatter format (giua 2 dong triple-dash):
  agent: pd-sec-scanner
  category: xss
  outcome: vulnerabilities_found
  timestamp: 2026-03-26T10:00:00+07:00
  session: abc123
  commit_sha: a1b2c3d
</interfaces>
</context>

<feature>
  <name>session-delta pure functions</name>
  <files>bin/lib/session-delta.js, test/smoke-session-delta.test.js</files>
  <behavior>
    classifyDelta(oldEvidence, changedFiles):
    - oldEvidence=null/empty -> { functions: Map(0), summary: {skip:0, rescan:0, new:0, knownUnfixed:0}, isFullScan: true }
    - oldEvidence khong co ## Function Checklist -> isFullScan=true
    - PASS + file khong doi -> SKIP (D-04)
    - PASS + file doi -> RE-SCAN (D-02)
    - FLAG + file khong doi -> KNOWN-UNFIXED (D-01)
    - FLAG + file doi -> RE-SCAN (open question 1 — research recommends RE-SCAN)
    - FAIL + file khong doi -> KNOWN-UNFIXED (D-05)
    - FAIL + file doi -> RE-SCAN (open question 2 — research recommends RE-SCAN)
    - SKIP verdict -> giu SKIP (D-06)
    - Map key = `${file}::${name}` compound key (Pitfall 5)
    - Path normalize: strip leading ./, replace \\ thanh / (Pitfall 3)
    - summary counts khop voi functions Map size

    appendAuditHistory(evidenceContent, auditEntry):
    - evidenceContent co ## Audit History -> append dong cuoi table
    - evidenceContent KHONG co ## Audit History -> append section + header + dong moi cuoi file
    - auditEntry format: { date: 'YYYY-MM-DD', commit: '7-char', verdictSummary: 'N PASS, M FLAG, K FAIL', deltaSummary: 'N new, M re-scan, K skip' }
    - Output la string evidence content da cap nhat

    parseAuditHistory(evidenceContent):
    - Co ## Audit History -> tra ve array entries [{ date, commit, verdict, delta }]
    - Khong co section -> tra ve []
    - Separator row va header row khong bi parse nham la data
  </behavior>
  <implementation>
    Tao bin/lib/session-delta.js theo pattern smart-selection.js:
    - 'use strict' + JSDoc
    - Constants: DELTA_STATUS = { SKIP, RESCAN, NEW, KNOWN_UNFIXED }
    - Helper: normalizePath(p) — strip ./, normalize \\
    - Helper: parseFunctionChecklist(content) — regex parse ## Function Checklist table
    - Main: classifyDelta(oldEvidence, changedFiles) — per D-01 den D-09
    - Util: appendAuditHistory(content, entry) — per D-10, D-11
    - Util: parseAuditHistory(content) — per D-12
    - module.exports = { classifyDelta, appendAuditHistory, parseAuditHistory, DELTA_STATUS }
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — Tests truoc, implement session-delta.js sau</name>
  <files>bin/lib/session-delta.js, test/smoke-session-delta.test.js</files>
  <read_first>
    bin/lib/smart-selection.js
    test/smoke-smart-selection.test.js
    bin/lib/evidence-protocol.js
  </read_first>
  <behavior>
    - Test 1: classifyDelta(null, []) -> isFullScan=true, functions.size=0
    - Test 2: classifyDelta('', []) -> isFullScan=true
    - Test 3: classifyDelta('no checklist section', []) -> isFullScan=true
    - Test 4: classifyDelta(evidenceWithChecklist, []) -> PASS=SKIP, FLAG=KNOWN-UNFIXED, FAIL=KNOWN-UNFIXED (no files changed)
    - Test 5: classifyDelta(evidenceWithChecklist, ['src/api/users.js']) -> PASS+changed=RE-SCAN, FLAG+changed=RE-SCAN, FAIL+changed=RE-SCAN
    - Test 6: classifyDelta path normalization — './src/file.js' matches 'src/file.js'
    - Test 7: classifyDelta SKIP verdict -> giu SKIP bat ke file doi hay khong
    - Test 8: classifyDelta compound key — 2 ham trung ten khac file khong collision
    - Test 9: classifyDelta summary counts = functions Map size
    - Test 10: appendAuditHistory khi KHONG co section -> append ## Audit History + header + row
    - Test 11: appendAuditHistory khi DA co section -> append row cuoi table
    - Test 12: parseAuditHistory khi co data -> tra ve array entries dung
    - Test 13: parseAuditHistory khi khong co section -> tra ve []
    - Test 14: DELTA_STATUS export co 4 keys
  </behavior>
  <action>
    RED phase:
    1. Tao test/smoke-session-delta.test.js voi 14 test cases nhu behavior tren
    2. Dung pattern tu test/smoke-smart-selection.test.js: require('node:test'), require('node:assert/strict')
    3. Import tu '../bin/lib/session-delta': classifyDelta, appendAuditHistory, parseAuditHistory, DELTA_STATUS
    4. Fixture evidence string cho tests — YAML frontmatter giua 2 dong "triple-dash", tiep theo body voi Function Checklist table 4 ham:
       Ham 1: src/api/users.js | getUserById | 42 | FLAG | IDOR
       Ham 2: src/api/auth.js | login | 15 | PASS | OK
       Ham 3: src/api/admin.js | deleteUser | 88 | FAIL | No auth check
       Ham 4: src/utils/helper.js | formatDate | 10 | SKIP | Khong lien quan
    5. Chay `node --test test/smoke-session-delta.test.js` — PHAI FAIL (module chua ton tai)

    GREEN phase:
    6. Tao bin/lib/session-delta.js:
       - 'use strict';
       - DELTA_STATUS = { SKIP: 'SKIP', RESCAN: 'RE-SCAN', NEW: 'NEW', KNOWN_UNFIXED: 'KNOWN-UNFIXED' }
       - normalizePath(p): return p.replace(/\\/g, '/').replace(/^\.\//, '')
       - parseFunctionChecklist(content):
         * Match /## Function Checklist\s*\n([\s\S]*?)(?=\n## |\s*$)/
         * Split lines, skip header + separator rows (line.match(/^\s*\|[\s-|]+\|$/))
         * Parse cols: #, File, Ham, Dong, Verdict, Chi tiet
         * Return array [{ file, name, line, verdict, detail }]
       - classifyDelta(oldEvidence, changedFiles):
         * Validate: !oldEvidence || typeof !== 'string' || trim()==='' -> isFullScan=true
         * parseFunctionChecklist -> empty -> isFullScan=true
         * changedSet = new Set(changedFiles.map(normalizePath))
         * For each fn in checklist:
           key = `${fn.file}::${fn.name}`
           fileChanged = changedSet.has(normalizePath(fn.file))
           verdict = fn.verdict.toUpperCase()
           PASS + !changed -> SKIP (D-04)
           PASS + changed -> RE-SCAN (D-02)
           FLAG + !changed -> KNOWN-UNFIXED (D-01)
           FLAG + changed -> RE-SCAN
           FAIL + !changed -> KNOWN-UNFIXED (D-05)
           FAIL + changed -> RE-SCAN
           SKIP -> SKIP (D-06)
         * Return { functions: Map, summary: {skip, rescan, new, knownUnfixed}, isFullScan: false }
       - appendAuditHistory(evidenceContent, auditEntry):
         * auditEntry: { date, commit, verdictSummary, deltaSummary }
         * newRow = `| ${date} | ${commit} | ${verdictSummary} | ${deltaSummary} |`
         * Co ## Audit History -> tim dong cuoi co | trong section, splice row sau do
         * Khong co -> append '\n## Audit History\n\n| Date | Commit | Verdict | Delta |\n|------|--------|---------|-------|\n' + newRow + '\n'
         * Return updated string
       - parseAuditHistory(evidenceContent):
         * Match /## Audit History\s*\n([\s\S]*?)(?=\n## |\s*$)/
         * Split lines, skip header + separator, parse 4 cols: date, commit, verdict, delta
         * Return array [{ date, commit, verdict, delta }]
       - module.exports = { classifyDelta, appendAuditHistory, parseAuditHistory, DELTA_STATUS }
    7. Chay `node --test test/smoke-session-delta.test.js` — PHAI PASS tat ca 14 tests

    REFACTOR (neu can):
    8. Don dep code, them JSDoc day du cho moi function
  </action>
  <verify>
    <automated>node --test test/smoke-session-delta.test.js</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/session-delta.js ton tai va co `module.exports = { classifyDelta, appendAuditHistory, parseAuditHistory, DELTA_STATUS }`
    - test/smoke-session-delta.test.js ton tai va co it nhat 14 test cases (14 it() blocks)
    - `node --test test/smoke-session-delta.test.js` exit code 0
    - session-delta.js KHONG co require('fs') (pure function constraint per D-13)
    - session-delta.js co `const DELTA_STATUS = ` voi 4 keys: SKIP, RESCAN, NEW, KNOWN_UNFIXED
    - session-delta.js co `function classifyDelta(oldEvidence, changedFiles)` signature
    - session-delta.js co `function appendAuditHistory(evidenceContent, auditEntry)` signature
    - session-delta.js co `function parseAuditHistory(evidenceContent)` signature
    - classifyDelta return object co truong isFullScan (boolean)
    - Map key dung compound format — grep 'file.*::.*name' bin/lib/session-delta.js match
  </acceptance_criteria>
  <done>
    14 tests PASS. classifyDelta phan loai dung 7 truong hop theo D-01 den D-06 + D-09. appendAuditHistory tao/append audit history table dung format D-10/D-11. parseAuditHistory doc table cu. Module la pure function khong require('fs').
  </done>
</task>

</tasks>

<verification>
node --test test/smoke-session-delta.test.js
npm test
</verification>

<success_criteria>
- session-delta.js export 4 symbols: classifyDelta, appendAuditHistory, parseAuditHistory, DELTA_STATUS
- 14 tests pass covering DELTA-01 (classification), DELTA-02 (git diff scope), DELTA-03 (audit history)
- Module la pure function — khong require('fs'), khong side effects
</success_criteria>

<output>
After completion, create `.planning/phases/49-session-delta/49-01-SUMMARY.md`
</output>
