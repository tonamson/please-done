---
phase: 86-error-handling-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
  - bin/plan-check.js
  - bin/lib/utils.js
autonomous: true
requirements:
  - ERR-01
  - ERR-02

must_haves:
  truths:
    - "bin/install.js line 396 uses process.env.PD_DEBUG (not DEBUG)"
    - "bin/plan-check.js has no bare catch {} blocks at lines 66 or 76"
    - "bin/plan-check.js catch blocks at lines 66 and 76 log to stderr when PD_DEBUG=1"
    - "bin/lib/utils.js fileHash catch block names the error variable and conditionally logs it"
    - "commandExists and isWSL catch blocks in utils.js remain intentionally silent (no changes)"
  artifacts:
    - path: "bin/install.js"
      provides: "Consistent PD_DEBUG flag (not DEBUG) for stack trace logging"
      contains: "process.env.PD_DEBUG"
    - path: "bin/plan-check.js"
      provides: "Observable debug output on filesystem/JSON failures"
      contains: "process.env.PD_DEBUG"
    - path: "bin/lib/utils.js"
      provides: "Observable debug output on fileHash failures"
      contains: "process.env.PD_DEBUG"
  key_links:
    - from: "bin/plan-check.js catch (err)"
      to: "stderr"
      via: "if (process.env.PD_DEBUG) console.error(...)"
      pattern: "PD_DEBUG.*console\\.error"
    - from: "bin/lib/utils.js fileHash catch (err)"
      to: "stderr"
      via: "if (process.env.PD_DEBUG) console.error(...)"
      pattern: "PD_DEBUG.*console\\.error"
---

<objective>
Fix bare `catch {}` blocks in `bin/plan-check.js` and `bin/lib/utils.js` so they produce observable debug output under `PD_DEBUG=1`. Also align `bin/install.js` to use `PD_DEBUG` instead of `DEBUG` for consistency across all files.

Purpose: ERR-01 and ERR-02 — errors that were previously swallowed silently now surface diagnostic information when the developer sets `PD_DEBUG=1`.
Output: 3 modified files. No behavior change in normal operation. Debug output available under `PD_DEBUG=1`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/86-error-handling-hardening/86-CONTEXT.md
@.planning/phases/86-error-handling-hardening/86-RESEARCH.md

<interfaces>
<!-- Key decisions locked in CONTEXT.md — follow exactly -->

D-01: Use `process.env.PD_DEBUG` (not `process.env.DEBUG`)
D-02: Update bin/install.js:396 from `process.env.DEBUG` → `process.env.PD_DEBUG`
D-03: Only `fileHash` in utils.js gets debug logging; `commandExists` and `isWSL` stay silent (D-03b)

Debug log format pattern (from existing install.js pattern):
```javascript
if (process.env.PD_DEBUG) console.error('<context>', err);
```

<!-- Exact current state of all catch blocks to be changed: -->

<!-- bin/install.js line 394-398 -->
main().catch((err) => {
  log.error(err.message);
  if (process.env.DEBUG) console.error(err.stack);   // ← line 396 — change DEBUG → PD_DEBUG
  process.exit(1);
});

<!-- bin/plan-check.js line 61-66 -->
try {
  hasResearchFiles = (
    (fs.existsSync(researchInternalDir) && ...) ||
    (fs.existsSync(researchExternalDir) && ...)
  );
} catch {}   // ← line 66 — bare, needs catch (err) + PD_DEBUG

<!-- bin/plan-check.js line 72-76 -->
  try {
    const config = JSON.parse(fs.readFileSync(configPath, 'utf8'));
    check06Severity = config?.checks?.research_backing?.severity;
    check07Severity = config?.checks?.hedging_language?.severity;
  } catch {}  // ← line 76 — bare, needs catch (err) + PD_DEBUG

<!-- bin/lib/utils.js line 136-143 (fileHash) -->
function fileHash(filePath) {
  try {
    const content = fs.readFileSync(filePath);
    return crypto.createHash("sha256").update(content).digest("hex");
  } catch {        // ← line 140 — bare, needs (err) + PD_DEBUG
    return null;
  }
}

<!-- DO NOT TOUCH: commandExists (lines 164-172) and isWSL (lines 195-203) -->
<!-- Both are intentionally silent per D-03b — returning false is expected behavior -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update install.js PD_DEBUG flag + fix plan-check.js bare catches (ERR-01, D-02)</name>
  <files>bin/install.js, bin/plan-check.js</files>

  <read_first>
    - bin/install.js (read lines 394-398 to confirm exact current text before editing)
    - bin/plan-check.js (read lines 58-78 to confirm exact current text before editing)
    - .planning/phases/86-error-handling-hardening/86-CONTEXT.md (decisions D-01, D-02)
  </read_first>

  <action>
Make exactly 3 changes across 2 files. No other modifications.

**Change 1 — bin/install.js line 396:**
Find the exact text:
```javascript
  if (process.env.DEBUG) console.error(err.stack);
```
Replace with:
```javascript
  if (process.env.PD_DEBUG) console.error(err.stack);
```
(One-word change: `DEBUG` → `PD_DEBUG`. This is the only change to install.js.)

**Change 2 — bin/plan-check.js line 66:**
Find the exact text (standalone line after the try block):
```javascript
} catch {}
```
Replace with:
```javascript
} catch (err) {
  if (process.env.PD_DEBUG) console.error('[plan-check] research dir read error:', err);
}
```
Context: This is the catch at line 66 that wraps `hasResearchFiles = (...)` at lines 62-65. There is no indentation — it starts at column 0.

**Change 3 — bin/plan-check.js line 76:**
Find the exact text (inside the `if (fs.existsSync(configPath))` block):
```javascript
  } catch {}
```
Replace with:
```javascript
  } catch (err) {
    if (process.env.PD_DEBUG) console.error('[plan-check] config.json parse error:', err);
  }
```
Context: This is the catch at line 76 indented with 2 spaces (inside the if block at line 71).
  </action>

  <verify>
    <automated>grep -n "process.env.DEBUG" bin/install.js | grep -v "PD_DEBUG" && echo "FAIL: old DEBUG found" || echo "OK: no bare DEBUG"; grep -c "catch {}" bin/plan-check.js && echo "FAIL: bare catches remain" || echo "OK: no bare catches"; grep -c "PD_DEBUG" bin/plan-check.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep "process.env.DEBUG" bin/install.js` returns 0 matches (the word DEBUG alone, not PD_DEBUG)
    - `grep "process.env.PD_DEBUG" bin/install.js` returns exactly 1 match on line 396
    - `grep -c "catch {}" bin/plan-check.js` returns 0
    - `grep -c "PD_DEBUG" bin/plan-check.js` returns 2 (one per catch block)
    - `grep "plan-check.*research dir" bin/plan-check.js` returns 1 match
    - `grep "plan-check.*config.json" bin/plan-check.js` returns 1 match
    - `node --test test/smoke-plan-checker.test.js` exits 0 (165/165 pass)
  </acceptance_criteria>

  <done>install.js uses PD_DEBUG consistently; plan-check.js has no bare catches; both catch blocks log to stderr under PD_DEBUG=1.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix utils.js fileHash bare catch (ERR-02)</name>
  <files>bin/lib/utils.js</files>

  <read_first>
    - bin/lib/utils.js (read lines 133-203 to confirm exact current text and to see all three catch blocks before editing)
    - .planning/phases/86-error-handling-hardening/86-CONTEXT.md (decisions D-03, D-03b — only fileHash changes)
  </read_first>

  <action>
Make exactly 1 change to utils.js. The other two catch blocks (commandExists at line 169, isWSL at line 200) MUST NOT be touched — they are intentionally silent per D-03b.

**Change — bin/lib/utils.js lines 140-142 (inside fileHash function):**
Find the exact text:
```javascript
  } catch {
    return null;
  }
```
Replace with:
```javascript
  } catch (err) {
    if (process.env.PD_DEBUG) console.error('[fileHash]', err);
    return null;
  }
```
Context: This is the catch block inside `function fileHash(filePath)` at line 136. The indentation is 2 spaces.

After this change, confirm commandExists (lines 164-172) and isWSL (lines 195-203) still have their original bare `catch {` lines — do NOT modify them.
  </action>

  <verify>
    <automated>grep -n "catch" bin/lib/utils.js; node --test test/smoke-utils.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "\} catch {" bin/lib/utils.js` returns exactly 2 matches (commandExists + isWSL) — NOT 3
    - `grep -n "catch (err)" bin/lib/utils.js` includes a match inside the `fileHash` function (around line 140)
    - `grep "PD_DEBUG" bin/lib/utils.js` returns 1 match containing `[fileHash]`
    - `grep -n "catch {" bin/lib/utils.js` still shows lines for commandExists (~169) and isWSL (~200) — those are unchanged
    - `node --test test/smoke-utils.test.js` exits 0 (36/36 pass — fileHash still returns null on failure)
    - `npm test` exits 0 (full suite — no regressions)
  </acceptance_criteria>

  <done>fileHash catch names the error and logs it under PD_DEBUG=1. commandExists and isWSL remain intentionally silent. Full test suite green.</done>
</task>

</tasks>

<verification>
After both tasks:
```bash
# Confirm PD_DEBUG flag consistent across all 3 files
grep -rn "process.env.PD_DEBUG" bin/install.js bin/plan-check.js bin/lib/utils.js
# Expected: 3 files, 4 total matches (1 in install.js, 2 in plan-check.js, 1 in utils.js)

# Confirm no bare catch {} remains in any of the 3 files
grep -c "catch {}" bin/install.js bin/plan-check.js bin/lib/utils.js
# Expected: all 0

# Full regression check
npm test
```
</verification>

<success_criteria>
- `grep "catch {}" bin/plan-check.js` → 0 matches (ERR-01 ✓)
- `grep "PD_DEBUG" bin/plan-check.js` → 2 matches (ERR-01 ✓)
- `grep "PD_DEBUG" bin/lib/utils.js` → 1 match inside fileHash (ERR-02 ✓)
- `grep "catch {" bin/lib/utils.js` → exactly 2 matches (commandExists + isWSL unchanged) (D-03b ✓)
- `grep "PD_DEBUG" bin/install.js` → 1 match at line 396 (D-02 ✓)
- `npm test` → 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/86-error-handling-hardening/86-01-SUMMARY.md`
</output>
