---
phase: 77-codebase-map-staleness-detection
plan: 02
type: execute
wave: 1
depends_on:
  - 77-03
files_modified:
  - workflows/scan.md
autonomous: true
requirements:
  - STALE-01

must_haves:
  truths:
    - "scan.md contains a Step 0 that checks codebase map freshness"
    - "Step 0 reads .planning/codebase/META.json and counts commits since mapped_at_commit"
    - "When commit count > 20, a warning with the exact count is emitted"
    - "When META.json is missing or git fails, Step 0 skips silently without error"
    - "Step 0 is non-blocking — always continues to Step 1 regardless"
  artifacts:
    - path: "workflows/scan.md"
      provides: "Step 0: codebase map freshness check"
      contains: "Step 0"
  key_links:
    - from: "workflows/scan.md"
      to: ".planning/codebase/META.json"
      via: "Step 0 reads mapped_at_commit"
      pattern: "META\\.json"
    - from: "workflows/scan.md"
      to: "git rev-list"
      via: "commit delta count command"
      pattern: "git rev-list.*\\.\\."
---

<objective>
Insert Step 0 into scan.md to check codebase map freshness before the scan proceeds.

Purpose: This is the "reader" half of STALE-01. When META.json exists (written by Plan 01's mapper change), Step 0 counts commits since the last map and warns if stale. The check is always non-blocking.

Output: Modified `workflows/scan.md` with new Step 0 before existing Step 1.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-codebase-map-staleness-detection/77-RESEARCH.md
@workflows/scan.md

<interfaces>
<!-- Current scan.md lines 13-15 (insertion point): -->
```markdown
<process>

## Step 1: Determine path
```
<!-- Step 0 must be inserted between line 13 (<process>) and line 15 (## Step 1). -->
<!-- The blank line 14 becomes part of the inserted block. -->
<!-- No renumbering needed — existing Steps 1-7 keep their numbers. Step 0 is a new prefix. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Step 0 into workflows/scan.md</name>
  <files>workflows/scan.md</files>
  <action>
Open `workflows/scan.md`.

Find the EXACT text (lines 13-15):
```
<process>

## Step 1: Determine path
```

Replace it with the following (inserts Step 0 between the process tag and Step 1):
```
<process>

## Step 0: Check codebase map freshness (non-blocking)
- Read `.planning/codebase/META.json` → if file does not exist → skip to Step 1.
- Extract the `mapped_at_commit` field → if missing or empty → skip to Step 1.
- Run: `git rev-list <mapped_at_commit>..HEAD --count 2>/dev/null`
  - If command fails (no git, invalid SHA, shallow clone) → skip silently to Step 1.
- Parse output as integer `N`.
- If `N > 20`:
  > ⚠️ **Codebase map is stale** — generated **N commits ago** (where N is the actual count).
  > Run `/pd:scan` to refresh before continuing for accurate results.
- If `N ≤ 20` → no output, continue silently.
- **Always continue to Step 1 regardless of outcome.**

## Step 1: Determine path
```

**Do NOT change anything else in the file.** Steps 1-7, the purpose, conditional_reading, and rules sections remain exactly as they are.

**Key constraints (from research):**
- Non-blocking: ALL code paths end with "continue to Step 1". The staleness check NEVER prevents scan from proceeding.
- Silent skip: When META.json is absent, `mapped_at_commit` is missing, or `git rev-list` fails — produce NO output. Just continue.
- Exact count: The warning must include the actual integer N (e.g., "generated 47 commits ago"), NOT a vague "may be stale" message.
- Stderr suppression: `2>/dev/null` is mandatory on the git command to handle shallow clones and missing SHAs gracefully.
- Prompt `/pd:scan` specifically (not `/pd:init` or other commands).
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && grep -q "Step 0" workflows/scan.md && grep -q "META.json" workflows/scan.md && grep -q "git rev-list" workflows/scan.md && grep -q "pd:scan" workflows/scan.md && grep -q "20" workflows/scan.md && node --test --test-name-pattern="scan.md" test/smoke-codebase-staleness.test.js && echo "PASS"</automated>
  </verify>
  <done>
    - scan.md has Step 0 before Step 1
    - Step 0 reads META.json and extracts mapped_at_commit
    - Step 0 runs `git rev-list <sha>..HEAD --count 2>/dev/null`
    - When N > 20: warning with exact commit count + "/pd:scan" prompt is emitted
    - When N ≤ 20: no output
    - When META.json missing or git error: silent skip, no error
    - Step 0 always continues to Step 1
    - All 5 "scan.md" smoke tests pass (from Plan 03's test file)
  </done>
</task>

</tasks>

<verification>
- `grep "Step 0" workflows/scan.md` — present
- `grep "META.json" workflows/scan.md` — present
- `grep "git rev-list" workflows/scan.md` — present
- `grep "20" workflows/scan.md` — threshold present
- `grep "pd:scan" workflows/scan.md` — prompt present
- `node --test --test-name-pattern="scan.md" test/smoke-codebase-staleness.test.js` — all 5 tests pass
- Steps 1-7 are unchanged (spot-check: `grep "Step 1: Determine path" workflows/scan.md`)
</verification>

<success_criteria>
scan.md contains Step 0 with a non-blocking staleness check. The scan.md smoke tests from test/smoke-codebase-staleness.test.js all pass (GREEN). All existing Steps 1-7 are unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/77-codebase-map-staleness-detection/77-02-SUMMARY.md`
</output>
