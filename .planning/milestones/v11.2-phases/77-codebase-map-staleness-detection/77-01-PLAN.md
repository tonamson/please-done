---
phase: 77-codebase-map-staleness-detection
plan: 01
type: execute
wave: 1
depends_on:
  - 77-03
files_modified:
  - commands/pd/agents/pd-codebase-mapper.md
autonomous: true
requirements:
  - STALE-01

must_haves:
  truths:
    - "pd-codebase-mapper.md contains a Step 6 that writes META.json"
    - "Step 6 runs git rev-parse HEAD and skips if git is unavailable"
    - "META.json contains schema_version, mapped_at_commit, and mapped_at fields"
  artifacts:
    - path: "commands/pd/agents/pd-codebase-mapper.md"
      provides: "Step 6: Write META.json after map files"
      contains: "META.json"
  key_links:
    - from: "commands/pd/agents/pd-codebase-mapper.md"
      to: ".planning/codebase/META.json"
      via: "Step 6 write instruction"
      pattern: "Write.*META\\.json"
    - from: "commands/pd/agents/pd-codebase-mapper.md"
      to: "git rev-parse HEAD"
      via: "SHA capture command"
      pattern: "git rev-parse HEAD"
---

<objective>
Append Step 6 to pd-codebase-mapper.md so every mapper run writes META.json with the current git commit SHA.

Purpose: This is the "writer" half of STALE-01. Without META.json, scan.md (Plan 02) has nothing to read for staleness detection.

Output: Modified `commands/pd/agents/pd-codebase-mapper.md` with new Step 6.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-codebase-map-staleness-detection/77-RESEARCH.md
@commands/pd/agents/pd-codebase-mapper.md

<interfaces>
<!-- Current end of process section in pd-codebase-mapper.md (lines 35-40): -->
```markdown
5. **Write results.** Create files in `.planning/codebase/`:
   - `STRUCTURE.md` — directory tree with annotations
   - `TECH_STACK.md` — detected tech stack
   - `ENTRY_POINTS.md` — main entry points
   - `DEPENDENCIES.md` — internal dependency graph
</process>
```
<!-- Step 6 must be inserted between line 39 (end of Step 5 list) and line 40 (</process> tag). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Step 6 to pd-codebase-mapper.md</name>
  <files>commands/pd/agents/pd-codebase-mapper.md</files>
  <action>
Open `commands/pd/agents/pd-codebase-mapper.md`.

Find the EXACT text (lines 35-40):
```
5. **Write results.** Create files in `.planning/codebase/`:
   - `STRUCTURE.md` — directory tree with annotations
   - `TECH_STACK.md` — detected tech stack
   - `ENTRY_POINTS.md` — main entry points
   - `DEPENDENCIES.md` — internal dependency graph
</process>
```

Replace it with the following (adds Step 6 between Step 5 and the closing tag):
```
5. **Write results.** Create files in `.planning/codebase/`:
   - `STRUCTURE.md` — directory tree with annotations
   - `TECH_STACK.md` — detected tech stack
   - `ENTRY_POINTS.md` — main entry points
   - `DEPENDENCIES.md` — internal dependency graph

6. **Write META.json.** Record the current git commit SHA for staleness detection:
   - Run `git rev-parse HEAD 2>/dev/null` — if the command fails (no git repo), skip this step entirely. Do NOT write META.json without a valid SHA.
   - If the command succeeds, write `.planning/codebase/META.json`:
     ```json
     {
       "schema_version": 1,
       "mapped_at_commit": "<output of git rev-parse HEAD>",
       "mapped_at": "<current ISO-8601 timestamp>"
     }
     ```
   - The `mapped_at_commit` value MUST be the full 40-character SHA hex string.
   - The `mapped_at` value MUST be an ISO-8601 timestamp (e.g., `2026-04-02T10:00:00.000Z`).
</process>
```

**Do NOT change anything else in the file.** Steps 1-5, the frontmatter, objective, and rules sections remain exactly as they are.

**Key constraints (from research):**
- Only write META.json if `git rev-parse HEAD` exits 0 AND returns a non-empty 40-char string
- Never write `{"mapped_at_commit": null}` — that causes downstream parse failures
- The `schema_version: 1` field is mandatory for future-proofing
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && grep -q "META.json" commands/pd/agents/pd-codebase-mapper.md && grep -q "git rev-parse HEAD" commands/pd/agents/pd-codebase-mapper.md && grep -q "mapped_at_commit" commands/pd/agents/pd-codebase-mapper.md && grep -q "schema_version" commands/pd/agents/pd-codebase-mapper.md && node --test --test-name-pattern="mapper" test/smoke-codebase-staleness.test.js && echo "PASS"</automated>
  </verify>
  <done>
    - pd-codebase-mapper.md has 6 steps (was 5)
    - Step 6 instructs agent to run `git rev-parse HEAD` and write META.json
    - META.json format specifies all 3 required fields: schema_version, mapped_at_commit, mapped_at
    - Step 6 explicitly skips when git is unavailable
    - All 4 "mapper" smoke tests pass (from Plan 03's test file)
  </done>
</task>

</tasks>

<verification>
- `grep -c "META.json" commands/pd/agents/pd-codebase-mapper.md` — at least 2 occurrences (step title + write instruction)
- `grep "git rev-parse HEAD" commands/pd/agents/pd-codebase-mapper.md` — present
- `grep "mapped_at_commit" commands/pd/agents/pd-codebase-mapper.md` — present
- `grep "schema_version" commands/pd/agents/pd-codebase-mapper.md` — present
- `node --test --test-name-pattern="mapper" test/smoke-codebase-staleness.test.js` — all 4 tests pass
</verification>

<success_criteria>
pd-codebase-mapper.md contains Step 6 with META.json write instructions. The mapper smoke tests from test/smoke-codebase-staleness.test.js all pass (GREEN).
</success_criteria>

<output>
After completion, create `.planning/phases/77-codebase-map-staleness-detection/77-01-SUMMARY.md`
</output>
