---
phase: 50-poc-fix-phases
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - commands/pd/agents/pd-sec-fixer.md
  - commands/pd/agents/pd-sec-scanner.md
  - commands/pd/agents/pd-sec-reporter.md
  - templates/security-fix-phase.md
  - bin/lib/resource-config.js
  - workflows/audit.md
  - test/smoke-agent-files.test.js
  - test/smoke-resource-config.test.js
autonomous: true
requirements: [POC-01, POC-02, FIX-01, FIX-02, FIX-03]

must_haves:
  truths:
    - "Scanner tao section ## POC trong evidence khi --poc active va finding la FAIL/FLAG (D-01, D-02, D-03)"
    - "Reporter B6 goi detectChains() de phat hien gadget chains va ghi vao SECURITY_REPORT (D-05)"
    - "pd-sec-fixer tao fix phases proposal voi priority order tu orderFixPriority() (D-14, D-15)"
    - "Fix phases decimal N.1, N.2... voi SEC-VERIFY la phase cuoi cung (D-10, D-13)"
    - "Template security-fix-phase.md co 4 sections: Evidence goc, Gadget Chain, Huong sua, Tieu chi hoan thanh (D-11)"
    - "B3 truyen --poc flag xuong scanner thay vi bao 'Chua ho tro' (D-03)"
    - "B8 dispatch pd-sec-fixer thay vi stub (D-14)"
  artifacts:
    - path: "commands/pd/agents/pd-sec-fixer.md"
      provides: "Agent tao fix phases proposal"
      contains: "pd-sec-fixer"
    - path: "templates/security-fix-phase.md"
      provides: "Template cho fix phases decimal"
      contains: "Evidence goc"
    - path: "commands/pd/agents/pd-sec-scanner.md"
      provides: "Scanner voi POC generation khi --poc"
      contains: "## POC"
    - path: "commands/pd/agents/pd-sec-reporter.md"
      provides: "Reporter voi gadget chain detection trong B6"
      contains: "detectChains"
  key_links:
    - from: "commands/pd/agents/pd-sec-reporter.md"
      to: "bin/lib/gadget-chain.js"
      via: "goi detectChains() trong phan tich cheo"
      pattern: "detectChains"
    - from: "commands/pd/agents/pd-sec-fixer.md"
      to: "bin/lib/gadget-chain.js"
      via: "goi orderFixPriority() de sap fix phases"
      pattern: "orderFixPriority"
    - from: "workflows/audit.md"
      to: "commands/pd/agents/pd-sec-fixer.md"
      via: "B8 dispatch pd-sec-fixer"
      pattern: "pd-sec-fixer"
---

<objective>
Tao pd-sec-fixer agent, security-fix-phase template, wire POC vao scanner, wire gadget chain vao reporter B6, wire fixer vao B8 cua audit workflow. Dong thoi dang ky pd-sec-fixer trong AGENT_REGISTRY va mo rong tests.

Purpose: Hoan thanh pipeline POC + fix phases: scanner tao POC (POC-01), reporter phat hien gadget chain (POC-02 wire), fixer tao fix phases proposal (FIX-01, FIX-02), SEC-VERIFY pattern (FIX-03).
Output: 3 agent files updated/created, 1 template, workflow wired, tests extended
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-poc-fix-phases/50-CONTEXT.md
@.planning/phases/50-poc-fix-phases/50-RESEARCH.md
@.planning/phases/50-poc-fix-phases/50-01-SUMMARY.md

<interfaces>
<!-- Interfaces tu Plan 01 -->
From bin/lib/gadget-chain.js (Plan 01 output):
```javascript
const SEVERITY_ORDER = ['LOW', 'MEDIUM', 'HIGH', 'CRITICAL'];
function detectChains(findings, templates) // returns { chains: Array<{id, name, root, findings, originalSeverities, escalatedSeverity}>, linkedFindingKeys: string[] }
function escalateSeverity(severities) // returns string
function orderFixPriority(chains) // returns Array<{...chain, fixPhases: [{priority, category, findings}]}>
module.exports = { detectChains, escalateSeverity, orderFixPriority, SEVERITY_ORDER };
```

From bin/lib/session-delta.js:
```javascript
function parseFunctionChecklist(content) // returns Array<{file, name, line, verdict, detail}>
function classifyDelta(oldEvidence, changedFiles) // returns {functions: Map, summary, isFullScan}
module.exports = { classifyDelta, appendAuditHistory, parseAuditHistory, parseFunctionChecklist, DELTA_STATUS };
```

From bin/lib/resource-config.js:
```javascript
const AGENT_REGISTRY = { /* 9 entries hien tai */ };
function getAgentConfig(agentName) // returns { name, tier, model, effort, maxTurns, tools, ...extra }
```

From references/gadget-chain-templates.yaml (Plan 01 output):
```yaml
chains:
  - id: sqli-data-leak
    name: "SQL Injection -> Data Leak"
    links: [{from_cat: sql-injection, to_cat: secrets}]
    root: sql-injection
    escalation: "+1"
  # ... 6 templates nua
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tao pd-sec-fixer agent + security-fix-phase template + dang ky AGENT_REGISTRY</name>
  <files>commands/pd/agents/pd-sec-fixer.md, templates/security-fix-phase.md, bin/lib/resource-config.js, test/smoke-resource-config.test.js, test/smoke-agent-files.test.js</files>
  <read_first>commands/pd/agents/pd-sec-reporter.md, commands/pd/agents/pd-fix-architect.md, bin/lib/resource-config.js, test/smoke-resource-config.test.js, test/smoke-agent-files.test.js, templates/verification-report.md</read_first>
  <action>
**1a. Tao pd-sec-fixer.md** tai `commands/pd/agents/pd-sec-fixer.md`.

File nay theo dung format agent template cua du an (giong pd-sec-reporter.md). YAML frontmatter co 4 truong:
- name: pd-sec-fixer
- description: Phan tich findings bao mat va tao de xuat fix phases decimal theo thu tu uu tien.
- tier: architect
- allowed-tools: Read, Write, Glob, Grep

Sau frontmatter la 3 sections XML:

**`<objective>`**: Phan tich SECURITY_REPORT.md va evidence files de tao danh sach fix phases decimal (N.1, N.2...) sap theo gadget chain nguoc. Mode tich hop: hoi user approve roi ghi vao ROADMAP. Mode doc lap: chi in goi y.

**`<process>`** gom 9 buoc:
1. Doc SECURITY_REPORT.md tu session dir. Parse master table (findings theo severity), Gadget Chains section (neu co), Remediation priorities (P0, P1, P2).
2. Doc evidence files tu session dir bang Glob `{session_dir}/03-dispatch/evidence_sec_*.md`. Parse Function Checklist tu moi evidence file. Thu thap findings FAIL/FLAG voi file, function, line, severity, category.
3. Doc gadget chain templates tu `references/gadget-chain-templates.yaml`.
4. Goi detectChains va orderFixPriority bang Bash node -e: `const {detectChains, orderFixPriority}=require('./bin/lib/gadget-chain'); const result=detectChains(findings, templates); const ordered=orderFixPriority(result.chains);`
5. Tao fix phases proposal. Voi moi chain (sap theo orderFixPriority): Root category = fix phase dau tien (P0). Cac link categories = fix phases tiep theo (P1, P2...). Findings khong thuoc chain nao = nhom theo severity (CRITICAL truoc).
6. Them SEC-VERIFY la fix phase cuoi cung. SEC-VERIFY dung classifyDelta() chi scan lai files da fix.
7. Xac dinh mode va output: Mode "tich-hop" (co .planning/) hien thi proposal, hoi "Tao fix phases vao ROADMAP? [y/N]", neu approve thi ghi ROADMAP.md. Mode "doc-lap" (khong co .planning/) ghi proposal vao {session_dir}/fix-phases-proposal.md.
8. Tao fix phase files (mode tich hop, khi user approve): voi moi fix phase tao file tu template `templates/security-fix-phase.md`, dien thong tin evidence goc, gadget chain, huong sua tu security-rules.yaml fixes[], tieu chi hoan thanh.
9. Ghi ket qua vao `{session_dir}/05-fix-routing.md` voi status, chains_detected, fix_phases_proposed, user_approved.

**`<rules>`** gom 5 rules:
- Moi output PHAI bang tieng Viet co dau
- KHONG tu y sua ROADMAP — chi sua khi user approve (per D-08)
- Fix phases decimal numbering: doc ROADMAP.md tim existing N.X de tranh collision (per Pitfall 4)
- SEC-VERIFY la fix phase cuoi cung (per D-13)
- Mode doc lap: chi in goi y, KHONG ghi ROADMAP

**1b. Tao security-fix-phase.md** tai `templates/security-fix-phase.md`.

Template co 5 sections:

```
# Fix Phase {PHASE_NUMBER}: {VULNERABILITY_NAME}

## Evidence goc
(table: Finding | File | Dong | Severity | Category)
(blockquote: Trich dan tu evidence file)

## Gadget Chain (neu co)
Chain: {CHAIN_NAME}
Vi tri trong chain: {CHAIN_POSITION}
Fix finding nay se: {CHAIN_IMPACT}

## Huong sua
{FIX_DESCRIPTION}
### Code pattern truoc (nguy hiem) — code block
### Code pattern sau (an toan) — code block

## Files can sua
(table: # | File | Ham | Dong | Hanh dong)

## Tieu chi hoan thanh
- [ ] {CRITERION_1}
- [ ] {CRITERION_2}
- [ ] Chay scanner category {CATEGORY} tren files da sua -> verdict PASS
```

**1c. Dang ky pd-sec-fixer trong AGENT_REGISTRY** tai `bin/lib/resource-config.js`:

Them entry moi vao AGENT_REGISTRY object, SAU entry `pd-sec-reporter`:

```javascript
  "pd-sec-fixer": {
    tier: "architect",
    tools: ["Read", "Write", "Glob", "Grep"],
  },
```

**1d. Mo rong test/smoke-resource-config.test.js:**

Them test case kiem tra pd-sec-fixer registration. Tim describe block `getAgentConfig` hoac cuoi file, them:

```javascript
it('pd-sec-fixer co tier architect va 4 tools', () => {
  const config = getAgentConfig('pd-sec-fixer');
  assert.equal(config.tier, 'architect');
  assert.equal(config.model, 'opus');
  assert.equal(config.tools.length, 4);
  assert.ok(config.tools.includes('Read'));
  assert.ok(config.tools.includes('Write'));
  assert.ok(config.tools.includes('Glob'));
  assert.ok(config.tools.includes('Grep'));
});
```

**1e. Mo rong test/smoke-agent-files.test.js:**

Them test rieng cho commands/pd/agents/ path va templates:

```javascript
it('pd-sec-fixer.md ton tai tai commands/pd/agents/', () => {
  const path = join(__dirname, '..', 'commands', 'pd', 'agents', 'pd-sec-fixer.md');
  const content = readFileSync(path, 'utf8');
  assert.ok(content.includes('name: pd-sec-fixer'));
  assert.ok(content.includes('tier: architect'));
});

it('security-fix-phase.md template ton tai va co 4 sections', () => {
  const path = join(__dirname, '..', 'templates', 'security-fix-phase.md');
  const content = readFileSync(path, 'utf8');
  assert.ok(content.includes('## Evidence goc'));
  assert.ok(content.includes('## Gadget Chain'));
  assert.ok(content.includes('## Huong sua'));
  assert.ok(content.includes('## Tieu chi hoan thanh'));
});
```
  </action>
  <verify>
    <automated>node --test test/smoke-resource-config.test.js && node --test test/smoke-agent-files.test.js</automated>
  </verify>
  <acceptance_criteria>
    - commands/pd/agents/pd-sec-fixer.md ton tai, chua `name: pd-sec-fixer` va `tier: architect`
    - templates/security-fix-phase.md ton tai, chua 4 headings: `## Evidence goc`, `## Gadget Chain`, `## Huong sua`, `## Tieu chi hoan thanh`
    - bin/lib/resource-config.js AGENT_REGISTRY co key `pd-sec-fixer` voi tier `architect` va 4 tools
    - node --test test/smoke-resource-config.test.js exit 0
    - node --test test/smoke-agent-files.test.js exit 0
  </acceptance_criteria>
  <done>pd-sec-fixer agent + template + AGENT_REGISTRY dang ky + tests mo rong, tat ca pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire POC vao scanner + gadget chain vao reporter B6 + fixer vao B8</name>
  <files>commands/pd/agents/pd-sec-scanner.md, commands/pd/agents/pd-sec-reporter.md, workflows/audit.md</files>
  <read_first>commands/pd/agents/pd-sec-scanner.md, commands/pd/agents/pd-sec-reporter.md, workflows/audit.md, references/gadget-chain-templates.yaml, bin/lib/gadget-chain.js</read_first>
  <action>
**2a. Wire POC vao pd-sec-scanner.md (per D-01, D-02, D-03, D-04):**

Mo `commands/pd/agents/pd-sec-scanner.md`. Them buoc 10 sau buoc 9 hien tai ("Ghi evidence file"), van nam trong `<process>` tag:

Noi dung buoc 10:

```
10. **Tao POC (neu --poc active).** Kiem tra trong prompt context co `--poc` khong.
   - Neu KHONG co --poc: bo qua buoc nay, KHONG tao POC section
   - Neu CO --poc: voi MOI finding co verdict FAIL hoac FLAG, them section ## POC vao evidence file DA ghi o buoc 9.

   Format POC per finding (append vao cuoi evidence file):

   ## POC

   ### POC-1: {ten finding — vi du: Raw SQL noi chuoi trong getUserById}
   **Input vector:** {mo ta diem vao — endpoint, parameter, header cu the}
   **Payload mau:** `{payload cu the — vi du: ' OR 1=1 --}`
   **Buoc tai hien:**
   1. {buoc 1 — vi du: Gui GET /api/users?id=' OR 1=1 --}
   2. {buoc 2 — vi du: Quan sat response tra ve tat ca users}
   3. {buoc 3 — vi du: Xac nhan SQL injection thanh cong}
   **Ket qua du kien:** {mo ta hanh vi nguy hiem — vi du: Tra ve toan bo du lieu users table}
   **Severity:** {severity cua finding}

   Luu y:
   - POC chi mo ta bang text, KHONG tao script chay that (per D-04)
   - Payload mau dua tren category patterns tu security-rules.yaml
   - Moi finding FAIL/FLAG co 1 POC entry rieng
```

**2b. Wire gadget chain vao pd-sec-reporter.md (per D-05, D-06):**

Mo `commands/pd/agents/pd-sec-reporter.md`. Tim buoc 5 "Phan tich cheo (cross-analysis)". THAY THE noi dung buoc 5 bang phien ban nang cap co gadget chain detection. Giu lai logic cu (hot spot, refactor) va THEM gadget chain:

Noi dung buoc 5 moi:

```
5. **Phan tich cheo va Gadget Chain detection (per D-05, D-06):**

   a. Thu thap tat ca findings FAIL/FLAG tu cac evidence files da parse o buoc 3.5:
      - Moi finding can co: category, file, name (ten ham), verdict, severity
      - Chi lay FAIL va FLAG (bo qua PASS va SKIP)

   b. Doc gadget chain templates:
      node -e "const yaml=require('fs').readFileSync('references/gadget-chain-templates.yaml','utf8'); console.log(yaml);"

   c. Goi detectChains():
      node -e "const {detectChains}=require('./bin/lib/gadget-chain'); const findings=$FINDINGS_JSON; const templates=$TEMPLATES_JSON; const result=detectChains(findings, templates); console.log(JSON.stringify(result));"

   d. Voi moi chain phat hien, ghi vao section ## Gadget Chains trong SECURITY_REPORT.md:

      ## Gadget Chains

      | # | Chain | Root | Severity | Findings |
      |---|-------|------|----------|----------|
      | 1 | {chain.name} | {chain.root} | {chain.escalatedSeverity} | {chain.findings.length} |

      Ghi chi tiet tung chain: findings lien ket, severity escalation.

   e. Van giu phan tich cheo cu (hot spot, refactor khuyen nghi) — them gadget chain vao, khong xoa logic cu:
      - Cung 1 endpoint bi nhieu loai tan cong -> danh dau "hot spot"
      - Input validation thieu o 1 cho -> kiem tra cac cho khac dung cung input
      - Attack chain analysis: bo sung bang gadget chain detection tu buoc c
```

**2c. Wire --poc va B8 fixer vao audit.md:**

Mo `workflows/audit.md`. Thuc hien 3 thay doi:

**(i) B3 — Bo stub --poc, truyen flag xuong scanner:**

Tim dong: `4. **--poc** — parse nhung bao "Chua ho tro trong phien ban nay" (per D-04)`
THAY bang: `4. **--poc** — parse va luu poc_enabled=true. Flag nay se duoc truyen xuong B5 dispatch de scanner tao section ## POC trong evidence.`

Tim dong trong `<rules>` section: `- Khi --poc hoac --auto-fix duoc truyen: thong bao "Chua ho tro trong phien ban nay" va tiep tuc`
THAY bang 2 dong:
`- Khi --poc duoc truyen: truyen flag --poc cho scanner trong B5 dispatch prompt`
`- Khi --auto-fix duoc truyen: thong bao "Chua ho tro trong phien ban nay" va tiep tuc`

**(ii) B5 — Truyen --poc cho scanner:**

Tim prompt cho scanner trong B5 dong co: `Quet bao mat category {slug} tai path {scanPath}. Session dir: {session_dir}/03-dispatch/. Ghi evidence file vao session dir.`
THEM dieu kien poc: `{neu poc_enabled: ' --poc: Tao section ## POC cho moi finding FAIL/FLAG.'}`

**(iii) B8 — Thay TOAN BO Buoc 8 hien tai (tu "## Buoc 8: Fix routing (STUB)" den het "Ghi {session_dir}/05-fix-routing.md voi: status=stub, note") bang noi dung sau:**

```
## Buoc 8: Fix routing

Spawn pd-sec-fixer agent de phan tich findings va tao de xuat fix phases.

1. Lay agent config:
   node -e "const {getAgentConfig}=require('./bin/lib/resource-config');console.log(JSON.stringify(getAgentConfig('pd-sec-fixer')))"

2. Spawn pd-sec-fixer agent:
   - Input: session_dir (chua SECURITY_REPORT.md va evidence files)
   - Prompt: "Phan tich SECURITY_REPORT.md va evidence files trong {session_dir}. Tao de xuat fix phases. Mode: {mode}. Output dir: {output_dir}. Audit phase number: {current_phase_number}."
   - Agent doc SECURITY_REPORT.md + evidence files + gadget-chain-templates.yaml
   - Agent goi detectChains() va orderFixPriority() de sap thu tu fix

3. Agent output:
   - Mode "tich-hop": hien thi proposal, hoi user approve, neu approve thi ghi vao ROADMAP.md
   - Mode "doc-lap": ghi proposal vao {session_dir}/fix-phases-proposal.md

4. Ghi {session_dir}/05-fix-routing.md voi:
   - status: completed
   - mode: {mode}
   - chains_detected: {so chain phat hien}
   - fix_phases_proposed: {so fix phases de xuat}
   - user_approved: {true/false} (chi mode tich hop)

5. Neu pd-sec-fixer fail: ghi {session_dir}/05-fix-routing.md voi status=error, tiep tuc B9 (khong chan audit)
```
  </action>
  <verify>
    <automated>grep -c "## POC" commands/pd/agents/pd-sec-scanner.md && grep -c "detectChains" commands/pd/agents/pd-sec-reporter.md && grep -c "pd-sec-fixer" workflows/audit.md && grep -c "poc_enabled" workflows/audit.md</automated>
  </verify>
  <acceptance_criteria>
    - commands/pd/agents/pd-sec-scanner.md chua `## POC` va `--poc` handling logic (buoc 10 moi)
    - commands/pd/agents/pd-sec-scanner.md co cac truong POC: `Input vector`, `Payload mau`, `Buoc tai hien`, `Ket qua du kien`
    - commands/pd/agents/pd-sec-reporter.md chua `detectChains` va `gadget-chain-templates.yaml` reference
    - commands/pd/agents/pd-sec-reporter.md co format `## Gadget Chains` trong buoc 5
    - workflows/audit.md B3 KHONG con chuoi "Chua ho tro" cho --poc (chi con cho --auto-fix)
    - workflows/audit.md B3 co `poc_enabled=true`
    - workflows/audit.md B5 prompt co `--poc` conditional
    - workflows/audit.md B8 co `pd-sec-fixer` thay vi "STUB"
    - workflows/audit.md B8 co `getAgentConfig('pd-sec-fixer')`
    - grep "Chua ho tro" workflows/audit.md chi tra ve 1 ket qua (cho --auto-fix), KHONG con cho --poc
    - `node --test 'test/*.test.js'` van green (khong break gi)
  </acceptance_criteria>
  <done>POC wired vao scanner, gadget chain wired vao reporter, pd-sec-fixer wired vao B8, --poc flag truyen tu B3->B5->scanner, tat ca prose files updated</done>
</task>

</tasks>

<verification>
1. `node --test test/smoke-resource-config.test.js` — pd-sec-fixer registered
2. `node --test test/smoke-agent-files.test.js` — pd-sec-fixer.md + security-fix-phase.md ton tai
3. `node --test test/smoke-gadget-chain.test.js` — gadget chain logic (tu Plan 01)
4. `node --test 'test/*.test.js'` — full suite green
5. `grep "pd-sec-fixer" bin/lib/resource-config.js` — registered
6. `grep "## POC" commands/pd/agents/pd-sec-scanner.md` — POC section
7. `grep "detectChains" commands/pd/agents/pd-sec-reporter.md` — gadget chain wired
8. `grep "pd-sec-fixer" workflows/audit.md` — B8 wired
9. `grep "Chua ho tro" workflows/audit.md` — chi 1 ket qua (--auto-fix)
</verification>

<success_criteria>
- pd-sec-fixer.md agent moi voi tier architect, 4 tools (Read, Write, Glob, Grep)
- security-fix-phase.md template co 4 sections bat buoc (per D-11)
- Scanner tao ## POC chi khi --poc active (per D-01, D-03)
- POC format: 4 truong text (input vector, payload, buoc tai hien, ket qua) — KHONG script (per D-04)
- Reporter B6 goi detectChains() va ghi Gadget Chains section (per D-05, D-06)
- Severity escalation max+1 cap CRITICAL trong gadget chain (per D-07)
- B8 dispatch pd-sec-fixer thay vi stub (per D-14)
- pd-sec-fixer hoi user approve truoc khi sua ROADMAP (per D-08)
- Fix phases decimal N.1, N.2... voi SEC-VERIFY cuoi (per D-10, D-12, D-13)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/50-poc-fix-phases/50-02-SUMMARY.md`
</output>
