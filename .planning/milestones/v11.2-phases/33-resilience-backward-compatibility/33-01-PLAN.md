---
phase: 33-resilience-backward-compatibility
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/outcome-router.js
  - test/smoke-outcome-router.test.js
autonomous: true
requirements:
  - FLOW-06

must_haves:
  truths:
    - "buildInconclusiveContext() trich xuat Elimination Log tu evidence content va tao prompt cho Buoc 2"
    - "canContinue = true khi round <= 3, false khi round > 3"
    - "Warning khi evidence thieu Elimination Log section"
    - "MAX_INCONCLUSIVE_ROUNDS export bang 3"
  artifacts:
    - path: "bin/lib/outcome-router.js"
      provides: "buildInconclusiveContext(), MAX_INCONCLUSIVE_ROUNDS"
      exports: ["buildInconclusiveContext", "MAX_INCONCLUSIVE_ROUNDS"]
    - path: "test/smoke-outcome-router.test.js"
      provides: "Unit tests cho buildInconclusiveContext"
      contains: "buildInconclusiveContext"
  key_links:
    - from: "bin/lib/outcome-router.js"
      to: "bin/lib/evidence-protocol.js"
      via: "parseEvidence(evidenceContent)"
      pattern: "parseEvidence\\(evidenceContent\\)"
---

<objective>
Them buildInconclusiveContext() vao outcome-router.js va unit tests — pure function trich xuat Elimination Log tu evidence_architect.md, tao prompt cho Buoc 2 agents khi INCONCLUSIVE loop-back.

Purpose: FLOW-06 can pure function de tao context cho loop-back. Function nay la building block cho Plan 02 khi sua fix-bug.md workflow.
Output: outcome-router.js co them buildInconclusiveContext() + MAX_INCONCLUSIVE_ROUNDS, tests pass.
</objective>

<execution_context>
@workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-resilience-backward-compatibility/33-CONTEXT.md
@.planning/phases/33-resilience-backward-compatibility/33-RESEARCH.md

<interfaces>
<!-- outcome-router.js existing exports -->
From bin/lib/outcome-router.js:
```javascript
module.exports = {
  buildRootCauseMenu,
  prepareFixNow,
  prepareFixPlan,
  prepareSelfFix,
  ROOT_CAUSE_CHOICES,
};
```

From bin/lib/evidence-protocol.js:
```javascript
// parseEvidence(content) -> { frontmatter, body, sections, outcome, agent, session }
// sections la object voi key = heading text, value = content sau heading
// VD: sections['Elimination Log'] tra ve content cua ## Elimination Log
```

From bin/lib/checkpoint-handler.js (PATTERN REFERENCE):
```javascript
const MAX_CONTINUATION_ROUNDS = 2;

function buildContinuationContext({ evidencePath, userAnswer, sessionDir, currentRound, agentName }) {
  const warnings = [];
  const canContinue = currentRound <= MAX_CONTINUATION_ROUNDS;
  // ...
  return { prompt, agentName, round: currentRound, canContinue, warnings };
}
```

From test/smoke-outcome-router.test.js (existing helper):
```javascript
function makeEvidence({ agent = 'pd-fix-architect', outcome = 'root_cause', session = 'S001', body = '' } = {}) {
  return `---\nagent: ${agent}\noutcome: ${outcome}\ntimestamp: 2026-03-24T10:00:00+07:00\nsession: ${session}\n---\n${body}`;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD buildInconclusiveContext() — RED tests truoc, GREEN implementation sau</name>
  <files>bin/lib/outcome-router.js, test/smoke-outcome-router.test.js</files>
  <read_first>
    - bin/lib/outcome-router.js
    - test/smoke-outcome-router.test.js
    - bin/lib/checkpoint-handler.js
    - bin/lib/evidence-protocol.js
  </read_first>
  <behavior>
    - Test 1: buildInconclusiveContext voi round=1, co Elimination Log -> canContinue=true, prompt chua "Vong 1/3", eliminationLog chua noi dung table
    - Test 2: buildInconclusiveContext voi round=3 -> canContinue=true (vi <= 3)
    - Test 3: buildInconclusiveContext voi round=4 -> canContinue=false, warnings co "Da vuot qua 3 vong"
    - Test 4: prompt chua Elimination Log content va vong hien tai
    - Test 5: prompt chua userInputPath khi co, khong chua khi null
    - Test 6: warning khi evidence thieu section "Elimination Log"
    - Test 7: MAX_INCONCLUSIVE_ROUNDS export bang 3
  </behavior>
  <action>
    **RED phase:**
    1. Mo test/smoke-outcome-router.test.js
    2. Them import `buildInconclusiveContext, MAX_INCONCLUSIVE_ROUNDS` vao destructure tu require('../bin/lib/outcome-router')
    3. Them constant BODY_INCONCLUSIVE:
    ```javascript
    const BODY_INCONCLUSIVE = `## INVESTIGATION INCONCLUSIVE\n\n## Elimination Log\n| File | Logic | Ket qua |\n|------|-------|--------|\n| src/api.js | null check | Binh thuong |\n| src/db.js | query logic | Binh thuong |\n\n## Huong dieu tra tiep\nCan kiem tra them middleware layer.`;
    ```
    4. Them describe('MAX_INCONCLUSIVE_ROUNDS') voi 1 test: assert.equal(MAX_INCONCLUSIVE_ROUNDS, 3)
    5. Them describe('buildInconclusiveContext') voi 6 tests theo behavior tren
    6. Chay tests -> PHAI fail vi function chua ton tai

    **GREEN phase:**
    7. Mo bin/lib/outcome-router.js
    8. Them constant: `const MAX_INCONCLUSIVE_ROUNDS = 3;`
    9. Them function buildInconclusiveContext (per D-01):
    ```javascript
    function buildInconclusiveContext({ evidenceContent, userInputPath, sessionDir, currentRound }) {
      const warnings = [];
      const canContinue = currentRound <= MAX_INCONCLUSIVE_ROUNDS;

      if (!canContinue) {
        warnings.push(`Da vuot qua ${MAX_INCONCLUSIVE_ROUNDS} vong dieu tra — can nguoi xem xet`);
      }

      const parsed = parseEvidence(evidenceContent);
      const eliminationLog = parsed.sections['Elimination Log'] || '';

      if (!eliminationLog) {
        warnings.push('Evidence thieu Elimination Log section');
      }

      const promptParts = [
        `INCONCLUSIVE LOOP-BACK — Vong ${currentRound}/${MAX_INCONCLUSIVE_ROUNDS}`,
        `Session dir: ${sessionDir}`,
        `Elimination Log tu vong truoc:\n${eliminationLog}`,
      ];

      if (userInputPath) {
        promptParts.push(`Thong tin bo sung tu user: ${userInputPath}`);
      }

      const prompt = promptParts.join('\n');

      return { prompt, eliminationLog, round: currentRound, canContinue, warnings };
    }
    ```
    10. Them vao module.exports: `buildInconclusiveContext, MAX_INCONCLUSIVE_ROUNDS`
    11. Chay tests -> PHAI pass

    Pattern: Theo sat buildContinuationContext() cua checkpoint-handler.js. Pure function — KHONG import fs. Content truyen qua tham so, return structured object voi warnings array.
  </action>
  <verify>
    <automated>node --test test/smoke-outcome-router.test.js</automated>
  </verify>
  <acceptance_criteria>
    - outcome-router.js chua `const MAX_INCONCLUSIVE_ROUNDS = 3`
    - outcome-router.js chua `function buildInconclusiveContext(`
    - outcome-router.js exports chua `buildInconclusiveContext` va `MAX_INCONCLUSIVE_ROUNDS`
    - smoke-outcome-router.test.js chua `describe('buildInconclusiveContext'`
    - smoke-outcome-router.test.js chua `describe('MAX_INCONCLUSIVE_ROUNDS'`
    - `node --test test/smoke-outcome-router.test.js` exit 0
  </acceptance_criteria>
  <done>buildInconclusiveContext() va MAX_INCONCLUSIVE_ROUNDS export tu outcome-router.js, 7+ tests moi pass trong smoke-outcome-router.test.js</done>
</task>

</tasks>

<verification>
node --test test/smoke-outcome-router.test.js
</verification>

<success_criteria>
- buildInconclusiveContext() la pure function trong outcome-router.js
- MAX_INCONCLUSIVE_ROUNDS = 3 export tu outcome-router.js
- 7+ unit tests moi pass
- Tat ca tests cu cua outcome-router van pass
</success_criteria>

<output>
After completion, create `.planning/phases/33-resilience-backward-compatibility/33-01-SUMMARY.md`
</output>
