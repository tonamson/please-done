---
phase: 35-fix-evidence-encoding-critical-wiring
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - bin/lib/outcome-router.js
  - workflows/fix-bug.md
  - test/smoke-outcome-router.test.js
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PROT-03, PROT-04, PROT-06, MEM-04, ORCH-03, FLOW-03, FLOW-05]
gap_closure: true

must_haves:
  truths:
    - "buildContinuationContext nhan currentRound=1 lan dau -> canContinue=true"
    - "prepareFixPlan tra ve planPath chua session_dir (khong phai bare FIX-PLAN.md)"
    - "createBugRecord nhan existingBugs tu Glob -> ID tang dung (khong luon BUG-001)"
    - "SESSION.md duoc cap nhat day du tai isHeavyAgent warning va Repro FAIL"
    - "REQUIREMENTS.md checkboxes ORCH-01/02 va MEM-01..04 duoc danh [x]"
  artifacts:
    - path: "bin/lib/outcome-router.js"
      provides: "prepareFixPlan tra ve full path"
      contains: "${sessionDir}/FIX-PLAN.md"
    - path: "workflows/fix-bug.md"
      provides: "roundNumber init, createBugRecord fix, write-back pattern"
      contains: "roundNumber = 1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated checkboxes"
      contains: "[x] **ORCH-01**"
  key_links:
    - from: "workflows/fix-bug.md"
      to: "bin/lib/checkpoint-handler.js"
      via: "currentRound: roundNumber"
      pattern: "roundNumber = 1"
    - from: "workflows/fix-bug.md"
      to: "bin/lib/bug-memory.js"
      via: "existingBugs param"
      pattern: "existingBugs"
    - from: "bin/lib/outcome-router.js"
      to: "sessionDir"
      via: "planPath template literal"
      pattern: "sessionDir.*FIX-PLAN"
---

<objective>
Fix 4 wiring bugs con lai trong workflow va modules: roundNumber undefined (INT-02), FIX-PLAN.md path sai (INT-03), createBugRecord thieu existingBugs (INT-04), SESSION.md write-back thieu (INT-05), va cap nhat REQUIREMENTS.md (D-07).

Purpose: 4 integration issues con lai sau khi INT-01 encoding da fix o Plan 01. Toan bo la wiring bugs — code logic dung nhung module goi sai params hoac thieu calls.
Output: Workflow va module wiring chinh xac, documentation cap nhat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-fix-evidence-encoding-critical-wiring/35-CONTEXT.md
@.planning/phases/35-fix-evidence-encoding-critical-wiring/35-RESEARCH.md
@.planning/phases/35-fix-evidence-encoding-critical-wiring/35-01-SUMMARY.md

<interfaces>
<!-- outcome-router.js prepareFixPlan signature -->
```javascript
function prepareFixPlan(evidenceContent, sessionDir) {
  // ...
  return { action: 'fix_plan', planContent, planPath: 'FIX-PLAN.md', warnings: [] };
}
```

<!-- checkpoint-handler.js buildContinuationContext signature -->
```javascript
function buildContinuationContext({ evidencePath, userAnswer, sessionDir, currentRound, agentName }) {
  const canContinue = currentRound <= MAX_CONTINUATION_ROUNDS; // MAX = 2
  // ...
}
```

<!-- bug-memory.js createBugRecord signature -->
```javascript
function createBugRecord({ existingBugs = [], file, functionName, errorMessage, sessionId, rootCause, fix } = {}) {
  // existingBugs: Array<{number: number}> — dung de tinh max+1
  return { id, fileName, content, number };
}
```

<!-- session-manager.js updateSession signature -->
```javascript
function updateSession(currentMd, options) {
  // options: { status?, appendToBody? }
  return updatedMd; // string
}
```

<!-- fix-bug.md relevant sections -->
- Buoc 2 dong 107-108: isHeavyAgent warning — thieu full Read->updateSession->Write pattern
- Buoc 3 dong 189-191: Repro FAIL — thieu full Read->updateSession->Write pattern
- Buoc 4 dong 233: buildContinuationContext({ currentRound: roundNumber }) — roundNumber undefined
- Buoc 4 dong 217-218: planPath ghi — dung bare planPath thay vi session_dir + planPath
- Buoc 5 dong 323-326: createBugRecord goi — thieu existingBugs, dung bugRecordMd thay vi result.content
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix prepareFixPlan planPath va cap nhat test (INT-03)</name>
  <files>bin/lib/outcome-router.js, test/smoke-outcome-router.test.js</files>
  <read_first>bin/lib/outcome-router.js, test/smoke-outcome-router.test.js</read_first>
  <action>
  **outcome-router.js — prepareFixPlan function:**
  Dong 125, doi:
  ```javascript
  planPath: 'FIX-PLAN.md',
  ```
  thanh:
  ```javascript
  planPath: `${sessionDir}/FIX-PLAN.md`,
  ```

  **smoke-outcome-router.test.js — prepareFixPlan test:**
  Dong 93, doi:
  ```javascript
  assert.equal(result.planPath, 'FIX-PLAN.md');
  ```
  thanh:
  ```javascript
  assert.ok(result.planPath.includes('/S001-test/FIX-PLAN.md'));
  ```
  (Vi test goi `prepareFixPlan(evidence, '/tmp/S001-test')` tai dong 89)

  Chay test de verify: `node --test test/smoke-outcome-router.test.js`
  </action>
  <verify>
    <automated>cd /home/please-done && node --test test/smoke-outcome-router.test.js</automated>
  </verify>
  <acceptance_criteria>
  - outcome-router.js dong prepareFixPlan return chua `${sessionDir}/FIX-PLAN.md` (template literal)
  - outcome-router.js KHONG chua `planPath: 'FIX-PLAN.md'` (bare filename)
  - smoke-outcome-router.test.js planPath assertion kiem tra chua `/S001-test/FIX-PLAN.md`
  - `node --test test/smoke-outcome-router.test.js` exit 0
  </acceptance_criteria>
  <done>prepareFixPlan tra ve planPath day du voi session_dir prefix, test PASS</done>
</task>

<task type="auto">
  <name>Task 2: Fix roundNumber, createBugRecord, write-back trong fix-bug.md (INT-02, INT-04, INT-05)</name>
  <files>workflows/fix-bug.md</files>
  <read_first>workflows/fix-bug.md, bin/lib/checkpoint-handler.js, bin/lib/bug-memory.js, bin/lib/session-manager.js</read_first>
  <action>
  Sua 4 van de trong workflows/fix-bug.md:

  **INT-02: roundNumber undefined (Buoc 4, CHECKPOINT block, khoang dong 233)**
  Tim dong chua `buildContinuationContext({ evidencePath:`. TRUOC dong nay (truoc block `**NEU outcome = 'checkpoint':**`), them:
  ```
  Khoi tao roundNumber: Neu la vong dau tien (chua co continuation truoc do), roundNumber = 1. Neu da co continuation truoc do, roundNumber = vong hien tai (lay tu bien dem hoac SESSION.md).
  ```
  Cu the: Them dong sau dong `### Routing theo outcome:` va truoc `**NEU outcome = 'checkpoint':**`:
  ```
  Khoi tao roundNumber = 1 (mac dinh cho lan dau). Neu quay lai Buoc 4 sau continuation, tang roundNumber len 1.
  ```

  **INT-04: createBugRecord thieu existingBugs (Buoc 5e, khoang dong 323-326)**
  Tim dong chua `Goi \`createBugRecord({ file: targetFile`. Doi toan bo block 5e buoc 1-2 tu:
  ```
  1. Goi `createBugRecord({ file: targetFile, functionName: targetFunction, errorMessage: originalError, rootCause, fix: fixDescription, sessionId: folderName })` tu `bin/lib/bug-memory.js`
     -> bugRecordMd
  2. Ghi bugRecordMd vao `.planning/bugs/BUG-{NNN}.md`
     (Xac dinh NNN: Glob `.planning/bugs/BUG-*.md` -> tim so cao nhat + 1, bat dau tu 001)
  ```
  thanh:
  ```
  1. Glob `.planning/bugs/BUG-*.md` -> parse so tu moi filename (VD: BUG-003.md -> {number: 3}) -> tao mang existingBugs
  2. Goi `createBugRecord({ existingBugs, file: targetFile, functionName: targetFunction, errorMessage: originalError, rootCause, fix: fixDescription, sessionId: folderName })` tu `bin/lib/bug-memory.js`
     -> bugRecord (object voi { id, fileName, content, number })
  3. Ghi bugRecord.content vao `.planning/bugs/${bugRecord.fileName}`
  ```

  **INT-05a: SESSION.md write-back thieu tai isHeavyAgent warning (Buoc 2, khoang dong 107-108)**
  Tim dong: `Ghi warning vao \`{session_dir}/SESSION.md\` qua updateSession() neu co`
  Doi thanh pattern day du:
  ```
     Read `{session_dir}/SESSION.md` -> currentMd
     Goi `updateSession(currentMd, { appendToBody: 'WARNING: Detective dung heavy tool. Chi 1 tac vu nang tai 1 thoi diem.' })` tu `bin/lib/session-manager.js`
     Ghi ket qua sessionMd vao `{session_dir}/SESSION.md`
  ```

  **INT-05b: SESSION.md write-back thieu tai Repro FAIL (Buoc 3, khoang dong 189-191)**
  Tim block `Repro FAIL (agent throw/timeout):`. Doi 2 dong:
  ```
  - WARNING: "Khong tao duoc test tai hien. Tiep tuc voi evidence phan tich."
  - Ghi warning vao SESSION.md qua updateSession()
  ```
  thanh pattern day du:
  ```
  - WARNING: "Khong tao duoc test tai hien. Tiep tuc voi evidence phan tich."
  - Read `{session_dir}/SESSION.md` -> currentMd
    Goi `updateSession(currentMd, { appendToBody: 'WARNING: Repro Engineer fail. Tiep tuc voi evidence phan tich.' })` tu `bin/lib/session-manager.js`
    Ghi ket qua sessionMd vao `{session_dir}/SESSION.md`
  ```

  Ngoai ra, fix-bug.md dong 217-218 (fix_plan path): doi:
  ```
  Ghi planContent vao planPath.
  ```
  thanh:
  ```
  Ghi planContent vao {session_dir}/FIX-PLAN.md (planPath da chua full path tu prepareFixPlan).
  ```
  </action>
  <verify>
    <automated>cd /home/please-done && grep -c "roundNumber = 1" workflows/fix-bug.md && grep -c "existingBugs" workflows/fix-bug.md && grep -c "bugRecord.content" workflows/fix-bug.md && grep -c "bugRecord.fileName" workflows/fix-bug.md</automated>
  </verify>
  <acceptance_criteria>
  - fix-bug.md chua `roundNumber = 1` (INT-02 khoi tao)
  - fix-bug.md chua `existingBugs` trong block createBugRecord (INT-04)
  - fix-bug.md chua `bugRecord.content` thay vi `bugRecordMd` (INT-04 field name)
  - fix-bug.md chua `bugRecord.fileName` thay vi `BUG-{NNN}.md` (INT-04 field name)
  - fix-bug.md Buoc 2 isHeavyAgent warning co day du `Read` -> `updateSession` -> `Ghi ket qua` pattern (INT-05a)
  - fix-bug.md Buoc 3 Repro FAIL co day du `Read` -> `updateSession` -> `Ghi ket qua` pattern (INT-05b)
  - fix-bug.md KHONG chua `-> bugRecordMd` (old field name)
  - fix-bug.md chua `{session_dir}/FIX-PLAN.md` trong fix_plan block
  </acceptance_criteria>
  <done>Workflow fix-bug.md co roundNumber init, existingBugs truyen dung, result fields dung, write-back day du, planPath day du</done>
</task>

<task type="auto">
  <name>Task 3: Cap nhat REQUIREMENTS.md checkboxes va Traceability (D-07)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>
  Cap nhat REQUIREMENTS.md tai 2 vi tri:

  **1. Checkboxes (phan dau file):**
  Doi 6 checkboxes tu `[ ]` thanh `[x]`:
  - Dong 12: `- [ ] **ORCH-01**:` -> `- [x] **ORCH-01**:`
  - Dong 13: `- [ ] **ORCH-02**:` -> `- [x] **ORCH-02**:`
  - Dong 30: `- [ ] **MEM-01**:` -> `- [x] **MEM-01**:`
  - Dong 31: `- [ ] **MEM-02**:` -> `- [x] **MEM-02**:`
  - Dong 32: `- [ ] **MEM-03**:` -> `- [x] **MEM-03**:`
  - Dong 33: `- [ ] **MEM-04**:` -> `- [x] **MEM-04**:`

  Dong thoi cap nhat cac requirement khac ma Phase 35 fix:
  - Dong 21: `- [ ] **PROT-02**:` -> `- [x] **PROT-02**:`
  - Dong 22: `- [ ] **PROT-03**:` -> `- [x] **PROT-03**:`
  - Dong 23: `- [ ] **PROT-04**:` -> `- [x] **PROT-04**:`
  - Dong 24: `- [ ] **PROT-05**:` -> `- [x] **PROT-05**:`
  - Dong 25: `- [ ] **PROT-06**:` -> `- [x] **PROT-06**:`
  - Dong 37: `- [ ] **FLOW-01**:` -> `- [x] **FLOW-01**:`
  - Dong 38: `- [ ] **FLOW-02**:` -> `- [x] **FLOW-02**:`
  - Dong 39: `- [ ] **FLOW-03**:` -> `- [x] **FLOW-03**:`
  - Dong 40: `- [ ] **FLOW-04**:` -> `- [x] **FLOW-04**:`
  - Dong 41: `- [ ] **FLOW-05**:` -> `- [x] **FLOW-05**:`

  **2. Traceability table (cuoi file, dong 82-107):**
  Cap nhat Status tu `Pending` thanh `Complete` cho cac dong:
  - ORCH-01: `Pending` -> `Complete`
  - ORCH-02: `Pending` -> `Complete`
  - PROT-02: `Pending` -> `Complete`
  - PROT-03: `Pending` -> `Complete`
  - PROT-04: `Pending` -> `Complete`
  - PROT-05: `Pending` -> `Complete`
  - PROT-06: `Pending` -> `Complete`
  - MEM-01: `Pending` -> `Complete`
  - MEM-02: `Pending` -> `Complete`
  - MEM-03: `Pending` -> `Complete`
  - MEM-04: `Pending` -> `Complete`
  - FLOW-01: `Pending` -> `Complete`
  - FLOW-02: `Pending` -> `Complete`
  - FLOW-03: `Pending` -> `Complete`
  - FLOW-04: `Pending` -> `Complete`
  - FLOW-05: `Pending` -> `Complete`

  Cap nhat dong cuoi: `*Last updated: 2026-03-25 after Phase 35 gap closure completion*`

  Chay full test suite sau khi hoan tat tat ca tasks: `node --test test/smoke-*.test.js`
  </action>
  <verify>
    <automated>cd /home/please-done && grep -c "\[x\] \*\*ORCH-01\*\*" .planning/REQUIREMENTS.md && grep -c "\[x\] \*\*MEM-04\*\*" .planning/REQUIREMENTS.md && grep -c "\[x\] \*\*FLOW-05\*\*" .planning/REQUIREMENTS.md && node --test test/smoke-*.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - REQUIREMENTS.md chua `[x] **ORCH-01**`
  - REQUIREMENTS.md chua `[x] **ORCH-02**`
  - REQUIREMENTS.md chua `[x] **MEM-01**`
  - REQUIREMENTS.md chua `[x] **MEM-02**`
  - REQUIREMENTS.md chua `[x] **MEM-03**`
  - REQUIREMENTS.md chua `[x] **MEM-04**`
  - REQUIREMENTS.md chua `[x] **PROT-02**`
  - REQUIREMENTS.md chua `[x] **FLOW-05**`
  - REQUIREMENTS.md Traceability table: ORCH-01 Status = Complete
  - REQUIREMENTS.md Traceability table: MEM-04 Status = Complete
  - REQUIREMENTS.md KHONG chua `[ ] **ORCH-01**` (unchecked)
  - `node --test test/smoke-*.test.js` exit 0 (full suite regression check)
  </acceptance_criteria>
  <done>REQUIREMENTS.md checkboxes va Traceability table cap nhat day du, full test suite PASS</done>
</task>

</tasks>

<verification>
1. `node --test test/smoke-outcome-router.test.js` — planPath test PASS
2. `grep "roundNumber = 1" workflows/fix-bug.md` — co ket qua
3. `grep "existingBugs" workflows/fix-bug.md` — co ket qua
4. `grep "bugRecord.content" workflows/fix-bug.md` — co ket qua
5. `grep -c "\[x\]" .planning/REQUIREMENTS.md` — so luong tang dung
6. `node --test test/smoke-*.test.js` — full suite PASS, khong regression
</verification>

<success_criteria>
- prepareFixPlan tra ve planPath day du voi session_dir
- fix-bug.md co roundNumber = 1 init truoc CHECKPOINT block
- fix-bug.md truyen existingBugs va dung result.content/result.fileName
- fix-bug.md co day du Read->updateSession->Write tai 2 vi tri thieu
- REQUIREMENTS.md 16 checkboxes va 16 Traceability entries duoc cap nhat
- Full test suite PASS
</success_criteria>

<output>
After completion, create `.planning/phases/35-fix-evidence-encoding-critical-wiring/35-02-SUMMARY.md`
</output>
