---
phase: 54-platform-mapping-fallback
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/resource-config.js
  - bin/lib/platforms.js
  - test/platform-models.test.js
autonomous: true
requirements: [PLAT-01, PLAT-02]

must_haves:
  truths:
    - "getModelForTier('scout', 'claude') tra ve model: 'claude-haiku-4-5-20251001'"
    - "getModelForTier('architect', 'gemini') fallback ve gemini-2.5-pro voi fallback: true"
    - "getModelForTier('scout') khong platform van tra ve { model: 'haiku', effort: 'low', maxTurns: 15 } -- backward compatible 100%"
    - "PLATFORMS object co 7 entries bao gom cursor va windsurf"
    - "Existing tests (smoke-resource-config.test.js, smoke-agent-files.test.js) pass nguyen trang"
  artifacts:
    - path: "bin/lib/resource-config.js"
      provides: "PLATFORM_MODEL_MAP, FALLBACK_CHAIN, getModelForTier() mo rong"
      contains: "PLATFORM_MODEL_MAP"
      exports: ["PLATFORM_MODEL_MAP", "FALLBACK_CHAIN"]
    - path: "bin/lib/platforms.js"
      provides: "Cursor va Windsurf platform definitions"
      contains: "cursor:"
    - path: "test/platform-models.test.js"
      provides: "Tests cho tat ca platform x tier combinations + fallback"
      min_lines: 80
  key_links:
    - from: "bin/lib/resource-config.js"
      to: "TIER_MAP"
      via: "PLATFORM_MODEL_MAP keys match TIER_MAP[tier].model"
      pattern: "PLATFORM_MODEL_MAP"
    - from: "test/platform-models.test.js"
      to: "bin/lib/resource-config.js"
      via: "require('../bin/lib/resource-config')"
      pattern: "getModelForTier.*platform"
    - from: "bin/lib/platforms.js"
      to: "getGlobalDir()"
      via: "case cursor va windsurf trong switch statement"
      pattern: "case 'cursor'|case 'windsurf'"
---

<objective>
Them platform-specific model resolution vao he thong tier hien tai. Moi platform (claude, codex, gemini, opencode, copilot, cursor, windsurf) co mapping tu generic tier name (haiku/sonnet/opus) sang model thuc te. Khi platform thieu tier cao, fallback tu dong xuong tier thap hon.

Purpose: Cho phep cung 1 agent config chay tren nhieu platforms voi model phu hop nhat cho tung platform.
Output: `PLATFORM_MODEL_MAP` trong resource-config.js, `getModelForTier(tier, platform?)` mo rong, cursor/windsurf trong platforms.js, test file moi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-platform-mapping-fallback/54-CONTEXT.md
@.planning/phases/54-platform-mapping-fallback/54-RESEARCH.md

@bin/lib/resource-config.js
@bin/lib/platforms.js
@test/smoke-resource-config.test.js

<interfaces>
<!-- Key types va contracts executor can. Extracted tu codebase. -->

From bin/lib/resource-config.js:
```javascript
const TIER_MAP = {
  scout: { model: "haiku", effort: "low", maxTurns: 15 },
  builder: { model: "sonnet", effort: "medium", maxTurns: 25 },
  architect: { model: "opus", effort: "high", maxTurns: 30 },
};

function getModelForTier(tier) {
  // ... tra ve { model, effort, maxTurns }
  return { ...entry };
}

module.exports = {
  getModelForTier, getAgentConfig, getParallelLimit,
  getAdaptiveParallelLimit, isHeavyAgent, shouldDegrade,
  TIER_MAP, AGENT_REGISTRY, PARALLEL_LIMIT,
  PARALLEL_MIN, PARALLEL_MAX, PARALLEL_DEFAULT,
  HEAVY_TOOL_PATTERNS,
};
```

From bin/lib/platforms.js:
```javascript
const TOOL_MAP = { claude: {}, codex: {}, gemini: {...}, opencode: {}, copilot: {...} };
const PLATFORMS = { claude: {...}, codex: {...}, gemini: {...}, opencode: {...}, copilot: {...} };

function getGlobalDir(runtime, explicitDir) { ... }
function getAllRuntimes() { return Object.keys(PLATFORMS); }

module.exports = { PLATFORMS, TOOL_MAP, getGlobalDir, getLocalDir, convertCommandRef, getAllRuntimes };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tao test file platform-models.test.js (RED phase)</name>
  <files>test/platform-models.test.js</files>
  <read_first>
    - bin/lib/resource-config.js (hieu getModelForTier() hien tai, TIER_MAP structure, module.exports)
    - bin/lib/platforms.js (hieu PLATFORMS object hien tai, 5 platforms)
    - test/smoke-resource-config.test.js (pattern: node:test + assert/strict, naming convention tieng Viet)
  </read_first>
  <behavior>
    - Test 1: getModelForTier('scout', 'claude') tra ve { model: 'claude-haiku-4-5-20251001', effort: 'low', maxTurns: 15 }
    - Test 2: getModelForTier('builder', 'claude') tra ve { model: 'claude-sonnet-4-6-20250217', effort: 'medium', maxTurns: 25 }
    - Test 3: getModelForTier('architect', 'claude') tra ve { model: 'claude-opus-4-6-20250205', effort: 'high', maxTurns: 30 }
    - Test 4: getModelForTier('scout', 'codex') tra ve { model: 'gpt-5.4-mini', effort: 'low', maxTurns: 15 }
    - Test 5: getModelForTier('builder', 'codex') tra ve { model: 'gpt-5.3-codex', effort: 'medium', maxTurns: 25 }
    - Test 6: getModelForTier('architect', 'codex') tra ve { model: 'gpt-5.4', effort: 'high', maxTurns: 30 }
    - Test 7: getModelForTier('scout', 'gemini') tra ve { model: 'gemini-2.5-flash', effort: 'low', maxTurns: 15 }
    - Test 8: getModelForTier('builder', 'gemini') tra ve { model: 'gemini-2.5-pro', effort: 'medium', maxTurns: 25 }
    - Test 9: getModelForTier('architect', 'gemini') FALLBACK: tra ve { model: 'gemini-2.5-pro', effort: 'medium', maxTurns: 25, fallback: true, requestedTier: 'architect', resolvedTier: 'builder' }
    - Test 10: getModelForTier('scout') khong platform tra ve { model: 'haiku', effort: 'low', maxTurns: 15 } -- KHONG co fallback field
    - Test 11: getModelForTier('architect') khong platform tra ve { model: 'opus', effort: 'high', maxTurns: 30 }
    - Test 12: getModelForTier('scout', 'unknown-platform') tra ve generic { model: 'haiku' } -- platform khong biet = generic
    - Test 13: PLATFORM_MODEL_MAP co du 7 keys (claude, codex, gemini, opencode, copilot, cursor, windsurf)
    - Test 14: FALLBACK_CHAIN la ['architect', 'builder', 'scout']
    - Test 15: opencode, copilot, cursor, windsurf tat ca co 3 tiers day du (khong fallback)
    - Test 16: PLATFORMS object co 7 entries (them cursor + windsurf) -- import tu platforms.js
    - Test 17: PLATFORMS.cursor co dirName '.cursor', commandPrefix '/pd:'
    - Test 18: PLATFORMS.windsurf co dirName '.windsurf', commandPrefix '/pd:'
    - Test 19: getGlobalDir('cursor') va getGlobalDir('windsurf') khong throw
    - Test 20: getAllRuntimes() tra ve array co 7 items bao gom 'cursor' va 'windsurf'
  </behavior>
  <action>
    Tao file `test/platform-models.test.js` voi cau truc:

    ```javascript
    'use strict';
    const { describe, it } = require('node:test');
    const assert = require('node:assert/strict');
    ```

    Import tu resource-config.js: `getModelForTier, PLATFORM_MODEL_MAP, FALLBACK_CHAIN, TIER_MAP`
    Import tu platforms.js: `PLATFORMS, getAllRuntimes, getGlobalDir`

    Tao 4 describe blocks:

    1. `describe('PLATFORM_MODEL_MAP structure')` — Test 13, 14: kiem tra 7 keys, FALLBACK_CHAIN value

    2. `describe('getModelForTier voi platform')` — Test 1-9: moi platform co describe con, test tung tier. Gemini architect test fallback fields.

    3. `describe('getModelForTier backward compatibility')` — Test 10, 11, 12: khong platform = generic, unknown platform = generic. Assert KHONG co `fallback` field (assert.equal(result.fallback, undefined)).

    4. `describe('platforms.js: cursor va windsurf')` — Test 16-20: kiem tra PLATFORMS keys, cursor/windsurf config fields, getGlobalDir khong throw, getAllRuntimes co 7 items.

    Dung pattern giong smoke-resource-config.test.js: tieng Viet cho mo ta, assert/strict, khong dung any/external deps.

    Chu y: Tests se FAIL (RED) vi PLATFORM_MODEL_MAP, FALLBACK_CHAIN chua ton tai, cursor/windsurf chua co trong platforms.js. Day la TDD RED phase — commit tests truoc.
  </action>
  <verify>
    <automated>node --test test/platform-models.test.js 2>&1 | head -5</automated>
    Tests PHAI FAIL (RED phase). Kiem tra file ton tai va co it nhat 20 test cases. File KHONG duoc rong.
  </verify>
  <acceptance_criteria>
    - test/platform-models.test.js ton tai va co > 80 dong
    - File chua `require('node:test')` va `require('node:assert/strict')`
    - File chua `require('../bin/lib/resource-config')` voi import PLATFORM_MODEL_MAP, FALLBACK_CHAIN
    - File chua `require('../bin/lib/platforms')` voi import PLATFORMS, getAllRuntimes
    - File chua it nhat 4 describe blocks
    - File chua it nhat 20 it() blocks
    - File chua test cho 'gemini' + 'architect' voi assert cho fallback: true
    - File chua test backward compatibility: assert.equal(result.fallback, undefined)
    - File chua test cho PLATFORMS.cursor va PLATFORMS.windsurf
  </acceptance_criteria>
  <done>Test file ton tai voi day du test cases cho PLAT-01 (platform mapping) va PLAT-02 (fallback). Tests fail vi implementation chua co (RED phase).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PLATFORM_MODEL_MAP, extend getModelForTier(), them cursor/windsurf (GREEN phase)</name>
  <files>bin/lib/resource-config.js, bin/lib/platforms.js</files>
  <read_first>
    - bin/lib/resource-config.js (TIER_MAP line 22-26, getModelForTier line 153-167, module.exports line 303-317)
    - bin/lib/platforms.js (TOOL_MAP line 13-39, PLATFORMS line 42-93, getGlobalDir switch line 111-127, module.exports line 159-166)
    - test/platform-models.test.js (tests vua tao o Task 1 — biet chinh xac can implement gi)
    - test/smoke-resource-config.test.js (baseline tests phai pass nguyen trang)
  </read_first>
  <action>
    **A. Sua `bin/lib/resource-config.js`:**

    1. Them `FALLBACK_CHAIN` array ngay sau TIER_MAP (line ~27):
    ```javascript
    const FALLBACK_CHAIN = ['architect', 'builder', 'scout'];
    ```

    2. Them `PLATFORM_MODEL_MAP` ngay sau FALLBACK_CHAIN (per D-03, D-04, D-05). Keys la generic model names (haiku/sonnet/opus) de match voi TIER_MAP[tier].model:
    ```javascript
    const PLATFORM_MODEL_MAP = {
      claude: {
        haiku: 'claude-haiku-4-5-20251001',
        sonnet: 'claude-sonnet-4-6-20250217',
        opus: 'claude-opus-4-6-20250205',
      },
      codex: {
        haiku: 'gpt-5.4-mini',
        sonnet: 'gpt-5.3-codex',
        opus: 'gpt-5.4',
      },
      gemini: {
        haiku: 'gemini-2.5-flash',
        sonnet: 'gemini-2.5-pro',
        // khong co opus -> fallback sang builder (gemini-2.5-pro)
      },
      opencode: {
        haiku: 'claude-haiku-4-5-20251001',
        sonnet: 'claude-sonnet-4-6-20250217',
        opus: 'claude-opus-4-6-20250205',
      },
      copilot: {
        haiku: 'claude-haiku-4-5-20251001',
        sonnet: 'claude-sonnet-4-6-20250217',
        opus: 'claude-opus-4-6-20250205',
      },
      cursor: {
        haiku: 'claude-haiku-4-5-20251001',
        sonnet: 'claude-sonnet-4-6-20250217',
        opus: 'claude-opus-4-6-20250205',
      },
      windsurf: {
        haiku: 'claude-haiku-4-5-20251001',
        sonnet: 'claude-sonnet-4-6-20250217',
        opus: 'claude-opus-4-6-20250205',
      },
    };
    ```

    3. Thay the `getModelForTier(tier)` (line 153-167) bang version mo rong per D-07, D-08:
    ```javascript
    function getModelForTier(tier, platform) {
      if (tier == null || typeof tier !== 'string') {
        throw new Error('thieu tham so tier');
      }

      const normalized = tier.toLowerCase();
      const entry = TIER_MAP[normalized];

      if (!entry) {
        throw new Error(`tier khong hop le: ${tier}`);
      }

      // Khong co platform -> tra ve generic (backward compatible, per D-07, D-11)
      if (!platform) {
        return { ...entry };
      }

      const platformMap = PLATFORM_MODEL_MAP[platform];
      if (!platformMap) {
        // Platform khong biet -> tra ve generic (per D-09)
        return { ...entry };
      }

      // Tim model cho tier, neu khong co thi fallback (per D-08)
      const startIdx = FALLBACK_CHAIN.indexOf(normalized);
      for (let i = startIdx; i < FALLBACK_CHAIN.length; i++) {
        const fallbackTier = FALLBACK_CHAIN[i];
        const tierEntry = TIER_MAP[fallbackTier];
        if (platformMap[tierEntry.model]) {
          const result = {
            ...tierEntry,
            model: platformMap[tierEntry.model],
          };
          if (fallbackTier !== normalized) {
            result.fallback = true;
            result.requestedTier = normalized;
            result.resolvedTier = fallbackTier;
          }
          return result;
        }
      }

      // Khong nen xay ra — de phong (per D-09)
      return { ...entry };
    }
    ```

    4. Cap nhat JSDoc cua getModelForTier: them `@param {string} [platform]` va mo ta return object co the chua `fallback`, `requestedTier`, `resolvedTier`.

    5. Them `PLATFORM_MODEL_MAP` va `FALLBACK_CHAIN` vao `module.exports` block (line 303-317):
    ```javascript
    module.exports = {
      getModelForTier,
      getAgentConfig,
      // ... existing exports giu nguyen ...
      PLATFORM_MODEL_MAP,
      FALLBACK_CHAIN,
    };
    ```

    **B. Sua `bin/lib/platforms.js`:**

    1. Them cursor va windsurf vao TOOL_MAP (line ~38, truoc dong `};`):
    ```javascript
    cursor: {},    // Cursor dung Claude native tool names
    windsurf: {},  // Windsurf dung Claude native tool names
    ```

    2. Them cursor va windsurf vao PLATFORMS object (line ~92, truoc dong `};`):
    ```javascript
    cursor: {
      name: 'Cursor',
      dirName: '.cursor',
      commandPrefix: '/pd:',
      commandSeparator: ':',
      envVar: 'CURSOR_CONFIG_DIR',
      skillFormat: 'nested',
      frontmatterFormat: 'yaml',
      toolMap: TOOL_MAP.cursor,
    },
    windsurf: {
      name: 'Windsurf',
      dirName: '.windsurf',
      commandPrefix: '/pd:',
      commandSeparator: ':',
      envVar: 'WINDSURF_CONFIG_DIR',
      skillFormat: 'nested',
      frontmatterFormat: 'yaml',
      toolMap: TOOL_MAP.windsurf,
    },
    ```

    3. Them case cho cursor va windsurf trong `getGlobalDir()` switch (line ~125, truoc `default:`):
    ```javascript
    case 'cursor':
      return path.join(home, '.cursor');
    case 'windsurf':
      return path.join(home, '.windsurf');
    ```

    **Anti-patterns tranh (tu RESEARCH.md):**
    - KHONG sua TIER_MAP structure (per research anti-pattern 1)
    - KHONG them fields fallback/requestedTier khi khong co platform parameter (per pitfall 1)
    - KHONG import platforms.js trong resource-config.js (per research anti-pattern 3)
    - KHONG throw error khi fallback (per D-09, research anti-pattern 4)
  </action>
  <verify>
    <automated>node --test test/platform-models.test.js && node --test test/smoke-resource-config.test.js && node --test test/smoke-agent-files.test.js</automated>
  </verify>
  <acceptance_criteria>
    - resource-config.js chua `const PLATFORM_MODEL_MAP = {` voi 7 platform keys
    - resource-config.js chua `const FALLBACK_CHAIN = ['architect', 'builder', 'scout']`
    - resource-config.js: getModelForTier co 2 params: `function getModelForTier(tier, platform)`
    - resource-config.js: module.exports chua `PLATFORM_MODEL_MAP` va `FALLBACK_CHAIN`
    - resource-config.js: PLATFORM_MODEL_MAP.gemini KHONG co key `opus`
    - resource-config.js: PLATFORM_MODEL_MAP.claude.haiku === 'claude-haiku-4-5-20251001'
    - resource-config.js: PLATFORM_MODEL_MAP.codex.opus === 'gpt-5.4'
    - platforms.js chua `cursor:` va `windsurf:` trong ca TOOL_MAP va PLATFORMS
    - platforms.js: PLATFORMS.cursor.dirName === '.cursor'
    - platforms.js: PLATFORMS.windsurf.dirName === '.windsurf'
    - platforms.js: getGlobalDir switch co case 'cursor' va case 'windsurf'
    - `node --test test/platform-models.test.js` exit code 0 (TAT CA tests pass)
    - `node --test test/smoke-resource-config.test.js` exit code 0 (backward compatible)
    - `node --test test/smoke-agent-files.test.js` exit code 0 (backward compatible)
  </acceptance_criteria>
  <done>PLATFORM_MODEL_MAP co 7 platforms, getModelForTier() ho tro optional platform parameter voi fallback tu dong, cursor/windsurf co trong PLATFORMS. Tat ca tests pass bao gom backward compatibility.</done>
</task>

</tasks>

<verification>
1. `node --test test/platform-models.test.js` — tat ca platform x tier combinations + fallback pass
2. `node --test test/smoke-resource-config.test.js` — 38 existing tests pass nguyen trang (backward compatible)
3. `node --test test/smoke-agent-files.test.js` — agent tests pass
4. `node --test 'test/*.test.js'` — full suite green
</verification>

<success_criteria>
- PLAT-01: Moi platform resolve dung model cho moi tier — verified qua test matrix 7 platforms x 3 tiers
- PLAT-02: Gemini architect fallback ve builder voi fallback: true — verified qua dedicated test
- Backward compatible: getModelForTier('scout') khong platform = generic haiku — verified qua existing + new tests
- 0 test failures tren toan bo test suite
</success_criteria>

<output>
After completion, create `.planning/phases/54-platform-mapping-fallback/54-01-SUMMARY.md`
</output>
