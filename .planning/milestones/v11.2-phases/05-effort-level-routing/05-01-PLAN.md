---
phase: 05-effort-level-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/tasks.md
  - templates/plan.md
  - references/conventions.md
  - workflows/plan.md
  - workflows/write-code.md
  - workflows/fix-bug.md
  - workflows/test.md
autonomous: true
requirements: [TOKN-04]

must_haves:
  truths:
    - "TASKS.md template includes Effort field in task metadata line"
    - "PLAN.md template includes Effort field guidance in Truths section"
    - "conventions.md documents effort level enum (simple|standard|complex)"
    - "Planner workflow has effort classification guidelines with signals table"
    - "write-code workflow resolves effort->model and passes model to Agent tool in Buoc 10"
    - "fix-bug workflow infers effort from bug classification (risk level)"
    - "test workflow mirrors effort from the task being tested"
  artifacts:
    - path: "templates/tasks.md"
      provides: "Effort field in task metadata template"
      contains: "Effort:"
    - path: "templates/plan.md"
      provides: "Effort field guidance"
      contains: "Effort"
    - path: "references/conventions.md"
      provides: "Effort level enum documentation"
      contains: "simple"
    - path: "workflows/plan.md"
      provides: "Effort classification guidelines"
      contains: "effort"
    - path: "workflows/write-code.md"
      provides: "Effort-to-model routing in executor"
      contains: "Effort"
    - path: "workflows/fix-bug.md"
      provides: "Effort inference from bug classification"
      contains: "Effort"
    - path: "workflows/test.md"
      provides: "Effort mirroring from task"
      contains: "Effort"
  key_links:
    - from: "workflows/plan.md"
      to: "templates/tasks.md"
      via: "Planner writes Effort field using guidelines, stored in TASKS.md"
      pattern: "Effort:"
    - from: "workflows/write-code.md"
      to: "templates/tasks.md"
      via: "Executor reads Effort field from TASKS.md task metadata"
      pattern: "Effort.*model"
    - from: "references/conventions.md"
      to: "workflows/plan.md"
      via: "Effort enum documented in conventions, used in planner guidelines"
      pattern: "simple.*standard.*complex"
---

<objective>
Add effort-level routing to the planner and executor workflow instructions.

Purpose: Enable smaller/faster models for simple tasks (haiku) while reserving Opus for complex reasoning, reducing cost and latency per TOKN-04.
Output: Updated templates, conventions, and 4 workflow files with effort classification and routing instructions.
</objective>

<execution_context>
@/Users/tonamson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-effort-level-routing/05-CONTEXT.md
@.planning/phases/05-effort-level-routing/05-RESEARCH.md

<interfaces>
<!-- Key patterns from the codebase that executors need to follow -->

From templates/tasks.md — Current task metadata line:
```markdown
> Trang thai: ⬜ | Uu tien: Cao | Phu thuoc: Khong | Loai: Backend
> Files: [danh sach files du kien]
> Truths: [T1, T2]
```

From workflows/plan.md — Buoc 5 location (task splitting step):
```markdown
## Buoc 5: Chia cong viec
CONTEXT.md → Tech Stack → Backend, Frontend, hoac ca hai. Xem @references/prioritization.md.
Nguyen tac:
1. **Entity/Model truoc** → Service → Controller → DTO (Backend)
...
```

From workflows/write-code.md — Buoc 10 parallel spawning:
```markdown
## Buoc 10: Tiep tuc hoac dung
### `--parallel` (multi-agent song song)
1. **Spawn Agent tool** cho moi task song song — moi agent nhan: PLAN.md, task detail, rules, CONTEXT.md, docs. Chi dan: Buoc 2→3→4→5 (KHONG report/TASKS/commit)
```

From workflows/fix-bug.md — Bug classification in Buoc 6a:
```markdown
### 6a. Phan loai rui ro
Phan loai theo @references/prioritization.md → 'Phan loai rui ro bug'. Cap nhat SESSION.
```

From workflows/test.md — Buoc 1 task identification:
```markdown
## Buoc 1: Xac dinh scope + doc context
```

From references/conventions.md — Current structure (ends after "Bao mat" section):
```markdown
## Bao mat — Files cam doc
CAM doc/hien thi noi dung: `.env`, ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add effort field to templates and conventions reference</name>
  <files>templates/tasks.md, templates/plan.md, references/conventions.md</files>
  <read_first>
    - templates/tasks.md (current task metadata template structure)
    - templates/plan.md (current PLAN.md template structure)
    - references/conventions.md (current conventions, will add effort enum section)
  </read_first>
  <action>
**templates/tasks.md** — Add `Effort:` to the task metadata line template. Change the existing line:
```
> Trang thai: ⬜ | Uu tien: Cao | Phu thuoc: Khong | Loai: Backend
```
to:
```
> Trang thai: ⬜ | Uu tien: Cao | Phu thuoc: Khong | Loai: Backend | Effort: standard
```

Also add a brief note in the "Loai task" section (after the existing `Backend | Frontend | ...` line), adding a new section:

```markdown
## Effort level

`simple` | `standard` | `complex`

Mac dinh: `standard`. Xem @references/conventions.md → 'Effort level'.
```

**templates/plan.md** — In the "Su that phai dat (Truths)" table example, no change needed (Truths are about outcomes, not effort). Instead, add `Effort:` to the task example within the "Thu tu thuc hien" section at the bottom. After the existing `## Thu tu thuc hien` section, add a note:

```markdown
> Moi task trong TASKS.md PHAI co truong `Effort:` trong metadata line.
> Mac dinh: `standard`. Xem @references/conventions.md.
```

**references/conventions.md** — Add a new section BEFORE the "Bao mat" section (so it appears after "Ngon ngu" and before "Bao mat"). Add:

```markdown
## Effort level

| Effort | Model | Vi du |
|--------|-------|-------|
| simple | haiku | doi ten bien, them import, sua typo, cap nhat config |
| standard | sonnet | tao component moi, API endpoint, bo unit test |
| complex | opus | refactor nhieu file, quyet dinh kien truc, tich hop |

Mac dinh: `standard` (sonnet). Task thieu truong Effort → xu ly nhu `standard`.

Phan loai:
| Tin hieu | simple | standard | complex |
|----------|--------|----------|---------|
| Files sua/tao | 1-2 | 3-4 | 5+ |
| So Truths | 1 | 2-3 | 4+ |
| Phu thuoc | 0 | 1-2 | 3+ |
| Da domain | khong | khong | co |

Planner CO THE override guidelines dua tren hieu biet context.
User override: sua truc tiep `Effort:` trong TASKS.md truoc khi chay.
```
  </action>
  <verify>
    <automated>grep -c "Effort:" templates/tasks.md && grep -c "Effort" templates/plan.md && grep -c "simple.*haiku" references/conventions.md</automated>
  </verify>
  <acceptance_criteria>
    - templates/tasks.md contains the string "| Effort: standard" in the task metadata line template
    - templates/tasks.md contains section "## Effort level" with values "simple | standard | complex"
    - templates/plan.md contains "Effort:" reference note near "Thu tu thuc hien"
    - references/conventions.md contains section "## Effort level"
    - references/conventions.md contains table row "| simple | haiku |"
    - references/conventions.md contains table row "| standard | sonnet |"
    - references/conventions.md contains table row "| complex | opus |"
    - references/conventions.md contains "Mac dinh: `standard`"
    - references/conventions.md "## Effort level" section appears BEFORE "## Bao mat"
  </acceptance_criteria>
  <done>Templates include Effort field in task metadata, conventions.md documents the effort enum with model mapping and classification signals</done>
</task>

<task type="auto">
  <name>Task 2: Add effort routing to planner and executor workflows</name>
  <files>workflows/plan.md, workflows/write-code.md, workflows/fix-bug.md, workflows/test.md</files>
  <read_first>
    - workflows/plan.md (find Buoc 5 location for classification guidelines)
    - workflows/write-code.md (find Buoc 1 task selection and Buoc 10 parallel spawning)
    - workflows/fix-bug.md (find Buoc 6a risk classification)
    - workflows/test.md (find Buoc 1 scope identification)
    - references/conventions.md (effort enum just added by Task 1)
  </read_first>
  <action>
**workflows/plan.md** — Add effort classification guidelines to Buoc 5 (task splitting step). After the existing rule 10 ("Dependency chinh xac cho parallel execution..."), add rule 11:

```markdown
11. **Effort level** (TOKN-04): moi task PHAI co `Effort:` trong metadata. Mac dinh: `standard`.

### Phan loai effort cho task

| Tin hieu | simple | standard | complex |
|----------|--------|----------|---------|
| Files sua/tao | 1-2 | 3-4 | 5+ |
| So Truths | 1 | 2-3 | 4+ |
| Phu thuoc | 0 | 1-2 | 3+ |
| Da domain | khong | khong | co |

Vi du:
- simple: doi ten bien, them import, sua typo, cap nhat config
- standard: tao component moi, API endpoint, bo unit test
- complex: refactor nhieu file, quyet dinh kien truc, tich hop

Planner CO THE override guidelines dua tren hieu biet context.
```

**workflows/write-code.md** — Two changes:

(A) In Buoc 1 (task selection), after the "Chon task" table and before "**Persist 🔄 ngay**", add:

```markdown
**Doc effort va chon model:**
Doc `Effort:` tu metadata task trong TASKS.md:
| Effort | Model |
|--------|-------|
| simple | haiku |
| standard | sonnet |
| complex | opus |
| (thieu/khong ro) | sonnet |

Thong bao: "Spawning {model} agent cho {task_id} ({effort})..."
```

(B) In Buoc 10 parallel section, update the existing step 1 from:
```
1. **Spawn Agent tool** cho moi task song song — moi agent nhan: PLAN.md, task detail, rules, CONTEXT.md, docs. Chi dan: Buoc 2→3→4→5 (KHONG report/TASKS/commit)
```
to:
```
1. **Spawn Agent tool** cho moi task song song — moi agent nhan: PLAN.md, task detail, rules, CONTEXT.md, docs.
   - Doc `Effort:` tu task metadata -> chon model tuong ung (simple->haiku, standard->sonnet, complex->opus, mac dinh->sonnet)
   - Truyen model vao Agent tool: `model: {resolved_model}`
   - Thong bao: "Spawning {model} agent cho {task_id} ({effort})..."
   - Chi dan: Buoc 2→3→4→5 (KHONG report/TASKS/commit)
```

**workflows/fix-bug.md** — Add effort routing AFTER Buoc 6a (risk classification). Insert a new sub-step 6a.1:

```markdown
### 6a.1. Effort routing cho fix-bug

fix-bug khong doc tu TASKS.md effort. Effort suy ra tu phan loai rui ro:

| Phan loai bug | Effort | Model |
|---------------|--------|-------|
| 🟢 Sua nhanh | simple | haiku |
| 🟡 Loi logic | standard | sonnet |
| 🟠 Loi du lieu | standard | sonnet |
| 🔴 Bao mat | complex | opus |
| 🔵 Ha tang | simple | haiku |

Thong bao: "Spawning {model} agent cho fix-bug ({effort} — {phan_loai})..."
```

**workflows/test.md** — Add effort routing to Buoc 1 (scope identification). After the existing "Chi test tasks ✅" line and before "**KHONG co task ✅ nao**", add:

```markdown
**Effort routing cho test:**
Test mirrors effort cua task dang test:
- Doc `Effort:` tu task metadata trong TASKS.md
- Thieu truong Effort → mac dinh `standard` (sonnet)
- Thong bao: "Spawning {model} agent cho test ({effort})..."
```
  </action>
  <verify>
    <automated>grep -c "Effort" workflows/plan.md && grep -c "Effort" workflows/write-code.md && grep -c "Effort" workflows/fix-bug.md && grep -c "Effort" workflows/test.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/plan.md contains "Effort level" in Buoc 5 section
    - workflows/plan.md contains classification table with "| Files sua/tao | 1-2 | 3-4 | 5+ |"
    - workflows/write-code.md contains "Doc effort va chon model" section
    - workflows/write-code.md Buoc 10 contains "model: {resolved_model}"
    - workflows/write-code.md contains effort->model mapping table with "| simple | haiku |"
    - workflows/write-code.md contains "mac dinh->sonnet" (backward compat)
    - workflows/fix-bug.md contains "6a.1. Effort routing cho fix-bug"
    - workflows/fix-bug.md contains bug-to-effort mapping table with 5 rows (green/yellow/orange/red/blue)
    - workflows/test.md contains "Effort routing cho test"
    - workflows/test.md contains "mac dinh `standard`"
    - All 4 workflows contain "Thong bao:" logging instruction per D-11
  </acceptance_criteria>
  <done>All 4 workflows (plan, write-code, fix-bug, test) have effort routing instructions. Planner has classification guidelines. Executor workflows have effort->model mapping with backward compatibility defaults and logging.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -r "Effort" templates/ references/conventions.md workflows/plan.md workflows/write-code.md workflows/fix-bug.md workflows/test.md` — all 7 files contain Effort references
2. `grep "mac dinh" workflows/write-code.md workflows/fix-bug.md workflows/test.md` — all 3 executor workflows have backward compat default
3. `grep "Thong bao" workflows/write-code.md workflows/fix-bug.md workflows/test.md` — all 3 executor workflows have logging
4. `node --test 'test/*.test.js'` — existing tests still pass (no regression from markdown edits)
</verification>

<success_criteria>
- All 7 files modified with effort-level routing content
- TASKS.md template has Effort field in metadata line
- conventions.md documents effort enum with model mapping
- Planner workflow has classification guidelines
- All 3 executor workflows have effort->model resolution with backward compat defaults
- All 3 executor workflows have logging instruction per D-11
- Existing test suite passes without regression
</success_criteria>

<output>
After completion, create `.planning/phases/05-effort-level-routing/05-01-SUMMARY.md`
</output>
