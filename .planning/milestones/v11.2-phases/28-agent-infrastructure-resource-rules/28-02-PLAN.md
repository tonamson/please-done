---
phase: 28-agent-infrastructure-resource-rules
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - .claude/agents/pd-bug-janitor.md
  - .claude/agents/pd-code-detective.md
  - .claude/agents/pd-doc-specialist.md
  - .claude/agents/pd-repro-engineer.md
  - .claude/agents/pd-fix-architect.md
  - test/smoke-agent-files.test.js
autonomous: true
requirements:
  - ORCH-01
must_haves:
  truths:
    - "5 agent files ton tai tai .claude/agents/"
    - "Moi agent file co YAML frontmatter voi name, description, tools, model, maxTurns, effort"
    - "Scout agents co model haiku, Builder agents co model sonnet, Architect co model opus"
    - "Agent files co markdown body giu nguyen noi dung tu commands/pd/agents/"
    - "Hardcoded values trong resource-config.js khop voi YAML frontmatter trong agent files"
  artifacts:
    - path: ".claude/agents/pd-bug-janitor.md"
      provides: "Scout agent file"
    - path: ".claude/agents/pd-code-detective.md"
      provides: "Builder agent file"
    - path: ".claude/agents/pd-doc-specialist.md"
      provides: "Scout agent file"
    - path: ".claude/agents/pd-repro-engineer.md"
      provides: "Builder agent file"
    - path: ".claude/agents/pd-fix-architect.md"
      provides: "Architect agent file"
    - path: "test/smoke-agent-files.test.js"
      provides: "Integration test verify agent files khop voi resource-config"
  key_links:
    - from: "test/smoke-agent-files.test.js"
      to: ".claude/agents/*.md"
      via: "fs.readFileSync de doc va parse YAML frontmatter"
    - from: "test/smoke-agent-files.test.js"
      to: "bin/lib/resource-config.js"
      via: "require de verify consistency"
---

<objective>
Tao 5 agent files tai `.claude/agents/` theo Claude Code native format (YAML frontmatter + markdown body) va integration test verify agent files khop voi hardcoded values trong resource-config.js.

Purpose: Agent files la source-of-truth cho Claude Code runtime de spawn subagents voi dung model, tools, va maxTurns. Integration test dam bao agent files va resource-config.js luon dong bo.
Output: 5 agent files + 1 integration test file, tat ca tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-agent-infrastructure-resource-rules/28-CONTEXT.md
@.planning/phases/28-agent-infrastructure-resource-rules/28-RESEARCH.md
@.planning/phases/28-agent-infrastructure-resource-rules/28-01-SUMMARY.md

@commands/pd/agents/pd-bug-janitor.md
@commands/pd/agents/pd-code-detective.md
@commands/pd/agents/pd-doc-specialist.md
@commands/pd/agents/pd-repro-engineer.md
@commands/pd/agents/pd-fix-architect.md

<interfaces>
<!-- Key exports tu resource-config.js (tao boi Plan 01) -->
From bin/lib/resource-config.js:
```javascript
const TIER_MAP = {
  scout:     { model: 'haiku',  effort: 'low',    maxTurns: 15 },
  builder:   { model: 'sonnet', effort: 'medium', maxTurns: 25 },
  architect: { model: 'opus',   effort: 'high',   maxTurns: 30 },
};
const AGENT_REGISTRY = {
  'pd-bug-janitor':      { tier: 'scout',     tools: ['Read', 'Glob', 'Grep', 'AskUserQuestion', 'Bash'] },
  'pd-code-detective':   { tier: 'builder',   tools: ['Read', 'Glob', 'Grep', 'mcp__fastcode__code_qa'] },
  'pd-doc-specialist':   { tier: 'scout',     tools: ['Read', 'mcp__context7__resolve-library-id', 'mcp__context7__query-docs'] },
  'pd-repro-engineer':   { tier: 'builder',   tools: ['Read', 'Write', 'Edit', 'Bash'] },
  'pd-fix-architect':    { tier: 'architect',  tools: ['Read', 'Write', 'Edit', 'Bash', 'Glob', 'Grep'] },
};
module.exports = { getModelForTier, getAgentConfig, getParallelLimit, isHeavyAgent, shouldDegrade, TIER_MAP, AGENT_REGISTRY, PARALLEL_LIMIT, HEAVY_TOOL_PATTERNS };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tao 5 agent files tai .claude/agents/ theo Claude Code native format</name>
  <files>.claude/agents/pd-bug-janitor.md, .claude/agents/pd-code-detective.md, .claude/agents/pd-doc-specialist.md, .claude/agents/pd-repro-engineer.md, .claude/agents/pd-fix-architect.md</files>
  <read_first>
    - commands/pd/agents/pd-bug-janitor.md (source body content)
    - commands/pd/agents/pd-code-detective.md (source body content)
    - commands/pd/agents/pd-doc-specialist.md (source body content)
    - commands/pd/agents/pd-repro-engineer.md (source body content)
    - commands/pd/agents/pd-fix-architect.md (source body content)
    - .planning/phases/28-agent-infrastructure-resource-rules/28-RESEARCH.md (Agent File Conversion Map tai dong 570-587)
  </read_first>
  <action>
Tao thu muc `.claude/agents/` neu chua ton tai: `mkdir -p .claude/agents`

Tao 5 agent files theo Claude Code native format. Moi file gom YAML frontmatter (giua 2 dong `---`) va markdown body phia duoi.

YAML frontmatter fields (per D-01): `name`, `description`, `tools`, `model`, `maxTurns`, `effort`
KHONG dung fields: `tier`, `allowed-tools`, `memory`, `background`, `hooks`, `mcpServers`, `permissionMode`, `isolation`, `skills`

**File 1: .claude/agents/pd-bug-janitor.md**
YAML frontmatter:
  name: pd-bug-janitor
  description: Nhan vien ve sinh boi canh — Loc log rac va trich xuat trieu chung vang. Dung khi bat dau dieu tra loi moi de thu thap 5 thong tin trieu chung cot loi.
  tools: Read, Glob, Grep, AskUserQuestion, Bash
  model: haiku
  maxTurns: 15
  effort: low
Body: Copy noi dung tu `commands/pd/agents/pd-bug-janitor.md` (cac block `<objective>`, `<process>`, `<rules>`). Giu nguyen tieng Viet co dau.

**File 2: .claude/agents/pd-code-detective.md**
YAML frontmatter:
  name: pd-code-detective
  description: Tham tu hien truong — Truy vet nguyen nhan loi trong ma nguon du an. Dung khi can phan tich code va tim diem gay loi dua tren trieu chung tu Janitor.
  tools: Read, Glob, Grep, mcp__fastcode__code_qa
  model: sonnet
  maxTurns: 25
  effort: medium
Body: Copy tu `commands/pd/agents/pd-code-detective.md`.

**File 3: .claude/agents/pd-doc-specialist.md**
YAML frontmatter:
  name: pd-doc-specialist
  description: Chuyen gia tra cuu thu vien — Tim loi lien quan den thu vien ben thu ba qua tai lieu chinh thuc. Dung song song voi Code Detective de kiem tra Breaking Changes va Known Issues.
  tools: Read, mcp__context7__resolve-library-id, mcp__context7__query-docs
  model: haiku
  maxTurns: 15
  effort: low
Body: Copy tu `commands/pd/agents/pd-doc-specialist.md`.

**File 4: .claude/agents/pd-repro-engineer.md**
YAML frontmatter:
  name: pd-repro-engineer
  description: Ky su tai hien — Viet test case toi gian de nhin thay loi (Red Test). Dung sau khi co bang chung tu Detective va DocSpec de xac nhan loi bang test that bai.
  tools: Read, Write, Edit, Bash
  model: sonnet
  maxTurns: 25
  effort: medium
Body: Copy tu `commands/pd/agents/pd-repro-engineer.md`.

**File 5: .claude/agents/pd-fix-architect.md**
YAML frontmatter:
  name: pd-fix-architect
  description: Kien truc su sua loi — Tong hop bao cao tu 4 Agent tham tu va ra phan quyet cuoi cung ve nguyen nhan va giai phap. Dung khi da co day du evidence tu Janitor, Detective, DocSpec, va Repro.
  tools: Read, Write, Edit, Bash, Glob, Grep
  model: opus
  maxTurns: 30
  effort: high
Body: Copy tu `commands/pd/agents/pd-fix-architect.md`.

Luu y quan trong:
- KHONG xoa hoac sua cac file trong `commands/pd/agents/` (per D-02)
- Tools field dung comma-separated string (Claude Code native format), KHONG dung array YAML
- Description phai cu the de Claude dispatch dung agent (per Pitfall 2 trong RESEARCH)
- Model PHAI ghi ro (haiku/sonnet/opus), KHONG de trong (per Pitfall 1)
  </action>
  <verify>
    <automated>ls -la .claude/agents/pd-*.md | wc -l | grep -q "5" && echo "5 agent files exist" && grep -l "model: haiku" .claude/agents/pd-bug-janitor.md .claude/agents/pd-doc-specialist.md && grep -l "model: sonnet" .claude/agents/pd-code-detective.md .claude/agents/pd-repro-engineer.md && grep -l "model: opus" .claude/agents/pd-fix-architect.md && echo "All model mappings correct"</automated>
  </verify>
  <acceptance_criteria>
    - Thu muc .claude/agents/ ton tai
    - 5 files ton tai: pd-bug-janitor.md, pd-code-detective.md, pd-doc-specialist.md, pd-repro-engineer.md, pd-fix-architect.md
    - pd-bug-janitor.md chua `name: pd-bug-janitor` va `model: haiku` va `maxTurns: 15` va `effort: low`
    - pd-code-detective.md chua `name: pd-code-detective` va `model: sonnet` va `maxTurns: 25` va `effort: medium`
    - pd-doc-specialist.md chua `name: pd-doc-specialist` va `model: haiku` va `maxTurns: 15` va `effort: low`
    - pd-repro-engineer.md chua `name: pd-repro-engineer` va `model: sonnet` va `maxTurns: 25` va `effort: medium`
    - pd-fix-architect.md chua `name: pd-fix-architect` va `model: opus` va `maxTurns: 30` va `effort: high`
    - Moi file co markdown body chua `<objective>` va `<process>` va `<rules>`
    - Cac file trong commands/pd/agents/ VAN TON TAI va KHONG bi sua
  </acceptance_criteria>
  <done>5 agent files ton tai tai .claude/agents/ voi dung YAML frontmatter va markdown body giu nguyen tu reference files</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test — verify agent files khop voi resource-config.js</name>
  <files>test/smoke-agent-files.test.js</files>
  <read_first>
    - bin/lib/resource-config.js (hardcoded values can verify)
    - .claude/agents/pd-bug-janitor.md (vua tao — kiem tra format thuc te)
    - test/smoke-resource-config.test.js (pattern test da dung)
  </read_first>
  <behavior>
    - Test doc 5 agent files tu .claude/agents/ va parse YAML frontmatter
    - Verify moi agent file co fields bat buoc: name, description, tools, model, maxTurns, effort
    - Verify model trong agent file khop voi AGENT_REGISTRY + TIER_MAP trong resource-config.js
    - Verify tools trong agent file khop voi AGENT_REGISTRY trong resource-config.js
    - Verify maxTurns trong agent file khop voi TIER_MAP trong resource-config.js
    - Verify KHONG co agent file nao co model: inherit hoac thieu model field
  </behavior>
  <action>
Tao file `test/smoke-agent-files.test.js`:

```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const { readFileSync } = require('node:fs');
const { join } = require('node:path');
const { AGENT_REGISTRY, TIER_MAP } = require('../bin/lib/resource-config');
```

Helper function `parseAgentFrontmatter(filePath)`:
- Doc file bang readFileSync(filePath, 'utf8')
- Tach YAML frontmatter giua 2 dong `---` (dong dau tien va dong tiep theo)
- Parse tung dong YAML bang regex `/^(\w+):\s*(.+)$/` don gian (KHONG can js-yaml — frontmatter don gian)
- Tra ve object voi cac fields: name, description, tools (array tu comma-separated string), model, maxTurns (parseInt), effort

Cac test:

1. `describe('Agent files existence')` — 1 test:
   - `it('5 agent files ton tai tai .claude/agents/')` — readFileSync moi file, khong throw

2. `describe('Agent files frontmatter')` — 5 tests (1 per agent):
   - Moi test verify: name, model, tools (as array), maxTurns, effort deu co va dung gia tri

3. `describe('Agent files body')` — 1 test:
   - `it('moi agent file co objective, process, rules blocks')` — kiem tra body chua '<objective>', '<process>', '<rules>'

4. `describe('Agent files consistency voi resource-config')` — 2 tests:
   - `it('model trong agent files khop voi TIER_MAP')` — voi moi agent trong AGENT_REGISTRY, doc agent file, verify model trong file === TIER_MAP[agentInfo.tier].model
   - `it('tools trong agent files khop voi AGENT_REGISTRY')` — voi moi agent, verify cac tools trong file chua tat ca tools tu AGENT_REGISTRY

5. `describe('Agent files khong co model inherit')` — 1 test:
   - `it('khong co agent nao co model: inherit')` — kiem tra tat ca 5 files, model !== 'inherit'

Chay: `node --test test/smoke-agent-files.test.js` — tat ca tests PHAI pass.

Luu y: Day la integration test (doc file tu disk) — KHONG phai pure function test. Dat trong file rieng de tach biet tu smoke-resource-config.test.js.
  </action>
  <verify>
    <automated>node --test test/smoke-agent-files.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File test/smoke-agent-files.test.js ton tai
    - File chua `require('node:fs')` (can doc agent files tu disk)
    - File chua `require('../bin/lib/resource-config')` (import AGENT_REGISTRY, TIER_MAP)
    - File chua `describe('Agent files existence'`
    - File chua `describe('Agent files consistency voi resource-config'`
    - `node --test test/smoke-agent-files.test.js` exit code 0 (tat ca tests pass)
  </acceptance_criteria>
  <done>Integration test ton tai, verify 5 agent files co dung format va khop voi resource-config.js, tat ca tests pass</done>
</task>

</tasks>

<verification>
- `ls .claude/agents/pd-*.md | wc -l` tra ve 5
- `node --test test/smoke-agent-files.test.js` — tat ca tests pass
- `node --test test/smoke-resource-config.test.js` — van pass (regression check)
- `ls commands/pd/agents/pd-*.md | wc -l` van tra ve 5 (khong xoa reference files per D-02)
- `grep "model:" .claude/agents/*.md` — moi file co model ro rang (haiku/sonnet/opus), khong co inherit
</verification>

<success_criteria>
- 5 agent files ton tai tai .claude/agents/ voi Claude Code native YAML frontmatter
- Tier mapping dung: Janitor+DocSpec = haiku, Detective+Repro = sonnet, Architect = opus (per D-04)
- Moi agent file co markdown body giu nguyen noi dung tu commands/pd/agents/
- Integration test pass — agent files va resource-config.js dong bo
- Reference files tai commands/pd/agents/ VAN TON TAI (per D-02)
</success_criteria>

<output>
Sau khi hoan thanh, tao `.planning/phases/28-agent-infrastructure-resource-rules/28-02-SUMMARY.md`
</output>
