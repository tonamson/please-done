---
phase: 28-agent-infrastructure-resource-rules
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/resource-config.js
  - test/smoke-resource-config.test.js
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04

must_haves:
  truths:
    - "getModelForTier('scout') tra ve { model: 'haiku', effort: 'low', maxTurns: 15 }"
    - "getModelForTier('builder') tra ve { model: 'sonnet', effort: 'medium', maxTurns: 25 }"
    - "getModelForTier('architect') tra ve { model: 'opus', effort: 'high', maxTurns: 30 }"
    - "getAgentConfig('pd-bug-janitor') tra ve full config voi tier scout, model haiku, tools co Read"
    - "getAgentConfig tra ve config cho tat ca 5 agents: pd-bug-janitor, pd-code-detective, pd-doc-specialist, pd-repro-engineer, pd-fix-architect"
    - "getParallelLimit() tra ve 2"
    - "isHeavyAgent('pd-code-detective') tra ve true vi co mcp__fastcode__ tool"
    - "isHeavyAgent('pd-bug-janitor') tra ve false vi khong co heavy tool"
    - "shouldDegrade tra ve true cho TIMEOUT, RESOURCE_EXHAUSTED, RATE_LIMIT, duration > 120000, message co 'agent.*fail'"
    - "shouldDegrade tra ve false cho error binh thuong va null"
  artifacts:
    - path: "test/smoke-resource-config.test.js"
      provides: "Unit tests cho resource-config module"
      contains: "describe('getModelForTier'"
    - path: "bin/lib/resource-config.js"
      provides: "Pure function module: tier mapping, parallel limit, heavy lock, degradation"
      exports: ["getModelForTier", "getAgentConfig", "getParallelLimit", "isHeavyAgent", "shouldDegrade"]
  key_links:
    - from: "test/smoke-resource-config.test.js"
      to: "bin/lib/resource-config.js"
      via: "require('../bin/lib/resource-config')"
      pattern: "require\\('\\.\\./bin/lib/resource-config'\\)"
---

<objective>
Tao module resource-config.js theo TDD — viet test truoc, implement sau. Module cung cap pure functions de truy van tier mapping, parallel limit, heavy agent detection, va degradation logic.

Purpose: Day la nen tang config cho toan bo v2.1 Detective Orchestrator. Workflow (Phase 32) se dung cac functions nay de quyet dinh model routing, gioi han parallel, va ha cap tu dong.
Output: `bin/lib/resource-config.js` (module) + `test/smoke-resource-config.test.js` (tests), tat ca tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-agent-infrastructure-resource-rules/28-CONTEXT.md
@.planning/phases/28-agent-infrastructure-resource-rules/28-RESEARCH.md

@bin/lib/logic-sync.js
@test/smoke-repro-test-generator.test.js
</context>

<feature>
  <name>resource-config: Pure function module cho agent tier mapping va resource rules</name>
  <files>bin/lib/resource-config.js, test/smoke-resource-config.test.js</files>
  <behavior>
    getModelForTier:
    - getModelForTier('scout') => { model: 'haiku', effort: 'low', maxTurns: 15 }
    - getModelForTier('builder') => { model: 'sonnet', effort: 'medium', maxTurns: 25 }
    - getModelForTier('architect') => { model: 'opus', effort: 'high', maxTurns: 30 }
    - getModelForTier('SCOUT') => { model: 'haiku', ... } (case-insensitive)
    - getModelForTier(null) => throw Error('thieu tham so tier')
    - getModelForTier('invalid') => throw Error('tier khong hop le: invalid')

    getAgentConfig:
    - getAgentConfig('pd-bug-janitor') => { name: 'pd-bug-janitor', tier: 'scout', model: 'haiku', tools: ['Read', 'Glob', 'Grep', 'AskUserQuestion', 'Bash'], maxTurns: 15, effort: 'low' }
    - getAgentConfig('pd-code-detective') => { tier: 'builder', model: 'sonnet', tools: ['Read', 'Glob', 'Grep', 'mcp__fastcode__code_qa'], maxTurns: 25, effort: 'medium' }
    - getAgentConfig('pd-doc-specialist') => { tier: 'scout', model: 'haiku', tools: ['Read', 'mcp__context7__resolve-library-id', 'mcp__context7__query-docs'], maxTurns: 15, effort: 'low' }
    - getAgentConfig('pd-repro-engineer') => { tier: 'builder', model: 'sonnet', tools: ['Read', 'Write', 'Edit', 'Bash'], maxTurns: 25, effort: 'medium' }
    - getAgentConfig('pd-fix-architect') => { tier: 'architect', model: 'opus', tools: ['Read', 'Write', 'Edit', 'Bash', 'Glob', 'Grep'], maxTurns: 30, effort: 'high' }
    - getAgentConfig(null) => throw Error('thieu tham so agentName')
    - getAgentConfig('unknown-agent') => throw Error('agent khong ton tai: unknown-agent')

    getParallelLimit:
    - getParallelLimit() => 2

    isHeavyAgent:
    - isHeavyAgent('pd-code-detective') => true (co mcp__fastcode__code_qa)
    - isHeavyAgent('pd-bug-janitor') => false
    - isHeavyAgent('pd-doc-specialist') => false
    - isHeavyAgent('pd-repro-engineer') => false
    - isHeavyAgent('pd-fix-architect') => false
    - isHeavyAgent('nonexistent') => false

    shouldDegrade:
    - shouldDegrade({ code: 'TIMEOUT' }) => true
    - shouldDegrade({ code: 'OTHER', duration: 130000 }) => true
    - shouldDegrade({ code: 'RESOURCE_EXHAUSTED' }) => true
    - shouldDegrade({ code: 'RATE_LIMIT' }) => true
    - shouldDegrade({ code: 'ERR', message: 'Agent spawn failed' }) => true
    - shouldDegrade({ code: 'SYNTAX_ERROR' }) => false
    - shouldDegrade(null) => false
    - shouldDegrade(undefined) => false

    Constants (export cho testing):
    - TIER_MAP co 3 entries: scout, builder, architect
    - AGENT_REGISTRY co 5 entries: pd-bug-janitor, pd-code-detective, pd-doc-specialist, pd-repro-engineer, pd-fix-architect
    - PARALLEL_LIMIT = 2
    - HEAVY_TOOL_PATTERNS = ['mcp__fastcode__']
  </behavior>
  <implementation>
    RED: Tao test/smoke-resource-config.test.js voi tat ca test cases tren. Chay `node --test test/smoke-resource-config.test.js` — PHAI fail (module chua ton tai).

    GREEN: Tao bin/lib/resource-config.js theo dung pattern cua logic-sync.js:
    - 'use strict' dau file
    - Constants (TIER_MAP, AGENT_REGISTRY, HEAVY_TOOL_PATTERNS, PARALLEL_LIMIT, DEGRADATION_TIMEOUT_MS)
    - Pure functions (getModelForTier, getAgentConfig, getParallelLimit, isHeavyAgent, shouldDegrade)
    - module.exports cuoi file
    - KHONG require('fs') — KHONG doc file (per D-07)

    Chay `node --test test/smoke-resource-config.test.js` — PHAI pass tat ca.

    REFACTOR (neu can): Clean up code style, them JSDoc comments day du.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Viet tests cho resource-config module</name>
  <files>test/smoke-resource-config.test.js</files>
  <read_first>
    - test/smoke-repro-test-generator.test.js (pattern mau cho test structure)
    - test/smoke-logic-sync.test.js (pattern mau cho module co nhieu functions)
    - bin/lib/logic-sync.js (pattern module dich — hieu export style)
  </read_first>
  <action>
Tao file `test/smoke-resource-config.test.js` theo dung pattern cua smoke-repro-test-generator.test.js.

Dau file:
```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const {
  getModelForTier, getAgentConfig, getParallelLimit,
  isHeavyAgent, shouldDegrade, TIER_MAP, AGENT_REGISTRY,
  PARALLEL_LIMIT, HEAVY_TOOL_PATTERNS,
} = require('../bin/lib/resource-config');
```

Tao cac describe blocks:

1. `describe('getModelForTier')` — 5 tests:
   - `it('tra ve haiku cho scout')` — assert.equal(result.model, 'haiku'), assert.equal(result.effort, 'low'), assert.equal(result.maxTurns, 15)
   - `it('tra ve sonnet cho builder')` — assert.equal(result.model, 'sonnet'), assert.equal(result.effort, 'medium'), assert.equal(result.maxTurns, 25)
   - `it('tra ve opus cho architect')` — assert.equal(result.model, 'opus'), assert.equal(result.effort, 'high'), assert.equal(result.maxTurns, 30)
   - `it('throw khi tier null')` — assert.throws(() => getModelForTier(null), /thieu tham so/)
   - `it('throw khi tier khong hop le')` — assert.throws(() => getModelForTier('invalid'), /khong hop le/)

2. `describe('getAgentConfig')` — 7 tests:
   - `it('tra ve full config cho pd-bug-janitor')` — assert tier='scout', model='haiku', tools includes 'Read', 'Glob', 'Grep', 'AskUserQuestion', 'Bash', maxTurns=15
   - `it('tra ve full config cho pd-code-detective')` — assert tier='builder', model='sonnet', tools includes 'mcp__fastcode__code_qa'
   - `it('tra ve full config cho pd-doc-specialist')` — assert tier='scout', model='haiku', tools includes 'mcp__context7__resolve-library-id', 'mcp__context7__query-docs'
   - `it('tra ve full config cho pd-repro-engineer')` — assert tier='builder', model='sonnet', tools includes 'Write', 'Edit', 'Bash'
   - `it('tra ve full config cho pd-fix-architect')` — assert tier='architect', model='opus', tools includes 'Glob', 'Grep'
   - `it('throw khi agent null')` — assert.throws(() => getAgentConfig(null), /thieu tham so/)
   - `it('throw khi agent khong ton tai')` — assert.throws(() => getAgentConfig('unknown'), /khong ton tai/)

3. `describe('getParallelLimit')` — 1 test:
   - `it('tra ve 2')` — assert.equal(getParallelLimit(), 2)

4. `describe('isHeavyAgent')` — 4 tests:
   - `it('true cho pd-code-detective (co fastcode)')` — assert.equal(isHeavyAgent('pd-code-detective'), true)
   - `it('false cho pd-bug-janitor')` — assert.equal(isHeavyAgent('pd-bug-janitor'), false)
   - `it('false cho pd-fix-architect')` — assert.equal(isHeavyAgent('pd-fix-architect'), false)
   - `it('false cho agent khong ton tai')` — assert.equal(isHeavyAgent('nonexistent'), false)

5. `describe('shouldDegrade')` — 7 tests:
   - `it('true khi TIMEOUT')` — assert.equal(shouldDegrade({ code: 'TIMEOUT' }), true)
   - `it('true khi duration > 120000')` — assert.equal(shouldDegrade({ code: 'OTHER', duration: 130000 }), true)
   - `it('true khi RESOURCE_EXHAUSTED')` — assert.equal(shouldDegrade({ code: 'RESOURCE_EXHAUSTED' }), true)
   - `it('true khi RATE_LIMIT')` — assert.equal(shouldDegrade({ code: 'RATE_LIMIT' }), true)
   - `it('true khi message co agent fail')` — assert.equal(shouldDegrade({ code: 'ERR', message: 'Agent spawn failed' }), true)
   - `it('false khi error binh thuong')` — assert.equal(shouldDegrade({ code: 'SYNTAX_ERROR' }), false)
   - `it('false khi null')` — assert.equal(shouldDegrade(null), false)

6. `describe('Constants')` — 4 tests:
   - `it('TIER_MAP co 3 tiers')` — assert.equal(Object.keys(TIER_MAP).length, 3)
   - `it('AGENT_REGISTRY co 5 agents')` — assert.equal(Object.keys(AGENT_REGISTRY).length, 5)
   - `it('PARALLEL_LIMIT la 2')` — assert.equal(PARALLEL_LIMIT, 2)
   - `it('HEAVY_TOOL_PATTERNS co mcp__fastcode__')` — assert.ok(HEAVY_TOOL_PATTERNS.includes('mcp__fastcode__'))

Chay: `node --test test/smoke-resource-config.test.js` — ket qua PHAI fail (module chua ton tai).
Commit: `test(28-01): them failing tests cho resource-config module`
  </action>
  <verify>
    <automated>node --test test/smoke-resource-config.test.js 2>&1 | grep -q "failing\|ERR_MODULE_NOT_FOUND\|Cannot find module" && echo "RED phase OK: tests fail as expected"</automated>
  </verify>
  <acceptance_criteria>
    - File test/smoke-resource-config.test.js ton tai
    - File chua `require('../bin/lib/resource-config')` o dong import
    - File chua `describe('getModelForTier'` (it nhat 5 it blocks)
    - File chua `describe('getAgentConfig'` (it nhat 7 it blocks)
    - File chua `describe('getParallelLimit'`
    - File chua `describe('isHeavyAgent'`
    - File chua `describe('shouldDegrade'`
    - File chua `describe('Constants'`
    - Chay `node --test test/smoke-resource-config.test.js` PHAI fail (module chua ton tai)
  </acceptance_criteria>
  <done>File test ton tai voi 28+ test cases, tat ca fail vi module chua co</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement resource-config.js de pass tat ca tests</name>
  <files>bin/lib/resource-config.js</files>
  <read_first>
    - test/smoke-resource-config.test.js (vua tao o Task 1 — doc de biet tests expect gi)
    - bin/lib/logic-sync.js (pattern mau cho module structure: 'use strict', constants, functions, module.exports)
    - bin/lib/repro-test-generator.js (pattern mau cho pure function don gian)
  </read_first>
  <action>
Tao file `bin/lib/resource-config.js` theo dung pattern cua logic-sync.js (per D-06, D-07).

Structure:
```javascript
'use strict';

// ─── Constants ────────────────────────────────────────────

const TIER_MAP = {
  scout:     { model: 'haiku',  effort: 'low',    maxTurns: 15 },
  builder:   { model: 'sonnet', effort: 'medium', maxTurns: 25 },
  architect: { model: 'opus',   effort: 'high',   maxTurns: 30 },
};

const AGENT_REGISTRY = {
  'pd-bug-janitor':      { tier: 'scout',     tools: ['Read', 'Glob', 'Grep', 'AskUserQuestion', 'Bash'] },
  'pd-code-detective':   { tier: 'builder',   tools: ['Read', 'Glob', 'Grep', 'mcp__fastcode__code_qa'] },
  'pd-doc-specialist':   { tier: 'scout',     tools: ['Read', 'mcp__context7__resolve-library-id', 'mcp__context7__query-docs'] },
  'pd-repro-engineer':   { tier: 'builder',   tools: ['Read', 'Write', 'Edit', 'Bash'] },
  'pd-fix-architect':    { tier: 'architect',  tools: ['Read', 'Write', 'Edit', 'Bash', 'Glob', 'Grep'] },
};

const HEAVY_TOOL_PATTERNS = ['mcp__fastcode__'];
const PARALLEL_LIMIT = 2;
const DEGRADATION_TIMEOUT_MS = 120_000;
```

Functions (xem chi tiet trong 28-RESEARCH.md phan Code Examples):
- `getModelForTier(tier)` — validate tier, tra ve copy cua TIER_MAP entry
- `getAgentConfig(agentName)` — validate agentName, tra ve merged object tu AGENT_REGISTRY + TIER_MAP
- `getParallelLimit()` — return PARALLEL_LIMIT
- `isHeavyAgent(agentName)` — check agent tools co match HEAVY_TOOL_PATTERNS
- `shouldDegrade(error)` — check error.code, error.duration, error.message

module.exports cuoi file:
```javascript
module.exports = {
  getModelForTier, getAgentConfig, getParallelLimit,
  isHeavyAgent, shouldDegrade,
  TIER_MAP, AGENT_REGISTRY, PARALLEL_LIMIT, HEAVY_TOOL_PATTERNS,
};
```

KHONG require('fs') — KHONG doc file (per D-07).
KHONG require bat ky module ben ngoai nao — zero dependencies.

Chay: `node --test test/smoke-resource-config.test.js` — tat ca 28+ tests PHAI pass.
Commit: `feat(28-01): implement resource-config module voi 5 pure functions`
  </action>
  <verify>
    <automated>node --test test/smoke-resource-config.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File bin/lib/resource-config.js ton tai
    - File bat dau voi `'use strict';`
    - File KHONG chua `require('fs')` hoac `require('node:fs')`
    - File chua `const TIER_MAP =` voi 3 entries (scout, builder, architect)
    - File chua `const AGENT_REGISTRY =` voi 5 entries
    - File chua `const PARALLEL_LIMIT = 2`
    - File chua `const HEAVY_TOOL_PATTERNS = ['mcp__fastcode__']`
    - File chua `function getModelForTier(tier)`
    - File chua `function getAgentConfig(agentName)`
    - File chua `function getParallelLimit()`
    - File chua `function isHeavyAgent(agentName)`
    - File chua `function shouldDegrade(error)`
    - File chua `module.exports =` voi tat ca 5 functions va 4 constants
    - `node --test test/smoke-resource-config.test.js` exit code 0 (tat ca tests pass)
  </acceptance_criteria>
  <done>Module resource-config.js ton tai, tat ca 28+ tests pass, KHONG co fs import</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-resource-config.test.js` — tat ca tests pass
- `grep -c "require('fs')" bin/lib/resource-config.js` tra ve 0 (khong doc file)
- `grep -c "module.exports" bin/lib/resource-config.js` tra ve 1
- `node -e "const m = require('./bin/lib/resource-config'); console.log(Object.keys(m).join(', '))"` — in ra 9 exports
</verification>

<success_criteria>
- resource-config.js ton tai tai bin/lib/ voi 5 pure functions + 4 exported constants
- 28+ unit tests pass trong test/smoke-resource-config.test.js
- Module KHONG co bat ky I/O nao (khong fs, khong fetch, khong process)
- Tier mapping dung: scout=haiku, builder=sonnet, architect=opus (per D-04)
- Parallel limit = 2 (per ORCH-02)
- Heavy agent detection dua tren mcp__fastcode__ pattern (per ORCH-03, D-09)
- Degradation logic cover TIMEOUT, RESOURCE_EXHAUSTED, RATE_LIMIT, agent fail message (per ORCH-04, D-11)
</success_criteria>

<output>
Sau khi hoan thanh, tao `.planning/phases/28-agent-infrastructure-resource-rules/28-01-SUMMARY.md`
</output>
