---
phase: 056-skill-agent-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/parallel-dispatch.js
  - bin/lib/init.cjs
autonomous: true
requirements:
  - SKIL-02
  - SKIL-04

must_haves:
  truths:
    - "parallel-dispatch.js export buildResearchSquadPlan() tao plan cho 3 research agents"
    - "parallel-dispatch.js export mergeResearchResults() hop nhat ket qua research"
    - "init.cjs tra ve strategy_path trong plan-phase init JSON"
  artifacts:
    - path: "bin/lib/parallel-dispatch.js"
      provides: "buildResearchSquadPlan() va mergeResearchResults() functions"
      exports: ["buildResearchSquadPlan", "mergeResearchResults"]
    - path: "bin/lib/init.cjs"
      provides: "strategy_path field trong plan-phase init output"
      contains: "strategy_path"
  key_links:
    - from: "bin/lib/parallel-dispatch.js"
      to: "bin/lib/resource-config.js"
      via: "getAgentConfig() calls cho pd-codebase-mapper, pd-security-researcher, pd-feature-analyst"
      pattern: "getAgentConfig"
    - from: "bin/lib/init.cjs"
      to: ".planning/research/TECHNICAL_STRATEGY.md"
      via: "file existence check de set strategy_path"
      pattern: "TECHNICAL_STRATEGY"
---

<objective>
Tao runtime functions cho Research Squad dispatch va them strategy_path vao GSD init output.

Purpose: Cung cap buildResearchSquadPlan() de spawn 3 PD research agents song song, va strategy_path de plan-phase inject TECHNICAL_STRATEGY.md.
Output: 2 runtime files cap nhat (parallel-dispatch.js, init.cjs)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/056-skill-agent-integration/056-CONTEXT.md

<interfaces>
<!-- parallel-dispatch.js exports: buildParallelPlan, mergeParallelResults, buildScannerPlan, mergeScannerResults -->
<!-- resource-config.js exports: getAgentConfig, getAdaptiveParallelLimit, isHeavyAgent, shouldDegrade, PARALLEL_MIN, PARALLEL_MAX -->

<!-- Agent names for Research Squad:
  - pd-codebase-mapper (Scout tier, haiku)
  - pd-security-researcher (Scout tier, haiku)
  - pd-feature-analyst (Scout tier, haiku)
  - pd-research-synthesizer (Architect tier, opus) — runs AFTER 3 agents complete
-->

<!-- init.cjs cmdInitPlanPhase() returns JSON with state_path, roadmap_path, requirements_path, context_path, research_path, etc. -->
<!-- Need to add strategy_path pointing to .planning/research/TECHNICAL_STRATEGY.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Them buildResearchSquadPlan() va mergeResearchResults() vao parallel-dispatch.js (SKIL-02)</name>
  <files>bin/lib/parallel-dispatch.js</files>
  <read_first>
    - bin/lib/parallel-dispatch.js
    - bin/lib/resource-config.js
    - .claude/agents/pd-codebase-mapper.md
    - .claude/agents/pd-security-researcher.md
    - .claude/agents/pd-feature-analyst.md
    - .claude/agents/pd-research-synthesizer.md
  </read_first>
  <action>
Them 2 functions moi vao parallel-dispatch.js, dat TRUOC section `// --- Exports ---`.

**Function 1: buildResearchSquadPlan(outputDir)**

```javascript
// --- buildResearchSquadPlan -----------------------------------------

/**
 * Tao ke hoach spawn 3 PD research agents song song (SKIL-02).
 * pd-codebase-mapper, pd-security-researcher, pd-feature-analyst chay dong thoi.
 * pd-research-synthesizer chay SAU khi 3 agents hoan thanh (caller xu ly sequential).
 *
 * @param {string} outputDir - Path toi output directory (vd: '.planning/research')
 * @returns {{ agents: Array<{name: string, config: object, outputFile: string, critical: boolean}>, synthesizer: {name: string, config: object, outputFile: string}, warnings: string[] }}
 */
function buildResearchSquadPlan(outputDir) {
  const warnings = [];

  const mapperConfig = getAgentConfig('pd-codebase-mapper');
  const securityConfig = getAgentConfig('pd-security-researcher');
  const featureConfig = getAgentConfig('pd-feature-analyst');
  const synthesizerConfig = getAgentConfig('pd-research-synthesizer');

  return {
    agents: [
      {
        name: 'pd-codebase-mapper',
        config: mapperConfig,
        outputFile: 'codebase/',
        critical: false,
      },
      {
        name: 'pd-security-researcher',
        config: securityConfig,
        outputFile: 'evidence_security_research.md',
        critical: false,
      },
      {
        name: 'pd-feature-analyst',
        config: featureConfig,
        outputFile: 'evidence_features.md',
        critical: false,
      },
    ],
    synthesizer: {
      name: 'pd-research-synthesizer',
      config: synthesizerConfig,
      outputFile: 'TECHNICAL_STRATEGY.md',
    },
    warnings,
  };
}
```

Per D-06: 3 agents chay song song.
Per D-07: synthesizer chay SAU 3 agents (caller xu ly sequential step).
Per D-08: individual agent failure khong block — synthesizer van chay voi evidence co san (da xu ly trong agent file).

**Function 2: mergeResearchResults(researchResults)**

```javascript
// --- mergeResearchResults -------------------------------------------

/**
 * Hop nhat ket qua tu 3 research agents (SKIL-02).
 * Agent fail → ghi warning, van cho phep synthesizer chay.
 *
 * @param {Array<{agent: string, success: boolean, error?: {message: string}}>} researchResults
 * @returns {{ completedCount: number, failedCount: number, canSynthesize: boolean, warnings: string[] }}
 */
function mergeResearchResults(researchResults) {
  const warnings = [];
  let completedCount = 0;
  let failedCount = 0;

  for (const item of researchResults) {
    if (item.success) {
      completedCount++;
    } else {
      failedCount++;
      const errMsg = item.error?.message || 'Khong co ket qua';
      warnings.push(`${item.agent}: research that bai — ${errMsg}`);
    }
  }

  // D-08: synthesizer van chay neu co bat ky evidence nao
  const canSynthesize = completedCount > 0;

  return { completedCount, failedCount, canSynthesize, warnings };
}
```

**Cap nhat exports:**
Thay dong `module.exports = { buildParallelPlan, mergeParallelResults, buildScannerPlan, mergeScannerResults };` thanh:
```javascript
module.exports = { buildParallelPlan, mergeParallelResults, buildScannerPlan, mergeScannerResults, buildResearchSquadPlan, mergeResearchResults };
```
  </action>
  <verify>
    <automated>node -e "const m = require('./bin/lib/parallel-dispatch.js'); console.log('buildResearchSquadPlan:', typeof m.buildResearchSquadPlan); console.log('mergeResearchResults:', typeof m.mergeResearchResults); const plan = m.buildResearchSquadPlan('.planning/research'); console.log('agents:', plan.agents.length); console.log('synthesizer:', plan.synthesizer.name); const res = m.mergeResearchResults([{agent:'a',success:true},{agent:'b',success:false,error:{message:'fail'}}]); console.log('canSynthesize:', res.canSynthesize, 'completed:', res.completedCount, 'failed:', res.failedCount);"</automated>
  </verify>
  <acceptance_criteria>
    - parallel-dispatch.js chua `function buildResearchSquadPlan(outputDir)` (grep xac nhan)
    - parallel-dispatch.js chua `function mergeResearchResults(researchResults)` (grep xac nhan)
    - module.exports chua `buildResearchSquadPlan` va `mergeResearchResults`
    - buildResearchSquadPlan('.planning/research') tra ve object co agents (length 3), synthesizer.name === 'pd-research-synthesizer'
    - mergeResearchResults([{agent:'a',success:true}]) tra ve canSynthesize === true, completedCount === 1
    - mergeResearchResults([{agent:'a',success:false}]) tra ve canSynthesize === false, failedCount === 1
    - Cac function cu (buildParallelPlan, mergeParallelResults, buildScannerPlan, mergeScannerResults) van export binh thuong
  </acceptance_criteria>
  <done>parallel-dispatch.js export buildResearchSquadPlan() va mergeResearchResults(), ca 2 hoat dong dung, exports cu khong bi anh huong</done>
</task>

<task type="auto">
  <name>Task 2: Them strategy_path vao init.cjs plan-phase output (SKIL-04)</name>
  <files>bin/lib/init.cjs</files>
  <read_first>
    - bin/lib/init.cjs
  </read_first>
  <action>
Sua function `cmdInitPlanPhase()` trong bin/lib/init.cjs. Tim block noi cac file paths duoc resolve (khoang dong 199-230 — noi co state_path, roadmap_path, requirements_path, context_path, research_path...).

Them strategy_path check SAU block `if (phaseInfo?.directory)` (sau dong 230), TRUOC dong `output(withProjectRoot(cwd, result), raw);`:

```javascript
  // SKIL-04: strategy_path cho TECHNICAL_STRATEGY.md injection
  const strategyFile = path.join(planningDir(cwd), 'research', 'TECHNICAL_STRATEGY.md');
  if (fs.existsSync(strategyFile)) {
    result.strategy_path = toPosixPath(path.relative(cwd, strategyFile));
  }
```

Dieu nay dam bao:
- Per D-14: Neu file khong ton tai → strategy_path khong co trong JSON (undefined/null) → plan-phase skip injection
- Per D-12: Chi inject vao pd-planner context (plan-phase.md la noi pd-planner duoc spawn)
- Per D-13: Inject toan bo file (plan-phase.md se dung Read de doc full file)
  </action>
  <verify>
    <automated>grep -n "strategy_path\|TECHNICAL_STRATEGY" bin/lib/init.cjs</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/init.cjs chua `strategy_path` (grep xac nhan)
    - bin/lib/init.cjs chua `TECHNICAL_STRATEGY.md` (grep xac nhan)
    - bin/lib/init.cjs chua `fs.existsSync(strategyFile)` (conditional check)
    - strategy_path chi duoc set khi file ton tai (khong hardcode path khi file missing)
    - Code dat trong cmdInitPlanPhase function (khong anh huong functions khac)
  </acceptance_criteria>
  <done>init.cjs tra ve strategy_path trong plan-phase init JSON khi TECHNICAL_STRATEGY.md ton tai, skip khi khong co</done>
</task>

</tasks>

<verification>
- node -e "const m = require('./bin/lib/parallel-dispatch.js'); console.log(Object.keys(m));" → bao gom buildResearchSquadPlan, mergeResearchResults
- grep "strategy_path" bin/lib/init.cjs → co ket qua
- npm test (neu co) → tat ca tests hien tai van pass
</verification>

<success_criteria>
- buildResearchSquadPlan() tao plan cho 3 PD research agents + synthesizer metadata
- mergeResearchResults() hop nhat results va xac dinh canSynthesize
- init.cjs plan-phase output co strategy_path khi TECHNICAL_STRATEGY.md ton tai
</success_criteria>

<output>
After completion, create `.planning/phases/056-skill-agent-integration/056-02-SUMMARY.md`
</output>
