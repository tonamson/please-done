---
phase: 02-cross-skill-deduplication
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - commands/pd/write-code.md
  - commands/pd/test.md
  - commands/pd/fix-bug.md
  - commands/pd/plan.md
  - commands/pd/init.md
  - commands/pd/scan.md
  - commands/pd/new-milestone.md
  - commands/pd/complete-milestone.md
  - commands/pd/fetch-doc.md
  - commands/pd/what-next.md
  - commands/pd/update.md
  - commands/pd/conventions.md
autonomous: true
requirements: [TOKN-01]

must_haves:
  truths:
    - "No skill file contains duplicated guard text that exists in a guard micro-template"
    - "Each guard micro-template is referenced by 2+ skills"
    - "Updating a shared guard requires changing exactly one file"
    - "All converters still produce correct output for all 12 skills"
  artifacts:
    - path: "commands/pd/write-code.md"
      provides: "Deduplicated guards with @references/guard-context.md, guard-fastcode.md, guard-context7.md"
      contains: "@references/guard-context.md"
    - path: "commands/pd/test.md"
      provides: "Deduplicated guards with @references/guard-context.md, guard-fastcode.md, guard-context7.md"
      contains: "@references/guard-context.md"
    - path: "commands/pd/fix-bug.md"
      provides: "Deduplicated guards with @references/guard-context.md, guard-fastcode.md, guard-context7.md"
      contains: "@references/guard-context.md"
    - path: "commands/pd/plan.md"
      provides: "Deduplicated guards with @references/guard-context.md, guard-fastcode.md, guard-context7.md"
      contains: "@references/guard-context.md"
    - path: "commands/pd/init.md"
      provides: "Deduplicated guards with @references/guard-valid-path.md, guard-fastcode.md"
      contains: "@references/guard-fastcode.md"
    - path: "commands/pd/scan.md"
      provides: "Deduplicated guards with @references/guard-context.md, guard-valid-path.md, guard-fastcode.md"
      contains: "@references/guard-context.md"
  key_links:
    - from: "commands/pd/write-code.md"
      to: "references/guard-context.md"
      via: "@references/guard-context.md in <guards>"
      pattern: "@references/guard-context\\.md"
    - from: "commands/pd/init.md"
      to: "references/guard-fastcode.md"
      via: "@references/guard-fastcode.md in <guards>"
      pattern: "@references/guard-fastcode\\.md"
    - from: "test/smoke-integrity.test.js"
      to: "commands/pd/*.md"
      via: "guard deduplication verification tests"
      pattern: "guard.*trung lap"
---

<objective>
Replace inline guard text in all 12 skill files with @references/guard-*.md references, completing the cross-skill deduplication.

Purpose: Eliminate guard text duplication across 12 skills so that updating a shared guard requires changing exactly one file. This turns the TDD RED tests from Plan 01 GREEN.
Output: 12 updated skill files with deduplicated guards, all tests passing including the new deduplication tests.
</objective>

<execution_context>
@/Users/tonamson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cross-skill-deduplication/02-CONTEXT.md
@.planning/phases/02-cross-skill-deduplication/02-RESEARCH.md
@.planning/phases/02-cross-skill-deduplication/02-01-SUMMARY.md

<interfaces>
<!-- Guard micro-templates created by Plan 01 -->

From references/guard-context.md:
```
- [ ] `.planning/CONTEXT.md` ton tai -> "Chay `/pd:init` truoc."
```

From references/guard-fastcode.md:
```
- [ ] FastCode MCP ket noi thanh cong -> "Kiem tra Docker dang chay va FastCode MCP da duoc cau hinh."
```

From references/guard-context7.md:
```
- [ ] Context7 MCP ket noi thanh cong -> "Kiem tra Context7 MCP da duoc cau hinh."
```

From references/guard-valid-path.md:
```
- [ ] Tham so path hop le (neu co) -> "Path khong ton tai hoac khong phai thu muc."
```

<!-- Guard usage map (which skills use which guards): -->
<!-- guard-context.md: write-code, test, fix-bug, plan, scan, new-milestone, complete-milestone, fetch-doc (8 skills) -->
<!-- guard-fastcode.md: write-code, test, fix-bug, plan, init, scan (6 skills + init has same wording) -->
<!-- guard-context7.md: write-code, test, fix-bug, plan, new-milestone (5 skills) -->
<!-- guard-valid-path.md: init, scan (2 skills) -->

<!-- Diacritical normalization needed for: plan.md, new-milestone.md, complete-milestone.md, fetch-doc.md -->
<!-- These skills currently use diacritical Vietnamese in guards but guard templates use non-diacritical form -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deduplicate guards in batch 1 (non-diacritical skills: init, scan, write-code, test, fix-bug, conventions)</name>
  <files>commands/pd/init.md, commands/pd/scan.md, commands/pd/write-code.md, commands/pd/test.md, commands/pd/fix-bug.md, commands/pd/conventions.md</files>
  <action>
    For each of the 6 skills that already use non-diacritical Vietnamese in guards, replace the inline guard text with @references/guard-*.md references. The replacement is a direct line-for-line swap since these skills already match the guard template wording exactly.

    **init.md guards -- replace:**
    ```
    - [ ] Tham so path hop le (neu co) -> "Path khong ton tai hoac khong phai thu muc."
    - [ ] FastCode MCP ket noi thanh cong -> "Kiem tra Docker dang chay va FastCode MCP da duoc cau hinh."
    ```
    **with:**
    ```
    @references/guard-valid-path.md
    @references/guard-fastcode.md
    ```

    **scan.md guards -- replace:**
    ```
    - [ ] `.planning/CONTEXT.md` ton tai -> "Chay `/pd:init` truoc."
    - [ ] Tham so path hop le (neu co) -> "Path khong ton tai hoac khong phai thu muc."
    - [ ] FastCode MCP ket noi thanh cong -> "Kiem tra Docker dang chay va FastCode MCP da duoc cau hinh."
    ```
    **with:**
    ```
    @references/guard-context.md
    @references/guard-valid-path.md
    @references/guard-fastcode.md
    ```

    **write-code.md guards -- replace:**
    ```
    - [ ] `.planning/CONTEXT.md` ton tai -> "Chay `/pd:init` truoc."
    (keep unique) - [ ] Task number hop le hoac co flag --auto/--parallel -> ...
    (keep unique) - [ ] PLAN.md + TASKS.md ton tai cho phase hien tai -> ...
    - [ ] FastCode MCP ket noi thanh cong -> "Kiem tra Docker dang chay va FastCode MCP da duoc cau hinh."
    - [ ] Context7 MCP ket noi thanh cong -> "Kiem tra Context7 MCP da duoc cau hinh."
    ```
    **with:**
    ```
    @references/guard-context.md
    - [ ] Task number hop le hoac co flag --auto/--parallel -> "Cung cap so task hoac flag che do."
    - [ ] PLAN.md + TASKS.md ton tai cho phase hien tai -> "Chay `/pd:plan` truoc de tao plan."
    @references/guard-fastcode.md
    @references/guard-context7.md
    ```

    **test.md guards -- replace:**
    ```
    - [ ] `.planning/CONTEXT.md` ton tai -> "Chay `/pd:init` truoc."
    (keep unique) - [ ] Task number hop le hoac co flag --all -> ...
    - [ ] FastCode MCP ket noi thanh cong -> ...
    - [ ] Context7 MCP ket noi thanh cong -> ...
    (keep unique) - [ ] Co it nhat 1 task voi trang thai done -> ...
    ```
    **with:**
    ```
    @references/guard-context.md
    - [ ] Task number hop le hoac co flag --all -> "Cung cap so task hoac dung `--all`."
    @references/guard-fastcode.md
    @references/guard-context7.md
    - [ ] Co it nhat 1 task voi trang thai done -> "Chua co task nao hoan thanh. Chay `/pd:write-code` truoc."
    ```

    **fix-bug.md guards -- replace:**
    ```
    - [ ] `.planning/CONTEXT.md` ton tai -> "Chay `/pd:init` truoc."
    (keep unique) - [ ] Mo ta loi duoc cung cap -> ...
    - [ ] FastCode MCP ket noi thanh cong -> ...
    - [ ] Context7 MCP ket noi thanh cong -> ...
    ```
    **with:**
    ```
    @references/guard-context.md
    - [ ] Mo ta loi duoc cung cap -> "Cung cap mo ta loi hoac ten phien dieu tra."
    @references/guard-fastcode.md
    @references/guard-context7.md
    ```

    **conventions.md:** This skill has NO shared guards (its only guard is unique: "Thu muc du an co source code"). Do NOT modify it. Leave as-is.

    CRITICAL rules:
    - Keep the `DUNG va huong dan user neu bat ky dieu kien nao that bai:` preamble line
    - Keep skill-specific unique guards as inline checklist items
    - Place @references/ lines exactly where the original guard line was (preserve ordering)
    - Do NOT modify any section other than `<guards>`
    - Do NOT add guard refs to `<execution_context>` (per user decision, guards reference in guards section is sufficient; execution_context lists workflow/template/reference docs for reading, not guard templates)
  </action>
  <verify>
    <automated>node --test test/smoke-integrity.test.js 2>&1 | grep -E '(pass|fail)'</automated>
  </verify>
  <done>
    - 5 skill files updated (init, scan, write-code, test, fix-bug) with @references/guard-*.md replacing inline guard text
    - conventions.md unchanged (no shared guards)
    - All skills still have valid <guards> sections
    - Canonical structure tests still pass
    - Existing reference existence tests still pass (guard files exist from Plan 01)
  </done>
</task>

<task type="auto">
  <name>Task 2: Deduplicate guards in batch 2 (diacritical skills: plan, new-milestone, complete-milestone, fetch-doc, what-next, update)</name>
  <files>commands/pd/plan.md, commands/pd/new-milestone.md, commands/pd/complete-milestone.md, commands/pd/fetch-doc.md, commands/pd/what-next.md, commands/pd/update.md</files>
  <action>
    For each of the 6 skills that currently use diacritical Vietnamese in guards, replace matching guard text with @references/guard-*.md references. These skills need BOTH the inline text replaced AND the guard preamble line normalized to non-diacritical form where applicable.

    **plan.md guards -- current:**
    ```
    DỪNG và hướng dẫn user nếu bất kỳ điều kiện nào thất bại:

    - [ ] `.planning/CONTEXT.md` tồn tại -> "Chạy `/pd:init` trước."
    - [ ] `.planning/ROADMAP.md` tồn tại -> "Chạy `/pd:new-milestone` trước."
    - [ ] `.planning/CURRENT_MILESTONE.md` tồn tại -> "Thiếu CURRENT_MILESTONE.md. Chạy `/pd:new-milestone` để tạo."
    - [ ] FastCode MCP kết nối thành công (mcp__fastcode__code_qa)
    - [ ] Context7 MCP kết nối thành công (mcp__context7__resolve-library-id)
    ```
    **replace with:**
    ```
    DUNG va huong dan user neu bat ky dieu kien nao that bai:

    @references/guard-context.md
    - [ ] `.planning/ROADMAP.md` ton tai -> "Chay `/pd:new-milestone` truoc."
    - [ ] `.planning/CURRENT_MILESTONE.md` ton tai -> "Thieu CURRENT_MILESTONE.md. Chay `/pd:new-milestone` de tao."
    @references/guard-fastcode.md
    @references/guard-context7.md
    ```
    Note: Normalize preamble to non-diacritical. Normalize unique guards to non-diacritical too. Replace shared guards with refs.

    **new-milestone.md guards -- current:**
    ```
    DỪNG và hướng dẫn user nếu bất kỳ điều kiện nào thất bại:

    - [ ] `.planning/CONTEXT.md` tồn tại -> "Chạy `/pd:init` trước."
    - [ ] `.planning/rules/general.md` tồn tại -> "Rules bị thiếu. Chạy `/pd:init` để tạo lại."
    - [ ] Tên milestone được cung cấp hoặc sẽ hỏi user nếu không có
    - [ ] Context7 MCP kết nối thành công (mcp__context7__resolve-library-id)
    - [ ] WebSearch khả dụng cho nghiên cứu
    ```
    **replace with:**
    ```
    DUNG va huong dan user neu bat ky dieu kien nao that bai:

    @references/guard-context.md
    - [ ] `.planning/rules/general.md` ton tai -> "Rules bi thieu. Chay `/pd:init` de tao lai."
    - [ ] Ten milestone duoc cung cap hoac se hoi user neu khong co
    @references/guard-context7.md
    - [ ] WebSearch kha dung cho nghien cuu
    ```

    **complete-milestone.md guards -- current:**
    ```
    DỪNG và hướng dẫn user nếu bất kỳ điều kiện nào thất bại:

    - [ ] `.planning/CONTEXT.md` tồn tại -> "Chạy `/pd:init` trước."
    - [ ] Tất cả tasks trong milestone có status hoàn thành -> "Còn tasks chưa xong. Hoàn thành trước khi đóng milestone."
    - [ ] Không có bugs mở chưa giải quyết -> "Còn bugs mở. Chạy `/pd:fix-bug` để sửa trước."
    ```
    **replace with:**
    ```
    DUNG va huong dan user neu bat ky dieu kien nao that bai:

    @references/guard-context.md
    - [ ] Tat ca tasks trong milestone co status hoan thanh -> "Con tasks chua xong. Hoan thanh truoc khi dong milestone."
    - [ ] Khong co bugs mo chua giai quyet -> "Con bugs mo. Chay `/pd:fix-bug` de sua truoc."
    ```

    **fetch-doc.md guards -- current:**
    ```
    DỪNG và hướng dẫn user nếu bất kỳ điều kiện nào thất bại:

    - [ ] `.planning/CONTEXT.md` tồn tại -> "Chạy `/pd:init` trước." (nếu thiếu `.planning/docs/` thì tạo `mkdir -p .planning/docs`)
    - [ ] URL hợp lệ (có protocol http/https) -> "URL không hợp lệ. Kiểm tra lại."
    - [ ] WebFetch tool khả dụng -> "WebFetch không khả dụng. Kiểm tra cài đặt MCP."
    ```
    **replace with:**
    ```
    DUNG va huong dan user neu bat ky dieu kien nao that bai:

    @references/guard-context.md
    - [ ] URL hop le (co protocol http/https) -> "URL khong hop le. Kiem tra lai."
    - [ ] WebFetch tool kha dung -> "WebFetch khong kha dung. Kiem tra cai dat MCP."
    ```
    Note: fetch-doc's CONTEXT.md guard had a parenthetical note about creating .planning/docs/. That note is about process behavior, not the guard check itself. The guard template covers the check; the process section already handles creating .planning/docs/.

    **what-next.md guards -- current:**
    ```
    DỪNG và hướng dẫn user nếu bất kỳ điều kiện nào thất bại:

    - [ ] Thư mục `.planning/` tồn tại -> "Chưa khởi tạo dự án. Chạy `/pd:init` trước."
    ```
    **replace with:**
    ```
    DUNG va huong dan user neu bat ky dieu kien nao that bai:

    - [ ] Thu muc `.planning/` ton tai -> "Chua khoi tao du an. Chay `/pd:init` truoc."
    ```
    Note: what-next checks `.planning/` directory, NOT `.planning/CONTEXT.md`. This is a DIFFERENT guard -- do NOT replace with @references/guard-context.md. Only normalize to non-diacritical.

    **update.md guards -- current:**
    ```
    DỪNG và hướng dẫn user nếu bất kỳ điều kiện nào thất bại:

    - [ ] `.pdconfig` tồn tại -> "Chưa cài đặt skills. Chạy `node bin/install.js` trước."
    - [ ] Kết nối mạng khả dụng (git fetch thành công) -> "Không thể kết nối GitHub. Kiểm tra mạng và thử lại."
    ```
    **replace with:**
    ```
    DUNG va huong dan user neu bat ky dieu kien nao that bai:

    - [ ] `.pdconfig` ton tai -> "Chua cai dat skills. Chay `node bin/install.js` truoc."
    - [ ] Ket noi mang kha dung (git fetch thanh cong) -> "Khong the ket noi GitHub. Kiem tra mang va thu lai."
    ```
    Note: update.md has NO shared guards (pdconfig and network checks are unique). Only normalize to non-diacritical.

    CRITICAL rules:
    - Normalize ALL guard preamble lines to non-diacritical: "DUNG va huong dan user neu bat ky dieu kien nao that bai:"
    - Normalize ALL unique guard checklist text to non-diacritical Vietnamese
    - Replace shared guard text with @references/ refs
    - Do NOT modify `<context>`, `<process>`, `<output>`, `<objective>`, `<execution_context>`, or `<rules>` sections
    - Preserve the exact meaning of every guard check (only change encoding, not semantics)
    - what-next checks `.planning/` directory (different from guard-context.md which checks `.planning/CONTEXT.md`) -- keep as unique guard
  </action>
  <verify>
    <automated>node --test 'test/*.test.js' 2>&1 | tail -10</automated>
  </verify>
  <done>
    - All 6 remaining skill files have non-diacritical Vietnamese in guards
    - Shared guards replaced with @references/guard-*.md references where applicable
    - Skills with unique-only guards (conventions, what-next, update) properly normalized without refs
    - TDD RED tests from Plan 01 now PASS (guard references verified, no duplication detected)
    - All canonical structure tests pass
    - All converter tests pass (all 4 platforms)
    - Full test suite green: `node --test 'test/*.test.js'`
  </done>
</task>

</tasks>

<verification>
1. Guard deduplication complete: `node --test test/smoke-integrity.test.js` -- ALL tests pass including new guard dedup tests
2. No regression: `node --test 'test/*.test.js'` -- full suite green
3. Reference count: `grep -rl '@references/guard-context.md' commands/pd/ | wc -l` shows 8 skills
4. Reference count: `grep -rl '@references/guard-fastcode.md' commands/pd/ | wc -l` shows 6 skills
5. Reference count: `grep -rl '@references/guard-context7.md' commands/pd/ | wc -l` shows 5 skills
6. Reference count: `grep -rl '@references/guard-valid-path.md' commands/pd/ | wc -l` shows 2 skills
7. No diacritical guards: `grep -l 'DỪNG' commands/pd/*.md` returns empty (all normalized)
8. Single-source verification: Changing references/guard-context.md content would be reflected in all 8 skill outputs
</verification>

<success_criteria>
- Zero duplicated guard text across all 12 skills (guards appearing in 2+ skills now use @references/)
- All 12 skill files use non-diacritical Vietnamese in guards (per Phase 1 convention)
- guard-context.md referenced by 8 skills, guard-fastcode.md by 6, guard-context7.md by 5, guard-valid-path.md by 2
- Updating a shared guard instruction requires changing exactly 1 file
- Full test suite passes including new deduplication tests
- All 4 converter platforms still produce correct output
</success_criteria>

<output>
After completion, create `.planning/phases/02-cross-skill-deduplication/02-02-SUMMARY.md`
</output>
