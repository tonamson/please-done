---
phase: 06-context7-standardization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/context7-pipeline.md
  - references/guard-context7.md
  - test/smoke-integrity.test.js
autonomous: true
requirements: [LIBR-01]

must_haves:
  truths:
    - "references/context7-pipeline.md exists with 2-step invocation pattern, multi-lib batching, and error handling"
    - "guard-context7.md has 2+ checklist items including operational resolve-library-id check"
    - "Smoke tests verify pipeline content, guard content, and will verify workflow references (RED until Plan 02)"
  artifacts:
    - path: "references/context7-pipeline.md"
      provides: "Canonical Context7 pipeline pattern for all 5 skills"
      contains: "resolve-library-id"
    - path: "references/guard-context7.md"
      provides: "Enhanced guard with operational check"
      min_lines: 2
    - path: "test/smoke-integrity.test.js"
      provides: "Context7 standardization smoke tests"
      contains: "context7 standardization"
  key_links:
    - from: "references/context7-pipeline.md"
      to: "workflows/*.md"
      via: "@references/context7-pipeline.md reference"
      pattern: "context7-pipeline"
    - from: "references/guard-context7.md"
      to: "commands/pd/*.md"
      via: "@references/guard-context7.md in <guards> section"
      pattern: "guard-context7"
---

<objective>
Create the Context7 pipeline reference file and enhanced guard, then add smoke tests to verify standardization across the repo.

Purpose: Establish the canonical Context7 integration pattern (reference file + guard) that Plan 02 will wire into all 5 workflows.
Output: `references/context7-pipeline.md`, enhanced `references/guard-context7.md`, smoke tests in `smoke-integrity.test.js`
</objective>

<execution_context>
@/Users/tonamson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-context7-standardization/06-CONTEXT.md
@.planning/phases/06-context7-standardization/06-RESEARCH.md

<interfaces>
<!-- Existing guard file pattern (single checklist line, no frontmatter, no XML tags) -->
From references/guard-context7.md (current - 1 line):
```
- [ ] Context7 MCP ket noi thanh cong -> "Kiem tra Context7 MCP da duoc cau hinh."
```

<!-- Test file imports and helpers -->
From test/smoke-integrity.test.js (lines 1-42):
```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');
const { parseFrontmatter, extractReadingRefs, inlineWorkflow, listSkillFiles, extractXmlSection } = require('../bin/lib/utils');
const ROOT = path.resolve(__dirname, '..');
const COMMANDS_DIR = path.join(ROOT, 'commands', 'pd');
const WORKFLOWS_DIR = path.join(ROOT, 'workflows');
function read(relPath) { return fs.readFileSync(path.join(ROOT, relPath), 'utf8'); }
```

<!-- Skills with Context7 in allowed-tools (verified by grep) -->
5 skills: write-code, plan, new-milestone, fix-bug, test
7 skills WITHOUT: scan, init, fetch-doc, update, complete-milestone, conventions, what-next

<!-- Existing test describe blocks at end of file (line 475 is last line) -->
Last block: 'Repo integrity -- effort-level routing' (lines 422-475)
New block should be appended after line 475.

<!-- Current Context7 in write-code.md Buoc 3 (source of truth for pipeline content): -->
```markdown
**Context7** (task dung thu vien ngoai):
1. `resolve-library-id` -> lay ID
2. `query-docs` -> docs dung version
- TU DONG tra cuu -- KHONG can user yeu cau
- Admin (antd): BAT BUOC tra Context7 moi component moi
- Guard/JWT/Role: BAT BUOC tra Context7 (nestjs) + FastCode pattern
- Context7 khong co -> `.planning/docs/` hoac knowledge san
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create context7-pipeline.md reference and enhance guard-context7.md</name>
  <files>references/context7-pipeline.md, references/guard-context7.md</files>
  <read_first>
    - references/guard-context7.md (current 1-line guard to be enhanced)
    - references/guard-context.md (example of guard format with non-diacritical Vietnamese)
    - references/guard-fastcode.md (example of guard format)
    - workflows/write-code.md lines 124-137 (source of truth for Context7 pattern to extract)
    - workflows/plan.md lines 99-103 (existing Context7 in plan workflow)
    - workflows/fix-bug.md lines 90-92 (existing Context7 in fix-bug workflow)
  </read_first>
  <action>
**Create `references/context7-pipeline.md`** with the following exact content structure (~20-25 lines):

```markdown
# Context7 Pipeline

> Dung boi: write-code, plan, new-milestone, fix-bug, test
> Nguon su that cho quy trinh tra cuu thu vien ngoai

## Khi nao

BAT KY task nao su dung thu vien ngoai -> TU DONG tra cuu, KHONG can user yeu cau.

## Quy trinh 2 buoc

### Buoc 1: Resolve
`resolve-library-id` cho TUNG thu vien -> lay ID.
Nhieu thu vien -> resolve TAT CA truoc khi query.

### Buoc 2: Query
`query-docs` voi ID da resolve -> docs dung version.

## Xu ly loi

Context7 loi hoac khong co -> **DUNG** task, thong bao user:
1. Tiep tuc khong docs — chap nhan rui ro API sai
2. Dung, sua Context7 roi chay lai
3. Dung /pd:fetch-doc tai docs thu cong

KHONG am tham tiep tuc khi Context7 khong kha dung.
```

Key decisions reflected:
- D-05: "TU DONG tra cuu" - always automatic, no user request needed
- D-07: No stack-specific rules (no antd=required, auth=required) - universal "all external libraries"
- D-08: "Nhieu thu vien -> resolve TAT CA truoc khi query" - batch resolve pattern
- D-10/D-11: Hard stop with 3 user choices in Vietnamese
- D-12: All text in Vietnamese (non-diacritical per guard convention)

**Enhance `references/guard-context7.md`** to have 2 checklist items:

```markdown
- [ ] Context7 MCP ket noi thanh cong -> "Kiem tra Context7 MCP da duoc cau hinh."
- [ ] Context7 MCP hoat dong (thu resolve-library-id "react") -> "Context7 khong phan hoi. Kiem tra ket noi MCP."
```

Key decisions:
- D-09: Guard expanded with operational check using resolve-library-id "react"
- Use "react" as test library (virtually guaranteed in Context7 database, avoids false negatives)
- Non-diacritical Vietnamese consistent with all other guard files
  </action>
  <verify>
    <automated>test -f references/context7-pipeline.md && grep -q "resolve-library-id" references/context7-pipeline.md && grep -q "query-docs" references/context7-pipeline.md && grep -q "DUNG" references/context7-pipeline.md && test $(wc -l < references/guard-context7.md) -ge 2 && grep -q "resolve-library-id" references/guard-context7.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - references/context7-pipeline.md exists and contains "resolve-library-id"
    - references/context7-pipeline.md contains "query-docs"
    - references/context7-pipeline.md contains "TU DONG tra cuu"
    - references/context7-pipeline.md contains "DUNG" (hard stop per D-10)
    - references/context7-pipeline.md contains "Tiep tuc khong docs" (option 1 from D-11)
    - references/context7-pipeline.md contains "/pd:fetch-doc" (option 3 from D-11)
    - references/context7-pipeline.md contains "resolve TAT CA truoc" (batch pattern per D-08)
    - references/context7-pipeline.md does NOT contain "antd" or "Guard/JWT" (removed per D-07)
    - references/guard-context7.md has at least 2 lines starting with "- [ ]"
    - references/guard-context7.md contains "resolve-library-id" (operational check per D-09)
    - references/guard-context7.md contains "react" (test library)
  </acceptance_criteria>
  <done>Pipeline reference file created with 2-step invocation, batch resolve, error handling with 3 Vietnamese options. Guard enhanced with operational resolve check using "react".</done>
</task>

<task type="auto">
  <name>Task 2: Add Context7 standardization smoke tests to smoke-integrity.test.js</name>
  <files>test/smoke-integrity.test.js</files>
  <read_first>
    - test/smoke-integrity.test.js (full file - must see current structure, imports, helpers, last line number)
    - references/context7-pipeline.md (just created in Task 1 - verify content exists for test assertions)
    - references/guard-context7.md (just enhanced in Task 1 - verify 2-line format for test assertions)
  </read_first>
  <action>
Append a new `describe('Repo integrity -- context7 standardization')` block AFTER the last line of smoke-integrity.test.js (after line 475, after the effort-level routing block).

Add the following test cases using existing test patterns (same imports, `ROOT`, `COMMANDS_DIR`, `parseFrontmatter`, `listSkillFiles` already available):

```javascript
// ─── Context7 standardization verification ──────────────
// These tests verify Context7 pipeline integration (LIBR-01).
// Pipeline/guard tests pass immediately. Workflow reference tests
// are TDD RED until Plan 02 updates workflows.

describe('Repo integrity -- context7 standardization', () => {
  const CONTEXT7_SKILLS = ['write-code', 'plan', 'new-milestone', 'fix-bug', 'test'];
  const NON_CONTEXT7_SKILLS = ['scan', 'init', 'fetch-doc', 'update', 'complete-milestone', 'conventions', 'what-next'];

  it('context7-pipeline.md ton tai voi noi dung bat buoc', () => {
    const pipelinePath = path.join(ROOT, 'references', 'context7-pipeline.md');
    assert.ok(fs.existsSync(pipelinePath), 'thieu references/context7-pipeline.md');
    const content = fs.readFileSync(pipelinePath, 'utf8');
    assert.match(content, /resolve-library-id/, 'pipeline thieu resolve-library-id');
    assert.match(content, /query-docs/, 'pipeline thieu query-docs');
    assert.match(content, /TU DONG/, 'pipeline thieu TU DONG trigger rule');
    assert.match(content, /DUNG/, 'pipeline thieu error handling DUNG');
    assert.match(content, /resolve TAT CA/, 'pipeline thieu multi-lib batch pattern');
  });

  it('pipeline KHONG co stack-specific rules (D-07)', () => {
    const content = fs.readFileSync(path.join(ROOT, 'references', 'context7-pipeline.md'), 'utf8');
    assert.ok(!content.includes('antd'), 'pipeline con sot antd rule');
    assert.ok(!content.includes('Guard/JWT'), 'pipeline con sot Guard/JWT rule');
    assert.ok(!content.includes('BAT BUOC tra Context7 (nestjs)'), 'pipeline con sot nestjs-specific rule');
  });

  it('pipeline co 3 lua chon xu ly loi (D-11)', () => {
    const content = fs.readFileSync(path.join(ROOT, 'references', 'context7-pipeline.md'), 'utf8');
    assert.match(content, /Tiep tuc khong docs/, 'pipeline thieu option 1');
    assert.match(content, /sua Context7/, 'pipeline thieu option 2');
    assert.match(content, /\/pd:fetch-doc/, 'pipeline thieu option 3');
  });

  it('5 skills co Context7 trong allowed-tools', () => {
    for (const name of CONTEXT7_SKILLS) {
      const content = fs.readFileSync(path.join(COMMANDS_DIR, `${name}.md`), 'utf8');
      const { frontmatter } = parseFrontmatter(content);
      const tools = frontmatter['allowed-tools'] || [];
      assert.ok(tools.includes('mcp__context7__resolve-library-id'), `${name}: thieu resolve-library-id trong allowed-tools`);
      assert.ok(tools.includes('mcp__context7__query-docs'), `${name}: thieu query-docs trong allowed-tools`);
    }
  });

  it('7 skills KHONG co Context7 trong allowed-tools', () => {
    for (const name of NON_CONTEXT7_SKILLS) {
      const content = fs.readFileSync(path.join(COMMANDS_DIR, `${name}.md`), 'utf8');
      const { frontmatter } = parseFrontmatter(content);
      const tools = frontmatter['allowed-tools'] || [];
      assert.ok(!tools.includes('mcp__context7__resolve-library-id'), `${name}: KHONG nen co resolve-library-id`);
      assert.ok(!tools.includes('mcp__context7__query-docs'), `${name}: KHONG nen co query-docs`);
    }
  });

  it('guard-context7.md co kiem tra hoat dong (D-09)', () => {
    const content = fs.readFileSync(path.join(ROOT, 'references', 'guard-context7.md'), 'utf8');
    assert.match(content, /resolve-library-id/, 'guard thieu kiem tra resolve-library-id');
    const checklistLines = content.split('\n').filter(l => l.trim().startsWith('- [ ]'));
    assert.ok(checklistLines.length >= 2, `guard can it nhat 2 dieu kien kiem tra, co ${checklistLines.length}`);
  });

  it('workflows co tham chieu context7-pipeline', () => {
    const workflowsWithContext7 = ['write-code', 'plan', 'fix-bug', 'test'];
    for (const name of workflowsWithContext7) {
      const content = fs.readFileSync(path.join(ROOT, 'workflows', `${name}.md`), 'utf8');
      assert.match(content, /context7-pipeline/, `workflows/${name}.md: thieu tham chieu context7-pipeline`);
    }
  });

  it('workflows KHONG con stack-specific Context7 rules (D-07)', () => {
    const content = fs.readFileSync(path.join(ROOT, 'workflows', 'write-code.md'), 'utf8');
    assert.ok(!content.includes('Admin (antd): BAT BUOC'), 'write-code.md con sot antd rule');
    assert.ok(!content.includes('Guard/JWT/Role: BAT BUOC'), 'write-code.md con sot Guard/JWT rule');
  });

  it('workflows KHONG con silent fallback (D-10)', () => {
    const workflowsToCheck = ['write-code', 'plan', 'fix-bug'];
    for (const name of workflowsToCheck) {
      const content = fs.readFileSync(path.join(ROOT, 'workflows', `${name}.md`), 'utf8');
      assert.ok(
        !content.includes('knowledge san') && !content.includes('knowledge s\u1EB5n'),
        `workflows/${name}.md: con sot silent fallback "knowledge san"`
      );
    }
  });
});
```

**Test status expectations:**
- Tests 1-6 (pipeline content, pipeline no stack rules, pipeline error options, 5 skills allowed-tools, 7 skills no Context7, guard operational check): GREEN immediately after Task 1
- Tests 7-8 (workflow references, no stack-specific rules in workflows): TDD RED until Plan 02 refactors workflows
- Test 9 (no silent fallback): TDD RED until Plan 02 removes old fallback patterns

Note: Tests 7-9 being RED is expected and intentional (TDD pattern from Phase 2). Add a comment before these tests: `// TDD RED until Plan 02 refactors workflows`
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && node --test test/smoke-integrity.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test/smoke-integrity.test.js contains `describe('Repo integrity -- context7 standardization'`
    - test/smoke-integrity.test.js contains `CONTEXT7_SKILLS` array with exactly 5 entries: write-code, plan, new-milestone, fix-bug, test
    - test/smoke-integrity.test.js contains `NON_CONTEXT7_SKILLS` array with exactly 7 entries
    - test/smoke-integrity.test.js contains test for "context7-pipeline.md ton tai"
    - test/smoke-integrity.test.js contains test for "pipeline KHONG co stack-specific rules"
    - test/smoke-integrity.test.js contains test for "pipeline co 3 lua chon xu ly loi"
    - test/smoke-integrity.test.js contains test for "guard-context7.md co kiem tra hoat dong"
    - test/smoke-integrity.test.js contains test for "workflows co tham chieu context7-pipeline"
    - test/smoke-integrity.test.js contains test for "workflows KHONG con stack-specific"
    - Tests 1-6 (pipeline, guard, skills allowed-tools) PASS
    - Tests 7-9 (workflow references, stack rules removal, silent fallback) expected to FAIL (TDD RED for Plan 02)
  </acceptance_criteria>
  <done>Smoke test block with 9 test cases added. Pipeline/guard/skills tests GREEN. Workflow refactoring tests RED (TDD for Plan 02).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `references/context7-pipeline.md` exists with ~20-25 lines covering 2-step invocation, batch resolve, error handling
2. `references/guard-context7.md` has 2 checklist items (connection + operational)
3. `node --test test/smoke-integrity.test.js` runs with pipeline/guard/skills tests passing, workflow tests RED
4. No stack-specific rules (antd, Guard/JWT) in pipeline reference
5. Error handling includes 3 Vietnamese options per D-11
</verification>

<success_criteria>
- Pipeline reference file created with all D-04/D-05/D-07/D-08/D-10/D-11/D-12 decisions implemented
- Guard enhanced with operational check per D-09
- 9 smoke tests added, 6 GREEN (pipeline + guard + skills), 3 RED (TDD for Plan 02 workflow refactoring)
</success_criteria>

<output>
After completion, create `.planning/phases/06-context7-standardization/06-01-SUMMARY.md`
</output>
