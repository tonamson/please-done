---
phase: 08-wave-based-parallel-execution
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - workflows/write-code.md
  - workflows/plan.md
autonomous: true
requirements:
  - PARA-01
  - PARA-02
  - PARA-03
must_haves:
  truths:
    - "write-code.md Bước 10 has agent context minimization (KHÔNG dump toàn bộ PLAN.md)"
    - "write-code.md Bước 10 has post-wave safety net with git diff conflict check"
    - "write-code.md Bước 10 has post-wave build check that stops on failure"
    - "write-code.md Bước 10 has wave summary report after all waves complete"
    - "plan.md Bước 5 has > Files: enforcement for plans >= 3 tasks"
    - "All 7 wave-based parallel execution smoke tests pass"
  artifacts:
    - path: "workflows/write-code.md"
      provides: "Enhanced Bước 10 with agent context, post-wave safety net, build check, wave summary"
      contains: "KHÔNG dump toàn bộ PLAN"
    - path: "workflows/plan.md"
      provides: "> Files: enforcement instruction in Bước 5"
      contains: "> Files:"
  key_links:
    - from: "workflows/plan.md"
      to: "workflows/write-code.md"
      via: "plan.md enforces > Files: metadata that write-code.md Bước 1.5 reads for conflict detection"
      pattern: "> Files:"
    - from: "test/smoke-integrity.test.js"
      to: "workflows/write-code.md"
      via: "smoke tests assert Bước 10 instructions"
      pattern: "write-code\\.md"
    - from: "test/smoke-integrity.test.js"
      to: "workflows/plan.md"
      via: "smoke tests assert > Files: enforcement"
      pattern: "plan\\.md"
---

<objective>
Improve write-code.md Bước 10 parallel execution flow with agent context minimization, post-wave safety net, and build check. Add > Files: enforcement to plan.md Bước 5. All 7 smoke tests from Plan 01 must pass.

Purpose: Bước 10 currently dumps full PLAN.md to each agent and hard-stops on any conflict. This plan upgrades it to send only task-relevant context (D-11), adds post-wave git diff safety net (D-06), build verification (D-07), and agent spawn instructions (D-12/D-13). plan.md gets > Files: enforcement (D-14) to ensure conflict detection has the metadata it needs.

Output: Enhanced Bước 10 in write-code.md + > Files: enforcement in plan.md + all 7 smoke tests green.
</objective>

<execution_context>
@/Users/tonamson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wave-based-parallel-execution/08-CONTEXT.md
@.planning/phases/08-wave-based-parallel-execution/08-RESEARCH.md
@.planning/phases/08-wave-based-parallel-execution/08-01-SUMMARY.md
</context>

<interfaces>
<!-- Current Bước 10 parallel section in workflows/write-code.md (lines 308-322) — will be REPLACED -->
```markdown
### `--parallel` (multi-agent song song)
Thực thi theo waves từ Bước 1.5:

**Với mỗi wave:**
1. **Spawn Agent tool** cho mỗi task song song — mỗi agent nhận: PLAN.md, task detail, rules, CONTEXT.md, docs.
   - Đọc `Effort:` từ task metadata -> chọn model tương ứng (simple->haiku, standard->sonnet, complex->opus, mặc định->sonnet)
   - Truyền model vào Agent tool: `model: {resolved_model}`
   - Thông báo: "Spawning {model} agent cho {task_id} ({effort})..."
   - Chỉ dẫn: Bước 2→3→4→5 (KHÔNG report/TASKS/commit)
2. **Agent Frontend đặc biệt** (song song Backend): đọc PLAN.md "API Endpoints" → tạo types/interfaces từ response shape (KHÔNG cần API thật) → tạo API functions + components. Sau Backend xong → verify types khớp response thực tế
3. **Chờ TẤT CẢ agents wave hoàn thành**
4. **Sau wave** (orchestrator): thu thập kết quả, kiểm tra conflicts (2 agents sửa cùng file → **DỪNG** báo user). Build fail → **DỪNG** báo task cụ thể. OK → report (Bước 6) + TASKS.md + commit (Bước 7)
5. **Verify integration** (Backend + Frontend wave): so sánh TypeScript interfaces frontend với response DTO backend, kiểm tra endpoint paths. Mismatch → sửa frontend, commit `[TASK-N] Đồng bộ types với backend`
6. **Wave tiếp** → lặp từ bước 1
7. **Hết waves** → Bước 9 → thông báo tổng kết: phase, tasks, waves, gợi ý
```

<!-- Current plan.md Bước 5 Nguyên tắc 10 (lines 240-243) — will ADD new item 12 -->
```markdown
10. **Dependency chính xác** cho parallel execution: ghi task number cụ thể (`Task A`). Phân biệt:
   - **Phụ thuộc code**: task B import/dùng function task A → ghi `Task A`
   - **Phụ thuộc design**: dùng response shape từ PLAN.md (không cần code thực) → ghi `Không` (parallel-safe)
   - **Phụ thuộc file**: sửa chung file → ghi `Task A (shared file)`
11. **Effort level** (TOKN-04): mỗi task PHẢI có `Effort:` trong metadata. Mặc định: `standard`.
```

<!-- Smoke tests from Plan 01 that must pass — tests 4, 5, 7 are the key ones for Plan 02 -->
```javascript
// Test 4: write-code.md co post-wave build check (D-07)
assert.match(content, /build fail.*D[UƯ]NG|D[UƯ]NG.*build fail/is, ...);

// Test 5: write-code.md co agent context minimization (D-11)
assert.match(content, /KH[OÔ]NG dump to[aà]n b[oộ] PLAN/i, ...);

// Test 7: plan.md co > Files: enforcement cho plans >= 3 tasks (D-14)
assert.match(content, /> Files:/, ...);
assert.match(content, /3 tasks|>= 3|≥ 3/, ...);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Improve write-code.md Bước 10 with agent context, safety net, and build check</name>
  <files>workflows/write-code.md</files>
  <read_first>
    - workflows/write-code.md (MUST read — Bước 10 parallel section lines 308-322 will be replaced, plus understand Bước 1.5 changes from Plan 01)
    - .planning/phases/08-wave-based-parallel-execution/08-CONTEXT.md (locked decisions D-05 through D-13)
    - .planning/phases/08-wave-based-parallel-execution/08-RESEARCH.md (code examples 4 and 5 for agent spawn and post-wave safety net)
    - test/smoke-integrity.test.js (read tests 4 and 5 assertions to ensure content matches)
  </read_first>
  <action>
Replace the `### --parallel (multi-agent song song)` section inside Bước 10 of workflows/write-code.md. Keep the `### --auto` and `### Mặc định` sections completely unchanged.

The new `--parallel` section content must include ALL of the following, in compressed Vietnamese prose:

**1. Agent spawn per task (D-11, D-12, D-13):**
```markdown
### `--parallel` (multi-agent song song)
Thực thi theo waves từ Bước 1.5:

**Với mỗi wave:**
1. **Spawn Agent tool** cho mỗi task — KHÔNG dump toàn bộ PLAN.md. Mỗi agent nhận:
   - Task detail từ TASKS.md (mô tả + tiêu chí chấp nhận + ghi chú kỹ thuật)
   - PLAN.md sections liên quan đến task (quyết định thiết kế, API endpoints cho task đó)
   - Applicable `.planning/rules/` files
   - CONTEXT.md path
   - Effort→model: `Effort:` từ task metadata → model (simple→haiku, standard→sonnet, complex→opus, mặc định→sonnet)
   - Truyền `model: {resolved_model}` vào Agent tool
   - Thông báo: "Spawning {model} agent cho {task_id} ({effort})..."
   - Chỉ dẫn agent: Bước 2→3→4→5 (research → code → lint/build → test). KHÔNG report, KHÔNG commit, KHÔNG cập nhật TASKS.md — orchestrator làm sau wave
```

**2. Frontend parallel (keep existing):**
```markdown
2. **Agent Frontend đặc biệt** (song song Backend): đọc PLAN.md "API Endpoints" → tạo types/interfaces từ response shape (KHÔNG cần API thật) → tạo API functions + components. Sau Backend xong → verify types khớp response thực tế
```

**3. Wait + post-wave safety net (D-06, D-07):**
```markdown
3. **Chờ TẤT CẢ agents wave hoàn thành**
4. **Post-wave safety net** (orchestrator):
   a. `git diff --name-only` → danh sách files đã sửa
   b. Kiểm tra: 2+ agents sửa cùng file? → **DỪNG**: "Conflict phát hiện: [file] bị sửa bởi Task X và Task Y. Cần resolve thủ công."
   c. Build check: chạy lint + build → build fail → **DỪNG**: "Build fail sau wave N. Task [X] có thể là nguyên nhân. Output: [lỗi]". KHÔNG chạy wave tiếp khi build fail
   d. OK → report (Bước 6) + TASKS.md (Bước 7a) + commit (Bước 7b) cho TỪNG task
```

**4. Integration verify + next wave + summary:**
```markdown
5. **Verify integration** (Backend + Frontend wave): so sánh TypeScript interfaces frontend với response DTO backend, kiểm tra endpoint paths. Mismatch → sửa frontend, commit `[TASK-N] Đồng bộ types với backend`
6. **Wave tiếp** → lặp từ bước 1
7. **Hết waves** → Bước 9 → thông báo tổng kết:
```
Tổng kết: [N] tasks, [M] waves hoàn thành
Wave 1: Task 1 ✅, Task 3 ✅ (2 song song)
Wave 2: Task 2 ✅ (1 tuần tự)
Conflicts resolved: [K] tasks dời wave
```
Gợi ý: `/pd:test`, `/pd:plan`, `/pd:complete-milestone`
```

CRITICAL:
- The phrase "KHÔNG dump toàn bộ PLAN.md" MUST appear exactly (smoke test 5 checks for this)
- "build fail" and "DỪNG" MUST appear in proximity (smoke test 4 checks for this)
- Keep `### --auto` and `### Mặc định` sections EXACTLY unchanged
- Use the project's compressed Vietnamese prose style
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && node --test test/smoke-integrity.test.js 2>&1 | grep -E "wave-based|FAIL|PASS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - workflows/write-code.md contains "KHÔNG dump toàn bộ PLAN.md" (grep -c "KHÔNG dump toàn bộ PLAN" workflows/write-code.md returns >= 1)
    - workflows/write-code.md contains "build fail" near "DỪNG" (grep -c "build fail" workflows/write-code.md returns >= 1)
    - workflows/write-code.md contains "git diff --name-only" post-wave safety net
    - workflows/write-code.md contains "KHÔNG report, KHÔNG commit, KHÔNG cập nhật TASKS.md" agent instructions
    - workflows/write-code.md contains wave summary format with "Tổng kết"
    - Smoke tests 4 and 5 from Plan 01 now PASS
    - All existing tests still PASS
    - The --auto and Mặc định sections are completely unchanged
  </acceptance_criteria>
  <done>Bước 10 parallel section upgraded with agent context minimization, post-wave git diff safety net, build verification, and wave summary report. Smoke tests 4 and 5 pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add > Files: enforcement to plan.md Bước 5 for plans >= 3 tasks</name>
  <files>workflows/plan.md</files>
  <read_first>
    - workflows/plan.md (MUST read — Bước 5 Nguyên tắc section lines 226-261 to understand current content and find insertion point after item 11)
    - .planning/phases/08-wave-based-parallel-execution/08-CONTEXT.md (locked decisions D-14, D-15)
    - test/smoke-integrity.test.js (read test 7 assertions for > Files: enforcement)
  </read_first>
  <action>
Add a new Nguyên tắc item 12 to plan.md Bước 5, after item 11 (Effort level) and before the `### Phân loại effort cho task` subsection.

The new item to add:

```markdown
12. **`> Files:` bắt buộc** (PARA-03): plan có >= 3 tasks → planner PHẢI ghi đầy đủ `> Files:` field cho mỗi task. Dựa trên Ghi chú kỹ thuật + mô tả task. Không cần chính xác 100% — heuristic đủ cho conflict detection. Thiếu `> Files:` → parallel mode không thể phân tích conflict → kém hiệu quả. Ghi CẢ files tạo mới VÀ files sửa
```

This adds exactly ONE new numbered item. Do NOT change any existing items (1-11). Do NOT change the effort classification table or examples below.

Verify insertion point:
- Line containing "11. **Effort level**" should be directly ABOVE the new item
- Line containing "### Phân loại effort cho task" should be directly BELOW the new item
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && node --test test/smoke-integrity.test.js 2>&1 | grep -E "wave-based|FAIL|PASS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - workflows/plan.md contains "> Files:" in Bước 5 (grep -c "> Files:" workflows/plan.md returns >= 2 — one in existing item 10 and one in new item 12)
    - workflows/plan.md contains ">= 3 tasks" or "≥ 3" or "3 tasks" condition near "> Files:" enforcement
    - workflows/plan.md contains item numbered "12." with "> Files: bắt buộc"
    - workflows/plan.md existing items 1-11 are unchanged
    - Smoke test 7 (plan.md > Files: enforcement) now PASSES
    - ALL 7 wave-based parallel execution smoke tests now PASS
    - All existing tests still PASS
    - `npm test` or `node --test test/smoke-integrity.test.js test/smoke-utils.test.js` exits 0
  </acceptance_criteria>
  <done>> Files: enforcement added as Nguyên tắc 12 in plan.md Bước 5. All 7 smoke tests pass. Full test suite green.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `node --test test/smoke-integrity.test.js test/smoke-utils.test.js` — full suite exits 0
2. ALL 7 wave-based parallel execution smoke tests pass
3. `grep -c "KHÔNG dump toàn bộ PLAN" workflows/write-code.md` returns >= 1
4. `grep -c "build fail" workflows/write-code.md` returns >= 1
5. `grep -c "git diff --name-only" workflows/write-code.md` returns >= 1
6. `grep -c "> Files:" workflows/plan.md` returns >= 2
7. All locked decisions D-01 through D-15 implemented
</verification>

<success_criteria>
- write-code.md Bước 10 sends only task-relevant context to agents, not full PLAN.md (D-11)
- write-code.md Bước 10 agent instructions: Bước 2→3→4→5 only, no report/commit/TASKS update (D-12)
- write-code.md Bước 10 effort→model routing reuses Phase 5 mapping (D-13)
- write-code.md Bước 10 post-wave safety net: git diff checks for same-file conflicts (D-06)
- write-code.md Bước 10 post-wave build check: build fail → DỪNG (D-07)
- write-code.md Bước 10 wave summary report at end (Claude's discretion)
- plan.md Bước 5 enforces > Files: for plans >= 3 tasks (D-14)
- plan.md Bước 5 says heuristic is sufficient, 100% accuracy not required (D-15)
- All 7 smoke tests pass, full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/08-wave-based-parallel-execution/08-02-SUMMARY.md`
</output>
