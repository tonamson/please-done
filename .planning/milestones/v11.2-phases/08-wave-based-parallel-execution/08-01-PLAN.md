---
phase: 08-wave-based-parallel-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/smoke-integrity.test.js
  - workflows/write-code.md
autonomous: true
requirements:
  - PARA-01
  - PARA-02
  - PARA-03
must_haves:
  truths:
    - "write-code.md Bước 1.5 has explicit Kahn's algorithm instructions for topological sort with wave grouping"
    - "write-code.md Bước 1.5 has static hotspot patterns table covering 6 framework stacks"
    - "write-code.md Bước 1.5 has dynamic > Files: cross-reference algorithm for conflict detection"
    - "write-code.md Bước 1.5 has pre-wave auto-serialize logic that moves conflicting tasks to next wave"
    - "Smoke tests verify hotspot patterns, auto-serialize, and wave grouping instructions are present"
  artifacts:
    - path: "test/smoke-integrity.test.js"
      provides: "Parallel execution smoke tests"
      contains: "wave-based parallel execution"
    - path: "workflows/write-code.md"
      provides: "Enhanced Bước 1.5 with topological sort, hotspot patterns, auto-serialize"
      contains: "hotspot"
  key_links:
    - from: "test/smoke-integrity.test.js"
      to: "workflows/write-code.md"
      via: "content assertions on write-code.md"
      pattern: "write-code\\.md"
---

<objective>
Add TDD smoke tests for wave-based parallel execution, then expand write-code.md Bước 1.5 with proper topological sort algorithm, static hotspot patterns table, dynamic > Files: cross-reference, and pre-wave auto-serialize conflict handling.

Purpose: Bước 1.5 currently has vague instructions ("Grep kiểm tra import chung"). This plan replaces them with an explicit, framework-aware algorithm that the orchestrating agent can follow precisely to detect file conflicts and group tasks into parallel-safe waves.

Output: Enhanced Bước 1.5 in write-code.md + smoke tests verifying all parallel-specific instructions are present.
</objective>

<execution_context>
@/Users/tonamson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wave-based-parallel-execution/08-CONTEXT.md
@.planning/phases/08-wave-based-parallel-execution/08-RESEARCH.md
</context>

<interfaces>
<!-- Current Bước 1.5 content in workflows/write-code.md (lines 87-95) — will be REPLACED entirely -->
```markdown
### Bước 1.5: Phân tích dependency + nhóm wave (CHỈ `--parallel`)

1. Đọc TASKS.md → tasks ⬜ + cột `Phụ thuộc`. Không có ⬜ → DỪNG parallel
2. **Dependency graph**: không phụ thuộc/phụ thuộc ✅ → sẵn sàng. Phụ thuộc ⬜/🔄 → chờ
3. **Nhóm waves** (topological sort): Wave 1 = sẵn sàng, Wave N = phụ thuộc Wave N-1
4. **Song song trong wave**: không chia sẻ files → song song Agent tool. Sửa chung file → tuần tự. Cảnh báo shared files ẩn: Grep kiểm tra import chung → phát hiện → tuần tự hoặc cảnh báo
5. **Backend + Frontend song song**: PLAN.md có thiết kế API → Frontend dùng response shape từ PLAN.md → cùng wave. Sau cả hai xong → verify integration

Hiển thị wave plan → hỏi xác nhận → **Bước 10** (parallel). Không `--parallel` → bỏ qua.
```

<!-- Existing smoke test describe blocks (for reference on test style) -->
```javascript
describe('Repo integrity -- effort-level routing', () => { ... });
describe('Repo integrity -- context7 standardization', () => { ... });
describe('Repo integrity -- library fallback and version detection', () => { ... });
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 smoke tests for wave-based parallel execution</name>
  <files>test/smoke-integrity.test.js</files>
  <read_first>
    - test/smoke-integrity.test.js (current test file — see existing describe blocks and assertion patterns)
    - workflows/write-code.md (target file being tested — understand current Bước 1.5 and Bước 10 content)
    - workflows/plan.md (target file being tested — understand current Bước 5 content)
    - .planning/phases/08-wave-based-parallel-execution/08-CONTEXT.md (locked decisions D-01 through D-15)
  </read_first>
  <behavior>
    - Test: write-code.md contains hotspot patterns table with barrel exports, config files, and framework-specific entries (index.ts, index.js, package.json, tsconfig.json, *.config.*, app.module.ts, layout.tsx, pubspec.yaml, main.dart, functions.php)
    - Test: write-code.md contains auto-serialize instructions with "dời" or "doi" and "wave" in proximity
    - Test: write-code.md contains topological sort / wave grouping instructions with "in-degree" or "Kahn" reference
    - Test: write-code.md contains post-wave build check instructions with "build fail" and "DỪNG" or "DUNG"
    - Test: write-code.md contains agent context minimization (KHÔNG dump toàn bộ PLAN.md)
    - Test: write-code.md contains > Files: cross-reference instructions
    - Test: plan.md contains > Files: enforcement instruction for plans >= 3 tasks
  </behavior>
  <action>
Add a new `describe('Repo integrity -- wave-based parallel execution', () => { ... })` block at the end of test/smoke-integrity.test.js (after the library fallback describe block, line 654).

Tests to add (all reading file content and asserting with `assert.match` or `assert.ok`):

1. `'write-code.md co bang hotspot patterns (PARA-03/D-02)'`
   ```javascript
   const content = fs.readFileSync(path.join(ROOT, 'workflows', 'write-code.md'), 'utf8');
   assert.match(content, /hotspot/, 'write-code.md: thieu hotspot patterns');
   assert.match(content, /index\.ts/, 'write-code.md: thieu index.ts trong hotspot');
   assert.match(content, /app\.module\.ts/, 'write-code.md: thieu app.module.ts trong hotspot');
   assert.match(content, /layout\.tsx/, 'write-code.md: thieu layout.tsx trong hotspot');
   assert.match(content, /pubspec\.yaml/, 'write-code.md: thieu pubspec.yaml trong hotspot');
   assert.match(content, /functions\.php/, 'write-code.md: thieu functions.php trong hotspot');
   ```

2. `'write-code.md co auto-serialize conflict handling (PARA-02/D-05)'`
   ```javascript
   const content = fs.readFileSync(path.join(ROOT, 'workflows', 'write-code.md'), 'utf8');
   assert.match(content, /auto-serialize|d[oờ]i.*wave/i, 'write-code.md: thieu auto-serialize logic');
   assert.match(content, /conflict/, 'write-code.md: thieu conflict handling');
   ```

3. `'write-code.md co topological sort voi wave grouping (PARA-01)'`
   ```javascript
   const content = fs.readFileSync(path.join(ROOT, 'workflows', 'write-code.md'), 'utf8');
   assert.match(content, /in-degree|topological/, 'write-code.md: thieu topological sort instructions');
   assert.match(content, /Wave 1/, 'write-code.md: thieu wave grouping');
   ```

4. `'write-code.md co post-wave build check (D-07)'`
   ```javascript
   const content = fs.readFileSync(path.join(ROOT, 'workflows', 'write-code.md'), 'utf8');
   assert.match(content, /build fail.*D[UƯ]NG|D[UƯ]NG.*build fail/is, 'write-code.md: thieu post-wave build check');
   ```

5. `'write-code.md co agent context minimization (D-11)'`
   ```javascript
   const content = fs.readFileSync(path.join(ROOT, 'workflows', 'write-code.md'), 'utf8');
   assert.match(content, /KH[OÔ]NG dump to[aà]n b[oộ] PLAN/i, 'write-code.md: thieu agent context minimization');
   ```

6. `'write-code.md co > Files: cross-reference (PARA-03/D-04)'`
   ```javascript
   const content = fs.readFileSync(path.join(ROOT, 'workflows', 'write-code.md'), 'utf8');
   assert.match(content, /> Files:.*cross-reference|giao nhau/i, 'write-code.md: thieu > Files: cross-reference');
   ```

7. `'plan.md co > Files: enforcement cho plans >= 3 tasks (D-14)'`
   ```javascript
   const content = fs.readFileSync(path.join(ROOT, 'workflows', 'plan.md'), 'utf8');
   assert.match(content, /> Files:/, 'plan.md: thieu > Files: enforcement');
   assert.match(content, /3 tasks|>= 3|≥ 3/, 'plan.md: thieu >= 3 tasks condition');
   ```

These tests should all FAIL initially (TDD RED) since write-code.md Bước 1.5 doesn't yet have these instructions.

Follow existing test file patterns:
- Use `fs.readFileSync(path.join(ROOT, ...)` for reading files
- Use `assert.match(content, /pattern/, 'error message')` for assertions
- Vietnamese test names without diacritics
- Error messages describe what's missing
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && node --test test/smoke-integrity.test.js 2>&1 | grep -E "wave-based|FAIL|PASS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - test/smoke-integrity.test.js contains `describe('Repo integrity -- wave-based parallel execution'`
    - test/smoke-integrity.test.js contains at least 7 `it(` calls inside that describe block
    - test/smoke-integrity.test.js contains assertions for: hotspot, auto-serialize, topological/in-degree, build fail, KHONG dump, > Files: cross-reference, >= 3 tasks
    - All 7 new tests FAIL (TDD RED) — this is expected since write-code.md and plan.md haven't been updated yet
    - All EXISTING tests still PASS (no regressions)
  </acceptance_criteria>
  <done>7 smoke tests added for wave-based parallel execution, all failing (TDD RED), all prior tests still green</done>
</task>

<task type="auto">
  <name>Task 2: Expand write-code.md Bước 1.5 with topological sort, hotspot patterns, and auto-serialize</name>
  <files>workflows/write-code.md</files>
  <read_first>
    - workflows/write-code.md (MUST read current content — Bước 1.5 lines 87-95 will be replaced, rest preserved)
    - .planning/phases/08-wave-based-parallel-execution/08-CONTEXT.md (locked decisions D-01 through D-10)
    - .planning/phases/08-wave-based-parallel-execution/08-RESEARCH.md (code examples for hotspot table, wave display, auto-serialize)
    - test/smoke-integrity.test.js (read the new tests from Task 1 to ensure content matches assertions)
  </read_first>
  <action>
Replace the ENTIRE Bước 1.5 section in workflows/write-code.md (lines 87-95) with the expanded version below. Keep the same `### Bước 1.5:` heading and `---` separator before/after. Keep the section after Bước 1.5 (Bước 1.6) completely unchanged.

The new Bước 1.5 content must follow the project's compressed Vietnamese prose style (telegraphic, table-heavy, per Phase 3 patterns). It must include ALL of the following subsections:

**Section 1: Dependency graph (Kahn's algorithm)**
```markdown
### Bước 1.5: Phân tích dependency + nhóm wave (CHỈ `--parallel`)

1. Đọc TASKS.md → tasks ⬜ + cột `Phụ thuộc`. Không có ⬜ → DỪNG parallel
2. **Dependency graph** (Kahn's algorithm / topological sort):
   - Xây adjacency list từ cột `Phụ thuộc`:
     - `Task A` (code dependency) → edge A→B
     - `Không` (design dependency) → không edge (parallel-safe)
     - `Task A (shared file)` → edge A→B
   - Tính in-degree cho mỗi task
   - Wave 1 = tất cả tasks có in-degree 0 VÀ status ⬜
   - Mỗi wave tiếp: xóa tasks wave trước → tính lại in-degree → in-degree 0 mới = wave tiếp
   - Không task nào in-degree 0 + còn tasks chưa xử lý → **DỪNG**: "Circular dependency phát hiện."
```

**Section 2: Shared-file detection (two-layer per D-01)**
```markdown
3. **Phát hiện shared files** (two-layer detection):

**Layer 1 — Bảng hotspot patterns (static):**

| Stack | Files | Pattern |
|-------|-------|---------|
| Chung | Barrel exports | `index.ts`, `index.js` |
| Chung | Config | `package.json`, `tsconfig.json`, `*.config.*` |
| NestJS | Registration | `app.module.ts`, `main.ts`, `*.module.ts` |
| Next.js | Layout/Middleware | `layout.tsx`, `middleware.ts`, `next.config.*` |
| Flutter | Core | `pubspec.yaml`, `main.dart`, `routes.dart` |
| WordPress | Theme/Plugin | `functions.php`, `style.css` |
| Solidity | Config | `hardhat.config.*`, migration files |

**Layer 2 — `> Files:` cross-reference (dynamic):**
- Thu thập `> Files:` của tất cả tasks trong cùng wave
- So sánh từng cặp: giao nhau > 0 files → conflict
- File match hotspot pattern + xuất hiện trong 2+ tasks → conflict
- Task thiếu `> Files:` → cảnh báo nhưng vẫn cho chạy song song (degraded detection)
```

**Section 3: Auto-serialize conflict handling (D-05, D-08)**
```markdown
4. **Xử lý conflict (auto-serialize — KHÔNG hard-stop):**
   - Conflict phát hiện → giữ task số nhỏ trong wave hiện tại, dời task số lớn sang wave tiếp
   - Hiển thị: "Task X dời sang wave N+1 (conflict: shared-file.ts)"
   - TẤT CẢ tasks còn lại conflict lẫn nhau → chuyển sequential (deadlock fallback)
```

**Section 4: Backend + Frontend (keep existing)**
```markdown
5. **Backend + Frontend song song**: PLAN.md có thiết kế API → Frontend dùng response shape từ PLAN.md → cùng wave. Sau cả hai xong → verify integration
```

**Section 5: Wave plan display + confirmation (D-09, D-10)**
```markdown
6. **Hiển thị wave plan** (bảng compact):
```
Wave 1: Task 1 (simple/haiku), Task 3 (standard/sonnet) — 2 song song
Wave 2: Task 2 (complex/opus) — phụ thuộc Task 1
Conflict: Task 4 dời W1→W2 (shared: app.module.ts)
Tổng: [N] tasks, [M] waves, tối đa [K] song song/wave
```
Xác nhận chạy? (Y/n) — hỏi 1 lần, sau đó chạy liên tục → **Bước 10** (parallel).

Không `--parallel` → bỏ qua Bước 1.5.
```

CRITICAL formatting rules:
- Use the project's compressed Vietnamese prose style (no extra words)
- Tables in compact markdown format
- Keep the `---` section separator before Bước 1.6
- The final line "Không `--parallel` → bỏ qua Bước 1.5." must be preserved
- Do NOT modify any content outside Bước 1.5 (lines 1-86 and 96-367 must remain identical)
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && node --test test/smoke-integrity.test.js 2>&1 | grep -c "pass"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/write-code.md contains "hotspot" (grep -c "hotspot" workflows/write-code.md returns >= 1)
    - workflows/write-code.md contains "in-degree" (grep -c "in-degree" workflows/write-code.md returns >= 1)
    - workflows/write-code.md contains "auto-serialize" or "dời.*wave" pattern
    - workflows/write-code.md contains "index.ts" in hotspot table
    - workflows/write-code.md contains "app.module.ts" in hotspot table
    - workflows/write-code.md contains "layout.tsx" in hotspot table
    - workflows/write-code.md contains "pubspec.yaml" in hotspot table
    - workflows/write-code.md contains "functions.php" in hotspot table
    - workflows/write-code.md contains "> Files:" and "cross-reference" or "giao nhau"
    - workflows/write-code.md contains "KHÔNG dump toàn bộ PLAN" is NOT required here (that's in Bước 10 — Plan 02)
    - workflows/write-code.md contains "build fail" and "DỪNG" is NOT required here (that's in Bước 10 — Plan 02)
    - All 6 Bước 1.5-related smoke tests from Task 1 now PASS (tests 1-6)
    - Test 7 (plan.md > Files: enforcement) still FAILS — that's Plan 02
    - All existing tests still PASS
  </acceptance_criteria>
  <done>Bước 1.5 expanded with Kahn's algorithm, hotspot patterns table (6 stacks), > Files: cross-reference, auto-serialize logic, wave plan display, and confirmation UX. 6 of 7 new smoke tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `node --test test/smoke-integrity.test.js` — all existing tests pass + 6/7 new tests pass (test 7 for plan.md deferred to Plan 02)
2. `grep -c "hotspot" workflows/write-code.md` returns >= 1
3. `grep -c "in-degree" workflows/write-code.md` returns >= 1
4. `grep -c "auto-serialize" workflows/write-code.md` returns >= 1
5. `grep -c "cross-reference\|giao nhau" workflows/write-code.md` returns >= 1
</verification>

<success_criteria>
- write-code.md Bước 1.5 contains explicit Kahn's algorithm for topological sort
- write-code.md Bước 1.5 contains static hotspot patterns table with all 6 stacks from D-02
- write-code.md Bước 1.5 contains dynamic > Files: cross-reference algorithm
- write-code.md Bước 1.5 contains auto-serialize logic (not hard-stop) per D-05
- write-code.md Bước 1.5 contains wave plan display format per D-09
- write-code.md Bước 1.5 contains confirmation UX per D-10
- Smoke tests verify presence of all critical instructions
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/08-wave-based-parallel-execution/08-01-SUMMARY.md`
</output>
