---
phase: 41-bao-ve-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/plan-checker.js
  - bin/plan-check.js
  - test/smoke-plan-checker.test.js
autonomous: true
requirements:
  - GUARD-01
  - GUARD-02
must_haves:
  truths:
    - "CHECK-06 tra PASS khi research dir rong (khong co files)"
    - "CHECK-06 tra WARN khi co research files nhung plan khong reference .planning/research/"
    - "CHECK-06 tra PASS khi plan co reference .planning/research/"
    - "CHECK-06 configurable severity WARN/BLOCK/OFF"
    - "CHECK-07 tra PASS khi < 2 hedging patterns"
    - "CHECK-07 tra WARN khi >= 2 hedging patterns voi goi y pd research"
    - "CHECK-07 configurable severity WARN/BLOCK/OFF"
    - "runAllChecks tra ve 10 checks (khong phai 8)"
  artifacts:
    - path: "bin/lib/plan-checker.js"
      provides: "checkResearchBacking, checkHedgingLanguage functions"
      exports: ["checkResearchBacking", "checkHedgingLanguage"]
    - path: "bin/plan-check.js"
      provides: "CLI truyen check06Options + check07Severity vao runAllChecks"
    - path: "test/smoke-plan-checker.test.js"
      provides: "Tests cho CHECK-06, CHECK-07, checks.length === 10"
  key_links:
    - from: "bin/lib/plan-checker.js"
      to: "runAllChecks"
      via: "checkResearchBacking + checkHedgingLanguage trong checks array"
      pattern: "checkResearchBacking.*checkHedgingLanguage"
    - from: "bin/plan-check.js"
      to: "runAllChecks"
      via: "truyen check06Options va check07Severity"
      pattern: "check06Options.*check07Severity"
---

<objective>
Them CHECK-06 (Research Backing) va CHECK-07 (Hedging Language) vao plan-checker.js. Ca 2 la pure functions theo dung pattern 8 checks hien co, severity WARN default configurable.

Purpose: Plan-checker tu dong phat hien khi plan thieu research backing hoac co ngon ngu mo ho — giup nguoi dung biet can nghien cuu them truoc khi code.
Output: 2 check functions moi, CLI update, tests pass voi 10 checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-bao-ve-workflow/41-CONTEXT.md
@.planning/phases/41-bao-ve-workflow/41-RESEARCH.md

@bin/lib/plan-checker.js
@bin/plan-check.js
@test/smoke-plan-checker.test.js

<interfaces>
<!-- Pattern tu CHECK-05 (line 698): -->
function checkLogicCoverage(planContent, tasksContent, options = {}) {
  const severity = options.severity || 'warn';
  const result = { checkId: 'CHECK-05', status: 'pass', issues: [] };
  // ... logic ...
  result.status = result.issues.length > 0 ? severity : 'pass';
  return result;
}

<!-- runAllChecks (line 954): -->
function runAllChecks({ planContent, tasksContent, requirementIds, check05Severity }) {
  const checks = [
    checkRequirementCoverage(planContent, requirementIds),
    checkTaskCompleteness(planContent, tasksContent),
    checkDependencyCorrectness(planContent, tasksContent),
    checkTruthTaskCoverage(planContent, tasksContent),
    checkLogicCoverage(planContent, tasksContent, { severity: check05Severity }),
    checkKeyLinks(planContent, tasksContent),
    checkScopeThresholds(planContent, tasksContent),
    checkEffortClassification(planContent, tasksContent),
  ];
  // ... aggregate ...
  return { overall, checks };
}

<!-- module.exports (line 975): -->
module.exports = {
  detectPlanFormat, checkRequirementCoverage, checkTaskCompleteness,
  checkDependencyCorrectness, checkTruthTaskCoverage, checkLogicCoverage,
  runAllChecks, checkKeyLinks, checkScopeThresholds, checkEffortClassification,
  // ... helpers ...
};

<!-- bin/plan-check.js (line 57): -->
const result = runAllChecks({ planContent, tasksContent, requirementIds });

<!-- checks.length assertions trong test (line 828, 909, 1380, 1452): -->
assert.equal(result.checks.length, 8);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Them checkResearchBacking va checkHedgingLanguage vao plan-checker.js + tests</name>
  <files>bin/lib/plan-checker.js, test/smoke-plan-checker.test.js</files>
  <read_first>
    - bin/lib/plan-checker.js (line 690-1007) — CHECK-05 pattern, runAllChecks, exports
    - test/smoke-plan-checker.test.js (line 820-920) — runAllChecks tests pattern
    - test/smoke-plan-checker.test.js (line 1370-1460) — historical validation tests
  </read_first>
  <behavior>
    CHECK-06 checkResearchBacking:
    - Test: hasResearchFiles=false -> status='pass', issues=[] (per D-04)
    - Test: hasResearchFiles=true, planContent co '.planning/research/' -> status='pass'
    - Test: hasResearchFiles=true, planContent KHONG co '.planning/research/' -> status='warn', 1 issue voi fixHint chua 'pd research'
    - Test: severity='off' -> status='pass' bat ke input
    - Test: severity='block', co issue -> status='block'

    CHECK-07 checkHedgingLanguage:
    - Test: planContent co 0 hedging patterns -> status='pass'
    - Test: planContent co 1 hedging pattern -> status='pass' (threshold la 2)
    - Test: planContent co 2+ hedging patterns -> status='warn', 1 issue voi message chua 'pd research'
    - Test: severity='off' -> status='pass'
    - Test: severity='block', co 2+ matches -> status='block'
    - Test: planContent rong/null -> status='pass'

    runAllChecks:
    - Cap nhat checks.length tu 8 len 10 tai 4 vi tri (line 828, 909, 1380, 1452)
    - Test: runAllChecks tra ve 10 checks
  </behavior>
  <action>
    1. VIET TESTS TRUOC (RED phase):
       - Trong test/smoke-plan-checker.test.js, them import checkResearchBacking va checkHedgingLanguage tu plan-checker.js
       - Them describe('CHECK-06: Research Backing') voi 5 test cases theo behavior o tren
       - Them describe('CHECK-07: Hedging Language') voi 6 test cases theo behavior o tren
       - Cap nhat TAT CA 4 cho `checks.length === 8` thanh `checks.length === 10` (line 828, 909, 1380, 1452)
       - Chay tests — PHAI FAIL (functions chua ton tai)

    2. IMPLEMENT (GREEN phase) trong bin/lib/plan-checker.js:
       - Them function checkResearchBacking(planContent, options = {}) TRUOC runAllChecks (per D-01):
         * const severity = options.severity || 'warn' (per D-03)
         * const result = { checkId: 'CHECK-06', status: 'pass', issues: [] }
         * if (!options.hasResearchFiles) return result (per D-04)
         * if (severity === 'off') return result
         * const hasRef = /\.planning\/research\//.test(planContent) (per D-02)
         * Neu khong co ref: push issue voi message 'Plan khong co reference den research files', fixHint chua 'pd research' (per D-05)
         * result.status = result.issues.length > 0 ? severity : 'pass'

       - Them function checkHedgingLanguage(planContent, options = {}) (per D-06):
         * const HEDGING_PATTERNS = /chua ro|can tim hieu|co the.*hoac|khong chac|chua xac dinh|can nghien cuu/gi (per D-07)
         * const severity = options.severity || 'warn' (per D-08)
         * const result = { checkId: 'CHECK-07', status: 'pass', issues: [] }
         * if (severity === 'off') return result
         * if (!planContent) return result
         * const matches = planContent.match(HEDGING_PATTERNS) || []
         * if (matches.length >= 2) push issue voi message chua so matches va 'pd research' (per D-09)
         * result.status = result.issues.length > 0 ? severity : 'pass'

       - Cap nhat runAllChecks signature: them check06Options, check07Severity (per D-05, D-09)
         * Them vao checks array: checkResearchBacking(planContent, check06Options || {}), checkHedgingLanguage(planContent, { severity: check07Severity })
         * Cap nhat JSDoc comment tu "8 checks" thanh "10 checks"

       - Cap nhat module.exports: them checkResearchBacking, checkHedgingLanguage

    3. Chay tests — PHAI PASS
  </action>
  <verify>
    <automated>node --test test/smoke-plan-checker.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "checkResearchBacking" bin/lib/plan-checker.js && echo OK
    - grep -q "checkHedgingLanguage" bin/lib/plan-checker.js && echo OK
    - grep -q "CHECK-06" bin/lib/plan-checker.js && echo OK
    - grep -q "CHECK-07" bin/lib/plan-checker.js && echo OK
    - grep -q "checkResearchBacking" test/smoke-plan-checker.test.js && echo OK
    - grep -q "checkHedgingLanguage" test/smoke-plan-checker.test.js && echo OK
    - grep -c "checks.length, 10" test/smoke-plan-checker.test.js | grep -q "4" && echo "4 assertions updated"
    - node --test test/smoke-plan-checker.test.js 2>&1 | tail -1 | grep -q "pass" && echo TESTS_PASS
  </acceptance_criteria>
  <done>
    CHECK-06 va CHECK-07 la pure functions trong plan-checker.js. runAllChecks tra ve 10 checks. Tat ca tests pass bao gom historical validation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cap nhat bin/plan-check.js CLI truyen check06Options va check07Severity</name>
  <files>bin/plan-check.js</files>
  <read_first>
    - bin/plan-check.js — toan bo file (72 lines)
    - bin/lib/plan-checker.js — runAllChecks signature moi (sau Task 1)
  </read_first>
  <action>
    Cap nhat bin/plan-check.js de truyen 2 params moi vao runAllChecks:

    1. Sau line 55 (sau khi parse requirementIds), them logic kiem tra research dirs:
       ```
       // Kiem tra co research files khong (per D-04)
       const researchInternalDir = path.resolve(planDir, '../../../research/internal');
       const researchExternalDir = path.resolve(planDir, '../../../research/external');
       let hasResearchFiles = false;
       try {
         hasResearchFiles = (
           (fs.existsSync(researchInternalDir) && fs.readdirSync(researchInternalDir).filter(f => f.endsWith('.md')).length > 0) ||
           (fs.existsSync(researchExternalDir) && fs.readdirSync(researchExternalDir).filter(f => f.endsWith('.md')).length > 0)
         );
       } catch {}
       ```

    2. Doc config.json cho severity overrides (NEU co):
       ```
       let check06Severity, check07Severity;
       const configPath = path.resolve(planDir, '../../../config.json');
       if (fs.existsSync(configPath)) {
         try {
           const config = JSON.parse(fs.readFileSync(configPath, 'utf8'));
           check06Severity = config?.checks?.research_backing?.severity;
           check07Severity = config?.checks?.hedging_language?.severity;
         } catch {}
       }
       ```

    3. Cap nhat runAllChecks call (line 57):
       ```
       const check06Options = { hasResearchFiles, severity: check06Severity || 'warn' };
       const result = runAllChecks({
         planContent, tasksContent, requirementIds,
         check06Options,
         check07Severity
       });
       ```
  </action>
  <verify>
    <automated>node bin/plan-check.js .planning/phases/41-bao-ve-workflow 2>&1 | grep -c "CHECK-0" | grep -q "10" && echo "10 checks shown"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "hasResearchFiles" bin/plan-check.js && echo OK
    - grep -q "check06Options" bin/plan-check.js && echo OK
    - grep -q "check07Severity" bin/plan-check.js && echo OK
    - grep -q "research_backing" bin/plan-check.js && echo OK
    - node bin/plan-check.js .planning/phases/41-bao-ve-workflow 2>&1 | grep -q "CHECK-06" && echo CLI_OK
    - node bin/plan-check.js .planning/phases/41-bao-ve-workflow 2>&1 | grep -q "CHECK-07" && echo CLI_OK
  </acceptance_criteria>
  <done>
    CLI hien 10 checks, truyen hasResearchFiles flag va severity configs cho CHECK-06/CHECK-07.
  </done>
</task>

</tasks>

<verification>
node --test test/smoke-plan-checker.test.js — tat ca tests pass bao gom CHECK-06, CHECK-07, checks.length === 10, historical validation
</verification>

<success_criteria>
- plan-checker.js export 2 functions moi: checkResearchBacking, checkHedgingLanguage
- runAllChecks tra ve 10 checks (khong phai 8)
- bin/plan-check.js truyen check06Options + check07Severity tu filesystem + config
- Tat ca tests pass (601+ existing + new CHECK-06/CHECK-07 tests)
- False positive rate: historical validation plans cu van pass (CHECK-06 hasResearchFiles=false -> PASS)
</success_criteria>

<output>
After completion, create `.planning/phases/41-bao-ve-workflow/41-01-SUMMARY.md`
</output>
