---
phase: 01-skill-structure-normalization
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - commands/pd/plan.md
  - commands/pd/new-milestone.md
  - commands/pd/complete-milestone.md
  - commands/pd/what-next.md
  - commands/pd/fetch-doc.md
  - commands/pd/update.md
autonomous: true
requirements:
  - READ-01
  - READ-02

must_haves:
  truths:
    - "All 6 skills follow identical section order: objective, guards, context, execution_context, process, output, rules"
    - "Each skill has guards extracted from context into a dedicated guards section"
    - "Each skill has a new output section with files created, next step, success criteria, error cases"
    - "Each skill has standardized frontmatter with all 5 required fields in consistent order"
    - "fetch-doc and update keep their empty execution_context with explicit comment instead of empty tags"
    - "Reference tags in execution_context are marked required or optional"
  artifacts:
    - path: "commands/pd/plan.md"
      provides: "Normalized plan skill (most references)"
      contains: "<guards>"
    - path: "commands/pd/new-milestone.md"
      provides: "Normalized new-milestone skill"
      contains: "<output>"
    - path: "commands/pd/complete-milestone.md"
      provides: "Normalized complete-milestone skill"
      contains: "<guards>"
    - path: "commands/pd/what-next.md"
      provides: "Normalized what-next skill"
      contains: "<rules>"
    - path: "commands/pd/fetch-doc.md"
      provides: "Normalized fetch-doc skill (section order fixed)"
      contains: "<guards>"
    - path: "commands/pd/update.md"
      provides: "Normalized update skill (inline process preserved)"
      contains: "<output>"
  key_links:
    - from: "commands/pd/fetch-doc.md"
      to: "bin/lib/utils.js"
      via: "extractXmlSection must find reordered sections"
      pattern: "execution_context"
    - from: "commands/pd/update.md"
      to: "test/smoke-integrity.test.js"
      via: "ALLOWED_NO_WORKFLOW set must still include update"
      pattern: "ALLOWED_NO_WORKFLOW"
    - from: "commands/pd/*.md"
      to: "bin/lib/converters/*.js"
      via: "Converters parse skill files"
      pattern: "convertSkill"
---

<objective>
Normalize the second batch of 6 more complex/divergent skill files (plan, new-milestone, complete-milestone, what-next, fetch-doc, update) to the canonical structure. These skills have more inconsistencies: fetch-doc and update have reversed section order (execution_context before objective), update has 120+ lines of inline process, what-next and complete-milestone lack argument-hint, and only fetch-doc and update have existing rules sections.

Purpose: Complete the normalization of all 12 skills. After this plan, every skill file is structurally identical.
Output: 6 normalized skill files in commands/pd/.
</objective>

<execution_context>
@/Users/tonamson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skill-structure-normalization/01-RESEARCH.md
@.planning/phases/01-skill-structure-normalization/01-01-SUMMARY.md

<interfaces>
<!-- Canonical section order from Research -->
The canonical section order for ALL skills is:
1. Frontmatter (name, description, model, argument-hint, allowed-tools)
2. <objective>
3. <guards>
4. <context>
5. <execution_context>
6. <process>
7. <output>
8. <rules>

<!-- Frontmatter field order (consistent across all skills) -->
```yaml
name: pd:skill-name
description: Vietnamese description
model: haiku|sonnet|opus
argument-hint: "[hint text]"
allowed-tools:
  - Tool1
```

<!-- Guard format (checklist) -->
```markdown
<guards>
DUNG va huong dan user neu bat ky dieu kien nao that bai:

- [ ] Condition -> "Error message"
</guards>
```

<!-- Output section format -->
```markdown
<output>
**Tao/Cap nhat:**
- File list

**Buoc tiep theo:** `/pd:next-command`

**Thanh cong khi:**
- Criteria

**Loi thuong gap:**
- Error -> fix
</output>
```

<!-- Special cases from Research -->
- fetch-doc: execution_context is EMPTY currently. Use: "Khong co -- skill nay xu ly truc tiep, khong dung workflow rieng."
- update: execution_context is EMPTY currently. Use: "Khong co -- skill nay xu ly truc tiep, khong dung workflow rieng."
- update: Has 120+ lines inline process. KEEP inline -- do NOT extract to workflow (out of scope).
- what-next: Missing argument-hint. Add: argument-hint: "(khong can tham so)"
- complete-milestone: Missing argument-hint. Add: argument-hint: "(khong can tham so)"
- fetch-doc, update: Already have <rules> sections. KEEP existing rules content, just ensure proper position.
- ALLOWED_NO_WORKFLOW in test file: Must still include 'fetch-doc' and 'update'. Do NOT add workflow files.

<!-- Guard inventory per skill (from Research) -->
plan: CONTEXT.md, ROADMAP.md, CURRENT_MILESTONE.md, --auto/--discuss parse, FastCode + Context7
new-milestone: CONTEXT.md, rules/general.md, milestone name, Context7 + WebSearch
complete-milestone: CONTEXT.md, all tasks done, no open bugs
what-next: .planning/ directory exists (minimal)
fetch-doc: CONTEXT.md, URL validation, WebFetch available
update: .pdconfig exists, network connectivity
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize plan, new-milestone, and complete-milestone skills</name>
  <files>commands/pd/plan.md, commands/pd/new-milestone.md, commands/pd/complete-milestone.md</files>
  <action>
For each of the 3 skills, read the current file and restructure to canonical format.

**General rules (same as Plan 02):**
- MOVE content between sections -- do NOT rewrite behavioral content
- Frontmatter field order: name, description, model, argument-hint, allowed-tools
- Section order: objective, guards, context, execution_context, process, output, rules
- Tag every reference in execution_context as (required) or (optional)
- For skills without existing rules, add `<rules>` with minimal constraints

**plan.md specifics:**
- Has the most execution_context references (8 refs). Tag each: @workflows/plan.md (required), @templates/plan.md (required), @templates/tasks.md (required), @templates/research.md (required), @references/questioning.md (optional), @references/conventions.md (optional), @references/prioritization.md (optional), @references/ui-brand.md (optional)
- Guards: Extract the "Tu kiem tra truoc khi bat dau" block from <context>: CONTEXT.md, ROADMAP.md, CURRENT_MILESTONE.md must exist. Add FastCode + Context7 MCP guards.
- Context: Keep the argument parsing logic and "Doc them" references. Remove the guard block that was extracted.
- Output: Creates RESEARCH.md, PLAN.md, TASKS.md in milestones dir. Next step: `/pd:write-code`. Success: plan covers all requirements, tasks are specific.
- Rules: Add minimal rules (e.g., Vietnamese output, respect --auto/--discuss mode)

**new-milestone.md specifics:**
- Read current file first
- Guards: `.planning/CONTEXT.md` exists, `rules/general.md` exists, milestone name provided, Context7 + WebSearch MCP
- Output: Creates ROADMAP.md, REQUIREMENTS.md, CURRENT_MILESTONE.md, etc. Next step: `/pd:plan`.
- Rules: Add minimal rules from existing content

**complete-milestone.md specifics:**
- Missing argument-hint -- add `argument-hint: "(khong can tham so)"`
- Read current file first
- Guards: `.planning/CONTEXT.md` exists, all tasks in milestone are done, no open bugs
- Output: Derive from objective/process. Next step: `/pd:new-milestone` or project complete.
- Rules: Add minimal rules

After each file, run `node --test test/smoke-integrity.test.js` to verify existing tests still pass.
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && node --test test/smoke-integrity.test.js 2>&1 | grep -E "(pass|fail|#)" | head -20</automated>
  </verify>
  <done>plan.md, new-milestone.md, complete-milestone.md follow canonical structure. Existing smoke tests still pass. Each file has all 7 sections in correct order, all 5 frontmatter fields, guards separated from context, output section, references tagged.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize what-next, fetch-doc, and update skills</name>
  <files>commands/pd/what-next.md, commands/pd/fetch-doc.md, commands/pd/update.md</files>
  <action>
For each of the 3 skills, read the current file and restructure to canonical format. These are the most divergent skills -- pay special attention to the noted issues.

**what-next.md specifics:**
- Missing argument-hint -- add `argument-hint: "(khong can tham so)"`
- Guards: Minimal -- ".planning/ directory exists" or "Khong co dieu kien tien quyet dac biet. Skill nay chi doc, khong sua file."
- Output: Derive from objective -- shows progress report + suggested command. Next step: the suggested command itself.
- Rules: Add minimal rules (e.g., read-only, Vietnamese output, no MCP calls)

**fetch-doc.md specifics (CRITICAL -- section order is currently WRONG):**
- Currently has `<execution_context>` BEFORE `<objective>` -- must fix order to: objective, guards, context, execution_context, process, output, rules
- execution_context is currently empty tags `<execution_context></execution_context>` -- replace with: `<execution_context>` containing "Khong co -- skill nay xu ly truc tiep, khong dung workflow rieng." and `</execution_context>`
- Guards: Extract from process step 1 -- `.planning/CONTEXT.md` exists (or create .planning/docs/), URL is valid, WebFetch available
- Output: Creates `.planning/docs/[ten].md`. Next step: `/pd:plan` or `/pd:write-code`. Success: doc cached with version + table of contents.
- Rules: Already has rules section -- KEEP existing content exactly, just ensure it's in the last position
- The process section is long (90 lines) -- keep it intact, just move to correct position

**update.md specifics (CRITICAL -- most divergent skill):**
- Currently has `<execution_context>` BEFORE `<objective>` -- must fix order
- execution_context is currently empty tags -- replace with explicit comment: "Khong co -- skill nay xu ly truc tiep, khong dung workflow rieng."
- Has 120+ lines of inline process -- KEEP ALL inline process content exactly as-is. Do NOT extract to workflow.
- Guards: Extract from context -- `.pdconfig` exists (error: "Chua cai dat skills. Chay `node bin/install.js` truoc."), network connectivity for git fetch
- Output: Updates skills via git pull. Next step: restart Claude Code. Success: VERSION updated, .pdconfig updated. Errors: network fail, git conflict.
- Rules: Already has rules section -- KEEP existing content exactly, just ensure it's in the last position
- The context section currently contains the .pdconfig check -- extract that to guards, keep remaining context clean

After all 3 files, run the FULL test suite: `npm test`. This verifies converters, inline workflows, and all integrity tests still work with the reordered sections.

Then verify ALL 12 skills pass the canonical structure tests.
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>what-next.md, fetch-doc.md, update.md follow canonical structure. Full test suite (npm test) passes. fetch-doc and update have fixed section order (objective before execution_context). update's 120+ line inline process is preserved. All 12 skills now pass the canonical structure smoke tests. ALLOWED_NO_WORKFLOW still correctly lists fetch-doc and update.</done>
</task>

</tasks>

<verification>
1. All 6 skill files have sections in order: objective, guards, context, execution_context, process, output, rules
2. All 6 skill files have frontmatter fields: name, description, model, argument-hint, allowed-tools
3. fetch-doc and update: execution_context contains explicit "Khong co" message (not empty)
4. fetch-doc and update: section order fixed (objective before execution_context)
5. update: inline process (120+ lines) preserved unchanged
6. `npm test` passes (full suite including converter tests)
7. ALL 12 skills pass canonical structure smoke tests
8. `git diff` for update.md shows only structural rearrangement, not content changes in process section
</verification>

<success_criteria>
- All 12 skill files (across Plans 02 + 03) follow identical canonical structure
- Full test suite green (all 184+ tests pass)
- All canonical structure smoke tests from Plan 01 pass for all 12 skills
- Converter pipeline verified (Codex, Gemini, Copilot, OpenCode)
- A developer reading any one skill can understand the structure of all other skills
</success_criteria>

<output>
After completion, create `.planning/phases/01-skill-structure-normalization/01-03-SUMMARY.md`
</output>
