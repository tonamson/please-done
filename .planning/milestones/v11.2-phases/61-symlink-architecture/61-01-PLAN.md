---
phase: 61-symlink-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/pd-bug-janitor.md
  - .claude/agents/pd-code-detective.md
  - .claude/agents/pd-codebase-mapper.md
  - .claude/agents/pd-doc-specialist.md
  - .claude/agents/pd-evidence-collector.md
  - .claude/agents/pd-fact-checker.md
  - .claude/agents/pd-feature-analyst.md
  - .claude/agents/pd-fix-architect.md
  - .claude/agents/pd-planner.md
  - .claude/agents/pd-regression-analyzer.md
  - .claude/agents/pd-repro-engineer.md
  - .claude/agents/pd-research-synthesizer.md
  - .claude/agents/pd-sec-fixer.md
  - .claude/agents/pd-sec-reporter.md
  - .claude/agents/pd-sec-scanner.md
  - .claude/agents/pd-security-researcher.md
autonomous: true
requirements: [SYML-01, SYML-02]

must_haves:
  truths:
    - "Tất cả 16 agent files trong .claude/agents/ là symlinks, không phải file thật"
    - "Mỗi symlink trỏ đúng đến file tương ứng trong commands/pd/agents/"
    - "Nội dung đọc qua symlink giống hệt file gốc"
    - "Claude Code vẫn load được agent qua symlink"
  artifacts:
    - path: ".claude/agents/pd-bug-janitor.md"
      provides: "Symlink → ../../commands/pd/agents/pd-bug-janitor.md"
    - path: ".claude/agents/pd-sec-fixer.md"
      provides: "Symlink mới (trước đây không có trong .claude/agents/)"
    - path: ".claude/agents/pd-sec-reporter.md"
      provides: "Symlink mới (trước đây không có trong .claude/agents/)"
  key_links:
    - from: ".claude/agents/*.md"
      to: "commands/pd/agents/*.md"
      via: "relative symlink ../../commands/pd/agents/"
      pattern: "readlink.*\\.claude/agents/.*\\.md"
---

<objective>
Thay thế 14 file thật trong `.claude/agents/` bằng symlinks và tạo thêm 2 symlinks mới, tổng cộng 16 symlinks trỏ về `commands/pd/agents/` (source of truth).

Purpose: Duy trì tương thích Claude Code (đọc agents từ `.claude/agents/`) trong khi centralize source tại `commands/pd/agents/`.
Output: 16 relative symlinks trong `.claude/agents/`, 3 test thực tế xác nhận hoạt động.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-agent-consolidation/60-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo 16 relative symlinks</name>
  <files>.claude/agents/pd-bug-janitor.md, .claude/agents/pd-code-detective.md, .claude/agents/pd-codebase-mapper.md, .claude/agents/pd-doc-specialist.md, .claude/agents/pd-evidence-collector.md, .claude/agents/pd-fact-checker.md, .claude/agents/pd-feature-analyst.md, .claude/agents/pd-fix-architect.md, .claude/agents/pd-planner.md, .claude/agents/pd-regression-analyzer.md, .claude/agents/pd-repro-engineer.md, .claude/agents/pd-research-synthesizer.md, .claude/agents/pd-sec-fixer.md, .claude/agents/pd-sec-reporter.md, .claude/agents/pd-sec-scanner.md, .claude/agents/pd-security-researcher.md</files>
  <action>
    Per SYML-01: Tạo relative symlinks trong `.claude/agents/` trỏ đến `commands/pd/agents/`.

    1. Xoá 14 file thật hiện có trong `.claude/agents/`:
       ```bash
       cd .claude/agents && rm -f pd-*.md
       ```

    2. Tạo 16 symlinks (bao gồm 2 file mới pd-sec-fixer, pd-sec-reporter):
       ```bash
       for agent in pd-bug-janitor pd-code-detective pd-codebase-mapper pd-doc-specialist pd-evidence-collector pd-fact-checker pd-feature-analyst pd-fix-architect pd-planner pd-regression-analyzer pd-repro-engineer pd-research-synthesizer pd-sec-fixer pd-sec-reporter pd-sec-scanner pd-security-researcher; do
         ln -s "../../commands/pd/agents/${agent}.md" "${agent}.md"
       done
       ```

    Relative path `../../commands/pd/agents/` vì `.claude/agents/` cách project root 2 cấp.

  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && test $(find .claude/agents -name "pd-*.md" -type l | wc -l | tr -d ' ') -eq 16 && echo "PASS: 16 symlinks" || echo "FAIL"</automated>
  </verify>
  <done>16 symlinks tồn tại trong .claude/agents/, không còn file thật nào cho pd-*.md</done>
</task>

<task type="auto">
  <name>Task 2: Verify symlinks — 3 test thực tế</name>
  <files></files>
  <action>
    Per SYML-02: Chạy 3 bài test thực tế xác nhận symlinks hoạt động:

    **Test 1 — Readlink resolve:** Kiểm tra tất cả 16 symlinks resolve đúng target:
    ```bash
    cd .claude/agents
    for f in pd-*.md; do
      target=$(readlink "$f")
      if [[ "$target" != "../../commands/pd/agents/$f" ]]; then
        echo "FAIL: $f -> $target (expected ../../commands/pd/agents/$f)"
        exit 1
      fi
    done
    echo "PASS: all readlinks correct"
    ```

    **Test 2 — Content integrity:** So sánh nội dung đọc qua symlink vs file gốc (md5):
    ```bash
    cd /Volumes/Code/Nodejs/please-done
    for agent in commands/pd/agents/pd-*.md; do
      name=$(basename "$agent")
      sym=".claude/agents/$name"
      if ! diff -q "$agent" "$sym" > /dev/null 2>&1; then
        echo "FAIL: content mismatch $name"
        exit 1
      fi
    done
    echo "PASS: all content matches"
    ```

    **Test 3 — Smoke test existing:** Chạy smoke test agent files nếu có:
    ```bash
    cd /Volumes/Code/Nodejs/please-done
    node --experimental-vm-modules node_modules/.bin/jest test/smoke-agent-files.test.js --no-coverage 2>&1 || true
    ```
    Nếu test fail do path mismatch (AGENTS_DIR vẫn trỏ `.claude/agents/`) — đó là expected, sẽ fix ở Phase 62. Chỉ cần confirm symlinks readable.

  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && readlink .claude/agents/pd-bug-janitor.md | grep -q "commands/pd/agents" && diff -q .claude/agents/pd-sec-fixer.md commands/pd/agents/pd-sec-fixer.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>3 test thực tế pass: (1) readlink đúng target, (2) nội dung qua symlink khớp file gốc, (3) file readable qua symlink kể cả 2 file mới pd-sec-fixer/pd-sec-reporter</done>
</task>

</tasks>

<verification>
- `find .claude/agents -name "pd-*.md" -type l | wc -l` = 16
- `find .claude/agents -name "pd-*.md" -type f | wc -l` = 0 (không còn file thật)
- `readlink .claude/agents/pd-sec-fixer.md` = `../../commands/pd/agents/pd-sec-fixer.md`
- `cat .claude/agents/pd-sec-fixer.md` trả về nội dung hợp lệ
</verification>

<success_criteria>

- 16 relative symlinks trong `.claude/agents/` trỏ đúng đến `commands/pd/agents/`
- Không còn file thật nào (pd-\*.md) trong `.claude/agents/`
- Nội dung đọc qua symlink giống hệt file gốc
- 3 test thực tế đều pass
  </success_criteria>

<output>
After completion, create `.planning/phases/61-symlink-architecture/61-01-SUMMARY.md`
</output>
