---
phase: 055-parallel-dispatch-wiring
plan: 02
type: execute
wave: 2
depends_on: ["055-01"]
files_modified:
  - bin/lib/parallel-dispatch.js
  - test/smoke-parallel-dispatch.test.js
autonomous: true
requirements:
  - PARA-01
  - PARA-02
  - PARA-03
  - PARA-04

must_haves:
  truths:
    - "buildScannerPlan() khong truyen batchSize thi dung getAdaptiveParallelLimit().workers"
    - "buildScannerPlan() truyen batchSize explicit thi dung gia tri do (khong adaptive)"
    - "isHeavyAgent pd-sec-scanner la true thi batchSize giam 1 (nhung khong duoi PARALLEL_MIN)"
    - "batchSize luon trong khoang [PARALLEL_MIN, PARALLEL_MAX] = [2, 4]"
    - "mergeScannerResults set backpressure=true khi shouldDegrade(error) la true"
    - "mergeParallelResults set backpressure=true khi shouldDegrade(error) la true"
    - "buildParallelPlan giu nguyen 2 agents co dinh (khong adaptive)"
  artifacts:
    - path: "bin/lib/parallel-dispatch.js"
      provides: "Adaptive dispatch + backpressure"
      contains: "getAdaptiveParallelLimit"
    - path: "test/smoke-parallel-dispatch.test.js"
      provides: "Tests cho adaptive, heavy, min/max, backpressure"
      contains: "backpressure"
  key_links:
    - from: "bin/lib/parallel-dispatch.js"
      to: "bin/lib/resource-config.js"
      via: "require('./resource-config')"
      pattern: "getAdaptiveParallelLimit|isHeavyAgent|shouldDegrade|PARALLEL_MIN|PARALLEL_MAX"
---

<objective>
Wire adaptive worker count, heavy agent check, min/max enforcement, va backpressure flag vao parallel-dispatch.js.

Purpose: PARA-01 (adaptive), PARA-02 (heavy agent), PARA-03 (min/max), PARA-04 (backpressure).
Output: parallel-dispatch.js goi resource-config functions thay vi hardcode, merge functions tra backpressure flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/055-parallel-dispatch-wiring/055-CONTEXT.md
@.planning/phases/055-parallel-dispatch-wiring/055-01-SUMMARY.md

<interfaces>
From bin/lib/resource-config.js (exports used by this plan):
```javascript
// Functions to import
function getAdaptiveParallelLimit()
  // Returns: { workers: number, reason: string, cpu: number, freeMemGB: string, loadAvg: number }

function isHeavyAgent(agentName)
  // Returns: boolean — true neu agent co tool match HEAVY_TOOL_PATTERNS (mcp__fastcode__)

function shouldDegrade(error)
  // Returns: boolean — true neu error.code in DEGRADATION_CODES || duration > 120s || message match agent.*fail

// Constants
const PARALLEL_MIN = 2;
const PARALLEL_MAX = 4;
```

From bin/lib/parallel-dispatch.js (functions to modify):
```javascript
// Line 13: current import
const { getAgentConfig } = require('./resource-config');

// Line 113: buildScannerPlan signature
function buildScannerPlan(categories, batchSize = 2, scanPath = '.')

// Line 157: mergeScannerResults signature
function mergeScannerResults(scanResults)
  // Returns: { results, completedCount, failedCount, warnings }

// Line 66: mergeParallelResults signature
function mergeParallelResults({ detectiveResult, docSpecResult })
  // Returns: { results, allSucceeded, warnings }

// Line 27: buildParallelPlan — giu nguyen, chi them comment
function buildParallelPlan(sessionDir, janitarEvidencePath)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Them tests cho adaptive, heavy, min/max, backpressure</name>
  <files>test/smoke-parallel-dispatch.test.js</files>
  <read_first>
    - test/smoke-parallel-dispatch.test.js
    - bin/lib/parallel-dispatch.js
    - bin/lib/resource-config.js
  </read_first>
  <behavior>
    - PARA-01: buildScannerPlan(categories) khong truyen batchSize → waves phu thuoc adaptive (khong phai hardcode 2)
    - PARA-01: buildScannerPlan(categories, 3) truyen explicit → van dung 3
    - PARA-02: buildScannerPlan giam batch khi isHeavyAgent('pd-sec-scanner') = true (luc nay no la true vi pd-sec-scanner co mcp__fastcode__code_qa)
    - PARA-03: batchSize luon >= PARALLEL_MIN (2) va <= PARALLEL_MAX (4)
    - PARA-04: mergeScannerResults tra ve backpressure field, = true khi co shouldDegrade error
    - PARA-04: mergeParallelResults tra ve backpressure field, = true khi co shouldDegrade error
    - D-02: buildParallelPlan van tra ve 2 agents (khong doi)
  </behavior>
  <action>
Them 4 describe blocks moi vao cuoi file `test/smoke-parallel-dispatch.test.js`:

Import them tu resource-config de dung constants trong assertions:
```javascript
const { PARALLEL_MIN, PARALLEL_MAX } = require('../bin/lib/resource-config');
```

**Block 1: buildScannerPlan — adaptive default (PARA-01)**
```javascript
describe('buildScannerPlan — adaptive default (PARA-01)', () => {
  const ALL_13 = [
    'sql-injection', 'xss', 'cmd-injection', 'path-traversal',
    'secrets', 'auth', 'deserialization', 'misconfig',
    'prototype-pollution', 'crypto', 'insecure-design', 'vuln-deps', 'logging',
  ];

  it('khong truyen batchSize → dung adaptive (khong hardcode 2)', () => {
    const plan = buildScannerPlan(ALL_13);
    // Adaptive workers phu thuoc may, nhung totalScanners luon = 13
    assert.equal(plan.totalScanners, 13);
    // batchSize phai trong [PARALLEL_MIN, PARALLEL_MAX]
    const inferredBatch = plan.waves[0].length;
    assert.ok(inferredBatch >= PARALLEL_MIN, `batch ${inferredBatch} >= ${PARALLEL_MIN}`);
    assert.ok(inferredBatch <= PARALLEL_MAX, `batch ${inferredBatch} <= ${PARALLEL_MAX}`);
  });

  it('truyen batchSize explicit → van dung gia tri do', () => {
    const plan = buildScannerPlan(ALL_13, 3);
    // Explicit 3, nhung heavy agent co the giam xuong 2
    const inferredBatch = plan.waves[0].length;
    assert.ok(inferredBatch >= PARALLEL_MIN);
    assert.ok(inferredBatch <= 3);
  });
});
```

**Block 2: buildScannerPlan — heavy agent (PARA-02)**
```javascript
describe('buildScannerPlan — heavy agent check (PARA-02)', () => {
  it('pd-sec-scanner la heavy (co mcp__fastcode__) → batch giam so voi adaptive', () => {
    // pd-sec-scanner co tool mcp__fastcode__code_qa → isHeavyAgent = true
    // Khi adaptive = N va N > PARALLEL_MIN → batch = N - 1
    const plan = buildScannerPlan(['xss', 'auth', 'secrets', 'crypto']);
    const batchSize = plan.waves[0].length;
    assert.ok(batchSize >= PARALLEL_MIN, `batch ${batchSize} >= PARALLEL_MIN`);
    assert.ok(batchSize <= PARALLEL_MAX, `batch ${batchSize} <= PARALLEL_MAX`);
  });
});
```

**Block 3: buildScannerPlan — min/max enforce (PARA-03)**
```javascript
describe('buildScannerPlan — min/max enforce (PARA-03)', () => {
  it('batchSize khong bao gio duoi PARALLEL_MIN', () => {
    const plan = buildScannerPlan(['xss', 'auth']);
    assert.ok(plan.waves[0].length >= PARALLEL_MIN || plan.totalScanners < PARALLEL_MIN);
  });

  it('batchSize khong bao gio vuot PARALLEL_MAX', () => {
    const plan = buildScannerPlan(['xss', 'auth', 'secrets', 'crypto', 'cmd-injection'], 10);
    // Even explicit 10 should be clamped to PARALLEL_MAX
    assert.ok(plan.waves[0].length <= PARALLEL_MAX, `batch ${plan.waves[0].length} <= ${PARALLEL_MAX}`);
  });
});
```

**Block 4: Backpressure flag (PARA-04)**
```javascript
describe('mergeScannerResults — backpressure (PARA-04)', () => {
  function makeScannerEvidence(category) {
    return \`---\nagent: pd-sec-scanner\noutcome: root_cause\ntimestamp: 2026-03-26T10:00:00Z\nsession: SCAN001\n---\n## ROOT CAUSE FOUND\n\n## Nguyên nhân\nLoi \${category}.\n\n## Bằng chứng\nsrc/app.js:10\n\n## Đề xuất\nFix \${category}.\`;
  }

  it('tra ve backpressure=false khi khong co degradation error', () => {
    const result = mergeScannerResults([
      { category: 'xss', evidenceContent: makeScannerEvidence('xss') },
    ]);
    assert.equal(result.backpressure, false);
  });

  it('tra ve backpressure=true khi co TIMEOUT error', () => {
    const result = mergeScannerResults([
      { category: 'xss', evidenceContent: null, error: { code: 'TIMEOUT' } },
    ]);
    assert.equal(result.backpressure, true);
  });

  it('tra ve backpressure=true khi co duration > 120000ms', () => {
    const result = mergeScannerResults([
      { category: 'xss', evidenceContent: null, error: { duration: 150000 } },
    ]);
    assert.equal(result.backpressure, true);
  });
});

describe('mergeParallelResults — backpressure (PARA-04)', () => {
  it('tra ve backpressure=false khi khong co degradation error', () => {
    const detectiveEvidence = \`---\nagent: pd-code-detective\noutcome: root_cause\ntimestamp: 2026-03-24T10:00:00+07:00\nsession: S001\n---\n## ROOT CAUSE FOUND\n\n## Nguyên nhân\nLoi o dong 42.\n\n## Bằng chứng\nsrc/api.js:42\n\n## Đề xuất\nSua dong 42.\`;
    const result = mergeParallelResults({
      detectiveResult: { evidenceContent: detectiveEvidence },
      docSpecResult: { evidenceContent: null, error: { message: 'simple error' } },
    });
    assert.equal(result.backpressure, false);
  });

  it('tra ve backpressure=true khi co RESOURCE_EXHAUSTED error', () => {
    const result = mergeParallelResults({
      detectiveResult: { error: { code: 'RESOURCE_EXHAUSTED' } },
      docSpecResult: { error: { message: 'ok' } },
    });
    assert.equal(result.backpressure, true);
  });
});
```
  </action>
  <verify>
    <automated>node --test test/smoke-parallel-dispatch.test.js 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test/smoke-parallel-dispatch.test.js chua `adaptive default (PARA-01)`
    - test/smoke-parallel-dispatch.test.js chua `heavy agent check (PARA-02)`
    - test/smoke-parallel-dispatch.test.js chua `min/max enforce (PARA-03)`
    - test/smoke-parallel-dispatch.test.js chua `backpressure (PARA-04)`
    - test/smoke-parallel-dispatch.test.js chua `PARALLEL_MIN, PARALLEL_MAX`
    - Tests FAIL (RED) vi adaptive/heavy/backpressure chua implement trong parallel-dispatch.js
  </acceptance_criteria>
  <done>Tests cho 4 PARA requirements da viet, FAIL RED vi chua implement</done>
</task>

<task type="auto">
  <name>Task 2: Wire adaptive + heavy + min/max + backpressure vao parallel-dispatch.js</name>
  <files>bin/lib/parallel-dispatch.js</files>
  <read_first>
    - bin/lib/parallel-dispatch.js
    - bin/lib/resource-config.js
    - test/smoke-parallel-dispatch.test.js
  </read_first>
  <action>
Sua `bin/lib/parallel-dispatch.js` theo 4 thay doi:

**1. Mo rong import (dong 13):**
Thay:
```javascript
const { getAgentConfig } = require('./resource-config');
```
Bang:
```javascript
const { getAgentConfig, getAdaptiveParallelLimit, isHeavyAgent, shouldDegrade, PARALLEL_MIN, PARALLEL_MAX } = require('./resource-config');
```

**2. Sua buildScannerPlan (dong 113) — PARA-01, PARA-02, PARA-03:**
Thay signature va body:
```javascript
function buildScannerPlan(categories, batchSize = null, scanPath = '.') {
  if (!Array.isArray(categories) || categories.length === 0) {
    return {
      waves: [], totalWaves: 0, totalScanners: 0,
      warnings: ['Danh sach categories rong — khong co scanner nao de dispatch'],
    };
  }

  // D-01: adaptive default khi caller khong truyen batchSize
  if (batchSize == null) {
    batchSize = getAdaptiveParallelLimit().workers;
  }

  // D-05: heavy agent check — pd-sec-scanner co mcp__fastcode__, giam 1 worker
  if (isHeavyAgent('pd-sec-scanner') && batchSize > PARALLEL_MIN) {
    batchSize -= 1;
  }

  // PARA-03: enforce min/max [2, 4]
  batchSize = Math.max(PARALLEL_MIN, Math.min(PARALLEL_MAX, batchSize));

  const waves = [];
  for (let i = 0; i < categories.length; i += batchSize) {
    const batch = categories.slice(i, i + batchSize);
    const wave = batch.map(cat => ({
      category: cat,
      agentName: 'pd-sec-scanner',
      outputFile: `evidence_sec_${cat}.md`,
    }));
    waves.push(wave);
  }

  return { waves, totalWaves: waves.length, totalScanners: categories.length, warnings: [] };
}
```

**3. Sua mergeScannerResults (dong 157) — PARA-04:**
Them `shouldDegrade` check va `backpressure` field:
- Them `let backpressure = false;` sau `let failedCount = 0;`
- Trong vong lap `for (const item of scanResults)`, them TRUOC `if (item.evidenceContent)`:
```javascript
    // D-07: check shouldDegrade cho backpressure
    if (item.error && shouldDegrade(item.error)) {
      backpressure = true;
    }
```
- Sua return statement thanh:
```javascript
  return { results, completedCount, failedCount, warnings, backpressure };
```

**4. Sua mergeParallelResults (dong 66) — PARA-04:**
Them `shouldDegrade` check va `backpressure` field:
- Them `let backpressure = false;` sau `const results = [];`
- Trong block xu ly detectiveResult (dong 77-81), them SAU `} else {`:
```javascript
    if (detectiveResult?.error && shouldDegrade(detectiveResult.error)) {
      backpressure = true;
    }
```
- Trong block xu ly docSpecResult (dong 90-94), them SAU `} else {`:
```javascript
    if (docSpecResult?.error && shouldDegrade(docSpecResult.error)) {
      backpressure = true;
    }
```
- Sua return statement thanh:
```javascript
  return { results, allSucceeded, warnings, backpressure };
```

**5. Them comment cho buildParallelPlan — D-02/D-06:**
Them comment o dong 27 (truoc function):
```javascript
// D-02: Detective + DocSpec giu nguyen 2 agents co dinh — 2 <= PARALLEL_MIN, khong can adaptive.
// D-06: Detective la heavy (fastcode), DocSpec la light. Ghi chu, khong thay doi logic.
```
KHONG thay doi logic cua buildParallelPlan.
  </action>
  <verify>
    <automated>node --test test/smoke-parallel-dispatch.test.js 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/parallel-dispatch.js dong 13 chua `getAdaptiveParallelLimit, isHeavyAgent, shouldDegrade, PARALLEL_MIN, PARALLEL_MAX`
    - bin/lib/parallel-dispatch.js chua `batchSize = null` (default param)
    - bin/lib/parallel-dispatch.js chua `getAdaptiveParallelLimit().workers`
    - bin/lib/parallel-dispatch.js chua `isHeavyAgent('pd-sec-scanner')`
    - bin/lib/parallel-dispatch.js chua `Math.max(PARALLEL_MIN, Math.min(PARALLEL_MAX, batchSize))`
    - bin/lib/parallel-dispatch.js chua `shouldDegrade(item.error)` trong mergeScannerResults
    - bin/lib/parallel-dispatch.js chua `shouldDegrade(detectiveResult.error)` trong mergeParallelResults
    - bin/lib/parallel-dispatch.js mergeScannerResults return chua `backpressure`
    - bin/lib/parallel-dispatch.js mergeParallelResults return chua `backpressure`
    - bin/lib/parallel-dispatch.js chua comment `D-02:` va `D-06:` truoc buildParallelPlan
    - `node --test test/smoke-parallel-dispatch.test.js` exit code 0 (GREEN)
    - `node --run test` pass toan bo suite
  </acceptance_criteria>
  <done>parallel-dispatch.js dung adaptive workers, heavy agent check, min/max clamp, backpressure flag — tat ca tests GREEN</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-parallel-dispatch.test.js` — tat ca tests pass (cu + moi)
- `node --test test/smoke-resource-config.test.js` — tat ca tests pass
- `node --run test` — full suite pass
- `node -e "const pd = require('./bin/lib/parallel-dispatch'); console.log(pd.buildScannerPlan(['xss','auth','secrets']))"` — waves dung adaptive batch
- `node -e "const pd = require('./bin/lib/parallel-dispatch'); console.log(pd.mergeScannerResults([{category:'x',error:{code:'TIMEOUT'}}]))"` — co backpressure: true
</verification>

<success_criteria>
- buildScannerPlan() khong truyen batchSize → goi getAdaptiveParallelLimit().workers
- buildScannerPlan() truyen explicit batchSize → dung gia tri do
- isHeavyAgent('pd-sec-scanner') = true → giam 1 worker (nhung >= PARALLEL_MIN)
- batchSize luon trong [2, 4]
- mergeScannerResults va mergeParallelResults tra ve backpressure field
- backpressure = true khi shouldDegrade(error) = true
- buildParallelPlan khong thay doi logic, chi them comment D-02/D-06
- Full test suite pass
</success_criteria>

<output>
After completion, create `.planning/phases/055-parallel-dispatch-wiring/055-02-SUMMARY.md`
</output>
