---
phase: 39-tieu-chuan-kiem-chung-chi-muc
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/research-store.js
  - test/smoke-research-store.test.js
autonomous: true
requirements:
  - AUDIT-02
  - AUDIT-04
  - STORE-03
must_haves:
  truths:
    - "validateEvidence phat hien file thieu section Bang chung"
    - "validateEvidence phat hien claim thieu source citation"
    - "appendAuditLog tao header khi file rong va append row khi co header"
    - "generateIndex tao markdown table tu entries array"
    - "generateIndex sort entries theo created (moi nhat truoc)"
  artifacts:
    - path: "bin/lib/research-store.js"
      provides: "validateEvidence, appendAuditLog, generateIndex — 3 functions moi"
      exports: ["validateEvidence", "appendAuditLog", "generateIndex"]
    - path: "test/smoke-research-store.test.js"
      provides: "Tests mo rong cho 3 functions moi"
      min_lines: 80
  key_links:
    - from: "bin/lib/research-store.js"
      to: "research workflow"
      via: "export validateEvidence, appendAuditLog, generateIndex"
      pattern: "module\\.exports.*validateEvidence"
    - from: "bin/lib/research-store.js"
      to: ".planning/research/AUDIT_LOG.md"
      via: "appendAuditLog return string, caller ghi file"
      pattern: "appendAuditLog"
    - from: "bin/lib/research-store.js"
      to: ".planning/research/INDEX.md"
      via: "generateIndex return string, caller ghi file"
      pattern: "generateIndex"
---

<objective>
Mo rong research-store.js voi 3 functions moi: validateEvidence, appendAuditLog, generateIndex — TDD.

Purpose: Hoan thanh 3 requirements con lai cua Phase 39 (AUDIT-02, AUDIT-04, STORE-03).
Output: research-store.js mo rong + tests xanh cho 3 functions moi.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-tieu-chuan-kiem-chung-chi-muc/39-CONTEXT.md
@.planning/phases/39-tieu-chuan-kiem-chung-chi-muc/39-RESEARCH.md

<interfaces>
<!-- Existing exports trong research-store.js — DO NOT modify -->
From bin/lib/research-store.js:
```javascript
const CONFIDENCE_LEVELS = { HIGH: 'HIGH', MEDIUM: 'MEDIUM', LOW: 'LOW' };
const CONFIDENCE_CRITERIA = { HIGH: '...', MEDIUM: '...', LOW: '...' };
const REQUIRED_FIELDS = ['agent', 'created', 'source', 'topic', 'confidence'];
const SOURCE_TYPES = ['internal', 'external'];
function validateConfidence(level) { /* return boolean */ }
function generateFilename(options) { /* return string */ }
function createEntry(options) { /* return { content, filename } */ }
function parseEntry(content) { /* return { frontmatter, body, valid, errors } */ }
```

From bin/lib/evidence-protocol.js (pattern tham khao):
```javascript
function validateEvidence(content) {
  // Return { valid, outcome, agent, warnings }
  // Throw Error khi content null/rong
  // warnings la mang string
}
```

From bin/lib/bug-memory.js (pattern tham khao):
```javascript
function buildIndex(bugRecords) {
  // Return markdown string voi table
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD validateEvidence — RED + GREEN</name>
  <files>bin/lib/research-store.js, test/smoke-research-store.test.js</files>
  <read_first>
    - bin/lib/research-store.js (toan bo — hieu structure de them function dung cho)
    - bin/lib/evidence-protocol.js (validateEvidence pattern — dong 68-109)
    - test/smoke-research-store.test.js (toan bo — hieu test structure)
    - .planning/phases/39-tieu-chuan-kiem-chung-chi-muc/39-CONTEXT.md (D-01 den D-04)
  </read_first>
  <behavior>
    - validateEvidence(content voi "## Bang chung" + claims co source) => { valid: true, warnings: [] }
    - validateEvidence(content KHONG co section "## Bang chung") => { valid: false, warnings: ['thieu section: ## Bang chung'] }
    - validateEvidence(content co section nhung rong) => { valid: false, warnings: ['section Bang chung rong'] }
    - validateEvidence(content co claim KHONG co source separator) => { valid: false, warnings: ['claim thieu source: ...'] }
    - validateEvidence(null) => throw Error('thieu tham so content')
    - validateEvidence('') => throw Error('thieu tham so content')
    - Claim format: `- [text] — [source] (confidence: LEVEL)` — cho phep ca em dash va double dash
    - Section detected by regex: /^## Bang chung/m
  </behavior>
  <action>
    TDD RED-GREEN cycle. Per D-01 den D-04:

    **RED:** Them tests vao test/smoke-research-store.test.js:
    - describe('validateEvidence') block moi
    - Test valid file: co section + claims voi source => { valid: true, warnings: [] }
    - Test thieu section => { valid: false, warnings co 'thieu section' }
    - Test section rong => { valid: false, warnings co 'section Bang chung rong' }
    - Test claim thieu source (khong co '—' hoac '--') => warnings co 'claim thieu source'
    - Test null/empty => throw Error
    - Test content co nhieu sections — chi check section Bang chung
    - Import validateEvidence tu research-store (them vao destructuring hien co)
    - Chay test — PHAI fail
    - Commit: test(39-02): them failing tests cho validateEvidence

    **GREEN:** Them validateEvidence vao bin/lib/research-store.js:
    - Dat truoc module.exports block
    - Function signature: validateEvidence(content) per D-04
    - Throw Error khi content null/rong/khong phai string (nhat quan voi evidence-protocol.js)
    - Kiem tra section `## Bang chung` ton tai (regex /^## Bang chung/m)
    - Extract section content giua ## Bang chung va ## tiep theo (hoac EOF)
    - Kiem tra section khong rong
    - Kiem tra tung claim (lines bat dau `-`) co source separator ('—' em dash HOAC '--' double dash)
    - Return { valid: warnings.length === 0, warnings }
    - Them validateEvidence vao module.exports (KHONG xoa exports cu)
    - Chay test — PHAI pass
    - Commit: feat(39-02): implement validateEvidence cho research files
  </action>
  <verify>
    <automated>node --test test/smoke-research-store.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "validateEvidence" bin/lib/research-store.js
    - grep -q "Bang chung" bin/lib/research-store.js
    - grep -q "validateEvidence" test/smoke-research-store.test.js
    - node --test test/smoke-research-store.test.js 2>&1 | grep -q "pass"
  </acceptance_criteria>
  <done>
    validateEvidence phat hien file thieu section Bang chung, claim thieu source.
    Return { valid, warnings } non-blocking. Tests xanh.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD appendAuditLog + generateIndex — RED + GREEN</name>
  <files>bin/lib/research-store.js, test/smoke-research-store.test.js</files>
  <read_first>
    - bin/lib/research-store.js (toan bo — sau khi Task 1 da them validateEvidence)
    - bin/lib/bug-memory.js (buildIndex pattern — dong 153-231)
    - test/smoke-research-store.test.js (toan bo — sau khi Task 1 da them tests)
    - .planning/phases/39-tieu-chuan-kiem-chung-chi-muc/39-CONTEXT.md (D-05 den D-11)
  </read_first>
  <behavior>
    appendAuditLog:
    - appendAuditLog('', { agent: 'collector', action: 'collect', topic: 'test', sourceCount: 3, confidence: 'HIGH' }) => string co header + 1 row
    - appendAuditLog(existing_with_header, entry) => string co header + old rows + new row
    - appendAuditLog(null, entry) => tao header + 1 row (null treated as empty)
    - Row format: | timestamp | agent | action | topic | sourceCount | confidence |
    - Header: | Timestamp | Agent | Action | Topic | Sources | Confidence |
    - appendAuditLog('', null) => throw Error('thieu tham so entry')

    generateIndex:
    - generateIndex([]) => markdown string voi "Tong so: 0 files" va KHONG co table rows
    - generateIndex([{ fileName, source, topic, confidence, created }]) => markdown table voi 1 row
    - generateIndex(entries nhieu items) => markdown table sorted theo created (moi nhat truoc)
    - generateIndex(null) => markdown string voi "Tong so: 0 files"
    - Table header: | File | Source | Topic | Confidence | Created |
  </behavior>
  <action>
    TDD RED-GREEN cycle. Per D-05 den D-11:

    **RED:** Them tests vao test/smoke-research-store.test.js:
    - describe('appendAuditLog') block moi:
      - Test empty string => tao header + separator + 1 row
      - Test null => tao header + 1 row
      - Test existing content voi header => append row (khong duplicate header)
      - Test row co dung format | timestamp | agent | action | topic | sourceCount | confidence |
      - Test thieu entry => throw Error
      - Import appendAuditLog tu research-store
    - describe('generateIndex') block moi:
      - Test empty array => "Tong so: 0 files", khong co table
      - Test null => "Tong so: 0 files"
      - Test 1 entry => table voi 1 row
      - Test nhieu entries => sorted theo created (moi nhat truoc)
      - Test table header dung format: | File | Source | Topic | Confidence | Created |
      - Import generateIndex tu research-store
    - Chay test — PHAI fail
    - Commit: test(39-02): them failing tests cho appendAuditLog va generateIndex

    **GREEN:** Them 2 functions vao bin/lib/research-store.js:

    appendAuditLog(existingContent, entry):
    - Constants: AUDIT_HEADER = '| Timestamp | Agent | Action | Topic | Sources | Confidence |', AUDIT_SEPARATOR = '|---|---|---|---|---|---|'
    - Validate entry khong null/undefined, co agent/action/topic/sourceCount/confidence
    - Neu existingContent rong/null/khong co AUDIT_HEADER => tao header block
    - Append row: | new Date().toISOString() | entry.agent | entry.action | entry.topic | entry.sourceCount | entry.confidence |
    - Return updated string

    generateIndex(entries):
    - Neu entries null/rong => return markdown header voi "Tong so: 0 files"
    - Sort entries theo created descending (moi nhat truoc) — per Pitfall 3
    - Tao markdown: # Research Index, **Cap nhat:** timestamp, **Tong so:** N files
    - Table header: | File | Source | Topic | Confidence | Created |
    - Map entries thanh rows
    - Return markdown string

    Them appendAuditLog, generateIndex vao module.exports (KHONG xoa exports cu).
    Chay test — PHAI pass.
    Commit: feat(39-02): implement appendAuditLog va generateIndex

    Chay toan bo test suite: npm test — PHAI pass.
  </action>
  <verify>
    <automated>node --test test/smoke-research-store.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "appendAuditLog" bin/lib/research-store.js
    - grep -q "generateIndex" bin/lib/research-store.js
    - grep -q "AUDIT_HEADER\|Timestamp.*Agent.*Action" bin/lib/research-store.js
    - grep -q "Research Index" bin/lib/research-store.js
    - grep -q "appendAuditLog" test/smoke-research-store.test.js
    - grep -q "generateIndex" test/smoke-research-store.test.js
    - node --test test/smoke-research-store.test.js 2>&1 | grep -q "pass"
  </acceptance_criteria>
  <done>
    appendAuditLog tao/append AUDIT_LOG rows, generateIndex tao INDEX markdown table.
    Ca 2 la pure functions (khong ghi file). Tests xanh toan bo bao gom tests cu.
    npm test pass — khong break bat ky test nao.
  </done>
</task>

</tasks>

<verification>
node --test test/smoke-research-store.test.js test/smoke-confidence-scorer.test.js && npm test
</verification>

<success_criteria>
- research-store.js export 7 functions (4 cu + 3 moi) va 4 constants
- validateEvidence return { valid, warnings } — non-blocking
- appendAuditLog tao header khi empty, append row khi co header
- generateIndex tao markdown table sorted theo created
- KHONG modify 4 functions cu (createEntry, parseEntry, validateConfidence, generateFilename)
- KHONG require('fs') trong module
- Toan bo test suite xanh (cu + moi)
</success_criteria>

<output>
After completion, create `.planning/phases/39-tieu-chuan-kiem-chung-chi-muc/39-02-SUMMARY.md`
</output>
