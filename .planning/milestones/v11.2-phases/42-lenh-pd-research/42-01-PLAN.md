---
phase: 42-lenh-pd-research
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/research-store.js
  - test/smoke-research-store.test.js
autonomous: true
requirements:
  - STORE-04

must_haves:
  truths:
    - "routeQuery('ham createUser trong user.service.ts') tra ve 'internal'"
    - "routeQuery('React Query caching strategy') tra ve 'external'"
    - "routeQuery('') tra ve 'external' (fallback)"
    - "routeQuery(null) tra ve 'external' (fallback)"
  artifacts:
    - path: "bin/lib/research-store.js"
      provides: "routeQuery pure function"
      exports: ["routeQuery"]
      contains: "function routeQuery"
    - path: "test/smoke-research-store.test.js"
      provides: "routeQuery unit tests"
      contains: "describe('routeQuery"
  key_links:
    - from: "bin/lib/research-store.js"
      to: "module.exports"
      via: "export routeQuery"
      pattern: "routeQuery"
---

<objective>
Them function routeQuery vao research-store.js theo phuong phap TDD — pure function phan loai query thanh 'internal' hoac 'external' dua tren keyword heuristic.

Purpose: STORE-04 yeu cau lenh pd research tu dong route internal vs external. routeQuery la logic core cho viec nay.
Output: routeQuery function + unit tests trong research-store.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@bin/lib/research-store.js
@test/smoke-research-store.test.js
</context>

<tasks>

<feature>
  <name>routeQuery — Keyword Heuristic Routing</name>
  <files>bin/lib/research-store.js, test/smoke-research-store.test.js</files>
  <read_first>
    - bin/lib/research-store.js (hieu existing module pattern, exports, constants)
    - test/smoke-research-store.test.js (hieu existing test patterns, describe/it structure)
  </read_first>
  <behavior>
    Per D-01: routeQuery(query) return 'internal' | 'external'.
    Per D-02: Keyword heuristic — ten file/function/class/variable -> internal, ten thu vien/API/protocol -> external.
    Per D-03: Fallback khi khong match: default 'external'.

    Test cases (RED phase):

    Internal (phai return 'internal'):
    - routeQuery('ham createUser trong user.service.ts') -> 'internal' (co .ts extension)
    - routeQuery('class AuthController') -> 'internal' (co PascalCase class name)
    - routeQuery('src/lib/utils.js') -> 'internal' (co path pattern src/)
    - routeQuery('file ./config.json') -> 'internal' (co relative path ./)
    - routeQuery('function parseEntry') -> 'internal' (co function keyword)
    - routeQuery('test/smoke-research-store.test.js') -> 'internal' (co test/ path + .js extension)
    - routeQuery('bin/lib/research-store.js exports') -> 'internal' (co bin/ path + .js)
    - routeQuery('interface UserPayload') -> 'internal' (co interface keyword)
    - routeQuery('enum ConfidenceLevel') -> 'internal' (co enum keyword)
    - routeQuery('validateConfidence hoat dong the nao') -> 'internal' (co camelCase function name)

    External (phai return 'external'):
    - routeQuery('React Query caching strategy') -> 'external' (thu vien name)
    - routeQuery('Stripe API webhook configuration') -> 'external' (API name)
    - routeQuery('GraphQL subscription protocol') -> 'external' (protocol)
    - routeQuery('cach dung PostgreSQL index') -> 'external' (external tech)
    - routeQuery('so sanh Redux va Zustand') -> 'external' (library names, no code patterns)

    Edge cases (phai return 'external' — D-03 fallback):
    - routeQuery('') -> 'external'
    - routeQuery(null) -> 'external'
    - routeQuery(undefined) -> 'external'
    - routeQuery(42) -> 'external'
    - routeQuery('   ') -> 'external' (whitespace only)
  </behavior>
  <action>
    **RED phase:** Them describe('routeQuery') vao test/smoke-research-store.test.js voi tat ca test cases o tren. Import routeQuery tu module. Chay tests — phai FAIL vi routeQuery chua ton tai.

    **GREEN phase:** Them routeQuery function vao bin/lib/research-store.js:

    1. Dau file, sau constants hien co, them section `// --- routeQuery ---`
    2. Implement routeQuery(query):
       - Neu typeof query !== 'string' hoac !query.trim() -> return 'external' (D-03)
       - Tao const q = query.trim()
       - Kiem tra internalPatterns (mang regex):
         ```javascript
         const internalPatterns = [
           /\.[tj]sx?(?:\s|$|,|:|'|"|\))/i,          // .ts, .js, .tsx, .jsx extensions
           /\.[mc]js(?:\s|$|,|:|'|"|\))/i,            // .mjs, .cjs
           /\.(?:py|rb|go|rs|java|dart)(?:\s|$|,|:|'|"|\))/i, // other source extensions
           /\.(?:md|json|yaml|yml|toml)(?:\s|$|,|:|'|"|\))/i, // config/doc extensions
           /(?:src|lib|bin|test|commands|workflows)\//i, // path patterns
           /\.\/[a-zA-Z]/,                             // relative paths (./)
           /(?:^|\s)(?:ham|function|class|interface|type|enum)\s+[a-zA-Z]/i, // definition keywords (VN + EN)
           /[a-z][a-zA-Z]+\.(test|spec)\./i,           // test file patterns
           /(?:^|\s)[a-z]+[A-Z][a-zA-Z]*(?:\s|$|\()/,  // camelCase (validateConfidence, createUser)
           /(?:^|\s)[A-Z][a-z]+[A-Z][a-zA-Z]*(?:\s|$)/, // PascalCase (AuthController, UserPayload)
         ];
         ```
       - Loop qua internalPatterns, neu bat ky pattern.test(q) -> return 'internal'
       - Default: return 'external' (D-03)
    3. Them routeQuery vao module.exports object (sau generateIndex)
    4. Chay tests — phai PASS

    **REFACTOR phase (neu can):** Don dep regex, them comments giai thich tung pattern group.
  </action>
  <verify>
    <automated>node --test test/smoke-research-store.test.js</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/research-store.js chua `function routeQuery(query)`
    - bin/lib/research-store.js chua `routeQuery` trong module.exports object
    - test/smoke-research-store.test.js chua `describe('routeQuery`
    - test/smoke-research-store.test.js co it nhat 15 test cases (10 internal + 5 external + edge cases)
    - `node --test test/smoke-research-store.test.js` exit code 0
    - routeQuery('user.service.ts') === 'internal'
    - routeQuery('React Query') === 'external'
    - routeQuery('') === 'external'
    - routeQuery(null) === 'external'
  </acceptance_criteria>
  <done>routeQuery function exported tu research-store.js, tat ca tests PASS, phan loai dung internal/external theo keyword heuristic voi fallback external</done>
</feature>

</tasks>

<verification>
node --test test/smoke-research-store.test.js
</verification>

<success_criteria>
- routeQuery function ton tai va exported trong research-store.js
- Tat ca routeQuery tests PASS (>= 15 test cases)
- Internal patterns nhan dien: file extensions, path patterns, camelCase/PascalCase, definition keywords
- External patterns: fallback cho moi thu khong match internal
- Edge cases xu ly an toan (null, empty, non-string)
</success_criteria>

<output>
After completion, create `.planning/phases/42-lenh-pd-research/42-01-SUMMARY.md`
</output>
