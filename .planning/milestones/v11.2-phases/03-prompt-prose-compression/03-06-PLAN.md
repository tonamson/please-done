---
phase: 03-prompt-prose-compression
plan: 06
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
  - 03-04
  - 03-05
files_modified:
  - templates/plan.md
  - templates/roadmap.md
  - templates/research.md
  - templates/verification-report.md
  - templates/tasks.md
  - templates/requirements.md
  - templates/project.md
  - templates/state.md
  - templates/current-milestone.md
  - templates/progress.md
autonomous: false
requirements:
  - TOKN-02

must_haves:
  truths:
    - "All template placeholder patterns ([bracketed], {curly}) are preserved"
    - "Template structural markdown (tables, headings) is intact"
    - "Instruction comments in templates are compressed but templates still function"
    - "Total token reduction across ALL files (workflows + commands + references + templates) is >= 30%"
    - "Full test suite passes after all compression"
  artifacts:
    - path: "templates/plan.md"
      provides: "Compressed plan template"
    - path: "templates/roadmap.md"
      provides: "Compressed roadmap template"
    - path: "templates/project.md"
      provides: "Compressed project template"
  key_links:
    - from: "templates/plan.md"
      to: "workflows/plan.md"
      via: "plan workflow references plan template"
      pattern: "@templates/plan.md"
    - from: "templates/project.md"
      to: "workflows/init.md"
      via: "init workflow creates project from template"
      pattern: "@templates/project.md"
---

<objective>
Compress all 10 template files (770 lines total), then run final token measurement to verify the 30-40% reduction target is met across all file categories.

Purpose: Templates are the final compression target (12% of total). After this plan, all 4 file categories are compressed and the phase goal is verified.
Output: 10 compressed template files, final token measurement showing >= 30% total reduction.
</objective>

<execution_context>
@/Users/tonamson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-prompt-prose-compression/03-RESEARCH.md
@.planning/phases/03-prompt-prose-compression/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compress all 10 template files</name>
  <files>templates/plan.md, templates/roadmap.md, templates/research.md, templates/verification-report.md, templates/tasks.md, templates/requirements.md, templates/project.md, templates/state.md, templates/current-milestone.md, templates/progress.md</files>
  <action>
For EACH of the 10 template files (48-189 lines each):

**Before compression:**
1. Read the file
2. Identify: placeholder patterns (`[tên]`, `[version]`, `[DD_MM_YYYY]`, `{curly}` patterns), structural markdown (tables, headings, code blocks), instruction comments (`<!-- ... -->`, `> Dung boi:` headers)

**Compression rules:**
- PRESERVE: ALL placeholder patterns ([bracketed] and {curly}) -- these are fill-in fields used at runtime
- PRESERVE: structural markdown (table structure, heading hierarchy, code block fences)
- PRESERVE: YAML frontmatter if present
- COMPRESS: instruction/explanation comments (`<!-- ... -->` content, `> Ghi chu:` lines) -> shorten or remove if obvious
- COMPRESS: `> Dung boi:` header descriptions -> concise 1-line
- COMPRESS: verbose section descriptions that explain what to fill in -> telegraphic
- COMPRESS: repeated guidance text within templates
- COMPRESS: filler words and verbose transitions

**After compression:**
3. Verify all placeholder patterns are still present (count [brackets] and {curlies} before/after -- counts must match)
4. Run smoke tests

**Special cases:**
- plan.md (189 lines): Largest template. Contains detailed planning instructions. Compress instruction prose, keep structural skeleton.
- roadmap.md (89 lines): Contains table structures. Keep tables, compress instructions.
- research.md (76 lines): Contains methodology instructions. Compress verbose guidance.
- Templates are used by skills to create .planning/ files. The template structure IS the output format -- keep structural elements intact.
  </action>
  <verify>
    <automated>node --test 'test/*.test.js'</automated>
  </verify>
  <done>All 10 template files compressed, all placeholder patterns preserved, all tables intact, full test suite passes</done>
</task>

<task type="auto">
  <name>Task 2: Final token measurement and gap remediation</name>
  <files>scripts/count-tokens.js</files>
  <action>
1. Run `node scripts/count-tokens.js --compare` to get the final measurement
2. Record: per-file reduction %, per-category reduction %, total reduction %
3. If total reduction is below 30%:
   - Identify files with lowest compression ratios
   - Apply additional compression passes to those files (review for remaining verbose prose, redundant explanations, further arrow notation opportunities)
   - Re-run measurement until >= 30% achieved
4. If total reduction is above 45%:
   - Review files with highest compression ratios (>50%) for potential over-compression
   - Verify behavioral instructions are truly preserved (not just "technically present" but clear enough for an AI to follow)
5. Run full test suite to confirm everything passes
6. Print final summary table with per-category and total results
  </action>
  <verify>
    <automated>node scripts/count-tokens.js --compare && node --test 'test/*.test.js'</automated>
  </verify>
  <done>Total token reduction >= 30% across all files. Full test suite passes. Per-file reduction tracked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of compression quality</name>
  <what-built>All 45 target files (10 workflows, 12 commands, 7 references, 10 templates) compressed using telegraphic shorthand. Token measurement shows >= 30% total reduction.</what-built>
  <how-to-verify>
    1. Review the `node scripts/count-tokens.js --compare` output to confirm >= 30% total reduction
    2. Spot-check 2-3 compressed files (suggested: workflows/new-milestone.md, commands/pd/write-code.md, templates/plan.md) -- verify they read clearly in telegraphic style and no behavioral instructions feel missing
    3. Run `npm test` to confirm all tests pass
    4. Optionally: invoke a skill (e.g., `/pd:scan`) and verify it behaves identically to before compression
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `node scripts/count-tokens.js --compare` shows >= 30% total reduction
- `node --test 'test/*.test.js'` passes (full suite)
- Per-file reductions logged (no file below 15% except already-dense files, no file above 50%)
- Human verification of compressed output quality
</verification>

<success_criteria>
Phase 3 is complete: total token count across all skill files, workflows, references, and templates is reduced by >= 30% compared to baseline. All behavioral instructions preserved. All tests pass. Human has verified compressed output quality.
</success_criteria>

<output>
After completion, create `.planning/phases/03-prompt-prose-compression/03-06-SUMMARY.md`
</output>
