---
phase: 03-prompt-prose-compression
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - workflows/new-milestone.md
  - workflows/write-code.md
  - workflows/plan.md
  - workflows/fix-bug.md
autonomous: true
requirements:
  - TOKN-02

must_haves:
  truths:
    - "All behavioral instructions in the 4 largest workflows are preserved after compression"
    - "Compressed workflows still inline correctly via inlineWorkflow()"
    - "XML section tags (<process>, <objective>, etc.) remain intact"
    - "Token count for these 4 files is reduced by 25-45% each"
  artifacts:
    - path: "workflows/new-milestone.md"
      provides: "Compressed new-milestone workflow"
      contains: "<process>"
    - path: "workflows/write-code.md"
      provides: "Compressed write-code workflow"
      contains: "<process>"
    - path: "workflows/plan.md"
      provides: "Compressed plan workflow"
      contains: "<process>"
    - path: "workflows/fix-bug.md"
      provides: "Compressed fix-bug workflow"
      contains: "<process>"
  key_links:
    - from: "workflows/new-milestone.md"
      to: "commands/pd/new-milestone.md"
      via: "@workflows/new-milestone.md reference in skill"
      pattern: "@workflows/new-milestone.md"
    - from: "workflows/write-code.md"
      to: "commands/pd/write-code.md"
      via: "@workflows/write-code.md reference in skill"
      pattern: "@workflows/write-code.md"
---

<objective>
Compress the 4 largest workflow files (2,046 lines total) using telegraphic shorthand style while preserving all behavioral instructions.

Purpose: These 4 files represent 61% of workflow content and are the highest-value compression targets.
Output: 4 compressed workflow files, each reduced 25-45% in tokens.
</objective>

<execution_context>
@/Users/tonamson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-prompt-prose-compression/03-RESEARCH.md
@.planning/phases/03-prompt-prose-compression/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compress workflows/new-milestone.md and workflows/write-code.md</name>
  <files>workflows/new-milestone.md, workflows/write-code.md</files>
  <action>
For EACH file (new-milestone.md: 667 lines, write-code.md: 526 lines):

**Before compression:**
1. Read the file fully
2. Extract all behavioral instructions -- scan for: DUNG/STOP conditions, PHAI/MUST requirements, KHONG/MUST NOT prohibitions, AskUserQuestion blocks, conditional branches (Neu...thi), commit actions, create/update/delete actions, jump-to-step instructions, error recovery steps
3. Count these as the behavioral checklist

**Compression rules (apply in this order):**
- DO NOT touch frontmatter (YAML between --- markers)
- DO NOT touch XML section tags (<process>, <objective>, <guards>, etc.) -- keep tags, compress content inside
- DO NOT touch @references/ and @workflows/ paths
- DO NOT touch git/bash commands
- DO NOT touch step numbers (Buoc 1, Buoc 2...) -- used for cross-references
- DO NOT touch guard checklist lines or @references/guard-*.md lines
- DO NOT change diacritical convention (guards: non-diacritical, other sections: diacritical)
- COMPRESS: explanatory paragraphs -> bullet shorthand
- COMPRESS: verbose conditionals -> arrow notation (`X -> khong co -> DUNG`)
- COMPRESS: multi-branch if/else -> tables where 3+ branches
- COMPRESS: purpose descriptions ("> Muc dich:...") -> remove or shorten to 1 line
- COMPRESS: filler words (cac, tat ca cac, sau do, tiep tuc, bat dau, dau tien) when not adding meaning
- COMPRESS: redundant cross-references when target is already in execution_context
- COMPRESS: repeated explanations of the same concept

**After compression:**
4. Verify every behavioral instruction from checklist is still present in compressed version
5. If any missing, add it back
6. Run `node --test test/smoke-integrity.test.js` to verify structure preserved
  </action>
  <verify>
    <automated>node --test test/smoke-integrity.test.js</automated>
  </verify>
  <done>new-milestone.md and write-code.md compressed with all behavioral instructions preserved, smoke tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Compress workflows/plan.md and workflows/fix-bug.md</name>
  <files>workflows/plan.md, workflows/fix-bug.md</files>
  <action>
For EACH file (plan.md: 454 lines, fix-bug.md: 399 lines):

Apply the SAME compression process as Task 1:

1. Read file fully
2. Extract behavioral checklist (DUNG/STOP, PHAI/MUST, KHONG, AskUserQuestion, conditionals, commits, create/update/delete, jump-to-step, error recovery)
3. Apply compression rules:
   - Preserve: frontmatter, XML tags, @reference paths, git/bash commands, step numbers, guard lines, diacritical convention
   - Compress: explanatory paragraphs -> bullets, verbose conditionals -> arrows, multi-branch -> tables, purpose lines -> shorten/remove, filler words -> remove, redundant cross-refs -> remove, repeated explanations -> remove
4. Verify every behavioral instruction from checklist survives compression
5. If any missing, add back
6. Run smoke tests

**Specific to fix-bug.md:** Preserve error recovery steps (Buoc 1.1 pattern) -- these are behavioral, not explanatory.

**Specific to plan.md:** Preserve AskUserQuestion blocks fully (label/description/options structure). Shorten option descriptions but keep options intact.
  </action>
  <verify>
    <automated>node --test test/smoke-integrity.test.js</automated>
  </verify>
  <done>plan.md and fix-bug.md compressed with all behavioral instructions preserved, smoke tests pass</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-integrity.test.js` passes
- `node --test test/smoke-converters.test.js` passes (converter compatibility)
- `node scripts/count-tokens.js --compare` shows reduction for these 4 files
- Each file still contains its <process> section with all steps
</verification>

<success_criteria>
The 4 largest workflow files are compressed 25-45% each while preserving all behavioral instructions. Smoke tests and converter tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-prompt-prose-compression/03-02-SUMMARY.md`
</output>
