---
phase: 03-prompt-prose-compression
plan: 05
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - references/security-checklist.md
  - references/ui-brand.md
  - references/verification-patterns.md
  - references/state-machine.md
  - references/conventions.md
  - references/questioning.md
  - references/prioritization.md
autonomous: true
requirements:
  - TOKN-02

must_haves:
  truths:
    - "Reference file tables retain all rows (each row is a lookup entry)"
    - "Code examples and regex patterns in references are preserved exactly"
    - "Guard micro-template files (references/guard-*.md) are untouched"
    - "Token count reduced 15-30% across reference files"
  artifacts:
    - path: "references/security-checklist.md"
      provides: "Compressed security checklist (prose only, tables intact)"
      contains: "Phan D"
    - path: "references/conventions.md"
      provides: "Compressed conventions reference"
    - path: "references/ui-brand.md"
      provides: "Compressed UI brand reference (tables intact)"
    - path: "references/state-machine.md"
      provides: "Compressed state machine reference (transition tables intact)"
  key_links:
    - from: "references/security-checklist.md"
      to: "workflows/scan.md"
      via: "@references/security-checklist.md in scan workflow"
      pattern: "@references/security-checklist.md"
    - from: "references/conventions.md"
      to: "workflows/conventions.md"
      via: "@references/conventions.md in conventions workflow"
      pattern: "@references/conventions.md"
---

<objective>
Compress 7 non-guard reference files (981 lines total) using telegraphic shorthand while preserving all table content, code examples, and regex patterns.

Purpose: References are lookup tables used during skill execution, representing 16% of total content. Tables must remain intact; only surrounding prose is compressed.
Output: 7 compressed reference files with structural and data integrity preserved.
</objective>

<execution_context>
@/Users/tonamson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-prompt-prose-compression/03-RESEARCH.md
@.planning/phases/03-prompt-prose-compression/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compress 4 larger reference files</name>
  <files>references/security-checklist.md, references/ui-brand.md, references/verification-patterns.md, references/state-machine.md</files>
  <action>
For EACH of the 4 reference files:

**Before compression:**
1. Read the file
2. Identify: tables (keep rows intact), prose paragraphs (compress), code examples (keep), regex patterns (keep)

**Compression rules:**
- PRESERVE: all table rows -- each row is a lookup entry used during skill execution
- PRESERVE: code examples and regex patterns (exact syntax matters)
- PRESERVE: table headers and column structure
- PRESERVE: heading hierarchy (used for section navigation)
- COMPRESS: prose paragraphs between tables -> telegraphic shorthand
- COMPRESS: introductory/explanatory sections -> concise bullets
- COMPRESS: redundant descriptions that restate what a table already shows
- COMPRESS: filler words and verbose transitions

**After compression:**
3. Verify table row counts unchanged
4. Run smoke tests

**Special cases:**
- security-checklist.md (296 lines): BEHAVIORAL-HEAVY. Tables in "Phan D" are lookup tables for code review. Compress surrounding prose only, keep ALL table content intact. This file may only achieve 15-20% reduction.
- ui-brand.md (223 lines): Contains design tokens and style rules. Tables are reference data -- keep intact. Compress explanatory prose.
- verification-patterns.md (150 lines): Contains test patterns. Keep code examples and pattern descriptions.
- state-machine.md (105 lines): Contains state transition tables. Keep tables, compress prose.

**DO NOT touch:** references/guard-*.md files (4 files, 1 line each -- already micro-templates from Phase 2)
  </action>
  <verify>
    <automated>node --test 'test/*.test.js'</automated>
  </verify>
  <done>4 larger reference files compressed with all tables intact, prose reduced, full test suite passes</done>
</task>

<task type="auto">
  <name>Task 2: Compress 3 smaller reference files</name>
  <files>references/conventions.md, references/questioning.md, references/prioritization.md</files>
  <action>
For EACH of the 3 reference files:

Apply the SAME compression process as Task 1:

1. Read file, identify tables/prose/code/regex
2. Compress using telegraphic shorthand:
   - Preserve: all table rows, code examples, regex patterns, table structure, heading hierarchy
   - Compress: prose paragraphs -> shorthand, introductions -> bullets, redundant descriptions -> remove, filler words -> remove
3. Verify table row counts unchanged
4. Run smoke tests

**DO NOT touch:** references/guard-*.md files
  </action>
  <verify>
    <automated>node --test 'test/*.test.js'</automated>
  </verify>
  <done>All 7 reference files compressed with all tables intact, prose reduced, full test suite passes</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-integrity.test.js` passes (skill structure)
- `node --test test/smoke-converters.test.js` passes (converter compatibility)
- `node scripts/count-tokens.js --compare` shows reduction for references/
- All table row counts unchanged in reference files
- guard-*.md files unchanged (still 1 line each)
</verification>

<success_criteria>
All 7 non-guard reference files are compressed 15-30% while preserving all tables, code examples, and regex patterns. Smoke tests and converter tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-prompt-prose-compression/03-05-SUMMARY.md`
</output>
