---
phase: 03-prompt-prose-compression
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - commands/pd/update.md
  - commands/pd/fetch-doc.md
  - commands/pd/write-code.md
  - commands/pd/plan.md
  - commands/pd/new-milestone.md
  - commands/pd/test.md
  - commands/pd/fix-bug.md
  - commands/pd/complete-milestone.md
  - commands/pd/init.md
  - commands/pd/scan.md
  - commands/pd/conventions.md
  - commands/pd/what-next.md
autonomous: true
requirements:
  - TOKN-02

must_haves:
  truths:
    - "All 12 skill files preserve their canonical section order and frontmatter fields"
    - "All behavioral instructions in commands are preserved"
    - "Guard micro-template files (references/guard-*.md) are untouched"
    - "Token count reduced 20-35% across command files"
  artifacts:
    - path: "commands/pd/write-code.md"
      provides: "Compressed write-code skill"
      contains: "<guards>"
    - path: "commands/pd/plan.md"
      provides: "Compressed plan skill"
      contains: "<guards>"
    - path: "commands/pd/update.md"
      provides: "Compressed update skill (inline process preserved)"
      contains: "<process>"
    - path: "commands/pd/fetch-doc.md"
      provides: "Compressed fetch-doc skill (inline process preserved)"
      contains: "<process>"
  key_links:
    - from: "commands/pd/write-code.md"
      to: "workflows/write-code.md"
      via: "@workflows/write-code.md in execution_context"
      pattern: "@workflows/write-code.md"
    - from: "commands/pd/update.md"
      to: "inline process"
      via: "update has inline process (no workflow file)"
      pattern: "<process>"
---

<objective>
Compress all 12 command/skill files (1,125 lines total) using telegraphic shorthand while preserving all behavioral instructions.

Purpose: Commands are skill entry points representing 18% of total content. This plan handles the command file category exclusively.
Output: 12 compressed command files with behavioral and structural integrity preserved.
</objective>

<execution_context>
@/Users/tonamson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonamson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-prompt-prose-compression/03-RESEARCH.md
@.planning/phases/03-prompt-prose-compression/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compress first 6 command/skill files</name>
  <files>commands/pd/init.md, commands/pd/scan.md, commands/pd/write-code.md, commands/pd/test.md, commands/pd/fix-bug.md, commands/pd/conventions.md</files>
  <action>
For EACH of the 6 skill files:

**Before compression:**
1. Read the file
2. Extract behavioral checklist: DUNG/STOP conditions, PHAI/MUST, KHONG/MUST NOT, AskUserQuestion blocks, conditional branches, error handling

**Compression rules:**
- PRESERVE: frontmatter (YAML between ---), all 5 required frontmatter fields (name, description, model, argument-hint, allowed-tools)
- PRESERVE: XML section tags (<objective>, <guards>, <context>, <execution_context>, <process>, <output>, <rules>)
- PRESERVE: canonical section ORDER (Phase 1 convention -- do not reorganize)
- PRESERVE: @references/guard-*.md lines in <guards> sections
- PRESERVE: @workflows/ and @references/ paths in execution_context
- PRESERVE: diacritical convention (guards: non-diacritical, other sections: diacritical)
- COMPRESS: explanatory text within sections -> telegraphic shorthand
- COMPRESS: verbose output section descriptions -> concise bullets
- COMPRESS: filler words and redundant context
- COMPRESS: long descriptions in objective sections -> 1-2 line summaries

**After compression:**
3. Verify every behavioral instruction survives
4. Run `node --test test/smoke-integrity.test.js` after all 6 files

**Important:** These are small files (58-190 lines). Many are already fairly dense after Phase 1 normalization. Expect 20-30% reduction on average. Do not over-compress -- if a file is already dense, 15% is acceptable.

**Special case:**
- conventions.md (58 lines): Already small. Minimal compression expected.
  </action>
  <verify>
    <automated>node --test test/smoke-integrity.test.js</automated>
  </verify>
  <done>6 skill files compressed, canonical structure preserved, all frontmatter fields intact, smoke tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Compress remaining 6 command/skill files</name>
  <files>commands/pd/plan.md, commands/pd/new-milestone.md, commands/pd/complete-milestone.md, commands/pd/what-next.md, commands/pd/fetch-doc.md, commands/pd/update.md</files>
  <action>
For EACH of the 6 skill files:

Apply the SAME compression process as Task 1:

1. Read file, extract behavioral checklist
2. Compress using telegraphic shorthand:
   - Preserve: frontmatter, XML tags, canonical order, guard refs, workflow/reference paths, diacritical convention
   - Compress: explanatory text -> shorthand, output descriptions -> bullets, filler words -> remove, long descriptions -> 1-2 lines
3. Verify all behavioral instructions survive
4. Run smoke tests after all 6 files

**Special cases:**
- update.md (190 lines): Has inline <process> (no workflow file). Compress process content using same rules as workflow compression. Preserve all behavioral steps.
- fetch-doc.md (152 lines): Has inline process. Same treatment as update.md.
  </action>
  <verify>
    <automated>node --test test/smoke-integrity.test.js</automated>
  </verify>
  <done>All 12 skill files compressed, canonical structure preserved, all frontmatter fields intact, smoke tests pass</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-integrity.test.js` passes (skill structure)
- `node --test test/smoke-converters.test.js` passes (converter compatibility)
- `node scripts/count-tokens.js --compare` shows reduction for commands/
- All 12 skills have 5 frontmatter fields and 7 canonical sections
- guard-*.md files unchanged (still 1 line each)
</verification>

<success_criteria>
All 12 command files are compressed 20-35% while preserving canonical structure and all behavioral instructions. Smoke tests and converter tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-prompt-prose-compression/03-04-SUMMARY.md`
</output>
