---
phase: 32-orchestrator-workflow
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - workflows/fix-bug.md
autonomous: true
requirements: [FLOW-03, FLOW-04, FLOW-05]

must_haves:
  truths:
    - "Buoc 3 spawn pd-repro-engineer tao Red Test tu evidence Buoc 2"
    - "Buoc 4 spawn pd-fix-architect tong hop evidence va route 3 outcomes (root_cause, checkpoint, inconclusive)"
    - "Buoc 5 orchestrator truc tiep sua code, chay test, commit fix([LOI]), goi v1.5 modules, tao bug record sau user verify"
    - "Session lifecycle hoan tat: fix commit -> user verify -> createBugRecord -> buildIndex -> updateSession(resolved)"
  artifacts:
    - path: "workflows/fix-bug.md"
      provides: "Orchestrator workflow hoan chinh 5 buoc + rules + success_criteria"
      contains: "pd-fix-architect"
  key_links:
    - from: "workflows/fix-bug.md"
      to: "bin/lib/outcome-router.js"
      via: "goi buildRootCauseMenu, prepareFixNow, prepareFixPlan, prepareSelfFix"
      pattern: "buildRootCauseMenu|prepareFixNow"
    - from: "workflows/fix-bug.md"
      to: "bin/lib/checkpoint-handler.js"
      via: "goi extractCheckpointQuestion, buildContinuationContext"
      pattern: "extractCheckpointQuestion|buildContinuationContext"
    - from: "workflows/fix-bug.md"
      to: "bin/lib/bug-memory.js"
      via: "goi createBugRecord, buildIndex sau user verify"
      pattern: "createBugRecord|buildIndex"
    - from: "workflows/fix-bug.md"
      to: "bin/lib/debug-cleanup.js"
      via: "goi scanDebugMarkers, matchSecurityWarnings truoc commit"
      pattern: "scanDebugMarkers|matchSecurityWarnings"
    - from: "workflows/fix-bug.md"
      to: "bin/lib/logic-sync.js"
      via: "goi runLogicSync sau user verify"
      pattern: "runLogicSync"
    - from: "workflows/fix-bug.md"
      to: "bin/lib/regression-analyzer.js"
      via: "goi analyzeFromCallChain hoac analyzeFromSourceFiles truoc khi fix"
      pattern: "analyzeFromCallChain|analyzeFromSourceFiles"
---

<objective>
Hoan thanh workflow fix-bug.md voi Buoc 3 (Repro Engineer), Buoc 4 (Fix Architect + outcome routing), Buoc 5 (Fix + Commit + Post-fix), rules, va success_criteria.

Purpose: Hoan tat nua sau cua orchestrator workflow — tu tao test tai hien den sua code, commit, va dong session. Tich hop outcome-router, checkpoint-handler, bug-memory, va 3 v1.5 modules.
Output: workflows/fix-bug.md hoan chinh (tat ca 5 buoc + rules + success_criteria)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-orchestrator-workflow/32-CONTEXT.md
@.planning/phases/32-orchestrator-workflow/32-RESEARCH.md
@.planning/phases/32-orchestrator-workflow/32-01-SUMMARY.md

@workflows/fix-bug.md
@bin/lib/outcome-router.js
@bin/lib/checkpoint-handler.js
@bin/lib/bug-memory.js
@bin/lib/debug-cleanup.js
@bin/lib/logic-sync.js
@bin/lib/regression-analyzer.js
@bin/lib/repro-test-generator.js
@bin/lib/evidence-protocol.js
@bin/lib/session-manager.js

@.claude/agents/pd-repro-engineer.md
@.claude/agents/pd-fix-architect.md

<interfaces>
<!-- Key APIs from modules the executor needs -->

From bin/lib/outcome-router.js:
  buildRootCauseMenu(evidenceContent) -> { question, choices: [{id, label, description}] }
  prepareFixNow(evidenceContent) -> { fixInstructions, targetFiles, rootCause }
  prepareFixPlan(evidenceContent, sessionDir) -> { planPath, planContent }
  prepareSelfFix(evidenceContent) -> { summary, suggestedSteps }

From bin/lib/checkpoint-handler.js:
  extractCheckpointQuestion(evidenceContent) -> { question, context }
  buildContinuationContext(previousEvidence, userAnswer, roundNumber) -> { canContinue, prompt, warnings }
  MAX_CONTINUATION_ROUNDS = 2

From bin/lib/bug-memory.js:
  createBugRecord({ file, functionName, errorMessage, rootCause, fix, sessionId }) -> bugRecordMd
  searchBugs(indexContent, { file, functionName, errorMessage }) -> { matches, score }
  buildIndex(bugRecords) -> indexMd

From bin/lib/debug-cleanup.js:
  scanDebugMarkers(stagedFiles) -> [{ file, line, content }]
  matchSecurityWarnings(reportContent, filePaths) -> [{ file, severity, message }]

From bin/lib/logic-sync.js:
  runLogicSync({ diffText, bugReportContent, sessionContent, claudeContent, reportContent, planContents }) -> { hasLogicChange, signals, diagramUpdated, rulesSuggested }

From bin/lib/regression-analyzer.js:
  analyzeFromCallChain({ callChainText, targetFile, targetFunction }) -> { affectedFiles, riskLevel }
  analyzeFromSourceFiles({ sourceFiles, targetFile, targetFunction }) -> { affectedFiles, riskLevel }

From bin/lib/repro-test-generator.js:
  generateReproTest({ symptoms, bugTitle, filePath, functionName }) -> { testFileName, testContent }

From bin/lib/evidence-protocol.js:
  validateEvidence(content) -> { valid, outcome, warnings, sections }
  parseEvidence(content) -> { frontmatter, body, sections }
  OUTCOME_TYPES = { ROOT_CAUSE: 'root_cause', CHECKPOINT: 'checkpoint', INCONCLUSIVE: 'inconclusive' }

From bin/lib/session-manager.js:
  updateSession(currentMd, { status, appendToBody }) -> { sessionMd, warnings }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Viet Buoc 3 (Repro Engineer) va Buoc 4 (Fix Architect + Outcome Routing)</name>
  <files>workflows/fix-bug.md</files>
  <read_first>
    - workflows/fix-bug.md (ket qua Plan 01 — can doc de tiep tuc append)
    - bin/lib/outcome-router.js (4 exported functions: buildRootCauseMenu, prepareFixNow, prepareFixPlan, prepareSelfFix)
    - bin/lib/checkpoint-handler.js (extractCheckpointQuestion, buildContinuationContext, MAX_CONTINUATION_ROUNDS=2)
    - bin/lib/repro-test-generator.js (generateReproTest API)
    - bin/lib/evidence-protocol.js (validateEvidence, parseEvidence, OUTCOME_TYPES)
    - .claude/agents/pd-repro-engineer.md (agent config)
    - .claude/agents/pd-fix-architect.md (agent config)
    - .planning/phases/32-orchestrator-workflow/32-RESEARCH.md (Pattern 4: Outcome Routing, Pitfall 2)
  </read_first>
  <action>
Append vao `<process>` section cua workflows/fix-bug.md, SAU Buoc 2.

### Buoc 3: Tao test tai hien (FLOW-03)

```markdown
## Buoc 3: Tao test tai hien

--- Buoc 3/5: Tao test tai hien ---

Spawn Agent `pd-repro-engineer`:
  "Session dir: {absolute_session_dir}.
   Doc evidence_janitor.md va evidence_code.md (va evidence_docs.md neu co).
   Tao reproduction test va ghi evidence_repro.md."

Sau khi agent hoan tat:
1. Read `{session_dir}/evidence_repro.md`
2. Goi `validateEvidence(content)` tu `bin/lib/evidence-protocol.js`
3. Kiem tra:
   - valid=true -> tiep tuc Buoc 4
   - valid=false -> WARNING: "Repro Engineer khong tao duoc test tai hien." Tiep tuc Buoc 4 voi evidence tu Buoc 2.
4. updateSession(currentMd, { appendToBody: '- evidence_repro.md: da ghi' })

Repro FAIL (agent throw/timeout):
- WARNING: "Khong tao duoc test tai hien. Tiep tuc voi evidence phan tich."
- Tiep tuc Buoc 4 (Repro la bo sung, khong block workflow)
```

### Buoc 4: Tong hop va ra phan quyet (FLOW-04)

```markdown
## Buoc 4: Tong hop va ra phan quyet

--- Buoc 4/5: Tong hop va ra phan quyet ---

Spawn Agent `pd-fix-architect`:
  "Session dir: {absolute_session_dir}.
   Doc TAT CA evidence files (evidence_janitor.md, evidence_code.md, evidence_docs.md, evidence_repro.md).
   Tong hop va ra phan quyet. Ghi evidence_architect.md."

Sau khi agent hoan tat:
1. Read `{session_dir}/evidence_architect.md`
2. Goi `validateEvidence(content)` -> { valid, outcome }
3. Goi `parseEvidence(content)` -> { frontmatter, body, sections }

### Routing theo outcome:

**NEU outcome = 'root_cause':**
  1. Goi `buildRootCauseMenu(content)` tu `bin/lib/outcome-router.js`
     -> { question, choices } (3 lua chon: fix_now, fix_plan, self_fix)
  2. Hien question va 3 lua chon cho user (dung cau hoi truc tiep, KHONG hien agent output)
  3. User chon:
     - fix_now -> Goi `prepareFixNow(content)` -> { fixInstructions, targetFiles, rootCause } -> Buoc 5
     - fix_plan -> Goi `prepareFixPlan(content, sessionDir)` -> { planPath, planContent }
       Ghi planContent vao planPath. Thong bao: "Da tao ke hoach sua tai {planPath}."
       updateSession(currentMd, { status: 'paused' }). DUNG workflow.
     - self_fix -> Goi `prepareSelfFix(content)` -> { summary, suggestedSteps }
       Hien summary va suggestedSteps. updateSession(currentMd, { status: 'paused' }). DUNG workflow.

**NEU outcome = 'checkpoint':**
  1. Goi `extractCheckpointQuestion(content)` tu `bin/lib/checkpoint-handler.js`
     -> { question, context }
  2. Hien question cho user, cho tra loi
  3. User tra loi -> Goi `buildContinuationContext(content, userAnswer, roundNumber)`
     -> { canContinue, prompt, warnings }
  4. canContinue = true -> Spawn lai `pd-fix-architect` voi continuation prompt. Quay lai dau Buoc 4.
  5. canContinue = false (da vuot MAX_CONTINUATION_ROUNDS=2) -> Thong bao: "Da vuot 2 vong hoi dap. Can nguoi xem xet."
     updateSession(currentMd, { status: 'paused' }). DUNG workflow.

**NEU outcome = 'inconclusive':**
  1. Hien Elimination Log tu evidence_architect.md (section ## Elimination Log)
  2. De xuat 2 lua chon:
     (1) Bo sung thong tin moi -> ghi thong tin vao SESSION.md, updateSession(status='paused'). DUNG workflow.
         (Quay lai Buoc 2 la FLOW-06 — Phase 33, NGOAI scope Phase 32)
     (2) Dung dieu tra -> updateSession(currentMd, { status: 'paused' }). DUNG workflow.

Architect FAIL (agent throw/timeout):
- Hien tat ca evidence da thu thap (janitor, detective, docs, repro) truc tiep cho user
- Hoi: "Architect khong tra phan quyet. Ban muon: (1) Xem evidence va tu quyet dinh, (2) Dung lai?"
- User chon (1) -> hien evidence, cho user quyet dinh fix_now/fix_plan/self_fix
- User chon (2) -> updateSession(paused). DUNG.
```
  </action>
  <verify>
    <automated>grep "pd-repro-engineer" workflows/fix-bug.md && grep "pd-fix-architect" workflows/fix-bug.md && grep "buildRootCauseMenu" workflows/fix-bug.md && grep "prepareFixNow" workflows/fix-bug.md && grep "extractCheckpointQuestion" workflows/fix-bug.md && grep "buildContinuationContext" workflows/fix-bug.md && grep "Buoc 3/5" workflows/fix-bug.md && grep "Buoc 4/5" workflows/fix-bug.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/fix-bug.md chua "## Buoc 3: Tao test tai hien"
    - workflows/fix-bug.md chua "Spawn Agent `pd-repro-engineer`"
    - workflows/fix-bug.md chua "## Buoc 4: Tong hop va ra phan quyet"
    - workflows/fix-bug.md chua "Spawn Agent `pd-fix-architect`"
    - workflows/fix-bug.md chua "buildRootCauseMenu(content)" hoac "buildRootCauseMenu("
    - workflows/fix-bug.md chua "prepareFixNow(" va "prepareFixPlan(" va "prepareSelfFix("
    - workflows/fix-bug.md chua "extractCheckpointQuestion("
    - workflows/fix-bug.md chua "buildContinuationContext("
    - workflows/fix-bug.md chua "MAX_CONTINUATION_ROUNDS" hoac "2 vong"
    - workflows/fix-bug.md chua xu ly ca 3 outcomes: "root_cause", "checkpoint", "inconclusive"
    - workflows/fix-bug.md chua "--- Buoc 3/5:" va "--- Buoc 4/5:"
    - Buoc 4 co fail-forward: Architect fail -> hien evidence cho user tu chon
  </acceptance_criteria>
  <done>Buoc 3 (Repro spawn + fail-forward) va Buoc 4 (Architect spawn + 3-way outcome routing + checkpoint continuation + inconclusive handling) da hoan thanh</done>
</task>

<task type="auto">
  <name>Task 2: Viet Buoc 5 (Fix + Commit + Post-fix), rules, va success_criteria</name>
  <files>workflows/fix-bug.md</files>
  <read_first>
    - workflows/fix-bug.md (ket qua Task 1 — can doc de tiep tuc append va dong cac tags)
    - bin/lib/debug-cleanup.js (scanDebugMarkers, matchSecurityWarnings APIs)
    - bin/lib/logic-sync.js (runLogicSync API va 4 sub-functions)
    - bin/lib/regression-analyzer.js (analyzeFromCallChain, analyzeFromSourceFiles APIs)
    - bin/lib/bug-memory.js (createBugRecord, buildIndex APIs)
    - bin/lib/session-manager.js (updateSession API)
    - .planning/phases/32-orchestrator-workflow/32-CONTEXT.md (D-07, D-08, D-10, D-11)
    - .planning/phases/32-orchestrator-workflow/32-RESEARCH.md (Pattern 5: Session Lifecycle, Pitfall 5-6)
  </read_first>
  <action>
Append vao `<process>` section cua workflows/fix-bug.md, SAU Buoc 4. Sau do dong `</process>` tag va viet `<rules>` va `<success_criteria>`.

### Buoc 5: Sua code va commit (FLOW-05)

```markdown
## Buoc 5: Sua code va commit

--- Buoc 5/5: Sua code va commit ---

### 5a: Regression analysis (truoc khi sua)
1. Doc fixInstructions tu prepareFixNow() (Buoc 4) -> lay targetFiles, targetFunction
2. Try:
   - Dung FastCode `code_qa`: "Liet ke cac files import hoac goi {targetFunction} trong {targetFile}"
   - Thanh cong -> goi `analyzeFromCallChain({ callChainText, targetFile, targetFunction })` tu `bin/lib/regression-analyzer.js`
   - FastCode loi -> doc source files quanh targetFile -> goi `analyzeFromSourceFiles({ sourceFiles, targetFile, targetFunction })`
   Catch: WARNING: "Khong phan tich duoc regression: {error.message}". Tiep tuc.
3. Ket qua affectedFiles -> ghi vao SESSION.md: "Regression: {N} files bi anh huong: {list}"

### 5b: Sua code
1. Doc fixInstructions va rootCause tu prepareFixNow() output
2. Ap dung fix theo huong dan
3. Chay test: xac dinh test command tu project (package.json scripts hoac .planning rules)
4. Test FAIL -> doc error, dieu chinh fix, chay lai (toi da 3 lan)
5. Test PASS -> tiep tuc 5c

### 5c: Debug cleanup (truoc commit)
1. `git diff --cached --name-only` -> danh sach staged files
2. Read noi dung tung staged file -> tao array [{path, content}]
3. Try: goi `scanDebugMarkers(stagedFiles)` tu `bin/lib/debug-cleanup.js`
   Catch: WARNING: "Debug cleanup loi: {error.message}". Tiep tuc.
4. Ket qua co markers -> hien danh sach theo file:
   ```
   [PD-DEBUG] Tim thay debug markers:
     {file}: Dong {line}: {content}
   Xoa tat ca debug markers? (Y/n)
   ```
   User Y -> xoa markers, git add lai files
   User n -> WARNING: "Debug markers van con trong commit."
5. Try: doc `.planning/scan/SCAN_REPORT.md` (neu ton tai va < 7 ngay)
   -> goi `matchSecurityWarnings(reportContent, filePaths)`
   Catch: bo qua.
   Co canh bao -> hien non-blocking (toi da 3):
   ```
   Canh bao bao mat lien quan:
   - {file}: [{severity}] {message}
   ```

### 5d: Commit
```
git add {fixed files} {session_dir}/SESSION.md {session_dir}/evidence_*.md
git commit -m "fix([LOI]): {mo_ta_ngan}"
```
(Per D-08: commit message format `fix([LOI]): mo ta`)

### 5e: User verify (per D-10)
Hoi: "Da sua {mo_ta}. Vui long kiem tra va xac nhan."

**User xac nhan OK:**
  1. Goi `createBugRecord({ file: targetFile, functionName: targetFunction, errorMessage: originalError, rootCause, fix: fixDescription, sessionId: folderName })` tu `bin/lib/bug-memory.js`
     -> bugRecordMd
  2. Ghi bugRecordMd vao `.planning/bugs/BUG-{NNN}.md`
     (Xac dinh NNN: Glob `.planning/bugs/BUG-*.md` -> tim so cao nhat + 1, bat dau tu 001)
  3. Glob `.planning/bugs/BUG-*.md` -> Read tat ca -> parse thanh records
     Goi `buildIndex(bugRecords)` tu `bin/lib/bug-memory.js` -> indexMd
     Ghi indexMd vao `.planning/bugs/INDEX.md`
  4. Goi `updateSession(currentMd, { status: 'resolved' })` (per D-11: session resolved SAU bug record + INDEX)
  5. Git add va commit: `.planning/bugs/BUG-*.md .planning/bugs/INDEX.md {session_dir}/SESSION.md`
     `git commit -m "fix([LOI]): ghi bug record va dong session {sessionId}"`

**User xac nhan CHUA SUA:**
  - Thu thap them trieu chung moi tu user
  - Quay lai 5b voi thong tin bo sung
  - Toi da 3 lan -> goi y: "Da thu 3 lan. De xuat: phan tich lai tu Buoc 2 hoac dung lai."

### 5f: Logic sync (non-blocking, SAU user verify thanh cong)
1. `git diff HEAD~1` -> diffText
2. Read SESSION.md -> sessionContent
3. Read BUG report vua tao -> bugReportContent
4. Read CLAUDE.md -> claudeContent (neu ton tai)
5. Glob `.planning/reports/*.md` -> reportContent (file moi nhat, neu co)
6. Try: goi `runLogicSync({ diffText, bugReportContent, sessionContent, claudeContent, reportContent, planContents: [] })` tu `bin/lib/logic-sync.js`
   -> { hasLogicChange, signals, diagramUpdated, rulesSuggested }
   Catch: WARNING: "Logic sync loi: {error.message}". KHONG block.
7. hasLogicChange = true va diagramUpdated -> hoi: "Cap nhat lai PDF? (Y/n)"
   Y -> `node bin/generate-pdf-report.js {reportPath}`
8. rulesSuggested co noi dung -> hien va hoi: "Them vao CLAUDE.md? (Y/n)"
   Y -> append vao CLAUDE.md, git add va commit
```

### Dong `</process>` tag

### Viet `<rules>` moi:

```markdown
<rules>
- Tuan thu `.planning/rules/` (general + stack-specific)
- CAM doc/hien thi file nhay cam (`.env`, `credentials.*`, `*.pem`, `*.key`, `*secret*`, `wp-config.php`)
- PHAI spawn agents theo dung thu tu: Janitor -> Detective+DocSpec -> Repro -> Architect -> Fix
- PHAI truyen absolute session_dir path khi spawn moi agent
- PHAI goi validateEvidence() sau moi agent hoan tat
- PHAI xu ly ca 3 outcomes sau Buoc 4: root_cause, checkpoint, inconclusive
- PHAI tao bug record SAU user verify (KHONG truoc) — per D-10
- PHAI dong session SAU bug record + INDEX rebuild — per D-11
- KHONG hien chi tiet agent output cho user — chi hien banners va ket qua cuoi (progressive disclosure)
- KHONG block workflow khi DocSpec hoac Repro fail — chi WARNING va tiep tuc
- CHI STOP khi: (1) Janitor fail khong co trieu chung, (2) Detective fail, (3) User chon dung
- Moi v1.5 module call (debug-cleanup, logic-sync, regression-analyzer) PHAI wrap trong try/catch — loi chi tao WARNING
- Commit message format: `fix([LOI]): mo ta` — per D-08
- Tiep tuc phien cu -> doc SESSION.md TRUOC, khong bat dau lai
- KHONG de agent spawn agent — chi orchestrator (workflow nay) moi spawn agent
</rules>
```

### Viet `<success_criteria>` moi:

```markdown
<success_criteria>
- [ ] Session duoc tao/tiep tuc dung qua session-manager.js
- [ ] Janitor thu thap trieu chung va ghi evidence_janitor.md
- [ ] Detective va DocSpec chay tuan tu, ket qua merge qua parallel-dispatch.js
- [ ] Repro Engineer tao test tai hien (hoac WARNING neu fail)
- [ ] Architect tong hop evidence va ra 1 trong 3 phan quyet
- [ ] Outcome routing xu ly dung ca 3 truong hop
- [ ] Fix duoc ap dung, test pass, commit voi format fix([LOI])
- [ ] Bug record tao SAU user verify, INDEX.md cap nhat
- [ ] Session dong voi status=resolved SAU bug record
- [ ] Logic sync chay non-blocking sau verify
- [ ] Moi buoc hien banner progressive disclosure
- [ ] User xac nhan da sua thanh cong
</success_criteria>
```
  </action>
  <verify>
    <automated>grep "Buoc 5:" workflows/fix-bug.md && grep "scanDebugMarkers" workflows/fix-bug.md && grep "createBugRecord" workflows/fix-bug.md && grep "buildIndex" workflows/fix-bug.md && grep "runLogicSync" workflows/fix-bug.md && grep "analyzeFromCallChain\|analyzeFromSourceFiles" workflows/fix-bug.md && grep "fix(\[LOI\])" workflows/fix-bug.md && grep "</rules>" workflows/fix-bug.md && grep "</success_criteria>" workflows/fix-bug.md && grep "</process>" workflows/fix-bug.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/fix-bug.md chua "## Buoc 5: Sua code va commit"
    - workflows/fix-bug.md chua "--- Buoc 5/5:" banner
    - workflows/fix-bug.md chua "analyzeFromCallChain(" hoac "analyzeFromSourceFiles("
    - workflows/fix-bug.md chua "scanDebugMarkers(" va "matchSecurityWarnings("
    - workflows/fix-bug.md chua "fix([LOI]):" trong commit pattern
    - workflows/fix-bug.md chua "createBugRecord(" va "buildIndex("
    - workflows/fix-bug.md chua "updateSession(currentMd, { status: 'resolved' })" hoac tuong duong
    - workflows/fix-bug.md chua "runLogicSync("
    - workflows/fix-bug.md chua "</process>" (process tag dong dung)
    - workflows/fix-bug.md chua "<rules>" va "</rules>" voi noi dung moi (khong phai v1.5)
    - workflows/fix-bug.md chua "<success_criteria>" va "</success_criteria>" voi noi dung moi
    - rules chua "PHAI spawn agents theo dung thu tu"
    - rules chua "try/catch" hoac "wrap trong try/catch" cho v1.5 modules
    - Buoc 5e co flow: user verify -> createBugRecord -> buildIndex -> updateSession(resolved)
    - Buoc 5f co runLogicSync la non-blocking (try/catch)
    - `node --test test/smoke-*.test.js` van pass
  </acceptance_criteria>
  <done>Workflow fix-bug.md hoan chinh: 5 buoc orchestrator + rules moi + success_criteria moi. Tat ca 10 modules duoc tich hop dung, session lifecycle day du, progressive disclosure xuyen suot.</done>
</task>

</tasks>

<verification>
- workflows/fix-bug.md la workflow hoan chinh voi 5 buoc (0, 0.5, 1, 2, 3, 4, 5) + rules + success_criteria
- Moi buoc co progressive disclosure banner "--- Buoc N/5: ... ---"
- Buoc 3 spawn pd-repro-engineer, fail-forward (WARNING, tiep tuc)
- Buoc 4 spawn pd-fix-architect, xu ly ca 3 outcomes (root_cause 3 lua chon, checkpoint max 2 vong, inconclusive 2 lua chon)
- Buoc 5 gom: regression analysis, fix, debug cleanup, commit, user verify, bug record, index rebuild, session close, logic sync
- Tat ca v1.5 module calls wrap trong try/catch (per D-04, Pitfall 6)
- Session lifecycle: fix commit -> user verify -> createBugRecord -> buildIndex -> updateSession(resolved) (per D-10, D-11)
- Commit format: fix([LOI]): mo ta (per D-08)
- `node --test test/smoke-*.test.js` van pass (khong thay doi JS modules)
- `npm test` pass (full suite)
</verification>

<success_criteria>
- Workflow fix-bug.md hoan chinh voi 5 buoc + rules + success_criteria
- Tat ca FLOW-03, FLOW-04, FLOW-05 requirements duoc phu
- Tat ca locked decisions (D-04, D-07, D-08, D-10, D-11) duoc phan anh
- 3-way outcome routing hoat dong (root_cause, checkpoint, inconclusive)
- Session lifecycle hoan tat tu tao den dong
- Smoke tests va full test suite van pass
</success_criteria>

<output>
After completion, create `.planning/phases/32-orchestrator-workflow/32-02-SUMMARY.md`
</output>
