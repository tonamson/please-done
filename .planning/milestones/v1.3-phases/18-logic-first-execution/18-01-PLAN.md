---
phase: 18-logic-first-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/write-code.md
  - templates/verification-report.md
  - test/snapshots/codex/write-code.md
  - test/snapshots/gemini/write-code.md
  - test/snapshots/copilot/write-code.md
  - test/snapshots/opencode/write-code.md
autonomous: true
requirements: [EXEC-01, EXEC-02]
must_haves:
  truths:
    - "write-code.md contains Buoc 1.7 Re-validate Logic step between Buoc 1.6 and Buoc 2"
    - "Buoc 1.7 instructs AI to output targeted paraphrase of Business Logic per task (~100 tokens)"
    - "Buoc 1.7 includes confirmation prompt 'Logic dung chua? (Y/n)' before proceeding"
    - "Parallel agents (Buoc 10) are instructed to run Buoc 1.7 for their own task"
    - "verification-report template has 'Loai bang chung' column with 4 evidence types"
    - "verification-report section header uses 'Truths Verified' language"
    - "All 4 write-code.md snapshots regenerated and pass smoke tests"
  artifacts:
    - path: "workflows/write-code.md"
      provides: "Buoc 1.7 Re-validate Logic + updated 9.5d + updated Buoc 10 agent instruction"
      contains: "Buoc 1.7"
    - path: "templates/verification-report.md"
      provides: "Truths Verified table with Loai bang chung column"
      contains: "Loai bang chung"
    - path: "test/snapshots/codex/write-code.md"
      provides: "Codex converter snapshot with Buoc 1.7"
      contains: "1.7"
    - path: "test/snapshots/gemini/write-code.md"
      provides: "Gemini converter snapshot with Buoc 1.7"
      contains: "1.7"
    - path: "test/snapshots/copilot/write-code.md"
      provides: "Copilot converter snapshot with Buoc 1.7"
      contains: "1.7"
    - path: "test/snapshots/opencode/write-code.md"
      provides: "Opencode converter snapshot with Buoc 1.7"
      contains: "1.7"
  key_links:
    - from: "workflows/write-code.md Buoc 1.7"
      to: "templates/plan.md Truths table"
      via: "AI reads PLAN.md Truths at Buoc 1.7 runtime"
      pattern: "PLAN\\.md.*Truths"
    - from: "workflows/write-code.md Buoc 9.5e"
      to: "templates/verification-report.md"
      via: "@templates/verification-report.md path reference"
      pattern: "@templates/verification-report"
---

<objective>
Implement Logic-First Execution: add Buoc 1.7 "Re-validate Logic" to write-code workflow (EXEC-01) and restructure verification-report template with typed evidence (EXEC-02).

Purpose: Ensure AI always validates business logic understanding before writing code, and reports verification with structured evidence types per Truth.
Output: Updated write-code.md workflow, updated verification-report.md template, 4 regenerated snapshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-logic-first-execution/18-CONTEXT.md
@.planning/phases/18-logic-first-execution/18-RESEARCH.md

<interfaces>
<!-- Key structures executor needs. Read actual files via read_first for full context. -->

Insertion point in workflows/write-code.md: Buoc 1.6 ends at line 150, followed by blank + `___` separator (line 152) + blank, then Buoc 2 starts at line 154. Insert Buoc 1.7 between the separator and Buoc 2.

Buoc 9.5d current text (line 320-321): "9.5d -- Cap 4: Kiem tra logic Truths" + single-line description.

Buoc 10 agent instruction (line 364): "Buoc 2->3->4->5" needs "Buoc 1.7->2->3->4->5".

Current verification-report.md Truths table: 4-column (# | Su that | Trang thai | Bang chung). Target: 5-column (# | Su that | Trang thai | Loai | Bang chung).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Buoc 1.7 Re-validate Logic + update Buoc 9.5d and Buoc 10 in write-code.md</name>
  <files>workflows/write-code.md</files>
  <read_first>
    - workflows/write-code.md (full file -- see current step structure, separator pattern, Vietnamese style)
    - .planning/phases/18-logic-first-execution/18-CONTEXT.md (decisions D-01 through D-07)
    - .planning/phases/18-logic-first-execution/18-RESEARCH.md (insertion point analysis, code examples)
    - templates/plan.md (Truths table structure -- source data for Buoc 1.7)
  </read_first>
  <action>
Three surgical edits to workflows/write-code.md:

**Edit 1 -- Insert Buoc 1.7 between line 152 (the `---` after Buoc 1.6) and line 154 (Buoc 2):**

After the existing `---` separator following Buoc 1.6 (line 152), insert a new blank line, then the Buoc 1.7 section, then a blank line + `---` + blank line before Buoc 2. The exact markdown to insert between the existing `---` (after Buoc 1.6) and `## Buoc 2`:

```
## Buoc 1.7: Re-validate Logic -- xac nhan Business Logic truoc khi code

Doc PLAN.md "Tieu chi thanh cong -> Su that phai dat" VA task detail `> Truths:`.
In ra **targeted paraphrase** cua Business Logic lien quan toi task nay:

    **Logic can dam bao (Task [N]):**
    - T[x]: [dien giai ngan gon su that + gia tri nghiep vu, KHONG sao chep nguyen van]
    - T[y]: [tuong tu]

    Logic dung chua? (Y/n)

Quy tac:
- Chi Truths ma task nay map toi (tu `> Truths:` trong TASKS.md)
- Toi da ~100 tokens -- dien giai, KHONG copy-paste bang
- Muc dich: kiem tra AI HIEU logic truoc khi viet code, khong chi doc qua
- Khong co Truths (plan format cu) -> bo qua Buoc 1.7
- User tra loi "n" hoac chi ra sai -> doc lai PLAN.md phan Truths, sua dien giai, hoi lai
```

NOTE: The output example block should use 4-space indentation (not triple backtick fenced block) to avoid markdown nesting issues in the workflow file. The section must end with blank line + `---` + blank line before Buoc 2.

Per D-01: bullet paraphrase format, one line per Truth.
Per D-02: output visible to user, not internal reasoning.
Per D-03: always run, even for 1 Truth.
Per D-04: includes "Logic dung chua? (Y/n)" confirmation prompt + re-read flow on "n".
Per D-05: placed after Buoc 1.6 and before Buoc 2.

**Edit 2 -- Update Buoc 9.5d (around line 320-321) to mention evidence types:**

Replace the current 9.5d text:

    **9.5d -- Cap 4: Kiem tra logic Truths**
    Voi moi Truth: verify "Cach kiem chung" tren code thuc te. Cross-check: Truths ma TAT CA artifacts dat Cap 1-3 -> kha nang cao dat.

With:

    **9.5d -- Cap 4: Truths Verified (kiem tra logic)**
    Voi moi Truth: verify "Cach kiem chung" tren code thuc te.
    - Ghi nhan **loai bang chung** cho moi Truth: Test | Log | Screenshot | File | Manual
    - Cross-check: Truths ma TAT CA artifacts dat Cap 1-3 -> kha nang cao dat.

Per D-08: adds evidence type classification.
Per D-09: only modifies the Truths-related section; other sections unchanged.

**Edit 3 -- Update Buoc 10 parallel agent instruction (line 364):**

Replace:

    Chi dan agent: Buoc 2->3->4->5 (research -> code -> lint/build -> test).

With:

    Chi dan agent: Buoc 1.7->2->3->4->5 (validate logic -> research -> code -> lint/build -> test).

The rest of the line stays the same ("KHONG report, KHONG commit...").

Per D-06: each parallel agent runs Buoc 1.7 independently for its own task.

**CRITICAL:** Maintain the `---` horizontal rule separator pattern between steps. Each Buoc section ends with blank line + `---` + blank line. Use Vietnamese with diacritics throughout (matching existing style). Do NOT renumber any existing steps (Buoc 2 through Buoc 10 keep their numbers).
  </action>
  <verify>
    <automated>grep -c "Buoc 1.7" workflows/write-code.md && grep -c "Logic dung chua" workflows/write-code.md && grep -c "Truths Verified" workflows/write-code.md && grep "1\.7.*2.*3.*4.*5" workflows/write-code.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Buoc 1.7" workflows/write-code.md` returns >= 1
    - `grep "Re-validate Logic" workflows/write-code.md` finds the step header
    - `grep "Logic dung chua" workflows/write-code.md` finds the confirmation prompt
    - `grep "~100 tokens" workflows/write-code.md` finds the token budget instruction
    - `grep "Truths Verified" workflows/write-code.md` finds updated 9.5d header
    - `grep "loai bang chung" workflows/write-code.md` finds evidence type instruction in 9.5d
    - `grep "1\.7.*2.*3.*4.*5" workflows/write-code.md` finds updated parallel agent instruction
    - Buoc 1.7 appears between Buoc 1.6 section and Buoc 2 section (sequential order preserved)
    - No existing step numbers (Buoc 2 through Buoc 10) were renumbered
  </acceptance_criteria>
  <done>write-code.md has Buoc 1.7 with bullet paraphrase format, confirmation prompt, ~100 token budget, skip-if-no-Truths rule, re-read flow on "n" answer. Buoc 9.5d updated with evidence type classification. Buoc 10 parallel agent instruction includes Buoc 1.7.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure verification-report template with Truths Verified and evidence types</name>
  <files>templates/verification-report.md</files>
  <read_first>
    - templates/verification-report.md (full file -- current template structure)
    - .planning/phases/18-logic-first-execution/18-CONTEXT.md (decisions D-08, D-09, specifics section)
    - .planning/phases/18-logic-first-execution/18-RESEARCH.md (EXEC-02 template redesign analysis)
  </read_first>
  <action>
Four targeted edits to templates/verification-report.md:

**Edit 1 -- Update section header (currently line 20):**

Replace: `## Truths -- Su that phai dat`
With: `## Truths Verified -- Su that da xac minh`

**Edit 2 -- Update Truths table header (currently line 21):**

Replace:

    | # | Su that | Trang thai | Bang chung |
    |---|---------|-----------|-----------|

With:

    | # | Su that | Trang thai | Loai | Bang chung |
    |---|---------|-----------|------|-----------|

**Edit 3 -- Update table example rows (currently lines 23-25):**

Replace the 3 example rows with 4 rows showing each evidence type:

    | T1 | [mo ta] | DAT | Test | [test name pass N/N] |
    | T2 | [mo ta] | DAT | Log | [console/API output] |
    | T3 | [mo ta] | CHUA DAT | Screenshot | [mo ta screenshot] |
    | T4 | [mo ta] | CAN KIEM TRA THU CONG | Manual | [can chay app / kiem tra giao dien] |

The 4 evidence types per D-08: **Test** (unit/integration test pass), **Log** (console/API output), **Screenshot** (chup man hinh), **Manual** (can user kiem tra). Additionally **File** is accepted (file existence/content check).

**Edit 4 -- Update header metadata and summary section:**

In the header metadata block (around line 17), replace:

    > Ket qua: [N]/[M] tieu chi dat

With:

    > Ket qua: [N]/[M] Truths verified

In the summary section (around line 53), replace:

    - Dat: [N] Truths

With:

    - Verified: [N] Truths

Per D-08: evidence types formalized as column.
Per D-09: other sections (Artifacts, Key Links, Anti-pattern) remain unchanged.

**CRITICAL:** Do NOT modify the Artifacts, Key Links, Anti-pattern, or Gaps sections. Only the Truths table section, header metadata line, and summary line change.
  </action>
  <verify>
    <automated>grep -c "Loai" templates/verification-report.md && grep "Truths Verified" templates/verification-report.md && grep "Truths verified" templates/verification-report.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Truths Verified" templates/verification-report.md` finds the new section header
    - `grep "| Loai |" templates/verification-report.md` finds the evidence type column header
    - `grep "| Test |" templates/verification-report.md` finds Test evidence type example
    - `grep "| Log |" templates/verification-report.md` finds Log evidence type example
    - `grep "| Manual |" templates/verification-report.md` finds Manual evidence type example
    - `grep "Truths verified" templates/verification-report.md` finds updated summary metric
    - `grep "Su that phai dat" templates/verification-report.md` returns NO matches (old header replaced)
    - `grep "tieu chi dat" templates/verification-report.md` returns NO matches (old metric replaced)
    - Artifacts section still contains `| Artifact |` (unchanged)
    - Key Links section still contains `| Tu | Den |` (unchanged)
  </acceptance_criteria>
  <done>verification-report template has "Truths Verified" section header, 5-column table with Loai evidence type column, 4 example rows showing Test/Log/Screenshot/Manual types, and updated summary metrics.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate write-code.md snapshots and verify full test suite</name>
  <files>
    test/snapshots/codex/write-code.md
    test/snapshots/gemini/write-code.md
    test/snapshots/copilot/write-code.md
    test/snapshots/opencode/write-code.md
  </files>
  <read_first>
    - workflows/write-code.md (verify Buoc 1.7 is present after Task 1)
    - templates/verification-report.md (verify Truths Verified is present after Task 2)
    - test/generate-snapshots.js (understand regeneration script)
  </read_first>
  <action>
1. Run snapshot regeneration: `node test/generate-snapshots.js`

2. Verify exactly 4 write-code.md snapshots were updated: `git diff --name-only test/snapshots/`
Expected: exactly these 4 files changed:
- test/snapshots/codex/write-code.md
- test/snapshots/gemini/write-code.md
- test/snapshots/copilot/write-code.md
- test/snapshots/opencode/write-code.md

NOTE: templates/verification-report.md does NOT cascade to snapshots (referenced by path, not inlined). So only write-code.md snapshots change.

3. Run smoke snapshot tests: `node --test test/smoke-snapshot.test.js`
All 48 snapshot tests must pass.

4. Run full test suite: `node --test 'test/*.test.js'`
Must pass with 0 new failures vs baseline (455+ tests). Pre-existing historical validation failures in worktree context are acceptable (known issue from Phase 17).

5. Verify each snapshot contains Buoc 1.7 content: `grep -l "1.7" test/snapshots/*/write-code.md`
Must return all 4 platform paths.
  </action>
  <verify>
    <automated>node --test test/smoke-snapshot.test.js && grep -c "1.7" test/snapshots/codex/write-code.md test/snapshots/gemini/write-code.md test/snapshots/copilot/write-code.md test/snapshots/opencode/write-code.md</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/smoke-snapshot.test.js` exits 0 (all 48 snapshot tests pass)
    - `grep "1.7" test/snapshots/codex/write-code.md` returns at least 1 match
    - `grep "1.7" test/snapshots/gemini/write-code.md` returns at least 1 match
    - `grep "1.7" test/snapshots/copilot/write-code.md` returns at least 1 match
    - `grep "1.7" test/snapshots/opencode/write-code.md` returns at least 1 match
    - `git diff --name-only test/snapshots/ | grep write-code | wc -l` returns 4
    - `node --test 'test/*.test.js'` does not introduce new failures beyond pre-existing baseline
  </acceptance_criteria>
  <done>All 4 write-code.md snapshots regenerated with Buoc 1.7 content. Smoke snapshot tests (48) pass. Full test suite shows no new regressions.</done>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:

1. EXEC-01 verified:
   - `grep -c "Buoc 1.7" workflows/write-code.md` >= 1
   - `grep "Re-validate Logic" workflows/write-code.md` finds step header
   - `grep "Logic dung chua" workflows/write-code.md` finds confirmation prompt
   - `grep "~100 tokens" workflows/write-code.md` finds token budget
   - `grep "1\.7.*2.*3.*4.*5" workflows/write-code.md` finds parallel update

2. EXEC-02 verified:
   - `grep "Truths Verified" templates/verification-report.md` finds new header
   - `grep "| Loai |" templates/verification-report.md` finds evidence column
   - `grep "Truths Verified" workflows/write-code.md` finds 9.5d update

3. Snapshot cascade verified:
   - All 4 write-code.md snapshots contain "1.7"
   - `node --test test/smoke-snapshot.test.js` passes (48 tests)
   - `node --test 'test/*.test.js'` shows no new regressions
</verification>

<success_criteria>
- write-code.md has Buoc 1.7 between Buoc 1.6 and Buoc 2 with: bullet paraphrase format, confirmation prompt, ~100 token budget, Truths-reference instruction, skip-if-no-Truths fallback
- verification-report.md has "Truths Verified" header and 5-column table with Loai evidence type column
- write-code.md Buoc 9.5d mentions evidence type classification
- write-code.md Buoc 10 parallel agent instruction includes Buoc 1.7
- All 4 platform snapshots regenerated and passing smoke tests
- Full test suite shows no new regressions
</success_criteria>

<output>
After completion, create `.planning/phases/18-logic-first-execution/18-01-SUMMARY.md`
</output>
