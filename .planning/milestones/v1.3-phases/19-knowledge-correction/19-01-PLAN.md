---
phase: 19-knowledge-correction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/fix-bug.md
  - workflows/write-code.md
  - templates/progress.md
  - test/snapshots/codex/fix-bug.md
  - test/snapshots/gemini/fix-bug.md
  - test/snapshots/copilot/fix-bug.md
  - test/snapshots/opencode/fix-bug.md
  - test/snapshots/codex/write-code.md
  - test/snapshots/gemini/write-code.md
  - test/snapshots/copilot/write-code.md
  - test/snapshots/opencode/write-code.md
autonomous: true
requirements: [CORR-01, CORR-02]

must_haves:
  truths:
    - "fix-bug.md has Buoc 6.5 Logic Update step between 6c and Buoc 7"
    - "Buoc 6.5 includes logic bug classification, PLAN.md lookup, user confirmation, Truth update, and separate [LOI] commit"
    - "BUG report template in fix-bug.md Buoc 7 has Logic Changes table section"
    - "templates/progress.md has conditional Logic Changes section"
    - "write-code.md has Logic Changes instructions in PROGRESS.md flow"
    - "All 8 converter snapshots (4 fix-bug + 4 write-code) are regenerated and match"
  artifacts:
    - path: "workflows/fix-bug.md"
      provides: "Buoc 6.5 Logic Update step + Logic Changes in BUG report"
      contains: "Bước 6.5"
    - path: "workflows/write-code.md"
      provides: "Logic Changes instructions for PROGRESS.md"
      contains: "Logic Changes"
    - path: "templates/progress.md"
      provides: "Conditional Logic Changes section"
      contains: "Logic Changes"
    - path: "test/snapshots/codex/fix-bug.md"
      provides: "Updated Codex snapshot for fix-bug"
    - path: "test/snapshots/codex/write-code.md"
      provides: "Updated Codex snapshot for write-code"
  key_links:
    - from: "workflows/fix-bug.md"
      to: "templates/progress.md"
      via: "Logic Changes table format must match between BUG report and progress template"
      pattern: "Truth ID.*Thay"
    - from: "workflows/write-code.md"
      to: "templates/progress.md"
      via: "write-code references Logic Changes format from progress template"
      pattern: "Logic Changes"
---

<objective>
Add Buoc 6.5 "Logic Update" to fix-bug workflow and "Logic Changes" tracking to both workflows + progress template.

Purpose: When a bug is caused by wrong business logic (not a code typo), AI must correct the Truth in PLAN.md BEFORE fixing code. This ensures the source of truth stays accurate. Additionally, any logic changes discovered during work are recorded for traceability.

Output: 3 modified workflow/template files + 8 regenerated snapshots, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-knowledge-correction/19-CONTEXT.md
@.planning/phases/19-knowledge-correction/19-RESEARCH.md
@.planning/phases/18-logic-first-execution/18-01-SUMMARY.md

<interfaces>
<!-- Current fix-bug.md flow around insertion point (lines 184-192): -->

### 6c. Cong kiem tra truoc khi sua
**3 dieu kien BAT BUOC:**
1. Da tai hien HOAC bang chung thay the du manh
2. Da xac dinh file + logic cu the
3. Da co ke hoach kiem tra sau sua

Thieu dieu kien -> quay Buoc 5b/5c. Du -> Buoc 7.
<!-- ^ Line 190: "Du -> Buoc 7" must become "Du -> Buoc 6.5 (neu logic bug) hoac Buoc 7" -->

## Buoc 7: Viet bao cao loi
<!-- ^ Line 192: Buoc 7 starts here, Buoc 6.5 goes between 190 and 192 -->

<!-- Current BUG report template in Buoc 7 (lines 195-224) has these sections: -->
## Mo ta loi
## Trieu chung
## Buoc tai hien
## Phan tich nguyen nhan
## Anh huong
## Ke hoach kiem tra
## Xac nhan
<!-- Logic Changes section should be added AFTER "Phan tich nguyen nhan" and BEFORE "Anh huong" -->

<!-- Current write-code.md PROGRESS.md update instruction (line 222): -->
**Cap nhat PROGRESS.md** sau moi file: danh dau `[x]`, them ten file vao "Files da viet", cap nhat giai doan + thoi gian.

<!-- Current templates/progress.md structure: -->
## Cac buoc
## Files du kien
## Files da viet
<!-- Logic Changes section should be added AFTER "Files da viet" as conditional -->

<!-- Buoc 3 PLAN.md grep strategy (lines 76-78) -- reuse for 6.5a: -->
1. Grep chuc nang loi trong `.planning/milestones/[version-goc]/phase-*/PLAN.md`
2. CHI doc PLAN.md + CODE_REPORT cua phase(s) lien quan
3. Grep khong khop -> mo rong tim tat ca PLAN.md

<!-- Confirmation prompt pattern from write-code.md Buoc 1.7: -->
Logic dung chua? (Y/n)
<!-- Reuse this (Y/n) pattern for Buoc 6.5c confirmation -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Buoc 6.5 Logic Update to fix-bug.md + Logic Changes in BUG report</name>
  <files>workflows/fix-bug.md</files>
  <read_first>
    - workflows/fix-bug.md (full file -- understand current flow, especially lines 184-230)
    - .planning/phases/19-knowledge-correction/19-CONTEXT.md (decisions D-01 through D-10 for Buoc 6.5, D-11/D-12/D-14 for Logic Changes)
    - .planning/phases/19-knowledge-correction/19-RESEARCH.md (code examples, pitfalls)
  </read_first>
  <action>
Two surgical edits to workflows/fix-bug.md:

**Edit 1 -- Insert Buoc 6.5 between 6c and Buoc 7:**

1. Change line 190 from:
   `Thiếu điều kiện → quay Bước 5b/5c. Đủ → Bước 7.`
   to:
   `Thiếu điều kiện → quay Bước 5b/5c. Đủ → Bước 6.5 (nếu logic bug) hoặc Bước 7.`

2. Insert the following NEW section between the end of 6c (line 190) and `## Bước 7:` (line 192). Keep the blank line before Buoc 7:

```markdown

## Bước 6.5: Logic Update — cập nhật Truth khi bug do logic sai

**Phân loại:** Nguyên nhân bug (từ Bước 6b) liên quan đến business logic / Truth trong PLAN.md?
- Typo, off-by-one, import thiếu, lỗi cú pháp → KHÔNG phải logic bug → **skip 6.5, tới Bước 7**
- Logic tính toán sai, điều kiện nghiệp vụ sai, edge case thiếu, giá trị ngưỡng sai → **logic bug → tiếp tục 6.5**

### 6.5a. Tìm PLAN.md liên quan
Dùng cùng strategy Bước 3: Grep `.planning/milestones/[version-gốc]/phase-*/PLAN.md` → tìm bảng Truths 5 cột.
Không tìm thấy PLAN.md → ghi vào BUG report: "Không có PLAN.md để cập nhật Truth". Skip 6.5, tới Bước 7.

### 6.5b. Xác định Truth cần sửa
- CHỈ sửa Truth hiện có, KHÔNG thêm Truth mới
- CHỈ sửa cột liên quan (Sự thật, Trường hợp biên, Cách kiểm chứng — hoặc nhiều cột)
- Logic thiếu hoàn toàn → ghi Deferred, KHÔNG thêm Truth mới

### 6.5c. Xác nhận với user

```
Bug này do Truth sai — cần cập nhật PLAN.md:

| Truth | Hiện tại | Sửa thành |
|-------|---------|-----------|
| T[x] | [giá trị cũ] | [giá trị mới] |

Đồng ý sửa PLAN.md? (Y/n)
```

- User đồng ý → 6.5d
- User bác ("không phải logic bug") → skip 6.5, ghi SESSION: "User bác phân loại logic bug" → Bước 7

### 6.5d. Cập nhật PLAN.md + commit
- Sửa bảng Truths trong PLAN.md (CHỈ PLAN.md, KHÔNG sửa TASKS.md)
- Giá trị cũ ghi trong BUG report "Phân tích nguyên nhân"
- Commit riêng:
  ```
  git add [PLAN.md path]
  git commit -m "[LỖI] Cập nhật Truth [TX]: [tóm tắt thay đổi]"
  ```
- Tiếp tục Bước 7
```

**Edit 2 -- Add Logic Changes section to BUG report template in Buoc 7:**

In the BUG report markdown template (between `## Phân tích nguyên nhân` block ending at line ~216 and `## Ảnh hưởng` at line ~218), add:

```markdown

## Logic Changes (nếu có)
| Truth ID | Thay đổi | Lý do |
|----------|---------|-------|
```

This satisfies D-11 (both workflows track logic changes) and D-12 (table format) for the fix-bug context. Per D-14, this section is only created when logic changes actually occur (the "(nếu có)" suffix signals conditional inclusion). Per D-06, old Truth values go in "Phân tích nguyên nhân" above, and PLAN.md only has new values.

**Important: Do NOT modify any other step numbers.** Buoc 7, 8, 9, 10 remain unchanged. Only the flow reference in line 190 changes to route through 6.5.
  </action>
  <verify>
    <automated>grep -c "Bước 6.5" workflows/fix-bug.md | grep -q "^[1-9]" && grep -c "Logic Changes" workflows/fix-bug.md | grep -q "^[1-9]" && grep "6.5a\|6.5b\|6.5c\|6.5d" workflows/fix-bug.md | wc -l | grep -q "^[4-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/fix-bug.md contains "## Bước 6.5: Logic Update"
    - workflows/fix-bug.md contains "### 6.5a. Tìm PLAN.md liên quan"
    - workflows/fix-bug.md contains "### 6.5b. Xác định Truth cần sửa"
    - workflows/fix-bug.md contains "### 6.5c. Xác nhận với user"
    - workflows/fix-bug.md contains "### 6.5d. Cập nhật PLAN.md + commit"
    - workflows/fix-bug.md contains "Đồng ý sửa PLAN.md? (Y/n)"
    - workflows/fix-bug.md contains '[LỖI] Cập nhật Truth'
    - workflows/fix-bug.md contains "KHÔNG thêm Truth mới" (D-05)
    - workflows/fix-bug.md contains "KHÔNG sửa TASKS.md" (D-10)
    - workflows/fix-bug.md contains "User bác" with "SESSION" (D-03)
    - workflows/fix-bug.md contains "Không tìm thấy PLAN.md" skip path (D-09)
    - workflows/fix-bug.md line with "Đủ →" contains "Bước 6.5" (flow reference updated)
    - workflows/fix-bug.md BUG report template contains "## Logic Changes (nếu có)"
    - workflows/fix-bug.md BUG report contains "| Truth ID | Thay đổi | Lý do |"
    - Buoc 7, 8, 9, 10 step numbers are unchanged
  </acceptance_criteria>
  <done>fix-bug.md has complete Buoc 6.5 Logic Update flow with all 4 sub-steps and Logic Changes section in BUG report template. All 14 decisions (D-01 through D-14) applicable to fix-bug are honored.</done>
</task>

<task type="auto">
  <name>Task 2: Add Logic Changes to write-code.md + templates/progress.md</name>
  <files>workflows/write-code.md, templates/progress.md</files>
  <read_first>
    - workflows/write-code.md (full file -- especially lines 199-222 for Buoc 4 PROGRESS.md update, and lines 276-302 for Buoc 7 commit flow)
    - templates/progress.md (full file -- understand current template structure)
    - .planning/phases/19-knowledge-correction/19-CONTEXT.md (decisions D-11 through D-14)
    - .planning/phases/19-knowledge-correction/19-RESEARCH.md (Example 2 and Example 3)
  </read_first>
  <action>
Two files to edit:

**File 1 -- templates/progress.md:**

Add a conditional "Logic Changes" section to the template. Insert AFTER the `## Files đã viết` line (currently the last section in the template) and BEFORE the closing triple-backtick of the template code block. The new section:

```markdown

## Logic Changes (nếu có)
<!-- CHỈ tạo khi phát hiện logic nghiệp vụ cần điều chỉnh. Không có → KHÔNG tạo section này. -->
| Truth ID | Thay đổi | Lý do |
|----------|---------|-------|
```

Also add a new rule at the end of the `## Quy tắc` section:

```markdown
- Không có logic change → KHÔNG tạo section "Logic Changes" (D-14)
```

**File 2 -- workflows/write-code.md:**

Add Logic Changes instructions near the PROGRESS.md update instruction in Buoc 4. After line 222 which says:
`**Cập nhật PROGRESS.md** sau mỗi file: đánh dấu `[x]`, thêm tên file vào "Files đã viết", cập nhật giai đoạn + thời gian.`

Add the following new paragraph:

```markdown

**Logic Changes (nếu có):**
Phát hiện logic nghiệp vụ cần điều chỉnh trong quá trình code (VD: edge case mới, giá trị ngưỡng cần sửa):
1. Cập nhật PLAN.md Truths tương ứng (chỉ sửa Truth hiện có, KHÔNG thêm mới)
2. Ghi vào PROGRESS.md section "## Logic Changes":
   `| Truth ID | Thay đổi | Lý do |`
3. Không có logic change → KHÔNG tạo section này
```

This satisfies:
- D-11: Both write-code and fix-bug track logic changes
- D-12: Table format `| Truth ID | Thay đổi | Lý do |`
- D-13: PROGRESS.md deleted after commit (existing lifecycle unchanged)
- D-14: No logic change = no section created
  </action>
  <verify>
    <automated>grep -c "Logic Changes" templates/progress.md | grep -q "^[1-9]" && grep -c "Logic Changes" workflows/write-code.md | grep -q "^[1-9]" && grep "Truth ID.*Thay" templates/progress.md | wc -l | grep -q "^[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - templates/progress.md contains "## Logic Changes (nếu có)"
    - templates/progress.md contains "| Truth ID | Thay đổi | Lý do |"
    - templates/progress.md contains "KHÔNG tạo section" rule (D-14)
    - templates/progress.md contains "CHỈ tạo khi phát hiện logic nghiệp vụ"
    - workflows/write-code.md contains "**Logic Changes (nếu có):**"
    - workflows/write-code.md contains "chỉ sửa Truth hiện có, KHÔNG thêm mới"
    - workflows/write-code.md contains "| Truth ID | Thay đổi | Lý do |"
    - workflows/write-code.md contains "KHÔNG tạo section này"
    - The PROGRESS.md lifecycle (create -> update -> delete after commit) is unchanged
  </acceptance_criteria>
  <done>Both templates/progress.md and workflows/write-code.md have Logic Changes tracking with matching table format, conditional creation, and all D-11 through D-14 decisions honored.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate snapshots and verify all tests pass</name>
  <files>test/snapshots/codex/fix-bug.md, test/snapshots/gemini/fix-bug.md, test/snapshots/copilot/fix-bug.md, test/snapshots/opencode/fix-bug.md, test/snapshots/codex/write-code.md, test/snapshots/gemini/write-code.md, test/snapshots/copilot/write-code.md, test/snapshots/opencode/write-code.md</files>
  <read_first>
    - test/smoke-snapshot.test.js (understand snapshot comparison logic)
    - test/generate-snapshots.js (understand how to regenerate)
  </read_first>
  <action>
1. Run the snapshot generator to regenerate ALL snapshots:
   ```bash
   node test/generate-snapshots.js
   ```
   This regenerates all converter snapshots for all platforms (codex, gemini, copilot, opencode) for all skills including fix-bug and write-code.

2. Run the smoke integrity test to verify no broken references:
   ```bash
   node --test test/smoke-integrity.test.js
   ```

3. Run the snapshot comparison test to verify all snapshots match:
   ```bash
   node --test test/smoke-snapshot.test.js
   ```

4. If any test fails, investigate the specific failure and fix. Common issues:
   - Snapshot mismatch: re-run generator, ensure Task 1 and Task 2 edits are saved
   - Broken reference: check @templates/ or @references/ refs added in Tasks 1-2 point to existing files

5. Verify the full test suite (expect 48/48 snapshot tests pass, integrity tests pass):
   ```bash
   node --test 'test/*.test.js'
   ```
   Note: Some historical plan validation tests may fail (known pre-existing issue per Phase 18 summary). The key tests are smoke-integrity and smoke-snapshot.
  </action>
  <verify>
    <automated>node --test test/smoke-integrity.test.js && node --test test/smoke-snapshot.test.js && echo "ALL PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - test/snapshots/codex/fix-bug.md contains "6.5" (reflects Buoc 6.5 addition)
    - test/snapshots/gemini/fix-bug.md contains "6.5"
    - test/snapshots/copilot/fix-bug.md contains "6.5"
    - test/snapshots/opencode/fix-bug.md contains "6.5"
    - test/snapshots/codex/write-code.md contains "Logic Changes"
    - test/snapshots/gemini/write-code.md contains "Logic Changes"
    - test/snapshots/copilot/write-code.md contains "Logic Changes"
    - test/snapshots/opencode/write-code.md contains "Logic Changes"
    - `node --test test/smoke-integrity.test.js` exits 0
    - `node --test test/smoke-snapshot.test.js` exits 0
  </acceptance_criteria>
  <done>All 8 snapshots regenerated with Phase 19 content. smoke-integrity and smoke-snapshot tests pass green.</done>
</task>

</tasks>

<verification>
1. `grep "Bước 6.5" workflows/fix-bug.md` -- returns matches (Buoc 6.5 exists)
2. `grep "Logic Changes" workflows/fix-bug.md workflows/write-code.md templates/progress.md` -- all 3 files have Logic Changes
3. `grep "Truth ID.*Thay" workflows/fix-bug.md templates/progress.md` -- matching table format
4. `grep "KHÔNG thêm Truth mới" workflows/fix-bug.md` -- D-05 honored
5. `grep "KHÔNG sửa TASKS.md" workflows/fix-bug.md` -- D-10 honored
6. `grep "User bác" workflows/fix-bug.md` -- D-03 honored
7. `node --test test/smoke-integrity.test.js` -- all integrity checks pass
8. `node --test test/smoke-snapshot.test.js` -- all 48 snapshot comparisons pass
</verification>

<success_criteria>
- fix-bug.md has complete Buoc 6.5 with 4 sub-steps (6.5a-6.5d) implementing all decisions D-01 through D-10
- BUG report template has Logic Changes section
- templates/progress.md has conditional Logic Changes section
- write-code.md has Logic Changes instructions in Buoc 4
- All 8 converter snapshots regenerated and matching
- smoke-integrity + smoke-snapshot tests pass
- CORR-01 and CORR-02 requirements fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/19-knowledge-correction/19-01-SUMMARY.md`
</output>
