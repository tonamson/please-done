---
phase: 20-logic-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/plan-checker.js
  - test/smoke-plan-checker.test.js
  - references/plan-checker.md
  - workflows/plan.md
  - test/snapshots/opencode/plan.md
  - test/snapshots/copilot/plan.md
  - test/snapshots/gemini/plan.md
  - test/snapshots/codex/plan.md
autonomous: true
requirements: [AUDIT-01]

must_haves:
  truths:
    - "CHECK-05 returns 'warn' when task has no Truth mapping (default severity)"
    - "CHECK-05 returns 'block' when severity override is 'block'"
    - "CHECK-04 retains Direction 1 BLOCK behavior (Truth without task)"
    - "CHECK-04 no longer checks Direction 2 (Task without Truth)"
    - "runAllChecks returns 8 checks including CHECK-05"
    - "v1.0/unknown format produces graceful PASS for CHECK-05"
  artifacts:
    - path: "bin/lib/plan-checker.js"
      provides: "checkLogicCoverage function (CHECK-05)"
      exports: ["checkLogicCoverage"]
      contains: "function checkLogicCoverage"
    - path: "test/smoke-plan-checker.test.js"
      provides: "CHECK-05 test suite"
      contains: "CHECK-05: logicCoverage"
    - path: "references/plan-checker.md"
      provides: "CHECK-05 rule documentation"
      contains: "CHECK-05: Logic Coverage"
  key_links:
    - from: "bin/lib/plan-checker.js"
      to: "runAllChecks"
      via: "checkLogicCoverage added to checks array"
      pattern: "checkLogicCoverage\\(planContent"
    - from: "bin/lib/plan-checker.js"
      to: "module.exports"
      via: "checkLogicCoverage exported"
      pattern: "checkLogicCoverage"
---

<objective>
Split CHECK-04 bidirectional check into CHECK-04 (Direction 1: Truth without task = BLOCK) and new CHECK-05 (Direction 2: Task without Truth = configurable severity, default WARN). Implement `checkLogicCoverage` function per AUDIT-01.

Purpose: Decouple orphan code detection from orphan truth detection. Tasks without Truth mapping are technical debt (WARN), not blockers. Projects can override to BLOCK via options parameter.

Output: Working CHECK-05 in plan-checker.js with full test coverage, updated CHECK-04, updated docs and name mappings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-logic-audit/20-CONTEXT.md
@.planning/phases/20-logic-audit/20-RESEARCH.md

@bin/lib/plan-checker.js
@test/smoke-plan-checker.test.js
@references/plan-checker.md

<interfaces>
<!-- Existing pure function pattern used by all check functions -->

From bin/lib/plan-checker.js:
```javascript
// Result object format (all checks return this):
{ checkId: 'CHECK-XX', status: 'pass'|'warn'|'block', issues: [] }

// Issue format:
{ message: string, location: string, fixHint: string }

// Existing parsers to reuse (DO NOT rewrite):
function parseTruthsV11(planContent) // returns [{ id, description, verify }]
function parseTaskDetailBlocksV11(tasksContent) // returns [{ id, truths: string[], ... }]
function detectPlanFormat(planContent) // returns 'v1.0'|'v1.1'|'unknown'

// runAllChecks signature (line 936):
function runAllChecks({ planContent, tasksContent, requirementIds })

// module.exports (line 956-987): all check functions + helpers exported
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split CHECK-04 and implement CHECK-05 checkLogicCoverage with tests</name>
  <files>bin/lib/plan-checker.js, test/smoke-plan-checker.test.js</files>
  <read_first>
    - bin/lib/plan-checker.js (full file — understand check patterns, line 649-702 CHECK-04, line 936-952 runAllChecks, line 956-987 exports)
    - test/smoke-plan-checker.test.js (full file — identify all CHECK-04 Direction 2 tests to move, all checks.length===7 assertions)
  </read_first>
  <behavior>
    - CHECK-05 with task without Truth returns status 'warn' (default severity per D-04)
    - CHECK-05 with task without Truth and options.severity='block' returns status 'block'
    - CHECK-05 with all tasks having Truth refs returns status 'pass'
    - CHECK-05 with v1.0 format returns status 'pass' (graceful skip)
    - CHECK-05 with unknown format returns status 'pass' (graceful skip)
    - CHECK-05 issue message contains task ID and "technical debt" string
    - CHECK-05 checkId is 'CHECK-05' in result object
    - CHECK-04 still BLOCKs on Direction 1 (Truth T3 not in any task)
    - CHECK-04 does NOT check Direction 2 (no issue for task without Truth)
    - runAllChecks returns 8 checks (was 7)
    - runAllChecks includes CHECK-05 in checks array
    - runAllChecks passes check05Severity option through to checkLogicCoverage
  </behavior>
  <action>
    **Part A: Refactor CHECK-04 in bin/lib/plan-checker.js (line 649-702)**

    Remove Direction 2 logic (lines 686-696) from `checkTruthTaskCoverage()`. The function keeps:
    - Format detection (v1.0/unknown graceful PASS)
    - parseTruthsV11 + parseTaskDetailBlocksV11
    - Direction 1 only: iterate truths, check each has at least one task covering it, push BLOCK issues
    - Update JSDoc comment from "bidirectional" to "Direction 1: Truth without task"

    Exact change: remove lines 686-696 (the `// Direction 2:` for-loop). Keep `blockIssues` variable and `result.issues.push(...blockIssues)` pattern. Update comment at line 647 from "bidirectional" to "Direction 1 only: moi Truth phai co task".

    **Part B: Add CHECK-05 function after CHECK-04 (before ADV-01 section)**

    Add new function `checkLogicCoverage(planContent, tasksContent, options = {})` per D-02:

    ```javascript
    /**
     * CHECK-05: Logic Coverage (Direction 2)
     * Task khong co Truth nao map = technical debt.
     * Default severity: WARN (per D-04). Configurable via options.severity (per D-05).
     */
    function checkLogicCoverage(planContent, tasksContent, options = {}) {
      const severity = options.severity || 'warn';
      const result = { checkId: 'CHECK-05', status: 'pass', issues: [] };
      const format = detectPlanFormat(planContent);

      if (format === 'v1.0' || format === 'unknown') return result;
      if (!tasksContent) return result;

      const tasks = parseTaskDetailBlocksV11(tasksContent);

      for (const task of tasks) {
        if (task.truths.length === 0) {
          result.issues.push({
            message: `Task ${task.id} khong co Truth nao map — technical debt`,
            location: `TASKS.md Task ${task.id}`,
            fixHint: `Them > Truths: [TX] vao metadata cua Task ${task.id}`
          });
        }
      }

      result.status = result.issues.length > 0 ? severity : 'pass';
      return result;
    }
    ```

    **Part C: Update runAllChecks (line 936)**

    1. Change function signature from `{ planContent, tasksContent, requirementIds }` to `{ planContent, tasksContent, requirementIds, check05Severity }`.
    2. Add `checkLogicCoverage(planContent, tasksContent, { severity: check05Severity })` to checks array AFTER checkTruthTaskCoverage line.
    3. Update JSDoc comment from "7 checks" to "8 checks".

    **Part D: Update module.exports (line 956)**

    Add `checkLogicCoverage,` after the `checkTruthTaskCoverage,` line in the exports object.

    **Part E: Update tests in test/smoke-plan-checker.test.js**

    5a. MOVE 2 tests from `describe('CHECK-04: truthTaskCoverage')` to new `describe('CHECK-05: logicCoverage')`:

    - Test at line 697 `'v1.1 Task has no Truth mapping -> block (not warn)'`:
      - Move to CHECK-05 describe block
      - Change `pc.checkTruthTaskCoverage(plan, tasks)` to `pc.checkLogicCoverage(plan, tasks)`
      - Change expected status from `'block'` to `'warn'`
      - Rename to `'v1.1 Task has no Truth mapping -> warn (default severity per D-04)'`

    - Test at line 713 `'v1.1 Direction 2 message does NOT contain "(co the la infrastructure task)"'`:
      - Move to CHECK-05 describe block
      - Change `pc.checkTruthTaskCoverage(plan, tasks)` to `pc.checkLogicCoverage(plan, tasks)`
      - Rename to `'v1.1 Direction 2 message does NOT contain "(co the la infrastructure task)" (CHECK-05)'`

    5b. ADD new tests in `describe('CHECK-05: logicCoverage')`:

    - `'v1.0 -> auto pass'`: call `pc.checkLogicCoverage(makePlanV10(), null)`, assert status 'pass'
    - `'unknown format -> pass'`: call `pc.checkLogicCoverage('just text', null)`, assert status 'pass'
    - `'severity override to block'`: call `pc.checkLogicCoverage(plan, tasks, { severity: 'block' })` with orphan task, assert status 'block'
    - `'default severity is warn'`: call `pc.checkLogicCoverage(plan, tasks)` with orphan task (no options), assert status 'warn'
    - `'issue message contains task ID and technical debt'`: assert `result.issues[0].message.includes('Task 2')` and `result.issues[0].message.includes('technical debt')`
    - `'all tasks have truths -> pass'`: all tasks reference truths, assert status 'pass'
    - `'checkId is CHECK-05'`: assert `result.checkId === 'CHECK-05'`

    5c. UPDATE all `checks.length === 7` assertions to `checks.length === 8`:
    - Line 756: `assert.equal(result.checks.length, 8);`
    - Line 837: `assert.equal(result.checks.length, 8);`
    - Line 1307: `assert.equal(result.checks.length, 8);`
    - Line 1378: `assert.equal(result.checks.length, 8, ... instead of 8`);`

    5d. UPDATE describe block and comment text:
    - Line 1298: change `// --- runAllChecks with 7 checks` to `// --- runAllChecks with 8 checks`
    - Line 1301: change `'returns 7 checks total'` to `'returns 8 checks total'`
    - Line 1369: change `// Verify all 22 plans return 7 checks each` to `8 checks each`
    - Line 1370: change `'all 22 historical plans return 7 checks each'` to `8 checks each`
    - Line 1383: change `// Summary gate test -- 7 checks per plan` to `8 checks per plan`

    5e. ADD CHECK-05 assertion in `'returns 4 check results with correct checkIds'` test (line 831):
    - Add `assert.ok(result.checks.some(c => c.checkId === 'CHECK-05'));` after the CHECK-04 line
    - Also add in the `'returns 7 checks total'` test (line 1301 block): add CHECK-05 assertion

    5f. UPDATE the "one warn no block -> overall warn" test at line 770:
    - After the split, CHECK-04 no longer BLOCKs on Direction 2 (task without Truth).
    - The existing test at line 770 uses a scenario where task is missing optional `status` field to trigger CHECK-02 WARN. This still works. No change needed to this test.
    - BUT: after split, CHECK-05 will also run and may produce additional 'warn' results. The test's assertion `result.overall === 'warn'` still holds. No change needed.
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && node --test test/smoke-plan-checker.test.js</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/plan-checker.js contains `function checkLogicCoverage(planContent, tasksContent, options = {})`
    - bin/lib/plan-checker.js contains `checkId: 'CHECK-05'` inside checkLogicCoverage
    - bin/lib/plan-checker.js contains `const severity = options.severity || 'warn'`
    - bin/lib/plan-checker.js contains `technical debt` in CHECK-05 issue message
    - bin/lib/plan-checker.js `checkTruthTaskCoverage` does NOT contain `Direction 2` or `task.truths.length === 0`
    - bin/lib/plan-checker.js `runAllChecks` signature contains `check05Severity`
    - bin/lib/plan-checker.js `runAllChecks` calls `checkLogicCoverage(planContent, tasksContent, { severity: check05Severity })`
    - bin/lib/plan-checker.js module.exports contains `checkLogicCoverage`
    - test/smoke-plan-checker.test.js contains `describe('CHECK-05: logicCoverage'`
    - test/smoke-plan-checker.test.js CHECK-05 block has tests for: warn default, block override, v1.0 pass, unknown pass, message content, all tasks pass
    - test/smoke-plan-checker.test.js does NOT contain `checks.length, 7` (all updated to 8)
    - `node --test test/smoke-plan-checker.test.js` exits with code 0
  </acceptance_criteria>
  <done>CHECK-04 only checks Direction 1 (Truth without task = BLOCK). CHECK-05 checkLogicCoverage checks Direction 2 (Task without Truth = configurable WARN). runAllChecks returns 8 checks. All existing + new tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update reference spec, workflow name mapping, and snapshot files</name>
  <files>references/plan-checker.md, workflows/plan.md, test/snapshots/opencode/plan.md, test/snapshots/copilot/plan.md, test/snapshots/gemini/plan.md, test/snapshots/codex/plan.md</files>
  <read_first>
    - references/plan-checker.md (full file — CHECK-04 section at line 122, severity table at line 268, combined result at line 248)
    - workflows/plan.md (line 329-337 — name mapping section)
    - test/snapshots/opencode/plan.md (line 280-288 — name mapping)
    - test/snapshots/copilot/plan.md (line 270-278 — name mapping)
    - test/snapshots/codex/plan.md (line 289-297 — name mapping)
    - test/snapshots/gemini/plan.md (search for "CHECK-01 = Requirement" — name mapping)
  </read_first>
  <action>
    **Part A: Update references/plan-checker.md**

    A1. Update section header and content at line 122-143:

    Change section `## 5. CHECK-04: Truth-Task Coverage` to only describe Direction 1:

    ```markdown
    ## 5. CHECK-04: Truth-Task Coverage (Direction 1)

    > Per D-04, D-01 (Phase 20)

    ### v1.0 Format
    - Parse truths tu frontmatter `must_haves: truths:` (nested YAML)
    - Tasks khong co explicit Truth refs trong v1.0 -> tu dong PASS
    - Follows D-10 graceful-skip principle

    ### v1.1 Format
    - Parse Truths tu PLAN.md Truths table
    - Parse task Truth refs tu `> Truths: [T1, T2]` metadata trong TASKS.md
    - Direction 1 only: Truth khong co task nao map -> BLOCK

    **Severity:**
    | Dieu kien | Severity |
    |-----------|----------|
    | Truth khong co task nao map (v1.1+) | BLOCK |
    | v1.0 format (khong co Truth-Task mapping) | PASS (skip) |
    ```

    A2. Add new section after CHECK-04 (before `## 6. ADV-01`):

    ```markdown
    ## 5b. CHECK-05: Logic Coverage (Direction 2)

    > Per D-01, D-02, D-04, D-05, D-06 (Phase 20)

    ### v1.0 Format
    - Khong co Truth-Task mapping -> tu dong PASS

    ### v1.1 Format
    - Parse tasks tu TASKS.md
    - Direction 2: Task khong co Truth nao map -> severity configurable (default WARN per D-04)
    - Orphan tasks = technical debt, reported trong PASS table issues (per D-06)

    ### Configuration
    - Default severity: `'warn'`
    - Override via `options.severity` parameter truyen qua `runAllChecks({ check05Severity: 'block' })`
    - Du an strict co the nang len BLOCK (per D-05)

    **Severity:**
    | Dieu kien | Severity |
    |-----------|----------|
    | Task khong co Truth nao map (v1.1+) | WARN (default) |
    | Task khong co Truth nao map + severity override | BLOCK (configurable) |
    | v1.0 format | PASS (skip) |
    | unknown format | PASS (skip) |
    ```

    A3. Update combined result JSON at line 248-259:
    Add `{ "checkId": "CHECK-05", "status": "pass", "issues": [] }` after the CHECK-04 line.

    A4. Update severity summary table at line 268-293:
    - Remove row `| CHECK-04 | Task khong co Truth nao map (v1.1+) | BLOCK |` (line 283)
    - Add after CHECK-04 rows:
    ```
    | CHECK-05 | Task khong co Truth nao map (v1.1+) | WARN (default) |
    | CHECK-05 | Task khong co Truth nao map + severity override | BLOCK (configurable) |
    | CHECK-05 | v1.0/unknown format | PASS (skip) |
    ```

    **Part B: Update workflows/plan.md name mapping (line 330-337)**

    Add `- CHECK-05 = Logic Coverage` after the `- CHECK-04 = Truth-Task Coverage` line.

    **Part C: Update 4 snapshot files**

    In each snapshot file, find the "Check name mapping" section and add `- CHECK-05 = Logic Coverage` after `- CHECK-04 = Truth-Task Coverage`:

    1. `test/snapshots/opencode/plan.md` — after line 284
    2. `test/snapshots/copilot/plan.md` — after line 274
    3. `test/snapshots/codex/plan.md` — after line 293
    4. `test/snapshots/gemini/plan.md` — find the CHECK-04 line, add CHECK-05 after it
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && grep -c "CHECK-05" references/plan-checker.md && grep "CHECK-05 = Logic Coverage" workflows/plan.md && grep "CHECK-05 = Logic Coverage" test/snapshots/opencode/plan.md test/snapshots/copilot/plan.md test/snapshots/gemini/plan.md test/snapshots/codex/plan.md && node --test test/smoke-plan-checker.test.js</automated>
  </verify>
  <acceptance_criteria>
    - references/plan-checker.md contains `## 5b. CHECK-05: Logic Coverage (Direction 2)`
    - references/plan-checker.md severity table contains `| CHECK-05 | Task khong co Truth nao map (v1.1+) | WARN (default) |`
    - references/plan-checker.md severity table does NOT contain `| CHECK-04 | Task khong co Truth nao map`
    - references/plan-checker.md combined result JSON contains `"checkId": "CHECK-05"`
    - references/plan-checker.md CHECK-04 section does NOT contain `Task khong co Truth nao map`
    - workflows/plan.md contains `- CHECK-05 = Logic Coverage`
    - test/snapshots/opencode/plan.md contains `- CHECK-05 = Logic Coverage`
    - test/snapshots/copilot/plan.md contains `- CHECK-05 = Logic Coverage`
    - test/snapshots/gemini/plan.md contains `- CHECK-05 = Logic Coverage`
    - test/snapshots/codex/plan.md contains `- CHECK-05 = Logic Coverage`
    - `node --test test/smoke-plan-checker.test.js` exits with code 0 (snapshot tests still pass)
  </acceptance_criteria>
  <done>CHECK-05 documented in reference spec with correct severity table. All 4 snapshots + workflows/plan.md have CHECK-05 name mapping. All tests pass.</done>
</task>

</tasks>

<verification>
1. `node --test test/smoke-plan-checker.test.js` — all tests pass (0 failures)
2. `grep "function checkLogicCoverage" bin/lib/plan-checker.js` — function exists
3. `grep "CHECK-05" bin/lib/plan-checker.js` — CHECK-05 checkId present
4. `grep "checkLogicCoverage" bin/lib/plan-checker.js | grep "module.exports" -A 50` — exported
5. `grep "checks.length, 7" test/smoke-plan-checker.test.js` — returns 0 matches (all updated to 8)
6. `grep "CHECK-05 = Logic Coverage" workflows/plan.md test/snapshots/*/plan.md` — 5 files matched
</verification>

<success_criteria>
- CHECK-04 only checks Direction 1 (Truth without task = BLOCK)
- CHECK-05 checkLogicCoverage checks Direction 2 (Task without Truth = WARN default, configurable to BLOCK)
- runAllChecks returns 8 checks, passes check05Severity option through
- All 147+ existing tests still pass (updated where needed)
- New CHECK-05 tests cover: default warn, block override, v1.0 pass, unknown pass, message content, all-pass, checkId
- Reference spec, workflow name mapping, and 4 snapshot files updated
</success_criteria>

<output>
After completion, create `.planning/phases/20-logic-audit/20-01-SUMMARY.md`
</output>
