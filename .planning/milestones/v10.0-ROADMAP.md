# Milestone v10.0: Skill Repo Audit Fixes

**Status:** ✅ SHIPPED 2026-04-03
**Phases:** 84–87
**Total Plans:** 8 (3 + 1 + 2 + 2)

## Overview

Addressed all critical findings from the Skill Repo Audit (SKILL_REPO_AUDIT.md). Fixed documentation accuracy, cleaned up stale content, hardened error handling in installer and library code, and added smoke test coverage for the audit-gap areas.

## Phases

### Phase 84: Documentation & Version Consistency

**Goal:** All project documentation is accurate, version-consistent, and complete — no stale badges, dead links, or missing command docs.
**Depends on:** Nothing (independent of v9.0)
**Requirements:** DOC-01, DOC-02, DOC-03, DOC-04
**Effort:** Small (file edits + 4 new doc files)
**Plans:** 3/3 plans complete

Plans:
- [x] 84-01-PLAN.md — Fix README.md badge and add Supported Stacks section; add CHANGELOG.md deprecation notice
- [x] 84-02-PLAN.md — Create INTEGRATION_GUIDE.md
- [x] 84-03-PLAN.md — Create 4 missing command docs (audit, conventions, onboard, status)

**Success Criteria met:**
1. README.md version badge displays `4.0.0` matching `VERSION` file ✓
2. Every link in README.md resolves to an existing file — INTEGRATION_GUIDE.md created ✓
3. `docs/commands/` contains documentation for all 16 commands ✓
4. CHANGELOG.md deprecated with pointer to MILESTONES.md ✓

---

### Phase 85: Language & Content Cleanup

**Goal:** All content files use correct English conventions and only active, wired files remain in the repo tree.
**Depends on:** Nothing (independent)
**Requirements:** LANG-01, CLEAN-01, CLEAN-02
**Effort:** Small (3 targeted file edits/moves)
**Plans:** 1/1 plans complete

Plans:
- [x] 85-01-PLAN.md — Fix Vietnamese reference, verify mermaid-rules wiring, archive fix-bug-v1.5.md

**Success Criteria met:**
1. `workflows/write-code.md` line 471 uses English convention prefixes — Vietnamese mention removed ✓
2. `references/mermaid-rules.md` wired into mermaid-validator.js → generate-diagrams.js pipeline ✓
3. `workflows/fix-bug-v1.5.md` archived to `workflows/legacy/`, `fix-bug.md:32` updated ✓

---

### Phase 86: Error Handling Hardening

**Goal:** All error paths produce observable diagnostic output instead of silently swallowing failures, and no library code calls `process.exit()`.
**Depends on:** Nothing (independent)
**Requirements:** ERR-01, ERR-02, ERR-03
**Effort:** Small-Medium (3 JS files, targeted catch-block edits)
**Plans:** 2/2 plans complete

Plans:
- [x] 86-01-PLAN.md — Fix bare catches in plan-check.js + utils.js (fileHash); align install.js to PD_DEBUG (ERR-01, ERR-02)
- [x] 86-02-PLAN.md — Replace all 6 process.exit(1) calls in claude.js with throw new Error() (ERR-03)

**Success Criteria met:**
1. `bin/plan-check.js` zero bare `catch {}` blocks — all catches conditionally log under PD_DEBUG ✓
2. `bin/lib/utils.js` zero bare `catch` blocks — fileHash catch logs under PD_DEBUG ✓
3. `bin/lib/installers/claude.js` throws errors instead of calling `process.exit(1)` ✓
4. PD_DEBUG=1 reveals previously-swallowed error details ✓

---

### Phase 87: Test Coverage

**Goal:** New smoke tests cover the audit gaps (onboard, error-handling) and the entire suite passes with zero regressions after all v10.0 changes.
**Depends on:** Phase 84, Phase 85, Phase 86 (tests verify those changes)
**Requirements:** TEST-01, TEST-02, TEST-03
**Effort:** Small (2 test files + full suite run)
**Plans:** 2/2 plans complete

Plans:
- [x] 87-01-PLAN.md — Create test/smoke-onboard.test.js (skill structure, workflow reference, guard checks)
- [x] 87-02-PLAN.md — Update smoke-error-handling.test.js TARGET_FILES + full regression run

**Success Criteria met:**
1. `test/smoke-onboard.test.js` exists — 6 tests, all passing ✓
2. `test/smoke-error-handling.test.js` TARGET_FILES includes `bin/plan-check.js` and `bin/lib/utils.js` with exemption comments ✓
3. Full test suite: 1232 tests, 0 failures ✓

---

## Milestone Summary

**Stats:**
- 4 phases, 8 plans
- 41 commits over 1 day (2026-04-03)
- 55 files changed, 5254 insertions, 98 deletions

**Key Decisions:**
- ERR-03: Library code (claude.js) throws errors; `process.exit(1)` centralized only in `bin/install.js` main().catch() — cleaner propagation, testable
- ERR-01/ERR-02: PD_DEBUG pattern (console.error) chosen over log module for plan-check.js and utils.js — these are CLI/utility scripts where the `log` module isn't wired
- CLEAN-01: mermaid-rules.md retained (already wired into generator pipeline via mermaid-validator.js) — no change required
- TEST-02: plan-check.js and utils.js excluded from LOG_TARGET_FILES (log.warn loop) intentionally — documented in test comments

**Issues Resolved:**
- README version badge was stuck at 2.8.0 while VERSION file was at 4.0.0
- INTEGRATION_GUIDE.md was referenced in README but didn't exist (dead link)
- 4 command docs (audit, conventions, onboard, status) missing from docs/commands/
- Vietnamese text in write-code.md commit convention
- fix-bug-v1.5.md floating as an unwired file in workflows/
- 2 bare catch {} blocks in plan-check.js silently swallowing errors
- fileHash bare catch in utils.js silently swallowing hash errors
- 6 process.exit(1) calls in claude.js breaking error propagation chain

**Issues Deferred:**
- Vietnamese user-facing strings in bin/plan-check.js lines 6/21/30/34 (not in audit scope — flag for v11.0 language sweep)
- Phase 86 VALIDATION.md left in draft state (nyquist_compliant: false) — functional coverage verified by VERIFICATION.md

**Technical Debt Incurred:**
- SUMMARY.md frontmatter for phases 84, 86, 87 lacks `requirements_completed` field (only Phase 85 has it) — no functional impact, housekeeping only

---

_For current project status, see .planning/ROADMAP.md_
