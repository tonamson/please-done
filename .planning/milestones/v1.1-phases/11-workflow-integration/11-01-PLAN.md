---
phase: 11-workflow-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/plan.md
autonomous: false
requirements:
  - INTG-01
  - INTG-02
user_setup: []

must_haves:
  truths:
    - "Khi plan pass tat ca checks, user thay PASS voi summary table 4 checks"
    - "Khi plan co issues, user thay ISSUES FOUND voi grouped issues + fix hints"
    - "Plan checker tu dong chay sau Step 8 va truoc Step 8.5 — khong can user invoke rieng"
    - "User co the chon Fix / Proceed / Cancel khi gap issues"
    - "BLOCK proceed yeu cau xac nhan rieng (Force proceed)"
    - "Cancel giu files tren disk va ghi note vao STATE.md"
  artifacts:
    - path: "workflows/plan.md"
      provides: "Step 8.1 plan checker integration"
      contains: "## Buoc 8.1"
  key_links:
    - from: "workflows/plan.md (Step 8.1)"
      to: "bin/lib/plan-checker.js"
      via: "runAllChecks API call"
      pattern: "runAllChecks"
    - from: "workflows/plan.md (Step 8.1)"
      to: ".planning/STATE.md"
      via: "audit entries for proceed/cancel"
      pattern: "STATE.md"
    - from: "workflows/plan.md (Step 8.1)"
      to: ".planning/ROADMAP.md"
      via: "requirement IDs parsing"
      pattern: "Requirements"
---

<objective>
Integrate the plan-checker module (Phase 10) into the plan workflow as Step 8.1, running automatically after tracking update (Step 8) and before git commit (Step 8.5). Display PASS/ISSUES FOUND report, present user choices (Fix/Proceed/Cancel), handle BLOCK confirmation, fix loop, and write audit entries to STATE.md.

Purpose: Quality gate for plans — catch structural issues before commit and execution.
Output: Modified workflows/plan.md with Step 8.1 containing complete plan checker integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-core-plan-checks/10-01-SUMMARY.md
@.planning/phases/10-core-plan-checks/10-02-SUMMARY.md
@.planning/phases/11-workflow-integration/11-CONTEXT.md
@.planning/phases/11-workflow-integration/11-RESEARCH.md

<interfaces>
<!-- Key API and patterns the executor needs. Extracted from Phase 10 and existing workflow. -->

From bin/lib/plan-checker.js (runAllChecks API):
```javascript
const { runAllChecks, detectPlanFormat } = require('./plan-checker');

// Main entry point - pure function, no I/O
const result = runAllChecks({
  planContent,      // string: full PLAN.md content
  tasksContent,     // string: full TASKS.md content (null for v1.0)
  requirementIds    // string[]: e.g. ['INTG-01', 'INTG-02']
});

// Result structure:
// {
//   overall: 'pass' | 'block' | 'warn',
//   checks: [
//     { checkId: 'CHECK-01', status: 'pass'|'block'|'warn', issues: [{message, location, fixHint}] },
//     { checkId: 'CHECK-02', status: ... },
//     { checkId: 'CHECK-03', status: ... },
//     { checkId: 'CHECK-04', status: ... }
//   ]
// }
```

From workflows/plan.md (insertion point — lines 283-310):
```markdown
## Buoc 8: Cap nhat tracking
[content: updates CURRENT_MILESTONE.md, STATE.md, ROADMAP.md]
---

## Buoc 8.5: Git commit (CHI neu co git)
[content: git add + commit for plan files]
```

From workflows/new-milestone.md (AskUserQuestion pattern):
```
AskUserQuestion({
  questions: [{
    question: "...",
    header: "...",
    multiSelect: false,
    options: [
      { label: "Option (De xuat)", description: "..." },
      { label: "Other option", description: "..." }
    ]
  }]
})
```

From templates/state.md (Accumulated Context structure):
```markdown
## Boi canh tich luy
[Entries formatted as: - [context]: description]
```

From .planning/ROADMAP.md (requirement IDs format):
```markdown
### Phase 11: Workflow Integration
**Requirements**: INTG-01, INTG-02
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 8.1 (plan checker) to workflows/plan.md</name>
  <files>workflows/plan.md</files>
  <read_first>
    - workflows/plan.md (full file — understand existing step structure, insertion point between Step 8 line 296 and Step 8.5 line 298)
    - bin/lib/plan-checker.js (lines 600-643 — runAllChecks API signature and result structure)
    - workflows/new-milestone.md (lines 77-104 — AskUserQuestion pattern with single-select and "(De xuat)" convention)
    - .planning/phases/11-workflow-integration/11-CONTEXT.md (all 13 locked decisions D-01 through D-13)
    - .planning/phases/11-workflow-integration/11-RESEARCH.md (code examples sections — exact report formats, choice patterns, audit entries)
    - templates/state.md (Accumulated Context section structure)
  </read_first>
  <action>
Insert a new "## Buoc 8.1: Kiem tra plan" section in workflows/plan.md between the `---` after Step 8 (line 296) and `## Buoc 8.5` (line 298). The new step must contain ALL of the following behavior, implementing all 13 locked decisions:

**A. Data preparation:**
1. Read PLAN.md from `.planning/milestones/[version]/phase-[phase]/PLAN.md` (already created in Step 6)
2. Read TASKS.md from `.planning/milestones/[version]/phase-[phase]/TASKS.md` (already created in Step 7)
3. Parse requirement IDs from ROADMAP.md: find current phase section, extract comma-separated IDs from `**Requirements**:` line. If field missing, pass empty array `[]`
4. Call `runAllChecks({ planContent, tasksContent, requirementIds })` from `bin/lib/plan-checker.js`

**B. PASS path (per D-01):**
When `result.overall === 'pass'`, display summary table then continue to Step 8.5:
```markdown
### Kiem tra plan

| Check | Ket qua |
|-------|---------|
| CHECK-01: Requirement Coverage | PASS |
| CHECK-02: Task Completeness | PASS |
| CHECK-03: Dependency Correctness | PASS |
| CHECK-04: Truth-Task Coverage | PASS |

**Ket qua: PASS** — Plan dat chat luong, tiep tuc commit.
```

**C. ISSUES FOUND path (per D-02, D-03, D-04):**
When `result.overall === 'block'` or `result.overall === 'warn'`, display:
```markdown
### Kiem tra plan

**Ket qua: ISSUES FOUND**

#### CHECK-01: Requirement Coverage — [STATUS]
- [issue.message for each issue]

#### CHECK-02: Task Completeness — [STATUS]
- [issue.message for each issue]
```
Rules:
- Group by check: header per check that has issues, skip checks with status 'pass' (per D-02)
- Show ONLY `issue.message` — do NOT show `issue.fixHint` (fixHint is internal for Claude auto-fix) (per D-03)
- Max 10 issues total displayed. If more, append `+ [N] more issues` at the end (per D-04)
- For checks that passed, show as single line: `#### CHECK-0N: [Name] — PASS`

**D. User choice (per D-07, D-08):**
After displaying ISSUES FOUND report, ALWAYS ask user choice — even when only WARN issues exist (per D-07):
```
AskUserQuestion({
  questions: [{
    question: "Plan co issues. Ban muon xu ly the nao?",
    header: "Kiem tra plan",
    multiSelect: false,
    options: [
      { label: "Fix (De xuat)", description: "Claude tu dong sua issues va kiem tra lai" },
      { label: "Proceed with warnings", description: "Bo qua issues, tiep tuc commit" },
      { label: "Cancel", description: "Giu plan tren disk, ghi note vao STATE.md" }
    ]
  }]
})
```

**E. Fix path (per D-05, D-06):**
When user selects "Fix":
1. Claude reads all issues (including fixHint from result) and auto-fixes PLAN.md and/or TASKS.md directly (per D-05)
2. After fix, re-run `runAllChecks` with updated content (per D-06)
3. If pass → display PASS report → continue to Step 8.5
4. If still issues → display ISSUES FOUND report again → present user choice again
5. Max 3 re-run iterations. After 3rd failed fix → suggest Cancel (Claude discretion)

**F. Proceed path — WARN only (per D-12):**
When user selects "Proceed with warnings" and result.overall === 'warn':
1. Write acknowledged warnings to STATE.md "Boi canh tich luy" section (Decisions subsection):
   `- [Phase [N] Plan Check]: Plan [phase]-[plan] proceed with [count] warnings acknowledged: [list of check + issue summaries]`
2. Continue to Step 8.5

**G. Proceed path — has BLOCK (per D-09, D-10):**
When user selects "Proceed with warnings" and result contains BLOCK issues:
1. Show second confirmation (per D-09):
```
AskUserQuestion({
  questions: [{
    question: "Plan co BLOCK issues. Xac nhan force proceed?",
    header: "Canh bao: BLOCK issues",
    multiSelect: false,
    options: [
      { label: "Force proceed", description: "Bo qua BLOCK issues — se ghi audit vao STATE.md" },
      { label: "Quay lai (De xuat)", description: "Chon lai: Fix hoac Cancel" }
    ]
  }]
})
```
2. "Quay lai" → return to choice D above
3. "Force proceed" → write BLOCK audit to STATE.md "Boi canh tich luy" > Decisions (per D-10):
   `- [Phase [N] Plan Check]: Plan [phase]-[plan] proceed with [count] BLOCK overrides: [list of check + issue summaries]`
4. Also write WARN acknowledgments if any (per D-12)
5. Continue to Step 8.5

**H. Cancel path (per D-11):**
When user selects "Cancel":
1. Keep PLAN.md + TASKS.md on disk — do NOT delete them (per D-11)
2. Write cancel note to STATE.md "Boi canh tich luy" > Decisions:
   `- [Phase [N] Plan Check]: Plan [phase]-[plan] cancelled — [count] BLOCK issues, [count] WARN issues found`
3. STOP workflow — do NOT continue to Step 8.5 or Step 9

**I. Cumulative warning notice (per D-13):**
Before displaying user choice (Step D), check STATE.md "Boi canh tich luy" section for recent entries matching `[Phase * Plan Check]: Plan * proceed with * warnings`. If 3+ such entries exist in current milestone:
```markdown
> Luu y: [N] plans gan day deu co warnings duoc proceed. Review lai chat luong plan neu can.
```
This is informational only — does NOT block or change options.

**J. Re-plan interaction (Claude discretion):**
Step 8.1 runs identically regardless of whether plan was freshly created or loaded from disk via Step 1.5 "Giu nguyen". The checker is idempotent. If plan was kept unchanged and already checked, results will be same. No special handling needed.

**Format notes:**
- Use `---` separator before and after the new step (matching existing pattern)
- Step heading: `## Buoc 8.1: Kiem tra plan`
- Write instructions in Vietnamese (matching existing workflow language)
- Keep instructions concise but complete — this is a workflow instruction file read by Claude, not executable code
  </action>
  <verify>
    <automated>grep -c "Buoc 8.1" workflows/plan.md | grep -q "1" && grep -c "runAllChecks" workflows/plan.md | grep -q "[1-9]" && grep -c "AskUserQuestion" workflows/plan.md | grep -q "[1-9]" && echo "PASS: Step 8.1 exists with runAllChecks and AskUserQuestion" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/plan.md contains exactly one `## Buoc 8.1: Kiem tra plan` heading
    - workflows/plan.md contains `runAllChecks` reference (API call instruction)
    - workflows/plan.md contains at least 2 `AskUserQuestion` blocks (issues choice + BLOCK confirm)
    - The Step 8.1 section appears AFTER `## Buoc 8:` and BEFORE `## Buoc 8.5:`
    - workflows/plan.md contains `PASS` report format with CHECK-01 through CHECK-04 table
    - workflows/plan.md contains `ISSUES FOUND` report format with grouped checks
    - workflows/plan.md contains `fixHint` reference in Fix path (internal use, not displayed)
    - workflows/plan.md contains `Force proceed` option for BLOCK confirmation
    - workflows/plan.md contains STATE.md audit entry format for proceed/cancel
    - workflows/plan.md contains `+ [N] more issues` or equivalent truncation instruction
    - workflows/plan.md contains cumulative warning check instruction referencing `Luu y:` message
    - workflows/plan.md contains max 3 iterations instruction for fix loop
    - Existing steps (1-8, 8.5, 9) are unchanged
    - Full test suite still passes: `node --test 'test/*.test.js'` exits 0
  </acceptance_criteria>
  <done>Step 8.1 fully integrated into workflows/plan.md with all 13 decisions (D-01 through D-13) implemented: PASS report, ISSUES FOUND report, Fix loop, Proceed with WARN/BLOCK paths, Cancel path, STATE.md audit, cumulative warning notice</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Step 8.1 integration in workflow</name>
  <what-built>Step 8.1 plan checker integration in workflows/plan.md — automatic quality gate running after Step 8 (tracking update) and before Step 8.5 (git commit). Displays PASS/ISSUES FOUND report, presents Fix/Proceed/Cancel choices, handles BLOCK confirmation, writes audit to STATE.md.</what-built>
  <how-to-verify>
    1. Open workflows/plan.md and read Step 8.1 section
    2. Verify step order: Step 8 → Step 8.1 → Step 8.5 (no steps missing or reordered)
    3. Verify PASS report format matches D-01: table with CHECK-01 through CHECK-04 status
    4. Verify ISSUES FOUND format matches D-02/D-03/D-04: grouped by check, no fixHint shown, max 10 issues
    5. Verify user choice matches D-08: Fix (De xuat) / Proceed with warnings / Cancel
    6. Verify BLOCK confirmation matches D-09: Force proceed / Quay lai (De xuat)
    7. Verify Fix loop per D-05/D-06: fix → re-run → repeat or choice
    8. Verify Cancel path per D-11: keep files, write STATE.md note
    9. Verify Proceed paths per D-10/D-12: audit entries in STATE.md
    10. Verify cumulative warning per D-13: soft notice when 3+ plans had warnings
    11. Run full test suite: `node --test 'test/*.test.js'` — all tests should pass (no regressions)
    12. (Optional) Run `/pd:plan` on a test phase to see Step 8.1 in action
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `grep -n "Buoc 8.1" workflows/plan.md` — returns exactly one match
2. `grep -n "Buoc 8:" workflows/plan.md` then `grep -n "Buoc 8.1:" workflows/plan.md` then `grep -n "Buoc 8.5:" workflows/plan.md` — line numbers should be in ascending order (8 < 8.1 < 8.5)
3. `node --test 'test/*.test.js'` — all existing tests pass (no regressions to plan-checker.js)
4. Manual: Step 8.1 content covers all 13 decisions from 11-CONTEXT.md
</verification>

<success_criteria>
- workflows/plan.md contains Step 8.1 between Step 8 and Step 8.5
- Step 8.1 implements INTG-01: PASS report with summary table, ISSUES FOUND report with grouped issues
- Step 8.1 implements INTG-02: automatic execution in workflow, no separate invocation needed
- All 13 locked decisions (D-01 through D-13) are reflected in Step 8.1 instructions
- Existing test suite passes with zero regressions
- User approves the integration at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/11-workflow-integration/11-01-SUMMARY.md`
</output>
