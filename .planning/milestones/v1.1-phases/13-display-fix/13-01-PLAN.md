---
phase: 13-display-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [workflows/plan.md]
autonomous: true
requirements: [INTG-01]

must_haves:
  truths:
    - "PASS table hien thi tat ca 7 checks (CHECK-01..04, ADV-01..03) thay vi hardcode 4"
    - "Khi them check moi trong tuong lai, PASS table tu dong bao gom ma khong can sua template"
    - "Section C examples bao gom ca CHECK va ADV check de minh hoa pattern"
  artifacts:
    - path: "workflows/plan.md"
      provides: "Step 8.1 plan checker integration voi dynamic PASS table va complete examples"
      contains: "result.checks"
  key_links:
    - from: "workflows/plan.md Section B"
      to: "runAllChecks result.checks"
      via: "dynamic iteration instruction"
      pattern: "result\\.checks"
---

<objective>
Fix PASS report table trong workflows/plan.md Step 8.1 de hien thi day du tat ca checks tu runAllChecks result, thay vi hardcode 4 check names. Dong thoi them ADV check example vao Section C.

Purpose: Closes gap INTG-DISPLAY-01 tu v1.1 audit — user can thay tat ca 7 checks khi plan PASS, va template tu dong adapt khi them check moi.
Output: Updated workflows/plan.md voi dynamic PASS table va complete ISSUES FOUND examples.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-display-fix/13-CONTEXT.md
@workflows/plan.md (lines 298-345 — Step 8.1 sections B va C)
@references/plan-checker.md (section 9 — Result Format, 7-check structure)

<interfaces>
<!-- runAllChecks API tu bin/lib/plan-checker.js — KHONG thay doi -->

runAllChecks returns:
```json
{
  "overall": "pass|block|warn",
  "checks": [
    { "checkId": "CHECK-01", "status": "pass|block|warn", "issues": [...] },
    { "checkId": "CHECK-02", "status": "pass|block|warn", "issues": [...] },
    { "checkId": "CHECK-03", "status": "pass|block|warn", "issues": [...] },
    { "checkId": "CHECK-04", "status": "pass|block|warn", "issues": [...] },
    { "checkId": "ADV-01", "status": "pass|block|warn", "issues": [...] },
    { "checkId": "ADV-02", "status": "pass|block|warn", "issues": [...] },
    { "checkId": "ADV-03", "status": "pass|block|warn", "issues": [...] }
  ]
}
```

checkId name mapping (per D-03):
- CHECK-01 = Requirement Coverage
- CHECK-02 = Task Completeness
- CHECK-03 = Dependency Correctness
- CHECK-04 = Truth-Task Coverage
- ADV-01 = Key Links
- ADV-02 = Scope Thresholds
- ADV-03 = Effort Classification
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded PASS table with dynamic iteration instruction</name>
  <files>workflows/plan.md</files>
  <read_first>
    - workflows/plan.md (full file, focus lines 310-325 Section B)
    - references/plan-checker.md (section 9, Result Format — confirm 7-check structure)
    - .planning/phases/13-display-fix/13-CONTEXT.md (decisions D-01, D-02, D-03)
  </read_first>
  <action>
Thay the Section B (lines 310-325) trong workflows/plan.md. Giu nguyen header va dieu kien, thay noi dung ben trong.

THAY THE lines 314-325 (markdown code block voi hardcoded 4 rows) BANG:

```markdown
### Kiem tra plan

| Check | Ket qua |
|-------|---------|
<!-- Voi moi check trong result.checks, render 1 dong: -->
| {check.checkId}: {ten mo ta} | PASS |

**Ket qua: PASS** — Plan dat chat luong, tiep tuc commit.
```

Va THEM instruction text NGAY SAU closing ``` cua code block, TRUOC Section C:

```
Check name mapping (dung cho PASS table va ISSUES FOUND headers):
- CHECK-01 = Requirement Coverage
- CHECK-02 = Task Completeness
- CHECK-03 = Dependency Correctness
- CHECK-04 = Truth-Task Coverage
- ADV-01 = Key Links
- ADV-02 = Scope Thresholds
- ADV-03 = Effort Classification

**Quan trong:** Iterate qua `result.checks` array — KHONG hardcode ten check. Khi co check moi, chi can them vao mapping tren.
```

Cu the, thay the TOAN BO noi dung tu line 310 den line 325 bang:

---

### B. Ket qua PASS (D-01)

Khi `result.overall === 'pass'` — hien thi summary table roi tiep tuc Buoc 8.5:

```markdown
### Kiem tra plan

| Check | Ket qua |
|-------|---------|
<!-- Iterate qua result.checks array, moi check 1 dong: -->
| {check.checkId}: {ten mo ta} | PASS |

**Ket qua: PASS** — Plan dat chat luong, tiep tuc commit.
```

Check name mapping (dung cho PASS table va ISSUES FOUND headers):
- CHECK-01 = Requirement Coverage
- CHECK-02 = Task Completeness
- CHECK-03 = Dependency Correctness
- CHECK-04 = Truth-Task Coverage
- ADV-01 = Key Links
- ADV-02 = Scope Thresholds
- ADV-03 = Effort Classification

**Quan trong:** Iterate qua `result.checks` array — KHONG hardcode ten check. Khi co check moi, chi can them vao mapping tren.

---

Per D-01: Dynamic iteration qua result.checks.
Per D-02: Format moi dong `| {check.checkId}: {ten mo ta} | PASS |`.
Per D-03: Ten mo ta lay tu mapping co dinh (checkId la stable identifier).
  </action>
  <verify>
    <automated>grep -c "result.checks" workflows/plan.md | grep -q "[1-9]" && echo "PASS: result.checks reference found" || echo "FAIL: no result.checks reference"</automated>
  </verify>
  <acceptance_criteria>
    - grep "result.checks" workflows/plan.md tra ve it nhat 1 match
    - grep "ADV-01 = Key Links" workflows/plan.md tra ve match
    - grep "ADV-02 = Scope Thresholds" workflows/plan.md tra ve match
    - grep "ADV-03 = Effort Classification" workflows/plan.md tra ve match
    - grep -c "CHECK-01: Requirement Coverage | PASS" workflows/plan.md tra ve 0 (hardcoded row DA BI XOA)
    - grep "KHONG hardcode ten check" workflows/plan.md tra ve match
    - grep "check.checkId" workflows/plan.md tra ve match
  </acceptance_criteria>
  <done>Section B khong con hardcode 4 check names. Thay vao do co instruction iterate qua result.checks array voi name mapping. Future checks tu dong duoc bao gom khi them vao mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Add ADV check example to Section C ISSUES FOUND</name>
  <files>workflows/plan.md</files>
  <read_first>
    - workflows/plan.md (lines 327-345 Section C — sau khi Task 1 da thay doi Section B)
    - .planning/phases/13-display-fix/13-CONTEXT.md (decisions D-04, D-05)
  </read_first>
  <action>
Cap nhat Section C examples (hien tai chi co CHECK-01, CHECK-02) de them 1 ADV check minh hoa ca 2 loai.

THAY THE markdown code block trong Section C (hien tai lines 331-341):

TU:
```markdown
### Kiem tra plan

**Ket qua: ISSUES FOUND**

#### CHECK-01: Requirement Coverage — [STATUS]
- [issue.message cho tung issue]

#### CHECK-02: Task Completeness — [STATUS]
- [issue.message cho tung issue]
```

THANH:
```markdown
### Kiem tra plan

**Ket qua: ISSUES FOUND**

#### CHECK-01: Requirement Coverage — BLOCK
- [issue.message cho tung issue]

#### CHECK-02: Task Completeness — PASS

#### ADV-02: Scope Thresholds — WARN
- [issue.message cho tung issue]
```

Thay doi:
1. CHECK-01 them "BLOCK" status cu the thay vi "[STATUS]" — minh hoa BLOCK (per D-04)
2. CHECK-02 thay "[STATUS]" thanh "PASS" — minh hoa check pass trong ISSUES report (khop voi quy tac line 344 "Check PASS → hien thi 1 dong")
3. Them ADV-02 voi "WARN" — minh hoa advanced check va WARN severity (per D-04)

Per D-04: Them 1 ADV check vao examples de minh hoa ca 2 loai (core + advanced).
Per D-05: Giu nguyen quy tac nhom theo check o line 344, chi update examples.

LUU Y: Dong quy tac line 344 hien co pattern `CHECK-0N`. Thay doi thanh pattern tong quat hon `{checkId}` de phu hop voi ca ADV checks:
- TU: `Check PASS → hiển thị 1 dòng: #### CHECK-0N: [Tên] — PASS`
- THANH: `Check PASS → hiển thị 1 dòng: #### {checkId}: {Tên} — PASS`
  </action>
  <verify>
    <automated>grep "ADV-02: Scope Thresholds" workflows/plan.md | grep -q "WARN" && echo "PASS: ADV example found" || echo "FAIL: no ADV example"</automated>
  </verify>
  <acceptance_criteria>
    - grep "ADV-02: Scope Thresholds — WARN" workflows/plan.md tra ve match
    - grep "CHECK-01: Requirement Coverage — BLOCK" workflows/plan.md tra ve match
    - grep "CHECK-02: Task Completeness — PASS" workflows/plan.md tra ve match
    - grep "{checkId}" workflows/plan.md tra ve match (pattern tong quat trong quy tac)
    - grep -c "CHECK-0N" workflows/plan.md tra ve 0 (pattern cu DA BI THAY)
  </acceptance_criteria>
  <done>Section C examples bao gom ca CHECK (core) va ADV (advanced) check. Pattern quy tac da duoc tong quat hoa tu CHECK-0N thanh {checkId} de phu hop voi tat ca check types.</done>
</task>

</tasks>

<verification>
1. Section B: PASS table instruction la dynamic (iterate result.checks), KHONG hardcode check names
2. Section B: Name mapping co day du 7 entries (CHECK-01..04, ADV-01..03)
3. Section C: Examples bao gom it nhat 1 CHECK va 1 ADV check
4. Section C: Quy tac dung pattern tong quat {checkId} thay vi CHECK-0N
5. Phan con lai cua Step 8.1 (sections A, D-I) KHONG thay doi
6. File workflows/plan.md khong co syntax errors (markdown valid)
</verification>

<success_criteria>
- PASS table se hien thi day du 7 checks khi plan pass (verifiable bang doc Section B instruction)
- Future checks tu dong xuat hien chi can them vao name mapping (khong can sua template logic)
- ISSUES FOUND examples minh hoa ca core (CHECK) va advanced (ADV) check types
- Khong co side effects len cac sections khac cua Step 8.1
</success_criteria>

<output>
After completion, create `.planning/phases/13-display-fix/13-01-SUMMARY.md`
</output>
