---
phase: 10-core-plan-checks
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - test/smoke-plan-checker.test.js
  - bin/lib/plan-checker.js
autonomous: true
requirements: [CHECK-01, CHECK-02, CHECK-03, CHECK-04]
must_haves:
  truths:
    - "Unit tests cover all 4 checks with pass, block, and warn scenarios"
    - "Historical validation runs all 22 v1.0 PLAN.md files through runAllChecks with zero false positives"
    - "All tests pass via npm test"
  artifacts:
    - path: "test/smoke-plan-checker.test.js"
      provides: "Unit tests + historical validation for plan checker"
      contains: "describe.*plan-checker"
  key_links:
    - from: "test/smoke-plan-checker.test.js"
      to: "bin/lib/plan-checker.js"
      via: "imports all check functions and helpers"
      pattern: "require.*plan-checker"
    - from: "test/smoke-plan-checker.test.js"
      to: ".planning/phases/"
      via: "reads 22 historical PLAN.md files for validation"
      pattern: "planning.*phases.*PLAN"
---

<objective>
Create comprehensive tests for the plan checker module: unit tests for all 4 checks + historical validation against all 22 v1.0 plans with zero false positives (D-17 acceptance gate).

Purpose: Prove the checker works correctly on synthetic test cases AND produces zero false positives on real historical plans. Fix any issues discovered during historical validation.
Output: `test/smoke-plan-checker.test.js` with passing test suite. Any fixes to `bin/lib/plan-checker.js` needed to achieve zero false positives.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-core-plan-checks/10-CONTEXT.md
@.planning/phases/10-core-plan-checks/10-RESEARCH.md
@.planning/phases/10-core-plan-checks/10-01-SUMMARY.md

<interfaces>
<!-- plan-checker.js exports (created in Plan 01) -->
From bin/lib/plan-checker.js:
```javascript
// Main checks
function detectPlanFormat(planContent) -> 'v1.0' | 'v1.1' | 'unknown'
function checkRequirementCoverage(planContent, requirementIds) -> { checkId, status, issues }
function checkTaskCompleteness(planContent, tasksContent) -> { checkId, status, issues }
function checkDependencyCorrectness(planContent, tasksContent) -> { checkId, status, issues }
function checkTruthTaskCoverage(planContent, tasksContent) -> { checkId, status, issues }
function runAllChecks({ planContent, tasksContent, requirementIds }) -> { overall, checks }

// Helpers (also exported for testing)
function escapeRegex(str) -> string
function parseRequirements(frontmatter) -> string[]
function parseDependsOn(frontmatter) -> string[]
function parseMustHavesTruths(rawFrontmatter) -> [{ id, description }]
function parseTasksV10(planContent) -> [{ id, name, files, hasDescription, hasCriteria }]
function parseTruthsV11(planContent) -> [{ id, description }]
function parseTaskDetailBlocksV11(tasksContent) -> [{ id, effort, files, truths, ... }]
function detectCycles(nodes, edges) -> { hasCycle, nodesInCycle }
function findInvalidRefs(validIds, edges) -> [{ message, location, fixHint }]
```

<!-- Historical plan files (22 total) -->
Phase directories containing PLAN.md files:
- .planning/phases/01-skill-structure-normalization/ (01-01, 01-02, 01-03)
- .planning/phases/02-cross-skill-deduplication/ (02-01, 02-02)
- .planning/phases/03-prompt-prose-compression/ (03-01 through 03-06)
- .planning/phases/04-conditional-context-loading/ (04-01, 04-02)
- .planning/phases/05-effort-level-routing/ (05-01, 05-02)
- .planning/phases/06-context7-standardization/ (06-01, 06-02)
- .planning/phases/07-library-fallback-and-version-detection/ (07-01)
- .planning/phases/08-wave-based-parallel-execution/ (08-01, 08-02)
- .planning/phases/09-converter-pipeline-optimization/ (09-01, 09-02)

<!-- parseFrontmatter behavior for bracket notation -->
parseFrontmatter quirks verified by testing:
- `requirements: [LIBR-02, LIBR-03]` -> frontmatter.requirements = "[LIBR-02, LIBR-03]" (string)
- `requirements:` + `  - READ-01` -> frontmatter.requirements = ["READ-01"] (array)
- `depends_on: []` -> frontmatter.depends_on = "[]" (string)
- `depends_on: ["05-01"]` -> frontmatter.depends_on = "[\"05-01\"]" (string)
- `depends_on:` + `  - 01-01` -> frontmatter.depends_on = ["01-01"] (array)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unit tests for all 4 checks + helpers</name>
  <files>test/smoke-plan-checker.test.js</files>
  <read_first>
    - bin/lib/plan-checker.js (the module to test -- read current implementation)
    - test/smoke-integrity.test.js (existing test patterns: describe/it blocks, assert.strict, require style)
    - references/plan-checker.md (rules spec -- source of truth for expected behavior)
    - .planning/phases/10-core-plan-checks/10-CONTEXT.md (severity decisions D-01 through D-08)
  </read_first>
  <behavior>
    - escapeRegex escapes hyphens: escapeRegex('CHECK-01') produces regex that matches literal 'CHECK-01'
    - parseRequirements handles undefined -> []
    - parseRequirements handles string "[]" -> []
    - parseRequirements handles string "[LIBR-02, LIBR-03]" -> ['LIBR-02', 'LIBR-03']
    - parseRequirements handles string "[\"05-01\"]" -> ['05-01']
    - parseRequirements handles array ['READ-01', 'READ-02'] -> ['READ-01', 'READ-02']
    - parseDependsOn handles same formats as parseRequirements
    - detectPlanFormat returns 'v1.0' for content with must_haves in frontmatter
    - detectPlanFormat returns 'v1.0' for content with tasks tag
    - detectPlanFormat returns 'v1.1' for content with Truths table | T1 |
    - detectPlanFormat returns 'unknown' for plain markdown
    - CHECK-01: empty requirementIds -> pass
    - CHECK-01: all IDs present in content -> pass
    - CHECK-01: one ID missing -> block with 1 issue
    - CHECK-01: multiple IDs missing -> block with N issues
    - CHECK-02 v1.0: valid tasks with files+action+verify -> pass
    - CHECK-02 v1.0: task missing files -> block
    - CHECK-02 v1.0: task missing action/behavior -> block
    - CHECK-02 v1.1: valid tasks with all fields -> pass
    - CHECK-02 v1.1: task missing Effort -> block
    - CHECK-02 v1.1: task missing Truths -> block
    - CHECK-02 v1.1: task missing Mo ta section -> block
    - CHECK-02 v1.1: task missing Tieu chi chap nhan section -> block
    - CHECK-02 v1.1: task missing Trang thai -> warn (not block)
    - CHECK-02 v1.1: summary table missing Truths column -> block
    - CHECK-03: no dependencies -> pass
    - CHECK-03 v1.1: valid task refs -> pass
    - CHECK-03 v1.1: invalid task ref -> block
    - CHECK-03 v1.1: circular dependency A->B->A -> block
    - CHECK-03 v1.0: auto pass (single plan, no task-level deps)
    - CHECK-04 v1.0: auto pass (no Truth refs in v1.0 tasks)
    - CHECK-04 v1.1: all Truths covered by tasks and vice versa -> pass
    - CHECK-04 v1.1: Truth T3 not in any task -> block
    - CHECK-04 v1.1: Task has Truth T5 not in Truths table -> warn
    - runAllChecks: all pass -> overall 'pass'
    - runAllChecks: one block -> overall 'block'
    - runAllChecks: one warn no block -> overall 'warn'
    - detectCycles: no cycle -> hasCycle false
    - detectCycles: simple cycle A->B->A -> hasCycle true, nodesInCycle contains both
    - detectCycles: self-loop A->A -> hasCycle true
  </behavior>
  <action>
Create `test/smoke-plan-checker.test.js` using project test conventions:

```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
```

Import all exports from `../bin/lib/plan-checker`.

**Helper function `makePlanV10(overrides)`** -- Generate minimal v1.0 PLAN.md content. This helper builds a string with YAML frontmatter (containing `must_haves:` with `truths:` nested under it) and XML task blocks. The frontmatter section is delimited by `'` + `---` + `\n` lines (using string concatenation to avoid parser issues). Include fields: phase, plan, type, wave, depends_on, requirements, must_haves.truths. Then append `<objective>`, `<tasks>` XML with provided task objects having name/files/action/verify/done fields.

**Helper function `makePlanV11(overrides)`** -- Generate minimal v1.1 PLAN.md content with a Truths table section: `| # | Su that | Cach kiem chung |` header row + data rows like `| T1 | description | verification |`. No YAML frontmatter needed for v1.1 format detection (the Truths table pattern `| T1 |` is the indicator).

**Helper function `makeTasksV11(overrides)`** -- Generate minimal v1.1 TASKS.md content with:
- Summary table header: `| # | Cong viec | Trang thai | Uu tien | Phu thuoc | Loai | Truths |`
- Summary rows per task
- Detail blocks per task: `## Task N: [name]` followed by metadata line (`> Trang thai: ... | ... | Effort: standard`), `> Files:`, `> Truths: [T1, T2]`, `### Mo ta` section, `### Tieu chi chap nhan` section

**Test structure** -- Use `describe` blocks matching each check:

1. `describe('helpers')` -- escapeRegex, parseRequirements, parseDependsOn, detectPlanFormat, detectCycles, findInvalidRefs
2. `describe('CHECK-01: requirementCoverage')` -- pass/block cases per behavior list
3. `describe('CHECK-02: taskCompleteness')` -- v1.0 pass/block, v1.1 pass/block/warn cases
4. `describe('CHECK-03: dependencyCorrectness')` -- v1.0 pass, v1.1 pass/block/cycle cases
5. `describe('CHECK-04: truthTaskCoverage')` -- v1.0 pass, v1.1 pass/block/warn cases
6. `describe('runAllChecks')` -- overall aggregation: all-pass, one-block, one-warn

Each `it()` test:
- Construct test content using helper functions
- Call the check function
- Assert `result.checkId` equals expected
- Assert `result.status` equals expected ('pass', 'block', or 'warn')
- Assert `result.issues.length` matches expected count
- For block/warn cases, assert issue messages contain relevant text (requirement ID, field name, etc.)

**Important:** When building test fixture strings containing frontmatter, use string concatenation to build the `'---\n...\n---\n'` boundaries (e.g., `const fm = ['', '---', 'phase: test', '---', ''].join('\n');`). This avoids the gsd-tools frontmatter parser detecting extra frontmatter blocks inside PLAN.md files that embed code examples.
  </action>
  <verify>
    <automated>node --test test/smoke-plan-checker.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test/smoke-plan-checker.test.js exists and contains `require('node:test')`
    - test/smoke-plan-checker.test.js contains `require('../bin/lib/plan-checker')`
    - test/smoke-plan-checker.test.js contains describe blocks for 'CHECK-01', 'CHECK-02', 'CHECK-03', 'CHECK-04', 'runAllChecks'
    - test/smoke-plan-checker.test.js has at least 25 `it(` test cases covering pass/block/warn for each check
    - `node --test test/smoke-plan-checker.test.js` exits with code 0
    - Test output shows 0 failing tests
  </acceptance_criteria>
  <done>All unit tests pass covering every check's pass/block/warn scenarios, helper function edge cases, and runAllChecks aggregation.</done>
</task>

<task type="auto">
  <name>Task 2: Add historical validation tests + fix false positives</name>
  <files>test/smoke-plan-checker.test.js, bin/lib/plan-checker.js</files>
  <read_first>
    - test/smoke-plan-checker.test.js (current test file -- append historical tests)
    - bin/lib/plan-checker.js (current module -- may need fixes for false positives)
    - .planning/phases/01-skill-structure-normalization/01-01-PLAN.md (v1.0 format -- YAML array requirements)
    - .planning/phases/05-effort-level-routing/05-01-PLAN.md (v1.0 format -- bracket notation requirements)
    - .planning/phases/07-library-fallback-and-version-detection/07-01-PLAN.md (v1.0 format -- multi-item bracket notation)
    - .planning/phases/03-prompt-prose-compression/03-01-PLAN.md (v1.0 format -- empty requirements)
    - references/plan-checker.md (rules spec -- severity decisions)
  </read_first>
  <action>
Add a `describe('Historical v1.0 plan validation (D-17)')` block to `test/smoke-plan-checker.test.js`.

**Step 1: Define plan list.** Create array `HISTORICAL_PLANS` with all 22 paths:
- `.planning/phases/01-skill-structure-normalization/01-01-PLAN.md`
- `.planning/phases/01-skill-structure-normalization/01-02-PLAN.md`
- `.planning/phases/01-skill-structure-normalization/01-03-PLAN.md`
- `.planning/phases/02-cross-skill-deduplication/02-01-PLAN.md`
- `.planning/phases/02-cross-skill-deduplication/02-02-PLAN.md`
- `.planning/phases/03-prompt-prose-compression/03-01-PLAN.md` through `03-06-PLAN.md`
- `.planning/phases/04-conditional-context-loading/04-01-PLAN.md`, `04-02-PLAN.md`
- `.planning/phases/05-effort-level-routing/05-01-PLAN.md`, `05-02-PLAN.md`
- `.planning/phases/06-context7-standardization/06-01-PLAN.md`, `06-02-PLAN.md`
- `.planning/phases/07-library-fallback-and-version-detection/07-01-PLAN.md`
- `.planning/phases/08-wave-based-parallel-execution/08-01-PLAN.md`, `08-02-PLAN.md`
- `.planning/phases/09-converter-pipeline-optimization/09-01-PLAN.md`, `09-02-PLAN.md`

Use `const ROOT = path.resolve(__dirname, '..');` for path resolution (same pattern as smoke-integrity.test.js).

**Step 2: Individual plan tests.** For each plan in HISTORICAL_PLANS, create an `it()` that:
- Reads file with `fs.readFileSync(path.join(ROOT, planPath), 'utf8')`
- Calls `runAllChecks({ planContent, tasksContent: null, requirementIds: [] })`
- Asserts: `result.checks.filter(c => c.status === 'block').length === 0`
- Error message includes plan path and which check produced the block

**Step 3: Summary gate test.** Add `it('all 22 historical plans produce zero block results (D-17 gate)')` that loops all plans and asserts total blocks = 0.

**Step 4: Format detection test.** Add `it('all 22 historical plans detected as v1.0 format')` that asserts `detectPlanFormat(content) === 'v1.0'` for each plan.

**Step 5: Run and fix.** Execute `node --test test/smoke-plan-checker.test.js`. If any false positives:
- Identify which check and which plan triggered the block
- Fix the parsing in `bin/lib/plan-checker.js` (common issues from research):
  - `parseMustHavesTruths` regex indentation mismatch: adjust to handle 2-space or 4-space indent before `- "`
  - `parseTasksV10` missing task XML variations: some tasks use `<feature>` instead of `<task>`, or have `tdd="true"` attributes
  - `detectPlanFormat` edge case: plan has both `must_haves:` AND a Truths table pattern in a comment
- Add comment in plan-checker.js for each fix: `// Fix: [planFile] triggered false positive because [reason]`
- Re-run tests until all pass

**Step 6: Full suite.** Run `npm test` to confirm new tests integrate with existing suite.
  </action>
  <verify>
    <automated>node --test test/smoke-plan-checker.test.js 2>&1 | tail -5 && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test/smoke-plan-checker.test.js contains "Historical v1.0 plan validation" describe block
    - test/smoke-plan-checker.test.js references all 22 PLAN.md file paths from phases 01 through 09
    - test/smoke-plan-checker.test.js contains "D-17" in test description or assertion message
    - `node --test test/smoke-plan-checker.test.js` exits with code 0 (zero failures)
    - `npm test` exits with code 0 (all tests including existing ones pass)
    - Historical validation tests assert `blockChecks.length === 0` for every plan
    - All 22 plans detected as 'v1.0' format
  </acceptance_criteria>
  <done>All 22 historical v1.0 plans pass through runAllChecks with zero false positive blocks. npm test passes with the complete test suite including new plan checker tests.</done>
</task>

</tasks>

<verification>
- `node --test test/smoke-plan-checker.test.js` shows 0 failures with 30+ tests
- `npm test` shows 0 failures across all test files
- All 22 historical plans produce `overall: 'pass'` or `overall: 'warn'` (never 'block')
- All 22 plans detected as 'v1.0' format
</verification>

<success_criteria>
1. Unit tests cover all 4 checks (pass/block/warn) + helpers + orchestrator
2. Historical validation covers all 22 v1.0 PLAN.md files
3. Zero false positive blocks on historical plans (D-17 acceptance gate)
4. npm test passes with the complete test suite
5. Any parsing fixes needed for historical compatibility are documented in plan-checker.js
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-plan-checks/10-02-SUMMARY.md`
</output>
