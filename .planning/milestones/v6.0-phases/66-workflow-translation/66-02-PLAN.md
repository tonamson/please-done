---
phase: 66-workflow-translation
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - workflows/write-code.md
  - workflows/fix-bug.md
  - workflows/fix-bug-v1.5.md
  - workflows/plan.md
  - workflows/new-milestone.md
  - workflows/audit.md
  - test/smoke-integrity.test.js
autonomous: true
user_setup: []
requirements:
  - TRANS-03
must_haves:
  truths:
    - "All 13 workflow files contain zero Vietnamese text (diacritical and non-diacritical)"
    - "Step numbering uses 'Step X' in all 13 workflow files"
    - "XML tags, cross-references, and placeholders are preserved in all 6 files"
    - "smoke-integrity.test.js passes with all assertions matching English content"
  artifacts:
    - path: "workflows/write-code.md"
      provides: "English translation of write-code workflow"
    - path: "workflows/fix-bug.md"
      provides: "English translation of fix-bug workflow"
    - path: "workflows/fix-bug-v1.5.md"
      provides: "English translation of fix-bug-v1.5 workflow"
    - path: "workflows/plan.md"
      provides: "English translation of plan workflow"
    - path: "workflows/new-milestone.md"
      provides: "English translation of new-milestone workflow"
    - path: "workflows/audit.md"
      provides: "English translation of audit workflow"
  key_links:
    - from: "workflows/*.md"
      to: "test/smoke-integrity.test.js"
      via: "Test assertions match workflow content"
      pattern: "workflows"
    - from: "commands/pd/*.md"
      to: "workflows/*.md"
      via: "@workflows/name.md inline references"
      pattern: "@workflows/"
---

<objective>
Translate 6 larger workflow files to English and finalize all test assertions.

Purpose: Complete TRANS-03. These 6 files (2,552 lines total) are the heaviest Vietnamese content in the project.
Output: All 13 workflow files in English + all test assertions updated, full smoke-integrity suite passing.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-workflow-translation/66-CONTEXT.md
@.planning/phases/66-workflow-translation/66-RESEARCH.md
@.planning/phases/66-workflow-translation/66-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate 6 larger workflow files to English (per D-01, D-02, D-04, D-05)</name>
  <files>workflows/write-code.md, workflows/fix-bug.md, workflows/fix-bug-v1.5.md, workflows/plan.md, workflows/new-milestone.md, workflows/audit.md</files>
  <read_first>
    - workflows/write-code.md
    - workflows/fix-bug.md
    - workflows/fix-bug-v1.5.md
    - workflows/plan.md
    - workflows/new-milestone.md
    - workflows/audit.md
    - .planning/phases/66-workflow-translation/66-CONTEXT.md
    - .planning/phases/66-workflow-translation/66-RESEARCH.md
  </read_first>
  <action>
    For each of the 6 workflow files, translate ALL Vietnamese prose content to English:

    1. **Step numbering (per D-04):** Replace every "Bước X" with "Step X". Also replace non-diacritical "Buoc X" with "Step X". Preserve sub-step patterns: "Bước 1.7" → "Step 1.7", "Bước 5b.1" → "Step 5b.1".

    2. **Prose translation:** Translate all Vietnamese descriptive text, instructions, comments, and section headings to English. Use standard terminology (per D-05): `phase`, `milestone`, `verification`, `requirements`, `success criteria`.

    3. **Behavioral keywords:** Translate with matching force level:
       - "DỪNG" → "STOP"
       - "BẮT BUỘC" → "REQUIRED"
       - "KHÔNG" → "DO NOT"
       - "CHỈ" → "ONLY"

    4. **Preserve verbatim (per D-02, D-03, D-10):**
       - All XML tags: `<process>`, `<purpose>`, `<rules>`, `<required_reading>`, `<conditional_reading>`, `<research_injection>`, `<context>`, `<verification_loop>`, `<success_criteria>`, etc.
       - All frontmatter keys and values
       - All `@workflows/name.md`, `@references/name.md`, `@templates/name.md` path references
       - All placeholders: `$ARGUMENTS`, `$PHASE`, `{resolved_model}`, `{task_count}`, etc.
       - All command names: `/pd:*`, `pd:*`
       - All code blocks, inline code, regex patterns, grep commands
       - All file paths and directory references
       - All variable names, function names, agent names (pd-bug-janitor, pd-code-detective, etc.)

    5. **CRITICAL — Non-diacritical Vietnamese in fix-bug.md (per Research Pitfall 1):**
       `fix-bug.md` has 408 lines of Vietnamese WITHOUT diacritics (e.g., "Kiem tra che do hoat dong", "Buoc", "Thieu"). The diacritic grep will show 0 matches for this file, but it still needs full translation. Read the entire file and translate ALL Vietnamese content regardless of diacritics.

    6. **File-specific notes:**
       - `write-code.md` (~471 lines): Contains "DỪNG" (→ "STOP"), "KHÔNG dump toàn bộ PLAN" (→ "DO NOT dump the entire PLAN"), and "> Files: giao nhau" (→ "> Files: cross-reference"). Also has effort-model routing table — preserve table structure.
       - `fix-bug.md` (~408 lines): Non-diacritical Vietnamese throughout. "Kiem tra che do hoat dong" → "Check operating mode". Contains agent references (pd-bug-janitor, etc.) — preserve these.
       - `fix-bug-v1.5.md` (~438 lines): Dense procedural steps with evidence collection patterns.
       - `plan.md` (~524 lines): Largest file. Has classification signals table ("Files sửa/tạo | 1-2 | 3-4 | 5+") — translate cell text but preserve table structure. "Files sửa/tạo" → "Files modified/created". Contains effort level section.
       - `new-milestone.md` (~404 lines): Milestone lifecycle flow with structured steps.
       - `audit.md` (~307 lines): Security audit workflow with OWASP references — preserve technical terms.

    7. Do NOT rename files, change paths, or expand scope beyond these 6 files.

  </action>
  <verify>
    <automated>rg -c '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/write-code.md workflows/fix-bug.md workflows/fix-bug-v1.5.md workflows/plan.md workflows/new-milestone.md workflows/audit.md && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - `rg '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/write-code.md` returns empty
    - `rg '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/fix-bug.md` returns empty
    - `rg '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/fix-bug-v1.5.md` returns empty
    - `rg '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/plan.md` returns empty
    - `rg '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏõôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/new-milestone.md` returns empty
    - `rg '[àáạảãâầấậẩẫăằắặẳẵđèéẹẻẽêềếệểễìíịỉĩòóọỏ�ôồốộổỗơờớợởỡùúụủũưừứựửữỳýỵỷỹ]' workflows/audit.md` returns empty
    - `grep -c 'Step [0-9]' workflows/write-code.md` returns non-zero
    - `grep -c 'Bước' workflows/write-code.md` returns 0
    - `grep -c 'Kiem tra' workflows/fix-bug.md` returns 0 (non-diacritical Vietnamese removed)
    - `grep 'Check operating mode' workflows/fix-bug.md` returns a match
    - `grep 'STOP' workflows/write-code.md` returns a match (was "DỪNG")
    - `grep 'DO NOT dump' workflows/write-code.md` returns a match (was "KHÔNG dump")
    - All XML tags (`<process>`, `<purpose>`, `<rules>`, etc.) still present in each file
    - All `@workflows/`, `@references/`, `@templates/` path refs unchanged
    - `grep 'Files modified/created' workflows/plan.md` returns a match (was "Files sửa/tạo")
  </acceptance_criteria>
  <done>6 larger workflow files fully translated to English including non-diacritical Vietnamese, structure and behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize smoke-integrity.test.js assertions for all translated workflows (per D-08, D-09, D-11, D-12)</name>
  <files>test/smoke-integrity.test.js</files>
  <read_first>
    - test/smoke-integrity.test.js
    - workflows/write-code.md
    - workflows/fix-bug.md
    - workflows/plan.md
    - .planning/phases/66-workflow-translation/66-RESEARCH.md
  </read_first>
  <action>
    Update remaining test assertions that regex-match Vietnamese strings in the 6 translated workflow files. Also finalize the temporary regex from Plan 01. Per D-09, defer broader test string migration (test names, error messages) to Phase 69.

    **Line ~115 — inline workflow step regex (finalize):**
    Current (after Plan 01): `/<process>[\s\S]*(Step|Bước|Buoc) [0-9]+[\s\S]*<\/process>/`
    Change to: `/<process>[\s\S]*(Step) [0-9]+[\s\S]*<\/process>/`
    Reason: All 13 workflows now use "Step X". Remove Vietnamese alternation.

    **Line ~693 — fix-bug.md operating mode assertion:**
    Current: `/Kiem tra che do hoat dong/`
    Change to: `/Check operating mode/`
    Reason: fix-bug.md translated "Kiem tra che do hoat dong" → "Check operating mode".

    **Line ~1144 — write-code.md post-wave build check:**
    Current: `/build fail.*D[UƯ]NG|D[UƯ]NG.*build fail/is`
    Change to: `/build fail.*STOP|STOP.*build fail/is`
    Reason: write-code.md translated "DỪNG" → "STOP".

    **Line ~1156 — write-code.md agent context minimization:**
    Current: `/KH[OÔ]NG dump to[aà]n b[oộ] PLAN/i`
    Change to: `/DO NOT dump (the )?entire PLAN/i`
    Reason: write-code.md translated "KHÔNG dump toàn bộ PLAN" → "DO NOT dump the entire PLAN".

    **Line ~1168 — write-code.md Files cross-reference:**
    Current: `/> Files:.*cross-reference|giao nhau/i`
    Change to: `/> Files:.*cross-reference/i`
    Reason: write-code.md translated "giao nhau" → "cross-reference". Remove Vietnamese alternation.

    **DO NOT change:**
    - Test names (it/describe strings) — defer to Phase 69 per D-09
    - Assertion failure messages (e.g., "thieu") — defer to Phase 69 per D-09
    - Assertions for references, templates, or other non-workflow files

    After updating, run `node --test test/smoke-integrity.test.js` to confirm all tests pass.
    Then run `node --test test/smoke-*.test.js` as the phase gate (per VALIDATION.md).

  </action>
  <verify>
    <automated>node --test test/smoke-integrity.test.js && node --test test/smoke-*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'Step) \[0-9\]' test/smoke-integrity.test.js` shows finalized regex without Vietnamese alternation
    - `grep 'Bước|Buoc' test/smoke-integrity.test.js` returns zero matches in regex patterns (only in test names/error messages which are deferred)
    - `grep 'Check operating mode' test/smoke-integrity.test.js` returns a match
    - `grep 'Kiem tra che do hoat dong' test/smoke-integrity.test.js` returns empty
    - `grep 'STOP.*build fail\|build fail.*STOP' test/smoke-integrity.test.js` returns a match
    - `grep 'DỪNG' test/smoke-integrity.test.js` returns empty
    - `grep 'DO NOT dump' test/smoke-integrity.test.js` returns a match
    - `grep 'KHÔNG dump' test/smoke-integrity.test.js` returns empty
    - `grep 'giao nhau' test/smoke-integrity.test.js` returns empty
    - `node --test test/smoke-integrity.test.js` exits with code 0
    - `node --test test/smoke-*.test.js` exits with code 0
  </acceptance_criteria>
  <done>All test assertions updated to match English workflow content. Full smoke test suite passes. TRANS-03 complete.</done>
</task>

</tasks>

<verification>
- Vietnamese diacritic grep across ALL 13 workflow files returns zero matches
- `grep -rc 'Bước\|Buoc' workflows/*.md` returns 0 for every file
- `node --test test/smoke-integrity.test.js` passes
- `node --test test/smoke-*.test.js` passes (phase gate)
</verification>

<success_criteria>

- All 13 workflow files translated to English (TRANS-03 complete)
- Step numbering follows "Step X" convention in all 13 files
- Non-diacritical Vietnamese (fix-bug.md) also translated
- XML tags, cross-references, placeholders all preserved
- Full smoke test suite passes with updated assertions
  </success_criteria>

<output>
After completion, create `.planning/phases/66-workflow-translation/66-02-SUMMARY.md`
</output>
