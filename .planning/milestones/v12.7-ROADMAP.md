# Milestone v12.7: Security Audit Restoration

**Status:** ✅ SHIPPED 2026-04-10
**Phases:** 156
**Total Plans:** 1

## Overview

Restored `pd:audit` as a full OWASP/PTES security audit skill with auto-detect standalone/integrated mode. The skill was previously replaced with a discussion context trail viewer in commit `ac201af` (Apr 7) and the workflow file was deleted as "orphaned" in `fbbc74f`. This milestone restores the original security audit capability so it can run without requiring `pd:write-code` to have been run first.

## Phases

### Phase 156: Restore pd:audit Security Audit Skill

**Goal**: Restore pd:audit as OWASP/PTES security audit with auto-detect standalone/integrated mode — so it runs without requiring write-code artifacts.
**Depends on**: Phase 155
**Plans**: 1 plan

Plans:

- [x] 156-01: Restore audit.md + workflows/audit.md, update smoke test, regenerate snapshots

**Details:**
- Restored `commands/pd/audit.md` from git commit `0dd6cca` (OWASP/PTES, 13 scanners, 2/wave)
- Restored `workflows/audit.md` from git commit `5940c8b` (10-step pipeline with PTES recon + OSINT)
- Auto-detect mode: standalone (no `.planning/PROJECT.md`) → skip `guard-context.md`, run immediately; integrated (has PROJECT.md) → run all 3 guards
- Removed `audit` from `ALLOWED_NO_WORKFLOW` smoke-test whitelist in `smoke-integrity.test.js`
- Regenerated 80 snapshots (4 platforms × 20 skills)
- All 56 integrity tests pass; 21 pre-existing env failures unchanged

---

## Milestone Summary

**Key Deliverables:**
- `pd:audit` restored as full OWASP/PTES security audit (13 scanners, PTES recon, OSINT)
- Standalone mode: works on any directory without pd workflow prerequisites
- Integrated mode: includes milestone context when `.planning/PROJECT.md` exists
- `workflows/audit.md` restored (354 lines, 10-step execution pipeline)

**Root Cause:**
- Security audit was accidentally replaced in v12.2 (Phase 142) when discussion audit trail was added as a separate feature — the user's intent was to ADD discussion trail, not REPLACE the security audit
