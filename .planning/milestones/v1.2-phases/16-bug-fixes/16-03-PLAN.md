---
phase: 16-bug-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
files_modified:
  - commands/pd/plan.md
  - commands/pd/write-code.md
  - commands/pd/fix-bug.md
  - commands/pd/test.md
  - commands/pd/complete-milestone.md
  - commands/pd/new-milestone.md
  - commands/pd/fetch-doc.md
  - commands/pd/update.md
  - references/plan-checker.md
  - references/conventions.md
  - workflows/conventions.md
autonomous: true
requirements:
  - BFIX-01

must_haves:
  truths:
    - "plan.md skill has @references/plan-checker.md (optional) in execution_context"
    - "4 skill files have @references/context7-pipeline.md (optional) in execution_context"
    - "write-code.md skill has @templates/progress.md (optional) in execution_context"
    - "complete-milestone.md skill has @templates/verification-report.md (optional) in execution_context"
    - "4 files have intentional pattern audit comments"
    - "plan-checker.md khong con hardcoded version string"
    - "conventions.md co note ve write-code specific behavior"
  artifacts:
    - path: "commands/pd/plan.md"
      provides: "Updated execution_context with plan-checker.md + context7-pipeline.md refs"
      contains: "@references/plan-checker.md"
    - path: "commands/pd/write-code.md"
      provides: "Updated execution_context with progress.md + context7-pipeline.md refs + I4 audit comment"
      contains: "@templates/progress.md"
    - path: "commands/pd/fix-bug.md"
      provides: "Updated execution_context with context7-pipeline.md ref"
      contains: "@references/context7-pipeline.md"
    - path: "commands/pd/test.md"
      provides: "Updated execution_context with context7-pipeline.md ref"
      contains: "@references/context7-pipeline.md"
    - path: "commands/pd/complete-milestone.md"
      provides: "Updated execution_context with verification-report.md ref"
      contains: "@templates/verification-report.md"
    - path: "references/plan-checker.md"
      provides: "Removed hardcoded version string"
    - path: "references/conventions.md"
      provides: "write-code specific note on task status rule"
      contains: "Ap dung cho"
  key_links:
    - from: "commands/pd/plan.md"
      to: "references/plan-checker.md"
      via: "execution_context optional reference"
      pattern: "@references/plan-checker.md"
    - from: "commands/pd/write-code.md"
      to: "references/context7-pipeline.md"
      via: "execution_context optional reference"
      pattern: "@references/context7-pipeline.md"
---

<objective>
Wire missing references into skill execution_context sections (W1, W2, W4, W5), add intentional pattern audit comments (I1, I2, I4, I7), fix plan-checker.md hardcoded version (I3), and add conventions.md write-code note (V6).

Purpose: Complete all skill/reference wiring and documentation fixes identified in Phase 14 audit.
Output: Updated skill files with correct execution_context references, audit trail comments on intentional patterns, clean reference docs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-bug-fixes/16-CONTEXT.md
@.planning/phases/16-bug-fixes/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire missing references into skill execution_context sections (W1, W2, W4, W5)</name>
  <files>commands/pd/plan.md, commands/pd/write-code.md, commands/pd/fix-bug.md, commands/pd/test.md, commands/pd/complete-milestone.md</files>
  <read_first>
    - commands/pd/plan.md (lines 40-60 -- execution_context section)
    - commands/pd/write-code.md (lines 45-60 -- execution_context section)
    - commands/pd/fix-bug.md (lines 40-55 -- execution_context section)
    - commands/pd/test.md (lines 40-55 -- execution_context section)
    - commands/pd/complete-milestone.md (lines 35-50 -- execution_context section)
  </read_first>
  <action>
All changes below add `(optional)` references to the `execution_context:` section of each skill file. Find the existing list of `@` references and append the new ones at the end of the list.

**commands/pd/plan.md (W2 per D-22, W4 per D-24):**
Add these 2 lines to the execution_context section:
```
  - @references/plan-checker.md (optional)
  - @references/context7-pipeline.md (optional)
```

**commands/pd/write-code.md (W1 per D-21, W4 per D-24):**
Add these 2 lines to the execution_context section:
```
  - @templates/progress.md (optional)
  - @references/context7-pipeline.md (optional)
```

**commands/pd/fix-bug.md (W4 per D-24):**
Add this 1 line to the execution_context section:
```
  - @references/context7-pipeline.md (optional)
```

**commands/pd/test.md (W4 per D-24):**
Add this 1 line to the execution_context section:
```
  - @references/context7-pipeline.md (optional)
```

**commands/pd/complete-milestone.md (W5 per D-25):**
Add this 1 line to the execution_context section:
```
  - @templates/verification-report.md (optional)
```

IMPORTANT: These are `commands/pd/` files which are SKILL files (not workflow files). Changes to these files WILL affect converter snapshots and need regeneration in Plan 16-04.
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && grep -c "plan-checker.md" commands/pd/plan.md && grep -c "context7-pipeline.md" commands/pd/plan.md && grep -c "progress.md" commands/pd/write-code.md && grep -c "context7-pipeline.md" commands/pd/write-code.md && grep -c "context7-pipeline.md" commands/pd/fix-bug.md && grep -c "context7-pipeline.md" commands/pd/test.md && grep -c "verification-report.md" commands/pd/complete-milestone.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@references/plan-checker.md" commands/pd/plan.md` returns at least 1 match
    - `grep "@references/context7-pipeline.md" commands/pd/plan.md` returns at least 1 match
    - `grep "@templates/progress.md" commands/pd/write-code.md` returns at least 1 match
    - `grep "@references/context7-pipeline.md" commands/pd/write-code.md` returns at least 1 match
    - `grep "@references/context7-pipeline.md" commands/pd/fix-bug.md` returns at least 1 match
    - `grep "@references/context7-pipeline.md" commands/pd/test.md` returns at least 1 match
    - `grep "@templates/verification-report.md" commands/pd/complete-milestone.md` returns at least 1 match
    - All 5 files remain valid markdown with properly indented execution_context lists
  </acceptance_criteria>
  <done>W1 (progress.md wired to write-code.md), W2 (plan-checker.md wired to plan.md), W4 (context7-pipeline.md wired to 4 skills), W5 (verification-report.md wired to complete-milestone.md) all resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Add intentional pattern comments (I1, I2, I4, I7), fix I3, V6</name>
  <files>commands/pd/fetch-doc.md, commands/pd/update.md, commands/pd/write-code.md, workflows/conventions.md, references/plan-checker.md, references/conventions.md</files>
  <read_first>
    - commands/pd/fetch-doc.md (line 33 area -- execution_context "Khong co" statement)
    - commands/pd/update.md (line 35 area -- execution_context "Khong co" statement)
    - commands/pd/write-code.md (line 13 area -- Agent tool in allowed-tools)
    - workflows/conventions.md (line 1 -- beginning of file)
    - references/plan-checker.md (lines 295-300 -- version string at footer)
    - references/conventions.md (line 18 area -- task status rule)
  </read_first>
  <action>
**Intentional pattern audit comments (D-04):**

Use HTML comment format for markdown files. Add the comment on the line BEFORE or AFTER the pattern being documented.

**commands/pd/fetch-doc.md (I1):**
Find the line near 33 that says something like `Khong co -- skill nay xu ly truc tiep, khong dung workflow rieng.` Add immediately after that line:
```
<!-- Audit 2026-03-23: Intentional -- self-contained skill without workflow (lightweight/utility pattern). See Phase 14 Audit I1. -->
```

**commands/pd/update.md (I2):**
Find the line near 35 that says something like `Khong co -- skill nay xu ly truc tiep, khong dung workflow rieng.` Add immediately after that line:
```
<!-- Audit 2026-03-23: Intentional -- self-contained skill without workflow (lightweight/utility pattern). See Phase 14 Audit I2. -->
```

**commands/pd/write-code.md (I4):**
Find line 13 area where `Agent` is listed in allowed-tools. Add immediately after the allowed-tools line:
```
<!-- Audit 2026-03-23: Intentional -- Agent tool required for --parallel mode multi-agent execution. See Phase 14 Audit I4. -->
```

**workflows/conventions.md (I7):**
Add at line 1 (very top of file), BEFORE any existing content:
```
<!-- Audit 2026-03-23: Intentional -- simple conventions workflow without numbered steps (data-driven, not procedural). See Phase 14 Audit I7. -->
```

**references/plan-checker.md -- I3 (per D-02):**
Find the footer area (around lines 296-297) that contains:
```
*Plan Checker Rules v1.1*
*Updated: 23_03_2026*
```
Remove both lines entirely. The version is tracked by git, not by a manual string.

**references/conventions.md -- V6:**
Find line 18 area where the task status rule says something like "Danh dau hoan tat TRUOC commit. Commit fail -> revert, sua roi thu lai." Add a note right after:
```
(Ap dung cho /pd:write-code. Cac workflow khac (fix-bug) co logic rieng -- vd fix-bug update TASKS.md sau user confirm.)
```
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && grep -c "Audit 2026-03-23" commands/pd/fetch-doc.md && grep -c "Audit 2026-03-23" commands/pd/update.md && grep -c "Audit 2026-03-23" commands/pd/write-code.md && grep -c "Audit 2026-03-23" workflows/conventions.md && grep -c "Plan Checker Rules v1.1" references/plan-checker.md && grep -c "Ap dung cho" references/conventions.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Audit 2026-03-23" commands/pd/fetch-doc.md` returns 1 match (I1 comment)
    - `grep "Audit 2026-03-23" commands/pd/update.md` returns 1 match (I2 comment)
    - `grep "Audit 2026-03-23" commands/pd/write-code.md` returns at least 1 match (I4 comment)
    - `grep "Audit 2026-03-23" workflows/conventions.md` returns 1 match (I7 comment)
    - `grep -c "Plan Checker Rules v1.1" references/plan-checker.md` returns 0 (version string removed)
    - `grep "Ap dung cho" references/conventions.md` returns at least 1 match (V6 note)
  </acceptance_criteria>
  <done>I1, I2, I4, I7 documented with audit comments. I3 resolved (hardcoded version removed). V6 resolved (conventions.md clarified as write-code specific).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 5 skill files have updated execution_context sections
2. 4 files have audit trail comments
3. plan-checker.md has no hardcoded version
4. conventions.md has write-code specific note
5. `node --test test/smoke-*.test.js` -- WARNING: snapshot tests will FAIL because commands/pd/ files changed. This is EXPECTED and will be fixed in Plan 16-04.
</verification>

<success_criteria>
- W1, W2, W4, W5 resolved: all missing references wired
- I1, I2, I4, I7 resolved: intentional patterns documented
- I3 resolved: hardcoded version string removed
- V6 resolved: conventions.md clarified
- All skill/reference changes ready for snapshot regeneration in Plan 16-04
</success_criteria>

<output>
After completion, create `.planning/phases/16-bug-fixes/16-03-SUMMARY.md`
</output>
