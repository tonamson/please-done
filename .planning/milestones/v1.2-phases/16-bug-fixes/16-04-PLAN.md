---
phase: 16-bug-fixes
plan: 04
type: execute
wave: 3
depends_on:
  - 16-01
  - 16-03
files_modified:
  - test/snapshots/
autonomous: true
requirements:
  - BFIX-02

must_haves:
  truths:
    - "48/48 converter snapshots match source files after all Phase 16 code changes"
    - "node --test test/smoke-snapshot.test.js passes with 48/48"
    - "Full test suite (smoke-*.test.js) passes with 0 failures"
    - "No regression in existing 443+ tests"
  artifacts:
    - path: "test/snapshots/"
      provides: "Regenerated snapshots matching updated source files"
  key_links:
    - from: "test/snapshots/"
      to: "bin/lib/converters/codex.js"
      via: "Converter output reflects codex.js UTF-8 fix"
      pattern: "codex"
    - from: "test/snapshots/"
      to: "commands/pd/*.md"
      via: "Converter output reflects skill file reference changes"
      pattern: "snapshots"
---

<objective>
Regenerate all 48 converter snapshots after Phase 16 code changes (I5 codex.js UTF-8 fix + skill file reference wiring), then run full test suite to verify zero regressions.

Purpose: Satisfy BFIX-02 requirement -- snapshots in sync with source files. This is the final verification gate for Phase 16 per D-18 (one-shot regeneration AFTER all fixes).
Output: Updated test/snapshots/ directory with all 48 snapshots matching current source, full green test suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-bug-fixes/16-CONTEXT.md
@.planning/phases/16-bug-fixes/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate snapshots and verify full test suite (D-18, D-19)</name>
  <files>test/snapshots/</files>
  <read_first>
    - test/generate-snapshots.js (understand regeneration script)
    - test/smoke-snapshot.test.js (understand snapshot verification)
  </read_first>
  <action>
Per D-18 (one-shot regeneration) and D-19 (procedure):

**Step 1: Regenerate all 48 snapshots.**
Run: `node test/generate-snapshots.js`
This regenerates all 48 snapshots (4 platforms x 12 skills) from current source files.

**Step 2: Review snapshot diffs.**
Run: `git diff test/snapshots/` to see what changed.
Expected changes:
- Codex snapshots: Vietnamese UTF-8 characters replacing Unicode escapes (from I5 fix in codex.js)
- All platform snapshots for affected skills: Updated execution_context sections (from W1, W2, W4, W5 reference wiring in Plan 16-03)
- Skill snapshots for fetch-doc, update, write-code: Audit comments (from I1, I2, I4 in Plan 16-03)
- new-milestone skill snapshots: Guard error message (from W3 in Plan 16-02)

UNEXPECTED changes (investigate if seen):
- Snapshots for skills/platforms NOT affected by Phase 16 changes
- Content changes unrelated to Phase 16 fixes

**Step 3: Run snapshot smoke tests.**
Run: `node --test test/smoke-snapshot.test.js`
All 48 tests must pass.

**Step 4: Run full test suite.**
Run: `node --test test/smoke-*.test.js`
All tests must pass. This is the Phase 16 gate check -- verifies zero regressions across:
- smoke-utils.test.js (utils.js dead export removal)
- smoke-plan-checker.test.js (plan-checker.js comment update, library unchanged)
- smoke-converters.test.js (codex.js UTF-8 fix)
- smoke-snapshot.test.js (regenerated snapshots)
- All other smoke tests (unchanged code)

**Step 5: Stage and note files for commit.**
The executor should stage the snapshot changes (test/snapshots/) together with any previous unstaged changes. Per D-19, code + snapshots should be committed together.
  </action>
  <verify>
    <automated>cd /Volumes/Code/Nodejs/please-done && node test/generate-snapshots.js && node --test test/smoke-snapshot.test.js && node --test test/smoke-*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node test/generate-snapshots.js` exits with code 0
    - `node --test test/smoke-snapshot.test.js` reports 48/48 pass, 0 fail
    - `node --test test/smoke-*.test.js` reports 0 fail across all test files
    - `git diff --stat test/snapshots/` shows changes only in snapshots for skills/platforms affected by Phase 16 fixes
  </acceptance_criteria>
  <done>48/48 snapshots regenerated and verified. Full test suite passes with 0 failures. BFIX-02 satisfied. Phase 16 complete -- all bugs fixed, no regressions.</done>
</task>

</tasks>

<verification>
Final Phase 16 gate:
1. `node --test test/smoke-*.test.js` -- ALL pass, 0 fail
2. `node --test test/smoke-snapshot.test.js` -- 48/48 pass
3. `node bin/plan-check.js` -- exits with usage message (CLI works)
4. No unexpected changes in `git diff` outside of planned fix scope
</verification>

<success_criteria>
- BFIX-02 satisfied: 48/48 snapshots in sync after all Phase 16 changes
- Zero test regressions: full suite green
- All Phase 16 fixes validated end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/16-bug-fixes/16-04-SUMMARY.md`
</output>
