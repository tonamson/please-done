---
phase: 133
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/125-command-reference-fixes/125-VERIFICATION.md
  - .planning/phases/126-test-infrastructure-c-02/126-VERIFICATION.md
autonomous: true
requirements:
  - C-01
  - C-02

must_haves:
  truths:
    - "Phase 125 VERIFICATION.md exists with gsd-verifier format"
    - "Phase 126 VERIFICATION.md exists with gsd-verifier format"
    - "Both verification files contain Observable Truths tables with verified evidence"
  artifacts:
    - path: .planning/phases/125-command-reference-fixes/125-VERIFICATION.md
      provides: Phase 125 verification documentation
      contains: "phase: 125"
    - path: .planning/phases/126-test-infrastructure-c-02/126-VERIFICATION.md
      provides: Phase 126 verification documentation
      contains: "phase: 126"
---

<objective>
Add VERIFICATION.md files for Phases 125 and 126 using gsd-verifier format

Purpose: Create audit documentation for completed phases missing verification files
Output: Two VERIFICATION.md files following established patterns from Phases 127 and 132
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/133-add-missing-verification/133-CONTEXT.md
@.planning/phases/125-command-reference-fixes/125-01-PLAN.md
@.planning/phases/125-command-reference-fixes/125-01-SUMMARY.md
@.planning/phases/126-test-infrastructure-c-02/126-01-PLAN.md
@.planning/phases/126-test-infrastructure-c-02/126-01-SUMMARY.md
@.planning/phases/127-documentation-updates-c-04-h-03/127-VERIFICATION.md
@.planning/phases/132-complete-h-01-catch-block/132-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 125 VERIFICATION.md</name>
  <files>.planning/phases/125-command-reference-fixes/125-VERIFICATION.md</files>
  <action>
Create 125-VERIFICATION.md following the 127-VERIFICATION.md documentation verification pattern (per D-07).

**Template Structure (gsd-verifier format):**
1. YAML frontmatter: phase, verified (current timestamp), status, score, gaps, deferred
2. Goal Achievement section with Observable Truths table (2-3 truths)
3. Required Artifacts table
4. Behavioral Spot-Checks table (2-3 checks)
5. Requirements Coverage table
6. Human Verification Required assessment
7. Gaps Summary

**Verification Actions to Execute:**
1. Run: `grep -c "pd:scan" CLAUDE.md` — verify pd:scan references exist
2. Run: `grep -c "pd:map-codebase" CLAUDE.md || echo "0"` — verify zero broken references
3. Run: `grep -c "pd:verify" CLAUDE.md || echo "0"` — verify zero broken references
4. Run: `grep -oP '/pd:[a-z-]+' CLAUDE.md | sort -u` — verify all /pd: commands are valid

**Content to Document:**
- Observable Truth: All /pd: command references are correct (evidence from SUMMARY: 11 valid commands)
- Observable Truth: No broken references remain (pd:map-codebase replaced, pd:verify replaced)
- Observable Truth: CLAUDE.md command reference is internally consistent
- Required Artifact: CLAUDE.md modified with corrected references
- Behavioral Spot-Check: grep verification showing zero broken references
- Requirement: C-01 satisfied (all 5 broken references from plan fixed per SUMMARY)
- Human Verification: None required (all checks programmatic)

**File path:** .planning/phases/125-command-reference-fixes/125-VERIFICATION.md
  </action>
  <verify>
    <automated>test -f .planning/phases/125-command-reference-fixes/125-VERIFICATION.md && echo "PASS: Phase 125 VERIFICATION.md exists"</automated>
  </verify>
  <done>125-VERIFICATION.md created with complete gsd-verifier format documentation</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 126 VERIFICATION.md</name>
  <files>.planning/phases/126-test-infrastructure-c-02/126-VERIFICATION.md</files>
  <action>
Create 126-VERIFICATION.md following the 132-VERIFICATION.md code changes verification pattern (per D-08).

**Template Structure (gsd-verifier format):**
1. YAML frontmatter: phase, verified (current timestamp), status, score, gaps, deferred
2. Goal Achievement section with Observable Truths table (2-3 truths)
3. Required Artifacts table
4. Key Link Verification table (package.json → test scripts)
5. Behavioral Spot-Checks table (2-3 checks)
6. Requirements Coverage table
7. Human Verification Required assessment
8. Gaps Summary

**Verification Actions to Execute:**
1. Run: `cat package.json | grep '"test":'` — verify test script uses `'test/**/*.test.js'`
2. Run: `cat package.json | grep '"test:smoke":'` — verify smoke test script exists
3. Run: `cat package.json | grep '"test:integration":'` — verify integration test script exists
4. Run: `cat package.json | grep '"test:coverage":'` — verify coverage script exists
5. Run: `cat package.json | grep '"c8":'` — verify c8 dependency exists

**Content to Document:**
- Observable Truth: package.json test script catches nested files (pattern: `'test/**/*.test.js'`)
- Observable Truth: Three specialized test scripts available (test:smoke, test:integration, test:coverage)
- Observable Truth: c8 coverage tool installed as devDependency
- Required Artifact: package.json with updated test infrastructure
- Key Link: test script pattern → discovers nested test files
- Behavioral Spot-Check: package.json script verification via grep
- Requirement: C-02 satisfied (test script fixed, coverage tool added per SUMMARY)
- Human Verification: None required (all checks programmatic)

**File path:** .planning/phases/126-test-infrastructure-c-02/126-VERIFICATION.md
  </action>
  <verify>
    <automated>test -f .planning/phases/126-test-infrastructure-c-02/126-VERIFICATION.md && echo "PASS: Phase 126 VERIFICATION.md exists"</automated>
  </verify>
  <done>126-VERIFICATION.md created with complete gsd-verifier format documentation</done>
</task>

</tasks>

<verification>
**Phase-level verification:**

1. Both VERIFICATION.md files exist at expected paths
2. Phase 125 file uses 127-VERIFICATION.md documentary pattern (per D-07)
3. Phase 126 file uses 132-VERIFICATION.md code verification pattern (per D-08)
4. Both files contain gsd-verifier YAML frontmatter (phase, verified, status, score)
5. Both files contain Observable Truths table with verified status
6. Both files contain Required Artifacts table
7. Both files contain Behavioral Spot-Checks table
8. Both files contain Requirements Coverage table
9. Both files contain Gaps Summary showing "No gaps found"
10. No modification to existing artifacts (PLAN.md, SUMMARY.md remain unchanged per D-15)
</verification>

<success_criteria>
- 125-VERIFICATION.md created with gsd-verifier format
- 126-VERIFICATION.md created with gsd-verifier format
- Phase 125 verification documents C-01 satisfaction (command reference fixes)
- Phase 126 verification documents C-02 satisfaction (test infrastructure fixes)
- Existing artifacts (PLAN.md, SUMMARY.md) remain untouched
- Both files follow template patterns from Phases 127 and 132
</success_criteria>

<output>
After completion, create `.planning/phases/133-add-missing-verification/133-01-SUMMARY.md`
</output>