---
phase: 128
wave: 1
depends_on: []
requirements_addressed: [H-01]
files_modified:
  - bin/lib/recon-aggregator.js
  - bin/lib/osint-aggregator.js
  - bin/lib/ct-scanner.js
  - bin/lib/secret-detector.js
  - bin/lib/asset-discoverer.js
  - bin/lib/service-discovery.js
  - bin/lib/subdomain-osint.js
  - bin/lib/progress-tracker.js
  - bin/lib/skill-integrator.js
  - bin/lib/skill-executor.js
  - bin/commands/osint-report.js
  - bin/lib/report-filler.js
  - bin/lib/parallel-dispatch.js
  - bin/lib/logic-sync.js
  - bin/lib/recon-scanner.js
  - bin/lib/evasion-engine.js
autonomous: true
---

# Phase 128: Code Quality - Catch Blocks (H-01)

<objective>
Fix bare catch blocks by adding log.warn for consistency across the codebase.
</objective>

<tasks>

<task id="1">
<read_first>
- bin/lib/utils.js (log object)
- bin/lib/installers/claude.js (example of log.warn in catch)
</read_first>
<acceptance_criteria>
- [ ] All catch blocks in source files have log.warn or debug logging
- [ ] log is imported from utils where needed
- [ ] No bare catch blocks remain in bin/ source files
</acceptance_criteria>
<action>
1. First, scan all catch blocks in source files to identify bare ones:
   - Read each file listed in files_modified
   - Check each catch block for log.warn, log.error, or console.error inside
   - Identify truly bare catch blocks (no logging)

2. Add `log.warn` to bare catch blocks:
   - If file doesn't already import log from utils, add the import
   - Add `log.warn(\`Operation failed: \${err.message}\`)` inside each bare catch block
   - Use the caught error variable name in the message

3. Files to check and fix:
   - bin/lib/recon-aggregator.js (5 catch blocks)
   - bin/lib/osint-aggregator.js (6 catch blocks)
   - bin/lib/ct-scanner.js (7 catch blocks)
   - bin/lib/secret-detector.js (4 catch blocks)
   - bin/lib/asset-discoverer.js (2 catch blocks)
   - bin/lib/service-discovery.js (1 catch block)
   - bin/lib/subdomain-osint.js (1 catch block)
   - bin/lib/progress-tracker.js (3 catch blocks)
   - bin/lib/skill-integrator.js (1 catch block)
   - bin/lib/skill-executor.js (1 catch block)
   - bin/commands/osint-report.js (1 catch block)
   - bin/lib/report-filler.js (2 catch blocks)
   - bin/lib/parallel-dispatch.js (1 catch block)
   - bin/lib/logic-sync.js (3 catch blocks)
   - bin/lib/recon-scanner.js (1 catch block)
   - bin/lib/evasion-engine.js (2 catch blocks)

4. Common pattern to add at top of files that need log:
   `const { log } = require('../lib/utils');`

5. For each bare catch, add a log.warn call like:
   `catch (err) { log.warn(\`operationName failed: \${err.message}\`); }`
</action>
</task>

</tasks>

<verification>
## Verification

1. Run `grep -r "} catch" bin/ --include="*.js" | grep -v test` to list all catch blocks
2. For each, verify there's a log.warn, log.error, or console.error inside
3. Run tests to ensure no regressions: `npm run test`
</verification>

<must_haves>
- [ ] All bare catch blocks in bin/ source files have log.warn
- [ ] All files that needed log import have it added
- [ ] Tests pass after changes
</must_haves>
