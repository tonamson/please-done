---
phase: 125
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - C-01

must_haves:
  truths:
    - "All /pd: command references use correct command names"
    - "Library file paths in CLAUDE.md are accurate"
    - "Command documentation is consistent"
  artifacts:
    - path: CLAUDE.md
      provides: Command reference documentation
      contains: "/gsd:"
  key_links:
    - from: CLAUDE.md
      to: skill commands
      via: /gsd: prefix
      pattern: "/gsd:[a-z-]+"
---

<objective>
Fix 5 broken command references in CLAUDE.md

Purpose: Ensure CLAUDE.md command references are accurate and functional
Output: CLAUDE.md with corrected command references
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pd:map-codebase reference</name>
  <files>CLAUDE.md</files>
  <action>
Replace all instances of `pd:map-codebase` with `pd:scan` in CLAUDE.md:
- Line 106: "Run `/pd:map-codebase` to refresh" → "Run `/pd:scan` to refresh"
- Line 250: "`/pd:map-codebase`" → "`/pd:scan`"
- Line 251: "Auto-triggered by `/pd:init` when codebase not yet mapped" → "Auto-triggered by `/pd:scan` when codebase not yet mapped"

Also update the section title "Command Reference: pd:map-codebase" (line 245) to "Command Reference: pd:scan"
</action>
  <verify>
    <automated>grep -c "pd:scan" CLAUDE.md && grep -c "pd:map-codebase" CLAUDE.md | while read c; do [ "$c" -eq 0 ] && echo "PASS: No pd:map-codebase remaining"; done</automated>
  </verify>
  <done>pd:map-codebase replaced with pd:scan throughout CLAUDE.md</done>
</task>

<task type="auto">
  <name>Task 2: Fix pd:verify reference</name>
  <files>CLAUDE.md</files>
  <action>
Replace all instances of `pd:verify` with `pd:test` in CLAUDE.md:
- Line 170: "Run `/pd:verify` first" → "Run `/pd:test` first"
</action>
  <verify>
    <automated>grep -c "pd:verify" CLAUDE.md | while read c; do [ "$c" -eq 0 ] && echo "PASS: No pd:verify remaining"; done</automated>
  </verify>
  <done>pd:verify replaced with pd:test in CLAUDE.md</done>
</task>

<task type="auto">
  <name>Task 3: Fix library path references in pd:onboard</name>
  <files>CLAUDE.md</files>
  <action>
Find and fix any incorrect paths to `lib/key-file-selector.js` and `lib/doc-link-mapper.js` in the pd:onboard section (around lines 215-240).

The correct path is `lib/key-file-selector.js` and `lib/doc-link-mapper.js` (not `bin/lib/` or any other prefix).

If the paths are correct but the context is wrong, update the description to be accurate.
</action>
  <verify>
    <automated>grep -E "(bin/lib|lib/)" CLAUDE.md | grep -E "(key-file-selector|doc-link-mapper)" && echo "FAIL: Found incorrect lib paths" || echo "PASS: Lib paths correct"</automated>
  </verify>
  <done>Library paths in pd:onboard section are accurate</done>
</task>

<task type="auto">
  <name>Task 4: Verify all /pd: references are valid commands</name>
  <files>CLAUDE.md</files>
  <action>
After fixing the above, verify all /pd: command references in CLAUDE.md point to valid commands.

Valid /pd: commands that should be used:
- /pd:scan (renamed from pd:map-codebase)
- /pd:test (renamed from pd:verify)
- /pd:status
- /pd:plan
- /pd:fix-bug
- /pd:onboard
- /pd:new-milestone
- /pd:what-next
- /pd:write-code
- /pd:complete-milestone
- /pd:new-project
- /pd:research
- /pd:help

Check that any /pd: references are consistent with the Command Reference table at the bottom of CLAUDE.md.
</action>
  <verify>
    <automated>grep -oP '/pd:[a-z-]+' CLAUDE.md | sort -u | while read cmd; do echo "Checking $cmd..."; done && echo "Verification complete"</automated>
  </verify>
  <done>All /pd: command references in CLAUDE.md are valid</done>
</task>

</tasks>

<verification>
- CLAUDE.md contains no references to pd:map-codebase (only pd:scan)
- CLAUDE.md contains no references to pd:verify (only pd:test)
- Library paths use correct `lib/` prefix
- All command references in Command Reference table match actual usage
</verification>

<success_criteria>
- 5 broken references from C-01 are fixed
- CLAUDE.md is internally consistent
- All command syntax follows /pd: prefix convention
</success_criteria>

<output>
After completion, create `.planning/phases/125-command-reference-fixes/125-01-SUMMARY.md`
</output>
